# SWIM 남은 개발 계획

## 현재 기준

공개 프로필, 다른 사용자의 음악 다이어리, 프로필 URL 진입, 팔로우·좋아요 동기화, Monthly Recap, Spotify 사용자 계정 연결, Recap 플레이리스트 내보내기는 구현되어 자동 테스트를 통과했다.

이번 계획에는 완료된 작업을 제외하고, 다크 모드와 전체 사용자 흐름의 최종 검증·배포만 남긴다.

```text
테마 기반
→ 전체 화면 다크 스타일
→ 테마 접근성·회귀 검증
→ 전체 QA·배포
```

## 작업 목록

### 작업 1: 라이트·다크·시스템 테마 기반을 만든다

**목적**

사용자가 별도 로그인 정보나 서버 저장 없이 원하는 화면 테마를 선택하고, 새로고침 후에도 같은 설정을 사용할 수 있게 한다.

**수직 슬라이스**

```text
사용자 진입 또는 테마 선택
→ localStorage 설정 확인·저장
→ 저장값이 없으면 운영체제 테마 확인
→ 문서 루트에 테마 속성 적용
→ CSS 색상 토큰 변경
→ 선택한 테마로 화면 반영
```

**UI 설계**

- 헤더의 공통 영역에 테마 선택 요소를 배치한다.
- `라이트`, `다크`, `시스템` 세 가지 설정을 제공한다.
- 현재 설정과 버튼의 용도를 아이콘에만 의존하지 않고 접근 가능한 이름으로 전달한다.
- 초기 로딩에서 밝은 화면이 잠깐 나타나는 현상을 최소화한다.
- 테마 변경에 과도한 전환 애니메이션을 사용하지 않는다.

**데이터 설계**

| 항목 | 내용 |
|---|---|
| 저장 위치 | 브라우저 `localStorage` |
| 저장 키 | `swim-theme` |
| 저장값 | `light`, `dark`, `system` |
| DB 변경 | 없음 |
| API 변경 | 없음 |

**API 설계**

| 메서드 | 경로 | 요청 | 응답 | 인증 |
|---|---|---|---|---|
| 없음 | 없음 | 브라우저 내부 설정 | 브라우저 내부 반영 | 불필요 |

**작업 범위**

- 테마 타입과 초기값 결정
- 테마 저장·조회 함수
- `document.documentElement`의 단일 테마 속성 관리
- `system` 선택 시 `prefers-color-scheme` 반영
- 운영체제 테마가 실행 중 변경될 때 화면 갱신
- 잘못된 저장값과 `localStorage` 접근 실패 처리
- 테마 초기화·변경·저장 동작 테스트

**완료 기준**

- 첫 방문에서는 운영체제 설정과 같은 테마가 표시된다.
- 사용자가 세 가지 설정 중 하나를 선택할 수 있다.
- 새로고침과 로그인·로그아웃 이후에도 선택값이 유지된다.
- `system` 설정에서는 운영체제 테마 변경이 화면에 반영된다.
- 잘못된 저장값이나 저장소 오류가 앱 실행을 중단시키지 않는다.

**선행 작업**

- 없음

### 작업 2: 전체 화면에 SWIM 다크 색상 토큰을 적용한다

**목적**

단순한 색상 반전이 아니라 밤에 음악 일기를 돌아보는 듯한 차분한 SWIM 다크 모드를 모든 화면에 일관되게 제공한다.

**수직 슬라이스**

```text
사용자가 다크 테마 선택
→ 문서 루트 테마 속성 변경
→ 전역 색상 토큰 교체
→ 인증·기록·검색·피드·프로필·Recap·Spotify 화면 반영
→ 입력·버튼·카드의 상태까지 같은 테마로 표시
```

**UI 설계**

- 완전한 검정보다 깊은 청회색 계열의 배경을 사용한다.
- 카드 표면은 배경보다 한 단계 밝게 구분한다.
- 포인트 컬러 `#3E7FA3`의 정체성은 유지하되 어두운 배경에서 명도와 대비를 조정한다.
- 앨범 이미지와 감정 기록이 시각적 중심으로 남도록 장식을 최소화한다.
- 성공, 오류, 비활성, 선택, 포커스 상태를 색상 하나에만 의존하지 않는다.
- 빛나는 네온 효과나 과도한 그림자를 사용하지 않는다.

**데이터 설계**

| 항목 | 내용 |
|---|---|
| 테이블 | 변경 없음 |
| 컬럼 | 변경 없음 |
| 관계 | 변경 없음 |
| 제약조건 | 변경 없음 |
| RLS | 변경 없음 |

**API 설계**

| 메서드 | 경로 | 요청 | 응답 | 인증 |
|---|---|---|---|---|
| 없음 | 없음 | 기존 API 계약 유지 | 기존 응답 유지 | 변경 없음 |

**작업 범위**

- 기존 `:root` 색상을 의미 기반 토큰으로 정리
- 다크 테마의 배경·표면·본문·보조문자·테두리·그림자·위험 색상 정의
- 인증, 헤더, 입력 폼, 검색 결과 적용
- 음악 카드, 팔로잉 피드, 좋아요 사용자 목록 적용
- 사용자 검색, 공개 프로필, 공개 음악 다이어리 적용
- Monthly Recap, Spotify 연결, 플레이리스트 내보내기 적용
- 브라우저 기본 입력 요소와 `color-scheme` 동기화
- 앨범 이미지와 외부 이미지에 색상 필터를 적용하지 않음
- 컴포넌트별 색상 하드코딩 최소화

**완료 기준**

- 모든 주요 화면을 라이트와 다크 테마에서 읽을 수 있다.
- 배경, 카드, 입력창의 경계가 구분되면서도 과도하게 대비되지 않는다.
- 본문과 주요 컨트롤이 WCAG AA 수준의 대비를 만족한다.
- 포커스, 오류, 선택, 비활성 상태를 구분할 수 있다.
- 앨범 이미지 색상이 테마 때문에 변형되지 않는다.
- 기존 레이아웃과 기능이 깨지지 않는다.
- 데스크톱과 모바일에서 가로 넘침이나 레이아웃 변화가 없다.

**선행 작업**

- 작업 1

### 작업 3: 테마 전환을 접근성·회귀 관점에서 검증한다

**목적**

다크 모드가 일부 화면에서만 적용되거나 새로고침 시 깜빡이지 않도록 하고, 기존 핵심 사용자 흐름이 모든 테마에서 동일하게 동작하는지 확인한다.

**수직 슬라이스**

```text
시스템 테마로 최초 진입
→ 라이트 테마 선택
→ 회원가입·로그인
→ 음악 검색·기록 저장
→ 사용자 검색·팔로우·피드
→ 공개 프로필·좋아요·Recap
→ Spotify 플레이리스트 내보내기
→ 다크 테마 선택·새로고침
→ 동일 상태와 기능 유지 확인
```

**UI 설계**

- 키보드만으로 테마를 선택할 수 있어야 한다.
- 스크린 리더가 현재 설정과 선택 동작을 이해할 수 있어야 한다.
- 로딩, 빈 상태, 실패, 비활성, 포커스 상태를 두 테마에서 확인한다.
- 시스템 설정 전환과 새로고침 직후의 화면 깜빡임을 확인한다.

**데이터 설계**

| 항목 | 내용 |
|---|---|
| 저장 데이터 | `swim-theme` 설정값 |
| 확인 항목 | 저장, 복원, 잘못된 값 복구 |
| DB 영향 | 없음 |
| 인증 영향 | 로그인 세션과 독립적으로 유지 |

**API 설계**

| 메서드 | 경로 | 요청 | 응답 | 인증 |
|---|---|---|---|---|
| 기존 API 전체 | 기존 경로 유지 | 기존 계약 유지 | 테마와 무관하게 동일 | 기존 정책 유지 |

**작업 범위**

- 라이트·다크·시스템 초기화 테스트
- 새로고침 전후 설정 유지 테스트
- 운영체제 테마 변경 감지 테스트
- 인증 전후 테마 일치 확인
- 키보드와 스크린 리더용 이름·상태 확인
- 주요 텍스트와 컨트롤의 대비 확인
- 로딩·빈 상태·오류·비활성·포커스 상태 확인
- 전체 프론트엔드 테스트, 타입 검사, 프로덕션 빌드
- 데스크톱·모바일 수동 시각 검증
- README에 테마 동작과 저장 위치 반영

**완료 기준**

- 세 가지 설정에 대한 자동 테스트가 통과한다.
- 키보드와 스크린 리더로 현재 테마를 확인하고 변경할 수 있다.
- 새로고침 직후 잘못된 테마가 먼저 표시되지 않는다.
- 기존 핵심 기능이 라이트와 다크 테마에서 동일하게 동작한다.
- 브라우저 콘솔에 테마 관련 오류가 없다.
- 자동화할 수 없는 시각·접근성 항목은 수동 검증 결과와 재현 방법이 문서에 남는다.

**선행 작업**

- 작업 2

### 작업 4: 전체 사용자 흐름을 실제 계정으로 검증하고 배포한다

**목적**

회원가입부터 공개 프로필, Recap 플레이리스트, 다크 모드까지 포트폴리오 시연이 가능한 상태로 고정한다.

**수직 슬라이스**

```text
회원가입·로그인
→ 음악 검색·선택·기록 저장
→ 사용자 검색·팔로우·피드
→ 공개 프로필·좋아요
→ Monthly Recap
→ Spotify 연결·플레이리스트 내보내기
→ 테마 선택·새로고침
→ 배포 환경에서 전체 흐름 재검증
```

**UI 설계**

- 주요 흐름의 로딩, 빈 상태, 실패 상태를 실제 배포 환경에서 확인한다.
- 모바일과 데스크톱에서 화면 잘림이나 동작 불가 요소가 없는지 확인한다.
- 시연 순서를 방해하는 불필요한 오류 메시지나 콘솔 오류를 제거한다.

**데이터 설계**

| 항목 | 내용 |
|---|---|
| 확인 테이블 | `profiles`, `music_records`, `follows`, 좋아요 및 Spotify 연결 관련 현재 테이블 |
| 확인 내용 | 인증 사용자 귀속, 공개 조회 범위, 중복 방지, RLS |
| 스키마 변경 | 검증에서 실제 결함이 확인될 때 별도 작업으로만 진행 |
| 비밀정보 | 브라우저·로그·Git 노출 금지 |

**API 설계**

| 메서드 | 경로 | 요청 | 응답 | 인증 |
|---|---|---|---|---|
| 기존 API 전체 | 기존 경로 유지 | 실제 SWIM·Spotify 계정으로 검증 | 정상·오류 계약 확인 | 기존 정책 유지 |

**작업 범위**

- 두 개 이상의 실제 SWIM 계정과 Spotify 테스트 계정 사용
- 전체 자동 테스트, 타입 검사, 프로덕션 빌드
- 인증, RLS, OAuth, 비밀정보 노출 검증
- CORS, 환경변수, HTTPS, OAuth Redirect URI 확인
- 배포 환경에서 전체 사용자 흐름 수동 검증
- README, API, Architecture, QA 문서를 실제 구현과 일치시킴
- main 통합 전 Git 변경 범위와 시연 시나리오 확인

**완료 기준**

- `npm run typecheck`, `npm test`, `npm run build`가 통과한다.
- 실제 두 사용자 사이의 검색, 팔로우, 피드, 좋아요 상태가 새로고침 후에도 유지된다.
- 자신의 월간 기록으로 Spotify 비공개 플레이리스트를 만들고 링크를 열 수 있다.
- 라이트·다크·시스템 설정이 배포 환경에서도 유지된다.
- 비로그인 요청과 권한 없는 요청이 차단된다.
- 배포 URL에서 핵심 사용자 흐름을 처음부터 끝까지 재현할 수 있다.
- 수동 검증 결과와 남은 제한사항이 QA 문서에 기록된다.

**선행 작업**

- 작업 3

## 권장 구현 순서

```text
작업 1 테마 기반
→ 작업 2 전체 다크 스타일
→ 작업 3 테마 접근성·회귀 검증
→ 작업 4 전체 QA·배포
```

테마 저장과 전환 규칙을 먼저 고정해야 화면별 색상 작업이 중복되지 않는다. 이후 테마 회귀 검증을 마친 뒤 최종 QA와 배포를 진행해 같은 항목을 반복 검증하는 일을 줄인다.

## 위험 요소와 확인할 질문

- Spotify OAuth와 플레이리스트 내보내기는 자동 테스트를 통과했지만, 실제 Spotify 계정과 배포 Redirect URI를 사용한 수동 검증은 최종 QA에서 필요하다.
- 다크 모드의 정확한 대비는 자동 검사뿐 아니라 실제 브라우저와 앨범 이미지 조합으로 확인해야 한다.
- 배포 플랫폼과 운영 환경변수 등록 상태는 작업 4를 시작하기 전에 확인해야 한다.

## 제외

- 댓글, DM, 알림, 실시간 채팅
- AI 감정 분석
- 공개·협업 Spotify 플레이리스트
- 과도한 애니메이션
