# SWIM 남은 개발 계획

## 현재 기준과 목표

회원가입·로그인·세션 유지, 로그인 사용자 음악 기록, 다른 사용자 목록과 현재 팔로우 상태 조회까지 처리된 것으로 보고 완료된 작업은 이 계획에서 제외한다.

남은 목표는 사용자가 중복되지 않는 닉네임으로 다른 사람을 검색하고 팔로우 관계를 만들거나 해제한 뒤, 음악 기록에 남긴 좋아요 상태까지 새로고침 후 유지되는 흐름을 완성하는 것이다.

권장 구현 순서는 다음과 같다.

```text
닉네임 고유성 보장
→ 닉네임으로 사용자 검색
→ 검색 결과에서 팔로우·언팔로우
→ 좋아요 조회·저장
→ 통합 QA와 문서 정합성 갱신
```

`AGENTS.md`에서는 좋아요가 MVP 제외 기능이지만 기존 주간 목표에서 사용자가 명시적으로 포함했으므로 이 계획에 한해 범위 예외로 유지한다. 댓글, 알림, 좋아요 수 경쟁 UI 등은 추가하지 않는다.

---

## 작업 목록

### 작업 1: 닉네임을 중복되지 않는 공개 식별자로 보장한다

**목적**

사용자가 닉네임으로 정확한 팔로우 대상을 찾을 수 있도록 공백과 영문 대소문자 차이를 이용한 중복까지 DB에서 방지한다.

**수직 흐름**

```text
회원가입 화면
→ 닉네임 입력 및 정규화
→ Supabase Auth 회원가입
→ profiles 닉네임 고유성 검증
→ 프로필 생성 또는 중복 오류
→ 가입 결과를 화면에 표시
```

**작업 범위**

- 실제 Supabase DB의 `profiles.nickname` 고유 제약과 기존 데이터 확인
- 앞뒤 공백을 제거한 닉네임 저장 원칙 유지
- 영문 대소문자를 구분하지 않는 닉네임 고유성 정책 적용
- 기존 닉네임을 소문자로 변경하지 않고 비교 기준만 정규화
- 기존 데이터에 정규화 기준 중복이 있으면 임의 변경하지 않고 목록과 해결 방법 보고
- 신규 DB 변경은 별도 안전한 마이그레이션 SQL로 작성
- 회원가입 중복 닉네임 오류를 사용자가 이해할 수 있는 메시지로 표시
- 동시 가입 요청에서도 DB 고유 제약으로 중복 방지

**완료 기준**

- 동일한 닉네임으로 두 개의 프로필을 만들 수 없다.
- `SWIM`이 존재할 때 `swim` 또는 앞뒤 공백만 다른 닉네임으로 가입할 수 없다.
- 닉네임은 사용자가 입력한 표시 형태를 유지한다.
- 중복 가입 실패가 이메일 중복이나 일반 서버 오류와 구분되어 표시된다.
- 기존 프로필은 확인 없이 삭제되거나 임의로 이름이 변경되지 않는다.

**사용자 검증**

1. 기존 닉네임과 동일한 값으로 가입을 시도한다.
2. 영문 대소문자와 앞뒤 공백만 변경해 다시 가입을 시도한다.
3. 서로 다른 닉네임은 정상적으로 프로필 한 건을 생성하는지 확인한다.

**선행 작업**

- 없음

### 작업 2: 다른 사용자를 닉네임으로 검색하고 현재 팔로우 상태를 확인한다

**목적**

사용자가 전체 목록을 직접 훑지 않고 닉네임으로 팔로우 대상을 찾을 수 있게 한다.

**수직 흐름**

```text
사용자 검색창에 닉네임 입력
→ React 검색 상태와 지연 요청
→ GET /api/users?q= 요청
→ Express 입력 검증
→ profiles와 follows 검색·조회
→ 사용자와 isFollowing 응답
→ 검색 결과 또는 빈·실패 상태 표시
```

**작업 범위**

- 기존 사용자 목록 영역에 닉네임 검색 입력 추가
- 기존 `GET /api/users`에 선택적 `q` 검색 조건 추가
- 닉네임 부분 일치 및 영문 대소문자 비구분 검색
- 검색 결과에서도 자기 자신 제외
- 결과에 기존 최소 프로필 정보와 `isFollowing` 유지
- 검색어 앞뒤 공백 제거 및 최소 길이 검증
- 입력 지연 처리와 이전 요청 취소로 불필요한 연속 요청 방지
- 초기 전체 목록, 검색 중 로딩, 결과 없음, 요청 실패와 재시도 상태 구분
- 검색어를 지우면 전체 사용자 목록으로 복귀
- 내부 UUID와 이메일은 검색 조건이나 응답에 노출하지 않음

**완료 기준**

- 닉네임 일부를 입력하면 일치하는 사용자만 표시된다.
- 영문 대소문자가 달라도 같은 사용자를 찾을 수 있다.
- 자기 자신은 검색 결과에 표시되지 않는다.
- 각 결과의 기존 팔로우 상태가 정확히 표시된다.
- 결과가 없을 때 검색 결과 없음 상태가 표시된다.
- 빠르게 입력해도 오래된 응답이 최신 결과를 덮어쓰지 않는다.
- 검색어를 지우거나 새로고침하면 일관된 목록 상태로 돌아간다.
- 비로그인 요청은 거부된다.

**사용자 검증**

두 개 이상의 계정을 만든 뒤 전체 닉네임, 일부 닉네임, 영문 대소문자가 다른 검색어, 존재하지 않는 검색어를 차례로 입력해 결과와 팔로우 상태를 확인한다.

**선행 작업**

- 작업 1

### 작업 3: 검색 결과에서 팔로우와 언팔로우가 저장되고 즉시 반영된다

**목적**

사용자가 검색한 사람과 관계를 만들거나 해제하고 그 상태를 새로고침 후에도 유지하게 한다.

**수직 흐름**

```text
검색 결과의 팔로우 버튼 클릭
→ POST 또는 DELETE 요청
→ Express 인증·대상 검증
→ follows 생성 또는 삭제
→ 변경된 관계 응답
→ 해당 사용자의 버튼 상태 갱신
```

**작업 범위**

- `POST /api/follows` 팔로우
- `DELETE /api/follows/:followingId` 언팔로우
- 사용자 목록과 검색 결과에 동일한 팔로우 버튼 사용
- 인증 사용자 ID만 `follower_id`로 사용
- 생성·삭제에 필요한 `follows` RLS 정책
- 요청 중 해당 사용자 버튼 비활성화
- DB 고유 제약으로 중복 팔로우 방지
- 자기 자신, 존재하지 않는 사용자, 비로그인 요청 차단
- 이미 존재하거나 이미 해제된 관계의 API 응답 규칙 통일
- 실패 시 기존 버튼 상태 유지 및 재시도 가능
- 팔로우 수 경쟁 UI와 알림은 제외

**완료 기준**

- 검색한 사용자를 팔로우하면 버튼이 언팔로우 상태로 바뀐다.
- 새로고침하고 다시 검색해도 팔로우 상태가 유지된다.
- 언팔로우 후 새로고침해도 해제 상태가 유지된다.
- 전체 목록과 검색 결과에서 동일한 관계 상태가 표시된다.
- 연속 클릭이나 중복 요청으로 중복 관계가 생성되지 않는다.
- 자기 자신과 존재하지 않는 사용자를 팔로우할 수 없다.
- 요청 실패 시 화면이 성공 상태로 잘못 변경되지 않는다.

**사용자 검증**

두 계정으로 닉네임 검색 → 팔로우 → 새로고침 후 재검색 → 언팔로우 → 새로고침 후 재검색을 수행하고 DB에 중복 행이 없는지 확인한다.

**선행 작업**

- 작업 2

### 작업 4: 음악 기록의 사용자별 좋아요 상태를 조회한다

**목적**

사용자가 음악 기록에 남긴 공감 상태를 서버 데이터로 확인하고 새로고침 후에도 복원할 기반을 만든다.

**수직 흐름**

```text
음악 기록 목록 진입
→ 인증된 기록 조회
→ Express에서 기록과 likes 조회
→ 사용자별 liked 상태 응답
→ 각 카드에 현재 상태 표시
```

**작업 범위**

- 사용자와 음악 기록을 연결하는 좋아요 관계 모델
- 사용자·기록 조합의 DB 중복 방지 제약
- 현재 로컬 `liked` 상태를 서버 응답 기반 상태로 교체
- 기록 조회 응답에 현재 사용자의 `liked` 상태 포함
- 인증 사용자만 자신의 좋아요 관계를 판별하도록 RLS 설계
- 기록 없음, 좋아요 없음, 조회 실패 처리
- 좋아요 수는 노출하지 않고 현재 사용자의 상태만 표시

**완료 기준**

- 좋아요가 없는 기록은 미선택 상태로 표시된다.
- 현재 사용자가 좋아요한 기록만 선택 상태로 표시된다.
- 다른 사용자의 좋아요가 내 상태로 표시되지 않는다.
- 기록이 없을 때 기존 빈 상태가 정상 표시된다.
- 새로고침 후 서버에 저장된 상태가 복원된다.

**사용자 검증**

두 계정이 같은 기록 목록을 열었을 때 사용자별 선택 상태가 서로 분리되는지 확인한다.

**선행 작업**

- 없음

### 작업 5: 좋아요와 좋아요 취소가 즉시 반영되고 중복 저장되지 않는다

**목적**

현재 임시 좋아요 UI를 실제 DB 관계 생성·해제 흐름으로 완성한다.

**수직 흐름**

```text
좋아요 버튼 클릭
→ POST 또는 DELETE 요청
→ Express 인증·기록 존재 검증
→ likes 생성 또는 삭제
→ 변경된 상태 응답
→ 카드 버튼 상태 갱신
```

**작업 범위**

- `POST /api/music-records/:recordId/likes`
- `DELETE /api/music-records/:recordId/likes`
- 기존 `MusicCard` 버튼을 실제 API에 연결
- 인증 사용자 ID만 좋아요 주체로 사용
- 요청 중 중복 클릭 방지
- 없는 기록, 비로그인 요청과 서버 실패 처리
- 실패 시 이전 화면 상태 유지 또는 복원
- 댓글, 알림, 좋아요 수와 좋아요 사용자 목록은 제외

**완료 기준**

- 좋아요 후 선택 상태가 표시되고 DB에는 관계가 한 건만 존재한다.
- 새로고침 후에도 선택 상태가 유지된다.
- 좋아요 취소 후 DB 관계와 화면 상태가 모두 해제된다.
- 빠르게 반복 클릭해도 중복 행이 생기지 않는다.
- 없는 기록과 비로그인 요청은 적절한 오류로 실패한다.
- 서버 실패 시 성공한 것처럼 보이지 않는다.

**사용자 검증**

좋아요 → 새로고침 → 취소 → 새로고침을 수행하고 반복 클릭 뒤 DB 중복 여부를 확인한다.

**선행 작업**

- 작업 4

### 작업 6: 검색·팔로우·좋아요를 통합 검증하고 실제 상태에 맞게 문서화한다

**목적**

남은 기능이 하나의 사용자 흐름으로 연결되고 기존 인증·Spotify 검색·음악 기록 기능을 깨뜨리지 않았는지 확인한다.

**작업 범위**

- 회원가입 → 로그인 → 기록 작성 → 닉네임 검색 → 팔로우 → 좋아요 → 새로고침 → 로그아웃 전체 흐름
- 닉네임 중복, 빈 검색어, 검색 결과 없음, API 실패와 중복 요청 검증
- 자기 자신 팔로우, 다른 사용자 ID로 쓰기 시도와 RLS 검증
- 기존 Spotify 검색·기록 생성·조회 회귀 테스트
- 타입 검사, 빌드, 프런트엔드·서버 테스트
- 실제 API, 환경변수와 프로젝트 구조에 맞게 README 수정
- 필요한 경우 API·ERD·아키텍처 최소 문서 추가
- 포트폴리오 시연용 두 계정 시나리오 정리

**완료 기준**

- 두 계정으로 전체 사용자 흐름을 처음부터 끝까지 재현할 수 있다.
- 닉네임 검색 결과에서 팔로우하고 새로고침 후 같은 상태를 확인할 수 있다.
- 새로고침 후 세션·팔로우·좋아요 상태가 모두 유지된다.
- 사용자 A가 사용자 B의 소유 데이터로 쓰기 작업을 할 수 없다.
- 전체 테스트, 타입 검사와 빌드 결과가 기록된다.
- 실행하지 못한 검증은 통과로 처리하지 않고 이유와 수동 검증법을 남긴다.
- README와 실제 기능·API·환경변수가 일치한다.

**사용자 검증**

일반 창과 시크릿 창에서 서로 다른 계정으로 전체 시나리오를 수행한다.

**선행 작업**

- 작업 3
- 작업 5

---

## 구현 순서와 의존성

```text
작업 1 닉네임 고유성
  ↓
작업 2 닉네임 검색
  ↓
작업 3 팔로우·언팔로우 ─────┐
                             ├─→ 작업 6 통합 QA·문서
작업 4 좋아요 상태 조회      │
  ↓                          │
작업 5 좋아요·취소 ──────────┘
```

닉네임 검색과 팔로우 흐름을 우선 완성한다. 작업 4와 작업 5는 팔로우 흐름이 안정된 뒤 순서대로 진행하며, 통합 QA 전에 새 기능을 추가하지 않는다.

| 순서 | 작업 | 이유 |
|---|---|---|
| 1 | 닉네임 고유성 | 검색 결과의 사용자를 명확하게 식별하기 위한 선행 조건 |
| 2 | 닉네임 검색 | 팔로우 대상을 발견하는 사용자 흐름 |
| 3 | 팔로우·언팔로우 | 검색 결과를 실제 관계로 연결 |
| 4 | 좋아요 상태 조회 | 임시 로컬 상태를 서버 상태로 전환 |
| 5 | 좋아요·취소 | 실제 관계 저장과 해제 완성 |
| 6 | 통합 QA·문서 | 전체 흐름과 보안·회귀 검증 |

---

## 위험 요소와 확인 사항

- 저장소의 `profiles.sql`에는 `nickname text not null unique`가 선언되어 있지만 실제 Supabase DB에 적용됐는지 확인해야 한다.
- PostgreSQL의 일반 `unique`는 영문 대소문자를 구분한다. 검색과 중복 정책이 어긋나지 않도록 동일한 정규화 기준을 사용해야 한다.
- 기존 DB에 대소문자 또는 앞뒤 공백만 다른 닉네임이 존재하면 고유 제약을 바로 추가할 수 없다. 해당 사용자의 닉네임을 자동 변경하거나 삭제하지 않고 먼저 보고해야 한다.
- 닉네임 검색은 내부 UUID나 이메일 검색으로 확장하지 않는다.
- 검색 입력마다 즉시 요청하면 불필요한 호출과 응답 순서 문제가 생길 수 있으므로 입력 지연과 요청 취소가 필요하다.
- 팔로우와 좋아요는 UI 비활성화뿐 아니라 DB 고유 제약으로도 중복을 방지해야 한다.
- `follows`의 현재 RLS는 조회만 허용하므로 작업 3에서 생성·삭제 정책을 인증 사용자 기준으로 추가해야 한다.
- 좋아요는 현재 MVP 원칙상 예외 범위다. 좋아요 수, 사용자 목록, 알림이나 댓글로 확장하지 않는다.
- 현재 작업 4 사용자 목록 관련 변경은 아직 Git 작업 트리에 남아 있으므로 다음 구현 전에 검증과 커밋 상태를 확인해야 한다.

---

## 최종 자체 검토

### 크기

- 닉네임 DB 정책과 검색 UI·API를 서로 다른 작업으로 분리했다.
- 검색과 팔로우 관계 변경을 분리해 각 작업을 독립적으로 검증할 수 있다.
- 좋아요는 상태 조회와 관계 변경으로 분리했다.
- 마지막 작업은 새 기능 구현이 아니라 통합 검증과 문서 정합성만 담당한다.

### 완료 기준

- 각 작업은 화면에서 확인할 수 있는 결과와 DB·API 검증 방법을 함께 가진다.
- 단순한 코드 작성이나 테이블 생성만 완료 기준으로 사용하지 않는다.
- 검색, 팔로우와 좋아요 모두 새로고침 후 서버 상태 유지 여부를 확인한다.

### 누락 방지

- 닉네임 중복과 정규화
- 검색 결과 없음과 이전 요청 취소
- 비로그인 사용자
- API 요청 실패
- 중복 요청과 중복 관계
- 자기 자신 팔로우
- 다른 사용자 데이터 접근 제한
- 기존 인증·Spotify·음악 기록 회귀
