# SWIM 남은 개발 계획

## 현재 기준

핵심 MVP와 공개 프로필, 좋아요, Monthly Recap, Spotify 계정 연결·플레이리스트 내보내기, 라이트·다크·시스템 테마는 구현되어 있다.

2026-07-30 기준 자동 검증 결과는 다음과 같다.

- 타입 검사 통과
- 프론트엔드 테스트 143개 통과
- 서버 테스트 98개 통과
- 프로덕션 빌드 통과

완료된 구현과 자동 검증 계획은 제거했다. 아직 완료 증거가 없는 수동 시각·접근성 검증과 실제 계정·배포 검증만 남긴다.

```text
전체 화면 수동 UI·접근성 검증
→ 실제 계정 통합 검증
→ 배포 환경 최종 검증
```

## 작업 목록

### 작업 1: 전체 화면을 수동으로 시각·접근성 검증한다

**목적**

자동 테스트로 판단할 수 없는 테마의 실제 가독성, 키보드 조작, 화면 배치와 SWIM의 차분한 음악 다이어리 분위기를 브라우저에서 확인한다.

**수직 슬라이스**

```text
라이트·다크·시스템 테마 선택
→ 인증 화면 확인
→ 음악 기록·검색·피드 확인
→ 사용자 검색·공개 프로필 확인
→ Recap·Spotify 화면 확인
→ 키보드·반응형·Console 확인
→ QA 문서에 결과 기록
```

**UI 설계**

- 앨범 이미지가 각 화면의 시각적 중심으로 유지되는지 확인한다.
- 다크 모드가 네온이나 강한 그림자 없이 차분한 음악 일기 분위기를 유지하는지 확인한다.
- 배경, 카드, 입력창의 경계가 과도하지 않으면서 구분되는지 확인한다.
- 본문, 보조문자, 버튼, 오류, 비활성, 포커스 상태를 두 테마에서 읽을 수 있어야 한다.
- 키보드만으로 테마 선택과 주요 사용자 동작을 수행할 수 있어야 한다.
- 모바일과 데스크톱에서 가로 넘침, 요소 중첩, 잘림이 없어야 한다.

**데이터 설계**

| 항목 | 내용 |
|---|---|
| 저장 데이터 | 기존 `swim-theme` 설정값 |
| 확인 항목 | 선택, 새로고침 후 복원, 시스템 테마 변경 |
| DB 변경 | 없음 |
| RLS 변경 | 없음 |

**API 설계**

| 메서드 | 경로 | 요청 | 응답 | 인증 |
|---|---|---|---|---|
| 기존 API 전체 | 기존 경로 유지 | 기존 계약 유지 | 테마와 무관하게 동일 | 기존 정책 유지 |

**작업 범위**

- 인증 화면의 정상·오류·포커스·비활성 상태
- 홈과 음악 기록의 정상·로딩·빈 상태·오류 상태
- 사용자 검색과 피드의 검색 결과·빈 상태·오류·팔로우 처리 중 상태
- 공개 프로필의 정상·로딩·빈 다이어리·오류 상태
- Monthly Recap의 정상·로딩·빈 달·오류·재시도 상태
- Spotify 연결·내보내기의 미연결·연결·처리 중·오류·완료 상태
- 라이트·다크·시스템 설정과 새로고침 직후 깜빡임
- 키보드 `Tab`, `Enter`, `Space` 조작과 포커스 표시
- 데스크톱·모바일 화면의 가로 넘침과 요소 중첩
- 브라우저 Console 오류
- 앨범·아바타 이미지의 원본 색상 유지
- `docs/qa-report.md`의 `BLOCKED` 항목을 실제 결과로 갱신

**완료 기준**

- QA 문서의 테마 수동 검증 표에 모든 화면의 실제 결과가 기록된다.
- 라이트·다크·시스템 테마를 키보드로 선택할 수 있다.
- 새로고침 직후 잘못된 테마가 먼저 나타나는 현상이 없다.
- 주요 텍스트와 컨트롤이 WCAG AA 수준의 대비를 만족한다.
- 앨범 이미지와 감정 기록이 테마에 관계없이 중심으로 유지된다.
- 데스크톱과 모바일에서 가로 넘침이나 요소 중첩이 없다.
- 브라우저 Console에 테마 또는 렌더링 관련 오류가 없다.
- 발견된 결함은 이 계획에서 임의로 구현하지 않고 별도 수정 작업으로 기록한다.

**선행 작업**

- 없음

### 작업 2: 실제 계정으로 전체 흐름을 검증하고 배포한다

**목적**

회원가입부터 공개 프로필, Recap 플레이리스트와 테마 유지까지 실제 서비스처럼 동작하는지 확인하고 포트폴리오 시연이 가능한 배포 상태로 고정한다.

**수직 슬라이스**

```text
두 SWIM 계정 준비
→ 회원가입·로그인·음악 기록
→ 사용자 검색·팔로우·피드·좋아요
→ 공개 프로필·Monthly Recap
→ 실제 Spotify 계정 연결
→ 비공개 플레이리스트 내보내기
→ 테마 선택·새로고침
→ 배포 환경에서 전체 흐름 재검증
```

**UI 설계**

- 전체 흐름의 로딩, 빈 상태, 실패 상태가 사용자에게 이해 가능한지 확인한다.
- Spotify 연결 취소·실패·완료 후 사용자가 다음 행동을 알 수 있어야 한다.
- 시연 중 불필요한 Console 오류나 내부 정보가 노출되지 않아야 한다.
- 모바일과 데스크톱에서 핵심 동작이 모두 가능해야 한다.

**데이터 설계**

| 항목 | 내용 |
|---|---|
| 확인 테이블 | `profiles`, `music_records`, `follows`, 좋아요·Spotify 연결 관련 현재 테이블 |
| 확인 내용 | 인증 사용자 귀속, 공개 조회 범위, 중복 방지, RLS |
| 스키마 변경 | 실제 결함이 확인될 때 별도 작업으로만 진행 |
| 비밀정보 | 브라우저, 로그, Git에 노출 금지 |

**API 설계**

| 메서드 | 경로 | 요청 | 응답 | 인증 |
|---|---|---|---|---|
| 기존 API 전체 | 기존 경로 유지 | 실제 SWIM·Spotify 계정 | 정상·오류 계약 확인 | 기존 정책 유지 |

**작업 범위**

- 두 개 이상의 실제 SWIM 계정과 Spotify 테스트 계정 준비
- 회원가입·로그인·로그아웃·세션 유지
- 음악 검색·선택·기록 저장·내 기록 조회
- 닉네임 검색·팔로우·언팔로우·피드 반영
- 좋아요 사용자와 인원 확인
- 공개 프로필 URL 직접 접근과 뒤로 가기
- Monthly Recap 집계 확인
- Spotify OAuth 연결·취소·재연결·해제
- 월간 기록의 비공개 Spotify 플레이리스트 생성과 링크 확인
- 중복 내보내기 시 기존 플레이리스트 재사용 확인
- 라이트·다크·시스템 설정의 새로고침·로그인 전후 유지
- 인증, RLS, OAuth, 비밀정보 노출 검증
- CORS, 환경변수, HTTPS, OAuth Redirect URI 확인
- 배포 환경에서 전체 사용자 흐름 재실행
- README, Architecture, QA 문서를 실제 배포 상태와 일치시킴
- 최종 타입 검사, 전체 테스트, 프로덕션 빌드

**완료 기준**

- `npm run typecheck`, `npm test`, `npm run build`가 최종 변경 상태에서도 통과한다.
- 실제 두 사용자 사이의 검색, 팔로우, 피드, 좋아요 상태가 새로고침 후에도 유지된다.
- 실제 Spotify 계정을 연결하고 자신의 월간 기록으로 비공개 플레이리스트를 만들 수 있다.
- 생성된 Spotify 링크가 열리고 중복 내보내기가 새 플레이리스트를 만들지 않는다.
- 비로그인 요청과 권한 없는 요청이 차단된다.
- 비밀정보가 브라우저 응답, Console, 서버 로그와 Git에 노출되지 않는다.
- 라이트·다크·시스템 설정이 배포 환경에서도 유지된다.
- 배포 URL에서 핵심 사용자 흐름을 처음부터 끝까지 재현할 수 있다.
- 수동 검증 결과와 남은 제한사항이 `docs/qa-report.md`에 기록된다.

**선행 작업**

- 작업 1

## 권장 구현 순서

```text
작업 1 전체 화면 수동 UI·접근성 검증
→ 작업 2 실제 계정 통합 검증·배포
```

로컬 화면에서 시각·접근성 결함을 먼저 확인해야 배포 후 같은 검증을 반복하는 일을 줄일 수 있다. 작업 1에서 결함이 발견되면 해당 수정은 사용자 승인을 받은 별도 구현 작업으로 분리하고, 수정과 회귀 검증을 마친 뒤 작업 2로 이동한다.

## 위험 요소와 확인할 질문

- 현재 자동 검증은 통과했지만 실제 브라우저 수동 시각 검증 결과는 아직 `BLOCKED` 상태다.
- Spotify OAuth와 플레이리스트 내보내기는 실제 Spotify 계정과 운영 Redirect URI로 확인해야 완료할 수 있다.
- 배포 플랫폼과 운영 환경변수 등록 상태를 작업 2 시작 전에 확인해야 한다.
- 데이터베이스의 실제 RLS 정책은 두 계정으로 권한 경계를 재현해야 한다.

## 제외

- 댓글, DM, 알림, 실시간 채팅
- AI 감정 분석
- 공개·협업 Spotify 플레이리스트
- 과도한 애니메이션
