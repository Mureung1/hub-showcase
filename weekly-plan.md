# SWIM 남은 완성 계획

## 현재 기준

다음 기능은 현재 코드에 구현되어 있고 자동 검증을 통과했으므로 새 작업 목록에서 제외한다.

- 이메일 회원가입·로그인·로그아웃·세션 유지
- Supabase Auth와 공개 프로필 연결
- 닉네임 정규화 고유성 코드와 마이그레이션 SQL
- Spotify 음악 검색·선택·외부 링크
- 인증 사용자 음악 기록 저장·조회
- 닉네임 사용자 검색
- 팔로우·언팔로우와 관계 상태 유지
- 팔로잉 사용자의 음악 기록 피드
- 사용자별 좋아요 상태 조회
- 좋아요·좋아요 취소와 중복 저장 방지
- 로딩·빈 상태·실패·재시도 처리

2026-07-27 자동 검증 결과는 다음과 같다.

- 프런트엔드 테스트 88개 통과
- 서버 테스트 50개 통과
- TypeScript 타입 검사 통과
- 프로덕션 빌드 통과

자동 검증과 실제 Supabase 적용은 별개다. 원격 DB의 SQL·RLS 적용과 두 실제 계정 검증은 마지막 작업에서 확인한다.

---

## 완성 목표

사용자가 음악 기록에 공감한 사람의 수를 부담 없는 문장으로 확인하고, 그 문장을 눌러 함께 기억한 사람의 닉네임을 볼 수 있게 한다.

```text
음악 기록 카드
→ “3명이 기억했어요” 표시
→ 문장 선택
→ 인증된 상세 요청
→ likes와 profiles 조회
→ 닉네임·아바타 목록 표시
→ 좋아요 변경 시 수와 목록 동기화
```

좋아요를 순위나 경쟁 지표로 사용하지 않는다. 숫자는 작은 보조 정보로 표시하고, 사용자 목록은 “누가 반응했는가”보다 “누가 이 기억에 함께했는가”라는 의미로 제공한다.

---

## UI 설계 원칙

- 앨범 이미지, 음악, 감정 기록보다 좋아요 정보가 먼저 보이지 않게 한다.
- 좋아요가 한 건 이상일 때만 `N명이 기억했어요`를 작게 표시한다.
- 문장을 누르면 카드 아래에 가벼운 펼침 영역으로 닉네임 목록을 보여준다.
- 모달, 순위, 인기 배지, 실시간 알림은 추가하지 않는다.
- 목록에는 공개 닉네임과 아바타만 표시한다.
- 로딩, 목록 없음, 실패, 더 보기 상태를 카드 안에서 조용하게 처리한다.
- 키보드로 열고 닫을 수 있고 펼침 상태를 스크린 리더에 전달한다.

---

## 데이터 설계

| 항목 | 내용 |
|---|---|
| 기존 테이블 | `likes`, `profiles`, `music_records` |
| 신규 컬럼 | 없음 |
| 집계 데이터 | 음악 기록별 `likeCount` |
| 공개 사용자 정보 | `nickname`, `avatar_url` |
| 관계 | `likes.user_id → profiles.id`, `likes.record_id → music_records.id` |
| 제약조건 | 기존 `(user_id, record_id)` 기본 키로 중복 좋아요 방지 |
| 접근 범위 | 본인 기록 또는 현재 사용자가 팔로우해 읽을 수 있는 기록만 상세 조회 |
| RLS 원칙 | `likes` 전체 행을 클라이언트에 공개하지 않고, 검증된 서버·DB 조회를 통해 집계와 공개 프로필만 반환 |

현재 `likes` SELECT 정책은 자신의 좋아요 행만 허용하므로 단순 조회로는 전체 수를 계산할 수 없다. 모든 인증 사용자에게 `likes` 원본 행을 공개하는 정책으로 바꾸지 않는다. 인증 여부와 음악 기록 열람 권한을 확인하는 제한된 DB 함수 또는 동일 수준의 안전한 서버 조회를 사용한다.

---

## API 설계

| 메서드 | 경로 | 요청 | 응답 | 인증 |
|---|---|---|---|---|
| GET | `/api/music-records` | 없음 | 기존 기록 + `liked`, `likeCount` | 필요 |
| GET | `/api/feed` | 없음 | 기존 피드 + `liked`, `likeCount` | 필요 |
| GET | `/api/music-records/:recordId/likes` | 선택적 페이지 커서 | `likeCount`, 공개 사용자 목록, 다음 커서 | 필요 |
| POST | `/api/music-records/:recordId/likes` | 없음 | `liked: true`, 최신 `likeCount` | 필요 |
| DELETE | `/api/music-records/:recordId/likes` | 없음 | `liked: false`, 최신 `likeCount` | 필요 |

좋아요 사용자 응답은 다음 범위만 허용한다.

```json
{
  "data": {
    "recordId": 7,
    "likeCount": 3,
    "users": [
      {
        "nickname": "잔잔한파도",
        "avatarUrl": null
      }
    ],
    "nextCursor": null
  }
}
```

Auth UUID, 이메일, 좋아요 사용자의 내부 식별자와 access token은 응답하지 않는다.

---

## 남은 작업 목록

### 작업 1: 음악 기록에 함께 기억한 사람 수를 안전하게 집계한다

**목적**

현재 사용자의 `liked` 상태와 별개로, 음악 기록을 기억한 전체 인원수를 권한을 지키며 조회할 수 있게 한다.

**수직 흐름**

```text
내 기록 또는 팔로잉 피드 조회
→ GET /api/music-records 또는 GET /api/feed
→ Express 인증과 기록 열람 권한 확인
→ 기록별 likes 집계
→ liked와 likeCount 응답
→ 카드 데이터에 인원수 반영
```

**작업 범위**

- 기록별 전체 좋아요 수를 반환하는 제한된 DB 조회 설계
- 인증 사용자와 음악 기록 열람 권한 확인
- `likes` 원본 행 전체 공개 금지
- `GET /api/music-records` 응답에 `likeCount` 추가
- `GET /api/feed` 응답에 `likeCount` 추가
- 좋아요 생성·취소 응답에 최신 `likeCount` 추가
- 기록 없음, 좋아요 없음, 집계 실패 처리
- 현재 사용자의 `liked` 계산과 전체 `likeCount` 계산 분리
- Route → Controller → Service → DB 구조 유지
- DB 함수·Service·API 테스트

**완료 기준**

- 좋아요가 없으면 `likeCount`가 `0`이다.
- 서로 다른 세 사용자가 좋아요하면 모든 허용된 조회에서 `likeCount`가 `3`이다.
- 다른 사용자의 좋아요가 현재 사용자의 `liked` 상태로 섞이지 않는다.
- 중복 좋아요 요청으로 숫자가 증가하지 않는다.
- 좋아요 취소 후 숫자가 정확히 감소한다.
- 읽을 권한이 없는 기록의 집계를 조회할 수 없다.
- 일반 인증 사용자가 `likes` 원본 전체를 직접 조회할 수 없다.

**사용자 검증**

세 계정이 같은 기록에 좋아요를 누른 뒤 작성자와 팔로워 화면에서 동일한 숫자가 보이는지 확인한다. 한 계정이 좋아요를 취소하면 새로고침 후 숫자가 한 명 줄어야 한다.

**선행 작업**

- 없음

### 작업 2: “N명이 기억했어요”를 누르면 닉네임 목록이 펼쳐진다

**목적**

사용자가 숫자만 보는 데서 끝나지 않고, 음악 기록에 함께 공감한 사람을 공개 프로필 정보로 확인할 수 있게 한다.

**수직 흐름**

```text
MusicCard의 “N명이 기억했어요” 선택
→ GET /api/music-records/:recordId/likes
→ Express 인증과 기록 열람 권한 확인
→ likes와 profiles 조회
→ 닉네임·아바타·전체 수 응답
→ 카드 아래 사용자 목록 펼침
```

**작업 범위**

- `GET /api/music-records/:recordId/likes`
- 기록 존재 여부와 현재 사용자의 열람 권한 검증
- `likes`와 `profiles` 관계에서 공개 정보만 조회
- 기본 20명 단위 조회와 다음 커서
- Auth UUID와 이메일 응답 제외
- `MusicCard`에 작은 `N명이 기억했어요` 문장 추가
- 숫자가 0이면 문장과 펼침 버튼을 표시하지 않음
- 클릭 시 카드 아래 닉네임·아바타 목록 표시
- 다시 클릭하면 목록 닫기
- 로딩·빈 결과·실패·재시도·더 보기 상태
- 동시에 여러 카드 목록을 불필요하게 미리 요청하지 않음
- `aria-expanded`, 연결된 영역 ID와 키보드 조작
- 컴포넌트·Service·API 테스트

**완료 기준**

- 카드에서 `3명이 기억했어요`처럼 정확한 수가 표시된다.
- 문장을 누르기 전에는 사용자 목록 API를 호출하지 않는다.
- 문장을 누르면 좋아요한 사용자의 닉네임과 아바타만 표시된다.
- 이메일과 Auth UUID는 Network 응답에 포함되지 않는다.
- 20명을 초과하면 더 보기로 다음 사용자를 불러올 수 있다.
- 닫았다 다시 열어도 중복 사용자가 생기지 않는다.
- 요청 실패 시 카드와 좋아요 상태는 유지되고 재시도할 수 있다.
- 좋아요 또는 취소 직후 숫자와 열린 목록이 현재 상태로 갱신된다.

**사용자 검증**

계정 A·B·C가 같은 기록을 기억한 뒤 작성자 화면에서 `3명이 기억했어요`를 열어 세 닉네임을 확인한다. 한 계정이 취소하면 숫자와 목록에서 해당 사용자가 제거되어야 한다.

**선행 작업**

- 작업 1

### 작업 3: 실제 DB와 두 계정 이상으로 전체 흐름을 최종 검증한다

**목적**

좋아요 집계와 공개 사용자 목록이 기존 기록·검색·팔로우·피드 기능을 깨뜨리지 않고 실제 Supabase 환경에서 안전하게 동작하는지 확인한다.

**통합 흐름**

```text
회원가입·로그인
→ Spotify 음악 검색
→ 음악 기록 저장
→ 닉네임 검색·팔로우
→ 팔로잉 피드
→ 여러 계정 좋아요
→ “N명이 기억했어요” 확인
→ 닉네임 목록 확인
→ 좋아요 취소
→ 새로고침 상태 유지
```

**작업 범위**

- 실제 Supabase SQL과 RLS 적용 여부 확인
- 일반 창·시크릿 창을 사용한 다중 계정 검증
- 작성자·팔로워·권한 없는 사용자의 조회 범위 확인
- 집계 숫자와 실제 `likes` 행 수 비교
- 닉네임 목록의 공개 정보 범위 확인
- 새로고침 후 `liked`, `likeCount`, 사용자 목록 일치 확인
- Spotify 검색 장애 시 외부 API 실패로 구분해 기록
- 전체 테스트·타입 검사·프로덕션 빌드
- 브라우저 Console·Network와 Express 로그 확인
- README·API·DB·Architecture·QA 문서 갱신
- Git 브랜치와 원격 저장소 상태 정리

**완료 기준**

- 실제 계정으로 전체 사용자 흐름을 처음부터 끝까지 재현할 수 있다.
- DB의 좋아요 행 수, API의 `likeCount`, 화면 숫자가 일치한다.
- 닉네임 목록에 좋아요하지 않은 사용자가 섞이지 않는다.
- 권한 없는 사용자는 기록의 좋아요 상세를 조회할 수 없다.
- Auth UUID, 이메일, 비밀 키가 API 응답에 노출되지 않는다.
- 기존 팔로우·피드·좋아요 저장과 취소가 정상 동작한다.
- 전체 자동 검증이 통과한다.
- 실행하지 못한 항목은 통과로 처리하지 않고 이유와 수동 검증법을 남긴다.
- 실제 코드·DB·API와 문서가 일치한다.

**선행 작업**

- 작업 2

---

## 권장 구현 순서

```text
작업 1 좋아요 수 집계
→ 작업 2 함께 기억한 사용자 목록
→ 작업 3 실제 DB·다중 계정 통합 QA
```

| 순서 | 작업 | 이유 |
|---|---|---|
| 1 | 좋아요 수 집계 | 원본 좋아요 행을 공개하지 않고 숫자를 안전하게 제공하는 기반 |
| 2 | 닉네임 목록 | 집계 계약 위에 사용자가 확인할 수 있는 사람 중심 경험 연결 |
| 3 | 통합 QA·문서 | 실제 RLS, 공개 범위와 기존 기능 회귀 확인 |

각 작업은 하나의 수직 흐름과 하나의 커밋 단위로 진행한다. 작업 1이 검증되기 전에 사용자 목록 UI부터 만들지 않는다.

---

## 위험 요소와 확인 사항

- 현재 `likes` SELECT RLS는 자신의 좋아요만 허용하므로 전체 수와 사용자 목록을 그대로 조회할 수 없다.
- 모든 인증 사용자에게 `likes` SELECT를 허용하면 Auth UUID 관계가 과도하게 노출될 수 있다.
- `security definer` DB 함수를 사용한다면 `search_path` 고정, 인증 확인, 기록 열람 권한 확인과 실행 권한 제한이 필요하다.
- 피드에서 읽을 수 없는 기록의 좋아요 상세도 조회할 수 없어야 한다.
- 좋아요 생성 직후 로컬 숫자만 임의로 올리면 중복 요청이나 실패에서 실제 DB와 어긋날 수 있다. 서버가 반환한 최신 수를 사용한다.
- 사용자 목록은 한 번에 전부 가져오지 않고 페이지 단위로 조회한다.
- 숫자의 크기를 강조하거나 인기순 정렬·랭킹·알림으로 확장하지 않는다.
- 실제 Spotify `502` 장애는 SWIM DB 오류와 분리해서 기록한다.
- 원격 Supabase SQL 적용과 실제 계정 검증 전에는 최종 완료로 판단하지 않는다.

---

## 최종 범위

### 포함

- 음악 기록별 `likeCount`
- `N명이 기억했어요` 문장
- 좋아요 사용자 닉네임·아바타 목록
- 목록 페이지 조회와 더 보기
- 좋아요·취소 후 숫자와 목록 동기화
- 인증·기록 열람 권한·RLS 검증
- 통합 QA와 관련 문서 갱신

### 제외

- 인기순 정렬
- 좋아요 랭킹
- 좋아요 알림
- 좋아요 사용자 프로필 상세 화면
- 댓글
- DM
- 실시간 채팅
- Recap
- Spotify 플레이리스트 생성
- 복잡한 추천 알고리즘
- 과도한 애니메이션
