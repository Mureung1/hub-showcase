# SWIM 이번 주 개발 계획

## 이번 주 개발 로드맵

이번 주 목표는 회원가입을 포함한 로그인, 팔로우, 좋아요를 실제 사용자 흐름으로 완성하는 것이다.

현재 프로젝트는 Spotify 음악 검색, 음악 기록 생성, 전체 기록 조회가 구현되어 있다. 인증, 프로필, 팔로우 저장, 좋아요 저장은 구현되어 있지 않으며 음악 기록도 아직 사용자와 연결되지 않았다.

권장 구현 순서는 다음과 같다.

```text
인증 기반 구축
→ 기록을 로그인 사용자에게 귀속
→ 사용자 목록 조회
→ 팔로우·언팔로우
→ 좋아요·좋아요 취소
→ 통합 QA와 문서 정합성 갱신
```

인증을 먼저 구현해야 이후 데이터에서 누가 기록했고 누가 팔로우하거나 좋아요를 눌렀는지 안전하게 판별할 수 있다. 팔로우는 사용자 프로필 조회에 의존하고, 좋아요는 인증된 사용자와 음악 기록 양쪽에 의존한다.

`AGENTS.md`에서는 좋아요가 MVP 제외 기능이지만 이번 주 목표에서 사용자가 명시적으로 포함했으므로 이번 계획에 한해 범위 예외로 다룬다. 댓글, 알림, 좋아요 수 경쟁 UI 등은 추가하지 않는다.

---

## 작업 목록

### 작업 1: 회원가입 후 기본 프로필이 생성되고 로그인 화면으로 진입한다

**목적**

Supabase Auth 사용자와 SWIM 공개 사용자를 연결하는 인증 기반을 만든다.

**수직 흐름**

```text
회원가입 화면
→ 이메일·비밀번호·닉네임 입력
→ Supabase Auth 회원가입
→ profiles 생성
→ 가입 결과 반환
→ 로그인 또는 이메일 확인 안내
```

**작업 범위**

- 이메일, 비밀번호, 닉네임 회원가입 화면
- Supabase 브라우저 클라이언트 설정
- `profiles` 데이터 모델과 Auth 사용자 ID 관계
- 프로필 생성 실패 시 가입 완료 여부를 명확히 표시
- 빈 입력, 잘못된 이메일, 약한 비밀번호, 중복 이메일·닉네임 처리
- 제출 중 버튼 비활성화로 중복 요청 방지
- 회원가입 UI 및 프로필 생성 흐름 테스트
- `SUPABASE_URL`, 공개 anon key 환경변수 문서화
- 서비스 역할 키는 브라우저에 사용하지 않음

**완료 기준**

- 새 이메일과 닉네임으로 가입할 수 있다.
- Auth 사용자와 동일한 ID의 프로필이 한 건 생성된다.
- 빈 입력과 중복 가입은 사용자에게 원인을 표시한다.
- 빠르게 여러 번 눌러도 중복 프로필이 생기지 않는다.
- 이메일 확인 설정이 켜진 환경에서는 확인 안내가 표시된다.

**사용자 검증**

1. 새 계정으로 회원가입한다.
2. Supabase Auth와 `profiles`에서 사용자가 각각 한 건인지 확인한다.
3. 같은 이메일 또는 닉네임으로 다시 가입해 실패 상태를 확인한다.

**선행 작업**

- 없음

### 작업 2: 로그인·로그아웃과 새로고침 세션 유지가 동작한다

**목적**

인증한 사용자가 앱에 들어오고 브라우저 새로고침 후에도 로그인 상태를 유지하게 한다.

**수직 흐름**

```text
로그인 화면
→ 이메일·비밀번호 제출
→ Supabase Auth 인증
→ 세션 및 프로필 조회
→ 인증 결과 반환
→ 앱 화면 또는 로그인 오류 표시
```

**작업 범위**

- 로그인·로그아웃 UI
- 앱 시작 시 세션 복원
- 세션 확인 중 로딩 화면
- 인증 실패 메시지
- 로그아웃 후 인증 화면 복귀
- 인증이 필요한 화면의 접근 차단
- 새로고침·세션 만료 처리
- 연속 제출 방지

**완료 기준**

- 올바른 계정은 로그인되고 잘못된 비밀번호는 오류가 표시된다.
- 로그인 후 새로고침해도 동일 사용자로 유지된다.
- 세션 확인 전 보호 화면이 잠깐 노출되지 않는다.
- 로그아웃 후 보호된 앱 화면에 접근할 수 없다.
- 로그아웃 상태에서 직접 앱을 열면 로그인 화면이 표시된다.

**사용자 검증**

로그인 → 새로고침 → 프로필 확인 → 로그아웃 → 새로고침 순서로 상태를 확인한다.

**선행 작업**

- 작업 1

### 작업 3: 음악 기록을 로그인 사용자에게 귀속한다

**목적**

팔로우와 좋아요가 신뢰할 수 있도록 기존 익명 음악 기록을 사용자 데이터로 전환한다.

**수직 흐름**

```text
로그인 사용자가 기록 작성
→ 사용자 토큰을 포함한 API 요청
→ Express에서 인증 사용자 확인
→ music_records.user_id로 저장
→ 저장된 사용자 기록 응답
→ 화면 목록 갱신
```

**작업 범위**

- `music_records`와 Auth 사용자·프로필의 관계 설계
- 기존 기록의 처리 방침 확정 후 안전한 마이그레이션
- Express에서 Bearer 토큰 검증
- 인증된 사용자 ID로만 기록 생성
- 기록 응답에 작성자 식별에 필요한 최소 프로필 정보 포함
- 비로그인 생성 차단
- 현재 익명 생성 RLS 제거 및 인증 사용자 정책 적용
- 기존 Spotify 검색 흐름 회귀 확인

**완료 기준**

- 로그인 사용자의 새 기록에 정확한 `user_id`가 저장된다.
- 다른 사용자의 ID를 요청값에 넣어도 그 사용자로 저장할 수 없다.
- 비로그인 생성 요청은 401로 실패한다.
- 새로고침 후에도 기록과 작성자 관계가 유지된다.
- 기존 음악 검색과 기록 조회가 계속 동작한다.

**사용자 검증**

두 계정으로 각각 기록을 만든 뒤 DB와 화면에서 작성자가 올바른지 확인하고 토큰 없는 생성 요청이 거부되는지 확인한다.

**선행 작업**

- 작업 2

### 작업 4: 다른 사용자 목록과 현재 팔로우 상태를 조회한다

**목적**

사용자가 팔로우할 대상을 발견하고 현재 관계를 확인할 수 있게 한다.

**수직 흐름**

```text
사용자 목록 화면 진입
→ 인증 API 요청
→ Express에서 사용자 확인
→ profiles와 follows 조회
→ 사용자·팔로우 상태 응답
→ 목록 또는 빈·실패 상태 표시
```

**작업 범위**

- 최소 사용자 목록 영역
- 자기 자신을 목록에서 제외
- 닉네임과 필요한 최소 프로필 정보 표시
- 현재 사용자의 팔로우 여부 포함
- `follows` 관계 모델, 중복 방지 제약 및 자기 팔로우 방지
- 인증된 사용자만 관계 상태를 조회하도록 RLS 설계
- 로딩, 사용자가 없는 상태, 요청 실패 및 재시도
- 검색은 이번 주 필수 범위에서 제외하고 단순 목록 우선

**완료 기준**

- 다른 가입자가 있으면 목록에 표시된다.
- 자기 자신은 팔로우 대상으로 표시되지 않는다.
- 사용자가 없으면 명확한 빈 상태가 보인다.
- 새로고침 후에도 동일한 팔로우 상태가 표시된다.
- 비로그인 요청은 거부된다.

**사용자 검증**

두 계정을 만든 후 한 계정에서 다른 계정이 목록에 표시되는지 확인하고 혼자만 존재하는 환경에서는 빈 상태를 확인한다.

**선행 작업**

- 작업 2

### 작업 5: 팔로우와 언팔로우가 저장되고 즉시 화면에 반영된다

**목적**

사용자가 다른 사람과 관계를 만들거나 해제하고 그 상태를 지속시킨다.

**수직 흐름**

```text
팔로우 버튼 클릭
→ POST 또는 DELETE 요청
→ Express 인증·대상 검증
→ follows 생성 또는 삭제
→ 변경된 관계 응답
→ 버튼 상태 갱신
```

**작업 범위**

- `POST /api/follows` 팔로우
- `DELETE /api/follows/:followingId` 언팔로우
- 사용자 목록의 팔로우 버튼
- 요청 중 해당 버튼 비활성화
- 중복 팔로우를 성공적인 현재 상태로 처리하거나 명확한 409 응답으로 통일
- 자기 자신 팔로우, 없는 사용자, 비로그인 요청 차단
- 실패 시 기존 버튼 상태 유지 및 재시도 가능
- 팔로우 수 경쟁 UI와 알림은 제외

**완료 기준**

- 팔로우 후 버튼이 언팔로우 상태로 바뀐다.
- 새로고침 후에도 관계가 유지된다.
- 언팔로우 후 새로고침해도 해제 상태다.
- 연속 클릭으로 중복 관계가 생성되지 않는다.
- 자기 자신과 존재하지 않는 사용자를 팔로우할 수 없다.
- 요청 실패 시 화면이 성공 상태로 잘못 변경되지 않는다.

**사용자 검증**

두 계정으로 팔로우 → 새로고침 → 언팔로우 → 새로고침을 수행하고 DB에 중복 행이 없는지 확인한다.

**선행 작업**

- 작업 4

### 작업 6: 음악 기록의 좋아요 상태를 조회하고 저장한다

**목적**

사용자가 다른 사람의 음악 기록에 조용한 공감을 남기고 새로고침 후에도 유지하게 한다.

**수직 흐름**

```text
음악 기록 목록 진입
→ 인증된 기록 조회
→ Express에서 기록과 likes 조회
→ 사용자별 liked 상태 응답
→ 각 카드에 현재 상태 표시
```

**작업 범위**

- 사용자와 음악 기록을 연결하는 좋아요 관계 모델
- 사용자·기록 조합의 중복 방지 제약
- 현재 로컬 `liked` 상태를 서버 응답 기반 상태로 교체
- 기록 조회 응답에 현재 사용자의 `liked` 상태 포함
- 기록 없음, 좋아요 없음, 조회 실패 처리
- 좋아요 수는 노출하지 않고 개인 상태만 표시

**완료 기준**

- 좋아요가 없는 기록은 미선택 상태로 표시된다.
- 해당 사용자가 좋아요한 기록만 선택 상태로 표시된다.
- 다른 사용자의 좋아요가 내 상태로 표시되지 않는다.
- 기록이 없을 때 기존 빈 상태가 정상 표시된다.
- 새로고침 후 서버에 저장된 상태가 복원된다.

**사용자 검증**

두 계정이 같은 기록 목록을 열었을 때 사용자별 선택 상태가 분리되는지 확인한다.

**선행 작업**

- 작업 3

### 작업 7: 좋아요와 좋아요 취소가 즉시 반영되고 중복 저장되지 않는다

**목적**

현재 임시 UI 동작을 실제 DB 저장 흐름으로 완성한다.

**수직 흐름**

```text
좋아요 버튼 클릭
→ POST 또는 DELETE 요청
→ Express 인증·기록 존재 검증
→ likes 생성 또는 삭제
→ 변경된 상태 응답
→ 카드 버튼 상태 갱신
```

**작업 범위**

- `POST /api/music-records/:recordId/likes`
- `DELETE /api/music-records/:recordId/likes`
- 기존 `MusicCard` 버튼을 실제 API에 연결
- 요청 중 중복 클릭 방지
- 없는 기록, 비로그인, 요청 실패 처리
- 실패 시 이전 상태 복원
- 새로고침 상태 유지
- 댓글, 알림, 좋아요 사용자 목록은 제외

**완료 기준**

- 좋아요 후 선택 상태가 표시되고 DB에는 한 건만 존재한다.
- 새로고침 후에도 선택 상태가 유지된다.
- 좋아요 취소 후 DB 관계와 화면 상태가 모두 해제된다.
- 빠르게 반복 클릭해도 중복 행이 생기지 않는다.
- 없는 기록과 비로그인 요청은 적절한 오류로 실패한다.
- 서버 실패 시 성공한 것처럼 보이지 않는다.

**사용자 검증**

좋아요 → 새로고침 → 취소 → 새로고침을 수행하고 반복 클릭 뒤 DB 중복 여부를 확인한다.

**선행 작업**

- 작업 6

### 작업 8: 세 기능을 통합 검증하고 실제 상태에 맞게 문서화한다

**목적**

각 기능이 따로 동작하는 수준을 넘어 하나의 사용자 흐름으로 연결되고 기존 음악 기록 기능을 깨뜨리지 않았는지 확인한다.

**작업 범위**

- 회원가입 → 로그인 → 기록 작성 → 사용자 조회 → 팔로우 → 좋아요 → 새로고침 → 로그아웃 전체 흐름
- 비로그인 접근, 빈 입력, 데이터 없음, API 실패, 중복 요청 검증
- 다른 사용자 ID로 쓰기 시도와 RLS 검증
- 기존 Spotify 검색·기록 생성·조회 회귀 테스트
- 타입 검사, 빌드, 프런트엔드·서버 테스트
- README의 Demo/인증 제외 설명과 실제 API·환경변수·구조 수정
- 필요한 API·ERD·아키텍처 문서가 없다면 최소 문서 추가
- 포트폴리오 시연용 두 계정 시나리오 정리

**완료 기준**

- 두 계정으로 전체 사용자 흐름을 처음부터 끝까지 재현할 수 있다.
- 새로고침 후 세션·팔로우·좋아요 상태가 모두 유지된다.
- 사용자 A가 사용자 B의 소유 데이터로 쓰기 작업을 할 수 없다.
- 전체 테스트, 타입 검사와 빌드 결과가 기록된다.
- 실행하지 못한 검증은 통과로 처리하지 않고 이유와 수동 검증법을 남긴다.
- README와 실제 기능·API·환경변수가 일치한다.

**사용자 검증**

두 브라우저 세션 또는 일반·시크릿 창에서 서로 다른 계정으로 전체 시나리오를 수행한다.

**선행 작업**

- 작업 5
- 작업 7

---

## 구현 순서와 의존성

```text
작업 1 회원가입·프로필
  ↓
작업 2 로그인·세션
  ├─→ 작업 3 사용자 소유 기록 ─→ 작업 6 좋아요 조회 ─→ 작업 7 좋아요 변경
  └─→ 작업 4 사용자 목록 ─────→ 작업 5 팔로우 변경
                                      ↓
                              작업 8 통합 QA·문서
```

하루에 두 개의 반일 작업을 기준으로 하면 다음 배치가 현실적이다.

| 일정 | 오전 | 오후 |
|---|---|---|
| 1일차 | 작업 1 | 작업 2 |
| 2일차 | 작업 3 | 작업 4 |
| 3일차 | 작업 5 | 작업 6 |
| 4일차 | 작업 7 | 작업 8의 자동 검증 |
| 5일차 | 통합 수동 QA·결함 수정 여유 | 문서와 최종 시연 검증 |

결함 수정은 발견 전에 정확한 커밋 단위로 정의할 수 없으므로 5일차 버퍼로 확보한다. 새 기능을 넣는 시간으로 사용하지 않는다.

---

## 최종 자체 검토

### 너무 큰 작업

- 인증 전체를 한 작업으로 묶지 않고 회원가입·프로필과 로그인·세션으로 분리했다.
- 팔로우는 대상 조회와 관계 변경으로 나눴다.
- 좋아요는 상태 조회와 변경으로 나눴다.
- 작업 8은 기능 구현이 아니라 통합 검증과 문서 정합성만 담당한다.

### 누락 방지

다음 항목을 각 관련 작업의 완료 기준에 포함했다.

- 빈 입력
- 비로그인 사용자
- 데이터 없음
- API 요청 실패
- 새로고침 상태 유지
- 중복 제출과 중복 관계
- 자기 자신 팔로우
- 다른 사용자 데이터 접근 제한
- 기존 Spotify·음악 기록 회귀

### 위험한 작업

- 가장 큰 위험은 기존 익명 `music_records`를 필수 `user_id` 구조로 전환하는 작업이다. 기존 행을 임의 사용자에게 귀속하거나 삭제하면 안 된다. 작업 3 시작 전에 기존 데이터 수와 보존 여부를 확인하고, 소유자를 정할 수 없는 기존 데이터는 읽기 전용 레거시로 유지하거나 별도 백업 후 처리해야 한다.
- Supabase 이메일 확인 설정에 따라 회원가입 직후 세션 유무가 달라진다. 작업 1 전에 운영 설정을 확인해야 한다.
- `SUPABASE_ANON_KEY` 명칭은 서버와 브라우저에서 혼동될 수 있다. 공개 키만 프런트에 두고 서비스 역할 키는 사용하지 않는 방향으로 고정해야 한다.
- 팔로우와 좋아요는 중복 요청을 UI만으로 막지 않고 DB 고유 제약으로도 보장해야 한다.
- 현재 음악 기록 라우트에 DB 로직이 직접 들어 있다. 이번 기능에 필요한 부분만 Controller·Service로 분리하고 프로젝트 전체 리팩토링으로 확장하지 않아야 한다.
- README는 TypeScript 사용, API 경로, 인증 상태 등 현재 코드와 이미 불일치한다. 문서 변경은 구현 결과가 확정된 마지막 작업에서 수행해야 한다.
