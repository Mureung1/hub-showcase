# SWIM 남은 개발 계획

## 현재 기준

인증, Spotify 검색, 음악 기록, 사용자 검색, 팔로우·피드, 좋아요·집계·사용자 목록은 구현되어 자동 테스트를 통과했다. 남은 목표는 사람의 음악 다이어리를 열어보는 공개 프로필을 먼저 완성하고, Monthly Recap을 만든 뒤 그 기록을 Spotify 플레이리스트로 내보내는 것이다.

```text
공개 프로필
→ Monthly Recap
→ Spotify 계정 연결
→ Recap 플레이리스트 내보내기
→ 통합 QA·배포
```

## 작업 목록

### 작업 1: 검색 결과에서 공개 프로필 헤더를 연다

**목적**

닉네임이나 아바타를 누르면 공개 프로필과 팔로우 상태를 확인하고 이전 화면으로 돌아올 수 있게 한다.

**수직 흐름**

```text
검색 결과 선택 → GET /api/users/:nickname → profiles·follows 조회
→ 공개 프로필 응답 → 프로필 헤더 표시 → 돌아가기
```

**작업 범위**

- 닉네임 기반 공개 프로필 API
- `nickname`, `bio`, `avatarUrl`, `isMe`, `isFollowing`
- 검색 결과의 닉네임·아바타 진입
- 프로필 로딩·404·실패·재시도
- Auth UUID와 이메일 비노출

**완료 기준**

- 선택한 사용자의 공개 프로필이 표시된다.
- 본인 여부와 팔로우 상태가 정확하다.
- 비로그인 요청은 401, 없는 닉네임은 404다.
- 돌아가면 기존 홈 화면으로 복귀한다.

### 작업 2: 공개 프로필에 오늘의 기록과 음악 다이어리를 표시한다

**목적**

로그인 사용자가 다른 사람의 하루를 음악 기록으로 알아볼 수 있게 한다.

**작업 범위**

- 인증 사용자에게 공개 음악 기록을 허용하는 RLS
- 좋아요 RPC 열람 조건 동기화
- `GET /api/users/:nickname/music-records`
- 오늘의 기록과 지난 기록 분리
- cursor 페이지 조회와 더 보기
- 기록 없음·실패 상태

**완료 기준**

- 팔로우 전에도 로그인 사용자는 공개 다이어리를 볼 수 있다.
- 다른 사용자의 쓰기·수정·삭제 권한은 생기지 않는다.
- 오늘의 기록과 과거 기록이 날짜순으로 표시된다.

**선행 작업**

- 작업 1

### 작업 3: 피드 작성자와 URL을 공개 프로필에 연결한다

**목적**

검색과 피드 어디서든 같은 사람의 다이어리로 이동하고 새로고침·뒤로 가기를 지원한다.

**작업 범위**

- 피드 닉네임·아바타 진입
- `?profile={nickname}` URL 상태
- 직접 접근·새로고침·뒤로 가기
- 이전 요청 취소
- 새 라우팅 라이브러리 추가 없음

**선행 작업**

- 작업 2

### 작업 4: 공개 프로필의 팔로우와 좋아요를 동기화한다

**목적**

프로필에서 관계를 만들고 음악 기록에 공감한 결과가 홈·피드와 일치하게 한다.

**작업 범위**

- 프로필 팔로우·언팔로우
- 기존 좋아요와 `N명이 기억했어요` 재사용
- 홈·검색·피드 상태 갱신
- 요청 중 비활성화와 실패 복원

**선행 작업**

- 작업 3

### 작업 5: Monthly Recap API를 만든다

**목적**

한 달 동안 기록한 음악과 기억을 왜곡 없이 집계한다.

**작업 범위**

- `GET /api/recaps/monthly?year=&month=`
- 기록 일수, 기록 수, 자주 기록한 아티스트
- 첫 기록·마지막 기록·월간 곡 목록
- 인증 사용자 자신의 기록만 집계
- 월 입력 검증과 빈 달 처리

**선행 작업**

- 작업 4

### 작업 6: Monthly Recap 화면을 만든다

**목적**

통계 대시보드가 아니라 한 달의 음악 일기를 다시 펼쳐보는 화면을 제공한다.

**작업 범위**

- 월 선택
- 대표 앨범 이미지와 기록 일수
- 자주 함께한 아티스트
- 첫 음악·마지막 음악
- 월간 음악 타임라인
- 로딩·빈 상태·실패

**선행 작업**

- 작업 5

### 작업 7: Spotify 사용자 계정을 안전하게 연결한다

**목적**

Client Credentials가 아닌 사용자 OAuth로 본인 Spotify 계정에 쓰기 권한을 얻는다.

**작업 범위**

- Authorization Code Flow
- `playlist-modify-private` 중심 최소 scope
- connect·callback·connection·disconnect API
- refresh token 서버 전용 보관과 갱신
- 연결 실패·취소·만료 처리

**선행 작업**

- 작업 6

### 작업 8: Recap 곡을 Spotify 플레이리스트로 내보낸다

**목적**

사용자가 한 달의 SWIM 기록을 자신의 Spotify 비공개 플레이리스트로 만든다.

**작업 범위**

- `POST /api/spotify/playlists`
- 월간 `spotify_track_id`를 URI로 변환
- 플레이리스트 생성 후 최대 100곡 단위 추가
- 중복 생성 방지와 Spotify 링크 반환
- 401·403·429·5xx 처리

**선행 작업**

- 작업 7

### 작업 9: 전체 흐름을 실제 계정으로 검증하고 배포한다

**목적**

공개 프로필부터 Recap 플레이리스트까지 포트폴리오 시연 가능한 상태로 고정한다.

**작업 범위**

- 다중 SWIM 계정과 Spotify 테스트 사용자
- RLS·OAuth·비밀정보 노출 검증
- 전체 테스트·타입 검사·빌드
- CORS·환경변수·HTTPS 배포 검증
- README·API·Architecture·QA 문서
- main 통합과 시연 시나리오

**선행 작업**

- 작업 8

## 권장 구현 순서

```text
1 공개 프로필 헤더 → 2 공개 음악 다이어리 → 3 진입·URL
→ 4 관계 동기화 → 5 Recap API → 6 Recap UI
→ 7 Spotify OAuth → 8 플레이리스트 → 9 QA·배포
```

## 제외

- 댓글, DM, 알림, 실시간 채팅
- 인기순·랭킹
- AI 감정 분석
- 공개·협업 플레이리스트
- 과도한 애니메이션
