# tax-agent 방향 재검토 — 인계 프롬프트 (2026-07-08 세션)

새 세션(또는 다른 사람)이 이 문서 하나만 읽고 바로 이어서 작업할 수 있도록 정리한 인계 문서입니다.

## 1. 프로젝트 배경

NAVER AI Challenge 부트캠프 개인 프로젝트로 세무 관련 agent/자동화(`tax-agent`)를 만드는 중. 최종 방향이 아직 확정 안 됨 — 이미 한 번 방향 전환을 겪었고, 오늘 세션에서 또 재검토했음.

### 기존 히스토리 (이번 세션 이전)

`7-7/PLAN.md`, `7-7/tax-agent-workflow.pdf`에 기록됨:
1. 처음엔 "세금계산서 발행 RPA" 구상 → 모바일 2차인증 때문에 완전자동화 불가 판단, 보류
2. 리서치 후 "공동인증서(파일+비밀번호)면 완전자동화 가능" 확인 → 하지만 대신 **"매출/매입 불일치 탐지"(국세청 신고내역 vs 내 장부, 가산세 회피)를 1순위**로 확정, 세금계산서 발행은 2순위로 격하(발행 직전 사람 확인 게이트는 유지)
3. 이 방향으로 15일 마일스톤까지 상세 설계 완료된 상태였음(백엔드 Express+TS, SQLite, matcher.ts 등)

## 2. 오늘 세션에서 일어난 일 (시간순)

### 2-1. 방향 재검토 계기

사용자가 "불일치 탐지" 1순위 방향이 다시 아쉽다고 판단. 이유 3가지(전부 해당):
- **너무 수동적/읽기 전용** — 완전자동화여도 "조회 후 알림"뿐이라 에이전트라기보다 리포팅 툴에 가까움
- **차별성 부족** — 캐시노트/자비스/삼쩜삼 등 기존 서비스와 비슷해 보임
- **국세청 단속 리스크** — 국세청이 스크래핑 플랫폼(삼쩜삼, 토스 등)을 단속 중이라는 뉴스를 봄

대안으로 "세금계산서 발행 agent"를 재고 중이나, 본인인증/공인인증서를 agent가 컨트롤할 수 있을지 확신이 없어서 리서치 요청.

### 2-2. 리서치 진행 → `7-8/tax-agent-research.md` 작성

플랜 모드로 진행. 웹서치 기반으로 다음을 리서치해서 문서화:
1. **인증수단별 자동화 가능성**: 공동인증서(로컬 파일, 이미 자동화 검증됨) vs 금융인증서(클라우드, 최초 1회 사람 개입 후 자동연결 시 간편비밀번호만으로 재로그인 가능하다고 문서상 확인 — 단 Playwright 재현성은 당시 미확정) vs 간편인증(매번 모바일 앱푸시 필요, 완전자동화 불가 확정)
2. **국세청 규제 동향(2025~2026)**: 스크래핑 플랫폼(삼쩜삼, 토스) 단속 중, 오픈API 전환 + 이용수수료 부과 추진("엔티스고도화" 사업). 개인이 본인 계정으로 직접 조회하는 것까지 규제 대상인지는 뉴스에 명시 안 됨(추론상 상대적으로 안전할 가능성).
3. **공식 API vs RPA**: data.go.kr 공식 오픈API는 "사업자등록정보 진위확인/상태조회"뿐 — 매출/매입/세금계산서 데이터는 공식 무료 API 없음, ASP사(팝빌/홈택스빌)의 유료 연동만 합법적 대안 채널.
4. **기존 서비스 지형**: 삼쩜삼/자비스/캐시노트/위하고/팝빌 — 각각의 타겟과 빈틈 정리. 빈틈: 사전(prospective) 절세 시뮬레이션, 초소규모 프리랜서 대상 대화형 온보딩, 세무사 보조 B2B2C, 탐지 이후 능동적 후속조치.
5. **새 방향 후보 5개**(결론 아님, 브레인스토밍 시드): ① 탐지+능동적 후속조치 확장 ② ASP API 기반 + 판단레이어 차별화 ③ 프리랜서 대상 사전 절세 시뮬레이션 ④ 세무사 보조 B2B2C ⑤ 세금계산서 발행 agent 1순위 부활

전체 내용과 출처 링크는 `7-8/tax-agent-research.md` 참고.

### 2-3. 브레인스토밍 → 방향 좁히기 (AskUserQuestion으로 진행)

- 기존 설계(불일치 탐지 인프라) 살릴지/버릴지 → **"각각 테스트해보고 결정하자"**(미리 결정하지 않기로 함)
- 스크래핑 리스크 vs ASP 유료 API 비용 트레이드오프 → **"모의 테스트 후 결정"**(마찬가지로 유보)
- 사용자가 명시적으로: **"공인인증서(공동인증서) 방식은 제외"** — 현재 본인이 활용할 수 없는 수단이라서
- 현재 실제 보유/활용 가능한 것: **은행 계좌 / 금융인증서 보유** → 이후 스파이크는 금융인증서 중심으로 진행하기로 함

### 2-4. 금융인증서 스파이크 착수 → `7-8/hometax-fincert-spike/`

안전장치 먼저 확정:
- 자동연결(최초 1회, 사람이 실제 브라우저로 해야 함)은 **사용자가 아직 안 함, "조금 있다가 할게"** 상태
- 간편비밀번호(PIN) 등 민감값은 **`.env` 파일 + `.gitignore` 처리**로만 다루기로 함(코드에 하드코딩 금지, 로그 출력 금지)

`mcp__claude-in-chrome`(사용자의 실제 크롬 확장)을 시도했으나 **확장 미연결**로 실패 → 대신 Claude Code가 Bash로 `npm install playwright` + `npx playwright install chromium`을 직접 실행해서 **독립된 Chromium을 새로 설치**하고, 이걸로 Playwright 스크립트를 짜서 화면을 스크린샷으로 찍어 Read 도구로 확인하는 방식으로 진행(사용자의 평소 브라우저와는 무관한 별도 인스턴스).

**만든 파일**:
- `7-8/hometax-fincert-spike/package.json` — playwright devDependency
- `7-8/hometax-fincert-spike/.gitignore` — `node_modules/`, `.env`, `screenshots/`, `*.png` 제외
- `7-8/hometax-fincert-spike/.env.example` — `HOMETAX_FINCERT_PIN`, `HOMETAX_BUSINESS_ID` placeholder (아직 실제 값 없음, 2단계에서 사용 예정)
- `7-8/hometax-fincert-spike/explore-login.mjs` — **1단계 탐색 스크립트, 자격증명 전혀 사용 안 함**. 홈택스 로그인 화면까지만 진입해서 구조를 스크린샷/DOM 덤프로 기록. `reports/` 폴더에 결과 저장(`.gitignore` 처리됨 — 재실행하면 덮어써짐, 커밋 금지 대상)

**실행 결과로 실측 확인된 사실**(자격증명 입력 없이, `explore-login.mjs` 여러 번 반복 실행하며 selector를 관찰 기반으로 수정):
1. 홈택스 메인 → 헤더 "로그인" 링크(`#mf_wfHeader_group1503`, role 기반 selector가 더 안정적) 클릭 → 로그인 선택 화면
2. 로그인 방식 중 **"공동인증서"와 "금융인증서"가 "공동·금융인증서" 버튼 하나로 통합**돼 있음. 클릭하면 "인증서 선택창"(드림시큐리티 위젯) 모달이 뜨고, 내부에 위치 탭이 `브라우저 / 금융인증서 / 하드디스크·이동식디스크 / 휴대전화 / 스마트인증`으로 나뉨
3. **"금융인증서" 탭을 누르면 완전히 별도의 "금융인증서비스" 모달**이 새로 뜨고, 여기서 **"개인" vs "사업자(개인사업자/법인사업자)"를 먼저 선택**해야 함 → **사업자용 금융인증서가 실제로 지원됨을 확인**. 기존 화면에 있던 "전자(세금)계산서 발급은 사업자등록번호로 발급된 인증서만 가능" 안내와 맞물려, **사업자용 금융인증서를 쓰면 세금계산서 발행까지 커버할 가능성**이 생김(단, 실제 발급/발행까지 확인한 건 아니고 추론)
4. "공동인증서를 클라우드에 저장하기" 안내에는 **이름+휴대폰번호 입력 → SMS 확인코드 전송 → 코드로 로그인**이 명시돼 있어, 최초 1회 사람 개입이 필요하다는 기존 가정이 실측으로 뒷받침됨
5. 화면이 WebSquare 프레임워크 + iframe 다수 + 로딩스피너로 매우 동적이라, **정적 id selector(`mf_wfHeader_...` 같은 자동생성 ID)보다 `getByRole`/`getByText` 우선, 필요시 좌표 클릭(`page.mouse.click`) 혼합 전략이 필요**함을 확인
6. **여기서 실제 인증정보 입력 직전(개인/사업자 선택 화면)까지 도달, 자격증명 없이는 더 진행 불가능해서 의도적으로 멈춤**. `login-test.mjs`(2단계, 실제 PIN 입력 로그인 테스트)는 아직 작성 안 함.

리서치 문서 `7-8/tax-agent-research.md`의 "1-3. 실측 확인" 섹션에 위 내용을 그대로 반영해둠.

## 3. 지금 상태 요약 / 다음 세션이 이어받을 것

**사람이 해야 할 일(대기 중)**:
- 은행 앱에서 금융인증서 발급(가능하면 **사업자용**으로 — 세금계산서 발행 가능성까지 열어두려면)
- 손택스 또는 홈택스에서 "자동연결"을 실제 브라우저로 최소 1회 수동 설정
- 완료되면 다음 세션에서 그 사실을 알려주면 됨

**다음 세션이 할 일(코드)**:
1. `7-8/hometax-fincert-spike/login-test.mjs` 작성 — `.env`의 `HOMETAX_FINCERT_PIN`만으로 **새 브라우저 세션(자동연결 설정한 그 컨텍스트 밖)**에서 재로그인이 실제로 되는지 테스트. `explore-login.mjs`에서 확인한 selector(로그인 링크, 공동·금융인증서 버튼, 금융인증서 탭 좌표/역할, 개인/사업자 선택)를 그대로 재사용
2. 성공/실패 여부에 따라 리서치 문서의 "미확정" 표시를 "확정"으로 갱신
3. 남은 스파이크 후보(사용자가 "각각 테스트 후 결정" 방침이므로 순서는 사용자와 상의):
   - ASP(팝빌 등) 무료 체험/테스트 계정으로 세금계산서 조회 API 최소 호출 테스트
   - 세금계산서 발행 mock tool-calling 플로우(인증 문제와 분리해서 "쓰기 액션" 자체의 데모 임팩트 확인) — 기존 `NAVER_AIchallenge_personal_project`의 `src/App.jsx` 데모(`7-7/PLAN.md`에서 "가짜 데모"로 언급됨)를 참고할 수 있음
4. 아직 **최종 방향(무엇을 만들지)은 결정 안 됨** — 위 스파이크들 결과를 보고 사용자와 함께 결정하기로 한 상태. 섣불리 하나로 확정하지 말 것.

## 4. 안전/제약 사항 (반드시 지킬 것)

- PIN/비밀번호 등 자격증명은 **`.env`에만, 커밋 금지**(`.gitignore` 이미 처리됨). 로그·콘솔 출력에도 절대 노출 금지
- 실제 홈택스에 대한 **쓰기 액션(세금계산서 발행 등)은 사람의 명시적 확인 없이 자동 실행하지 말 것** — 이건 오늘 세션이 아니라 `7-7/PLAN.md`부터 이어져 온 원칙이고, 계속 유지
- 국세청이 스크래핑에 민감해진 시기라는 게 리서치로 확인됐으므로, 스크립트 실행 빈도/방식에 있어 과도한 요청은 지양(개인 계정 1회성 테스트 수준으로 유지)

## 5. 참고 파일 경로

- `7-7/PLAN.md`, `7-7/tax-agent-workflow.pdf` — 기존(이전 세션) 설계
- `7-8/tax-agent-research.md` — 오늘 리서치 전체 + 출처 + 실측 확인
- `7-8/hometax-fincert-spike/` — 금융인증서 스파이크 코드(`explore-login.mjs`, `reports/`는 재실행 시 갱신됨)
- `C:\Users\khans\.claude\plans\naver-aichallenge-optimized-wand.md` — 오늘 세션에서 승인된 플랜 원본(리서치+브레인스토밍 범위)
