# 디자인 가이드 (Design Guide)

> 참고 화면: 직장인 커뮤니티 앱 — 커뮤니티 피드 화면

---

## 1. 색상 팔레트 (Color Palette)

| 역할 | 색상 | 설명 |
|------|------|------|
| Primary (강조) | `#FF6B35` 계열 오렌지 | 주요 CTA 버튼, 카테고리 태그 테두리, 불릿 포인트 |
| Primary CTA Button | `#FF5A1F` (진한 오렌지-레드) | 플로팅 액션 버튼(FAB) 배경 |
| Tag Background | `#FFF3EC` (연한 오렌지 틴트) | 카테고리/태그 배지 배경 |
| Text — Primary | `#1A1A1A` (거의 검정) | 제목, 강조 텍스트 |
| Text — Secondary | `#888888` (중간 회색) | 부제목, 날짜, 메타 정보 |
| Text — Tertiary | `#BBBBBB` (연한 회색) | 비활성 탭, 보조 설명 |
| Background | `#FFFFFF` (흰색) | 카드 배경, 전체 배경 |
| Divider | `#F0F0F0` (매우 연한 회색) | 카드 사이 구분선 |
| Active Tab Underline | `#1A1A1A` (검정) | 활성 탭 하단 밑줄 |

---

## 2. 레이아웃 (Layout)

### 전체 구조
- **모바일 우선(Mobile-First)** 세로 단일 컬럼 레이아웃
- 상단 고정 헤더(Sticky Header) + 스크롤 가능한 컨텐츠 영역 + 하단 고정 내비게이션 바

### 영역 구분
```
┌─────────────────────────────┐
│  Status Bar (시스템)         │
├─────────────────────────────┤
│  App Header                 │  ← 페이지 타이틀 + 아이콘 버튼들
├─────────────────────────────┤
│  Tab Navigation             │  ← 수평 탭 메뉴
├─────────────────────────────┤
│                             │
│  Feed List (스크롤 가능)     │  ← 카드 리스트
│    ┌─────────────────────┐  │
│    │  Feed Card          │  │
│    └─────────────────────┘  │
│    ┌─────────────────────┐  │
│    │  Feed Card          │  │
│    └─────────────────────┘  │
│         ...                 │
│                             │
├─────────────────────────────┤
│  Bottom Navigation Bar      │  ← 하단 탭 바
└─────────────────────────────┘
         ↑ FAB (우하단)
```

### 여백 (Spacing)
- 카드 내부 패딩: 약 16px (상하), 20px (좌우)
- 카드 간 구분: 8~12px 높이의 배경색 변경 또는 명시적 Divider
- 좌우 여백(safe area): 좌 20px, 우 20px 일관 유지

---

## 3. 타이포그래피 (Typography)

| 역할 | 크기 | 굵기 | 색상 |
|------|------|------|------|
| 페이지 타이틀 | 20~22px | Bold (700) | `#1A1A1A` |
| 탭 메뉴 | 14~15px | Medium (500) | 활성: `#1A1A1A` / 비활성: `#BBBBBB` |
| 카드 제목 (포스트 제목) | 17~18px | Bold (700) | `#1A1A1A` |
| 카드 본문 미리보기 | 14px | Regular (400) | `#888888` |
| 작성자 · 직군 | 13px | Regular (400) | `#888888` |
| 날짜 · 메타 수치 | 13px | Regular (400) | `#AAAAAA` |
| 카테고리 태그 | 12~13px | Medium (500) | `#FF6B35` |
| 하단 탭 레이블 | 11~12px | Regular (400) | 활성: `#FF6B35` / 비활성: `#AAAAAA` |

**폰트 패밀리**: 한글 시스템 폰트 계열 (`Apple SD Gothic Neo`, `Noto Sans KR` 또는 유사 산세리프)

---

## 4. 컴포넌트 (Components)

### 4-1. App Header
- 좌측: 페이지 타이틀 (Bold, 큰 크기)
- 우측: 기능 아이콘 버튼 나열 (검색, MY, 알림, 메뉴)
- 배경: 흰색, 하단 shadow 또는 border 없이 자연스럽게 전환

### 4-2. Tab Navigation (수평 탭)
- 텍스트 전용 탭, 하단 밑줄(Underline)로 활성 상태 표시
- 활성 탭: 검정 텍스트 + 검정 밑줄
- 비활성 탭: 회색 텍스트, 밑줄 없음
- 탭 간 동일한 수평 패딩 유지

### 4-3. Feed Card (피드 카드)
- 카드 구성 순서 (위 → 아래):
  1. **카테고리 태그 배지** — 오렌지 테두리, 연한 오렌지 배경, 오렌지 텍스트
  2. **불릿 포인트(●)** + **포스트 제목** — 오렌지 불릿, Bold 검정 제목
  3. **본문 미리보기** — 1~2줄 말줄임 처리 (text-overflow: ellipsis)
  4. **작성자 닉네임 | 직군/직무** — 회색 소형 텍스트
  5. **구분선 (Divider)**
  6. **메타 정보** — 날짜(좌), 조회수·좋아요·댓글 수(우) 아이콘 + 숫자
- 카드 배경: 흰색, 모서리 없이 flat (border-radius 없거나 최소)
- 카드 간: 얇은 회색 Divider 또는 배경색 구분

### 4-4. Category Tag Badge
- 형태: 둥근 모서리(border-radius: 12~16px) 태그 형태
- 스타일: border: 1px solid #FF6B35, background: #FFF3EC, color: #FF6B35
- 크기: 패딩 4px 8px, 텍스트 12~13px

### 4-5. Meta Info Bar (카드 하단 정보)
- 좌측: 날짜 ("N시간 전" 형식)
- 우측: 조회수 · 좋아요 · 댓글 수 (아이콘 + 숫자 쌍)
- 아이콘: 라인 아이콘(outline style) 사용

### 4-6. FAB (Floating Action Button)
- 위치: 화면 우하단 고정 (position: fixed; bottom: 80px; right: 20px)
- 형태: 원형(border-radius: 50%), 크기 약 56px
- 색상: #FF5A1F 오렌지-레드 배경 + 흰색 아이콘 (연필/편집 아이콘)
- 그림자: 뚜렷한 drop shadow로 부유 효과 강조

### 4-7. Bottom Navigation Bar
- 5개 탭: 투데이 / 명함첩 / 채용공고(뱃지 포함) / 커뮤니티 / 내 프로필
- 아이콘 + 텍스트 레이블 수직 배치
- 활성 탭: 오렌지(#FF6B35) 아이콘 + 오렌지 텍스트
- 비활성 탭: 회색 아이콘 + 회색 텍스트
- 상단 얇은 border로 콘텐츠 영역과 구분

---

## 5. 아이콘 스타일 (Icon Style)

- **스타일**: Outline(라인) 아이콘 일관 사용
- **크기**: 헤더 22~24px, 하단 탭 22~24px, 메타 정보 14~16px
- **색상**: 활성 상태 오렌지, 비활성 회색

---

## 6. 인터랙션 패턴 (Interaction Patterns)

| 패턴 | 설명 |
|------|------|
| 탭 전환 | 탭 클릭 시 활성 탭 스타일 전환 + 콘텐츠 영역 교체 |
| 피드 카드 탭 | 카드 전체 영역이 터치 대상, 상세 페이지로 이동 |
| FAB 탭 | 새 글 작성 화면으로 이동 |
| 스크롤 | 세로 스크롤, 헤더/탭/하단바는 고정 유지 |
| 배지(Badge) | 채용공고 탭에 숫자 뱃지로 새 항목 수 표시 |
| 시간 표시 | 절대 시간 대신 "N시간 전" 상대 시간 포맷 사용 |

---

## 7. 설계 원칙 (Design Principles)

1. **정보 밀도 우선**: 한 화면에 다수의 피드 항목이 보이도록 간결한 카드 레이아웃 사용
2. **브랜드 컬러 집중**: 오렌지 계열 단일 포인트 컬러로 CTA, 태그, 활성 상태 모두 통일
3. **콘텐츠 중심 UI**: 장식적 요소 최소화, 텍스트 가독성 최우선
4. **모바일 엄지 존 고려**: 주요 탭은 하단 배치, FAB도 엄지가 닿기 쉬운 우하단 위치
5. **계층적 타이포그래피**: Bold 제목 → Regular 본문 → 소형 메타정보 의 명확한 시각 계층
6. **일관된 컴포넌트**: 태그, 아이콘, 탭 등 동일 스타일 컴포넌트 반복 사용으로 학습 비용 절감
