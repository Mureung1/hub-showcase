// ShowUp does not use Firebase Storage in MVP. Deny all access until a
// bucket-backed feature has its own path and authorization rules.
service firebase.storage {
  match /b/{bucket}/o/{allPaths=**} {
    allow read, write: if false;
  }
}
