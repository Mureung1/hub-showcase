# 웹(apps/web) 환경변수 예시 — 실제 값은 .env.local 에 두고 커밋하지 않는다.
# Vite는 VITE_ 접두 변수만 클라이언트에 노출한다. 시크릿(API 키)은 여기 두지 말 것.

# mock 데이터로 동작(기본, 데모 보험). 서버 실연동하려면 false 로.
VITE_MOCK_MODE=true

# 서버 API 베이스 URL (미설정 시 http://localhost:4000)
VITE_API_BASE=http://localhost:4000
