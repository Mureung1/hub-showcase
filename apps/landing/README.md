# `@ay-ple/landing`

`apps/landing`은 AY-PLE의 public product homepage와 release-generated install truth를 렌더링할 workspace다.

현재 `Spine S0`에서는 package identity와 test·typecheck·build seam만 제공한다. HTML rendering, release display input, static asset와 deployment behavior는 아직 구현하지 않는다.
