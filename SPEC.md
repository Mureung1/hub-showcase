# SPEC.md

## 기능 개발 체크리스트

새 기능은 아래 순서로 진행합니다. 기능 규모가 작아도 Issue, 설계, 검증, 문서 정리 기준은 생략하지 않습니다.

### 1. GitHub Issue 등록

- [ ] 기능 목적을 한 문장으로 작성한다.
- [ ] 사용자 관점의 필요성을 작성한다.
- [ ] 완료 기준을 체크리스트로 작성한다.
- [ ] 관련 문서 링크를 연결한다.
- [ ] 우선순위를 정한다.

### 2. 설계

- [ ] 사용자 흐름을 정리한다.
- [ ] 필요한 화면, 상태, 빈 상태, 오류 상태를 정리한다.
- [ ] 필요한 데이터 구조와 상태 관리 위치를 정리한다.
- [ ] 기존 문서 또는 코드와 충돌하는 부분을 확인한다.
- [ ] 구현 범위를 MVP와 후속 작업으로 나눈다.

### 3. 개발

- [ ] 브랜치 또는 작업 범위를 명확히 한다.
- [ ] 기존 컴포넌트, 토큰, 폴더 구조를 우선 따른다.
- [ ] mock 데이터가 필요하면 `src/data` 또는 기능 폴더 내부에 정리한다.
- [ ] 공유 상태가 필요할 때만 Zustand store를 사용한다.
- [ ] 스타일은 CSS Modules와 `src/styles` 토큰 기준으로 작성한다.
- [ ] 접근성 기본 요소를 확인한다.

### 4. 검증

- [ ] `npm run typecheck`를 통과한다.
- [ ] `npm run lint`를 통과한다.
- [ ] `npm run test`를 통과한다.
- [ ] `npm run build`를 통과한다.
- [ ] 모바일/데스크톱 반응형을 확인한다.
- [ ] 라이트/다크 모드 영향이 있으면 확인한다.

### 5. Pull Request

- [ ] 커밋 전에 `git status -sb`와 `git diff --cached --name-status`로 stage 목록을 확인한다.
- [ ] `.DS_Store`, `__MACOSX`, 임시 폴더, 생성 이미지 초안, 개인 자동화 파일처럼 push하면 안 되는 파일이 있으면 `.gitignore`에 추가하거나 stage에서 제외한다.
- [ ] PR 제목은 한국어 또는 Conventional Commit 형식으로 작성한다.
- [ ] 변경 요약을 작성한다.
- [ ] 테스트 결과를 작성한다.
- [ ] 관련 Issue를 연결한다.
- [ ] 화면 변경이 있으면 스크린샷 또는 짧은 설명을 첨부한다.

### 6. 승인 및 머지

- [ ] 사용자가 PR 내용을 확인한다.
- [ ] 사용자가 승인한다.
- [ ] 승인 후 머지한다.
- [ ] 머지 후 작업 브랜치 상태를 정리한다.

### 7. Issue Close

- [ ] 머지 후 연결된 Issue를 close 한다.
- [ ] Issue에 최종 반영 내용과 PR 링크를 남긴다.
- [ ] 남은 후속 작업이 있으면 새 Issue로 분리한다.

### 8. 문서 정리

- [ ] `docs/plan.md` 업데이트가 필요한지 확인한다.
- [ ] `docs/features/*` 기능 문서 업데이트가 필요한지 확인한다.
- [ ] `docs/design/*` 디자인 문서 업데이트가 필요한지 확인한다.
- [ ] `AGENTS.md` 업데이트가 필요한지 확인한다.
- [ ] `skills/design/SKILL.md` 업데이트가 필요한지 확인한다.

## 기본 원칙

- 기능 하나는 Issue 단위로 시작한다.
- 개발 전에 설계를 먼저 작성한다.
- 사용자가 승인하기 전에는 머지하지 않는다.
- React 화면과 mock data flow가 안정되기 전에 Electron, Monaco, RAG, Notion API 같은 큰 의존성을 붙이지 않는다.
- 실제 인증, 결제, 클라우드 동기화는 MVP 범위 밖으로 둔다.