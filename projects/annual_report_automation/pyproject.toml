[project]
name = "annual-report-automation"
version = "0.1.0"
description = "법인 연간결산 보고서 자동화 — 세무조정(4단계) 엔진 (Python)"
requires-python = ">=3.11"
# 런타임 의존성 0개 — 표준 라이브러리(decimal, csv, unittest)만 사용

[tool.pytest.ini_options]
testpaths = ["tests"]
