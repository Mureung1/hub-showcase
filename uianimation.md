# 기능 변경: UI 애니메이션 적용 (전체 인터랙션 확대 — v2)

클릭·호버가 있는 거의 모든 요소에 크고 작은 애니메이션을 적용한다.

## 배경

현재 화면은 클릭/호버 시 상태만 바뀔 뿐 시각적 피드백이 없어서 정적이고 딱딱한 인상을 준다. 마스코트, 지도 위 상태 배지(마감임박/한산해요), 마커 클릭 후 트레이에 담기는 동작, 버튼과 카드 전반에 이르기까지 — 사용자가 "뭔가를 눌렀을 때 화면이 반응한다"는 느낌을 주는 것이 이번 변경의 목표다.

## 변경 요약

- 마스코트: idle 애니메이션 + 호버 반응 (v1 범위, 유지)
- 로그인/회원가입 모달: 오픈/클로즈 전환 애니메이션 (v1 범위, 유지)
- **(신규)** 지도 위 상태 배지("마감임박"/"한산해요" 말풍선): 둥둥 떠 있는 느낌의 idle 모션
- **(신규)** 마커 클릭 → 트레이에 빵집 카드가 추가되는 순간의 등장 애니메이션 + 마커 자체의 클릭 피드백
- **(신규)** 버튼/카드/리스트 아이템 등 클릭·호버 가능한 요소 전반에 짧은 반응 애니메이션 일괄 적용
- **(신규)** 이 문서에 명시되지 않았더라도, 구현 중 애니메이션을 넣으면 좋겠다고 판단되는 지점이 있으면 자율적으로 추가 — 아래 "8) 그 외 자율 적용" 참고

## 기술 방식 결정 (반드시 이 기준으로 구분해서 적용할 것)

프로젝트 전체에 애니메이션 라이브러리를 무겁게 얹지 않기 위해, 애니메이션 성격에 따라 구현 방식을 나눈다. 새로 늘어난 항목들도 전부 이 기준을 따른다.

| 애니메이션 성격 | 방식 | 해당 예시 |
| --- | --- | --- |
| 상시 반복되는 idle 루프 | 순수 CSS `@keyframes` | 마스코트 float, 상태 배지 둥둥 모션 |
| 호버/클릭 즉시 반응 (마운트 상태 변화 없음) | 순수 CSS `:hover`/`:active` + `transition` | 마스코트 호버, 버튼 눌림, 카드 호버, 마커 클릭 바운스 |
| **컴포넌트가 나타나거나 사라지는 순간** (mount/unmount) | **Framer Motion** (`AnimatePresence`) | 모달 오픈/클로즈, 트레이에 카드가 추가/제거되는 순간 |

판단 기준은 하나다 — "요소가 화면에 생기거나 없어지는 순간을 예쁘게 보여줘야 하는가"이면 Framer Motion, "이미 떠 있는 요소가 반복되거나 반응만 하면 되는가"이면 CSS. 이 기준을 넘는 애니메이션(예: 요소가 A 지점에서 B 지점으로 실제로 이동하는 연출)은 공수 대비 효과를 먼저 판단하고, 무리라면 등장/사라짐 애니메이션으로 단순화해도 된다.

## 상세 스펙

### 1) 마스코트 idle 애니메이션 (v1 유지)

- `@keyframes mascot-float`: translateY 0→-6px, 2.8s, ease-in-out, infinite alternate
- 대상: 마스코트 SVG wrapper (`.mascot`)

### 2) 마스코트 호버 (v1 유지)

- `:hover` 시 scale(1.06) + rotate(-2deg), transition 150ms ease-out

### 3) 로그인/회원가입 모달 오픈·클로즈 (v1 유지)

- Framer Motion `AnimatePresence` + `motion.div`
- 오픈: opacity 0→1, scale 0.95→1, 200ms
- 클로즈: opacity 1→0, scale 1→0.97, 150ms
- (v1 문서에 있던 예시 코드 그대로 적용)

### 4) 지도 위 상태 배지 idle 모션 (신규)

- 대상: "마감임박"/"한산해요" 말풍선 배지 (마커 위에 클릭 없이 항상 표시되는 요소)
- 모션: 아주 미세하게 위아래로 둥둥 떠 있는 느낌 — 마스코트보다 진폭을 작게 (translateY 0→-3px), 주기도 살짝 다르게(3.2s)해서 마스코트와 박자가 겹쳐 산만해 보이지 않도록 함
- 배지가 여러 개 동시에 보일 경우, 각 배지의 애니메이션 시작 시점을 살짝 어긋나게(예: `animation-delay`를 배지 인덱스에 따라 0~1s 랜덤/순차 부여) 주어 전부 똑같이 움직이지 않게 할 것 — 여러 개가 박자를 맞춰 움직이면 오히려 기계적으로 보임
- 참고 CSS:

```css
@keyframes badge-float {
  from { transform: translateY(0); }
  to   { transform: translateY(-3px); }
}
.map-badge {
  animation: badge-float 3.2s ease-in-out infinite alternate;
}
```

### 5) 마커 클릭 → 트레이 담기 애니메이션 (신규)

두 개로 나눠서 구현한다.

- **마커 자체의 클릭 피드백**: 클릭 즉시 살짝 튀는 느낌(scale 1→1.25→1, 약 200ms). 클릭 시 클래스를 잠깐 부여했다가 애니메이션 종료 후 제거하는 방식(`animationend` 이벤트 또는 타임아웃)으로 매번 재트리거 가능하게 구현
- **트레이에 카드가 추가되는 순간**: 마운트되는 순간이므로 Framer Motion 사용. 트레이 리스트를 `AnimatePresence`로 감싸고, 각 카드(`motion.li` 또는 `motion.div`)에 `initial={{ opacity: 0, y: 12 }}`, `animate={{ opacity: 1, y: 0 }}`, `exit={{ opacity: 0, x: 20 }}` 정도로 아래에서 살짝 올라오며 나타나고, 제거될 때는 옆으로 슬라이드되며 사라지게 처리
- 참고: 트레이 카드 리스트가 이미 배열 상태(zustand `selectedIds` 등)로 관리되고 있을 것이므로, 렌더링 부분만 `AnimatePresence`/`motion` 요소로 교체하면 되고 상태 관리 로직은 변경하지 않는다

### 6) 버튼 (신규)

- 클릭 시 `:active` 상태에서 scale(0.97), transition 100ms — 눌리는 느낌
- 주요 CTA 버튼(예: "자동 추천받기", "선택함")은 호버 시 그림자(box-shadow)를 살짝 키워 존재감을 강조

### 7) 카드/리스트 아이템 (신규)

- 빵집 카드, 리스트 아이템: 호버 시 그림자 강조 + 아주 약한 translateY(-2px) 부양, transition 150ms
- 찜(하트) 아이콘: 클릭 시 짧은 바운스(scale 1→1.3→1, 200ms) — 찜 상태 변경이 눈에 띄게

### 8) 그 외 자율 적용 (지시 사항)

이 문서는 확인된 지점만 정리한 것이고, 전부는 아니다. 구현하면서 아래 기준에 맞는 지점을 발견하면 이 문서에 없어도 자율적으로 애니메이션을 추가해도 좋다.

- 클릭했을 때 "눌렀다"는 느낌이 전혀 없는 요소가 있다면 최소한 `:active` scale 정도는 넣을 것
- 리스트/카드가 나타나거나 사라지는 지점인데 이 문서에서 언급 안 된 곳이 있다면(예: 검색 필터 결과, 마이페이지 찜 목록 등) 3)·5)에 준하는 방식으로 동일하게 처리
- 페이지/화면 전환(라우팅) 자체에 전환 애니메이션을 넣을지는 별도 판단 — 공수가 크면 이번 범위에서 제외하고 코멘트로 남겨줄 것
- 다만 어떤 요소든 애니메이션 때문에 클릭 반응이 느려지거나(디바운스 없이 중복 트리거되거나) 텍스트가 읽히기 전에 사라지는 등 사용성이 나빠지는 방향으로는 넣지 말 것 — 발표 데모 중 어색해 보이는 것보다 아예 없는 게 낫다는 기준으로 판단

## 영향받는 문서/파일

- `client/package.json` — `framer-motion` 의존성 추가
- `client/src/screens/AuthModal.jsx` — 모달 렌더링 부분 Framer Motion 교체
- `client/src/components/SelectionCard.jsx`, `BakeryCard.jsx`, `RankCard.jsx` — 카드 호버/등장 애니메이션 적용 대상
- `client/src/components/NaverMapCanvas.jsx` — 마커 클릭 피드백, 상태 배지 idle 모션 적용 대상
- `client/src/styles/index.css` — 전 구간 공통 keyframe/transition 정의
- 트레이(선택 목록) 렌더링 컴포넌트 — `AnimatePresence` 적용 대상

## 결정된 세부사항

- idle/호버/클릭 즉시 반응은 CSS, 마운트·언마운트가 걸린 애니메이션(모달, 트레이 카드 추가/제거)만 Framer Motion — 이 기준은 v1과 동일하게 유지
- 배지처럼 여러 개가 동시에 존재하는 idle 애니메이션은 딜레이를 어긋나게 줘서 기계적으로 보이지 않게 할 것
- 마커 클릭 피드백과 트레이 등장 애니메이션은 서로 다른 기술(CSS/Framer Motion)로 분리 구현
- 사용성을 해치는 방향(반응 지연, 가독성 저하)의 애니메이션은 넣지 않음 — 이 문서에 없는 지점을 자율적으로 추가할 때도 동일 기준 적용

## 우선순위

부가 기능(발표 완성도 개선) 범주. 핵심 로직(로그인 유지, 마이페이지 실데이터 등 이번 주 우선순위) 작업에 지장 주지 않는 선에서, 여유 있을 때 순서대로(마스코트 → 모달 → 배지/마커/트레이 → 버튼/카드 → 자율 추가분) 진행한다.