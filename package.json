{
  "name": "hub",
  "private": true,
  "version": "0.0.0",
  "description": "MBTI×인지과학 공부법·스트레스 관리 웹앱 (frontend + backend 모노 구조)",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "install:all": "npm --prefix frontend install && npm --prefix backend install",
    "hooks:install": "git config core.hooksPath .githooks && echo '커밋 시크릿 스캔 훅 활성화됨(.githooks).'",
    "check:secrets": ".githooks/pre-commit",
    "verify:backend": "npm --prefix backend run verify"
  }
}
