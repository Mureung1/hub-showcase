# Week 1 Todo

## 이번 주 목표

3주 개발계획 기준 1주차 목표는 10단계 중 1~3단계를 끝내는 것이다.

- 1단계: 앱 입구 만들기
- 2단계: 공통 장부 만들기
- 3단계: 로그인과 역할 나누기

금요일까지 손님과 사장님이 실제 계정으로 로그인하고, 각자 맞는 화면으로 이동하며, Supabase에 만든 공통 데이터 장부에서 기본 데이터를 볼 수 있어야 한다.

## 금요일 완료 상태

- 손님 계정으로 로그인할 수 있다.
- 사장님 계정으로 로그인할 수 있다.
- 새로고침 후에도 로그인 상태가 유지된다.
- 손님은 손님 화면으로 이동한다.
- 사장님은 사장님 화면으로 이동한다.
- 역할에 맞지 않는 화면 접근은 차단된다.
- 손님은 자기 회원번호와 여러 카페의 기본 스탬프 데이터를 볼 수 있다.
- 사장님은 자기 담당 카페를 확인할 수 있다.
- 다음 주에 회원번호 조회와 스탬프 적립 기능을 붙일 수 있는 DB 구조가 준비되어 있다.

## GitHub Issue로 등록할 작업

### Issue 1. 앱 입구와 라우팅 구성

**관련 단계:** 1단계. 앱 입구 만들기

**무엇을 하나요?**

React 앱에 로그인, 손님, 사장님 화면으로 이동할 수 있는 기본 주소를 만든다.

**어떻게 하나요?**

- 기존 React 화면에서 MVP에 재사용할 화면과 제외할 화면을 구분한다.
- React Router를 설치하고 기본 라우트를 만든다.
- `/login`, `/customer`, `/owner` 주소를 연결한다.
- 없는 주소로 들어왔을 때 로그인 또는 기본 화면으로 이동하게 한다.

**연결되는 기능**

- 사용자가 앱에 들어오는 입구가 생긴다.
- 로그인 후 역할별 화면으로 이동할 기반이 생긴다.
- 새로고침해도 현재 주소에 맞는 화면이 유지된다.

**완료 기준**

- `/login`에서 로그인 화면이 열린다.
- `/customer`에서 손님 화면 기본 틀이 열린다.
- `/owner`에서 사장님 화면 기본 틀이 열린다.
- `npm run verify`가 통과한다.

### Issue 2. Supabase 연결 설정

**관련 단계:** 1단계, 2단계

**무엇을 하나요?**

프론트엔드가 Supabase Auth와 DB를 사용할 수 있도록 연결 설정을 만든다.

**어떻게 하나요?**

- Supabase JavaScript 클라이언트를 설치한다.
- `src` 안에 Supabase 클라이언트 파일을 만든다.
- `.env.example`에 `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`를 정리한다.
- 프론트엔드에 `service_role` 키를 넣지 않는다는 원칙을 확인한다.

**연결되는 기능**

- 로그인 기능을 Supabase Auth와 연결할 수 있다.
- 손님과 사장님 화면에서 실제 DB 데이터를 읽을 준비가 된다.

**완료 기준**

- 앱에서 Supabase 클라이언트를 import할 수 있다.
- 환경변수 이름이 문서화되어 있다.
- `service_role` 키가 프론트엔드에 포함되지 않는다.
- `npm run verify`가 통과한다.

### Issue 3. 공통 데이터 장부 테이블 만들기

**관련 단계:** 2단계. 공통 장부 만들기

**무엇을 하나요?**

손님 화면과 사장님 화면이 함께 사용할 Supabase 테이블을 만든다.

**어떻게 하나요?**

- `profiles` 테이블을 만든다.
- `customers` 테이블을 만든다.
- `cafes` 테이블을 만든다.
- `stamp_cards` 테이블을 만든다.
- `coupons` 테이블을 만든다.
- `notifications` 테이블을 만든다.
- 손님-카페 스탬프 카드가 중복되지 않도록 유일 제약을 둔다.
- 회원번호가 중복되지 않도록 유일 제약을 둔다.

**연결되는 기능**

- 손님은 자기 회원번호와 스탬프를 볼 수 있다.
- 사장님은 자기 담당 카페를 확인할 수 있다.
- 다음 주에 회원번호 조회, 적립, 쿠폰 발행, 알림 생성을 붙일 수 있다.

**완료 기준**

- 6개 테이블이 생성되어 있다.
- 테이블 관계와 기본 시간 필드가 준비되어 있다.
- `customers.member_number`는 중복될 수 없다.
- `stamp_cards`는 손님과 카페 조합이 중복될 수 없다.

### Issue 4. 시연용 기본 데이터 준비

**관련 단계:** 2단계. 공통 장부 만들기

**무엇을 하나요?**

1주차 기능 확인과 다음 주 적립 기능 개발에 사용할 기본 데이터를 준비한다.

**어떻게 하나요?**

- 고정 손님 계정 1개를 준비한다.
- 고정 사장님 계정 1개를 준비한다.
- 테스트 손님 1명을 만든다.
- 서로 다른 목표와 보상을 가진 카페 3개를 만든다.
- 사장님 계정을 카페 1개에 연결한다.
- 손님에게 카페별 스탬프 카드를 만든다.
- 다음 주 쿠폰 발행 테스트를 위해 담당 카페의 `stamp_goal - 1` 상태를 준비한다.

**연결되는 기능**

- 손님 화면에서 여러 카페의 스탬프 데이터를 확인할 수 있다.
- 사장님 화면에서 담당 카페를 확인할 수 있다.
- 다음 주에 일반 적립과 쿠폰 발행 시나리오를 바로 테스트할 수 있다.

**완료 기준**

- 손님 계정과 사장님 계정이 준비되어 있다.
- 카페 3개가 준비되어 있다.
- 손님에게 카페별 스탬프 카드가 있다.
- 사장님은 담당 카페 1개와 연결되어 있다.

### Issue 5. 이메일 로그인과 로그아웃 연결

**관련 단계:** 3단계. 로그인과 역할 나누기

**무엇을 하나요?**

Supabase Auth 이메일/비밀번호 로그인을 앱에 연결한다.

**어떻게 하나요?**

- 로그인 화면에 이메일, 비밀번호 입력을 연결한다.
- 로그인 버튼을 Supabase Auth 로그인 요청과 연결한다.
- 로그인 중 상태를 표시한다.
- 로그인 실패 메시지를 표시한다.
- 로그아웃 기능을 만든다.
- 새로고침 후 세션을 복원한다.

**연결되는 기능**

- 손님과 사장님이 고정 계정으로 실제 로그인할 수 있다.
- 시연 중 새로고침해도 로그인 상태가 유지된다.

**완료 기준**

- 올바른 손님 계정으로 로그인할 수 있다.
- 올바른 사장님 계정으로 로그인할 수 있다.
- 잘못된 비밀번호는 오류를 보여준다.
- 로그아웃할 수 있다.
- 새로고침 후 세션이 유지된다.

### Issue 6. 역할별 화면 이동과 접근 보호

**관련 단계:** 3단계. 로그인과 역할 나누기

**무엇을 하나요?**

로그인한 사용자의 역할에 따라 손님 화면 또는 사장님 화면으로 이동하게 만든다.

**어떻게 하나요?**

- 로그인 후 `profiles.role`을 조회한다.
- `customer` 역할은 `/customer`로 이동시킨다.
- `owner` 역할은 `/owner`로 이동시킨다.
- 비로그인 사용자는 보호 화면에 접근하지 못하게 한다.
- 손님이 사장님 화면에 접근하지 못하게 한다.
- 사장님이 손님 전용 화면에 접근하지 못하게 한다.

**연결되는 기능**

- 같은 로그인 화면을 쓰더라도 손님과 사장님이 각자 맞는 화면으로 들어간다.
- 다음 주 사장님 적립 기능을 안전하게 붙일 수 있다.

**완료 기준**

- 손님 로그인 후 손님 화면으로 이동한다.
- 사장님 로그인 후 사장님 화면으로 이동한다.
- 비로그인 사용자가 `/customer`, `/owner`에 직접 접근하면 차단된다.
- 역할에 맞지 않는 화면 접근이 차단된다.

### Issue 7. 손님 기본 스탬프 데이터 조회

**관련 단계:** 4단계 일부. 손님이 자기 회원번호와 스탬프 보기

**무엇을 하나요?**

손님 화면에서 실제 DB에 있는 회원번호와 카페별 스탬프 데이터를 보여준다.

**어떻게 하나요?**

- 로그인한 손님의 `profiles.customer_id`를 확인한다.
- `customers`에서 손님 이름과 회원번호를 읽는다.
- `stamp_cards`와 `cafes`를 함께 조회한다.
- 카페명, 현재 스탬프, 목표 스탬프, 보상명을 표시한다.
- 로딩 상태와 데이터가 없을 때의 상태를 표시한다.

**연결되는 기능**

- 손님이 자기 회원번호를 확인할 수 있다.
- 손님이 여러 카페의 현재 스탬프 상태를 볼 수 있다.
- 다음 주 사장님 적립 후 손님 새로고침 반영 기능의 기반이 된다.

**완료 기준**

- 손님 화면에 회원번호가 표시된다.
- 손님 화면에 여러 카페의 스탬프가 표시된다.
- 카페별 목표와 보상이 표시된다.
- 데이터 조회 중 로딩 상태가 보인다.

### Issue 8. 사장님 담당 카페 조회

**관련 단계:** 3단계 완료, 5단계 준비

**무엇을 하나요?**

사장님 화면에서 로그인한 사장님의 담당 카페를 보여준다.

**어떻게 하나요?**

- 로그인한 사장님의 `profiles.cafe_id`를 확인한다.
- `cafes`에서 담당 카페 정보를 읽는다.
- 카페명, 목표 스탬프, 보상명을 표시한다.
- 담당 카페가 없을 때의 오류 상태를 표시한다.

**연결되는 기능**

- 사장님이 자기 담당 카페를 확인할 수 있다.
- 다음 주 회원번호 조회와 적립 기능에서 담당 카페를 기준으로 처리할 수 있다.

**완료 기준**

- 사장님 화면에 담당 카페명이 표시된다.
- 담당 카페의 목표 스탬프와 보상이 표시된다.
- 담당 카페가 없으면 명확한 오류가 표시된다.

### Issue 9. 1주차 완료 검증

**관련 단계:** 1~3단계 완료 검증

**무엇을 하나요?**

이번 주 완료 조건이 실제로 동작하는지 확인한다.

**어떻게 하나요?**

- 손님 계정으로 로그인한다.
- 사장님 계정으로 로그인한다.
- 새로고침 후 세션 유지 여부를 확인한다.
- 손님 화면에서 회원번호와 스탬프 데이터가 보이는지 확인한다.
- 사장님 화면에서 담당 카페가 보이는지 확인한다.
- 역할에 맞지 않는 화면 접근이 차단되는지 확인한다.
- 모바일 320~480px에서 손님 화면 기본 사용성을 확인한다.
- 1024px 이상에서 사장님 화면 기본 사용성을 확인한다.
- `npm run verify`를 실행한다.

**연결되는 기능**

- 다음 주에 회원번호 조회와 스탬프 적립 기능을 붙일 수 있는지 판단한다.

**완료 기준**

- 손님과 사장님 모두 로그인할 수 있다.
- 새로고침 후 세션이 유지된다.
- 역할별 화면 보호가 동작한다.
- 손님 기본 데이터 조회가 동작한다.
- 사장님 담당 카페 조회가 동작한다.
- `npm run verify`가 통과한다.

## 요일별 배치

### 월요일: 앱 입구 만들기

**예상 시간:** 약 5시간

- Issue 1. 앱 입구와 라우팅 구성
- Issue 2 일부. Supabase 연결 설정 준비

**하루 끝 상태**

- `/login`, `/customer`, `/owner` 기본 화면이 열린다.
- 기존 데모 화면 중 MVP에 살릴 부분과 제외할 부분이 정리되어 있다.

### 화요일: 공통 장부 만들기

**예상 시간:** 약 5시간

- Issue 2. Supabase 연결 설정
- Issue 3. 공통 데이터 장부 테이블 만들기

**하루 끝 상태**

- 앱에서 Supabase에 연결할 준비가 되어 있다.
- 6개 핵심 테이블 구조가 준비되어 있다.

### 수요일: 시연 데이터 준비

**예상 시간:** 약 5시간

- Issue 4. 시연용 기본 데이터 준비

**하루 끝 상태**

- 손님 계정, 사장님 계정, 카페 3개, 스탬프 카드, 담당 카페 연결이 준비되어 있다.
- 다음 주 적립 기능을 테스트할 수 있는 기본 데이터가 있다.

### 목요일: 로그인과 역할 나누기

**예상 시간:** 약 5시간

- Issue 5. 이메일 로그인과 로그아웃 연결
- Issue 6. 역할별 화면 이동과 접근 보호

**하루 끝 상태**

- 손님과 사장님이 로그인할 수 있다.
- 로그인 후 각자 맞는 화면으로 이동한다.
- 새로고침 후 세션이 유지된다.
- 역할에 맞지 않는 화면 접근이 차단된다.

### 금요일: 기본 조회와 완료 검증

**예상 시간:** 약 5시간

- Issue 7. 손님 기본 스탬프 데이터 조회
- Issue 8. 사장님 담당 카페 조회
- Issue 9. 1주차 완료 검증

**하루 끝 상태**

- 손님은 자기 회원번호와 여러 카페의 기본 스탬프 데이터를 볼 수 있다.
- 사장님은 자기 담당 카페를 볼 수 있다.
- 1주차 완료 조건을 확인했다.

## GitHub Project 대시보드 추천 컬럼

- Todo
- In Progress
- Review / Check
- Done

처음에는 모든 이슈를 Todo에 두고, 작업하는 이슈만 In Progress로 옮긴다. 구현이 끝나면 Review / Check로 옮겨 검증하고, 완료 기준을 만족하면 Done으로 옮긴다.
