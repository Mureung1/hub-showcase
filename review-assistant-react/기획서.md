# 🍊 리뷰 매니저 AI — 기획서

> 2026-07-13: "리뷰 답변 도우미"(답변 초안 생성 중심)에서 "리뷰 매니저 AI"(분석·점수·통계까지 포함하는 리뷰 관리 도구)로 컨셉을 확장했다. 답변 초안 생성 기능은 그대로 유지하고, 그 위에 AI 점수·총 분석·월별 통계를 더했다.

## 0. 프로젝트 개요
| 항목 | 내용 |
|---|---|
| 서비스명 | 리뷰 매니저 AI (구 "리뷰 답변 도우미") |
| 대상 사용자 | 매일 손님 리뷰를 확인하고 답변을 다는 소상공인(카페, 식당 등) |
| 문제 정의 | 리뷰마다 톤을 고민하고 직접 답변을 쓰는 데 시간이 많이 든다. 반복되는 불만(대기시간, 친절도 등)을 놓치기 쉽고, 어떤 리뷰부터 먼저 챙겨야 할지 판단하기 어렵다. |
| 핵심 가치 | 리뷰를 붙여넣기만 하면 (1) 감정/키워드 분석, (2) 리뷰마다 AI 관심도 점수, (3) 답변 초안 3종, (4) 반복 문제 감지·개선 제안, (5) 누적 리뷰의 총 분석·월별 통계까지 한 번에 제공 |
| 차별점 | 단발성 "답장 생성기"가 아니라, 리뷰가 쌓일수록 반복 문제·점수·통계로 가게 상태를 파악하게 해주는 "관리" 도구 |
| 현재 구현 범위 | React(프론트) + Express(백엔드) 연동 완료. 분석 로직은 규칙 기반, 저장은 `node:sqlite`. Claude API 연동은 3주차 예정 |

---

## 0-1. 프로토타입

- 순수 HTML/CSS/JS로 제작한 핵심 기능 프로토타입: [index.html](../index.html)
- 외부 API 없이 브라우저 내 키워드 규칙으로 감정 분석·답변 생성이 동작하며, 아래 "핵심 기능" 목록과 1:1로 대응합니다.
- 동료 피드백 반영 이력은 이 섹션에 계속 업데이트합니다. (현재: 초안, 피드백 대기 중)


---

## 1. 사용자 관점 동작 시나리오

### 1-1. 메인 플로우
| 단계 | 사용자 행동 | 화면 반응 |
|---|---|---|
| 1 | 랜딩 페이지(`/`) 진입 | Hero에서 차별점("리뷰를 관리하면 매장의 문제가 보입니다") 확인, 서비스 소개·핵심 기능·대시보드 미리보기·사용 과정·분석 예시·월별 통계·후기 섹션 스크롤로 확인 |
| 2 | "지금 시작하기" 또는 nav의 "바로 사용하기" 클릭 | `/app`(도구 화면)으로 실제 페이지 이동 |
| 3 | nav의 "사용법" 클릭 | `/guide`(사용법 화면)으로 이동, 예시 결과·FAQ 확인 |
| 4 | 리뷰 텍스트를 줄바꿈으로 구분해 붙여넣기 (`/app`) | textarea에 입력, placeholder 예시로 입력 형식 안내 |
| 5 | "분석 시작" 버튼 클릭 | 버튼 비활성화 + "분석 중..." 표시, 백엔드(`POST /api/v1/reviews/analyze`) 호출 |
| 6 | 분석 완료 | 리뷰별 카드가 리스트로 렌더링 |
| 7 | 결과 카드 확인 | 감정 태그(긍정/부정/중립 색상), AI 관심도 점수, 키워드 태그, (부정일 경우) 개선 제안, 답변 초안 3종(정중함/친근함/간결함) 표시 |
| 8 | 마음에 드는 답변의 "복사하기" 클릭 | 클립보드 복사 + 버튼이 "복사됨 ✓"로 1.5초간 변경 |
| 9 | (반복 시) 같은 키워드의 부정 리뷰가 세션 누적 2건 이상 | 상단에 "⚠️ 반복되는 문제 감지" 패널 자동 노출, 키워드별 개선 제안 표시 |
| 10 | 필요 시 "누적 기록 초기화" 클릭 | 서버에 저장된 세션 히스토리 초기화 (현재 화면의 결과는 유지) |
| 11 | "총 분석 보기" 클릭 | `/dashboard`로 이동, 지금까지 분석한 리뷰의 총 건수·감정 분포·평균 점수·자주 언급된 키워드·월별 통계 확인 |

### 1-2. 예외 시나리오
| 상황 | 처리 |
|---|---|
| 입력 없이 "분석 시작" 클릭 | "리뷰를 먼저 입력해주세요." 에러 메시지 |
| 공백/줄바꿈만 입력 | "유효한 리뷰가 없어요." 에러 메시지 |
| 리뷰 15개 초과 입력 | "한 번에 최대 15개까지 분석할 수 있어요." 에러 메시지, 분석 미실행 |
| 클립보드 복사 실패 (권한 등) | "복사에 실패했어요. 직접 선택해서 복사해주세요." 에러 메시지 |
| 아직 분석한 리뷰가 없는 상태로 `/dashboard` 진입 | "아직 분석한 리뷰가 없어요" 빈 상태 문구 + `/app`로 가는 링크 |
| 통계 API 호출 실패 | 대시보드에 에러 메시지 노출 |

---

## 2. 화면 구조 (UI)

### 2-1. 전체 라우트 구조

React Router로 4개 화면을 분리했다(더 이상 앵커 스크롤 단일 페이지가 아니다).

| 경로 | 화면 | 구성 |
|---|---|---|
| `/` | 랜딩 | Navbar(사용법·총 분석·로그인/회원가입 버튼) → Hero(차별점 카피 + 미니 앱 미리보기 카드) → 반복 문제 감지 데모 → 수치(리뷰 15개/키워드 6종/톤 3종 카운트업) → 공감 포인트 3개 → 예시 답변 카드 → 3단계 사용법 → 타겟 업종 → Footer |
| `/app` | 도구(핵심 화면) | Navbar(총 분석 보기·홈으로) → 입력 카드 → (조건부) 상태/에러 메시지 → (조건부) 반복 문제 감지 패널 → 결과 리스트 |
| `/guide` | 사용법 | Navbar → Hero(사용법 설명) → 3단계 안내 → 예시 결과 카드 → FAQ → CTA |
| `/dashboard` | 총 분석 | Navbar → 제목/부제 → 수치(분석한 리뷰 수·평균 관심도 점수) → 감정 분포 → 자주 언급된 키워드 → 월별 통계 테이블 |

`/app` 화면(도구 영역, `id="tool"`) 상세:
```
┌───────────────────────────────────┐
│ [입력 카드]                         │
│  textarea (리뷰 여러 줄 붙여넣기)      │
│  hint: 줄바꿈으로 구분                │
│  [분석 시작] 버튼                     │
└───────────────────────────────────┘
(조건부) 상태 메시지 / 에러 메시지
(조건부) ⚠️ 반복되는 문제 감지 패널
    - 리셋 버튼
    - 키워드별: 발생건수 + 개선제안
[결과 리스트]
 ┌─────────────────────────────────┐
 │ 리뷰 카드 (색상 = 감정)              │
 │  "리뷰 원문"                       │
 │  [감정 태그] [관심도 점수] [키워드]     │
 │  💡 개선 제안 (부정일 때만)            │
 │  답변 옵션: 정중함 / 친근함 / 간결함      │
 │    각 옵션 [복사하기] 버튼             │
 └─────────────────────────────────┘
 ... (리뷰 개수만큼 반복) ...
```

### 2-2. 상태별 화면 분기
| 상태 | 노출 요소 |
|---|---|
| 초기 (results === null) | 입력 카드만 표시 |
| 로딩 중 | 입력 카드 + 상태 메시지("분석 중이에요...") |
| 에러 | 입력 카드 + 에러 박스 |
| 결과 있음 | 리뷰 카드 리스트 (입력 검증을 통과하면 최소 1건 이상 반환되므로 "결과 0건" 상태는 없음) |
| 반복 문제 감지됨 | 결과 리스트 위에 경고 패널 추가 노출 |

### 2-3. 감정별 색상 규칙
| 감정 | 색상 |
|---|---|
| 긍정 | 초록 |
| 부정 | 빨강 |
| 중립 | 회색 |

---

## 3. 핵심 기능 (우선순위 순)

### ① 리뷰 일괄 입력 & 파싱 — [핵심]
- 여러 리뷰를 줄바꿈으로 구분해 한 번에 입력
- 빈 줄 제거, 최대 15개 제한
- 입력값 검증 실패 시 명확한 에러 메시지

### ② 감정 분석 (긍정/부정/중립) — [핵심]
- 키워드 매칭 기반 규칙 엔진 (POSITIVE_WORDS / NEGATIVE_WORDS)
- "불친절"처럼 부정 접두사(불/안/못)가 붙은 경우 긍정 오탐 방지 처리
- 매칭 점수 비교로 최종 감정 판정

### ③ 핵심 키워드 추출 — [핵심]
- 맛/친절도/대기시간/가격/청결도/분위기 6개 카테고리
- 리뷰당 최대 3개 키워드, 매칭 없으면 "일반"

### ④ 답변 초안 3종 생성 — [핵심]
- 감정×키워드 조합으로 정중함/친근함/간결함 3가지 톤 생성
- 리뷰별로 즉시 확인·비교 가능

### ⑤ 답변 클립보드 복사 — [핵심]
- 버튼 클릭 시 즉시 복사, 1.5초간 "복사됨 ✓" 피드백
- 실패 시 에러 안내

### ⑥ 반복 문제 감지 & 개선 제안 — [핵심]
- 세션 누적 히스토리 기준, 동일 키워드의 부정 리뷰가 2건 이상이면 자동 노출
- 키워드별 구체적 개선 제안 문구 제공 (예: 대기시간 → "피크타임 인력 배치 조정")
- "누적 기록 초기화"로 리셋 가능

### ⑦ 에러/예외 처리 — [핵심]
- 빈 입력, 15개 초과, 클립보드 실패, 통계 조회 실패 케이스 커버

### ⑧ 리뷰마다 AI 관심도 점수 — [핵심, 신규]
- 리뷰마다 0~100점의 "관심 필요도" 점수를 함께 반환 (긍정 낮음 → 부정+복수 키워드 높음)
- 결과 카드에 배지로 노출, 어떤 리뷰부터 먼저 챙길지 판단하는 기준으로 사용
- 현재는 감정·키워드 개수 기반 규칙 점수, 3주차 Claude API 연동 시 점수 산정 방식 고도화 검토

### ⑨ 리뷰 총 분석 — [핵심, 신규]
- 세션 누적 기준 총 리뷰 수, 감정 분포(긍정/부정/중립), 평균 관심도 점수, 자주 언급된 키워드 top 5를 `/dashboard`에서 확인
- 아직 분석한 리뷰가 없으면 빈 상태 안내 + 도구 화면으로 이동하는 링크 제공

### ⑩ 월별 통계 — [핵심, 신규]
- 세션 누적 리뷰를 분석 시각(월 단위)로 묶어 월별 총 건수·평균 점수·부정 리뷰 건수 표시
- 세션이 유지되는 한(로그인 없이도 localStorage의 세션ID 기준) 누적됨

> 서브 기능(파일 업로드, 톤 커스터마이징, 실제 AI API 연동 등)은 위 핵심 기능이 안정된 뒤 논의할 "향후 개선 아이디어"로 별도 관리합니다.

---

## 4. 향후 확장 (핵심 기능 이후)
- 백엔드 분석 로직을 Claude API 기반 실제 AI 분석으로 교체 (3주차 예정, AI 관심도 점수 산정 방식 고도화 포함)
- 리뷰 CSV 업로드 지원
- 가게별 답변 톤 커스터마이징
- ~~기간별 감정 추이 통계 대시보드~~ → 2주차 중 "리뷰 매니저 AI" 컨셉 확장으로 조기 구현 완료 (`/dashboard`, 3번 항목 참고)

---

## 5. API 설계 — 리뷰 분석 · 답변 초안 · 총 분석 · 월별 통계

`POST /api/v1/reviews/analyze` — 리뷰 텍스트 배열(최대 15개)을 요청으로 받아, 리뷰별 감정(긍정/부정/중립)·키워드·AI 관심도 점수·답변 초안 3종을 응답으로 반환한다.
분석된 리뷰는 세션(`X-Session-Id`) 기준으로 `node:sqlite` DB에 저장되며, 동일 세션에서 같은 키워드의 부정 리뷰가 2건 이상 누적되면 응답에 반복 문제 감지 정보를 함께 포함한다.
입력이 비어있거나 15개를 초과하면 `400` 에러를, 분석 엔진 오류 시 `500` 에러를 코드와 함께 반환한다.
누적 기록은 `DELETE /api/v1/reviews/history`로 초기화할 수 있고, 누적된 리뷰는 `GET /api/v1/stats/summary`(총 분석)와 `GET /api/v1/stats/monthly`(월별 통계)로 조회한다.

### 5-1. 엔드포인트 개요
| 항목 | 내용 |
|---|---|
| Method / Path | `POST /api/v1/reviews/analyze` |
| 설명 | 리뷰 원문 배열을 받아 리뷰별 감정·키워드·개선 제안·답변 초안 3종을 반환하고, 세션 누적 기준 반복 문제도 함께 감지한다 |
| 인증 | `X-Session-Id` 헤더 (로그인 없는 익명 세션. 반복 문제 감지용 누적 히스토리를 세션 단위로 서버가 보관) |
| Idempotent 여부 | 아니오 (호출 시마다 해당 세션의 누적 히스토리에 반영되므로 부작용 있음) |

### 5-2. Request

```json
POST /api/v1/reviews/analyze
X-Session-Id: 6f1b2e3a-...

{
  "reviews": [
    "음식은 맛있었는데 너무 오래 기다렸어요.",
    "직원분이 너무 불친절했어요."
  ]
}
```

| 필드 | 타입 | 필수 | 제약 |
|---|---|---|---|
| `reviews` | string[] | Y | 최소 1개, 최대 15개. 각 항목은 공백 제거 후 1자 이상 |

### 5-3. Response `200 OK`

```json
{
  "results": [
    {
      "reviewId": "r_01",
      "originalText": "음식은 맛있었는데 너무 오래 기다렸어요.",
      "sentiment": "negative",
      "keywords": ["대기시간", "맛"],
      "score": 80,
      "improvementSuggestion": "피크타임 인력 배치 조정을 검토해보세요.",
      "replyDrafts": {
        "polite": "소중한 의견 감사합니다. 대기 시간으로 불편을 드려 죄송합니다...",
        "friendly": "앗, 많이 기다리셨죠 ㅠㅠ 더 신경쓸게요!",
        "concise": "대기시간 개선하겠습니다. 감사합니다."
      }
    }
  ],
  "recurringIssues": [
    {
      "keyword": "대기시간",
      "occurrenceCount": 3,
      "suggestion": "피크타임 인력 배치 조정을 검토해보세요."
    }
  ]
}
```

| 필드 | 타입 | 설명 |
|---|---|---|
| `results[].reviewId` | string | 요청 내 순번 기반 식별자 |
| `results[].sentiment` | enum | `positive` \| `negative` \| `neutral` |
| `results[].keywords` | string[] | 최대 3개, 매칭 없으면 `["일반"]` |
| `results[].score` | number | AI 관심도 점수 0~100. 긍정 10 / 중립 40 / 부정 70(+매칭 키워드 수에 따라 최대 +20) |
| `results[].improvementSuggestion` | string \| null | 부정 리뷰가 아니면 `null` |
| `results[].replyDrafts` | object | `polite` / `friendly` / `concise` 3종 고정 키 |
| `recurringIssues` | array | 세션 누적 기준 동일 키워드의 부정 리뷰가 2건 이상일 때만 포함, 없으면 빈 배열 |

### 5-4. 에러 응답

| 상태 코드 | `code` | 상황 | 메시지 예시 |
|---|---|---|---|
| 400 | `EMPTY_INPUT` | `reviews`가 빈 배열이거나 누락 | "리뷰를 먼저 입력해주세요." |
| 400 | `NO_VALID_REVIEW` | 공백/줄바꿈만 있는 항목뿐 | "유효한 리뷰가 없어요." |
| 400 | `TOO_MANY_REVIEWS` | 15개 초과 | "한 번에 최대 15개까지 분석할 수 있어요." |
| 400 | `INVALID_JSON` | 요청 바디가 올바른 JSON이 아님(파싱 실패) | "요청 형식이 올바르지 않아요." |
| 500 | `ANALYSIS_FAILED` | 분석 엔진(추후 Claude API) 오류 | "잠시 후 다시 시도해주세요." |

에러 바디 형식은 공통 규격을 따른다.
```json
{ "error": { "code": "TOO_MANY_REVIEWS", "message": "한 번에 최대 15개까지 분석할 수 있어요." } }
```

### 5-5. 부가 엔드포인트 — 누적 기록 초기화
| 항목 | 내용 |
|---|---|
| Method / Path | `DELETE /api/v1/reviews/history` |
| 설명 | 해당 `X-Session-Id`의 저장된 리뷰 히스토리(반복 문제 감지·총 분석·월별 통계가 모두 이 히스토리를 사용)를 초기화 (현재 표시 중인 분석 결과에는 영향 없음, 기획서 1-1 10단계와 대응) |
| 응답 | `204 No Content` |

### 5-6. 리뷰 총 분석

| 항목 | 내용 |
|---|---|
| Method / Path | `GET /api/v1/stats/summary` |
| 설명 | 해당 `X-Session-Id`가 지금까지 분석한 리뷰를 집계해 총 건수·감정 분포·평균 관심도 점수·자주 언급된 키워드 top 5를 반환 |
| 인증 | `X-Session-Id` 헤더 |

Response `200 OK`:
```json
{
  "totalReviews": 12,
  "sentimentBreakdown": { "positive": 5, "negative": 4, "neutral": 3 },
  "averageScore": 42,
  "topKeywords": [
    { "keyword": "대기시간", "count": 4 },
    { "keyword": "맛", "count": 3 }
  ]
}
```
리뷰가 하나도 없으면 `totalReviews: 0`, `topKeywords: []`로 응답한다(에러 아님).

### 5-7. 월별 통계

| 항목 | 내용 |
|---|---|
| Method / Path | `GET /api/v1/stats/monthly` |
| 설명 | 해당 `X-Session-Id`의 분석 리뷰를 분석 시각(월 단위, `YYYY-MM`)으로 묶어 월별 총 건수·감정별 건수·평균 관심도 점수를 반환 |
| 인증 | `X-Session-Id` 헤더 |

Response `200 OK`:
```json
{
  "months": [
    {
      "month": "2026-07",
      "totalReviews": 12,
      "positive": 5,
      "negative": 4,
      "neutral": 3,
      "averageScore": 42
    }
  ]
}
```
데이터가 없는 달은 배열에 포함되지 않는다(월을 0으로 채워 내려주지 않음).
