# tax-agent 작동 설계 설명 (비전공자용)

이 문서는 "세금계산서 발행 자동화 에이전트"를 어떤 구조로 만들기로 했는지, 그리고 왜 그렇게 정했는지를 설명합니다. 전문 용어가 나올 때마다 괄호 안에 쉬운 말로 뜻을 풀어씁니다.

---

## 1. 한 줄 요약

**"사람이 은행 인증서로 딱 한 번만 로그인해두면, 그 다음부터는 프로그램이 국세청 홈택스 사이트에 자동으로 들어가서 세금계산서 발행을 대신 진행하고, 실제로 제출(발행)하기 직전에만 사람에게 마지막으로 확인을 받는 시스템."**

---

## 2. 미리 알아두면 좋은 용어

| 용어 | 뜻 |
|---|---|
| **API** (Application Programming Interface) | 서로 다른 프로그램끼리 정해진 방식으로 "대화"할 수 있게 해주는 창구. 예: "Claude API를 부른다" = "AI 회사(Anthropic) 서버에 질문을 보내고 답을 받는다"는 뜻. |
| **LLM** (Large Language Model) | Claude 같은 대형 언어 AI 모델 자체를 가리키는 말. |
| **에이전트(Agent)** | 사람이 매 단계 시키지 않아도, AI가 스스로 "다음에 뭘 해야 할지" 판단하면서 여러 단계를 이어서 실행하는 프로그램. |
| **SDK** (Software Development Kit) | 어떤 서비스(API)를 개발자가 더 쉽게 쓰도록 미리 만들어놓은 코드 도구 묶음. |
| **툴 콜링(Tool Use / Tool Calling)** | AI가 "지금은 이 기능을 실행해야겠다"고 판단해서 요청만 하고, 실제 실행(계산, 조회 등)은 일반 프로그램 코드가 담당하는 방식. AI가 직접 컴퓨터를 조작하는 게 아니라, "이거 실행해줘"라고 부탁하는 구조. |
| **Playwright** | 사람이 브라우저로 클릭/입력하는 걸 코드로 자동화해주는 도구. 이번 프로젝트에서 홈택스 화면을 자동으로 조작하는 데 씀. |
| **헤드(head) 모드 / 헤드리스(headless) 모드** | 자동화 브라우저를 화면에 실제로 띄워서 보여줄지(헤드), 안 보이게 백그라운드로만 돌릴지(헤드리스)의 차이. |
| **세션(session) / storageState** | 로그인하면 생기는 "이미 로그인된 상태" 정보(쿠키 등)를 파일로 저장해둔 것. 이 파일만 있으면 아이디/비밀번호를 다시 안 치고도 로그인된 상태를 재현할 수 있음. |
| **금융인증서** | 은행에서 발급하는 본인 확인용 전자 인증서. 세금계산서를 실제로 "발행"하려면 개인용이 아니라 **사업자용** 인증서가 있어야 함(실측 확인됨). |
| **클로드 코드(Claude Code)** | 지금 이 대화를 하고 있는 바로 이 도구 — 개발자가 터미널에서 코딩을 도와받는 **개발용 CLI(명령줄 도구)**. 완성된 서비스가 실행되는 엔진이 아님. |
| **Claude Agent SDK** | 클로드 코드를 다른 프로그램 안에 라이브러리처럼 넣어서 쓸 수 있게 만든 공식 제품. 클로드 코드와는 별개 상품. |
| **Tool Runner** | Claude API의 SDK 안에 들어있는 도우미 기능. "AI에게 물어보기 → AI가 도구 실행을 요청 → 실제 실행 → 결과를 다시 AI에게 전달"하는 반복 과정을 자동으로 돌려줌. |
| **LangChain / LangGraph** | 여러 AI 호출을 조합해서 복잡한 흐름(그래프, 체인)을 만들 때 쓰는 서드파티(제3자) 프레임워크. |

---

## 3. 검토했던 다른 방향들과 최종 선택 이유

에이전트를 만드는 방법으로 세 가지를 검토했습니다.

### 검토 A — LangChain / LangGraph로 직접 구축
**채택 안 함.** 이번 에이전트가 쓰는 도구는 4개뿐입니다(거래처 조회, 최근 매입내역 조회, 공제 여부 판단, 세금계산서 초안 작성). 게다가 판단 로직 자체를 AI가 아니라 일반 코드(결정론적 규칙)로 짜기로 했습니다. 이 정도 규모에서는 Claude API SDK에 이미 들어있는 **Tool Runner**만으로 충분합니다. LangChain/LangGraph는 훨씬 복잡한 다중 에이전트 그래프나 대량의 외부 데이터 연동이 필요할 때 값어치를 하는데, 지금 케이스엔 그 정도 복잡도가 없어서 오히려 불필요한 무게만 늘립니다.

### 검토 B — "클로드 코드"를 완성된 서비스에서 계속 실행
**개념 자체가 성립하지 않음.** 클로드 코드는 개발자가 코드를 짤 때 옆에서 도와주는 도구지, 완성된 서비스의 실행 엔진이 아닙니다. 실제로 세금계산서를 발행하는 프로그램은 별도의 서버(백엔드)가 **Claude API를 직접 호출**하는 구조로 처음부터 설계돼 있었습니다. 클로드 코드를 "계속 돌린다"는 상황 자체가 원래 계획에 없었습니다.

### 검토 C — 유출된 클로드 코드 껍데기 + API 연결
**채택 안 함, 권장하지도 않음.** 두 가지 이유가 있습니다.
1. 유출/역설계된 코드 위에 뭔가를 만드는 건 법적으로 찜찜합니다.
2. 애초에 그럴 필요가 없습니다. Anthropic이 **Claude Agent SDK**를 공식으로 이미 배포하고 있어서, 유출본 없이도 같은 종류의 기능을 정식으로 쓸 수 있습니다. 그런데 이 Agent SDK조차 "파일 읽기/쓰기, 터미널 명령 실행" 같은 범용 코딩 기능을 위한 거라, 세금계산서 발행처럼 도구 몇 개짜리 좁은 에이전트에는 오버스펙입니다.

### 최종 선택
**Claude API(회사 서버에 직접 질문/응답하는 창구) + Tool Runner(SDK에 내장된 반복 루프 도우미)**를 자체 백엔드 서버 안에서 호출하는 구조. 별도 프레임워크도, 클로드 코드 관련 우회도 필요 없습니다.

---

## 4. 기술 스택 한눈에

| 영역 | 선택한 것 | 왜 |
|---|---|---|
| 백엔드 서버 | Node.js + Express (자바스크립트로 서버를 만드는 표준 조합) | 가볍고, 팀이 이미 익숙한 스택 |
| AI 호출 | Claude API (Anthropic 공식 API) + Tool Runner | 위 3번 항목 참고 |
| 홈택스 자동 조작 | Playwright | 사람이 클릭하듯 브라우저를 코드로 조작 |
| 로그인 유지 | 금융인증서 최초 1회 수동 로그인 + storageState 재사용 | 세션 파일만 있으면 재인증 없이 재로그인 가능함을 실측으로 확인 |
| 데이터 저장 | SQLite (파일 하나로 동작하는 가벼운 데이터베이스) | 개인 프로젝트 규모에 적합, 별도 서버 불필요 |
| 실시간 진행상황 전달 | SSE(Server-Sent Events, 서버가 실시간으로 진행 상황을 클라이언트에 흘려보내는 방식) | 에이전트가 지금 뭘 하고 있는지 화면에 실시간으로 보여주기 위함 |

---

## 5. 전체 작동 흐름

```
[사람] 은행 앱에서 사업자용 금융인증서 발급
   ↓ (최초 1회만, 사람이 직접)
[사람] Playwright로 띄운 브라우저에서 인증서로 직접 로그인 + "자동연결" 설정
   ↓
[프로그램] 로그인된 상태(storageState)를 파일로 저장
   ↓ (이후부터는 완전 자동, 재인증 없음 — 같은 PC 기준 실측 확인됨)
[프로그램] 저장된 상태를 재사용해 홈택스에 다시 접속 → 로그인 화면 건너뜀
   ↓
[AI 에이전트 루프 시작]
   1. "거래처 사업자번호가 유효한가?" → 조회 도구 실행 (공공데이터 API)
   2. "이 지출이 공제 대상인가?" → 판단 규칙(코드) 실행
   3. "세금계산서 초안을 만들어줘" → 금액 계산(공급가액/세액) 도구 실행
   4. AI가 각 단계 결과를 자연어로 설명 (이 부분만 AI가 담당, 판단 자체는 코드가 함)
   ↓
[미리보기 화면] 발행 전 최종 내용을 사람에게 보여줌
   ↓
[확인 게이트] 사람이 명시적으로 "발급 확인" 버튼/문구를 눌러야만 다음 단계 진행
   ↓
[실제 제출] 되돌릴 수 없는 법적 문서이므로, 이 단계는 절대 AI가 임의로 실행하지 않음
```

---

## 6. 안전장치 요약

- **비밀번호/인증서 정보는 AI(저)에게 전혀 노출되지 않음** — 사람이 직접 입력하고, 프로그램은 로그인 이후 상태만 재사용.
- **핵심 판단(공제 여부, 금액 계산)은 AI가 아니라 일반 코드가 담당** — AI는 그 결과를 사람이 이해하기 쉬운 말로 설명하는 역할만 함. AI가 판단 결과를 마음대로 숨기거나 바꿀 수 없음.
- **되돌릴 수 없는 행동(실제 발행 제출)은 반드시 사람의 명시적 확인을 거침** — 자동화는 "제출 직전까지"이고, 마지막 버튼은 사람이 누름.
- **모든 실제 홈택스 조작 기록은 감사 로그로 남김** — 나중에 무슨 일이 있었는지 추적 가능.
