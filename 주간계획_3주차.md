# 이번 주 계획 — 주변 경쟁업체 DB 연동 (3주차)

## 이번 주 목표 (금요일까지)

**"내 가게명을 입력하면, 네이버 API로 주변 경쟁업체를 찾아 DB에 저장하고, 새로고침해도 그 목록이 그대로 남아 있다."**

지금까지 목 데이터로만 돌던 화면을 실제 DB와 연결하는 것이 핵심입니다. 리뷰 분석(Gemini)은 이번 주 범위에서 제외하고, 다음 주에 연결합니다.

## 방향 결정 사항

| 항목 | 결정 |
|---|---|
| DB | FastAPI + MySQL 유지 (전환 안 함) |
| 데이터 수집 | 네이버 공식 API만 사용 (지역검색 + 블로그검색) |
| 크롤링/비공식 API | 사용하지 않음 (약관 위반 리스크) |
| 리뷰 분석(Gemini) | 이번 주 범위 제외, USE_MOCK으로 대체 |
| 거리 계산 | Haversine 공식 (Python 표준 계산, 외부 도구 불필요) |
| 비용 | 네이버 API는 무료 할당량 내로 충분, Gemini만 별도 관리 |

## 핵심 시나리오 (수직 슬라이스)

```
[React] 내 가게명 입력 → 검색
   ↓ POST /api/my-store/nearby-competitors
[FastAPI] 네이버 지역검색 API로 내 가게 좌표 조회
   ↓
[FastAPI] 주변 같은 업종 업체 검색 + Haversine 거리 계산 + 2km 필터
   ↓
[MySQL] 검색 결과 저장 (중복 방지)
   ↓
[React] 저장된 목록을 화면에 표시
   ↓ (새로고침)
[React] GET으로 DB에서 다시 조회 → 그대로 남아있음 확인
```

## 요일별 작업 배치

| 요일 | 작업 | 완료 기준 |
|---|---|---|
| 월 | 주간 계획 확정, 이슈 등록, DB 테이블 준비 | 이슈 등록 완료, 테이블 생성 |
| 화 | 내 가게 좌표 조회 + 주변 업체 검색 API | 좌표·목록 응답 확인 (Postman/브라우저) |
| 수 | 거리 계산 + DB 저장 로직 | DB에 실제로 행이 쌓이는지 확인 |
| 목 | 조회 API + 프론트 연결 (목 데이터 제거) | 화면에서 실제 DB 데이터로 표시 |
| 금 | 새로고침 지속성 확인, 버그 수정, 검증 Agent로 점검 | 새로고침 후에도 데이터 유지 확인 |

## GitHub 이슈 목록 (우선순위 순)

1. **[P0] DB 테이블 설계 반영** — my_stores, competitors_nearby 테이블을 실제 코드(database.py)에 생성
2. **[P0] 내 가게 좌표 조회 API** — 네이버 지역검색으로 업체명 → 좌표
3. **[P0] 주변 경쟁업체 검색 + 거리 계산** — 좌표 기준 반경 2km 내 같은 업종 검색, Haversine 적용
4. **[P0] 경쟁업체 DB 저장 (중복 방지)** — 검색 결과를 MySQL에 저장, 재검색 시 중복 안 쌓이게
5. **[P0] 저장된 목록 조회 API** — DB에서 저장된 경쟁업체 목록을 다시 불러오는 GET API
6. **[P0] 프론트엔드 실제 연결** — 목 데이터 제거, 실제 API 호출로 교체, 새로고침 테스트
7. **[P1] 기능 강화 후보 1** — 예: 업종 필터, 정렬 옵션 등 (아래 "기능 강화" 참고)
8. **[P1] 기능 강화 후보 2**

## 기능 강화 (1~2개, AI와 설계 후 이슈화)

이번 주 요구사항에 "핵심 기능 보완 + 사용성 개선 1~2개"가 있습니다. 수직 슬라이스(이슈 1~6) 완성 후, 아래 중 골라서 AI와 설계 후 이슈 등록:

- 반경 값을 사용자가 직접 조절 (1km/2km/3km 선택)
- 업종 자동 감지 실패 시 수동 선택 옵션
- 검색 기록 저장 (최근 검색한 내 가게 목록)

## 이번 주 산출물

- [ ] GitHub Issues 8개 등록 (P0 6개 + P1 2개)
- [ ] 아키텍처 다이어그램 (mermaid) → README에 삽입
- [ ] 테스트코드 생성 Skill
- [ ] 코드 검증 Agent
- [ ] TDD로 만든 기능 1개 (거리 계산 함수 추천)
- [ ] 워크플로우 문서화

## 다음 주 예고

- 저장된 경쟁업체 중 하나를 선택해 기존 리뷰 분석(Gemini) 기능과 연결
- Gemini 호출 최소화를 위한 캐싱 마무리
