import { getCoreAdapter } from '../../../web/adapters/core/registry';
import { ErrorState } from '../../../web/components/error-state';
import { FeaturedOfficialTeaser } from '../../../web/components/server/featured-official-teaser';
import { UserChallengeGrid } from '../../../web/components/server/user-challenge-grid';
export const metadata = { title: '챌린지 탐색' };
export default async function ChallengesPage({ searchParams }: { searchParams: Promise<{ q?: string; status?: 'recruiting' | 'closed' | 'all' }> }) { const params = await searchParams; const adapter = getCoreAdapter(); const [official, users] = await Promise.all([adapter.getFeaturedOfficialChallenge(), adapter.listPublicUserChallenges({ query: params.q, status: params.status ?? 'all' })]); return <main><FeaturedOfficialTeaser challenge={official.ok ? official.value : null} />{!official.ok && <ErrorState message="공식 챌린지 구성을 확인 중입니다." traceId={official.error.traceId} />}<section aria-labelledby="user-title"><p className="eyebrow">Community challenges</p><h2 id="user-title">함께 살아남을 스터디</h2><form className="glass card challenge-filter" action="/challenges"><div className="two-col"><label className="field">검색<input name="q" defaultValue={params.q} placeholder="제목이나 주제" /></label><label className="field">모집 상태<select name="status" defaultValue={params.status ?? 'all'}><option value="all">전체</option><option value="recruiting">모집 중</option><option value="closed">마감</option></select></label></div><button className="button button-secondary">필터 적용</button></form>{users.ok ? <UserChallengeGrid items={users.value.items} /> : <ErrorState message="사용자 챌린지를 불러오지 못했습니다." traceId={users.error.traceId} />}</section></main> }
