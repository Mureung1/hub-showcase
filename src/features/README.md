# Features

Product feature areas live here.

Planned first feature folders:

- `today-learning`
- `learning-workspace`
- `review`

Keep feature-specific components, hooks, mock data, and styles inside the feature folder until they become shared.
