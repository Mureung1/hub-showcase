import Link from 'next/link';
import type { UserChallengeCardDto } from '../../dto/challenge';
export function UserChallengeCard({ challenge }: { challenge: UserChallengeCardDto }) { const open = challenge.status === 'recruiting'; return <article className="glass card challenge-card"><span className={`status ${open ? '' : 'closed'}`}>{open ? '모집 중' : '마감/종료'}</span><div><h3>{challenge.title}</h3><p className="muted">{challenge.summary}</p></div><div className="meta"><span className="chip">{challenge.entryPoints.toLocaleString()} Point</span><span className="chip">매일 {challenge.dailyMinutes}분</span><span className="chip">{challenge.participantCount}/{challenge.capacity}명</span></div><p className="muted">{challenge.startsOn} – {challenge.endsOn}</p><Link className="button button-secondary" href={`/challenges/${challenge.challengeId}`}>자세히 보기</Link></article> }
