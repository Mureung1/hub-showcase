import type { LeaderboardDto } from '../../dto/study';
export function Leaderboard({ board }: { board: LeaderboardDto }) { return <><section className="glass plane"><p className="eyebrow">Alive now</p><p className="metric">{board.aliveCount}</p><h1>명이 오늘도 생존 중</h1></section><div className="glass card"><table className="leaderboard"><thead><tr><th>순위</th><th>학습자</th><th>연속</th><th>상태</th></tr></thead><tbody>{board.entries.map((entry) => <tr key={`${entry.rank}-${entry.displayName}`}><td>{entry.rank}</td><td>{entry.displayName}{entry.isViewer && ' (나)'}</td><td>{entry.streak}일</td><td><span className="status">{entry.status}</span></td></tr>)}</tbody></table></div></> }
