import Link from 'next/link';
import type { FeaturedOfficialChallengeDto } from '../../dto/challenge';
export function FeaturedOfficialTeaser({ challenge }: { challenge: FeaturedOfficialChallengeDto | null }) { if (!challenge) return <section className="glass plane"><p className="eyebrow">Official challenge</p><h2>다음 공식 챌린지를 준비하고 있어요</h2><p className="muted">새로운 30일 집중 루틴이 열리면 이곳에서 가장 먼저 알려 드릴게요.</p></section>; return <section className="glass plane"><p className="eyebrow">Featured official challenge</p><h1>{challenge.title}</h1><p>{challenge.summary}</p><div className="meta"><span className="chip">{challenge.entryPoints.toLocaleString()}P 참가</span><span className="chip">매일 {challenge.dailyMinutes}분</span><span className="chip">{challenge.status === 'recruiting' ? '모집 중' : challenge.status}</span></div><p className="muted">모집 마감 {challenge.recruitmentEndsAt?.slice(0,10) ?? '추후 공개'} · 시작 {challenge.challengeStartsAt.slice(0,10)}</p><Link className="button button-primary" href="/official-challenge">공식 챌린지 살펴보기</Link></section> }
