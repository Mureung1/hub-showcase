# 목요일까지 MVP 작업 이슈 정리

## 권장 작업 순서

```text
App.jsx 구조 정리
→ 사장님 회원번호/QR 입력과 조회 흐름
→ 사장님 큰 화면 UI
→ 손님 모바일 UI
→ 전체 QA
→ 배포
```

---

## Issue 1. App.jsx 화면 구조 분리

**관련 단계:** 작업 전 기반 정리

**무엇을 하나?**

`App.jsx`에 섞여 있는 손님 화면, 사장님 화면, mock 데이터, 공통 UI를 작은 파일로 분리한다.

**어떻게 하나?**

사장님 화면과 손님 화면을 별도 컴포넌트로 분리한다.  
mock 고객, 스탬프, 카페 데이터를 별도 파일로 분리한다.  
공통으로 쓰는 UI가 있으면 별도 컴포넌트로 분리한다.  
`App.jsx`는 앱 상태와 화면 전환 중심으로 줄인다.  
분리 후 기존 화면 동작이 바뀌지 않게 유지한다.

**기능으로 어떻게 연결되나?**

이후 사장님 화면과 손님 화면을 각각 안전하게 고칠 수 있다.  
큰 UI 변경 전에 파일 충돌과 실수를 줄이는 기반 작업이다.

**완료 기준**

기존 손님 화면이 정상 표시된다.  
기존 사장님 화면이 정상 표시된다.  
화면 전환 동작이 유지된다.  
`App.jsx`가 이전보다 명확히 짧아진다.  
`npm run verify`가 통과한다.

---

## Issue 2. 사장님 회원번호/QR 입력과 파싱

**관련 단계:** 5단계. 사장님이 매장 스캐너 또는 수동 입력으로 손님 찾기

**무엇을 하나?**

사장님 화면에 회원번호 또는 QR 값을 입력하는 입력창을 만들고, `cafe-stamp:` 접두어가 있는 값을 회원번호로 파싱한다.

**어떻게 하나?**

사장님 화면에 입력창과 조회 버튼을 추가한다.  
화면 진입 시 입력창에 자동 포커스를 준다.  
입력값 앞뒤 공백을 제거한다.  
`cafe-stamp:<회원번호>` 형식이면 접두어를 제거한다.  
접두어 없이 회원번호만 입력해도 조회할 수 있게 한다.  
잘못된 QR 형식이면 명확한 오류를 표시한다.  
Enter 입력으로 조회 버튼과 같은 흐름을 실행한다.  
조회 후 입력창에 다시 포커스를 준다.

**기능으로 어떻게 연결되나?**

실제 매장 스캐너가 키보드처럼 값을 입력하고 Enter를 보내는 흐름을 지원한다.  
스캐너가 없을 때도 사장님이 회원번호를 직접 입력해 같은 기능을 쓸 수 있다.  
이후 고객 조회와 스탬프 적립의 시작점이 된다.

**완료 기준**

입력창에 `cafe-stamp:C-1001`을 넣으면 `C-1001`로 파싱된다.  
입력창에 `C-1001`만 넣어도 조회 준비가 된다.  
빈 값은 조회되지 않고 오류가 보인다.  
잘못된 접두어 값은 QR 오류로 안내된다.  
Enter로 조회가 실행된다.  
조회 후 입력창에 다시 포커스가 돌아온다.

---

## Issue 3. 사장님 고객 조회 결과 표시

**관련 단계:** 6단계. 입력된 회원번호로 손님 정보 확인

**무엇을 하나?**

사장님이 입력한 회원번호로 mock 고객을 찾고, 조회 결과를 사장님 화면에 표시한다.

**어떻게 하나?**

회원번호 기준으로 mock 고객 데이터를 검색한다.  
고객이 있으면 이름, 회원번호, 현재 스탬프 개수, 사용 가능한 쿠폰 상태를 표시한다.  
고객이 없으면 "해당 회원을 찾을 수 없습니다" 같은 오류를 표시한다.  
새로운 값을 조회하면 이전 조회 결과와 오류 상태를 갱신한다.  
조회 성공 시 스탬프 적립 버튼을 사용할 수 있게 한다.

**기능으로 어떻게 연결되나?**

QR/회원번호 입력 이후 실제로 어떤 손님에게 스탬프를 적립할지 확인하는 단계다.  
잘못된 회원에게 적립하는 실수를 줄인다.

**완료 기준**

`C-1001` 조회 시 해당 mock 고객 정보가 보인다.  
존재하지 않는 회원번호는 오류가 보인다.  
조회 전에는 적립 버튼이 비활성화되거나 보이지 않는다.  
조회 성공 후에는 적립 액션을 진행할 수 있는 상태가 된다.

---

## Issue 4. 사장님 스탬프 적립 액션

**관련 단계:** 7단계. 사장님이 손님에게 스탬프 적립

**무엇을 하나?**

조회된 고객에게 사장님이 스탬프를 1개 적립할 수 있는 버튼과 결과 상태를 만든다.

**어떻게 하나?**

조회된 고객 정보 영역에 스탬프 적립 버튼을 추가한다.  
버튼 클릭 시 현재 스탬프 개수를 1 증가시킨다.  
최대 스탬프 개수에 도달하면 쿠폰 사용 가능 상태로 표시한다.  
적립 완료 후 성공 메시지를 보여준다.  
중복 클릭을 막기 위한 간단한 상태 처리를 한다.  
추후 Supabase 연동 시 교체하기 쉽도록 적립 로직을 한 함수로 분리한다.

**기능으로 어떻게 연결되나?**

MVP의 핵심 기능인 "매장에서 손님에게 스탬프를 적립한다" 흐름을 완성한다.

**완료 기준**

조회된 고객에게 스탬프 적립 버튼이 보인다.  
버튼 클릭 시 스탬프 개수가 1 증가한다.  
적립 완료 메시지가 보인다.  
스탬프가 가득 차면 쿠폰 사용 가능 상태가 보인다.  
조회된 고객이 없으면 적립할 수 없다.

---

## Issue 5. 사장님 큰 화면 대시보드 정리

**관련 단계:** 사장님용 넓은 화면 웹 다듬기

**무엇을 하나?**

사장님 화면을 모바일 느낌의 좁은 화면이 아니라, 매장 노트북/태블릿/데스크톱에서 쓰기 좋은 넓은 화면 대시보드로 다듬는다.

**어떻게 하나?**

사장님 화면의 최대 너비를 넓힌다.  
상단에 오늘 적립 수, 쿠폰 발급 수, 최근 방문 같은 요약 정보를 배치한다.  
회원번호/QR 입력 영역을 눈에 잘 띄는 위치에 둔다.  
고객 조회 결과와 최근 적립 내역을 나란히 배치한다.  
데스크톱에서는 2열 구조, 작은 화면에서는 1열 구조로 반응형 처리한다.  
반복 사용하기 좋은 조용하고 실용적인 UI 톤으로 정리한다.

**기능으로 어떻게 연결되나?**

사장님이 매장에서 빠르게 조회하고 적립하는 운영 화면이 된다.  
손님용 화면과 역할이 명확히 분리된다.

**완료 기준**

데스크톱 폭에서 사장님 화면이 넓게 보인다.  
입력, 조회 결과, 최근 내역이 한눈에 들어온다.  
작은 화면에서도 레이아웃이 깨지지 않는다.  
버튼과 입력창이 매장 사용 환경에서 충분히 크고 명확하다.

---

## Issue 6. 손님용 모바일 스탬프 카드 개선

**관련 단계:** 손님용 모바일 웹 다듬기

**무엇을 하나?**

손님이 보는 모바일 화면의 스탬프 카드와 혜택 상태를 더 예쁘고 명확하게 다듬는다.

**어떻게 하나?**

손님 화면을 모바일 우선 레이아웃으로 정리한다.  
현재 스탬프 개수가 한눈에 보이도록 카드 디자인을 개선한다.  
채워진 스탬프와 빈 스탬프의 차이를 명확히 만든다.  
쿠폰까지 남은 개수 또는 쿠폰 사용 가능 상태를 강조한다.  
카페 이름, 회원번호, 혜택 문구를 보기 좋게 정리한다.  
작은 화면에서 버튼과 텍스트가 겹치지 않게 확인한다.

**기능으로 어떻게 연결되나?**

손님이 자신의 적립 상태와 혜택을 쉽게 확인할 수 있다.  
MVP의 첫인상을 좋아 보이게 만드는 핵심 화면이다.

**완료 기준**

모바일 폭에서 손님 화면이 자연스럽게 보인다.  
현재 스탬프 개수가 즉시 이해된다.  
쿠폰까지 남은 상태가 명확하다.  
회원번호 또는 QR 표시 영역이 찾기 쉽다.  
텍스트나 버튼이 겹치지 않는다.

---

## Issue 7. 손님용 QR/회원번호 표시 영역 개선

**관련 단계:** 4단계. 손님이 매장에서 본인 회원번호 또는 QR을 제시

**무엇을 하나?**

손님 화면에서 사장님에게 보여줄 회원번호와 QR 값을 더 명확하게 표시한다.

**어떻게 하나?**

회원번호를 크게 표시한다.  
QR 데이터 값은 `cafe-stamp:<회원번호>` 형식으로 만든다.  
QR 이미지를 실제로 만들지 않는다면, MVP에서는 QR 자리와 데이터 값을 명확히 표시한다.  
사장님에게 보여주기 쉬운 위치에 배치한다.  
복사 버튼 또는 "직원에게 보여주세요" 문구를 추가할지 검토한다.

**기능으로 어떻게 연결되나?**

사장님 화면의 회원번호/QR 입력과 연결된다.  
손님이 자신의 회원번호를 제시하고, 사장님이 같은 값을 입력해 조회할 수 있다.

**완료 기준**

손님 화면에서 회원번호가 쉽게 보인다.  
QR 데이터 값이 `cafe-stamp:C-1001` 형식으로 표시된다.  
사장님 입력창에 해당 값을 넣으면 파싱 가능하다.  
모바일 화면에서 표시 영역이 깨지지 않는다.

---

## Issue 8. 전체 반응형 QA와 사용 흐름 점검

**관련 단계:** 배포 전 검증

**무엇을 하나?**

손님용 모바일 화면과 사장님용 큰 화면이 각각 의도한 폭에서 잘 보이는지 확인한다.

**어떻게 하나?**

모바일 폭에서 손님 화면을 확인한다.  
데스크톱 폭에서 사장님 화면을 확인한다.  
손님 회원번호 값을 사장님 화면에 입력해 흐름을 확인한다.  
빈 값, 잘못된 QR, 존재하지 않는 회원번호를 확인한다.  
스탬프 적립 후 화면 상태가 자연스럽게 바뀌는지 확인한다.  
`npm run verify`를 실행한다.

**기능으로 어떻게 연결되나?**

배포 전에 실제 MVP 시연 흐름이 깨지지 않는지 확인한다.

**완료 기준**

손님 화면 모바일 표시가 정상이다.  
사장님 화면 데스크톱 표시가 정상이다.  
회원번호 조회 흐름이 정상이다.  
오류 메시지가 필요한 상황에서 보인다.  
`npm run verify`가 통과한다.

---

## Issue 9. 배포 준비와 프로덕션 확인

**관련 단계:** 목요일까지 배포하기

**무엇을 하나?**

현재 MVP를 배포하고, 배포 URL에서 손님/사장님 흐름이 정상 동작하는지 확인한다.

**어떻게 하나?**

배포 방식과 대상 서비스를 확인한다.  
필요한 환경변수가 있는지 확인한다.  
`npm run build`를 실행한다.  
배포 명령 또는 배포 서비스를 통해 프로덕션에 올린다.  
배포 URL에서 손님 화면과 사장님 화면을 확인한다.  
회원번호/QR 입력, 고객 조회, 스탬프 적립 흐름을 다시 확인한다.

**기능으로 어떻게 연결되나?**

로컬 데모가 아니라 실제로 공유 가능한 MVP가 된다.

**완료 기준**

배포 URL이 생성된다.  
배포 URL에서 손님 화면이 정상 표시된다.  
배포 URL에서 사장님 화면이 정상 표시된다.  
핵심 흐름이 프로덕션에서도 동작한다.  
배포 전에 `npm run verify` 또는 `npm run build`가 통과한다.
