# 개선 백로그 1단계 Implementation Plan (헤더·카테고리·지역·신뢰도 표시)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 설계문서 `docs/superpowers/specs/2026-07-26-개선백로그-1-2단계-design.md`의 1단계(헤더 화면 끝 정렬, 카테고리 화이트리스트, 지역 시·군·구 전국 확장, 신뢰도 표시 개편)를 구현한다.

**Architecture:** BE 변경은 기존 supertest 라우트 테스트로 TDD(카테고리·지역 화이트리스트). 지역 데이터는 저장소 공용 JSON 1개(`hub/shared/regions.json`)를 FE·BE가 함께 참조하는 단일 소스로 둔다. 나머지(헤더 CSS, 신뢰도 배지, 지역 2단 셀렉트)는 FE 표시 계층 변경이라 자동 테스트 없이 `npm run lint` + `npm run build` + 명시된 수동 확인으로 검증한다.

**Tech Stack:** Node.js + Express + PostgreSQL(jest + supertest), React(Vite) FE(자동 테스트 없음, lint/build).

## Global Constraints

- 커밋 메시지는 **한글**로 작성. 하단에 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` 트레일러 유지.
- **BE 작업(Task 1·2)**: TDD. 테스트는 `bungae-moim-backend/tests/*.routes.test.js` 스타일(supertest + 실제 `bungae_test` DB). 실행 `cd bungae-moim-backend && npm test`. **기준선 187/187**을 깨지 않고 늘린다.
- **FE 작업(Task 3·4·5)**: 자동 테스트 없음. 검증 = `cd bungae-moim && npm run lint`(기존 `AppStateContext.jsx` 경고 **1건만** 허용, 그 외 경고·에러 0) + `npm run build`(성공) + 각 태스크에 적힌 수동 확인. **BE를 건드리지 않는다.**
- 지역 데이터: 공용 `hub/shared/regions.json` **단일 소스**. **시/군/구까지만**(읍/면/동 없음). **세종특별자치시는 `["세종특별자치시"]`**(시군구 자리에 시도명 그대로). `region_eupmyeondong` 컬럼은 nullable 유지.
- 카테고리 허용값은 **`['운동','스터디','취미','식사']` 4개만**. `'전체'`는 필터 전용이라 **제외**.
- 신뢰도 점수는 pg `numeric` → JS **문자열**로 온다. 표시 전 반드시 `Number()` 정규화.

---

## File Structure

**생성:**
- `hub/shared/regions.json` — 전국 17 시도 + 시군구 목록(2단계 구조). FE·BE 공용 단일 소스.
- `hub/bungae-moim-backend/src/utils/regions.js` — 공용 JSON 로더 + `isValidRegion(sido, sigungu)` 헬퍼.

**수정(BE):**
- `hub/bungae-moim-backend/src/utils/validators.js` — 카테고리·지역 화이트리스트 검증 추가.
- `hub/bungae-moim-backend/tests/meetings.create.routes.test.js` — 검증 테스트 추가.

**수정(FE):**
- `hub/bungae-moim/vite.config.js` — 상위 폴더(공용 JSON) fs 접근 허용.
- `hub/bungae-moim/src/data/mockData.js` — `REGIONS`를 공용 JSON에서 로드(2단계 구조).
- `hub/bungae-moim/src/components/MeetingForm.jsx` — 지역 2단 셀렉트(읍면동 제거).
- `hub/bungae-moim/src/pages/MeetingListPage.jsx` — 필터 지역 2단 구조 반영.
- `hub/bungae-moim/src/utils/meetingFormDefaults.js` — 기본값 2단 구조 반영.
- `hub/bungae-moim/src/components/TrustBadge.jsx` — 정수 + 이모지 + 색 밴드.
- `hub/bungae-moim/src/styles/components.css` — 신뢰도 색 밴드 클래스 + 다크 잔재 정리 + 헤더 화면 끝.

---

## Task 1: 카테고리 화이트리스트 (BE)

**Files:**
- Modify: `bungae-moim-backend/src/utils/validators.js`
- Test: `bungae-moim-backend/tests/meetings.create.routes.test.js`

**Interfaces:**
- Produces: `validateCreateMeeting`가 `category ∉ ['운동','스터디','취미','식사']`이면 `ApiError('VALIDATION_ERROR')`를 던진다. `validateUpdateMeeting`은 이를 재사용하므로 E4에도 자동 적용.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/meetings.create.routes.test.js`의 `describe('POST /api/meetings', ...)` 안에 추가:

```javascript
  it("category가 '전체'면 400 VALIDATION_ERROR (필터 전용 값)", async () => {
    const agent = await loginAgent();
    const res = await agent.post('/api/meetings').send({ ...validFlashBody, category: '전체' });
    expect(res.status).toBe(400);
    expect(res.body.error.code).toBe('VALIDATION_ERROR');
  });

  it('허용되지 않은 category면 400 VALIDATION_ERROR', async () => {
    const agent = await loginAgent();
    const res = await agent.post('/api/meetings').send({ ...validFlashBody, category: '게임' });
    expect(res.status).toBe(400);
    expect(res.body.error.code).toBe('VALIDATION_ERROR');
  });
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `cd bungae-moim-backend && npx jest tests/meetings.create.routes.test.js -t "category"`
Expected: FAIL — 지금은 화이트리스트가 없어 '전체'·'게임'이 201로 통과하므로 `expect(400)`에서 실패.

- [ ] **Step 3: 최소 구현**

`validators.js` 상단(다른 상수 근처, `MAX_LENGTHS` 위)에 추가:

```javascript
// 카테고리 화이트리스트. '전체'는 목록 필터 전용 값이므로 등록 허용값이 아니다.
const ALLOWED_CATEGORIES = ['운동', '스터디', '취미', '식사'];
```

`validateCreateMeeting` 안, `const category = requireString(body.category, 'category');`(현재 45행) **바로 아래**에 추가:

```javascript
  if (!ALLOWED_CATEGORIES.includes(category)) {
    throw new ApiError('VALIDATION_ERROR', 'category는 허용된 값이 아닙니다');
  }
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd bungae-moim-backend && npx jest tests/meetings.create.routes.test.js -t "category"`
Expected: PASS (2 테스트).

- [ ] **Step 5: 전체 테스트로 회귀 없음 확인**

Run: `cd bungae-moim-backend && npm test`
Expected: 전부 통과(189/189 — 기존 187 + 신규 2). 기존 `validFlashBody`(category '운동')·`validSmallBody`(category '스터디')는 허용값이라 영향 없음.

- [ ] **Step 6: 커밋**

```bash
git add bungae-moim-backend/src/utils/validators.js bungae-moim-backend/tests/meetings.create.routes.test.js
git commit -m "feat: 모임 카테고리 화이트리스트 검증 추가('전체' 제외)"
```

---

## Task 2: 지역 공용 JSON + BE 화이트리스트 검증 (BE)

**Files:**
- Create: `hub/shared/regions.json`
- Create: `bungae-moim-backend/src/utils/regions.js`
- Modify: `bungae-moim-backend/src/utils/validators.js`
- Test: `bungae-moim-backend/tests/meetings.create.routes.test.js`

**Interfaces:**
- Produces: `regions.js` — `module.exports = { REGIONS, isValidRegion }`. `REGIONS`는 `{ "시도명": ["시군구명", ...], ... }`. `isValidRegion(sido, sigungu) => boolean` (sido가 존재하고 sigungu가 그 배열에 포함될 때만 true). `validateCreateMeeting`이 유효하지 않은 지역이면 `ApiError('VALIDATION_ERROR')`.
- Consumes(Task 3): FE도 같은 `hub/shared/regions.json`을 로드한다.

- [ ] **Step 1: 공용 지역 JSON 생성**

`hub/shared/regions.json` 생성. 구조는 `{ "시도명": ["시군구명", ...] }`. **전국 17 시도 키를 모두 포함**하고(아래 정확한 명칭 사용), 각 시도의 시군구(자치구·시·군)를 채운다. **읍/면/동 계층은 넣지 않는다.**

17 시도 정확한 명칭(2024 기준, 특별자치도 개편 반영):
`서울특별시, 부산광역시, 대구광역시, 인천광역시, 광주광역시, 대전광역시, 울산광역시, 세종특별자치시, 경기도, 강원특별자치도, 충청북도, 충청남도, 전북특별자치도, 전라남도, 경상북도, 경상남도, 제주특별자치도`

규칙:
- **세종특별자치시**는 하위 시군구가 없으므로 `"세종특별자치시": ["세종특별자치시"]`(시도명 그대로 1개).
- 일반구가 있는 시(수원·성남·창원 등)는 **시 레벨까지만** 표기(예: `"수원시"` — "수원시 장안구"처럼 합치지 않는다. `region_sigungu`는 varchar(20)라 합치면 넘칠 수 있다).
- 제주는 행정시 `"제주시", "서귀포시"`를 시군구 자리에 넣는다.
- 기존 테스트·개발 데이터가 쓰는 값이 반드시 포함돼야 한다: 서울특별시에 `"강남구","마포구","관악구","송파구","종로구","강북구"`, 경기도에 `"성남시","수원시"`.

전체 시군구 목록은 행정안전부 행정표준코드(또는 동등한 2024 행정구역 목록)에서 시/군/구 레벨만 추출해 채운다. 예시(서울·세종·경기 일부 — 나머지 시도도 동일 형식으로 완성):

```json
{
  "서울특별시": ["강남구","강동구","강북구","강서구","관악구","광진구","구로구","금천구","노원구","도봉구","동대문구","동작구","마포구","서대문구","서초구","성동구","성북구","송파구","양천구","영등포구","용산구","은평구","종로구","중구","중랑구"],
  "세종특별자치시": ["세종특별자치시"],
  "경기도": ["수원시","성남시","고양시","용인시","부천시","안산시","안양시","남양주시","화성시","평택시","의정부시","시흥시","파주시","광명시","김포시","군포시","광주시","이천시","양주시","오산시","구리시","안성시","포천시","의왕시","하남시","여주시","동두천시","과천시","가평군","연천군","양평군"]
}
```

완성 후 검증(수동): 최상위 키가 정확히 17개, 각 값은 비어있지 않은 배열, `세종특별자치시`는 `["세종특별자치시"]`.

- [ ] **Step 2: BE 지역 로더 작성**

`bungae-moim-backend/src/utils/regions.js` 생성:

```javascript
// 공용 지역 데이터(FE와 같은 단일 소스). 시/군/구까지만.
const REGIONS = require('../../../shared/regions.json');

// sido가 존재하고 sigungu가 그 시도의 시군구 목록에 있을 때만 true.
function isValidRegion(sido, sigungu) {
  const list = REGIONS[sido];
  return Array.isArray(list) && list.includes(sigungu);
}

module.exports = { REGIONS, isValidRegion };
```

경로 확인: `src/utils/regions.js`에서 `../../../shared/regions.json` = `bungae-moim-backend/src/utils` → `../../../` → `hub` → `shared/regions.json`. 맞다.

- [ ] **Step 3: 실패하는 테스트 작성**

`tests/meetings.create.routes.test.js`의 `describe('POST /api/meetings', ...)` 안에 추가:

```javascript
  it('regionSido가 목록에 없으면 400 VALIDATION_ERROR', async () => {
    const agent = await loginAgent();
    const res = await agent
      .post('/api/meetings')
      .send({ ...validFlashBody, regionSido: '없는도', regionSigungu: '없는구' });
    expect(res.status).toBe(400);
    expect(res.body.error.code).toBe('VALIDATION_ERROR');
  });

  it('regionSigungu가 해당 시도 하위가 아니면 400 VALIDATION_ERROR', async () => {
    const agent = await loginAgent();
    // 서울특별시 + 수원시(경기 소속) 조합은 무효
    const res = await agent
      .post('/api/meetings')
      .send({ ...validFlashBody, regionSido: '서울특별시', regionSigungu: '수원시' });
    expect(res.status).toBe(400);
    expect(res.body.error.code).toBe('VALIDATION_ERROR');
  });

  it('세종특별자치시는 시군구=세종특별자치시로 통과한다', async () => {
    const agent = await loginAgent();
    const res = await agent
      .post('/api/meetings')
      .send({ ...validSmallBody, regionSido: '세종특별자치시', regionSigungu: '세종특별자치시' });
    expect(res.status).toBe(201);
  });
```

- [ ] **Step 4: 테스트가 실패하는지 확인**

Run: `cd bungae-moim-backend && npx jest tests/meetings.create.routes.test.js -t "region"`
Expected: FAIL — 아직 지역 화이트리스트가 없어 '없는도'·'수원시(서울)'가 201로 통과.
(세종 테스트는 이미 통과할 수 있으나, 무효 조합 2건이 실패하면 충분.)

- [ ] **Step 5: validators.js에 지역 검증 추가**

`validators.js` 상단 `require` 근처에 추가:

```javascript
const { isValidRegion } = require('./regions');
```

`validateCreateMeeting` 안, `regionSido`·`regionSigungu`의 `requireString`(현재 46~47행) **바로 아래**에 추가:

```javascript
  if (!isValidRegion(regionSido, regionSigungu)) {
    throw new ApiError('VALIDATION_ERROR', 'regionSido/regionSigungu가 유효한 지역이 아닙니다');
  }
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `cd bungae-moim-backend && npx jest tests/meetings.create.routes.test.js -t "region"`
Expected: PASS (3 테스트).

- [ ] **Step 7: 전체 테스트 회귀 확인**

Run: `cd bungae-moim-backend && npm test`
Expected: 전부 통과. 기존 `validFlashBody`(서울특별시/강남구)·`validSmallBody`(서울특별시/마포구)가 JSON에 포함돼 있어야 통과한다 — 누락 시 Step 1로 돌아가 채운다.

- [ ] **Step 8: 커밋**

```bash
git add "hub/shared/regions.json" bungae-moim-backend/src/utils/regions.js bungae-moim-backend/src/utils/validators.js bungae-moim-backend/tests/meetings.create.routes.test.js
git commit -m "feat: 전국 시군구 공용 지역 데이터 + BE 지역 화이트리스트 검증"
```
> 경로에 한글/공백이 있으므로 `git add`에서 `hub/shared/regions.json`이 인식되지 않으면 저장소 루트 기준 상대경로로 `git add shared/regions.json` 사용.

---

## Task 3: 지역 2단 셀렉트 FE 마이그레이션 (읍/면/동 제거)

**Files:**
- Modify: `bungae-moim/vite.config.js`
- Modify: `bungae-moim/src/data/mockData.js`
- Modify: `bungae-moim/src/components/MeetingForm.jsx`
- Modify: `bungae-moim/src/pages/MeetingListPage.jsx`
- Modify: `bungae-moim/src/utils/meetingFormDefaults.js`

**Interfaces:**
- Consumes: `hub/shared/regions.json`(Task 2 생성), 구조 `{ "시도명": ["시군구명", ...] }`.
- Produces: `mockData.js`의 `export const REGIONS`가 이제 **2단계 배열 구조**(`REGIONS[sido]`가 시군구 문자열 배열). 읍/면/동 셀렉트 제거, 제출 시 `regionEupmyeondong: null`.

> ⚠️ 이 태스크는 `REGIONS`의 형태를 3단계(`{시도:{시군구:[동]}}`)→2단계로 바꾼다. **소비처를 한 태스크 안에서 모두 고쳐야** FE 빌드가 깨지지 않는다.

- [ ] **Step 1: Vite가 상위 폴더의 공용 JSON을 읽도록 허용**

`bungae-moim/vite.config.js`에 `server.fs.allow`를 추가한다. 현재 파일은 `plugins`와 `server.proxy`만 있으므로, `server` 블록에 `fs`를 나란히 추가한 최종 형태:

```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': 'http://localhost:3000',
    },
    // 상위 hub/shared/regions.json 정적 import 허용(프로젝트 루트 밖 파일)
    fs: { allow: ['..'] },
  },
})
```

- [ ] **Step 2: mockData.js의 REGIONS를 공용 JSON으로 교체**

`src/data/mockData.js`에서 기존 `REGIONS` 객체 리터럴(6~19행)을 **삭제**하고, 파일 상단에 import 추가 + 재export:

```javascript
import regionsData from '../../../shared/regions.json'

export const CATEGORIES = ['전체', '운동', '스터디', '취미', '식사']

// 공용 단일 소스(BE와 동일). 구조: { 시도: [시군구, ...] }. 읍/면/동 없음.
export const REGIONS = regionsData
```
경로 확인: `src/data/mockData.js` → `../../../shared/regions.json` = `bungae-moim/src/data` → `../../../` → `hub` → `shared`. 맞다.

- [ ] **Step 3: MeetingForm.jsx 2단 셀렉트로 수정**

`src/components/MeetingForm.jsx`:

(a) 파생값(현재 31~32행)을 교체:
```javascript
  const sigunguOptions = REGIONS[sido] ?? []
```
(기존 `eupmyeondongOptions` 라인은 삭제.)

(b) 제출 payload(현재 51행)에서 읍면동을 항상 null로:
```javascript
        regionEupmyeondong: null,
```

(c) 시도 onChange의 첫 시군구 계산(현재 116행)을 교체:
```javascript
                const firstSigungu = REGIONS[nextSido][0]
                setSigungu(firstSigungu)
```
(그 아래 `setEupmyeondong('')` 라인은 삭제.)

(d) 지역 `field-row--three`(현재 109행)를 `field-row`로 바꾸고, **읍/면/동 select 블록(현재 143~155행) 전체를 삭제**한다. 시군구 select의 onChange(현재 131~134행)에서 `setEupmyeondong('')` 라인도 삭제:
```javascript
            <select
              className="field-select"
              value={sigungu}
              onChange={(e) => setSigungu(e.target.value)}
              aria-label="시/군/구"
            >
              {sigunguOptions.map((s) => (
                <option key={s} value={s}>
                  {s}
                </option>
              ))}
            </select>
```

(e) `eupmyeondong` 상태와 `setEupmyeondong`(현재 20행)은 더 이상 쓰이지 않으므로 삭제한다. `initialValues.eupmyeondong`는 무시된다(수정 페이지 프리필이 넘겨도 무해).

- [ ] **Step 4: MeetingListPage.jsx 필터 수정**

`src/pages/MeetingListPage.jsx`의 `sigunguOptions`(현재 40행)를 교체:
```javascript
  const sigunguOptions = sido === '전체' ? [] : (REGIONS[sido] ?? [])
```
(이 페이지엔 읍/면/동 필터가 원래 없으므로 그 외 변경 없음.)

- [ ] **Step 5: meetingFormDefaults.js 기본값 수정**

`src/utils/meetingFormDefaults.js`의 `defaultFormValues`(현재 16~18행) 지역 기본값을 2단계 구조로:
```javascript
    sido: Object.keys(REGIONS)[0],
    sigungu: REGIONS[Object.keys(REGIONS)[0]][0],
    eupmyeondong: '',
```
(`eupmyeondong: ''`는 남겨도 무해하지만, 폼이 더 이상 쓰지 않는다.)

- [ ] **Step 6: lint + build 검증**

Run: `cd bungae-moim && npm run lint`
Expected: 기존 `AppStateContext.jsx` 경고 1건 외 에러/경고 0. (삭제한 `eupmyeondong`/`setEupmyeondong` 미사용 경고가 남지 않도록 Step 3(e)에서 완전히 제거했는지 확인.)

Run: `cd bungae-moim && npm run build`
Expected: 빌드 성공.

- [ ] **Step 7: 수동 확인**

두 서버를 띄우고(`bungae-moim-backend`에서 `npm start`, `bungae-moim`에서 `npm run dev`) 로그인 후:
- 모임 등록 폼: 지역이 **시/도 → 시/군/구 2단 셀렉트**로 뜨고 읍/면/동 칸이 없다. 시/도를 바꾸면 시/군/구 목록이 그 시도의 것으로 바뀐다.
- 세종특별자치시 선택 시 시/군/구에 `세종특별자치시` 하나만.
- 모임 찾기 필터: 시/도 선택 시 시/군/구 목록이 채워지고 필터가 동작.
- 등록이 실제로 성공(BE 지역 검증 통과)하는지 1건 등록.

- [ ] **Step 8: 커밋**

```bash
git add bungae-moim/vite.config.js bungae-moim/src/data/mockData.js bungae-moim/src/components/MeetingForm.jsx bungae-moim/src/pages/MeetingListPage.jsx bungae-moim/src/utils/meetingFormDefaults.js
git commit -m "feat: 지역 입력을 전국 시/군/구 2단 셀렉트로 변경(읍면동 제거)"
```

---

## Task 4: 신뢰도 표시 개편 (정수 + 이모지 + 색 밴드)

**Files:**
- Modify: `bungae-moim/src/components/TrustBadge.jsx`
- Modify: `bungae-moim/src/styles/components.css`

**Interfaces:**
- Consumes: `TrustBadge`는 4곳에서 `score` prop으로 소비됨(SiteHeader:48, MyPage:70, MeetingDetailPage:224·276). prop 시그니처(`score`) 불변 — 컴포넌트 내부만 바꾼다.
- Produces: `Math.round(Number(score))` 정수 + 밴드별 이모지/색.

- [ ] **Step 1: TrustBadge.jsx 재작성**

`src/components/TrustBadge.jsx` 전체 교체:

```jsx
// 점수 밴드: 0–30 😢 / 31–60 😐 / 61–90 🙂 / 91–100 😍
const BANDS = [
  { max: 30, emoji: '😢', className: 'trust-badge--low' },
  { max: 60, emoji: '😐', className: 'trust-badge--mid' },
  { max: 90, emoji: '🙂', className: 'trust-badge--high' },
  { max: 100, emoji: '😍', className: 'trust-badge--top' },
]

export default function TrustBadge({ score }) {
  // pg numeric은 문자열로 오므로 Number 정규화 후 정수 반올림.
  const value = Math.round(Number(score) || 0)
  const band = BANDS.find((b) => value <= b.max) ?? BANDS[BANDS.length - 1]
  return (
    <span className={`trust-badge ${band.className}`}>
      <span aria-hidden="true">{band.emoji}</span> 신뢰도 <strong>{value}</strong>
    </span>
  )
}
```

- [ ] **Step 2: components.css에 밴드 색 추가 + 다크 잔재 정리**

`src/styles/components.css`의 `.trust-badge strong`(현재 179~184행) 아래에 밴드별 색 규칙을 추가한다. 기존 `.trust-badge strong { color: var(--accent-soft); }`는 밴드 클래스가 덮어쓴다:

```css
.trust-badge--low strong { color: #d64545; }   /* 우는 얼굴 — 낮음 */
.trust-badge--mid strong { color: var(--ink-mute); } /* 무표정 — 보통 */
.trust-badge--high strong { color: #2f9e54; }   /* 웃음 — 높음 */
.trust-badge--top strong { color: #c8901f; }    /* 하트 — 최상(골드) */
```

`.card--dark .trust-badge`(현재 186~188행)의 고아 토큰 `--cream-mute`를 유지 토큰으로 교체(다크 카드에서 밝은 글씨):

```css
.card--dark .trust-badge {
  color: var(--cream);
}
```

- [ ] **Step 3: lint + build 검증**

Run: `cd bungae-moim && npm run lint`
Expected: 기존 경고 1건 외 0.

Run: `cd bungae-moim && npm run build`
Expected: 성공.

- [ ] **Step 4: 수동 확인**

서버를 띄우고 로그인 후:
- **헤더** 우측 신뢰도가 `😐 신뢰도 50`처럼 **이모지 + 정수**로 표시(현재 개발 사용자 50점 → 무표정).
- **상세**(모임장·신청자 배지)·**마이페이지 프로필**에서도 동일하게 표시.
- 마이페이지 프로필은 다크 카드 위 — 글씨가 밝게(가독) 보이는지.
- **다크모드**(OS 설정 또는 콘솔 `document.documentElement.dataset.theme='dark'`)에서 배지 글씨/이모지 대비 확인.
- 색 밴드가 헤더 배경 위에서 읽히는지(특히 낮음 빨강·최상 골드). 대비가 약하면 색값을 조정.

- [ ] **Step 5: 커밋**

```bash
git add bungae-moim/src/components/TrustBadge.jsx bungae-moim/src/styles/components.css
git commit -m "feat: 신뢰도 배지 정수+이모지+색 밴드 표시 개편"
```

---

## Task 5: 헤더 화면 끝 정렬

**Files:**
- Modify: `bungae-moim/src/styles/components.css:275-283`

**Interfaces:**
- Produces: `.site-header-inner`가 전체 폭이 되어 브랜드/nav는 화면 왼쪽 끝, 인증 영역은 오른쪽 끝. 본문 `.page`는 1200px 유지(변경 없음).

- [ ] **Step 1: .site-header-inner에서 폭 제한 제거**

`src/styles/components.css`의 `.site-header-inner` 규칙(현재 275~283행 부근)에서 **`max-width: var(--w-wide);`와 `margin: 0 auto;` 두 줄을 삭제**하고, 좌우 패딩을 화면 끝에 붙되 숨 쉴 여백을 준다:

```css
.site-header-inner {
  height: var(--header-h);
  padding: 0 clamp(16px, 4vw, 40px);
  display: flex;
  align-items: center;
  /* 기존 gap 등 나머지 속성은 그대로 유지 */
}
```
> 기존 규칙의 `gap`·기타 속성은 지우지 말고 유지. `max-width`/`margin`만 제거하고 `padding`을 위와 같이.

- [ ] **Step 2: lint + build 검증**

Run: `cd bungae-moim && npm run lint`
Expected: 기존 경고 1건 외 0.

Run: `cd bungae-moim && npm run build`
Expected: 성공.

- [ ] **Step 3: 수동 확인**

서버를 띄우고:
- 넓은 화면에서 **헤더가 화면 전체 폭**을 쓰고, 브랜드가 왼쪽 끝·인증 영역이 오른쪽 끝에 붙는다.
- 본문(목록/상세)은 여전히 가운데 1200px로 정렬 — 헤더만 넓어졌다.
- 창을 **900px 미만**으로 줄여도 헤더가 깨지지 않고 가로 스크롤이 없다.

- [ ] **Step 4: 커밋**

```bash
git add bungae-moim/src/styles/components.css
git commit -m "feat: 헤더를 화면 끝 정렬로 변경(본문 1200px 유지)"
```

---

## Self-Review (작성자 확인 완료)

- **Spec 커버리지**: 1단계 4개 항목 전부 태스크 존재 — 헤더(T5), 카테고리 화이트리스트(T1), 지역 시군구+공용JSON+세종예외+화이트리스트(T2·T3), 신뢰도 정수·이모지·색밴드+다크잔재(T4). 2단계는 이 계획서 범위 밖(설계문서에 개요, 착수 시 별도 계획).
- **플레이스홀더**: 지역 JSON의 전체 250 시군구는 데이터 항목이라 Step 1에서 공식 목록으로 채우도록 명시(구조·17시도 정확명칭·세종규칙·필수 포함값·검증 제공). 그 외 코드 스텝은 실제 코드 포함.
- **타입 정합성**: `isValidRegion(sido, sigungu)` 시그니처가 T2 정의·T2 사용 일치. `REGIONS` 2단계 구조가 T2(JSON)·T3(FE 소비처 전부) 일치. `TrustBadge({ score })` prop 불변.
- **회귀 안전**: BE 태스크는 TDD로 187→189, 기존 valid 바디(서울/강남·마포, 운동·스터디)가 새 화이트리스트를 통과함을 Step에서 확인. FE 태스크는 BE 미변경 + lint/build.

## 착수 순서

T1(카테고리) → T2(지역 JSON+BE검증) → T3(지역 FE) → T4(신뢰도) → T5(헤더).
T3는 T2(공용 JSON)에 의존. T1·T4·T5는 상호 독립.

---

## 실행 결과 (2026-07-27, Subagent-Driven)

T1~T5 전부 구현·태스크별 리뷰 통과. 최종 전체리뷰(opus) 판정 **With fixes**(Critical 0, Important 3) → 수정 1회로 해소,
스코프 재리뷰에서 **All findings addressed, no new breakage**.

| 커밋 | 내용 |
|---|---|
| `1021d7a7` | T1 카테고리 화이트리스트 |
| `950976c3` | T2 `shared/regions.json`(17시도/229시군구) + BE 지역 검증 |
| `c3cc4ede` | T3 FE 지역 2단 셀렉트(읍면동 제거) |
| `21b503d3` | T4 신뢰도 배지 정수+이모지+색 밴드 |
| `4c3a8956` | T5 헤더 화면 끝 정렬 |
| `7ca45bbe` | 최종리뷰 수정 — 읍면동 표시·프리필 은퇴 정합화 + API 명세서 400 2종 반영 |
| `62d4e688` | 최종리뷰 수정 — 배지 사문화 색 제거, 낡은 주석 2곳, 지역 JSON 회귀 테스트 8건 |

- **BE 200/200**(기준선 187 + 검증 5 + 지역 JSON 불변식 8). FE lint 기존 경고 1건만 / build 성공. (컨트롤러가 직접 재확인)
- **계획서 이탈 1건(정당)**: T4 밴드 색은 계획서 단일 색값이 다크 배경에서 4.5:1을 못 채워 **라이트/다크 값을 분리**했다.
- **미실행**: 각 태스크의 "수동 확인"(브라우저) 스텝 — 구글 로그인이 필요해 사용자 육안 검증으로 일괄 연기.
- 진행 원장: `.superpowers/sdd/2026-07-26-개선백로그-1단계/progress.md`(git 미포함 스크래치).
