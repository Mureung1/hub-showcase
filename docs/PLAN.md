# MVP 3주 계획

> 2026-07-07 시작. 목표: 3주차 말에 docs/MVP.md의 완료 기준(DoD) 충족.
> 전략: **1~2주차는 목(mock) 응답으로 UI 흐름을 완성**하고, 3주차에 AI를 붙인다. 프론트에서 AI API 직접 호출은 금지 (EDGE_CASES 5-1).
> **구현 항목의 완료 체크는 docs/CHECKLIST.md(의존성 순서, T1~T23)가 단일 기준이다.** 이 문서의 체크박스는 주차 결산 시에만 갱신한다. CLAUDE.md 규칙에 따라 테스트 없이 완료 선언하지 않는다.

## 1주차 (7/7 ~ 7/13) — 기반 + 입력 흐름

목표: 시나리오 선택 → 입력까지 동작. 목 응답 기반이 준비되고, 시드 예시 절반 확보.

- [ ] Vitest + React Testing Library 도입 (테스트 규칙 이행의 전제 — 현재 미설치)
- [ ] 시나리오·톤 축 데이터 정의: SPEC 1장 확정값(4개 id·카드 문구·톤 라벨, 중간="기본")대로 타입 명시 (`any` 금지)
- [ ] 목(mock) 생성 응답 모듈: SPEC 2장 API 계약과 동일 타입 + 강제 케이스 스위치(normal/delay/error500/error429)
- [ ] S1 시나리오 선택 화면 (카드 4개, DESIGN.md 토큰 적용)
- [ ] S2 입력 화면: 목적 칩 6개(필수) + "받은 메시지 붙여넣기"(500자)·"상황 설명"(300자) 중 1개 이상 필수, 글자 수 표시, 개인정보 안내 문구
- [ ] step 상태 관리(scenario | input | result) + sessionStorage 보존 (카톡 전환 리로드 대응)
- [ ] 시드 예시 1차 작성: 팀플·교수님 시나리오 12개 — SPEC 4장 스키마·작성 기준(상황 1개당 3톤 세트, 먼저 보내는 상황 포함)대로
- [ ] 1주차 구현분 테스트 작성·통과

## 2주차 (7/14 ~ 7/20) — 결과 흐름 완성 (목 기반 E2E)

목표: 목 응답으로 "선택 → 입력 → 생성 → 복사" 전체 흐름이 모바일에서 끝까지 동작.

- [ ] S3 결과 화면: 톤 라벨 배지가 붙은 후보 3개 (톤 축 순서 배치)
- [ ] 복사 기능: 성공 토스트 + 클립보드 실패 시 텍스트 자동 선택 폴백 (카톡 인앱 브라우저 대응)
- [ ] 플레이스홀더 감지: `[...]`·`OO` 패턴 하이라이트 + "빈칸을 채워주세요" 배지
- [ ] 생성 상태 UI: 로딩(20초 타임아웃) / 실패(다시 시도) / 429(문구 구분) — 목의 강제 케이스로 검증
- [ ] 재시도 동선: "다시 만들기"(리롤) 1차 + "상황 수정"(입력 유지 복귀) 2차 — 리롤 중 버튼 비활성+로딩, **리롤 실패 시 기존 후보 3개 유지** + 오류 문구 (SCREENS S3 상태(리롤))
- [ ] 시나리오 변경("다시 선택") 처리: 입력값 유지, 기존 결과 폐기 (SCREENS S2 헤더 규칙)
- [ ] 입력 검증 UI: 목적·텍스트 미충족 시 버튼 비활성 + 안내 문구 / 결과에서 "상황을 더 적으면 더 맞는 보낼 말" 문구 (EDGE_CASES 1-1 개정)
- [ ] 모바일 375px 전수 점검 + DESIGN.md 토큰 정합성 확인 (액센트는 tertiary 단일 사용)
- [ ] 시드 예시 2차 작성: 선배·친구 12개 → 총 24개 완성 후 SPEC 4장 검수(블라인드 정렬 + 전송 가능성) 전수 통과
- [ ] 2주차 구현분 테스트 작성·통과

## 3주차 (7/21 ~ 7/27) — AI 연동 + 마감

목표: 실제 AI로 전환하고 배포. DoD 전 항목 충족.

- [x] AI 연동 구조 확정: **Vercel 서버리스 함수 1개 (`/api/generate`) → Claude API `claude-opus-4-8`** (2026-07-07 결정 — MVP.md "결정 완료". 코드 착수 시점에 구조가 바뀌면 재승인)
- [x] 배포 환경 결정: **Vercel** (정적 빌드 + `/api` 함수 한 배포, 키는 환경변수)
- [ ] 프록시 함수 구현: SPEC 2장 계약대로 `/api/generate` — `@anthropic-ai/sdk`로 Claude API 호출, API 키는 Vercel 환경변수(`ANTHROPIC_API_KEY`), 레이트리밋은 SPEC 6장(in-memory IP당 분당 10회), 타임아웃 20초, 입력 비저장
- [ ] few-shot 프롬프트 구성: SPEC 3장 구조대로 (안전 가이드·플레이스홀더 금지·데이터 블록 격리·structured outputs, 1회 호출 3후보)
- [ ] 받은 메시지 비저장 원칙 반영: 생성 요청에만 사용, 로그 저장 제외 (EDGE_CASES 1-5)
- [ ] 목 → 실 API 전환, 타임아웃·429·실패 처리 실동작 확인
- [ ] 품질 검수: SPEC 5장 합격선(톤 정렬 18/20, 전송 가능 48/60, 플레이스홀더 0건) — 미달 시 시드 보강→프롬프트 조정 루프(1루프 반나절)
- [ ] 전체 테스트 통과 + MVP.md DoD 체크리스트 검증
- [ ] 최소 계측: `generate`/`copy` 이벤트 (Vercel Analytics, 텍스트 미포함 — SPEC 7장, T24)
- [ ] 배포 및 실기기(모바일) 최종 확인

## 리스크 · 유동 항목

- ~~3주차 프록시가 가장 큰 미지수~~ → **해소 (2026-07-07): Vercel + `/api/generate` 함수 + Claude API로 확정** (MVP.md "결정 완료"). 남은 리스크는 프록시 구현 자체가 아니라 실 API 전환 후 품질 검수 시간 — 3주차 초에 프록시를 먼저 붙여 검수 기간을 확보한다. 1~2주차가 목 기반인 전략은 유지.
- 시드 예시 24개의 톤 구분 품질이 서비스 품질의 상한 — 2주차 검수에서 미달이면 3주차 품질 검수 시간을 늘린다.
- 피드백 수집(효과 있었어요)은 MVP Out — 3주 내 구현하지 않는다 (S3에 레이아웃 자리만 고려).
