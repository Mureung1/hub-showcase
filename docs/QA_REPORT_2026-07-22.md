# 기능 검증 보고서 — 2026-07-22

## 검증 환경

- 브랜치: `N091_박창현`
- 프런트엔드: `http://localhost:5173/`
- API: `http://localhost:3000/`
- 데이터 저장소: Supabase PostgreSQL

## 통과 항목

- 재료 목록 조회
- 재료 등록
- 동일 이름·보관 위치·단위·권장 날짜 재료의 수량 병합
- 재료 수량 수정
- 재료 삭제 및 삭제 후 목록 갱신
- 분류별 보관 방법과 권장 사용 날짜 자동 제안
- 추천 점수와 점수 근거 표시
- 메뉴 선택 후 코치 반응과 레시피 상세 이동
- 기본 양념 보유 상태 변경 시 부족 재료 즉시 반영
- 375px 모바일 화면에서 가로 넘침 없음
- 단위 테스트, lint, 프로덕션 빌드 통과

검증을 위해 만든 `QA 달걀 20260722` 데이터는 등록·병합·수정·삭제 확인 후 제거했다.

## 정리한 항목

- 추천 화면에서 동일한 `재료 1개 부족` 메뉴가 두 번 표시되던 중복 영역 제거
- 재료 조회 실패 화면에 오류 설명과 `다시 시도` 버튼 추가
- 재료 카드 태그 영역에서 분류·접두어·기한 상태를 제거하고 저장된 태그명만 표시
- D-6 이상으로 여유 있는 재료는 카드 색상을 유지하고 D-day 배지와 남은 기간만 초록색으로 강조
- 개발 가이드의 데이터 저장소 설명을 현재 Supabase 구성으로 갱신

## 후속 확인 항목

- 재료 API의 정상·입력 오류·없는 재료·병합 동작을 자동화한 통합 테스트가 필요하다.
- GitHub Actions에서 test, lint, build를 자동 실행하는 CI 구성이 필요하다.
