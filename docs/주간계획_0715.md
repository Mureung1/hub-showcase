# Phase 2 마무리 주간 계획 (7/15 수 ~ 7/17 금)

> 백로그: [`WeatherPilot_백로그.md`](./WeatherPilot_백로그.md) Phase 2 (2-3 ~ 2-7)
> 이 문서의 이슈 초안을 그대로 복사해 GitHub(`shyang0319/hub`) → Issues → New issue에 붙여넣으면 됩니다.

## 진행 상황

- ✅ **Phase 1** (1-1~1-8): 날씨 앙상블 + DB + API 전부 완료
- ✅ **Phase 2 착수**: 2-1 진단 로직, 2-2 LLM 제안(Groq)
- ⬜ **남은 Phase 2**: 2-3 검증 · 2-4 가드레일 · 2-5 API·순수함수화 · 2-6 크론 · 2-7 저장·조회

## 요일별 배치

핵심(제안 품질 안전장치 → API·저장 → 자동화) 순서로 배치한다. 목요일 저녁까지 Phase 2 DoD를 충족하고, 금요일은 자동화(크론)로 마무리한다.

| 요일 | 날짜 | Task | 백로그 # | 우선순위 |
|---|---|---|---|---|
| 수 | 7/15 | LLM 출력 zod 검증 + 재생성·템플릿 폴백, 가드레일(할인율·길이·금칙어) | 2-3, 2-4 | P0/P1 |
| 목 | 7/16 | `generateProposal` 순수함수화 + `POST /proposal/generate`, 제안 campaigns 저장 + `GET /proposal/today` | 2-5, 2-7 | P0 |
| 금 | 7/17 | node-cron 매일 06:30 자동 제안 생성 | 2-6 | P1 |

**목요일 저녁 체크포인트 (Phase 2 DoD)**: `curl localhost:4000/proposal/today` 호출 시 오늘 제안 JSON 확인 + `campaigns` 테이블에 저장 확인.

---

## GitHub 이슈 초안 (총 5개)

### [2-3] LLM 출력 zod 검증 + 재생성·템플릿 폴백
라벨 제안: `P0`, `phase2`, `agent`, `BE` | 요일: 수 7/15

**설명**
LLM(Groq) 응답을 zod 스키마로 검증한다. 파싱 실패·스키마 위반 시 1회 재생성하고, 재실패하면 기본/전일 템플릿으로 폴백해 서버가 항상 유효한 제안을 반환하도록 한다.

**완료 기준 (DoD)**
- [ ] `Proposal` zod 스키마 정의, 파싱 실패·필드 누락 감지
- [ ] 위반 시 1회 재생성 로직
- [ ] 재생성도 실패하면 템플릿 폴백 반환 (에러로 죽지 않음)
- [ ] 단위테스트 (정상 / 위반→재생성 성공 / 재실패→폴백)

**관련 파일**
- `apps/server/src/agent/generate.ts`
- `packages/shared/src/proposal.ts`

---

### [2-4] 가드레일 (할인율·길이·금칙어)
라벨 제안: `P1`, `phase2`, `agent`, `BE` | 요일: 수 7/15

**설명**
제안 문구·프로모션의 안전 검사. 할인율 ≤ 20%, LMS 2,000byte 이내, 금칙어(의료·과장 표현) 차단. 위반 시 2-3의 재생성 흐름과 연결한다.

**완료 기준 (DoD)**
- [ ] 할인율 21% 거부, 금칙어 검출, byte 초과 거부
- [ ] 경계값 단위테스트 (20% 허용 / 21% 거부, byte 경계)
- [ ] 가드레일 위반 시 재생성 트리거로 연결

**관련 파일**
- `apps/server/src/agent/guardrails.ts` (신규)
- `apps/server/src/agent/generate.ts`

---

### [2-5] generateProposal 순수함수화 + POST /proposal/generate
라벨 제안: `P0`, `phase2`, `api`, `BE` | 요일: 목 7/16

**설명**
`generateProposal(storeId, date)`를 크론·API·테스트에서 공용으로 호출 가능한 형태로 정리한다(날씨 수집 → 진단 → LLM 생성 → 검증·가드레일 오케스트레이션). `POST /proposal/generate` 엔드포인트로 노출한다.

**완료 기준 (DoD)**
- [ ] `generateProposal`이 storeId·date 입력만으로 전체 파이프라인 수행
- [ ] `POST /proposal/generate` 호출 시 제안 JSON 반환
- [ ] curl로 동작 확인

**관련 파일**
- `apps/server/src/agent/generate.ts`
- `apps/server/src/routes/`

---

### [2-7] 제안 campaigns 저장 + GET /proposal/today
라벨 제안: `P0`, `phase2`, `api`, `BE` | 요일: 목 7/16

**설명**
생성된 제안을 `campaigns` 테이블에 저장(status `draft`)하고, `GET /proposal/today`로 오늘 저장된 제안을 조회한다.

**완료 기준 (DoD)**
- [ ] 제안 생성 시 `campaigns`에 저장 (weather·proposal·status)
- [ ] `GET /proposal/today`가 오늘 매장 제안을 반환 (없으면 생성 or 빈 응답 정책 명시)
- [ ] 실 DB로 저장·조회 검증

**관련 파일**
- `apps/server/src/routes/`
- `apps/server/src/db/`

---

### [2-6] node-cron 매일 06:30 자동 제안 생성 (임계 발동)
라벨 제안: `P1`, `phase2`, `agent`, `BE` | 요일: 금 7/17

**설명**
node-cron으로 매일 06:30에 오늘 날씨·진단을 계산하고, **예상 하락률이 −20% 이상인 날에만** `generateProposal`을 실행해 `campaigns`에 저장한다(마케팅 남발 방지 — 기획서 카니발라이제이션 통제). 평범한 날은 조용히 넘어간다. "아침에 서버가 스스로 준비"하는 에이전트 서사의 핵심.

> 임계 발동 시 **사장님 아침 알림 문자**(운영 알림)는 별도 항목 **3-8**에서 Solapi 발송과 함께 붙인다. 이번 2-6에서는 임계 판정·조건부 생성까지.

**완료 기준 (DoD)**
- [ ] 오늘 예상 하락률 계산 (진단 `diagnose`의 오늘 상태 편차 활용)
- [ ] 하락률 −20% 이상이면 제안 생성·저장, 미만이면 스킵
- [ ] node-cron 스케줄 등록 (매일 06:30), 서버 기동 시 활성화
- [ ] 수동 트리거(또는 짧은 주기 테스트)로 임계 발동/스킵 동작 확인

**관련 파일**
- `apps/server/src/jobs/daily.ts` (신규)
- `apps/server/src/index.ts`
