# SpecFit 개발 Task

우선순위: **P0** = 이번 스프린트 필수 / **P1** = 중요하나 순서 조정 가능 / **P2** = 여유 있으면(시간 부족하면 가장 먼저 뺄 항목)

## 환경 점검

- [ ]  [P1] jobs_data.csv / synthesize_specs.py가 data/ 폴더에 실제로 들어와 있는지 확인
- [ ]  [P1] README에 합성 데이터 고지 문구 포함 여부 확인, 없으면 한 줄 추가

## 2주차 — DB 스키마/시드 + 레이아웃 + 스펙 입력 폼 (Phase 1)

- [ ]  [P0] DB 스키마 + 시드 스크립트 — jobs/job_requirements 테이블 정의, jobs_synthesized.json → SQLite 적재, 적재 검증(862건/9그룹/인턴 90건)
- [ ]  [P0] 레이아웃 컴포넌트 포팅 — 헤더(로고+스테퍼 한 줄+홈/초기화), 랜딩 히어로+3단계 카드, 이어하기 배너(빈 상태만 우선). demo_12.html 디자인 톤(인디고/바이올렛 단일 액센트+라벤더 배경, site-frame 없는 풀블리드 레이아웃) 그대로 포팅 — 2026-07-20 레퍼런스 디자인 반영으로 demo_11.html(민트+파스텔+다크 액센트)에서 교체됨.
- [ ]  [P0] 스펙 입력 폼 컴포넌트 — 학력/경력/전공/외국어/자격증/컴활 입력(포커스·커서 유지 로직, 커밋 버튼 방식), localStorage 저장/복원 로직까지 포함해서 이번 주에 완결.
- [ ]  → 3주차 시작 전까지: 위 세 가지 전부 완료가 목표

## 3주차 — 상태 공유 + 조건 필터링 + 갭 분석 로직 착수 (Phase 2 + Phase 3 앞부분)

- [ ]  [P0] AppStateContext(라우트 간 상태 공유) + localStorage 동기화
- [ ]  [P0] Express 필터 API — GET /api/jobs, 직종 + is_intern 불리언만(근무지역 파라미터는 안 만듦 — 확정 사항), 페이지네이션, 에러 핸들링
- [ ]  [P0] 필터 UI + 공고 리스트 카드 + 이어하기 배너 완성 — getResumeStep/renderResumeBanner, 손상된 저장 상태 안전장치
- [ ]  [P0] 갭 분석 로직 포팅 착수 — evaluateJob/describeRequirement/runGapAnalysis/pickPrimaryStatus(5개 항목 AND, 1개 항목 보완 시뮬레이션만 카운트, 단순 개수 기반 점수화)

## 4주차 — 시각화 + 결과 화면 + 통합/배포 (Phase 3 마무리 + Phase 4, 압축)

- [ ]  [P0] GET /api/gap-analysis 엔드포인트 + 유닛 테스트
- [ ]  [P0] 시각화 — 통계 카드 3개, 도넛/막대그래프(커스텀 SVG), 인사이트 배너
- [ ]  [P0] 보완 인사이트 팝업 — plan_3.md 기준 3분기만(😊 늘어남 / 🎉 이미 충족 / 💪 0건). 데모의 4번째 "복합 미충족" 분기는 시간상 넣지 않음
- [ ]  [P0] 결과 화면 인터랙션 — 상태 탭/정렬, 공고 상세 모달, 데스크톱/모바일 레이아웃
- [ ]  [P0] e2e 플로우 테스트 — 랜딩→필터→스펙→로딩→팝업→결과→상세, "홈" vs "초기화" 차이 검증
- [ ]  [P1] 배포 — 프론트 Vercel, 백엔드 Render/Railway, 환경변수/CORS/SQLite 영속성 확인
- [ ]  [P2] 반응형 세부 점검, 모달 키보드 접근성(ESC/포커스 트랩) — 시간 남으면