# 2주차 주간 계획 (7/13 월 ~ 7/17 금)

> 백로그: [`WeatherPilot_백로그.md`](./WeatherPilot_백로그.md) Phase 1(1-1~1-8) + Phase 2 착수(2-1, 2-2)
> 이 문서의 이슈 초안을 그대로 복사해 GitHub(`shyang0319/hub`) → Issues → New issue에 붙여넣으면 됩니다.

## 라벨 체계

영역 구분용 라벨을 추가한다. **Issues → Labels → New label**에서 아래 4개를 먼저 만들어야 이슈에 붙일 수 있다 (기존 P0/P1·phase1·weather 등 라벨은 그대로 유지, 영역 라벨을 추가로 붙이는 방식).

| 라벨 | 의미 | 이번 주 해당 |
|---|---|---|
| `BE` | `apps/server` 백엔드 작업 | 1-1~1-8, 2-1, 2-2 (전부) |
| `FE` | `apps/web` 프론트 작업 | 없음 — Phase 3(7/18~)부터 등장 |
| `SHARED` | `packages/shared` 공용 타입 | Phase0 마무리 |
| `DOCS` | 문서·README·기획 문서 작업 | 없음 — Phase 5(README 정리 등)부터 등장 |

이번 주 11개는 전부 백엔드 작업이라 `BE`(+`SHARED` 1개)만 붙는다. `FE`/`DOCS`는 지금 당장 쓸 이슈가 없더라도 라벨은 미리 만들어 두면 다음 주부터 바로 쓸 수 있다.

## 사전 준비 (월요일 착수 전)

- `apps/server/.env` 로컬 생성 — 기상청 서비스키, OpenWeatherMap API 키, Supabase URL/anon key 입력 (커밋 금지)
- Day1 작업(1-2) 안에서 `.env.example`을 키 이름만 채워 커밋

## 요일별 배치

| 요일 | 날짜 | Task | 백로그 # | 우선순위 |
|---|---|---|---|---|
| 월 | 7/13 | `packages/shared` 공용 타입 + CI 확인, 격자 변환 함수, 기상청 클라이언트 | 0-8, 0-9, 1-1, 1-2 | P0/P1 |
| 화 | 7/14 | OpenWeatherMap 클라이언트, 앙상블 병합 로직, 폴백 | 1-3, 1-4, 1-5 | P0/P1 |
| 수 | 7/15 | Supabase 5개 테이블 스키마 + DB 클라이언트 연결 | 1-6 | P0 |
| 목 | 7/16 | 시드 데이터, `GET /weather/today`·`POST /sales`·CSV 업로드 파서 | 1-7, 1-8 | P0/P1 |
| 금 | 7/17 | 매출×날씨 상관 진단 로직, LLM 프롬프트 설계 초안 | 2-1, 2-2 | P0 |

**목요일 저녁 체크포인트 (Phase 1 DoD)**: `curl localhost:4000/weather/today`로 실제 오늘 부산 날씨 확인 + Supabase `daily_sales`에 30건 확인.

---

## GitHub 이슈 초안 (총 11개)

### [Phase0 마무리] `packages/shared` 공용 타입 스켈레톤 + CI 확인
라벨 제안: `P1`, `phase0`, `SHARED` | 요일: 월 7/13

**설명**
`packages/shared`가 아직 비어 있다. FE·BE가 주고받을 공용 타입(Scenario, Proposal, Campaign 등)을 이곳에 옮겨 양쪽이 import하도록 스켈레톤을 만든다. 기존 hub의 ESLint·CI(`tsc --noEmit`) 설정이 이번 주 작업(새 파일들)에서도 계속 초록불인지 확인한다.

**완료 기준 (DoD)**
- [ ] `packages/shared/src`에 최소 1개 이상 공용 타입 파일 생성, `apps/web`에서 import 가능
- [ ] GitHub Actions CI(`tsc --noEmit`, ESLint)가 이번 주 변경 후에도 통과

**관련 파일**
- `packages/shared/src/` (신규)

---

### [1-1] 위경도→기상청 격자(nx, ny) 변환 함수
라벨 제안: `P0`, `phase1`, `weather`, `BE` | 요일: 월 7/13

**설명**
기상청 단기예보 API는 위경도가 아닌 격자좌표(nx, ny)를 요구한다. Lambert Conformal Conic 변환 공식으로 위경도→격자 변환 함수를 구현한다.

**완료 기준 (DoD)**
- [ ] 부산 매장 위경도 입력 시 기상청 공식 예제 nx, ny 값과 일치
- [ ] 순수 함수로 구현 (단위테스트 가능한 형태)

**관련 파일**
- `apps/server/src/agent/weather.ts` (신규)

---

### [1-2] 기상청 단기예보 클라이언트
라벨 제안: `P0`, `phase1`, `weather`, `BE` | 요일: 월 7/13

**설명**
공공데이터포털 기상청 단기예보 API 호출 클라이언트. 타임아웃 3초, 실패 시 재시도 1회.

**완료 기준 (DoD)**
- [ ] nx, ny 입력 시 오늘 기온·강수·하늘상태 JSON 반환
- [ ] 3초 타임아웃 + 재시도 1회 동작 확인
- [ ] `.env.example`에 기상청 서비스키 항목 추가 커밋 (`.env` 자체는 커밋 금지)

**관련 파일**
- `apps/server/src/agent/weather.ts`
- `.env.example`

---

### [1-3] OpenWeatherMap 클라이언트
라벨 제안: `P0`, `phase1`, `weather`, `BE` | 요일: 화 7/14

**설명**
OpenWeatherMap API 클라이언트. 앙상블 2순위 + 기상청 장애 시 폴백 소스.

**완료 기준 (DoD)**
- [ ] 위경도 입력 시 오늘 기온·강수·날씨상태 JSON 반환
- [ ] 3초 타임아웃 + 재시도 1회 동작 확인

**관련 파일**
- `apps/server/src/agent/weather.ts`

---

### [1-4] 앙상블 병합 로직
라벨 제안: `P0`, `phase1`, `weather`, `BE` | 요일: 화 7/14

**설명**
기상청·OWM 두 소스를 하나로 병합한다. 단위 통일(℃, mm/h, %) 후 가중 평균(기상청 0.6 / OWM 0.4), 상태 코드 매핑표(비/눈/맑음 → 내부 enum), 강수 여부가 소스 간 갈리면 보수적으로 '강수' 채택.

**완료 기준 (DoD)**
- [ ] 두 소스 정상 응답 시 가중 평균 병합 결과 반환
- [ ] 강수 불일치 케이스에서 '강수'로 보수적 채택되는지 단위테스트로 검증

**관련 파일**
- `apps/server/src/agent/weather.ts`

---

### [1-5] 한쪽 소스 장애 시 폴백
라벨 제안: `P1`, `phase1`, `weather`, `BE` | 요일: 화 7/14

**설명**
기상청 또는 OWM 중 한쪽이 실패해도 남은 소스만으로 진행하고, 응답에 소스 수(sourceCount)를 표기한다.

**완료 기준 (DoD)**
- [ ] 한쪽 소스 타임아웃/에러 시 나머지 소스 단독 값 반환
- [ ] 응답 JSON에 사용된 소스 수 필드 포함
- [ ] 단위테스트로 폴백 시나리오 검증

**관련 파일**
- `apps/server/src/agent/weather.ts`

---

### [1-6] Supabase 스키마 5개 테이블 생성
라벨 제안: `P0`, `phase1`, `db`, `BE` | 요일: 수 7/15

**설명**
Supabase(PostgreSQL)에 5개 테이블을 생성하고 서버에서 클라이언트로 연결한다.

```
stores        (id, name, category, menu_tags, tone, lat, lng, nx, ny)
customers     (id, store_id, name, phone, consent_at, opt_out_at)  ← 더미만
daily_sales   (store_id, date, revenue, weather_snapshot JSONB)
campaigns     (id, store_id, date, weather JSONB, proposal JSONB,
               edited_copy, channels[], status: draft|approved|sent|scheduled)
coupons       (id, campaign_id, code, issued_to, used_at, order_amount)
```

**완료 기준 (DoD)**
- [ ] Supabase 프로젝트에 5개 테이블 생성 확인
- [ ] 서버에서 Supabase client로 연결 성공 (간단 select 쿼리 동작)
- [ ] Supabase URL/anon key를 `.env.example`에 키 이름만 추가

**관련 파일**
- `apps/server/src/db/` (신규)
- `.env.example`

---

### [1-7] 시드 데이터
라벨 제안: `P1`, `phase1`, `db`, `BE` | 요일: 목 7/16

**설명**
데모·진단 로직 개발에 쓸 더미 데이터를 삽입한다. 매장 1개, 더미 단골 142명(그중 수신동의 98명), 일매출 30일치.

**완료 기준 (DoD)**
- [ ] DB 조회 시 매장 1건, 고객 142건(동의 98건), 일매출 30건 확인
- [ ] 실개인정보 아닌 더미 데이터임을 시드 스크립트 주석/문서에 명시

**관련 파일**
- `apps/server/src/db/seed.ts` (신규, 파일명 예시)

---

### [1-8] `GET /weather/today`, `POST /sales`, CSV 업로드 파서
라벨 제안: `P0`, `phase1`, `api`, `BE` | 요일: 목 7/16

**설명**
날씨 앙상블 결과를 노출하는 조회 API, 일매출 수동 입력 API, POS 엑셀 내보내기를 가정한 CSV 업로드 파서(papaparse 등)를 구현한다.

**완료 기준 (DoD)**
- [ ] `curl localhost:4000/weather/today` 호출 시 오늘 부산 날씨 JSON 응답
- [ ] `POST /sales`로 매출 등록 시 `daily_sales`에 반영
- [ ] CSV 업로드 시 다건 매출이 일괄 등록됨

**관련 파일**
- `apps/server/src/routes/` (신규)

---

### [2-1] 매출×날씨 상관 진단 로직
라벨 제안: `P0`, `phase2`, `agent`, `BE` | 요일: 금 7/17

**설명**
`daily_sales` × `weather_snapshot`을 조인해 날씨 유형별 평균 매출 편차를 실데이터로 계산한다 (예: "이 가게는 비 오는 날 −18%"). 데이터 부족(콜드스타트) 시 업종 기본 계수를 쓰고 "추정치" 라벨을 붙인다.

**완료 기준 (DoD)**
- [ ] 시드 데이터 기준 날씨 유형별 평균 편차 수치 산출
- [ ] 데이터 부족 케이스에서 업종 기본 계수 + "추정치" 라벨 동작

**관련 파일**
- `apps/server/src/agent/diagnose.ts` (신규)

---

### [2-2] LLM 프롬프트 설계 초안
라벨 제안: `P0`, `phase2`, `agent`, `BE` | 요일: 금 7/17 (다음 주로 이어짐)

**설명**
날씨 JSON + 매장 프로필(업종·메뉴·톤) + 매출 통계 요약을 입력으로 LLM(Groq — 무료·OpenAI 호환, 원안 Claude API)에 보낼 프롬프트 초안을 작성한다. zod 스키마 강제·가드레일은 다음 주 별도 이슈로 이어간다.

**완료 기준 (DoD)**
- [ ] 프롬프트 템플릿 초안 작성
- [ ] LLM(Groq) 1회 호출 테스트로 출력 형태(title, copy, promo, channels) 확인

**관련 파일**
- `apps/server/src/agent/generate.ts` (신규)
