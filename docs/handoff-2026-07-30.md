# Codex 인수인계 프롬프트 2026-07-30

이 문서는 다른 Codex 계정 또는 새 세션에서 같은 저장소 작업을 이어받기 위한 최신 인수인계다.

## 시작 전 공통 확인

```powershell
cd D:\2026.1\AIAgentChallenge\hub
git -c safe.directory=D:/2026.1/AIAgentChallenge/hub status --short
git -c safe.directory=D:/2026.1/AIAgentChallenge/hub branch --show-current
Get-Content docs/status.md -Tail 120
Get-Content docs/tasks.md -Tail 120
```

현재 브랜치 기준 최근 커밋:

- `56cf9d2c Update project status notes`
- `e1045a79 Add demo video brief deck`
- `37df7f52 Refine pet runtime and sprite tooling`
- `ae8a7b10 Add manager LLM contract safeguards`
- `42ef60e1 Add showcase demo video URL`

주의:

- `public/assets/fx/`의 대형 사운드 에셋 삭제/신규 폴더는 아직 커밋하지 않았다. 사운드 작업 세션이 맡기 전에는 stage하지 않는다.
- `.codex-ppt-build/`와 `outputs/demo-video-brief.pptx.inspect.ndjson`는 PPT 작업 부산물이다. 기본적으로 커밋하지 않는다.
- 실제 API Key, Supabase Key, OpenAI Key는 코드와 문서에 쓰지 않는다.

## 줄기별 최신 상태

| 줄기 | 이름 | 최신 상태 | 핵심 파일 | 다음 판단 |
|---|---|---|---|---|
| A | App Flow / Reset / Component Consistency | 시작 메뉴의 `매니저 바꾸기`와 `다시 시작` 성격을 분리했고, restart 시 profile/manager/log/window/pixel TV/outside pet/blink/sync notice 초기화 범위를 보강했다. `windowRegistry`, `useWindowManager`, `useQuestLogSync`, `useOutsidePetRuntime`, `useQuestFlow` 쪽으로 책임 분리를 진행했다. | `src/App.tsx`, `src/data/windowRegistry.ts`, `src/hooks/useWindowManager.ts`, `src/hooks/useOutsidePetRuntime.ts`, `src/domain/appLifecyclePolicy.ts` | 브라우저에서 첫 진입, 매니저 바꾸기, 다시 시작, 새로고침, 기록 노트 open/close flow를 수동 검수한다. |
| C | LLM Manager API | Hono Manager LLM API v1 route 5종과 OpenAI provider wrapper, strict schema tool call, rate limit, rule fallback을 추가했다. React는 server route만 호출하고 실패 시 기존 fallback을 유지한다. | `server/routes/managerLlm.ts`, `server/contracts/managerLlm.ts`, `server/lib/managerLlmProvider.ts`, `server/lib/managerLlmFallback.ts`, `src/layers/storage/managerLlmApi.ts` | 실제 env key가 있는 환경에서 Network와 server log로 enabled path를 확인한다. key가 없으면 rule fallback이 정상이다. |
| 1 | State Machine / Interaction | outside roaming loop를 보강했다. walk/run만 수평 이동하고 idle/happy/hiding/recovering/focused/climbing은 이동하지 않으며, ladder climb은 climb/hold/descend/idle loop, platform jump는 jump/land/idle_on_platform/jump_down/idle loop로 순환한다. 플랫폼/사다리 창 z-index를 따라 pet layer가 올라오도록 수정했다. | `src/domain/outsidePetRuntime.ts`, `src/hooks/useOutsidePetRuntime.ts`, `src/domain/managerRuntimePriority.ts`, `src/domain/windowCompatibilityPolicy.ts` | 브라우저에서 기록 노트 open 후 free roam, 사다리 이동 중 pet attach, 플랫폼 이동 중 follow, 방향 반전과 animation speed를 눈검수한다. |
| B | Asset Runtime / Placement / Review Tool | sprite verifier가 alpha/corner/frame bbox 요약을 출력한다. Review tool placement는 v2 profile(`runtime:<pet>:<stage>:canonical`, `review:<set-id>`)과 motion별 active edge를 저장한다. Runtime `WindowPetInteraction`도 저장된 active edge와 실제 window z-index를 사용한다. | `src/components/SpriteSheetReviewTool.tsx`, `src/data/windowPetPlacements.ts`, `src/data/spriteSheetMetrics.ts`, `scripts/verify-sprite-sheets.mjs` | review tool에서 pink manager stage 2/1 후보의 hanging/hiding/climbing 위치를 조정하고 runtime 화면과 일치하는지 확인한다. |
| 2 | Sound / Game Feel | 설정 창 sound toggle, sound policy, placeholder fallback은 연결되어 있으나 실제 cyber-purr/클릭/웃음/실망/격려/음악창 wav는 아직 커밋되지 않은 대형 asset 정리 단계다. | `src/data/assetManifest.ts`, `src/domain/soundPolicy.ts`, `public/assets/fx/` | 대형 wav를 그대로 커밋할지, 압축/선별/외부 보관할지 결정한 뒤 작은 manifest 변경과 나누어 진행한다. |
| 3 | Pixel TV / World Theme | Pixel TV 기본 실행은 권한 허용 웹캠 또는 파일 fallback을 canvas에서 픽셀화해 TV 안에 표시한다. Photo capture는 TV frame과 전자 매니저를 함께 photo card로 남기는 prototype이 들어갔다. Projection mode는 TV 우클릭 속성의 변환/원복 flow와 `?projection=pepper` hidden route로 분리되어 있다. 하루 흐름 web theme는 아직 다음 작업이다. | `src/domain/pixelizer.ts`, `src/data/windowRegistry.ts`, `src/App.tsx`, `src/styles.css` | Pixel TV 권한 거부/허용, 파일 fallback, photo capture, projection 변환/원복을 수동 검수한다. 이후 T-708 해/달/빛 theme로 넘어간다. |

## 새 세션 공통 프롬프트

```text
이 저장소는 D:\2026.1\AIAgentChallenge\hub 입니다.
먼저 파일을 수정하지 말고 AGENTS.md, docs/status.md, docs/tasks.md, docs/handoff-2026-07-30.md를 읽어주세요.
현재 브랜치는 N019_김동민이고, 최근 커밋은 56cf9d2c, e1045a79, 37df7f52, ae8a7b10, 42ef60e1 입니다.
대형 사운드 에셋(public/assets/fx)과 .codex-ppt-build, outputs/*.inspect.ndjson은 현재 의도적으로 미커밋 상태일 수 있으니 함부로 stage하지 마세요.
실제 Supabase/OpenAI key는 읽거나 문서에 쓰지 마세요.
```

## A 세션 프롬프트

```text
A: App Flow / Reset / Component Consistency를 맡아주세요.

목표:
- 첫 진입, 매니저 선택, 프로필 설정, 오늘의 퀘스트, 기록 노트, 매니저 바꾸기, 다시 시작, 새로고침/재접속 flow를 코드 기준으로 점검합니다.
- 종료와 다시 시작의 차이, localStorage 보존/삭제 범위, 열린 창/기록/level/exp 초기화 일관성을 확인합니다.
- 컴포넌트가 단일 책임에 맞게 분리됐는지, 창 계열 컴포넌트와 registry 사용이 일관적인지 점검합니다.

먼저 수정하지 말고 다음을 조사하세요:
- src/App.tsx
- src/hooks/useWindowManager.ts
- src/hooks/useQuestFlow.ts
- src/hooks/useQuestLogSync.ts
- src/hooks/useOutsidePetRuntime.ts
- src/data/windowRegistry.ts
- src/domain/appLifecyclePolicy.ts
- docs/runtime-flow-report.md
- docs/status.md

출력:
1. 현재 flow 상태
2. 의도와 다른 flow 후보
3. reset/localStorage 보존 규칙 표
4. 수정이 필요하다면 파일 단위 최소 계획
5. 브라우저 수동 검증 시나리오
```

## C 세션 프롬프트

```text
C: LLM Manager API를 맡아주세요.

목표:
- Hono Manager LLM API v1의 route, contract parser, provider wrapper, rule fallback, rate limit, React API adapter를 점검합니다.
- 실제 LLM key가 없는 환경에서는 fallback이 정상이고, key가 있는 환경에서는 server route만 provider를 호출해야 합니다.
- 퀘스트 추천, 수락 직전 difficulty evaluation, stat evaluation, manager line, behavior intent의 schema와 실패 응답이 일관적인지 확인합니다.

먼저 수정하지 말고 다음을 조사하세요:
- server/routes/managerLlm.ts
- server/contracts/managerLlm.ts
- server/lib/managerLlmProvider.ts
- server/lib/managerLlmFallback.ts
- src/layers/storage/managerLlmApi.ts
- src/domain/managerBehaviorIntent.ts
- docs/api-contracts.md
- docs/agent-design.md

출력:
1. route별 입력/출력/fallback 표
2. 실제 key 없이 가능한 검증
3. 실제 key 필요 수동 검증
4. 비용/호출 폭주 방지 정책 점검
5. 부족하면 최소 수정 계획
```

## 1 세션 프롬프트

```text
1: State Machine / Interaction을 맡아주세요.

목표:
- Lumi outside roaming state machine이 순환 가능한지 점검합니다.
- walk/run/jump/climbing/hiding/idle의 위치 변화와 animation state가 일치하는지 확인합니다.
- 사다리/플랫폼 창 이동 시 pet attach 위치와 z-index가 즉시 따라오는지 확인합니다.
- 없는 animation state는 다른 매니저 sprite로 fallback하지 않고 같은 pet/stage idle로 정규화되어야 합니다.

먼저 수정하지 말고 다음을 조사하세요:
- src/domain/outsidePetRuntime.ts
- src/domain/outsidePetRuntime.test.ts
- src/hooks/useOutsidePetRuntime.ts
- src/domain/managerRuntimePriority.ts
- src/domain/windowCompatibilityPolicy.ts
- src/App.tsx
- src/styles.css

출력:
1. state machine phase/state 표
2. 각 state에서 허용되는 이동/정지 규칙
3. ladder/platform attach와 z-index 점검 결과
4. browser 수동 검증 시나리오
5. 수정 필요 시 TDD 우선 계획
```

## B 세션 프롬프트

```text
B: Asset Runtime / Placement / Review Tool을 맡아주세요.

목표:
- review tool에서 조정한 pivot/placement/active edge가 runtime WindowPetInteraction과 같은 계산식으로 반영되는지 확인합니다.
- hanging/hiding/climbing 위치가 창 크기, edge, z-index 변화에도 안정적인지 점검합니다.
- canonical runtime folder와 production candidates가 섞이지 않도록 asset manifest와 review set을 점검합니다.

먼저 수정하지 말고 다음을 조사하세요:
- src/components/SpriteSheetReviewTool.tsx
- src/data/windowPetPlacements.ts
- src/data/windowPetPlacements.test.ts
- src/data/spriteSheetMetrics.ts
- src/data/spriteReviewAssets.ts
- src/data/assetManifest.ts
- scripts/verify-sprite-sheets.mjs

출력:
1. review profile 저장 구조
2. runtime placement 적용 구조
3. canonical/candidate asset 사용 현황
4. 저장값이 사라질 수 있는 경우
5. 수정 필요 시 최소 계획
```

## 2 세션 프롬프트

```text
2: Sound / Game Feel을 맡아주세요.

목표:
- 실제 사운드 에셋을 어떻게 가져갈지 먼저 결정합니다.
- 대형 wav 전체를 커밋하지 말고, 필요한 음원만 선별하거나 압축/외부 보관/manifest 연결 방식을 제안합니다.
- cyber-purr, 클릭음, 웃음, 실망, 격려, 음악창 playlist를 muted 기본값과 설정 toggle 기준으로 연결할 계획을 세웁니다.

먼저 수정하지 말고 다음을 조사하세요:
- public/assets/fx/
- src/data/assetManifest.ts
- src/domain/soundPolicy.ts
- src/App.tsx
- src/styles.css
- docs/dynamic-asset-requirements.md
- docs/tasks.md의 T-713, T-726, T-727

출력:
1. 현재 사운드 파일 현황과 용량 리스크
2. 커밋할 파일/커밋하지 않을 파일
3. manifest 설계
4. 음악창 UI 최소 계획
5. 검증 방법
```

## 3 세션 프롬프트

```text
3: Pixel TV / World Theme을 맡아주세요.

목표:
- Pixel TV 현실 픽셀화, projection 변환/원복, photo capture 흐름을 수동 검수합니다.
- 하루의 흐름 web theme(T-708): 현실 시간에 따라 해/달 asset과 빛의 강도/색상이 XP desktop 배경에 반영되는 구조를 계획합니다.
- Pixel TV와 기록 노트/사다리/플랫폼/퀘스트 창의 window compatibility가 깨지지 않게 합니다.

먼저 수정하지 말고 다음을 조사하세요:
- src/domain/pixelizer.ts
- src/domain/pixelizer.test.ts
- src/domain/windowCompatibilityPolicy.ts
- src/domain/managerRuntimePriority.ts
- src/data/windowRegistry.ts
- src/App.tsx
- src/styles.css
- public/assets/icons/
- docs/tasks.md의 T-721, T-724, T-725, T-708

출력:
1. Pixel TV 현재 구현 상태
2. projection/photo capture 검증 시나리오
3. 하루 흐름 theme 설계 후보
4. 수정 파일 계획
5. 회귀 위험
```

## 검증 명령

수정 후 기본 검증:

```powershell
npm.cmd test
npm.cmd run typecheck
```

필요 시:

```powershell
npm.cmd run typecheck:server
npm.cmd run build
npm.cmd run verify:sprites
powershell -ExecutionPolicy Bypass -File scripts/verify-harness.ps1
```

