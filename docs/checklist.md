# 바로진료 개발 체크리스트

기준 문서:

- [기획서](./plan.md)
- [기능 명세](./feature-spec.md)
- [ERD](./erd.md)
- [UX 구조](./ux-structure.md)
- [디자인 시스템](./design-system.md)
- [개발 Task 및 백로그](./tasks.md)

각 항목은 구현, 직접 확인, 자동 테스트가 가능한 크기로 나눕니다.

# 1주차 - 개발 환경과 도메인 기반

## 1. 프로젝트 환경

- [x] npm workspaces 루트 설정을 만든다.
- [x] 환자·병원 관리자·플랫폼 관리자 React 앱을 각각 분리한다.
- [x] Express + TypeScript 서버를 `apps/api`에 구성한다.
- [x] 공통 타입·Zod 스키마 패키지를 `packages/shared`에 구성한다.
- [x] 웹 앱 공통 컴포넌트와 클라이언트 기반을 `packages/web-shared`에 구성한다.
- [x] 루트에서 웹과 API를 함께 실행하는 명령을 만든다.
- [x] `.env.example`과 환경변수 검증을 추가한다.
- [x] ESLint, Prettier, TypeScript 검사 명령을 통일한다.
- [x] Vitest와 Supertest 테스트 환경을 구성한다.
- [x] 4주 개발 Task와 P2 확장 백로그를 문서화한다.
- [x] Supabase CLI를 설치하고 `supabase/migrations` 구조를 만든다.
- [x] 개발용 Supabase 클라우드 프로젝트를 만들고 저장소와 연결한다.
- [x] `pg` 연결 풀과 `DATABASE_URL` 검증을 구성한다.
- [x] React에 Supabase Auth 클라이언트와 공개 환경변수를 구성한다.

## 2. 공통 상수와 타입

- [x] 계정 유형 `patient`, `hospital_admin`, `platform_admin`을 정의한다.
- [x] 계정 상태를 정의한다.
- [x] 병원 승인 상태를 정의한다.
- [x] 날짜별 대기열 상태 `open`, `paused`, `closed`를 정의한다.
- [x] 웨이팅 출처 `remote`, `onsite`를 정의한다.
- [x] 웨이팅 상태 6개를 정의한다.
- [x] 알림 종류와 mock 발송 상태를 정의한다.
- [x] 병원별 동적 환자 분류와 합계 함수를 만든다.
- [x] 환자 인원 입력 모드 `categorized`, `total_only`를 정의한다.

## 3. 마이그레이션·Repository·개발 데이터

- [x] `auth.users`를 참조하는 `profiles` 마이그레이션을 만든다.
- [x] 환자 분류 설정 버전과 분류 항목 마이그레이션을 만든다.
- [x] 접수별 분류 인원 마이그레이션을 만든다.
- [x] 계정 Profile Repository 인터페이스를 만든다.
- [x] 계정 Profile `pg` 구현체와 개발 Supabase 조회 통합 테스트를 만든다.
- [x] 병원·간단 입점 문의 Repository 인터페이스와 `pg` 구현체를 만든다.
- [x] 상세 입점 신청·증빙 Repository 인터페이스와 `pg` 구현체를 만든다.
- [x] 간단 입점 문의 수락 후 병원·owner 소속을 생성하는 트랜잭션 Service를 만든다.
- [x] 날짜별 대기열 Repository 인터페이스와 `pg` 구현체를 만든다.
- [x] 웨이팅·이력 Repository 인터페이스와 `pg` 구현체를 만든다.
- [x] 알림 Repository 인터페이스를 만든다.
- [x] 각 Repository의 `pg` SQL 구현체를 만든다.
- [x] 테이블·인덱스·제약조건·RLS를 마이그레이션 SQL로 만든다.
- [x] 모든 발생 시각을 `timestamptz`로 정의한다.
- [x] `supabase/seed.sql`에 환자·병원 관리자·승인 병원 테스트 데이터를 만든다.
- [x] `supabase/seed.sql`에 날짜별 통합 대기열과 원격·현장 웨이팅 테스트 데이터를 만든다.

## 4. 인증 P0

- [x] Supabase Auth 이메일·비밀번호 환자 회원가입을 연결한다.
- [x] Supabase Auth 병원 관리자 회원가입을 연결한다.
- [x] Brevo Custom SMTP를 Supabase Auth에 연결한다.
- [x] 이메일 확인 완료 전 로그인을 차단한다.
- [x] 인증 메일 재발송 화면과 제한 상태를 만든다.
- [x] 이메일 중복 가입을 차단한다.
- [x] 전화번호 중복을 차단한다.
- [x] 이메일 확인 후 `profiles`를 생성한다.
- [x] 로그인·로그아웃과 현재 프로필 조회를 만든다.
- [x] Supabase Bearer JWT 검증 미들웨어를 만든다.
- [x] 환자·병원 관리자 권한 미들웨어를 만든다.
- [x] 비로그인 원격 접수를 차단하는 테스트를 만든다.
- [x] 이메일 미확인 계정의 로그인을 차단하는 테스트를 만든다.

## 5. 대기열 계산 P0

- [x] 가족 실제 환자 수를 계산한다.
- [x] 보류·호출·취소 항목을 활성 순서에서 제외한다.
- [x] 앞 대기 환자 수를 계산한다.
- [x] 현재 진료 대기 순서를 계산한다.
- [x] 예상 대기시간을 계산한다.
- [x] 가족 3명이 3명으로 반영되는 단위 테스트를 만든다.
- [x] 순서 변경 후 계산이 갱신되는 단위 테스트를 만든다.

# 2주차 - P0 원격·현장 통합 웨이팅

## 1. 날짜별 대기열

- [x] 병원별 하루 1개 대기열 생성 규칙을 만든다.
- [x] 기본 평균 진료시간 10분을 적용한다.
- [x] 준비 6번째, 입장 4번째, 도착 20분 기본값을 적용한다.
- [x] 최대 원격 대기 환자 20명 기본값을 적용한다.
- [x] 병원별 운영일마다 숫자 접수번호를 `1`부터 중복 없이 발급한다.
- [x] `open`, `paused`, `closed` 전환 API를 만든다.
- [x] 일시 중지 상태에서 현장 접수가 가능한지 테스트한다.

## 2. 원격 웨이팅 API

- [x] 로그인 환자의 가족 인원 입력을 검증한다.
- [x] 계정 전화번호를 접수 스냅샷으로 저장한다.
- [x] 원격 한도를 실제 환자 수로 검사한다.
- [x] 계정당 활성 원격 웨이팅 1건을 차단한다.
- [x] 통합 대기열 마지막에 `remote_waiting`으로 추가한다.
- [x] 나의 활성 웨이팅 조회 API를 만든다.
- [x] 환자 직접 미루기 API를 만든다.
- [x] 환자 직접 취소 API를 만든다.

## 3. 현장 웨이팅 API

- [x] 직원의 전화번호·가족 인원 입력을 검증한다.
- [x] 계정 없이 `onsite_waiting`으로 등록한다.
- [x] 현장 상태 조회 토큰을 만들고 해시만 저장한다.
- [x] 현장 상태 링크 조회 API를 만든다.
- [x] 현장 환자가 원격 한도에 포함되지 않는지 테스트한다.
- [x] 현장 상태 응답에 직접 취소 동작을 포함하지 않는다.

## 4. 병원 관리자 대기열 API

- [x] 병원 소속 권한을 검사한다.
- [x] 오늘의 통합 대기열 조회 API를 만든다.
- [x] 원격 환자 도착·데스크 접수 처리 API를 만든다.
- [x] 진료실 호출 API를 만든다.
- [x] 직원 취소와 사유 기록을 만든다.
- [x] 보류·보류 해제와 복귀 위치 지정을 만든다.
- [x] 순서 변경을 트랜잭션으로 처리한다.
- [x] 상태 변경 이력을 저장한다.

## 5. P0 화면

- [x] 환자 이메일 회원가입·확인·로그인 화면을 만든다.
- [x] 원격 가족 인원 선택 화면을 만든다.
- [x] 원격 환자 대기 현황 화면을 만든다.
- [x] 병원 관리자 로그인 화면을 만든다.
- [x] 통합 대기열 표를 만든다.
- [x] 현장 환자 등록 모달을 만든다.
- [x] 원격 환자 도착 처리 동작을 만든다.
- [x] 호출·보류·취소·순서 변경 동작을 만든다.

# 3주차 - 자동 알림과 상태 링크

## 1. Mock 알림 기반

- [x] `NotificationProvider` 인터페이스를 만든다.
- [x] `MockNotificationProvider`를 만든다.
- [x] 알림 템플릿 코드와 변수를 정의한다.
- [x] mock 성공·실패 결과를 저장한다.
- [x] 같은 조건의 중복 알림을 막는다.

## 2. 원격 환자 자동 알림

- [x] 6번째 이하 준비 알림 판정 함수를 만든다.
- [x] 준비 알림에 현재 순서와 예상 시간을 넣는다.
- [x] 4번째 이하 입장 요청 판정 함수를 만든다.
- [x] 입장 요청 시 20분 도착 기한을 저장한다.
- [x] 6번째를 건너뛰면 준비 알림을 생략한다.
- [x] 직접 미룬 환자가 다시 4번째가 되면 입장 요청을 다시 보낸다.

## 3. 미도착 처리

- [x] 차례 도달 미도착을 마지막으로 한 번 이동한다.
- [x] 미도착 이동이 환자의 직접 미루기 횟수를 소비하지 않게 한다.
- [x] 최초 도착 기한을 연장하지 않는다.
- [x] 20분 만료 시 자동 취소한다.
- [x] 도착 처리와 만료가 동시에 요청될 때 충돌을 처리한다.

## 4. 현장 환자 알림과 링크

- [x] 현장 등록 완료 알림에 상태 링크를 넣는다.
- [x] 현장 4번째 진료 임박 알림을 한 번 보낸다.
- [x] 현장 상태 화면에 병원 정보와 전화하기를 표시한다.
- [x] `웨이팅 취소는 병원으로 전화해 주세요`를 표시한다.
- [x] 현장 화면에서 미루기·취소 버튼을 숨긴다.
- [x] 호출·취소 시 조회 토큰을 즉시 제거한다.
- [x] 무효 링크에 접수 내용을 노출하지 않는다.

## 5. 환자·직원 동기화

- [x] 활성 환자·직원 화면을 10초 Polling으로 갱신한다.
- [x] 호출·취소·보류 후 전체 순서와 예상 시간을 갱신한다.
- [x] 알림 발송 이력을 직원 상세 영역에 표시한다. (TDD 권장)
- [x] 네트워크 실패와 재시도 상태를 표시한다. (TDD 권장)

## 6. 자동 만료 작업

- [x] Express 백그라운드 작업을 1분 간격으로 실행한다.
- [x] PostgreSQL advisory lock으로 중복 실행을 막는다.
- [x] 만료 취소와 이벤트·mock 알림 기록을 한 트랜잭션으로 처리한다.
- [x] 동시에 두 작업이 실행돼도 한 번만 취소되는 테스트를 만든다.

# 4주차 - P1 탐색·입점·품질

## 1. 병원 검색

- [x] 병원명 검색을 만든다.
- [x] 시·도와 시·군·구 필터를 만든다.
- [x] 대표 진료과 필터를 만든다.
- [x] `내 주변` mock 병원 영역을 만든다.
- [x] 병원 상세에 전화번호·주소·운영시간을 표시한다.

## 2. 병원 입점 신청

- [x] 병원 관리자 계정 생성 화면을 만든다. (TDD 권장)
- [x] 로그인한 병원 관리자의 간단 입점 문의 화면을 만든다. (TDD 권장)
- [x] 약식 플랫폼 관리자의 문의 수락·거절 화면을 만든다. (TDD 권장)
- [x] 문의 수락과 병원 생성을 한 트랜잭션으로 처리한다. (TDD 권장)
- [x] 병원 기본정보 입력을 만든다. (TDD 권장)
- [x] 사업자등록번호·대표자명·개업일자 입력을 만든다. (TDD 권장)
- [x] 요양기관기호 입력을 만든다. (TDD 권장)
- [x] 사업자등록증 mock 이미지 업로드를 만든다. (TDD 권장)
- [x] 의료기관 개설신고증명서 mock 이미지 업로드를 만든다. (TDD 권장)
- [x] 상세 신청을 `pending`으로 저장하고 mock 검증 결과를 기록한다. (TDD 권장)
- [x] 플랫폼 관리자가 상세 신청을 승인·거절한다. (TDD 권장)
- [x] 승인 전 대기열 운영을 차단한다. (TDD 권장)
- [x] 승인 병원 관리자 화면의 `입점 진행`을 `병원 관리`로 전환한다.
- [x] 병원 정보 변경 요청에 기존값과 제안값을 저장한다.
- [x] 플랫폼 관리자에게 `병원 정보 변경사항` 비교 화면을 만든다.
- [x] 플랫폼 관리자 승인 후에만 병원 정보를 트랜잭션으로 반영한다.

## 3. 운영 설정과 예외

- [x] 평균 진료시간을 5분 단위로 설정한다. (TDD 권장)
- [x] 준비 기준이 입장 기준보다 큰지 검증한다. (TDD 권장)
- [x] 원격 접수 한도를 변경한다. (TDD 권장)
- [x] 기존 인원보다 한도를 낮춰도 기존 접수를 유지한다. (TDD 권장)
- [x] 잘못된 상태 전환에 오류를 표시한다. (TDD 권장)
- [x] 동시 순서 변경 충돌을 처리한다. (TDD 권장)

## 4. 개인정보·접근성·반응형

- [x] 기본 대기열에서 전화번호를 숨긴다. (TDD 권장)
- [x] 비밀번호·전화번호·조회 토큰을 로그에서 가린다. (TDD 권장)
- [ ] 키보드만으로 핵심 동작을 수행한다. (TDD 비권장)
- [x] 상태를 색상 외 텍스트와 아이콘으로 구분한다. (TDD 비권장)
- [x] 모바일 360px에서 환자 화면 겹침을 확인한다. (TDD 비권장)
- [x] 데스크톱 1280px에서 대기열 핵심 열을 확인한다. (TDD 비권장)
- [x] 로딩·빈 목록·오류·마감 상태를 만든다. (TDD 권장)

## 5. 최종 검증

- [x] 원격 환자 전체 시나리오를 검증한다. (TDD 비권장)
- [x] 현장 환자 링크 시나리오를 검증한다.
- [x] 6번째·4번째·미도착 자동 처리 시나리오를 검증한다. (TDD 권장)
- [x] 가족 인원과 원격 한도 경계값을 검증한다. (TDD 권장)
- [x] 보류·복귀·순서 조정 시나리오를 검증한다. (TDD 권장)
- [x] 병원 mock 신청·승인 시나리오를 검증한다. (TDD 비권장)
- [x] 프로덕션 빌드와 타입 검사를 통과한다. (TDD 비권장)

# P2 확장 백로그

- [ ] 병원 검색을 `GET /api/hospitals`와 PostgreSQL 조회로 전환한다.
- [ ] DB 검색 결과를 `approval_status = approved`인 병원으로 제한한다.
- [ ] 카카오·구글 소셜 로그인을 추가한다.
- [ ] 실제 알림톡·SMS 통합 중계 provider를 구현한다.
- [ ] 알림톡 실패 시 SMS 대체 발송을 구현한다.
- [ ] 플랫폼 관리자 승인 병원 이용 중지·복구 화면을 만든다.
- [ ] 국세청 사업자 진위·휴폐업 API를 연결한다.
- [ ] 심평원 병원정보 API를 연결한다.
- [ ] 증빙용 비공개 객체 저장소와 서명 URL을 적용한다.
- [ ] 파일 형식·용량·악성 파일 검사와 자동 파기를 적용한다.
- [ ] GPS와 지도 API로 실제 내 주변 검색을 만든다.
- [ ] 여러 의사·진료과 대기열과 증상 입력을 만든다.
- [ ] 병원 전산 시스템을 연동한다.
- [ ] 실제 처리시간 기반 예상 시간 자동 보정을 만든다.
- [ ] 운영 통계와 다지점 관리를 만든다.
- [ ] Express 자동 만료 작업을 Supabase Cron과 PostgreSQL 함수로 이전한다.
- [ ] 외부 공개 배포 환경과 운영용 Supabase 프로젝트를 구성한다.

# MVP 완료 기준

- [x] 이메일 확인 후 로그인한 환자만 원격 웨이팅을 등록할 수 있다.
- [x] 계정당 활성 원격 웨이팅은 1건이다.
- [x] 원격과 현장 환자가 날짜별 한 대기열에 표시된다.
- [x] 실제 가족 인원으로 순서와 예상 시간을 계산한다.
- [x] 원격 6번째·4번째 mock 알림과 20분 기한이 동작한다.
- [x] 직원이 원격 환자의 도착과 현장 접수를 처리한다.
- [x] 현장 환자가 알림 링크로 상태를 보고 전화 취소 안내를 확인한다.
- [x] 호출·취소 직후 현장 링크가 무효화된다.
- [x] 병원 입점과 증빙은 mock 데이터로 승인할 수 있다.
- [x] 다른 환자의 개인정보가 노출되지 않는다.
