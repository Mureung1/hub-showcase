# 1주차 남은 작업 체크리스트

> 강사 지침: 프로토타입은 한 번에 전체가 아니라 직렬로 작은 단위씩 진행. 각 단위 완성 후 사용자 확인 받고 다음 단위로 넘어간다.

## 프로토타입 (순수 HTML/CSS) — 직렬 진행: S2 → S3 → S1 → S4

### S2 알림 리스트 (홈) — ★ 우선 진행
- [x] 헤더
- [x] 알림 카드 1개
- [x] 카드 목록
- [x] 필터 칩
- [x] 정렬 표시 (마감 임박순)
- [x] 하단 탭 (조립 시 누락, 화면 연결 단계에서 발견·보수)

### S3 알림 상세
> 화면 대부분이 새 요소라 미니 시안 대신 화면 전체를 시안(s3-mini-tile.html)으로 확정 →
> reference/s3-detail.html로 편입, docs/prototype/s3.html에 일괄 조립. 아래는 조립 후 대조 검증.
- [x] 상단 바 (뒤로가기 · 공유) — reference와 대조 확인
- [x] 뱃지·태그 줄 (D-day · 중요, 출처) — S2 카드 레시피 재사용, reference와 대조 확인
- [x] 에이전트 요약 박스 (A안 확정) — reference와 대조 확인
- [x] 정보 블록 (마감일 · 출처 · 관련 키워드) — reference와 대조 확인
- [x] 원문 보러가기 링크 — reference와 대조 확인
- [x] 하단 버튼 2개 (나중에 / 완료로 표시) — reference와 대조 확인

### S1 온보딩
> 레시피 없는 2개 요소(연동 버튼·발신처 입력)만 미니 시안(s1-mini-tile.html)으로 확정 →
> reference/s1-onboarding-parts.html로 편입, 나머지는 기존 레시피 재사용해 s1.html에 조립.
- [x] 타이틀 영역 (보쇼 · 안내 문구)
- [x] 이메일 연동 버튼 (B안, "읽기 전용" 명시)
- [x] 관심 키워드 선택 (칩 + 직접입력) — v2 키워드 칩 재사용
- [x] 필수 발신처 등록 (라벨+힌트+입력+칩)
- [x] 시작하기 버튼 — S3 primary 재사용

### S4 설정
- [x] 관심 키워드 수정 — S1 재사용, 등록 상태
- [x] 필수 발신처 수정 — S1 재사용
- [x] 확인 주기 (칩 단일선택, 1시간 on)
- [x] 연동 계정 토글 3종 (이메일 on / 포털 공지 on / 취업진로포털 off "검증 중")
- [x] 학사일정 바로가기 외부 링크 (↗ 문법)
- [x] 하단 탭 (설정 활성)

### 화면 연결
- [x] s1 시작하기 → s2
- [x] s2 첫 카드 → s3
- [x] s3 뒤로 → s2
- [x] 탭 홈↔설정 (s2 설정 탭 → s4 / s4 홈 탭 → s2)

## 그 외 1주차 작업
- [x] docs/checklist.md 작업 분해 작성 (본 파일)
- [x] 포털 "신뢰 기기등록" 후 세션 만료 주기 실측 (이번 주 병행)
- [ ] eclass Open API 존재 여부 확인 — 2주차로 이연 (공개 채널 2종으로 MVP 충분, 채널 플러그인 구조라 추후 추가)

## 2주차 — 화면과 서버를 연결한 핵심기능 (수직슬라이스)
> ⚠️ 초안 — 커리큘럼 요구사항 기반. 각 단계는 착수 시점에 학습 내용을 반영해 재분해한다 (1주차 S3~S4 방식과 동일)
> 스택 확정(커리큘럼): React / Express / Supabase / REST

### 0. 계획·계약 (최우선)
- [x] 알림 스키마 확정 — s3.html 빈칸에서 역산 → Supabase 테이블 설계로 사용
- [ ] 핵심 시나리오를 작업 단위로 나눠 GitHub 이슈 등록 (커리큘럼 요구)
- [ ] 계획 수립 전용 Agent 제작 — 요구사항→작업 분해·우선순위
- [ ] 기능 검증 전용 Agent 제작 — check_tokens.py의 정신을 기능 검증으로 확장

### 1. FE 선행 — mock으로 화면 흐름 (커리큘럼 순서)
> 컴포넌트 트리: HomeScreen(state: notifications, filter 보유)
>   ├─ Hero (presentational)
>   ├─ ChipFilter (props: categories, active, onFilterChange)
>   ├─ NotificationList → NotificationCard (props: notification, onComplete)
>   └─ TabBar (props: activeTab)
> 진행 방식: 아래에서부터 하나씩 — 한 task 완성 → 브라우저 검증 → 확인 → 다음 task
- [x] Vite 프로젝트 세팅 (web/) — npm run dev로 빈 화면 확인
- [x] tokens.css 사본 이식 (+출처 주석)
- [x] DdayBadge 컴포넌트 — 3단계(urgent/interest/normal) 배색 렌더 확인
- [x] NotificationCard — mock 1건을 props로 받아 s2 카드와 동일 렌더
- [x] NotificationList + mock 배열 — 카드 여러 개 렌더
- [x] ChipFilter — 필터 클릭 시 목록 변경
- [x] TabBar
- [x] Hero 컴포넌트 — 날짜·인사·임박건수(urgent 개수 계산) + 그라데이션 배경,
      s2.html 히어로 레시피 기준
- [x] 정렬 표시 — "마감 임박순 ↓", s2.html .sortbar 레시피 (필터와 목록 사이)
- [x] AppFrame — 2층 배경(--bg-deep 무대 + 340px 앱 면), 부품들을 이 안에 담기
- [x] HomeScreen 조립 — Hero→필터→정렬→목록→탭 배치 + 완료 동작
      · 완료 트리거는 카드에 작은 임시 버튼(체크)으로 붙임 — 카드 전체 클릭이
        아님 (카드 클릭 = 상세 열기 자리는 비워둠)
      · 완료 클릭 → 카드 흐려짐 + 필터에서 제외 (mock, 새로고침하면 되살아남)
      · 이 완료 버튼은 S2 임시 배치. 정식 위치는 S3 "완료로 표시" 버튼 —
        S3 React 이관 시 이어붙임 (기록용 주석 필수)
> 화면 전환(탭 라우팅)은 s1~s4 React 이관 후 연결, 원문 링크(카드→상세)는
> S3 이관 시 연결 — 둘 다 이번 FE 선행 범위 밖

### 1.5 MSW로 API 흐름 검증 (mock → fetch 전환, 백엔드 전 리허설)
> 목적: 컴포넌트를 "진짜 API 쓰는 형태"로 바꿔 fetch·로딩·에러 흐름을 미리
> 검증. 나중에 Express 생기면 MSW만 끄면 실서버로 전환됨(화면 코드 무변경).
> 스키마 계약(plan.md 7-1)이 FE와 (가짜)서버의 접점.
> 주의: MSW는 메모리라 저장 아님 — 완료해도 새로고침하면 되살아남.
>       진짜 저장은 2번 Supabase에서.

- [x] MSW 설치 + 초기화 — 개발 모드에서만 서비스워커 켜지게
- [x] mock 핸들러 작성 (확정 스키마 형태로 응답):
      · GET /api/notifications → 알림 목록 배열 반환
      · PATCH /api/notifications/:id → 해당 알림 done:true 처리 후 반환
      · mockNotifications를 App.jsx → 핸들러로 이동 (가짜 서버의 데이터)
- [x] App.jsx fetch 전환:
      · useEffect로 GET 호출 → 받은 목록을 useState에 담기 (첫 렌더 1회)
      · 완료 버튼 → PATCH 호출 → 응답 반영 (markDone을 fetch 기반으로)
- [x] 로딩·에러 상태 처리:
      · MSW에 짧은 딜레이 넣어 로딩 상태 실제로 보이게
      · 목록 오기 전 로딩 표시 / 실패 시 에러 표시
- 완료 기준:
      · 네트워크 탭에 /api/notifications 요청이 실제로 잡힘
      · 화면 동작은 이전과 동일 (목록·필터·완료 다 됨) + 로딩·에러 뜸
      · "겉보기 같지만 속은 API 통신 구조"가 검증 포인트

### 2. BE·DB — 한 사이클 관통
> 목표: 완료 누르고 새로고침해도 남는 상태 = 수직슬라이스 완성
> 검증 구역: D-day·API 계약은 자동 테스트 / 화면 모양은 눈 검증

> 마감 임박순 실제 정렬 — SortIndicator는 "마감 임박순 ↓" 표시만 하고 실제
>   정렬은 미구현. D-day 계산에 의존하므로 함수 완성 후 가능해졌으나, DB·API로
>   실데이터가 흐른 뒤(2-6 이후) 얹는다. done 카드 하단 정렬도 이때 함께.

#### 2-1. D-day 계산 (TDD 첫 바퀴) ← 교체보다 먼저. 뱃지가 이것에 의존
- [x] mock deadline을 표준 형식으로 교체 + 미래 날짜로 갱신
      완료: 4건 다 파싱 가능, 지난 날짜 없음
- [x] 시나리오 목록 — 정상 / 오늘 마감 / 지남 / 마감 없음 / 완료됨
      완료: 표로 정리, 내가 읽고 빠진 케이스 확인
- [x] red — 테스트 + 스텁. 기준일을 인자로 받는 형태
      완료: 터미널에 빨강 (import 오류 아닌 진짜 실패)
- [x] green — 최소 구현
      완료: npx vitest run 초록

> refactor: 코드가 짧고 한 가지 일만 하며 중복 없어 정리 생략 (억지 수정이 더 위험).
>   단 getHours()가 로컬 시간대 의존 — 배포 단계에서 재확인 (checklist 배포 항목 참고).

- [x] 화면 연결 — ddayLabel 임시 필드 제거
      · NotificationCard: 마감 없으면 뱃지 자체를 안 그림
      · Hero 날짜 "7월 13일" → 진짜 오늘
      완료: 뱃지가 계산값으로 나옴 + 레이아웃 안 깨짐 (눈 검증)

> 열린 질문(이후): 마감 지남 + done:false(사용자가 놓친 것)를 완료 카드와
>   똑같이 흐리게 처리할지 미정. 2-1은 뱃지 문구만 계산하고, 흐림 적용 범위는
>   화면 로직 단계에서 결정. "마감 지남" 뱃지 문구 자체는 2-1 시나리오에 포함.

#### 2-2. Supabase 테이블 + 시드
- [ ] 프로젝트 생성
- [ ] CREATE TABLE — 실행 전 한 줄씩 설명 듣고 승인
      완료: Table Editor에 확정 컬럼대로 생김
- [ ] 시드 4건 — deadline은 now() + interval 상대 날짜
      완료: 조회되고, 시간이 지나도 D-day가 유효

#### 2-3. Express 뼈대
- [ ] server/ 세팅 + Hello
- [ ] app 정의와 listen 분리 (테스트용 구조)
      완료: 왜 나눴는지 내 말로 설명 가능
- [ ] .gitignore 먼저 → .env → supabase client
      ⚠️ service_role 키 커밋 금지
      완료: select()가 시드를 배열로 반환
- [ ] express.json() + 로거

#### 2-4. GET /api/notifications
- [ ] router → controller → service 계층 구현
- [ ] controller 단위 테스트 (service는 스텁)
- [ ] supertest 도입 + 통합 테스트 (배열 반환)
      완료: 브라우저에서 JSON 확인

#### 2-5. PATCH /api/notifications/:id
- [ ] 구현 + 테스트 (done:true / 없는 id는 404)
      ⚠️ params.id는 문자열, DB id는 숫자 — 변환 필요
      완료: DB에서 done이 실제로 바뀜

#### 2-6. 교체 ← 수직슬라이스 완성 지점
- [ ] vite.config.js에 proxy (/api → localhost:3000)
- [ ] MSW 끄기 (main.jsx)
      ⚠️ 안 끄면 서비스워커가 가로채서 교체 검증이 무효
      완료: 완료 누르고 새로고침 → 상태가 남음
      완료: 서버 끄면 에러 화면이 떠야 정상

#### 2-7. 기록
- [ ] plan.md 결정 히스토리 + 컬럼 확정
- [ ] CLAUDE.md 진행 상황
- [ ] 커밋 + PR (⚠️ push 전 #1653 상태 확인)

### 3. 에이전트 투입 (슬라이스 완성 후)
- [ ] Gmail/포털 수집 → LLM 판단 → Supabase에 알림 insert
      (2번까지 완성돼 있으면 에이전트가 넣는 순간 화면에 나타남)
- [ ] eclass Open API 확인 (1주차 이연분)

## 향후 고려
- 사용자 로그인 + 개인화 렌더링: 2~3번 사이 착수, 핵심 기능 B(관심 키워드 기반 필터링)와 연결 — S1 온보딩 등록값이 기준
- 알림 목록 개수 관리: 완료 처리·기간 지난 건 자동 정리로 1차 대응, 실제 데이터가 많아지면 페이징/제한 재검토 (지금은 미구현)
- 앱 높이 660px(`--frame-h`) 고정은 프로토타입/데스크톱 기준 — 실제 폰 화면 반응형(뷰포트 꽉 채우기)은 배포 단계에서 다듬을 것
- 완료 항목 목록 하단 정렬 — done인 카드를 아래로 내리는 정렬은 "실제 정렬(마감순)" task와 함께 처리 (지금은 제자리 흐림)
- 검증 방식 구분 — 정답이 정해진 로직(D-day 계산, API 계약, 파싱)은 자동 테스트(TDD), 보이는 것(화면 모양·색·여백)은 눈 검증. 담당 구역이 다름
- Hero 문구 조건부 — urgentCount가 0일 때 "마감 임박 0건, 놓치지 마세요" 대신 다른 문구(예: 급한 건 없다는 안내)로 분기. 구현은 삼항 연산 한 줄로 간단, 문구 카피는 디자인 시스템 톤에 맞춰 "하드코딩→실데이터 전환" 묶음에서 함께 결정

### 하드코딩 → 실데이터 전환 대기
- Hero 날짜: 현재 "7월 13일" 고정 → `new Date()`로 실제 날짜 계산. 단 mock의 D-day 기준과 맞물려 있으므로 deadline→D-day 계산 task와 함께 처리
- Hero 사용자 이름: "두희님" 고정 → 로그인/사용자 정보 단계에서 실제 이름으로 (위 "사용자 로그인 + 개인화 렌더링" 항목과 연결)
- D-day 라벨: `ddayLabel` 임시 하드코딩 → deadline에서 계산
