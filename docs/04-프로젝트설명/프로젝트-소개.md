# 프로젝트 소개

> Mealyze(구 CJMT = Chungnam Just-eat Meal Tracker) — 음식 사진 한 장 → 내 몸 기준 부족 영양소와
> 보충 메뉴·식당을 알려주는 한국어 영양 관리 웹 서비스.
> (Naver Connect / Boostcamp AI Agent Challenge 과제로 시작)

기술적으로 정확한 최신 상태(스택·아키텍처·환경변수)는 이 문서가 아니라 항상 `CLAUDE.md`와
[../03-개발스펙/architecture.md](../03-개발스펙/architecture.md), 루트의 `README.md`를 기준으로 본다. 이 문서는
"왜 이 프로젝트를 시작했고 무엇을 겪었는지"에 초점을 둔 서술형 소개다.

---

## 1. 문제 · 차별점

- **문제**: 학교·주변 식당은 영양 정보가 없고, 매번 칼로리를 손으로 계산하기는 귀찮고, 지금 내
  몸에 무엇이 부족한지는 더욱 판단하기 어렵다.
- **차별점**: 단순 "사진 찍으면 칼로리 나오는" 앱을 넘어, **내 신체 기준으로 지금 무엇을 더 먹어야
  하는지**를 진단하고, 그걸 채울 수 있는 메뉴·주변 식당까지 이어서 추천한다.
- **형태**: 모바일 우선 웹앱 + 같은 웹을 그대로 로드하는 Android WebView 래퍼(Capacitor). 웹을
  배포하면 앱도 자동으로 갱신된다(자세한 내용: `CLAUDE.md`의 "Android app wrapper" 절).

## 2. 화면 구조 (하단 탭 5개)

홈(촬영·분석) · 식단 · 달력 · 지도 · MY

- **홈(`/analyze`)**: 로그인·신체정보 여부와 무관하게 앱을 열면 항상 이 화면부터 시작한다(게스트
  우선 설계). 사진 업로드로 음식을 분석한다.
- **식단(`/meals`)**: 오늘 먹은 음식 목록 + 권장량 대비 그래프. 부족 영양소 기반 쿠팡 파트너스
  광고 캐러셀과, 로그인 계정끼리 비교하는 "오늘의 순위" 리더보드 카드가 이 탭에 있다.
- **달력(`/calendar`)**: 날짜별 영양 상태를 3단계 색상으로 보여준다.
- **지도(`/map`)**: 부족 영양소를 보완할 수 있는 주변 식당을 추천한다(네이버 지역 검색 + 네이버
  지도).
- **MY**: 신체정보 입력, 로그인/로그아웃, 데이터 백업(CSV) 내보내기/가져오기.

## 3. 알고리즘 핵심

식약처 공공 DB 기반 영양 매칭 캐스케이드, BMR/TDEE 기반 권장량 계산, 광고·리더보드 점수 로직은
전용 문서로 분리했다 — [../01-알고리즘/영양-분석-알고리즘.md](../01-알고리즘/영양-분석-알고리즘.md),
[../01-알고리즘/광고추천-리더보드-알고리즘.md](../01-알고리즘/광고추천-리더보드-알고리즘.md).

## 4. 개발 히스토리 — 주요 전환점

프로젝트가 진행되며 초기 설계에서 크게 바뀐 결정들이다. 상세 변경 이력은 루트의 `CHANGELOG.md`
참고.

| 초기 | 현재 | 이유 |
|---|---|---|
| Google Gemini 직접 호출 | OpenRouter 경유 Gemini | 무료 티어 한도(하루 20회)로 503/429 빈발 → 유료 크레딧 전환으로 안정화 |
| 카카오맵 + 카카오 로컬 검색 | 네이버 지도 + 네이버 API Hub 지역 검색 | 지도·식당 검색을 네이버로 이전(카카오 코드는 롤백용으로 저장소에 남겨둠) |
| Google OAuth 로그인 | 아이디+비밀번호 로그인 | 앱(WebView)에서 임베디드 OAuth가 막히는 문제를 원천 제거 |
| 비밀번호 평문 저장(초기 데모 한정 계획) | Supabase Auth(bcrypt) | 로그인 방식을 바꾸며 실제 해시 저장으로 전환 — 더 이상 평문 저장이 아님 |
| localStorage만 사용 | 로그인 시 Supabase, 게스트는 localStorage | `dataStore.js`가 세션 여부로 저장소를 분기 |
| 프로젝트명 CJMT | Mealyze | 명칭 변경. `cjmt` 폴더명·`cjmt:` localStorage 키 접두사는 기존 게스트 데이터 호환을 위해 코드에 남아 있음 |

## 5. 개발 과정에서 해결한 주요 이슈 (회고)

- **API 비용/한도**: 위 표 참고. 프록시에 429/503 지수 백오프(1s→2s→4s)를 적용해 안정화했다.
- **Gemini 인증 방식 변경**: 신형 키(`AQ.Ab...`)는 `?key=` 쿼리 대신 `x-goog-api-key` 헤더가
  필요했다.
- **영양 과대추정**: 프롬프트 튜닝(한국 표준 1인분 앵커)만으로는 부족해, 식약처 공공 DB 연동으로
  근본적으로 해결했다 — 자세한 내용은 알고리즘 문서 참고.
- **CSV 왕복 버그**: 내보내기 진입점이 2개(MY 탭/달력 탭)인데 가져오기가 한 형식만 인식하던 문제.
  가져오기가 두 형식을 자동 판별하도록 고쳐 해결했다 — `CLAUDE.md`의 "CSV backup" 절 참고.
- **환경 이슈**: OneDrive 동기화 경로 + 한글/공백이 섞인 디렉터리명에서 일부 CLI 동작이 불안정한
  경우가 있었다(예: `git mv`가 대상 디렉터리를 미리 만들어줘야 동작).

## 6. 남은 작업

기능 구현은 대부분 끝났고, 남은 항목은 대개 "코드가 아니라 사람이 콘솔/대시보드에서 눌러야 하는"
것들이다. 최신 체크리스트는 [week3-release-checklist.md](week3-release-checklist.md) §C 참고 —
Supabase 인증 설정, 기존 계정 데이터 정리, 쿠팡 파트너스 링크 교체, APK 재빌드.

그 외 보류 중인 작업:
- 쿠팡 상품 데이터 자동 수집(파트너스 API 승인 대기) —
  [../03-개발스펙/next-쿠팡API-자동추천-프롬프트.md](../03-개발스펙/next-쿠팡API-자동추천-프롬프트.md)
- 비용 규모 재검토(사용자 수 증가 시) — [cost-analysis.md](cost-analysis.md)
