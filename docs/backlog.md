# 개발 Task & 백로그 (`docs/backlog.md`)

기획서(`docs/plan.md`)에 명시된 화면·기능·시나리오에서만 Task를 도출했습니다. 제품 범위 확장(다국어, 소셜기능 등)은 기획서에 근거가 없어 포함하지 않았습니다.

## 백로그란?

앞으로 해야 할 일을 우선순위와 함께 모아둔 목록. 완료된 일이 아니라 **아직 안 한 일들의 저장소**로,
매주 우선순위 높은 것부터 꺼내 쓰는 방식으로 관리한다.

## 우선순위 기준

기획서 5번 항목에서 기능①이 "최우선순위"로 명시되어 있어, 이를 기준으로 삼았습니다.

- **최우선**: 기획서에서 명시적으로 최우선순위로 지정한 기능 (①)
- **P0**: 없으면 서비스가 굴러가지 않는 필수 기능 (②, ③) 및 그 기반이 되는 뼈대 작업
- **P1**: 필수 기능을 완성하기 위한 후속/보조 작업

---

## 4주 로드맵

| 주차 | 목표 | 상태 |
|---|---|---|
| 1주차 | 기획서 확정, 프로토타입(HTML/CSS) 3화면 제작, 디자인 Skill 정의, 개발 환경(React+Express) 구성 | ✅ 완료 |
| 2주차 | 화면 뼈대(대시보드/리더뷰/마이페이지) 및 백엔드 API 뼈대 제작 | 🔵 진행 중 |
| 3주차 | 기능①·②·③ 실제 로직 구현 | ⬜ 예정 |
| 4주차 | 통합 테스트, 버그 수정, 배포, 발표 준비 | ⬜ 예정 |

> 주차 배분은 기획서에 명시된 내용이 아니라, 이번 미션("4주간 언제 어떤 작업을 할지 정한다")에 따라 저희가 자체적으로 수립한 일정입니다.

---

## Task 목록

### 2주차 — 화면 뼈대 + 백엔드 뼈대

| Task | 설명 | 우선순위 | 상태 |
|---|---|---|---|
| 대시보드 화면 뼈대 | 오늘의 핵심 외신 3개 카드(로고+영문제목+한줄번역) UI | P0 | ⬜ |
| 리더뷰 화면 뼈대 | 원문 렌더링 컨테이너 레이아웃 (더미 텍스트) | 최우선 | ⬜ |
| 리더뷰 로딩 스켈레톤 UI | 원문 파싱 대기 중 노출할 스켈레톤/로딩 상태 UI | P0 | ⬜ |
| 단어장 화면 뼈대 (신규) | 최신순 정렬 단어 목록 + 출처 표기 UI (더미 데이터) | P1 | ⬜ |
| 문단 요약 UI | 요약 버튼 + 3줄 불릿 표시 영역 (더미 데이터) | P0 | ⬜ |
| AI 인사이트 패널 UI | 기사 하단 패널 (더미 문구) | P0 | ⬜ |
| 매수/관망/매도 버튼 UI | 버튼 3종 + 완료 토스트 UI | P0 | ⬜ |
| 마이페이지 화면 뼈대 | 히스토리 목록 UI + 나의 판단/marketSentiment 비교 표시 영역 (더미 데이터) | P1 | ⬜ |
| 서버 뼈대 구성 | API 서버 기본 구조 (디렉토리·라이브러리 설치, CLAUDE.md 반영) | P0 | ✅ |
| LLM Mock 모드 구성 | 환경변수 MOCK_LLM으로 전환되는 더미 응답 모드. 파라미터에 "FAIL_TEST" 포함 시 의도적으로 실패 케이스 반환(프론트 에러 UI 테스트용), 그 외엔 성공 케이스 반환. 과금·Rate Limit 부담 없이 개발 | P1 | 🔵 |
| API 스펙(JSON) 정의 | 기사 상세·요약·인사이트 응답 데이터 스키마를 프론트-백엔드가 사전 합의 (더미 API 제작 전 선행) | P0 | ✅ |
| 더미 응답 API | 합의된 스펙대로 화면-서버 통신 확인용 mock 데이터 | P0 | ⬜ |

### 3주차 — 기능 로직 구현

| Task | 설명 | 우선순위 | 상태 |
|---|---|---|---|
| [백엔드] 외신 URL 파싱 API | 프론트에서 브라우저로 직접 원문을 긁으면 CORS에 막히므로, 서버가 외신 URL을 받아 본문 텍스트만 파싱해 반환. User-Agent 헤더 필수 포함(봇 차단 대비), 파싱 실패 시 더미 기사 텍스트 Fallback 반환 | 최우선 | ⬜ |
| [프론트] 파싱 API 연동 및 렌더링 | 파싱 API 응답을 받아 리더뷰에 렌더링, 대기 중엔 로딩 스켈레톤 노출 | 최우선 | ⬜ |
| AI 프롬프트 최적화 및 튜닝 | 문장 번역·문단요약·인사이트·marketSentiment 해설이 원하는 형식으로 나오도록 프롬프트를 별도로 설계·반복 테스트 (로직 구현과 분리해 일정 리스크로 관리). 현재 analyzeArticle은 MOCK_LLM과 무관하게 항상 더미 응답이므로, 실제 로직 연결 시 MOCK_LLM 분기(FAIL_TEST 포함)도 함께 배선할 것 | 최우선 | ⬜ |
| 문장 단위 해설 생성 | analyze API 응답에 sentences 배열 추가 — LLM이 구조상 어려운 문장만 선별해 한글 번역 제공. 전체 문장이 아닌 선별된 일부에만 적용 (원문 학습 목적 훼손 방지) | 최우선 | ⬜ |
| 문장 단위 해설 UI | 어려운 문장을 탭하면 **아코디언**(팝업 아님)으로 문장 아래에 번역이 펼쳐지는 컴포넌트 — 긴 번역이 원문을 가리지 않게 함 | 최우선 | ⬜ |
| 핵심 용어 자동 추출·적재 로직 (신규) | analyze API가 기사당 3~5개 핵심 용어를 자동 선별, 출처(기사 제목/URL)와 함께 단어장 저장소에 자동 적재. 사용자 탭 등 별도 액션 불필요 | 최우선 | ⬜ |
| GET /api/vocabulary 구현 (신규) | 누적된 단어장을 최신순으로 반환하는 엔드포인트 | P0 | ⬜ |
| 단어장 화면 API 연동 (신규) | 단어장 화면에서 실제 데이터 조회, 출처 클릭 시 해당 기사(리더뷰)로 이동 | P1 | ⬜ |
| 문단 3줄 요약 생성 | 튜닝된 프롬프트로 AI 문단 요약 로직 연결 | P0 | ⬜ |
| 종목 영향 한 줄 해설 + marketSentiment 생성 | 튜닝된 프롬프트로 AI 인사이트 로직 연결, 기사 톤(호재/악재/중립) 판별 결과 함께 반환 | P0 | ⬜ |
| 투자 판단 저장 로직 | 매수/관망/매도 클릭 시 URL+AI요약본+선택+marketSentiment 서버 저장. decisions.json은 fs.readFileSync/writeFileSync로 동기 처리(연속 클릭 시 파일 깨짐 방지) | P0 | ⬜ |
| 완료 토스트 연동 | 저장 성공 시 "오늘의 모의 투자 판단 완료!" 노출 (서버 전송 → 저장 → 토스트 순서 준수) | P1 | ⬜ |
| 판단 없는 이탈 처리 | 매수/관망/매도 없이 뒤로가기/이탈 시 "읽기 완료"로만 처리되고 판단은 저장되지 않는지 확인. 완독(Primary)·판단수행률(Secondary) 지표를 분리 집계하는 로깅 포인트 확보 | P1 | ⬜ |

### 4주차 — 마무리

| Task | 설명 | 우선순위 | 상태 |
|---|---|---|---|
| 마이페이지 히스토리 연동 | 저장된 판단 기록 실제 조회, 나의 판단(decision)과 AI marketSentiment를 나란히 비교하는 UI 확인 | P0 | ⬜ |
| 히스토리 상세뷰 + 원문 버튼 | 히스토리 클릭 시 저장된 요약·판단·marketSentiment를 항상 먼저 보여주는 상세 화면(모달/뷰) 구현, 하단에 [원문 기사 보러가기 ↗] 버튼 배치. 서버 측 링크 만료 판단 로직은 불필요(설계 단순화로 제거 — 만료 시 사용자가 버튼 클릭 후 브라우저에서 자연스럽게 404를 보게 됨, 앱 내부 복기는 이미 달성됨) | P0 | ⬜ |
| 전체 시나리오 통합 테스트 | Step1~4(기사진입→독해→투자판단→히스토리) 끊김 없이 완주 확인 | P0 | ⬜ |
| UI 폴리싱 (사이드바) | 사이드바 스타일(아이콘 메뉴 2개만: Today's Feed→Dashboard, History→MyPage) 적용. Pinterest 레퍼런스의 3단 분할 레이아웃과 신호등(macOS 창 장식)은 우리 몰입형 리더뷰 구조와 안 맞아 제외. (배경색/카드 톤은 코드 검증 결과 이미 SKILL.md 스펙대로 적용 완료 — global.css의 .news-card/.history-card는 surface-card+radius-lg, .ai-summary/.ai-insight는 카드 톤이 아닌 패널 스타일로 의도대로 구현됨) | P1 | ⬜ |
| 버그 수정 | 통합 테스트 발견 이슈 처리 | P0 | ⬜ |
| 배포 | 데모용 배포 | P1 | ⬜ |
| 발표 자료 준비 | 기획·개발 결과 정리 | P1 | ⬜ |

---

## 스코프 제외 (기획서 4번 "제품 스코프" 기준)

- 실제 증권사 API와 연동된 매매 시스템 — 기획서에서 "모의 투자 판단 훈련 도구"로 명확히 스코프를 한정함
