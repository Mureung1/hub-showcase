# Bridge 백로그

> 기획서(`docs/plan.md`)에서 확정된 방향 기준으로 남은 일을 정리한 문서.
> 우선순위: P0 필수(배포 데모 필수) · P1 필요(완성도) · P2 여유(시간 남으면).
> 이번 사이클 목표: 배포까지(동작하는 E2E 데모). 인원: 1인. 기간: 2~4주차(7/13~7/31).

## 완료 (참고 — 백로그에서 제외)
- React 프로젝트 · Express 스캐폴드 · 모노레포 폴더 구조
- 프로토타입 6화면 React 이식 + 편지 상태머신 + 목데이터

## 백로그

### A. 백엔드 토대 & DB
- [P0] A1 Prisma + PostgreSQL 셋업(로컬+배포 DB 결정), .env
- [P0] A2 스키마 설계: User, Letter(초안/상태), Match(이음), Reply
- [P0] A3 서버 구조: routes/controllers/services + zod 검증 + 에러 핸들러

### B. 인증/사용자(최소)
- [P1] B1 최소 신원: 익명 세션 또는 JWT+bcrypt 경량 로그인
- [P2] B2 선호 카테고리 저장(매칭용)·프로필

### C. 편지 API
- [P0] C1 편지 작성 API(하루 1통 제한) → 모음소 등록
- [P0] C2 초안 자동저장 API(임시저장/이어쓰기)
- [P0] C3 저장소 조회 API(내가 쓴/받은/이어진)
- [P0] C4 답장 작성 API(8시간 후 전달 예약)

### D. AI 매칭(핵심 가치)
- [P0] D1 Claude SDK 연동: 편지 카테고리 분류
- [P0] D2 매칭 로직: 모음소에서 수신자 선정(선호 기반)
- [P0] D3 추천 이유 생성(Claude)
- [P1] D4 프롬프트/매칭 품질 튜닝

### E. 시간 규칙 & 스케줄링
- [P0] E3 하루 1통 제한(C1 연계)
- [P0] E1 24시간 후 추천 도착(on-read or node-cron)
- [P0] E2 8시간 후 답장 전달
- [P1] E4 데모용 시간 단축 토글

### F. 프론트–백엔드 연동
- [P0] F1 API 클라이언트 + react-query, 토큰 처리
- [P0] F2 작성/발송 흐름 연동(mock→API)
- [P0] F3 추천/답장 흐름 연동
- [P0] F4 저장소 실데이터 연동, mock.js 제거
- [P1] F5 로딩/에러 상태 UI

### G. UX 폴리싱
- [P0] G1 붙여넣기 차단(직접 타이핑 강제)
- [P0] G2 초안 자동저장 UX(글자수/저장 표시)
- [P2] G3 편지 개봉 애니메이션(motion)
- [P1] G4 카운트다운 실시간 정합(dayjs)

### H. 배포(이번 사이클 목표)
- [P0] H1 배포 스택 확정 & Postgres 프로비저닝
- [P0] H2 백엔드 배포 + 환경변수/시크릿(ANTHROPIC_API_KEY 등)
- [P0] H3 프론트 배포 + API base URL 연결 + CORS
- [P1] H4 자동배포 파이프라인/도메인
- [P1] H5 시드 데이터(모음소 예시 편지) — 데모용

### I. 테스트/검증
- [P1] I1 백엔드 API 테스트(Supertest) 핵심 경로
- [P1] I3 배포 환경 E2E 수동 체크리스트
- [P2] I2 프론트 테스트(Vitest) · I4 Playwright E2E

### J. 운영/프로세스(상시)
- [P1] J1 기능 단위 PR + 코드리뷰(PR은 직접 생성), 촘촘한 커밋
- [P2] J2 성능 최적화 · 상시 버그 수정

## 이번 주 Task (2주차: 7/13–7/19)

| Task | 우선순위 | 비고 |
|---|---|---|
| A1 Prisma+Postgres 셋업(+배포 DB 결정) | P0 | 배포 DB를 지금 정해야 이후가 안 막힘 |
| A2 스키마 설계(User/Letter/Match/Reply) | P0 | mock.js 구조 기준 |
| A3 서버 구조 + zod + 에러 핸들러 | P0 | 이후 모든 API의 틀 |
| C1 편지 작성 API(+하루 1통) | P0 | E3 포함 |
| C2 초안 자동저장 API | P0 | |
| C3 저장소 조회 API | P0 | |
| F1 API 클라이언트 + react-query 도입 | P0 | 연동 기반 |
| F2 작성/발송 흐름 연동 시작 | P1 | 여기까지면 2주차 성공 |
| B1 최소 신원(익명 세션 임시) | P1 | 막히면 하드코딩 유저로 미룸 |
| G1 붙여넣기 차단(직접 타이핑 강제) | P0 | 의존성 없음, 4주차 부담 완화용으로 당김 |
| J1 기능별 PR·코드리뷰 | P1 | 매 단위 커밋/PR |

## 3주 일정 (2~4주차)

### 2주차 (7/13–7/19) — 백엔드 토대 & 첫 연동
A1·A2·A3 → C1·C2·C3 → F1·(F2 시작) · G1
DoD: 로컬에서 편지가 DB에 저장/조회, 작성 화면이 API로 붙기 시작.

### 3주차 (7/20–7/26) — AI 매칭 & 시간 규칙
D1·D2·D3(핵심) → C4·E1·E2 → F3 → B1 마무리
DoD: 편지→모음소→AI 추천+이유→답장 루프 로컬 동작.

### 4주차 (7/27–7/31) — 통합·폴리싱·배포
F4(mock 제거) → G2(+여유 시 G3) → H1·H2·H3 → H5·I1·I3
DoD: 배포 URL에서 전체 흐름 시연, E4 토글로 데모.
