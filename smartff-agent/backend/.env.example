# Supabase 프로젝트 설정 (Settings > API)에서 확인
SUPABASE_URL=
# ⚠️ Service Role Key는 DB 전체 접근/RLS 우회 권한을 가진 비밀 키입니다.
# 절대 커밋하지 말고, 유출 시 Supabase 대시보드에서 즉시 재발급(rotate)하세요.
SUPABASE_SERVICE_ROLE_KEY=

# 서버 포트
PORT=5000

# CORS 허용 origin. 배포 시 프론트엔드 실제 도메인으로 지정
FRONTEND_ORIGIN=http://localhost:5173
