# 버전을 고정한다. 배포(Render)는 빈 환경에서 pip install을 새로 돌리므로,
# 고정하지 않으면 로컬과 다른 버전이 깔려 "로컬은 되는데 배포는 안 되는" 상태가 된다.
# 아래는 로컬에서 실제로 전 단계(에이전트·SSE·프론트 실연결)를 통과시킨 버전이다.

fastapi==0.139.2
uvicorn==0.51.0
arxiv==4.0.0
feedparser==6.0.12
google-generativeai==0.8.6
python-dotenv==1.2.2
pypdf==6.14.2

# 테스트 전용. 배포 런타임에는 필요 없지만 CI가 같은 파일을 쓰므로 함께 둔다.
pytest==9.1.1
