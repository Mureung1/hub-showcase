# Project

## 프로젝트 소개

CalMe는 대학생을 위한 AI 기반 일정 관리 웹 서비스다. 사용자가 학교 공지, 공모전, 대외활동 등의 공지를 입력하면 AI가 핵심 정보와 일정을 추출하고, 사용자가 검토한 일정을 웹 캘린더에 등록한다.

## 해결하려는 문제

- 여러 공지와 안내문에 흩어진 중요한 일정과 마감일을 놓치기 쉽다.
- 공지 내용을 직접 확인하고 캘린더에 옮기는 작업이 반복된다.

## 핵심 가치

- 공지에서 여러 일정과 핵심 정보를 추출한다.
- AI 결과를 사용자가 확인하고 수정한 뒤 저장한다.
- 캘린더와 대시보드에서 일정과 마감일을 한눈에 확인한다.

# Current MVP

## 현재 MVP 범위

- 이메일 회원가입, 로그인, 로그아웃 및 로그인 상태 유지
- 텍스트 공지 입력
- PDF 업로드 및 텍스트 추출
- AI 공지 요약 및 여러 일정 추출
- 일정명, 신청 기간, 마감일, 장소, 제출물 등 추출
- 분석 결과 선택, 수정, 제외, 삭제 및 일정 추가
- 선택한 일정의 캘린더 등록
- 중복 일정 확인
- 월간 캘린더, 마감 임박 대시보드 및 일정 상세
- 빈 상태, 로딩, 오류 및 모바일 화면 대응

## 제외 기능

- 이미지 OCR
- URL 크롤링
- Google 및 네이버 로그인
- Google Calendar 연동
- 알림 및 이메일 발송
- 일정 공유
- 반복 일정
- 자연어 챗봇
- 사용자 확인 없는 AI 자동 일정 등록

## 향후 확장 기능

MVP 제외 기능은 향후 확장 후보일 뿐, 별도 승인 없이 구현하지 않는다. 이미지와 URL 입력은 확장 가능한 구조만 고려한다.

# Tech Stack

## 확정

- Frontend: React `19.2.7`, React DOM `19.2.7`
- Build tool: Vite `8.1.1`
- Language: JavaScript, JSX, CSS
- Module system: ES Modules
- Lint: ESLint `10.6.0`, React Hooks 및 React Refresh 플러그인
- 별도 정적 프로토타입: HTML, CSS, JavaScript

## TODO / 미정

- Backend 기술 및 디렉터리 구조
- API 명세와 에러 응답 형식
- 인증 방식
- 데이터베이스 제품과 스키마 상세
- AI 제공자, 모델, 프롬프트 및 JSON Schema
- PDF 처리 라이브러리와 업로드 제한
- 테스트 도구와 테스트 범위
- 배포 환경

> 기획 문서에는 React와 Express의 역할 정의가 작업 항목으로 있으나, 현재 저장소에는 Express 백엔드가 없다. Express를 확정 기술로 간주하지 않는다.

# Project Structure

```text
/
├─ docs/               # 기획, 개발 계획, 디자인 가이드 및 화면 이미지
├─ prototype/          # HTML/CSS/JS 기반 인터랙션 프로토타입
├─ public/             # 정적 공개 자산
├─ src/                # React 애플리케이션 소스와 자산
├─ index.html          # Vite 진입 HTML
├─ package.json        # 의존성 및 실행 스크립트
├─ vite.config.js      # Vite 설정
└─ eslint.config.js    # ESLint 설정
```

- `docs/4주_작업분해.md`는 현재 존재하지 않는다.
- Backend, DB 및 테스트 디렉터리는 현재 존재하지 않는다.
- 구조 변경 전에는 현재 구조와 관련 문서를 먼저 확인한다.

# Coding Convention

- 파일명 규칙: TODO(미정)
- React 컴포넌트 규칙: TODO(미정)
- 변수명 규칙: TODO(미정)
- Git 브랜치 전략: TODO(미정)
- 커밋 메시지 규칙: TODO(미정)
- 기존 파일을 수정할 때는 해당 파일의 현재 형식을 우선 유지한다.
- ESLint 설정을 준수하고 작업 후 `npm run lint`로 확인한다.
- 빌드에 영향을 주는 변경은 `npm run build`로 확인한다.

# UI/UX Rules

- 분위기는 `clean`, `soft`, `calm`, `professional`을 유지한다.
- 시각적 우선순위는 콘텐츠, 일정 상태, 탐색 요소, 장식 순이다.
- 표면은 무채색 중심으로 구성하고 주요 파랑 `#4F7FB8`은 주요 버튼, 선택 및 현재 상태에 제한한다.
- 한 화면의 강조색 면적은 약 10% 이하로 유지한다.
- 강한 그림자와 장식적 그라데이션을 피하고 배경 차이, 1px 경계선, 여백을 사용한다.
- 상태를 색상만으로 표현하지 않고 텍스트 또는 아이콘을 함께 제공한다.
- 기본 간격은 4px 체계를 따른다.
- 데스크톱 우선 기준은 `1440 × 900px`, 앱 최대 너비는 `1360px`다.
- `1024px`, `768px`, `480px` 기준의 반응형 규칙을 따른다.
- 터치 대상은 최소 `40 × 40px`로 한다.
- hover, focus, active, disabled 상태를 구분한다.
- 월간 일정은 셀당 최대 3개를 노출하고 초과분은 `+ N개 더보기`로 표시한다.
- 상세한 색상, 크기, 타이포그래피와 레이아웃은 `docs/design.md`를 기준으로 한다.

# Development Rules

## 구현 우선순위

1. 텍스트 또는 PDF 공지 입력
2. AI 분석과 여러 일정 추출
3. 사용자 확인 및 수정
4. 선택 일정 저장과 중복 처리
5. 캘린더, 대시보드 및 일정 상세
6. 인증, 오류 처리, 모바일 대응, QA와 배포

- 항상 먼저 `"지금 MVP에 필요한가?"`를 판단한다.
- 완성도 높은 핵심 사용자 흐름을 기능 수보다 우선한다.
- 날짜를 추출하지 못해도 전체 분석을 실패 처리하지 않는다.
- AI 결과를 자동 저장하지 않는다.
- 일정별 선택, 수정 및 제외가 가능해야 한다.
- AI가 추출한 정보임을 명확히 표시한다.
- 중복 기준은 `같은 사용자 + 같은 일정명 + 같은 날짜`다.

## 기능 추가 원칙

- MVP 범위 밖 기능은 제안하거나 구현하지 않는다.
- 기능을 추가하기 전에 현재 코드와 관련 문서를 확인한다.
- 문서 간 충돌이나 미정 사항은 추측하지 말고 TODO로 표시하거나 사용자에게 확인한다.
- 기존 사용자 흐름과 데이터 관계를 유지한다.

## AI Agent 작업 절차

새 기능 또는 동작 변경 전에는 반드시 다음 순서를 따른다.

1. 현재 프로젝트와 관련 파일을 분석한다.
2. 수정할 파일과 수정 이유를 먼저 설명한다.
3. 구현 계획과 검증 방법을 제시한다.
4. 사용자의 명시적 승인을 기다린다.
5. 승인 후에만 파일을 수정한다.
6. 변경 범위에 맞게 lint, build 및 관련 테스트를 수행하고 결과를 보고한다.

- 요청 범위와 무관한 기존 변경은 보존한다.
- 관련 문서를 함께 갱신할 필요가 있으면 구현 전에 계획에 포함한다.

# Do NOT

- 문서와 코드에 없는 요구사항을 추측해서 구현하지 않는다.
- 사용자의 승인 전에 코드를 수정하지 않는다.
- 기존 기능, 사용자 흐름 또는 디자인을 임의로 변경하지 않는다.
- 허가 없이 새 라이브러리나 프레임워크를 추가하지 않는다.
- MVP 제외 기능을 구현하지 않는다.
- AI 분석 결과를 사용자 확인 없이 저장하지 않는다.
- 관련 없는 파일을 정리하거나 포맷하지 않는다.
- 미정인 기술, API, DB, 인증 및 배포 방식을 확정된 것처럼 작성하지 않는다.
- 비밀값이나 API Key를 저장소에 커밋하지 않는다.

# References

- `README.md`
- `docs/plan.md`
- `docs/design.md`
- `docs/checklist.md`

