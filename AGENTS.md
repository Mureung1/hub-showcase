# AGENTS.md
## 구현 원칙

- 현재 요구사항과 완료 조건에 필요한 범위만 구현한다.
- 새로운 구조를 만들기 전에 기존 코드와 패턴을 우선 재사용한다.
- 실제 사용 사례가 없는 추상화, 범용화, 헬퍼, 설정은 추가하지 않는다.
- 단순화를 이유로 입력 검증, 오류 처리, 보안, 접근성, 타입 안전성을 생략하지 않는다.
- 테스트는 핵심 동작과 변경으로 인한 회귀 위험을 검증하는 최소 범위로 작성한다.
- 요청 범위를 벗어난 리팩터링은 하지 않는다.
- 더 복잡한 구조가 필요한 경우에는 먼저 필요성과 근거를 설명한다.

## 1. 프로젝트 개요

이 저장소는 모바일 중심 웹 서비스 **나만의 레시피북 MVP**를 개발한다.

서비스의 핵심은 다음과 같다.

> 사용자가 URL 또는 직접 입력으로 레시피를 제공하면 AI가 이를 구조화하고, 사용자가 수정한 뒤 자신의 레시피북에 저장한다. 전달받은 레시피는 관계 정보와 함께 보관한다.

이 서비스는 SNS나 공개 레시피 플랫폼이 아니다.

다음 가치에 집중한다.

1. 흩어진 레시피를 AI로 정리한다.
2. 사용자가 AI 초안을 확인하고 수정한다.
3. 자신의 레시피북에 저장하고 다시 확인한다.
4. 전달받은 레시피의 관계와 기억을 함께 보관한다.

---

## 2. 현재 개발 목표

현재 최우선 목표는 다음 핵심 시나리오를 완성하는 것이다.

```text
로그인
→ 레시피 목록
→ 레시피 추가
→ AI 구조화
→ AI 결과 수정
→ 레시피 저장
→ 레시피 상세 조회
```

이 흐름이 완료되기 전에는 다음 작업을 우선하지 않는다.

* 열람 공유
* 전달 공유
* 복잡한 애니메이션
* 세부 시각 효과
* 불필요한 리팩터링
* MVP 범위 밖의 신규 기능

---

## 3. 기술 스택

### 확정된 기술

#### Frontend

* React
* Tailwind CSS
* React Router (`react-router`)

#### Backend

* Express
* PostgreSQL

#### Authentication

* Firebase Authentication 기반 Google 로그인
* Firebase ID 토큰 검증

MVP 초기에는 Google 로그인만 제공한다. 핵심 레시피 흐름이 완료된 뒤 같은 Firebase 프로젝트에 이메일/비밀번호와 카카오 로그인을 추가한다.

#### AI

* OpenAI Responses API
* GPT-5.6 Luna (`gpt-5.6-luna`)

AI 제공자와 모델은 `COMMON-AI-001`에서 사용자가 승인했다. API 키와 모델 설정은 백엔드 환경 변수로만 관리한다.

확정된 기술은 사용자의 명시적인 요청 없이 다른 기술로 교체하지 않는다.

---

### 아직 결정되지 않은 기술

다음 항목은 아직 확정되지 않았다.

* JavaScript 또는 TypeScript
* 프론트엔드 빌드 도구
* 서버 상태 관리
* 전역 상태 관리
* HTTP Client
* ORM 또는 데이터 접근 방식
* 입력값 검증 라이브러리
* 테스트 도구
* URL 콘텐츠 추출 방식
* 프론트엔드 배포 서비스
* 백엔드 배포 서비스
* CI/CD

미확정 기술을 이미 확정된 것처럼 가정하지 않는다.

새로운 기술 선택이 필요하면 다음 순서로 처리한다.

1. 현재 `package.json`과 기존 코드를 확인한다.
2. 이미 사용 중인 기술이 있는지 확인한다.
3. 기존 기술만으로 구현 가능한지 확인한다.
4. 새로운 의존성이 반드시 필요한지 판단한다.
5. 주요 기술은 사용자 결정 없이 임의로 도입하지 않는다.

---

### 기술 선택 기준

새로운 기술은 다음 기준으로 선택한다.

1. 3주 안에 MVP를 완성할 수 있는가?
2. 현재 개발자가 이해하고 유지보수할 수 있는가?
3. React, Tailwind CSS, Express와 자연스럽게 연결되는가?
4. 로컬 개발과 배포가 지나치게 복잡하지 않은가?
5. 무료 또는 감당 가능한 비용으로 운영할 수 있는가?
6. 공식 문서와 사용 사례가 충분한가?

기술이 새로 확정되면 이 문서와 README를 갱신한다.

---

## 4. 작업 전 확인할 문서

요청과 관련된 문서를 먼저 확인한다.

계획, 구현, 수정, 리뷰 작업에는 `$preserve-product-intent` 스킬을 사용한다.
단순 상태 확인, Git 전용 작업, 저장소 동작과 무관한 일반 질문에는 사용하지 않는다.

### 기획

* `docs/product/prd_mvp.md`
* `docs/product/feature_spec.md`
* `docs/product/user_flow.md`
* `docs/product/plan.md`

### 디자인

* `docs/design/screen_flow.md`
* `docs/design/design_system.md`
* `docs/design/recipebook_design_guidelines.md`

### API와 데이터 모델

* `docs/api/api_spec.md`
* `docs/architecture/data_model.md`

### 진행 확인

* `docs/product/checklist.md`

문서와 코드가 충돌하면 임의로 정책을 변경하지 않는다.

최신 문서와 현재 코드를 비교한 뒤 충돌 내용을 사용자에게 알린다.

---

## 5. 작업 기본 원칙

### 요청 범위만 수정한다

* 요청하지 않은 기능을 추가하지 않는다.
* 관련 없는 파일을 함께 수정하지 않는다.
* 대규모 리팩터링을 임의로 수행하지 않는다.
* 새로운 라이브러리를 임의로 설치하지 않는다.
* 기존 구조와 패턴을 먼저 확인한다.

### 최소 변경을 우선한다

문제를 해결할 수 있는 가장 작은 변경을 선택한다.

* 기존 함수 수정으로 해결할 수 있다면 새 구조를 만들지 않는다.
* 한 번만 사용하는 코드를 성급하게 공통화하지 않는다.
* 미래의 가능성만을 위해 복잡한 구조를 만들지 않는다.
* MVP에서 사용하지 않는 확장 기능을 미리 만들지 않는다.

### 기능을 세로로 완성한다

프론트엔드 전체와 백엔드 전체를 따로 완성하지 않는다.

하나의 기능을 다음 순서로 연결한다.

```text
데이터 구조 확인
→ API 계약 확인
→ 백엔드 구현
→ API 검증
→ 프론트엔드 구현
→ 실제 API 연결
→ 성공 및 오류 흐름 확인
```

### 기존 코드를 먼저 탐색한다

새 코드를 작성하기 전에 다음을 확인한다.

* 같은 역할의 기존 컴포넌트
* 기존 API 요청 방식
* 기존 오류 처리 방식
* 기존 타입
* 기존 환경 변수
* 기존 테스트 패턴

중복 구현을 피한다.

---

## 6. 프로젝트 구조 원칙

실제 저장소 구조를 우선한다.

예상 책임은 다음과 같다.

```text
frontend/
├─ src/
│  ├─ api/
│  ├─ components/
│  ├─ features/
│  ├─ hooks/
│  ├─ layouts/
│  ├─ pages/
│  ├─ routes/
│  ├─ styles/
│  ├─ types/
│  └─ utils/

backend/
├─ src/
│  ├─ config/
│  ├─ middlewares/
│  ├─ modules/
│  ├─ routes/
│  ├─ services/
│  ├─ repositories/
│  ├─ utils/
│  └─ app.ts

docs/
└─ 프로젝트 문서
```

현재 구조가 다르면 기존 구조를 따른다.

존재하지 않는 폴더를 규칙에 맞추기 위해 임의로 만들지 않는다.

---

## 7. 네이밍 컨벤션

### 공통

* 변수와 함수: `camelCase`
* 컴포넌트와 타입: `PascalCase`
* 환경 변수와 전역 상수: `SCREAMING_SNAKE_CASE`
* React Hook: `use`로 시작
* 이벤트 처리 함수: `handle`로 시작
* Callback Prop: `on`으로 시작
* Boolean 변수: `is`, `has`, `can`, `should`로 시작

예시:

```ts
const isLoading = true;
const hasActiveShare = false;
const canTransferRecipe = recipe.type === "OWNED";

function handleSubmit() {}

interface RecipeCardProps {
  onSelect: () => void;
}
```

`data`, `item`, `value`, `flag`처럼 의미가 불분명한 이름은 피한다.

### 파일 이름

| 대상         | 규칙                   | 예시                  |
| ---------- | -------------------- | ------------------- |
| React 컴포넌트 | PascalCase           | `RecipeCard.jsx`    |
| Hook       | camelCase            | `useCurrentUser.js` |
| 일반 코드 파일   | camelCase            | `recipeApi.js`      |
| 테스트 파일     | 대상 파일명 + `.test`     | `recipeApi.test.js` |
| 환경 변수      | SCREAMING_SNAKE_CASE | `DATABASE_URL`      |

프로젝트에 이미 일관된 다른 규칙이 있다면 기존 규칙을 우선한다.

---

## 8. 프론트엔드 규칙

### 컴포넌트

* 페이지 컴포넌트는 화면 조합과 데이터 연결을 담당한다.
* 재사용 컴포넌트는 표현과 상호작용에 집중한다.
* 컴포넌트 안에 API 주소를 직접 작성하지 않는다.
* 너무 큰 컴포넌트는 책임 단위로 나눈다.
* 한 번만 사용하는 작은 JSX까지 무조건 분리하지 않는다.

### 상태 구분

다음 상태를 구분한다.

* 서버 상태: API에서 가져온 데이터
* 폼 상태: 사용자가 입력 중인 데이터
* UI 상태: 모달, 토글, 선택 상태
* 인증 상태: 현재 사용자와 인증 확인 상태

서버 데이터를 불필요하게 전역 상태에 복사하지 않는다.

### API 요청

* API 요청 함수는 별도 모듈에서 관리한다.
* 컴포넌트마다 `fetch`나 HTTP 요청 코드를 반복하지 않는다.
* API 주소는 환경 변수로 관리한다.
* 성공과 오류 응답을 일관된 방식으로 처리한다.

### 비동기 상태

비동기 화면에서는 다음 상태를 고려한다.

* 로딩
* 성공
* 빈 데이터
* 오류

버튼 요청에서는 다음을 처리한다.

* 중복 클릭 방지
* 처리 중 상태
* 성공 피드백
* 실패 피드백
* 실패 후 입력값 유지

### 폼

* 모든 입력에는 Label을 제공한다.
* 프론트엔드 검증은 사용자 경험을 위한 것이다.
* 백엔드 검증을 대체하지 않는다.
* 오류 메시지는 해당 입력 근처에 표시한다.
* 저장 실패 시 사용자의 입력을 유지한다.

### 스타일

* 모바일 화면을 우선한다.
* Tailwind CSS를 기본 스타일링 방식으로 사용한다.
* 동일한 역할의 UI는 동일한 스타일을 사용한다.
* 임의의 색상과 간격을 반복해서 추가하지 않는다.
* 장식보다 정보 계층과 읽기 편의성을 우선한다.
* 클릭 요소는 가능한 한 `button` 또는 `a`를 사용한다.

---

## 9. 백엔드 규칙

### 역할 분리

* Route: 경로와 Middleware 연결
* Controller: HTTP 요청과 응답 처리
* Service: 비즈니스 규칙
* Repository 또는 데이터 접근 계층: 데이터베이스 처리
* Middleware: 인증, 검증, 오류 처리

Controller에 복잡한 비즈니스 로직을 작성하지 않는다.

프로젝트가 아직 작은 경우 불필요하게 계층을 늘리지 않는다.

### API 경로

* 리소스 이름은 복수 명사를 사용한다.
* 동사보다 HTTP Method로 행동을 표현한다.
* 기존 API 계약을 우선한다.

```text
GET    /api/recipes
POST   /api/recipes
GET    /api/recipes/:recipeId
PATCH  /api/recipes/:recipeId
DELETE /api/recipes/:recipeId
```

명확한 비즈니스 행동은 하위 경로를 사용할 수 있다.

```text
POST /api/transfer-invitations/:invitationId/accept
```

### API 응답

성공 응답:

```json
{
  "data": {}
}
```

오류 응답:

```json
{
  "error": {
    "code": "RECIPE_NOT_FOUND",
    "message": "레시피를 찾을 수 없습니다."
  }
}
```

* 프론트엔드가 분기할 수 있는 안정적인 오류 코드를 사용한다.
* 오류 메시지만으로 비즈니스 분기를 만들지 않는다.
* 내부 Stack Trace를 사용자에게 반환하지 않는다.
* HTTP 상태 코드를 의미에 맞게 사용한다.

### 검증

다음 외부 입력을 신뢰하지 않는다.

* Request Body
* Path Parameter
* Query Parameter
* 환경 변수
* AI 응답
* 외부 URL에서 가져온 데이터

잘못된 요청은 명확한 4xx 오류로 반환한다.

### 인증과 권한

* 인증 여부와 권한을 구분한다.
* 프론트엔드에서 버튼을 숨겼더라도 백엔드에서 다시 검사한다.
* 개인 레시피는 소유자만 조회하고 변경할 수 있다.
* 전달받은 레시피는 원본 내용을 수정할 수 없다.
* 전달받은 레시피는 재전달할 수 없다.
* 전달 초대는 한 번만 사용할 수 있다.

### 보안

* 비밀 키를 코드에 작성하지 않는다.
* `.env`를 Commit하지 않는다.
* API Key와 사용자 개인정보를 로그에 남기지 않는다.
* 데이터베이스 쿼리에 사용자 입력을 문자열로 직접 결합하지 않는다.
* 운영 환경에서 내부 오류 정보를 노출하지 않는다.

---

## 10. AI 기능 규칙

### 제공자와 운영 기준

* 백엔드는 `OPENAI_API_KEY`와 `OPENAI_MODEL`을 사용하며 프론트엔드에 비밀 키를 전달하지 않는다.
* MVP 모델은 `gpt-5.6-luna`이며 모델 변경은 비용과 품질을 다시 확인한 뒤 문서에 반영한다.
* OpenAI 요청에는 15초 Timeout을 적용하고 자동으로 재시도하지 않는다.
* 공백을 제거한 직접 입력은 최대 20,000자까지 허용한다.
* 인증된 사용자별 AI 구조화 요청은 10분에 10회로 제한한다.
* 요청 원문, AI 응답 전문과 API 키를 로그에 남기지 않는다.
* Timeout과 제공자 오류는 `AI_REQUEST_FAILED`, 서비스 요청 제한은 `AI_RATE_LIMITED`, 검증 실패는 `AI_RESPONSE_INVALID`로 반환한다.

### AI 출력 강제와 검증

* Responses API의 구조화 출력에 `RecipeDraft`와 `RecipeWarning` JSON Schema를 제공하고 스키마 밖 필드는 허용하지 않는다.
* 구조화 출력은 형식을 강제하는 경계일 뿐 값의 정확성을 보장하지 않는다.
* 백엔드는 응답을 다시 파싱하고 필수 필드, 타입, null 허용 범위, 배열 항목과 순서, 숫자 범위를 검증한다.
* `id`, `ownerId`, `type`, `memo`, 날짜 필드처럼 AI가 결정할 수 없는 서버 필드는 응답에 포함하지 않는다.
* 직접 입력만 사용하면 `source`는 `null`이어야 하며, URL 입력이면 사용자가 제출하고 서버가 검증한 URL만 `source.url`로 사용할 수 있다.
* 모호하거나 추정한 값은 `warnings`에 편집 폼의 필드 경로와 함께 표시한다.
* 검증에 실패한 AI 응답은 일부만 보정해 반환하지 않고 전체를 `AI_RESPONSE_INVALID`로 거부한다.
* 사용자가 수정한 저장 요청은 AI 응답과 별개로 전체 레시피 저장 규칙을 다시 검증한다.

### AI 결과는 초안이다

AI가 반환한 결과는 완성된 레시피가 아니라 사용자 편집용 초안이다.

* AI 결과를 자동으로 데이터베이스에 저장하지 않는다.
* 사용자가 수정한 뒤 별도의 저장 요청을 보낸다.
* AI 응답은 백엔드에서 검증한다.

### 입력 방식

다음 입력 방식을 지원한다.

1. URL만 입력
2. 직접 입력만 입력
3. URL과 직접 입력 모두 입력

둘 다 비어 있으면 요청을 거부한다.

URL과 직접 입력이 함께 있으면 직접 입력을 사용자의 보완 정보로 사용한다.

### URL 처리

* `http`, `https`만 허용한다.
* localhost와 사설 IP 접근을 차단한다.
* 요청 Timeout을 적용한다.
* 응답 크기와 본문 길이를 제한한다.
* HTML 전체를 AI에 그대로 전달하지 않는다.
* 수집 실패 시 사용자가 직접 입력할 수 있도록 오류를 반환한다.

---

## 11. 레시피 정책

레시피 유형은 다음과 같다.

```ts
type RecipeType = "OWNED" | "EXTERNAL" | "RECEIVED";
```

| 행동       |  OWNED | EXTERNAL | RECEIVED |
| -------- | -----: | -------: | -------: |
| 조회       |     가능 |       가능 |       가능 |
| 원본 수정    |     가능 |       가능 |       불가 |
| 삭제       |     가능 |       가능 |       가능 |
| 개인 메모 수정 | 정책에 따름 |   정책에 따름 |       가능 |
| 전달 공유    |     가능 |       불가 |       불가 |

세부 정책은 `docs/product/feature_spec.md`의 최신 내용을 우선한다.

문서마다 정책이 다르면 임의로 선택하지 않는다.

---

## 12. Git 규칙

### 기본 원칙

* 사용자의 기존 변경사항을 되돌리지 않는다.
* 요청과 무관한 파일을 수정하지 않는다.
* 비밀 키와 `.env` 파일을 Commit하지 않는다.
* 사용자의 요청 없이 Commit 또는 Push하지 않는다.
* 강제 Push를 사용하지 않는다.
* `git reset --hard`, `git clean -fd`를 사용하지 않는다.

### 브랜치 이름

형식:

```text
<type>/<task-id>-<short-description>
```

예시:

```text
feat/fe-recipe-010-add-recipe-form
feat/be-ai-003-structure-recipe-api
fix/fe-auth-002-auth-redirect
docs/common-004-update-schedule
```

Task ID가 없다면 짧고 명확한 설명을 사용한다.

```text
fix/recipe-list-loading
```

### Commit 메시지

Conventional Commits 형식을 사용한다.

```text
<type>(<scope>): <subject>
```

사용 가능한 Type:

| Type       | 의미               |
| ---------- | ---------------- |
| `feat`     | 새로운 기능           |
| `fix`      | 버그 수정            |
| `refactor` | 동작 변경 없는 구조 개선   |
| `test`     | 테스트 추가 또는 수정     |
| `docs`     | 문서 변경            |
| `style`    | 코드 동작과 무관한 포맷 변경 |
| `chore`    | 설정 및 기타 작업       |
| `build`    | 빌드 설정 변경         |
| `ci`       | CI 설정 변경         |

예시:

```text
feat(auth): Google 로그인 흐름을 구현한다
feat(recipe): 레시피 생성 API를 추가한다
fix(ai): JSON 파싱 실패 처리를 수정한다
docs(backlog): 2주차 개발 일정을 갱신한다
```

하나의 Commit에는 하나의 논리적인 변경을 담는다.

`작업`, `수정`, `업데이트`처럼 범위가 불명확한 제목은 피한다.

---

## 13. 주석과 문서화

코드가 무엇을 하는지 그대로 설명하는 주석은 작성하지 않는다.

주석은 다음 내용을 설명할 때 사용한다.

* 왜 이 방식이 필요한가?
* 어떤 정책이나 제약이 있는가?
* 예상하기 어려운 예외가 있는가?

예시:

```js
// 초대 수락과 레시피 생성을 함께 처리해
// 동시에 들어온 요청으로 중복 저장되는 것을 방지한다.
```

다음 변경이 발생하면 관련 문서도 확인한다.

* API 요청 또는 응답 변경
* 데이터 모델 변경
* 사용자 흐름 변경
* 권한 정책 변경
* 환경 변수 추가
* 실행 또는 배포 방법 변경

---

## 14. 테스트와 검증

작업 후 실제 `package.json`의 Script를 확인하고 가능한 검증을 실행한다.

예상 검증 항목:

```text
lint
test
build
```

존재하지 않는 명령을 임의로 가정하지 않는다.

### 프론트엔드 확인

* 화면이 정상적으로 렌더링되는가?
* 로딩과 오류 상태가 있는가?
* 빈 데이터를 처리하는가?
* 중복 요청을 방지하는가?
* 저장 실패 후 입력이 유지되는가?
* 모바일 너비에서 화면이 깨지지 않는가?

### 백엔드 확인

* 정상 요청이 예상한 데이터를 반환하는가?
* 잘못된 요청이 4xx로 거부되는가?
* 비로그인 요청이 차단되는가?
* 다른 사용자의 데이터 접근이 차단되는가?
* 데이터가 실제 데이터베이스에 저장되는가?
* 오류 응답이 공통 형식을 따르는가?

### 현재 핵심 검증 흐름

```text
로그인
→ 목록 조회
→ 레시피 추가
→ AI 구조화
→ AI 결과 수정
→ 저장
→ 상세 조회
→ 목록에서 다시 확인
```

부분 기능이 동작하더라도 이 흐름을 막는 문제가 있다면 완료로 처리하지 않는다.

---

## 15. 에이전트 작업 절차

### 티켓 기반 요청

`FE-AUTH-002`, `BE-AI-001`처럼 `docs/product/checklist.md`의 티켓 ID가 포함된 분석, 구현 또는 상태 요청에는 `$run-project-ticket` 스킬을 사용한다.

#### 역할 라우팅

* 사용자가 `analyst`와 티켓 ID를 함께 명시하면 `시작`, `진행`, `구현` 표현이 있어도 analyst의 읽기 전용 분석으로 고정한다.
* analyst는 저장소를 수정하거나 구현을 시작하지 않는다. 분석을 마친 뒤 사용자에게 `guide` 또는 `implement`를 선택하게 하고, 분석 결과와 선택을 implementer에게 인계한다.
* `guide`와 `implement`는 모두 implementer가 담당한다. `guide`에서는 한 번에 한 단계만 안내하고 파일을 수정하지 않으며, `implement`에서만 파일을 수정하고 검증한다.
* analyst가 명시된 요청은 root 또는 main agent가 직접 구현하지 않는다. analyst 인계와 사용자 선택이 없으면 implementer도 시작하지 않는다.
* analyst가 명시되지 않은 직접 티켓 요청은 `$run-project-ticket`의 일반 모드 판정을 따른다.

#### 역할 모델 확인

* 새 역할 에이전트를 시작하거나 역할을 전환할 때 작업 전에 사용자에게 해당 TOML 모델 설정을 요청하고 확인을 기다린다.
* analyst는 `gpt-5.6-terra`, reasoning effort `medium`; implementer는 `gpt-5.6`, reasoning effort `high`를 사용한다.
* 사용자의 명시적 설정 완료 응답을 런타임 검증 대신 사용한다. 같은 역할의 연속 작업에는 다시 묻지 않는다.
* 설정 모델을 자동으로 대체하거나 상향하지 않는다.

#### analyst 인계 계약

* 티켓 ID와 원래 요청, 선행 작업과 현재 상태
* 확인한 문서·코드 사실, 포함·제외 범위와 최소 구현 접근
* 성공·오류·빈 상태, 위험 요소, 완료 기준과 검증 명령
* 사용자가 선택한 `guide` 또는 `implement`

* 티켓의 선행 작업과 완료 조건을 작업 범위로 사용한다.
* `docs/project/context.md`와 관련 작업 이력을 확인한다.
* 분석과 상태 확인 요청에서는 저장소를 수정하지 않는다.
* 구현 요청에서는 검증된 완료 조건만 체크하고, 모든 조건이 끝난 경우에만 티켓을 완료한다.
* 구현 결과는 `docs/project/history.md`에 기록한다.
* 다음 작업에도 필요한 결정과 알려진 문제만 `docs/project/context.md`에 반영한다.
* 같은 작업 절차가 두 번째 반복되면 `docs/project/skill_candidates.md`에 기록하고 사용자에게 스킬 생성 여부를 묻는다.
* 사용자의 승인 없이 반복 작업을 자동으로 스킬화하지 않는다.

### 작업 시작 전

1. 요청을 한 문장으로 정리한다.
2. 관련 문서를 확인한다.
3. 현재 Git 변경사항을 확인한다.
4. 관련 코드를 탐색한다.
5. 영향 범위와 선행 작업을 확인한다.
6. 가장 작은 구현 계획을 세운다.

### 작업 중

1. 기존 패턴을 따른다.
2. 요청 범위 밖으로 확장하지 않는다.
3. 발견한 별도 문제를 임의로 수정하지 않는다.
4. 컴파일 오류나 테스트 실패를 숨기지 않는다.

### 작업 완료 후

1. 변경 파일을 검토한다.
2. 가능한 검증 명령을 실행한다.
3. 성공 흐름과 오류 흐름을 확인한다.
4. 관련 문서 변경이 필요한지 확인한다.
5. 완료 내용과 남은 문제를 보고한다.

---

## 16. 금지 사항

다음 행동은 사용자의 명시적인 요청 없이 수행하지 않는다.

* React, Tailwind CSS, Express 교체
* 주요 기술 스택 임의 결정
* 새로운 상태 관리 또는 UI 라이브러리 도입
* 데이터베이스 종류 변경
* 인증 방식 변경
* API 경로 전체 변경
* 대규모 폴더 재구성
* 기존 데이터 삭제
* 마이그레이션 이력 초기화
* 사용자 변경사항 되돌리기
* 임의 Commit 또는 Push
* MVP 범위 밖 기능 구현

---

## 17. 일정이 부족할 때

다음 순서로 범위를 줄인다.

1. 애니메이션 제거
2. 복잡한 시각 효과 제거
3. Toast와 Skeleton 고도화 제외
4. 재료와 조리 단계 순서 변경 제외
5. 조리 중 보기 제외
6. 출처 세부 편집 제외
7. 열람 공유 제외
8. 전달 공유 UI 단순화

다음 기능은 유지한다.

* 로그인
* 레시피 목록
* 레시피 입력
* AI 구조화
* AI 결과 수정
* 레시피 저장
* 레시피 상세
* 데이터베이스 연결
* 운영 배포

URL 콘텐츠 수집이 일정을 지연시키면 직접 입력 기반 AI 구조화를 먼저 완성한다.

---

## 18. 작업 결과 보고 형식

작업 완료 후 다음 내용을 보고한다.

### 완료한 작업

* 실제 구현한 내용

### 변경한 파일

* 변경한 파일과 목적

### 검증 결과

* 실행한 Lint, Test, Build
* 직접 확인한 사용자 흐름

### 남은 문제

* 완료하지 못한 내용
* 요청 범위 밖이라 수정하지 않은 문제
* 사용자가 결정해야 하는 사항

검증하지 않은 항목은 검증했다고 표현하지 않는다.
