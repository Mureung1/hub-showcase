# Codex 작업 지침

이 프로젝트는 여러 개인 카페의 스탬프를 한곳에서 확인하고 적립하는 모바일 웹 MVP입니다. 사용자가 방향과 결과를 감독하고, 실질적인 설계·구현·검증은 Codex가 수행합니다. 남은 개발 기간은 3주, 하루 약 5시간, 총 약 105시간을 기준으로 합니다.

## 작업 방식

- 구현 전에 현재 코드, `docs/service-plan.md`, `checklist.md`를 먼저 확인합니다.
- 요청 범위 안에서는 Codex가 합리적인 결정을 내리고 구현·검증까지 완료합니다.
- 제품 범위나 사용자 경험을 크게 바꾸는 결정은 구현 전에 사용자와 합의합니다.
- 기존 사용자 변경사항을 보존하고 관련 없는 파일은 수정하지 않습니다.
- 문서와 코드가 충돌하면 문서를 먼저 수정한 뒤 구현합니다.
- 기능을 추가하기 전에 3주 시연에 반드시 필요한지 확인합니다.
- 한 번에 너무 큰 변경을 만들지 말고, 기능 단위로 구현하고 확인합니다.

## 코드 컨벤션

### JavaScript와 JSX

- 이번 MVP에서는 JavaScript와 JSX를 유지하며 TypeScript로 전환하지 않습니다.
- 들여쓰기는 공백 2칸을 사용합니다.
- 문자열은 작은따옴표를 사용하고 세미콜론은 생략합니다.
- 기존 Oxlint 설정을 기준으로 정적 검사를 수행합니다.
- 자동 포맷터는 실제 필요가 확인되기 전까지 추가하지 않습니다.
- 한 파일은 하나의 주요 역할을 담당하게 하되, 한 번만 사용하는 작은 UI까지 과도하게 분리하지 않습니다.
- 페이지는 데이터 조회와 화면 상태를 조립하고 표시용 컴포넌트는 Supabase를 직접 호출하지 않습니다.
- Supabase Auth, 데이터 조회, RPC 호출은 `services`에 모읍니다.
- 인라인 스타일보다 CSS 클래스를 사용하고 기존 디자인 토큰을 우선 재사용합니다.

### 이름 규칙

- React 컴포넌트와 파일명은 `PascalCase`를 사용합니다. 예: `CustomerHomePage.jsx`, `CafeStampCard.jsx`
- 함수, 변수, 훅은 `camelCase`를 사용합니다. 예: `getCustomerStamps`, `isLoading`
- 전역 상수는 `UPPER_SNAKE_CASE`를 사용합니다.
- 이벤트 처리 함수는 `handle`로 시작합니다. 예: `handleLogin`, `handleStampAdd`
- CSS 클래스는 `kebab-case`를 사용합니다.
- 데이터베이스 테이블과 열은 `snake_case`를 사용합니다.
- 이름은 구현 방식보다 행동과 역할이 드러나게 작성합니다.

### 주석과 오류 처리

- 코드만 읽어도 알 수 있는 내용을 반복하는 주석은 작성하지 않습니다.
- 보안 결정, RPC와 RLS의 의도, 시연 전용 제약처럼 이유가 중요한 경우에만 주석을 남깁니다.
- Supabase 원본 오류 메시지를 그대로 사용자에게 노출하지 않습니다.
- 화면에는 사용자가 다음 행동을 알 수 있는 한국어 오류 메시지를 표시합니다.
- 비동기 요청은 로딩, 성공, 오류 상태를 함께 구현합니다.
- 요청 중 관련 버튼을 비활성화하고 실패 시 이전의 정상 데이터를 임의로 변경하지 않습니다.

## Git과 커밋 규칙

### 커밋 메시지

- Conventional Commits 형식을 사용합니다.
- 형식은 `type(scope): 한국어 설명`입니다.
- 타입과 scope는 영어로, 설명은 한국어로 작성합니다.
- 제목은 변경한 행동을 구체적으로 설명하고 마침표를 붙이지 않습니다.

주요 타입:

- `feat`: 새로운 기능
- `fix`: 버그 수정
- `refactor`: 동작 변화 없는 구조 개선
- `style`: CSS와 시각 표현 변경
- `test`: 테스트 추가 및 수정
- `docs`: 문서 변경
- `chore`: 설정, 의존성, 개발 도구 변경

데이터베이스 작업은 별도 `db` 타입을 만들지 않고 표준 타입과 `db` scope를 조합합니다.

```text
docs(plan): 로그인과 카페별 목표 반영
chore(deps): Supabase와 라우터 의존성 추가
feat(auth): 데모 계정 로그인과 세션 복원 구현
feat(db): 카페별 스탬프 데이터 구조 생성
feat(db): 역할별 RLS 정책 추가
feat(customer): 카페별 스탬프 목록 연결
feat(owner): 회원번호 고객 조회 구현
feat(stamp): 원자적 스탬프 적립 연결
test(stamp): 쿠폰 자동 발행 시나리오 추가
fix(auth): 새로고침 중 로그인 화면 노출 방지
style(customer): 320px 스탬프 카드 레이아웃 보정
```

### 커밋 단위

- 커밋은 독립적으로 설명하고 검증할 수 있는 유의미한 기능 블록 단위로 나눕니다.
- 파일 하나를 수정할 때마다 커밋하거나, 서로 다른 여러 기능을 한 커밋에 섞지 않습니다.
- 기능 구현과 무관한 사용자 변경사항을 커밋에 포함하지 않습니다.
- 미완성 상태나 검증 실패 상태를 완료 커밋으로 권하지 않습니다.

유의미한 블록의 예:

- 라이브러리와 기본 앱 구조 구성
- Supabase 스키마와 RLS 구성
- 로그인, 로그아웃, 세션 복원
- 손님 다중 카페 조회
- 사장님 회원번호 조회
- 원자적 스탬프 적립 RPC
- 쿠폰과 알림 연결
- 모바일 화면과 접근성 보정

### 커밋 체크포인트

- Codex는 유의미한 구현 블록을 완료하면 다음 블록으로 넘어가지 않고 작업을 멈춥니다.
- 멈추기 전에 변경 범위에 맞는 lint, 테스트, 프로덕션 빌드를 가능한 범위에서 실행합니다.
- 변경 파일, 구현 내용, 검증 결과, 남은 주의사항과 권장 커밋 메시지를 사용자에게 보고합니다.
- 보고 마지막에 현재가 커밋할 시점임을 명확히 알립니다.
- 사용자가 커밋하거나 계속 진행하라고 지시하기 전까지 다음 기능 블록을 시작하지 않습니다.
- 사소한 오타 수정이나 동일 기능 블록 안의 작은 보완마다 별도 체크포인트를 만들지는 않습니다.
- 검증이 실패하면 커밋 시점으로 보고하지 않고 먼저 원인을 해결합니다.
- Codex는 사용자가 명시적으로 요청할 때만 `git add` 또는 `git commit`을 실행합니다.
- 사용자가 직접 커밋하는 경우 바로 사용할 수 있는 권장 커밋 메시지를 제공합니다.

커밋 체크포인트 보고 형식:

```text
[기능 블록 이름]을 완료했습니다.

변경:
- 주요 구현 내용

검증:
- lint 결과
- 테스트 결과
- build 결과

권장 커밋:
feat(scope): 변경 내용

여기서 커밋할 시점입니다. 다음 작업은 사용자 지시 후 진행합니다.
```

### 브랜치와 커밋 제외 파일

- 별도 브랜치가 필요하면 `codex/` 접두사를 사용하고 3주 MVP에서는 주차 또는 큰 단계 단위로 나눕니다.
- `.env`, Supabase `service_role` 키, 실제 비밀번호, `dist`, 개인 로컬 설정과 임시 디버깅 파일은 커밋하지 않습니다.
- 프런트 코드와 공개 환경변수에는 비밀 키를 넣지 않습니다.

## 제품 목표

- 손님과 사장님이 Supabase의 같은 저장 데이터를 사용하는 모바일 웹을 구현합니다.
- 손님 화면에는 여러 카페와 카페별 스탬프 카드가 표시됩니다.
- 각 카페는 서로 다른 목표 스탬프 수와 보상을 가질 수 있습니다.
- 데모 사장님은 한 카페에만 소속되며 자신의 카페에서만 적립할 수 있습니다.
- 핵심 시연 흐름은 `로그인 → 회원번호 입력 → 고객 조회 → 스탬프 1개 적립 → 카페별 목표 달성 시 해당 카페 쿠폰 자동 발행 → 알림 생성`입니다.
- 손님은 새로고침 후 변경된 스탬프, 쿠폰, 알림을 확인할 수 있어야 합니다.

## 확정된 기술 구성

- 프런트엔드: React + Vite
- 라우팅: React Router
- 인증: Supabase Auth 이메일·비밀번호 로그인
- 데이터 저장: Supabase PostgreSQL
- 데이터 접근: Supabase JavaScript 클라이언트
- 원자적 적립 처리: PostgreSQL 함수와 Supabase RPC
- 스타일: 기존 프로젝트 디자인 시스템을 활용한 CSS
- 배포: Vercel 우선
- 별도 Express 서버는 사용하지 않습니다.

### 필요한 실행 라이브러리

- `react`
- `react-dom`
- `react-router-dom`
- `@supabase/supabase-js`

### 권장 테스트 라이브러리

- `vitest`
- `jsdom`
- `@testing-library/react`
- `@testing-library/user-event`
- `@testing-library/jest-dom`

`lucide-react`와 Supabase CLI는 실제 필요가 생겼을 때만 추가합니다. Axios, Redux/Zustand, TanStack Query, Tailwind, 폼·날짜 라이브러리, QR 스캔 라이브러리는 현재 MVP에 추가하지 않습니다.

## 로그인과 역할

- Supabase Auth에 고정된 데모 계정 2개를 사용합니다.
- 손님 계정 1개와 사장님 계정 1개를 기본으로 합니다.
- 이메일·비밀번호 로그인, 로그아웃, 새로고침 후 세션 유지만 구현합니다.
- 회원가입, 이메일 인증, 비밀번호 재설정, 소셜 로그인은 구현하지 않습니다.
- `profiles` 테이블로 인증 사용자와 서비스 역할을 연결합니다.
- 손님 프로필은 `customer_id`, 사장님 프로필은 담당 `cafe_id`를 가집니다.
- 로그인 후 `profiles.role`에 따라 손님 또는 사장님 화면으로 이동합니다.
- 화면 라우트 보호와 별개로 실제 데이터 권한은 Supabase RLS와 RPC에서 검증합니다.

## 확정된 MVP 범위

### 손님

- 고유 회원번호 확인
- 여러 카페의 스탬프 적립 현황과 서로 다른 목표 확인
- 발행된 쿠폰 확인
- 적립 및 쿠폰 발행 인앱 알림 확인
- 새로고침으로 최신 데이터 다시 조회

### 사장님

- 로그인한 사장님의 담당 카페 확인
- 고객 회원번호 입력
- 고객 정보와 담당 카페의 현재 스탬프 조회
- 스탬프 1개 적립
- 일반 적립 또는 쿠폰 자동 발행 결과 확인

### 적립 규칙

- 카페마다 `stamp_goal`과 `reward_title`을 가집니다.
- 데모 사장님의 카페는 목표 10개를 사용하되, 다른 카페는 6개나 8개 등 다른 목표를 가질 수 있습니다.
- 한 번의 처리로 스탬프 1개만 적립합니다.
- 현재 스탬프가 `stamp_goal - 1`이면 적립과 동시에 해당 카페 보상의 쿠폰 1장을 발행합니다.
- 쿠폰 발행 후 해당 스탬프 카드의 개수를 0개로 초기화합니다.
- 모든 적립에는 고객 적립 알림을 만들고, 쿠폰 발행 시 쿠폰 알림을 추가로 만듭니다.
- 발행된 쿠폰에는 발행 당시 `reward_title`을 복사하여 이후 카페 설정 변경의 영향을 받지 않게 합니다.

## 데이터 모델

- `profiles`: 인증 사용자, 역할, 연결된 고객 또는 카페
- `customers`: 회원번호와 손님 정보
- `cafes`: 카페명, 목표 스탬프 수, 보상명
- `stamp_cards`: 고객·카페별 현재 스탬프
- `coupons`: 고객에게 발행된 카페별 쿠폰
- `notifications`: 적립 및 쿠폰 발행 알림

`customers.member_number`는 고유해야 합니다. `stamp_cards`의 고객·카페 조합도 하나만 존재해야 합니다.

## 보안과 서버 처리

- 프런트엔드에는 `VITE_SUPABASE_URL`과 `VITE_SUPABASE_ANON_KEY`만 사용합니다.
- `service_role` 키는 프런트 코드나 공개 환경변수에 절대 넣지 않습니다.
- 손님은 자신의 스탬프, 쿠폰, 알림만 조회할 수 있어야 합니다.
- 사장님은 자신의 담당 카페에 대해서만 고객 조회와 적립을 할 수 있어야 합니다.
- 적립 RPC는 브라우저가 전달한 `cafe_id`를 신뢰하지 않습니다.
- RPC 내부에서 `auth.uid()`와 `profiles`를 사용해 사장님의 역할과 담당 카페를 확인합니다.
- 스탬프 증가, 쿠폰 발행, 스탬프 초기화, 알림 생성은 하나의 데이터베이스 트랜잭션에서 처리합니다.
- 실패하면 전체 작업을 롤백하여 중간 상태가 남지 않게 합니다.
- 실시간 구독은 구현하지 않습니다.

## 권장 디렉터리 구조

```text
src/
  app/
    App.jsx
    router.jsx
    AuthProvider.jsx
  routes/
    ProtectedRoute.jsx
    RoleRoute.jsx
  pages/
    LoginPage.jsx
    customer/
    owner/
  components/
    common/
    customer/
    owner/
  services/
    authService.js
    customerService.js
    ownerService.js
  lib/
    supabase.js
  utils/
  styles/
  main.jsx

supabase/
  migrations/
  seed.sql
  reset_demo.sql

tests/
  auth/
  customer/
  owner/
```

처음부터 빈 파일을 모두 만들지 않습니다. 기능을 구현할 때 필요한 디렉터리와 파일만 추가합니다. 페이지는 상태와 데이터 흐름을 조립하고, 표시용 컴포넌트는 Supabase를 직접 호출하지 않습니다. Supabase 요청은 `services`에 모읍니다.

## 기본 라우트

- `/login`
- `/customer`
- `/customer/coupons`
- `/customer/notifications`
- `/owner`

로그인하지 않은 사용자는 보호 화면에서 `/login`으로 이동합니다. 역할이 맞지 않는 주소에 접근하면 자신의 역할에 맞는 시작 화면으로 이동합니다.

## 제외 범위

- 실제 QR 카메라 스캔과 외부 바코드 스캐너 연동
- 결제와 POS 연동
- 문자, 카카오톡, 이메일, 브라우저 푸시 알림
- 위치 기반 탐색과 카페 검색
- 쿠폰 사용·승인·만료 처리
- 사장님이 직접 카페 목표와 보상을 설정하는 기능
- 카페 등록, 운영 통계, 관리자 화면
- 여러 카페를 관리하는 사장님
- 실제 회원가입, 비밀번호 찾기, 소셜 로그인
- 부정 적립 방지와 네이티브 앱

QR은 미래 흐름을 설명하는 시각 요소로 남길 수 있지만, 이번 MVP의 적립 로직과 연결하지 않습니다.

## 기존 프로토타입 정리 기준

- 손님 화면의 여러 카페 목록은 유지합니다.
- 카페별 목표와 보상은 데이터에서 읽어 표시합니다.
- 쿠폰 만료일, 만료 임박순, 쿠폰 사용 기능은 제거합니다.
- 닉네임 수정 등 마이페이지 편집 기능은 제외합니다.
- 사장님의 QR 스캔 버튼은 회원번호 입력 흐름으로 교체합니다.
- 프로토타입 HTML과 CSS는 디자인 참고 자료이며 실제 데이터의 기준으로 사용하지 않습니다.

## 검증과 완료 기준

1. 손님과 사장님 고정 계정으로 로그인할 수 있습니다.
2. 새로고침 후 로그인 세션과 현재 역할이 유지됩니다.
3. 손님은 여러 카페와 서로 다른 목표의 스탬프 카드를 볼 수 있습니다.
4. 사장님은 자신의 담당 카페에서 유효한 회원번호로 고객을 조회할 수 있습니다.
5. 적립 버튼을 한 번 누르면 해당 카페 스탬프가 정확히 1개 증가합니다.
6. `stamp_goal - 1` 상태에서 적립하면 해당 카페 쿠폰 1장이 발행되고 스탬프가 0개가 됩니다.
7. 일반 적립에는 적립 알림 1개, 쿠폰 발행 적립에는 적립·쿠폰 알림이 생성됩니다.
8. 손님 화면을 새로고침하면 사장님 화면의 처리 결과와 동일한 데이터가 표시됩니다.
9. 다른 역할이나 다른 카페 데이터에 대한 허용되지 않은 접근은 UI와 데이터베이스 모두에서 차단됩니다.
10. 시연 데이터를 목표 직전 상태로 반복해서 복원할 수 있습니다.
11. 320~480px 화면에서 가로 스크롤 없이 핵심 흐름을 사용할 수 있습니다.

## 우선순위

1. Supabase Auth와 역할 연결
2. 데이터베이스 스키마, RLS, 시연 데이터
3. 손님 다중 카페 조회
4. 사장님 회원번호 조회
5. 원자적 적립 RPC
6. 쿠폰과 알림 조회
7. 모바일 화면과 오류·로딩 상태
8. 통합 테스트, 배포, 시연 데이터 복구

시간이 부족하면 시각 효과와 부가 화면을 줄이고 로그인, 조회, 적립, 쿠폰, 알림, 새로고침 반영, 데이터 복구를 우선 완료합니다.
