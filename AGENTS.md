# 작업 지침

이 프로젝트는 개인 카페 스탬프를 하나의 웹 서비스에서 관리하는 무료 MVP 서비스입니다. 이후 작업자는 아래 방향을 우선 기준으로 삼아야 합니다.

## 제품 방향

- 웹 기반 서비스로 기획하고 구현합니다.
- 모바일 브라우저와 데스크톱 브라우저에서 모두 사용할 수 있는 반응형 UI를 우선합니다.
- 초기 로그인은 임시 테스트 로그인으로 처리합니다.
- 카카오 로그인은 필수 구현이 아니라 추후 선택 기능으로 둡니다.
- 핵심 경험은 손님이 웹 화면의 본인 QR 코드를 제시하고, 카페 직원이 매장용 웹 화면에서 이를 스캔해 스탬프를 즉시 적립하는 흐름입니다.
- 손님과 카페 사장님/직원을 모두 주요 사용자로 다룹니다.
- 카페별 스탬프 개수와 보상 내용은 자유롭게 설정할 수 있어야 합니다.
- 스탬프 조건이 충족되면 쿠폰은 자동 발급까지 처리합니다.

## 범위 기준

- 초기 MVP는 무료 사용을 전제로 합니다.
- 결제/POS 연동은 구현하지 않습니다.
- 지도 기반 탐색과 카페 검색은 구현하지 않습니다.
- 쿠폰 실제 사용 승인 흐름은 구현하지 않습니다.
- 부정 적립 방지는 이번 버전에서 구현하지 않고 향후 개선 과제로만 다룹니다.
- 네이티브 Android/iOS 앱은 초기 범위에 포함하지 않습니다.

## 문서 기준

- 서비스 기획의 기준 문서는 `docs/service-plan.md`입니다.
- 구현 작업 체크리스트의 기준 문서는 `checklist.md`입니다.
- README는 프로젝트의 짧은 소개와 MVP 범위 요약을 담습니다.
- 새로운 기능을 제안하거나 구현하기 전에 기존 기획 범위와 충돌하지 않는지 먼저 확인합니다.
- 기획 범위를 넓히는 변경은 핵심 MVP 흐름에 필요한지 검토한 뒤 반영합니다.

## 구현 판단 기준

- 사용 흐름은 단순해야 합니다: 테스트 로그인, 손님 QR 제시, 매장 웹 스캔, 스탬프 적립, 쿠폰 자동 발급.
- 초기 구현에서는 운영 효율보다 사용자 경험 검증을 우선합니다.
- 웹 브라우저 카메라 권한은 직원용 QR 스캔 화면에서만 요청합니다.
- 카페별 자유 규칙은 유지하되, 결제 검증이나 부정 방지처럼 복잡한 정책은 넣지 않습니다.
- 문서와 코드가 충돌하면 문서를 먼저 갱신해 의도를 명확히 한 뒤 구현합니다.
