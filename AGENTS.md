# AGENTS.md

이 파일은 이 저장소에서 작업하는 모든 AI 에이전트(Claude, Codex 등)가 따르는 짧고 잘 변하지 않는 규칙이다.

## 제품 규칙

1. 제품 기준 문서는 `docs/PRD.md`이다. 요구사항이 충돌하면 PRD를 우선하고, PRD 변경이 필요하면 먼저 사용자에게 확인한다.
2. MVP는 5개 기능이다: 리뷰 유형 분류, 톤앤매너(매장 프로필), 답글 초안 생성·복사, 리뷰 분석, 블랙컨슈머 대응 매뉴얼 (PRD 2.1~2.5). 리뷰 수동 입력과 앱 골격은 별도 기능이 아니라 모든 기능의 공통 기반이다.
3. 자동 리뷰 수집, 자동 답글 게시, 챗봇 알림, 멀티 플랫폼 확장은 Phase 2 이후로 둔다. 명시 요청 없이 MVP에 끼워 넣지 않는다.
4. 악성 리뷰 대응은 안내와 체크리스트까지만 제공한다. 법률 자문처럼 보이는 판단, 자동 신고, 자동 게시, 자동 보상 제안은 금지한다.
5. 답글 초안에는 보상·환불 약속, 확인되지 않은 사실 단정, 매장 프로필에 없는 정보, 위협적 표현을 넣지 않는다.
6. UI 문구는 한국어 존댓말을 기본으로 하고, 40~60대 소상공인이 읽기 쉬운 큰 글자·낮은 정보 밀도·명확한 버튼을 우선한다.
7. 폴더·파일 트리는 기능 위주로 구성한다. 기능 코드는 `features/<기능>` 폴더에 모으고, 여러 기능이 함께 쓰는 최소한만 `shared`에 둔다. 기준 트리는 `docs/CODE_MAP.md`에 있다.
8. 코드나 문서를 바꾸기 전에 관련 문서를 읽는다: `docs/PRD.md` → `docs/ARCHITECTURE.md` → `docs/CODE_MAP.md` → `HARNESS/README.md`.
9. 작업 후에는 변경된 의도와 다음 작업을 `LOG.md`와 `TODO.md`에 반영한다.

## 협업 규칙 (에이전트 역할 분담)

10. 문서(md) 작성은 Claude가 담당하고, 하네스·코드 실행과 로그·에러 파일 생성은 Codex가 담당한다. 서로의 영역을 자청해서 침범하지 않는다.
11. GitHub(원격)에는 md 파일만 올린다. 제품 코드, 하네스 실행 기록(`HARNESS/runs/`), 로그·에러 파일, JSON 설정·계약은 로컬 전용이며 절대 푸시하지 않는다.
12. 하네스 Phase는 기능 단위다. 기능 1개 = Phase 1개이며, Phase 0(공통 기반) 뒤 Phase 1~5가 MVP 5개 기능과 1:1로 대응하고, Phase 6은 기능이 아니라 전체 MVP를 통합 검증하는 별도 Phase다. 기준 문서는 `HARNESS/phases/PHASE_PLAN.md`다.
13. 개발 루프는 "기능(Phase) 1개를 하네스로 구현 → 실행해서 정상 동작 확인 → 다음 기능"의 반복이다. 마지막 Phase까지 완주하면 실행 가능한 프로젝트 하나가 완성된 상태여야 한다.
14. md 커밋은 과제 브랜치(`N092_박채빈`)에만 쌓는다. 하네스 실행과 그 auto-commit(코드 포함)은 푸시하지 않는 로컬 전용 브랜치에서 한다.
15. 커밋과 푸시는 사용자가 명시적으로 요청할 때만 수행한다.
16. 미커밋 파일과 `HARNESS/.gitignore`의 `*.json` 제외는 순차 제출 전략에 따른 의도된 상태다. 리뷰에서 문제로 지적하거나 임의로 되돌리지 않는다.
