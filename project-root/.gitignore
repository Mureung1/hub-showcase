# 의존성 (npm install로 언제든 재생성 가능)
node_modules/

# DB 파일 (실제 데이터 + 로컬 환경 산물, 커밋하면 안 됨)
backend/data/*.db
backend/data/*.db-wal
backend/data/*.db-shm

# 환경변수 (비밀키, API 키 등)
.env
.env.local

# OS/에디터 관련 잡파일
.DS_Store
Thumbs.db
.vscode/
CLAUDE.md

# 로그
*.log