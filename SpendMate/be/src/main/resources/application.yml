spring:
  application:
    name: SpendMate
  datasource:
    url: jdbc:postgresql://localhost:5432/spendmate
    username: soyeon
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true