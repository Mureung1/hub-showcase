# 검증 보고서: ORCH-2 — task-first 선택적 멀티에이전트 마이그레이션

> 상태: 통과
>
> 검증일: 2026-07-23
>
> 관련 계획: `plan.md`
>
> 검증 대상: 미커밋 작업트리의 운영 규칙·스킬·하네스 상태 검사

## 1. 변경·범위 요약

- 목표 대비 결과: T항목은 세밀한 백로그 단위로 유지하면서 실행 묶음 안의 독립 작업만 선택적으로 위임하는 task-first 운영으로 전환했다.
- 변경한 파일: `AGENTS.md`·`CLAUDE.md`, 오케스트레이션 스킬·역할 참조·UI metadata, `harness/README.md`, 상태 검사 스크립트·package 명령, T17·T25·T32~T36 상태 문서, `docs/CICD.md`, ORCH-2 계획·검증, `docs/LOG.md`
- 변경하지 않은 경계: 제품 코드·런타임·의존성·배포
- 시작 시 기존 변경 보존 여부: 사용자 소유 미추적 `.agents/skills/continue-dabnyangi-task/`, T25 CSV, `tmp/`를 수정하지 않음
- 허용 범위와 실제 diff 비교 결과: 운영 규칙·스킬·하네스·기존 상태 정본 보정에 한정했고 `src/`, `api/`, `public/`, 제품 계약은 변경하지 않음

## 2. 완료조건 추적

| ID | 검증 방법 | 결과 | 근거 |
| --- | --- | --- | --- |
| AC-1 | 미러·문서 검토 | 통과 | T항목·실행 묶음·에이전트 작업·역할 정의, `cmp AGENTS.md CLAUDE.md` 0 |
| AC-2 | 스킬·역할 참조 검토 | 통과 | 독립 산출물·고정 계약·안전 소유·순효율 네 게이트와 PM 단독 기본값 |
| AC-3 | 위임 시나리오 검토 | 통과 | 읽기 우선, writer 최대 2명, 중첩 금지, 공유 계약·CHECKLIST·LOG는 PM 소유 |
| AC-4 | 정상 실행·self-test·수정 전 실행 | 통과 | self-test 4건, 수정 전 16건 검출, 최종 22개 폴더 통과 |
| AC-5 | `npm run harness:check` | 통과 | T17·T25 종료/통과, T32~T36 허용 상태명과 CHECKLIST 정합성 확인 |
| AC-6 | 실제 diff 경계 검토 | 통과 | 제품 코드·MVP·런타임·의존성·배포 변경 없음 |
| AC-7 | validator·미러·링크·diff 검사 | 통과 | `Skill is valid!`, mirror/link/diff exit 0 |

## 3. 자동 검증

| 항목 | 명령 또는 방법 | 결과 | 근거·관찰 요약 |
| --- | --- | --- | --- |
| 관련 테스트 | `npm run harness:check -- --self-test` | 통과 | 4개 감지 self-test + 22개 실제 작업 폴더(15 T, 7 비-T) 통과 |
| 전체 테스트 | `npm test` | 통과 | 44파일 375테스트 |
| 린트 | `npm run lint` | 통과 | oxlint 오류 0 |
| 빌드 | `npm run build` | 통과 | TypeScript·Vite production build; 기존 CatCanvas 500 kB 경고만 유지 |
| 변경 형식 | `git diff --check` | 통과 | 공백 오류 0 |
| 작업별 추가 검증 | skill validator·미러·링크 검사 | 통과 | 임시 validator 환경의 PyYAML 6.0.3으로 `Skill is valid!`; 미러·대상 링크 exit 0 |

## 4. 수동 검증

| 시나리오 | 환경 | 기대 결과 | 실제 결과 | 상태·근거 |
| --- | --- | --- | --- | --- |
| 작은 한 파일 수정 | 스킬 라우팅 표 | PM 단독 | specialist를 호출하지 않도록 명시 | 통과 |
| 코드 탐색·테스트 로그 분석 | 스킬 라우팅 표 | 읽기 전용 작업 우선 | 1~2 읽기 전용 worker 후 PM 종합 | 통과 |
| 고정 계약의 FE/BE 수직 작업 | 위임 게이트·소유 규칙 | 비중첩 writer만 최대 2명 | 계약 고정·정확한 경로 소유·PM 통합 요구 | 통과 |
| T22→T23→T31 직렬 의존 | 스킬 예시 | T별 에이전트 자동 배정 금지 | 순차 구현 후 읽기 전용 검증으로 명시 | 통과 |

## 5. 정본·규칙 확인

- 작업별 정본 정합성: AGENTS/CLAUDE, 하네스 상태 계약, CHECKLIST와 기존 T 작업 헤더, CICD의 T17 최종 결정이 일치
- MVP 범위 준수: 예 — 제품 코드 미변경
- 코드 작업인 경우 `any` 미사용 확인 방법: 상태 검사 스크립트에 TypeScript `any` 해당 없음
- 미치환 필수 항목 없음: ORCH-2·스킬 대상 검색 확인
- 남은 위험·알려진 한계: 실제 개발 작업의 시간·토큰·인계·재작업 효과는 아직 측정하지 않았다. 검사기는 상태 헤더와 CHECKLIST 정합성을 검사하며 본문 전체 의미를 판정하지 않는다. 기존 jsdom `scrollTo` 로그와 CatCanvas 크기 경고는 이번 범위 밖이다.

## 6. 완료 판정·인계

- 필수 AC 전부 통과: 예
- 적용되는 자동·수동 검증 전부 통과: 예
- 미해결 차단사항 없음: 예
- `docs/CHECKLIST.md` 갱신 여부와 근거: 비-T 운영 변경이므로 미갱신
- `docs/LOG.md` 기록 및 계획·검증 보고서 링크: 2026-07-23 ORCH-2 항목에 기록
- 후속 작업 또는 사용자 판단이 필요한 사항: 다음 적합 작업 2~3건에서 활성 worker, 실제 병렬 구간, 충돌·재작업, 검증과 순효율을 기록한 뒤 custom agent/worktree 도입 여부 판단

위 조건을 충족하지 못하면 상태를 `통과`로 기록하지 않는다.
