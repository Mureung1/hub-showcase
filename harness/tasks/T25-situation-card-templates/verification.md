# T25 상황 카드 콘텐츠 — 72개 관계·톤 정합성 검증 기록

> 상태: 진행 중
>
> 검증일: 2026-07-12 (자동·자체 preflight분)
>
> 관련 계획: [`plan.md`](plan.md)
>
> 검증 대상: 작업트리의 `src/ProjectIntro.tsx` 72문구와 블라인드 검수 자료 — 사람 평가자 2인 전수 검수 전이므로 `통과`로 기록하지 않는다

## 범위

- 대상: 관계 4개 × 상황 6개 × B/S/C 3개 = 72개
- 승인된 계약 변경: `apologize` 카드 라벨을 `답장이 늦었을 때 사과`로 구체화
- 미변경: `SituationId`, 조회 키, 72개 수, 화면 단계, 라우팅, 백엔드, AI 경로
- 공식 T25 완료: 미완료 — 사람 평가자 2인 전수 검수 대기

## 자동 검증

| 검증 | 방법 | 결과 |
|---|---|---|
| 변경 전 기준선 | `npm test` | 통과 — 6개 파일, 35개 테스트 |
| 카드 계약 | 네 관계 모두 새 사과 카드 라벨 포함 | 통과 |
| 24개 카드·72개 후보 노출 | App 전수 렌더 | 통과 |
| 카드별 후보 고유 | 세 후보 `Set.size === 3` | 통과 — 24/24 |
| 전체 문구 고유 | 72개 문자열 비교 | 통과 — 72/72 |
| S 쿠션 수 | 승인된 완화 표현 목록 기준 | 통과 — 24개 모두 정확히 1개 |
| C 간결성 | C 길이 ≤ S 길이 | 통과 — 24/24 |
| 교수·조교 말투 | 가벼운 `감사해요·죄송해요·어려워요·용·ㅎㅎ·ㅠ` 금지 및 합니다체/`-까요?` 확인 | 통과 — 18/18 |
| 비공식 관계 문장부호 | 팀플·선배·친구 마침표 검사 | 통과 — 54/54 마침표 0건 |
| 구체 사과 | 사과 12개에 `답` 또는 `답장` 포함 | 통과 — 12/12 |
| 다른 카드의 답장 지연 창작 | 사과 외 카드에서 답장 지연 표현 검사 | 통과 — 0건 |
| 자리 표시자 | `[부탁할 내용]` 후보 수 | 12개 |
| 친밀 표지 | `ㅎㅎ` 1개, `용·ㅠ·!` | 각각 1/0/0/0 |
| 입력 없는 날짜·사유·약속 | 고정 금지 표현 검사 | 통과 — 0건 |
| 소스·전수 검수지 동기화 | 72개 원문 포함 여부 | 통과 — 누락 0건 |
| 전체 테스트 | `npm test` | 통과 — 6개 파일, 35개 테스트 |
| 린트 | `npm run lint` | 통과 |
| 프로덕션 빌드 | `npm run build` | 통과 |
| 공백·충돌 표식 | `git diff --check` | 통과 |

## 관계별 적용

| 관계 | 적용 결과 |
|---|---|
| 팀플·조모임 | 해요체 유지, B/S/C별 질문·완화·직접 요청 골격 분산 |
| 교수·조교 | 합니다체와 `-까요?`만 사용하고 가벼운 해요체·친밀 표지 제거 |
| 선배·동기 | 현재 공용 `senior` 키의 한계 안에서 양쪽에 안전한 해요체 사용 |
| 친구·연인 | 반말 사용, `미안하지만`·`마음 쓰인 부분` 같은 문어 표현 제거 |

## B/S/C 자체 preflight

- B는 추가 쿠션 없이 예의와 화행이 드러나는 기본 문구로 작성했다.
- S는 각 세트에 선택권·부담 완화 또는 관계상 자연스러운 완화 표지 하나만 사용했다.
- C는 핵심 의사를 앞에 두고 S보다 길지 않게 작성했다.
- 자동 검사는 문자열 규칙을 보장할 뿐, 사실·의도 동일성과 소리 내어 읽었을 때의 자연스러움은 사람 평가를 대신하지 않는다.
- `thanks_check`처럼 선택권이 없는 감사 화행의 S는 `덕분에` 또는 `정말` 한 개만 사용했다.

## 말투 취향 후속 방향

- 음슴체·용용체·다나까체는 B/S/C와 다른 사용자 표현 습관 축이다.
- 이번 72개에는 추가하지 않았다.
- 후속 검증은 필수 온보딩이 아니라 `다른 상황이냥?` AI 경로의 선택 기능으로 시작한다.
- 교수·조교에는 친밀체를 노출하지 않는 등 관계별 허용 범위를 둔다.
- 단순 어미 치환은 사용하지 않고 별도 프롬프트·검수 기준으로 평가한다.

## 2026-07-12 1차 검수 반영 수정

1차 검수(최종 의견: 수정 후 재검수, hard fail 0건, 자연 1점 약 27개)의 우선 수정 4범주를 16개 문구에 반영했다.

- 조건부 사과 4건 제거: `기다리게 했다면/해드렸다면/기다렸으면` → 무조건 사과 `기다리게 해서/해드려` (GW·PF·SR·FR APO S)
- 교수·조교 딱딱함 5건: 거절 세트 3건을 `어렵습니다` 단정에서 `어려울 것 같습니다/어렵겠습니다`로, `문의드립니다` 공문체 제거(ABS S), `혹시` 이중 완곡 제거(ASK S)
- 완곡 표현 중복 3건: `괜찮으시다면…알려주실 수 있을까요` 이중 완곡을 유연성 제안(`편하신 시간에 맞추겠습니다`)으로(SCH S), `부담되지 않으시면`→`시간 되실 때`(GW ASK S), `혹시 괜찮으시면`→`바쁘지 않으시면`(SR ASK S)로 분산
- 감사 톤 차이 2세트: GW THX를 B=알려주셔서/S=챙겨주신 덕분에/C=간결 확인으로 재작성(C1 대표 세트 — 내부 preflight 재기록), PF THX S에 `큰 도움이 됐습니다` 추가
- 판정 기준 명확화: `full-review.md`에 합격 집계는 `F/R/X`·모드·관계·전송·톤으로만 계산하고 `자연` 점수는 재작성 우선순위 참고 정보임을 명시 (1차 검수의 자연 점수↔합격선 해석 간극 제거)
- 테스트 동기화: 조건부 사과 3패턴을 금지 목록으로 이동, 완화 표현 목록에 신규 5종 추가, 대표 세트(C1·C2) 원문 갱신
- C1·C2 대표 문구가 바뀌었으므로 `representative-review.md` 동기화 — R1 평가자 2인 검수는 이 버전으로 수행한다

'답장/먼저 보내기 모두 통과' 평가 기준 수정(1차 검수 의견 5번)은 SPEC 계약 변경이라 반영하지 않았고 별도 제안 대기.

## 현재 판정

- 자동 형식·금지 규칙 preflight: 통과 (1차 검수 반영 수정 후 재실행)
- 자연스러움·사실/의도 동일성·관계/모드·톤 공식 판정: 사람 검수 대기
- T25 체크리스트: 변경하지 않음
- 다음 게이트: 평가자 2인의 `full-review.md` 독립 검수와 불일치 시 제3자 판정
