# 작업 계획: ORCH-1 — 답냥이 개발 AI 오케스트레이션

> 상태: 종료
>
> 작성일: 2026-07-15
>
> 최종 갱신일: 2026-07-15
>
> 현재 단계: 종료
>
> 다음 행동: 다음 적합한 실제 작업에서 스킬을 호출하고 효율·충돌 여부 재평가
>
> CHECKLIST 항목: 해당 없음(제품 기능이 아닌 개발 작업 절차)

## 1. 목표와 완료조건

- 해결할 사용자/제품 문제: 답냥이의 PM·디자인·프론트엔드·백엔드 작업을 한 에이전트가 순차 처리할 때 생기는 병목을 줄이되, 공유 작업트리 충돌과 범위 확장을 통제해야 한다.
- 목표 결과: PM을 단일 통합 책임자로 두고 필요한 전문 역할만 안전하게 병렬화하는 저장소 스킬과 역할·인계 계약을 만든다. 사용자 요청을 처리하는 제품 런타임은 기존 단일 AI 워크플로를 유지한다.

| ID | 검증 가능한 완료조건 | 검증 방법 | 필수 여부 |
| --- | --- | --- | --- |
| AC-1 | `orchestrate-dabnyangi-task` 스킬이 PM·디자인·프론트엔드·백엔드/AI 역할과 활성화 기준을 정의한다. | SKILL·역할 참조 검토 | 필수 |
| AC-2 | 병렬 작업 전에 계약·파일 소유권·완료조건을 고정하고, 같은 파일 동시 수정과 하위 에이전트의 임의 완료 판정을 금지한다. | 충돌 시나리오 전방 테스트 | 필수 |
| AC-3 | 역할별 작업 카드와 인계 형식이 정본 경로·소유 경로·제외 범위·검증·차단사항을 빠짐없이 전달한다. | 역할별 시범 프롬프트 평가 | 필수 |
| AC-4 | 앱 런타임 멀티에이전트와 개발 오케스트레이션을 명시적으로 분리하고 MVP 기능·코드는 변경하지 않는다. | AI_DESIGN·diff 검토 | 필수 |
| AC-5 | 스킬 형식·UI 메타데이터·AGENTS 미러·링크·diff가 검증된다. | `quick_validate.py`, mirror/link/diff 검사 | 필수 |

## 2. 의존성·정본 확인

- T항목과 의존성 상태 또는 비-T 식별 근거: 제품 CHECKLIST 기능이 아닌 저장소 개발 절차이며, 사용자가 역할 기반 AI 오케스트레이션 도입을 승인했다.
- CHECKLIST 본문이 직접 가리키는 문서·구간: 해당 없음
- 추가로 확인한 정본: `AGENTS.md`, `harness/README.md`, `docs/AI_DESIGN.md`의 단일 AI 워크플로, `skill-creator`, 기존 `build-cat-stage` 저장소 스킬
- 이번 작업에서 바꾸지 않는 계약·범위: S0~S3, 메시지 생성 API·DB·프롬프트, T29 UI, RAG/런타임 멀티에이전트 미사용, 제품 의존성

## 3. 작업트리 기준선

- 시작 시 `git status --short` 요약: T25 문구 검수, T28/T29 UI·에셋, 문서·패키지 변경과 사용자 제공 이미지가 함께 있는 미커밋 작업트리
- 기존 변경 중 반드시 보존할 파일·의도: 기존 변경 전체, T25 CSV, 루트 이미지와 새 `senior-cat-avatar-*.png`, T29 스킬·하네스 기록
- 이번 작업이 소유하는 경로: `.agents/skills/orchestrate-dabnyangi-task/`, `.claude/skills/orchestrate-dabnyangi-task`, `AGENTS.md`, `CLAUDE.md`, `docs/AI_DESIGN.md`, `docs/LOG.md`, `harness/tasks/2026-07-15-ai-orchestration/`

## 4. 5요소 계획

| 요소 | 계획 |
| --- | --- |
| 맥락 | 답냥이 저장소는 정본 문서와 T항목 하네스를 이미 사용한다. 새 도구 서버나 제품 의존성을 추가하지 않고 Codex 저장소 스킬로 개발 역할 조율만 표준화한다. |
| 구체성 | `skill-creator`로 저장소 스킬을 초기화하고 SKILL에 PM 오케스트레이션 단계, `references/roles-and-handoffs.md`에 역할 경계·작업 카드·인계 형식을 둔다. AGENTS/CLAUDE 라우팅과 AI_DESIGN의 런타임 분리 설명을 동기화한다. |
| 역할·예시 | 교수님 직접입력 수직 작업이면 PM이 SPEC 계약을 고정한 뒤 디자이너는 입력/고지 검토, 프론트는 폼·상태, 백엔드는 `/api/generate`·원문 비저장만 소유한다. 이미지 교체처럼 백엔드 무관 작업에는 해당 역할을 호출하지 않는다. |
| 단계화 | ① 기준선·정본 확인 → ② 계획 기록 → ③ 스킬 초기화 → ④ 핵심 워크플로·역할 참조 작성 → ⑤ AGENTS/AI_DESIGN 동기화 → ⑥ 역할별 읽기 전용 전방 테스트 → ⑦ 보정·검증·LOG |
| 검증 | 스킬 정적 validator, AGENTS=CLAUDE, 참조 경로, 미치환 템플릿, `git diff --check`를 확인한다. 디자인·프론트·백엔드 관점의 최소 맥락 전방 테스트로 파일 충돌·계약 누락·런타임 다중 호출 오해가 없는지 검토한다. |

## 5. 변경 경계와 위험

- 허용된 변경: 저장소 스킬·역할 참조·호환 심볼릭 링크·작업 절차 문서·AI 설계 구분·하네스/LOG
- 명시적으로 제외한 변경: 앱 코드, API/DB 구현, 새 npm 의존성, 실제 제품 런타임 멀티에이전트, 자동 커밋·푸시, 무제한 에이전트 루프
- 구조·계약·의존성 승인이 필요한 지점: 제품 기능·런타임 호출 구조·브랜치/커밋 정책 변경은 별도 승인 필요
- 예상 위험과 대응: 모든 작업에 네 역할을 강제하면 토큰·시간이 늘어나므로 역할은 필요할 때만 활성화한다. 공유 작업트리는 PM이 경로 소유권을 먼저 배정하고 같은 파일 병렬 수정을 금지한다.

## 6. 승인·범위 변경 기록

| 날짜 | 상태 | 승인 또는 변경 내용 | 근거 |
| --- | --- | --- | --- |
| 2026-07-15 | 승인됨 | PM·디자이너·프론트엔드·백엔드 역할의 개발용 AI 오케스트레이션을 저장소 운영 체계로 도입 | 사용자 “진행합니다” |
| 2026-07-15 | 진행 중 | 런타임이 아닌 저장소 스킬로 먼저 시범 운영하고 반복 절차만 자동화 | 직전 제안에 대한 사용자 승인 |

범위나 완료조건이 바뀌면 구현을 중단하고 이 표와 관련 항목을 갱신한 뒤 재승인받는다.

## 7. 진행·인계

- 마지막으로 끝낸 단계: 저장소 스킬·역할 계약·AGENTS 라우팅·런타임 구분, 역할별 읽기 전용 전방 테스트와 최종 검증
- 현재 작업 중인 단계: 없음
- 다음 행동: 다음 적합한 실제 작업에서 `$orchestrate-dabnyangi-task` 적용
- 보류 사유와 재개 조건: 없음

| 날짜 | 진행·결정 | 근거·영향 |
| --- | --- | --- |
| 2026-07-15 | PM은 루트 통합자, 전문 역할은 제한된 소유 경로의 실행자/검토자로 설계 | 완료 판정·정본 충돌 방지 |
| 2026-07-15 | UX 전용·교차 수직·migration 전용 읽기 전방 테스트 3건 통과 | 역할 선택·의존성 게이트·표준 인계 확인 |
| 2026-07-15 | skill validator·미러·심볼릭 링크·미치환 토큰·diff 검사 통과 | 검증 보고서 `통과`, 작업 종료 |
