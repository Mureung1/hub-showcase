# 작업 계획: 2026-07-12-ui-color-splash — 색 강화·폰트 정합·스플래시

> 상태: 종료
>
> 작성일: 2026-07-12
>
> 최종 갱신일: 2026-07-13
>
> 현재 단계: 완료 — 구현·자동 검증(테스트 56/56, 대비 16/16, 린트·빌드) + 사용자 실물 확인(2026-07-13) 통과
>
> 다음 행동: 없음 (추가 UI/UX 보강은 2주차 이슈 #5에서 별도 진행)
>
> CHECKLIST 항목: 해당 없음(T14 완료 후 사용자 요청 디자인 보강 — 비-T)

## 1. 목표와 완료조건

- 해결할 사용자/제품 문제: 전반적으로 색이 너무 연해 위계·액션이 묻힘(사용자 지적). 점검 중 접근성 결함 2건(진행 표시 대기 1.9:1, 포커스 링 알파 0.24)과 정본-코드 폰트 불일치(Gowun Dodum 합성 굵기) 발견. 진입 시 브랜드 한 줄 소개를 잠깐 보여주는 스플래시 요청.
- 목표 결과: 파스텔 무드를 유지한 채 한 단계 진한 토큰, AA 결함 해소, DESIGN.md 폰트 규칙 준수, 세션당 1회 스플래시.

| ID | 검증 가능한 완료조건 | 검증 방법 | 필수 여부 |
| --- | --- | --- | --- |
| AC-1 | 색 토큰 강화가 DESIGN.md와 코드에 동일 반영 | 두 파일 값 대조 | 필수 |
| AC-2 | 변경된 색 쌍 전부 AA(텍스트 4.5:1) 충족 — CTA 흰 글자, muted, 진행 대기, 파스텔 위 본문·강조 | 대비 계산 스크립트 실측 | 필수 |
| AC-3 | 진행 표시 대기 단계 1.9:1 결함과 포커스 링 식별성 수정 | 실측 + 코드 확인 | 필수 |
| AC-4 | 본문=Noto Sans KR 실제 굵기, Gowun Dodum은 브랜드 표시(400)만 — 합성 굵기 0건 | `font-weight: 650\|750` grep 0건 | 필수 |
| AC-5 | 스플래시: 첫 진입 표시→1.5초 후 페이드아웃, 탭·키 스킵, 세션당 1회, reduced-motion 무페이드, aria-hidden 장식 레이어 | Splash 테스트 4건 + 수동 확인 | 필수 |
| AC-6 | 기존 흐름 회귀 없음 | `npm test` 전체 + lint + build + `git diff --check` | 필수 |

## 2. 의존성·정본 확인

- 비-T 식별 근거: T14(모바일·접근성) 완료 후의 시각 보강. 화면 단계·라우팅·데이터 계약 무변경.
- 정본: docs/DESIGN.md(토큰·폰트 규칙·접근성 기준), docs/SCREENS.md(화면 흐름 — 스플래시 추가는 정본 먼저 갱신).
- 이번 작업에서 바꾸지 않는 계약·범위: S0~S3 단계·상태 전이, 시나리오·상황 카드 스키마, 템플릿 문구(T25 검수 대기 유지), sessionStorage `dabnyangi:flow` 계약.

## 3. 작업트리 기준선

- 시작 시 `git status --short`: 하네스 점검 변경 6건(LOG, tasks/README, T25 2건, product-audit, T17 폴더) — 커밋 대기 상태로 보존.
- 이번 작업이 소유하는 경로: docs/DESIGN.md, docs/SCREENS.md, src/index.css, src/app/App.css, src/app/App.tsx, src/app/Splash.tsx(신규), src/app/Splash.test.tsx(신규), docs/LOG.md, 이 폴더.

## 4. 5요소 계획

| 요소 | 계획 |
| --- | --- |
| 맥락 | React+TS(Vite), 라이브러리 추가 없음. 파스텔 무드 유지 + 위계 회복. 스플래시는 장식 레이어로 SR·초점 흐름 불변 |
| 구체성 | primary #2e7397(흰 글자 5.2:1)·soft/muted·bg·border·chip·card 5색 한 단계 하강, focus-ring 알파 0.45, 진행 대기 글자 muted, 히어로 오버레이 0.90/0.58, 폰트 Noto Sans KR 400~700 |
| 역할·예시 | 스플래시: sessionStorage `dabnyangi:splash-seen` 없으면 visible→(1.5s)→leaving→(0.3s)→gone, 클릭·키다운 즉시 gone, reduced-motion은 1s 후 즉시 gone |
| 단계화 | ① 정본(DESIGN·SCREENS) → ② index.css 토큰·폰트 → ③ App.css 색·굵기·히어로 → ④ Splash 컴포넌트·테스트 → ⑤ 대비 실측 → ⑥ 전체 게이트 |
| 검증 | AC표 그대로. 대비 스크립트 결과를 verification.md에 기록 |

## 5. 변경 경계와 위험

- 허용된 변경: 3절 소유 경로만.
- 명시적으로 제외: 화면 단계 추가 외의 구조 변경, 템플릿·시드 문구, 신규 라이브러리, T25/T16 게이트 우회.
- 위험: 기존 App 테스트 25건이 App을 렌더 — 스플래시가 aria-hidden이라 역할 조회는 안전하나, fake timer 테스트에서 act 경고 가능 → 실패 시 해당 테스트에 seen 키 시드. 색 하강으로 다른 쌍 대비 깨질 가능성 → 스크립트 전수 실측.

## 6. 승인·범위 변경 기록

| 날짜 | 상태 | 승인 또는 변경 내용 | 근거 |
| --- | --- | --- | --- |
| 2026-07-12 | 승인됨 → 진행 중 | 색 강화(사용자가 "너무 연함" 지적)와 스플래시(사용자 제안) 모두 사용자 발의 — 제안 표와 스펙 제시 후 착수 | 사용자 요청 대화 |

## 7. 진행·인계

- 마지막으로 끝낸 단계: ⑥ 전체 게이트 + 사용자 실물 확인
- 현재 작업 중인 단계: 없음 — 종료
- 다음 행동: 없음
- 보류 사유와 재개 조건: 해당 없음

| 날짜 | 진행·결정 | 근거·영향 |
| --- | --- | --- |
| 2026-07-12 | 점검 발견: 파스텔 5색 명도 90%+로 위계 소실, 진행 대기 1.9:1 AA 실패, 포커스 링 0.24, Gowun Dodum 합성 굵기 650/750 | App.css·index.css·DESIGN.md 대조 |
| 2026-07-13 | ①~⑥ 완료, 자동 검증 전부 통과 후 사용자 실물 색감·스플래시 확인 — 검증 `통과`·계획 `종료` | verification.md |
