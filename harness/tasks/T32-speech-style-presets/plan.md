# 작업 계획: T32 — 개인 말투 프리셋

> 상태: 진행 중
>
> 작성일: 2026-07-16
>
> 최종 갱신일: 2026-07-20
>
> 현재 단계: 카드 선행 말투 선택 제거·S2 병렬 경로·S3 말투 전환 구현 준비
>
> 다음 행동: 승인된 사용자 흐름 구현·회귀 검증 뒤 288문구 사람 검토와 실브라우저 검증
>
> CHECKLIST 항목: T32

## 1. 목표와 완료조건

- 해결할 사용자/제품 문제: 후보의 의미는 맞아도 평소 말끝과 달라 사용자가 다시 써야 하는 문제를 줄인다.
- 목표 결과: 카드 경로는 말투 선행 선택 없이 즉시 안전 기본 초안을 보여주고 S3에서 네 정적 말투 세트를 바꿀 수 있다. `내 상황을 직접 설명하기`는 동등한 경로로 보이며 AI 요청 전에만 말투를 명시적으로 고른다.

| ID | 검증 가능한 완료조건 | 검증 방법 | 필수 여부 |
| --- | --- | --- | --- |
| AC-1 | `습니다체 / 요체 / 이다체 / 용용체`의 타입·라벨·예시와 런타임 guard가 존재한다 | 도메인·계약 테스트 | 필수 |
| AC-2 | 네 말투가 네 관계의 카드·직접 설명 경로에 모두 제공된다 | 카탈로그·UI·계약 테스트 | 필수 |
| AC-3 | S2-a는 카드와 `내 상황을 직접 설명하기`를 동등한 경로로 제시하고, 말투를 고르지 않아도 카드를 눌러 즉시 S3에 도달한다 | RTL·수동 UI 확인 | 필수 |
| AC-4 | 선택값은 기존 30분 sessionStorage에 보존되고 오염값·구세션·전체 초기화에서 안전하게 복원·초기화된다 | 세션 상태 테스트 | 필수 |
| AC-5 | 카드 내부 조회는 저장된 말투 또는 관계별 안전 기본 `speechStyleId`를 필수로 사용하고, AI 요청은 S2-b에서 사용자가 고른 유효 말투만 받는다. 카드 요청은 목적·원문을 받지 않는다 | 도메인·공용 계약·handler 테스트 | 필수 |
| AC-6 | 목 생성과 서버 프롬프트가 선택 말투를 반영하며 기존 `기본/더 부드럽게/더 분명하게` 세 톤을 유지한다 | mock·prompt 테스트 | 필수 |
| AC-7 | 카드가 정확히 4관계×6상황×4말투×3톤=288개 초안을 가지며 조회 누락이 없고, 사람 검토 전에는 T25를 완료 처리하지 않는다 | 콘텐츠 커버리지 테스트·검토표 | 필수 |
| AC-8 | 관련·전체 테스트, API 타입검사, lint, build, diff가 통과한다 | 최종 게이트 | 필수 |
| AC-9 | 템플릿 S3에서 네 말투를 바꾸면 API·로딩·어미 변환 없이 같은 상황의 세 후보가 즉시 교체되고 복사 피드백이 초기화된다. AI S3는 숨은 재호출을 하지 않는다 | RTL·서비스 스파이 테스트 | 필수 |
| AC-10 | 카드 결과는 `선택한 관계와 상황을 기준으로 준비한 초안`이라고 정직하게 안내하고 AI 맞춤 결과로 오인시키지 않는다 | 카피·RTL 검토 | 필수 |

## 2. 의존성·정본 확인

- 의존: T3 생성 계약, T8 직접입력 생성 파이프라인 완료. T25 사람 검수는 미완료지만 사용자가 288문구를 먼저 생성하고 반복 검토하도록 승인했으므로 초안 구현은 진행하고 완료 체크는 보류한다.
- CHECKLIST 직접 참조: T25·T32와 SPEC 1~4장, SCREENS S2-a·S2-b·S3, EDGE_CASES 1-1·1-7.
- 추가 정본: MVP In/Out, AI_DESIGN의 단일 structured output·원문 비저장 경계.
- 변경하지 않는 계약: 카드 즉시 로컬 조회, 후보 3개와 toneLevel, AI 요청의 `speechStyleId`, provider/DB 연결, 영구 사용자 프로필, 자유 말투 입력.

## 3. 작업트리 기준선

- 시작 상태: `harness/tasks/T25-situation-card-templates/plan.md`는 이번 사용자 승인 기록으로 수정됨. 평가자 개인정보가 포함될 수 있는 T25 CSV 1개는 미추적 상태이며 읽기 전용·커밋 제외한다.
- 기준선: 1차 T32 구현 뒤 `npm test` 15개 파일 130개 통과. jsdom `scrollTo` 경고는 기존 비차단 경고다.
- PM 소유: 정본 문서, 공유 타입·계약, T32 계획·검증, CHECKLIST, LOG, 최종 통합.
- 제품 디자이너 소유: `src/entities/message/situationTemplates.ts`와 말투별 카드 검토 자료. 288문구는 검수 완료가 아닌 초안으로 작성한다.
- 프론트엔드 소유: `src/features/situation-select/SituationSelect.tsx`, `src/pages/message-flow/MessageFlow.tsx`, `src/app/App.css`, `src/app/App.test.tsx`.
- 백엔드/AI 소유: `api/_lib/prompt/speechStyleRules.ts`, 관련 prompt·handler 테스트. 실 provider·응답 schema는 바꾸지 않는다.

## 4. 5요소 계획

| 요소 | 계획 |
| --- | --- |
| 맥락 | React+TypeScript/Vite, 새 라이브러리·`any` 없이 개인 말끝 축을 기존 관계 태도 세 톤과 분리한다. 익명 MVP라 영구 학습 대신 현재 탭 30분 선택만 지원한다. |
| 구체성 | 템플릿 조회 키 `scenarioId × situationId × speechStyleId × toneLevel`은 유지하되, S2-a UI는 저장값 또는 관계 기본값을 결정해 바로 조회한다. S2-b는 AI 호출 전 명시 선택, 템플릿 S3는 로컬 정적 세트 전환을 제공한다. |
| 역할·예시 | 교수의 `thanks_check`도 `확인해 주셔서 감사합니다 / 확인해 주셔서 감사해요 / 확인해 주셔서 감사하다 / 확인해 주셔서 감사해용` 계열의 네 세트를 가지며 각 세트 안에서 B/S/C 의미를 유지한다. |
| 단계화 | ① 정본·관계 기본값 계약 → ② S2 경로 위계·카피 → ③ 카드 즉시 조회·S3 말투 전환·세션 → ④ 결정적 컴파일러 통합 → ⑤ 교차 검토·전체 검증 순으로 진행한다. |
| 검증 | 말투 미선택 카드 즉시 결과, 관계별 기본값, S3 네 말투 전환과 API 0회, S2-b 명시 선택·AI 1회, S2 병렬 경로 카피, 세션 복원·초기화, 96세트·288문구, 320/375·키보드를 확인한다. |

## 5. 변경 경계와 위험

- 허용: 카드 288문구 초안, 카드 안전 기본 말투와 결과 단계 전환, S2-b 명시 말투, sessionStorage 상태, 공유 요청·prompt/mock 유지, 검토 자료와 관련 정본·테스트.
- 제외: 런타임 템플릿 말끝 자동 변환, 자유 예시 입력, 로그인·DB·영구 프로필, provider 연결, 새 계측 속성.
- 관계 정책: 사용자 승인에 따라 네 말투를 네 관계에 모두 제공한다. 선택 가능 자체를 관계 적합성 합격으로 간주하지 않으며, 사람 검토에서 부적합 문구를 반복 수정한다.
- 위험: 288문구의 기계적 어미 치환과 관계 부적합을 자동 합격 처리하지 않는다. 정적 초안과 검토표를 함께 두고 T25는 외부 검토 전까지 미완료로 유지한다.

## 6. 승인·범위 변경 기록

| 날짜 | 상태 | 승인 또는 변경 내용 | 근거 |
| --- | --- | --- | --- |
| 2026-07-16 | 승인됨 | 개인별 말투 선택 기능 추가 | 사용자 요청 |
| 2026-07-16 | 승인됨 | `습니다체 / 요체 / 이다체 / 용용체` 네 말투 확정 | 사용자 요청 |
| 2026-07-16 | 승인됨 | 직접 설명 AI 경로 우선 적용, 관계 제한, 카드 72문구 유지 | 사용자 구현 시작 승인 |
| 2026-07-16 | 범위 확대 승인 | 카드 경로까지 네 말투를 제공하고 72×4=288문구를 먼저 생성한 뒤 사용자가 반복 검토·수정 | 사용자 요청 |
| 2026-07-20 | 사용자 흐름 재설계 승인 | 카드와 직접 설명을 동등한 경로로 제시하고 카드 선행 말투 선택을 제거. 관계 기본값으로 즉시 결과 뒤 S3에서 정적 말투 세트 전환 | 사용자 승인 |

## 7. 진행·인계

- 마지막으로 끝낸 단계: 기존 카드·직접 설명 공통 말투 선택, 288문구 초안, prompt/mock, 자동·역할 교차 검증.
- 현재 작업 중인 단계: 2026-07-20 승인된 S2 병렬 경로·카드 기본 말투·S3 전환으로 사용자 흐름 재구성.
- 다음 행동: 새 흐름의 RTL·전체 회귀 뒤 사용자가 검토표에서 문구를 반복 수정하고, Browser 제공 환경에서 320/375·Tab·방향키를 확인한다.
- 보류 사유: 사람 문구 검토는 T25, 실 provider 품질은 T20~T21, 실브라우저 증거는 T32에서 계속한다.

| 날짜 | 진행·결정 | 근거·영향 |
| --- | --- | --- |
| 2026-07-16 | T32 착수 | 말투 불일치로 인한 후수정 피드백과 사용자 구조 변경 승인 |
| 2026-07-16 | 코드·자동 검증 완료 | 전체 15파일 130테스트·API 타입검사·lint·build·diff 통과. 관계별 mock 회귀를 교차 검토에서 발견해 보완 |
| 2026-07-16 | 완료 체크 보류 | 소스·RTL·디자인 교차 검토는 통과했으나 실브라우저 320/375·키보드 확인이 남아 CHECKLIST T32는 미완료 유지 |
| 2026-07-16 | 카드 확장 재개 | 검토를 이유로 구현을 늦추지 않고 288문구 초안을 먼저 만든 뒤 사용자가 반복 검토하도록 범위 확대 승인 |
| 2026-07-16 | 확장 구현·자동 검증 완료 | 4관계×6상황×4말투×3톤, S2-a/S2-b 상태 공유, 카드 조회·prompt/mock·계약을 연결하고 전체 16파일 164테스트와 최종 게이트 통과 |
| 2026-07-16 | 검증 대기 | 사람 검토 전 T25를 미완료로 유지하고, Browser 부재로 320/375·Tab·방향키 실검증도 보류 |
| 2026-07-20 | 사용자 흐름 재개 | 템플릿 앱처럼 보이는 신뢰 위험을 줄이기 위해 S2 병렬 경로와 결과 단계 말투 전환을 승인받아 구현 재개 |
