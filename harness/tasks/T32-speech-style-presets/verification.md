# 검증 보고서: T32 — 개인 말투 프리셋

> 상태: 검증 대기
>
> 검증일: 2026-07-16
>
> 관련 계획: `plan.md`
>
> 검증 대상: 미커밋 작업트리

## 1. 변경·범위 요약

- 목표 대비 결과: 카드·`직접 설명할게요` 공통 말투 선택과 288문구 초안 구현·자동 검증 완료
- 변경한 파일: 도메인·288 템플릿, 공용 생성 계약, S2-a/S2-b 말투 선택·세션·결과 안내, 관계별 mock, 서버 prompt·요청 fixture, 제품 정본·T25/T32 하네스
- 변경하지 않은 경계: 실 provider/DB, 영구 프로필, 자유 말투 입력, 생성 응답·운영 메트릭 구조
- 시작 시 기존 변경: T25 계획의 사용자 승인 기록과 미추적 설문 CSV 보존

## 2. 완료조건 추적

| ID | 검증 방법 | 결과 | 근거 |
| --- | --- | --- | --- |
| AC-1 | 도메인·계약 테스트 | 통과 | 네 ID·라벨·예시와 런타임 guard 검증 |
| AC-2 | 카탈로그·UI·계약 테스트 | 통과 | 네 관계의 카드·직접 설명에 네 옵션 제공 |
| AC-3 | RTL·디자인 교차 검토·실브라우저 | 부분 통과 | 선택 전 카드 비활성, S2-a↔S2-b 공유, 직접 설명 진입은 통과. 320/375·키보드 실검증 대기 |
| AC-4 | App 세션 테스트 | 통과 | 30분 복원, 오염값·구세션, 관계 변경, 전체 초기화 검증 |
| AC-5 | 공용 계약·handler 테스트 | 통과 | 카드·AI 모두 유효 말투 필수, 카드의 목적·원문 금지, 유효 카드가 AI 경로로 새지 않음 |
| AC-6 | mock·prompt 테스트 | 통과 | 4관계×4말투와 세 톤 유지, 관계 규칙 우선 검증 |
| AC-7 | 콘텐츠 테스트·검토표 | 통과(초안) | 96세트·288문구·누락 0건. 사람 검토 전 T25 미완료 유지 |
| AC-8 | 최종 자동 게이트 | 통과 | 전체 16파일 164테스트, API 타입검사, lint, build, diff 통과 |

## 3. 자동 검증

| 항목 | 명령 또는 방법 | 결과 | 근거·관찰 요약 |
| --- | --- | --- | --- |
| 프론트 관련 테스트 | `npm test -- --run src/app/App.test.tsx` | 통과 | 44개. 카드 비활성·안내·직접 진입·화면 간 공유·세션·결과 설명 포함 |
| 콘텐츠 관련 테스트 | `npm test -- --run src/entities/message/situationTemplates.test.ts src/entities/message/message.test.ts` | 통과 | 2파일 14개. 콘텐츠 전용 9개 불변조건 포함 |
| 백엔드 관련 테스트 | prompt·mock·handler 중심 4파일 | 통과 | 58개. 4관계×4말투·세 톤·누락/unknown 400·카드 경로 분리 포함 |
| 전체 테스트 | `npm test` | 통과 | 16개 파일 164개. 기존 jsdom `scrollTo` 비차단 로그만 발생 |
| API 타입검사 | `npm run typecheck:api` | 통과 | 서버 전용 타입 포함 오류 0건 |
| 린트 | `npm run lint` | 통과 | 오류 0건 |
| 빌드 | `npm run build` | 통과 | main 245.18kB/73.47kB gzip. 기존 지연 CatCanvas 882.64kB/234.53kB와 500kB 경고 유지 |
| 변경 형식 | `git diff --check` | 통과 | 공백 오류 0건 |
| 저장소 규칙 | `cmp -s AGENTS.md CLAUDE.md`·변경 코드 `any` 검색 | 통과 | 미러 일치, 명시적 `any` 0건 |

## 4. 수동 검증

| 시나리오 | 환경 | 기대 결과 | 실제 결과 | 상태·근거 |
| --- | --- | --- | --- | --- |
| 관계별 말투 선택 | 320×568·375×667 | 허용 옵션만 한 열로 표시되고 키보드 선택·필수 안내·결과 말투 확인 가능 | Browser runtime 선택 실패, 목록 `[]` | 보류 — 브라우저 제공 환경에서 재실행 |

## 5. 정본·규칙 확인

- 정합성: 카드·AI 공통 말투 선택을 구현했고 S3의 `기본 / 더 부드럽게 / 더 분명하게` 전달 강도 축은 유지한다.
- MVP 범위: 사용자가 카드 288문구 확대를 명시 승인했다. 자유 입력·로그인·DB 저장은 추가하지 않는다.
- `any`: 변경 코드 명시 검색 0건.
- 개인정보: 평가자 정보가 있을 수 있는 미추적 T25 CSV는 읽기 전용으로 유지했고 변경·추적하지 않았다.
- 남은 위험: 실제 생성 품질과 output validator의 의미적 말투 판별은 T20~T21에서 확인한다. 작은 화면 스크롤 부담·가로 넘침·실제 키보드 동작은 수동 검증이 남았다.

## 6. 완료 판정·인계

- 필수 AC 전부 통과: 아니오 — 구현·자동 검증은 완료했으나 AC-3 실브라우저 증거가 남았다.
- CHECKLIST 갱신: T32 항목은 추가했고 완료 체크는 보류
- LOG 기록: 2026-07-16 확장 구현·자동 검증과 사람 검토·수동 보류 근거 기록
- 인계: 문구 수정은 `speech-style-review-draft.md`의 관계·상황·말투 행을 기준으로 반복하고, T25 사람 판정 전까지 운영 검수 완료로 표현하지 않는다.
