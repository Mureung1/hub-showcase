# Anthropic Claude API key (server-side only — never expose to the client bundle)
ANTHROPIC_API_KEY=

# server/
PORT=4000

# client/ (Vite only exposes VITE_-prefixed vars to the browser)
VITE_API_BASE_URL=http://localhost:4000
