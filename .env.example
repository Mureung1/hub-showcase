# 모두의 뇌 맥락 분석 API 설정
# 현재 기본값은 API 키 없이 동작하는 local-heuristic mock provider입니다.
MODU_BRAIN_ANALYSIS_PROVIDER=mock

# 추후 외부 LLM provider를 연결할 때 서버 환경에만 설정합니다.
# 클라이언트 코드에 API 키를 넣지 않습니다.
MODU_BRAIN_LLM_API_KEY=
