# CLAUDE.md — 자취방 청결관리사

## 0. 🚨 완료 보고 프로토콜 (가장 중요)

### 왜 이 규칙이 있는가

너는 거짓말을 하지 않는다. 하지만 **실행해보지 않고 "됐다"고 믿는** 경향이 있다.
코드를 짜고, 논리적으로 맞으니, 완료라고 보고한다. 실제로 안 돌려봤다.

그래서 **신뢰가 아니라 구조로 막는다.**

### 규칙: 실행 증거 없이 완료를 선언할 수 없다

**"완료했습니다", "동작합니다", "잘 됩니다"라는 말을 쓰려면
반드시 아래 형식을 채워야 한다:**

```
## 이슈 #N 완료 보고

### 실행한 명령어
$ bash scripts/verify.sh N

### 원시 출력 (요약 금지, 그대로 붙일 것)
[여기에 터미널 출력을 있는 그대로]

### 완료 기준 체크
- [x] 항목 A — 위 출력의 "✅ ..." 줄
- [ ] 항목 B — ❌ 실패. 이유: ...

### 판정
통과 N / 실패 M
```

**출력을 요약하지 마라. 붙여넣어라.**
"테스트가 모두 통과했습니다"는 증거가 아니다. `12 passed, 0 failed`가 증거다.

### 실패를 숨기지 마라

미달 항목이 있으면 **그대로 보고하고 이슈를 닫지 마라.**
"대부분 됐고 사소한 것만 남았습니다"라고 말하지 마라.
사소한지 아닌지는 사람이 판단한다.

**"모르겠습니다", "안 됩니다", "이 부분은 실패했습니다"는 정당한 답변이다.**
억지로 완료를 만들어내는 것보다 훨씬 낫다.

### 검증 스크립트가 없는 항목

UI처럼 자동 검증이 안 되는 건:
- 스크린샷을 찍어 PR에 첨부
- 또는 "수동 확인 필요: [구체적 절차]"라고 명시
- **"화면에서 잘 보입니다"라고 쓰지 마라.** 너는 화면을 못 본다.

---

## 1. 진단 엔진 불변식 (어기면 프로젝트가 무너진다)

### ① LLM에게 확률·판단을 시키지 않는다

LLM 역할은 딱 둘:
- **인식자**: 자유텍스트/사진 → KB 어휘 (`{smell_type: "rotten_egg"}`)
- **설명자**: 결정 → 사람 말 (`{move:"PROPOSE", top:"trap_dry"}` → 문장)

확률 갱신(베이즈), 질문 선택(정보이득), 종료 판단(임계값)은
**전부 `packages/kb/engine/`의 순수 함수.** LLM 관여 금지.

**이유**: 진단이 이상할 때 프롬프트를 주술처럼 만지는 대신 **KB 숫자를 고칠 수 있다.**
디버깅 가능성이 이 프로젝트의 핵심 설계다.

### ② KB를 고쳤으면 반드시 감사를 돌린다

```bash
cd packages/kb && node run.js audit <domain>
```

**"취약 쌍 0개"만 보고 안심하지 마라.**
반드시 **원인별 정확도 표**를 봐라. 취약 쌍이 0인데도 특정 원인이 0%일 수 있다.
(실제로 bathroom의 towel_damp가 그랬다)

### ③ 임계값(`engine/policy.js`의 `T`)을 손으로 고치지 않는다

정확도가 낮으면 `PROPOSE`를 낮추고 싶어질 것이다. **하지 마라.**

**실증**: `MIN_GAIN=0.15`는 v1(4축)에서 희귀 원인 정확도를 **0%**로 만들었다.
그리드 스윕으로 0.08을 찾아 45%로 복구했다. 그런데 v2(7축, 판별축 추가 후)에선
0.15여도 95%가 나왔다.

> **임계값 튜닝은 KB 결함의 증상 치료다. 근본 원인은 KB(관찰 축)다.**

정말 임계값을 바꿔야 한다면 `node engine/tune.js`로 스윕하고
근거를 `policy.js` 주석에 남겨라.

### ④ IG가 높은 질문 ≠ 좋은 질문

**실증**: `water_run_test`는 IG 1등(0.433)인데 단독 추가 시 정확도를 **떨어뜨렸다** (86%→75%).
평균적으로 유용하지만 특정 쌍을 확실히 못 가른다. 정책 엔진이 이걸 1순위로 뽑아
첫 턴을 낭비하고, 정작 취약한 쌍을 못 가른다.

> **IG(전역 불확실성 감소)와 D_JS(쌍별 판별력)는 다른 목적함수다. 둘 다 봐야 한다.**

축을 추가할 땐 `node engine/propose_axis.js`로 **추가하기 전에** 효과를 재라.

### ⑤ prior에 근거 없는 숫자 금지

모든 prior에 `prior_source`(어떻게 셌는지)와 `evidence`(URL 배열) 필수.
집계자가 자동으로 채운다. 손으로 고쳤으면 근거도 손으로 갱신하라.

### ⑥ confidence는 질적 라벨만

사용자에게 숫자(0.72) 노출 금지. `core.js`의 `label()` 사용: 유력/가능/낮음/희박.
API 응답에 `"confidence": 0.72`가 있으면 규칙 위반이다.

### ⑦ 골든셋(`packages/kb/goldenset.json`)을 수정하지 않는다

**절대 금지.** 시험 문제를 답에 맞춰 바꾸는 것과 같다.
KB가 골든셋을 못 맞히면, 고쳐야 할 것은 **KB**다.

골든셋 실행은 **이슈 #11(Day 9)에만.** 그 전에 돌리면 결과를 보고 KB를 고치고 싶어진다.
(예외: walking skeleton 확인용 smoke test로 K1 하나만. 점수로 세지 않는다.)

---

## 2. 아키텍처

```
apps/
  web/          React + Vite (프론트엔드)
  api/          Express (백엔드) — AI 호출은 여기서만
packages/
  kb/           ★ 진단 엔진 + 지식베이스
    engine/     순수 함수. LLM 없음. 테스트 있음.
    agents/     웹 크롤 → 카드 → 확률 (LLM은 구조화만)
    kb/         kitchen_odor.json, bathroom_mold.json
    goldenset.json   ⚠️ 수정 금지
  shared/       schema.js
scripts/
  verify.sh     ★ 완료 기준을 코드로
```

### 진단 흐름

```
사용자 입력
  → [LLM 인식자] 자유텍스트 → KB 어휘
  → [코드] bayes()로 사후확률 갱신
  → [코드] decide()로 다음 행동 결정 (ASK/VERIFY/PROPOSE/ASSUME)
  → [LLM 설명자] 결정 → 사람 말
  → 사용자 응답 → 반복
```

**LLM이 틀릴 수 있는 표면적이 2개뿐이고, 둘 다 클로드가 잘하는 일이다.**
진단 논리는 코드가 소유한다.

---

## 3. 명령어

```bash
npm run dev                        # FE :5173 + BE :3000

cd packages/kb
node engine/test.js                # 유닛테스트 12개 (손계산 대조)
node run.js collect <domain>       # 웹 크롤 → 카드 (LLM, API키 필요)
node run.js build   <domain>       # 카드 → KB (LLM 불필요)
node run.js audit   <domain>       # ★ 감사 리포트 (LLM 불필요)
node engine/tune.js                # 임계값 스윕 144조합
node engine/propose_axis.js        # 축 후보 평가

bash scripts/verify.sh <이슈번호>   # ★ 완료 기준 검증
```

---

## 4. 코드 규칙

- AI 호출은 **BE(apps/api)에서만** — API 키 보호
- 디자인은 **토큰 변수만** — raw hex 금지
- 컴포넌트 `PascalCase.jsx`, 나머지 `camelCase.js`
- 커밋: `type: 한글 요약` (feat/fix/docs/style/refactor/chore/test)
- PR 본문에 `Closes #N` + verify.sh 출력 첨부
- 파괴적 작업(대량 삭제, DB 초기화)은 확인 후

---

## 5. 스코프 고수

**임의로 기능을 추가하지 마라.** 핵심 하나를 탄탄히.

막히면 이 순서로 버린다 (그 자리에서 고민하지 말 것):
```
1순위 버림:  프로브 + 배경 연출 (E6)
2순위 버림:  사진 입력 (vision)
3순위 버림:  개인화 prior (E5)
4순위 버림:  bathroom 도메인

절대 안 버림:  환경 + 진단 + 턴 반복 + 종료 + 검증
```

---

## 6. 하루 시작/종료 프로토콜

### 시작
1. 어제 이슈의 `verify.sh`를 **다시 돌려라.** 어제 됐다고 오늘도 되는 게 아니다.
2. 오늘 이슈의 완료 기준을 읽어라.
3. **작업 계획을 3~5단계로 쪼개서 먼저 보여줘라.** 승인 없이 코딩 시작 금지.

### 종료
1. `bash scripts/verify.sh <N>` 실행
2. **원시 출력을 붙여서** 완료 보고 (§0 형식)
3. 미달 항목이 있으면 이슈에 남길 것 (숨기지 말 것)
4. 커밋 + PR (`Closes #N`)
