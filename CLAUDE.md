# Bridge — Claude 작업 가이드

## /clear 타이밍 가이드

### /clear란?
대화 기록(컨텍스트)만 삭제. 코드 파일·CLAUDE.md·메모리는 그대로 유지됨.
다음 대화 시작 시 CLAUDE.md가 자동 로드되므로 프로젝트 맥락은 이어짐.

### Claude가 /clear를 추천해야 하는 시점

아래 조건 중 하나라도 해당하면 대화 말미에 자연스럽게 `/clear` 후 새 대화를 제안할 것:

1. **마일스톤 완료** — 하나의 기능(컴포넌트·페이지·API 연동 등)이 완전히 구현되고 동작 확인까지 끝났을 때
2. **대화가 길어짐** — 주고받은 메시지가 많아져 앞의 내용을 참조하기 어려워질 때
3. **작업 맥락 전환** — 기획 → 구현, 프론트 → 백엔드 등 전혀 다른 작업으로 넘어갈 때
4. **오류 루프** — 같은 문제로 3회 이상 수정을 반복할 때 (컨텍스트 오염 가능성)

### 추천 문구 예시

> "이번 작업은 여기서 마무리됐어요. 대화가 길어졌으니 `/clear`로 컨텍스트를 비우고 새 대화에서 이어가는 걸 추천해요. 코드와 CLAUDE.md는 그대로 남아 있어요."

## Common commands

Frontend (`frontend/`)
- npm run dev
- npm run lint

Backend (`backend/`)
- npm run dev
