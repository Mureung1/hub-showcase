# CLAUDE.md

이 파일은 이 프로젝트에서 Claude Code가 따라야 할 작업 규칙입니다.

## 작업 범위

- 요청받은 파일/섹션/기능만 수정한다. 관련 있어 보인다는 이유로 요청하지 않은 다른 파일이나 섹션을 함께 고치지 않는다.
- 리팩터링, 구조 재구성, 표현 통일, 중복 제거 같은 "더 나아 보이는 개선"은 명시적으로 요청받았을 때만 한다. 스스로 판단해 먼저 하지 않는다.
- 여러 개선점이 보이면, 코드를 먼저 바꾸지 말고 무엇을 발견했는지 알려주고 진행 여부를 물어본다.
- 이미 확정된 것으로 언급된 내용(예: "이 섹션은 확정이야")은 참고만 하고 수정하지 않는다.
- 작업 범위가 애매하면, 넓게 해석해서 진행하지 말고 범위를 먼저 확인한다.

## 코드 작성 규칙

- 절대 모킹하지 않는다. 항상 실제로 동작하는 코드를 작성한다.
- 오버엔지니어링하지 않는다. 요청받은 기능만 구현하고, 요청하지 않은 기능이나 예외 처리를 임의로 추가하지 않는다.
- 함수/변수/파일 이름은 기능을 명확히 나타내도록 짓는다.
- 라이브러리를 새로 추가할 때는 최신 버전을 사용한다. 버전이나 API가 확실하지 않으면 추측하지 말고 공식 문서를 검색해서 확인한다.

## 문서(docs/) 작업 규칙

- `docs/` 폴더의 문서는 사용자가 지정한 파일/섹션만 수정한다.
- 문장을 다듬어달라는 요청이면 표현만 바꾸고 의미·구조·용어는 그대로 유지한다.
- 기존 내용을 삭제하거나 재구성하지 않는다. 새로운 내용을 추가하는 요청과 기존 내용을 수정하는 요청을 구분해서 처리한다.

## 답변 규칙

- 질문받은 것에만 답한다.
- 요청 범위를 벗어난 제안은 답변 끝에 짧게 언급하고, 먼저 실행하지 않는다.
