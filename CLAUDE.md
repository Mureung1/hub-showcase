# CLAUDE.md — Sherpa

> 이 파일은 매 세션 컨텍스트로 로드되는 **작업 지침**이다.
> "무엇을 했는가(진행/이력)"는 여기 적지 않는다 → git 커밋 · PR · `/docs`.
> 여기에는 "이 프로젝트에서 **어떻게** 일하는가"만 담는다 (잘 안 변하는 것).
>
> ⚠️ **위치 주의:** 이 파일이 있는 git 루트 `hub/`가 곧 pnpm 워크스페이스 루트다
> (중간 `sherpa-app/` 층은 없다). 코드·명령 모두 `hub/`에서 돈다(§5·§9).

---

## 1. 프로젝트 개요

**Sherpa(셰르파)** 는 소형 마트 사장님이 **재고**와 **유통기한**을 바코드 스캔만으로
관리하는 웹앱이다. 기존 범용 POS(윈포스 등)가 재고·유통기한 관리에 도움이 안 된다는
가족 마트 현장 경험에서 출발했다.

- **형태:** 설치 불필요, 브라우저에서 동작. 로컬 우선(local-first).
- **한 줄 정의:** 바코드로 재고를 넣고(입고) 빼고(출고), 유통기한을 관리하고, 임박 상품을 알려주는 도구.
- **진짜 목적:** **Toss Place 프론트엔드 개발자 포트폴리오의 핵심 산출물.**
  → 지원 직군이 프론트엔드이므로 **프론트엔드가 주인공**이다. 백엔드는 얇게 유지한다(§4, §11).

이름의 뜻: 히말라야에서 짐을 대신 지고 길을 안내하는 셰르파처럼, 사장님의 재고·유통기한 부담을 대신 진다.

---

## 2. 제품 철학 (모든 결정의 최상위 기준)

핵심은 **급진적 단순함(radical simplicity)**. 기능 수가 아니라 "얼마나 쉽게 이해되는가"로 성패가 갈린다.
새 기능이 이 원칙에 어긋나면 **넣지 않는다.**

- **비전문가 기준** — 훈련된 계산원이 아니라, 가끔 재고를 정리하는 사장님이 대상. 매뉴얼 없이 조작 가능해야 한다.
- **최소 버튼 / 스캔 우선** — 주 조작은 바코드 스캔. 한 화면엔 지금 해야 할 다음 행동 하나만.
- **점진적 노출(progressive disclosure)** — 옵션은 필요한 순간에만 등장. 고급 설정은 기본 화면에서 치운다.
- **맥락형 UI** — 지금 상황에 필요한 것만 보여준다.
- **우아한 오류 처리** — 실패는 복구 가능하게. 파괴적 동작(삭제 등)은 확인 절차 필수.
- **빠른 반복 입력** — 스캔 → 목록 누적 → 확정 흐름이 매끄러워야 한다.
- **색은 의미를 담을 때만** — 장식용 색 금지. 색만이 아니라 아이콘·라벨을 함께 써 색각 이상도 대응.

> **범위에서 의도적으로 제외:** 결제·판매 거래, 카드 단말기, 금전함, 영수증/라벨 출력, 매출·정산·발주.
> 재고가 빠져나가는 사건은 **순수 재고 차감(출고·소진)** 으로만 다룬다. "못 만든 게 아니라 안 만든 것."

---

## 3. 도메인 모델 & 용어집

**2계층 구조**: `Product`(상품 마스터) / `Lot`(입고 배치). 차감은 **FEFO**.
(실제 정의는 `packages/core/src/types.ts`.)

```ts
interface Product {          // 상품 마스터 (바코드로 식별)
  id: string;                // crypto.randomUUID() — 공유 PK (§4 참고)
  barcode: string;           // unique, indexed — 조회 키 (EAN-13)
  name: string;
  category?: string;
  createdAt: number;         // epoch ms
  updatedAt: number;         // 동기화 판단용
}

interface Lot {              // 입고 배치 (같은 상품도 입고 시점마다 별개 Lot)
  id: string;                // crypto.randomUUID() — 공유 PK
  productId: string;         // Product FK, indexed
  expiryDate: string;        // 유통기한 'YYYY-MM-DD' (문자열 사전순 = 날짜순 → FEFO 정렬 안전)
  quantity: number;          // 이 Lot의 잔여 수량 ← 재고의 source of truth
  inboundAt: number;         // 입고 시각 epoch ms
  updatedAt: number;
}
```

**핵심 규칙 (바꾸지 말 것):**
- **재고 수량은 `Lot`에만 존재한다.** `Product`의 총재고는 Lot 수량의 합으로 계산하는 **파생값**.
  원장(source of truth)을 Lot으로 단일화해 FEFO와 일관성을 유지한다.
- **FEFO(First Expired, First Out):** 출고 시 `expiryDate`가 빠른 Lot부터 차감한다.

**용어 매핑 (코드는 영어, 도메인 개념은 한글):**

| 코드 | 도메인 | 의미 |
|---|---|---|
| `product` | 상품 | 바코드로 식별되는 상품 마스터 |
| `lot` | 배치 | 한 번의 입고 단위. 유통기한·수량을 가짐 |
| `inbound` | 입고 | 재고 증가. 새 Lot 생성 |
| `outbound` | 출고 | 재고 감소. FEFO로 Lot 차감 |
| `fefo` | 선입선출(유통기한) | 유통기한 빠른 것부터 소진 |

**유통기한 상태(오늘 기준, 임계값은 설정에서 조정):**

| 상태 | 기준 | 색 |
|---|---|---|
| 만료 | 유통기한 < 오늘 | 빨강 |
| 임박 | 오늘 ~ D-3 | 앰버(주황) |
| 주의 | D-4 ~ D-7 | 노랑 |
| 정상 | D-8 이상 | 기본 |

---

## 4. 아키텍처

**local-first.** UI는 IndexedDB(Dexie)를 1차 데이터원으로 읽고, 서버(MySQL)를 진실의 원장으로 둔다.

| 영역 | 선택 | 비고 |
|---|---|---|
| 프론트 | React + TypeScript + Vite (SPA) | **주인공** · `apps/web` |
| 로컬 저장 | IndexedDB via **Dexie.js** | UI가 직접 읽는 1차 소스, 오프라인 동작 |
| 서버 | **Express** (Node.js) | **thin backend** — 상품 CRUD + 주문/재고 저장만. ⚠️ **아직 미생성**(§5) |
| 서버 DB | **MySQL** | 서버 측 source of truth. ⚠️ **아직 미생성** |
| ORM/드라이버 | `mysql2` **또는** Prisma | ⚠️ **미결** — §12 참고 |
| 배포 | Vercel(프론트) / Railway(Express) | 데모가 돌아가는 게 포트폴리오 가치 |

**공유 PK 전략 (확정):** IndexedDB와 MySQL이 같은 레코드를 나눠 갖고 나중에 동기화하므로,
auto-increment 대신 **클라이언트 생성 `crypto.randomUUID()`** 를 공유 PK로 쓴다.
로컬에서 정한 ID가 서버로 올라가도 그대로 유지되어 ID 충돌을 막는다.
(구현: `packages/core/src/id.ts`, Dexie 스토어는 `++` 미사용.)

**아직 만들지 않는 것:** 서버(Express/MySQL) 자체 · IndexedDB ↔ MySQL **동기화 로직**.
seam(교체 지점)만 확보하고 구현은 뒤로. 상품 조회는 **repository 인터페이스** 뒤에 두는 것이 목표이나,
**현재는 그 추상화가 없다** — `apps/web`의 `App.tsx`/`seed.ts`가 Dexie를 직접 호출한다.
나중에 API로 교체 가능하도록 이 직접 호출부를 repository seam으로 걷어내는 게 후속 과제.

> **왜 백엔드가 있는데 local-first인가:** 순수 오프라인이면 Express가 필요 없다. 굳이 Express를 넣는 이유는
> "실제 서버 왕복이 있을 때의 **낙관적 업데이트·에러 복구**를 프론트에서 어떻게 설계했나"를 보여주기 위함이다.
> 이 정당화가 백엔드 과투자를 막는 기준선이다.

---

## 5. 모노레포 구조

pnpm workspaces. **워크스페이스 루트는 git 루트 `hub/`와 동일하다**
(`pnpm-workspace.yaml`·`pnpm-lock.yaml`·루트 `package.json`이 모두 `hub/`에 있음). 명령도 거기서 실행(§9).

```
hub/                            # ★ git 루트(GitHub: chainru1e/hub) = pnpm 워크스페이스 루트 · 이 CLAUDE.md 위치
├─ package.json                 # 워크스페이스 루트 (private, scripts는 §9)
├─ pnpm-workspace.yaml          # packages: apps/* · packages/*
├─ pnpm-lock.yaml
├─ .nvmrc                       # Node 버전 고정 (22.22.0)
├─ .claude/skills/sherpa-pos-design/   # 디자인 헌법 스킬 (§8)
├─ apps/
│  ├─ web/                      # @sherpa/web — React + TS + Vite + Dexie (메인, Slice 0 구현)
│  └─ landing/                  # freshkeep-landing — 소개 페이지 (React + JS, Vite)
├─ packages/
│  └─ core/                     # @sherpa/core — 공유 도메인 타입(Product/Lot) + id 유틸 (load-bearing)
│                               #   ※ types.ts·id.ts 이미 존재하고 web이 소비. FEFO '로직'은 Slice 2에서 추가
└─ docs/
   └─ slice-0-decisions.md      # Slice 0 설계 결정 기록
```

- `packages/core`: web과 향후 앱이 함께 쓸 도메인 타입·유틸을 한 곳에. **타입(`types.ts`)·id(`id.ts`)는 이미 들어 있고 web이 `workspace:*`로 소비한다.** 아직 없는 것은 **FEFO 로직**뿐 — 실제 차감 로직이 필요한 **Slice 2**에서 core에 추가한다(빈 껍데기 로직 미리 만들지 말 것).
- **아직 없는 것(자리만 예약/계획):** `server/`(Express) · `apps/mobile`(네이티브). 필요 Slice에서 생성.

---

## 6. 상태 관리 규칙 (3계층 — 섞지 말 것)

> **현재 상태:** Slice 0은 규모가 작아 `App.tsx`의 **`useState`만** 쓴다(상태 라이브러리 미도입 — `docs/slice-0-decisions.md` 참조).
> 아래 3계층은 **화면·상태가 늘어날 때 적용할 규칙**이며, 필요해지는 Slice에서 해당 라이브러리를 도입한다.

| 계층 | 도구 | 용도 |
|---|---|---|
| 서버 유래 데이터 | **TanStack Query** | 서버에서 온 상태(캐싱·동기화) |
| 앱 전역 클라이언트 상태 | **Zustand** | 화면 간 공유되는, 서버 데이터가 **아닌** 상태 |
| 화면 로컬 세션 상태 | **useReducer** | 스캔 세션처럼 한 화면 안에서만 사는 상태 |

원칙: **화면 하나 안에서만 사는 상태에 Zustand를 쓰지 않는다.** (스캔 세션 = useReducer)

---

## 7. 스캔 화면 계약 (앱의 홈 — 핵심)

레거시 POS를 벤치마크한 영수증형 목록 화면. **입고/출고 세션 상태는 이 홈 화면 위의 상태**이지
별도 진입점이 아니다.

- **입력:** 스캐너용 항상-포커스 hidden input(keyboard-wedge) + 개발용 수동 EAN-13 입력칸.
  document-level `keydown`으로 스캐너 버스트(빠른 연타 + 끝에 Enter)를 사람 타이핑과 구분,
  버스트면 `preventDefault`로 이벤트를 삼켜 모달/다른 인풋 오염을 막는다.
- **세션 상태 = `lines: ScanLine[]` 배열** (단일 슬롯 덮어쓰기 **금지**). 스캔은 배열에 append,
  같은 상품 재스캔 시 해당 line 수량 +1. 수량은 라인별 +/− 로 조정.
- **입고/출고는 화면 전체 프레임 색으로 구분** (입고=초록 / 출고=빨강). 브랜드 포레스트그린과 충돌하지 않는 명도로.
  ※ +/− 는 라인 수량 조정용일 뿐, **입고/출고 구분을 대체할 수 없다** — 입고는 유통기한 있는 새 Lot 생성,
    출고는 FEFO 차감으로 **구조가 다른 연산**이기 때문.
- **미등록 상품 처리:** 목록의 `pending` 행이 source of truth. **모달은 저장소가 아니라 에디터.**
  - 입고 중 미등록 → 등록 모달(마스터만: 이름·카테고리·바코드). 저장 시 마스터 추가 + 세션 line으로 자동 append.
    유통기한/Lot 수량은 여기서 받지 않음(별도 리뷰 단계, 이 화면 범위 밖).
  - 모달 열려 있는 중 또 다른 미등록이 찍히면 → 이전 pending 행은 목록에 **그대로 남기고** 모달만 교체.
  - 출고 중 미등록 → 모달 열지 않고 toast 경고, 세션에 추가 안 함(유령 물량 차감 방지).
- **pending 행이 하나라도 있으면 commit 하드 블록**(개수 표시로 이유 안내). pending은 등록 완료(→ready) 또는 명시적 삭제로만 해소.
- **commit 자체는 stub.** 세션 누적 → 커밋 시 단일 트랜잭션으로 반영된다는 계약만 표현(실제 DB 반영은 뒤로).

> **현재 구현 범위(Slice 0):** 바코드 스캔 → 상품 조회 → 등록/미등록 2분기까지. 위 세션 배열(`lines`)·입고/출고 프레임·
> 등록 모달·commit은 **아직 미구현(계약 명세)** 이며 후속 Slice에서 채운다.

---

## 8. 디자인 시스템

색은 **역할**로 배정한다. 한 색이 두 역할을 겸하지 않는다.
핵심 원리: **같은 초록이라도 명도(value)를 크게 벌려 역할을 분리한다** (브랜드=어두움 / 콘텐츠=밝음).

| 역할 | 용도 | 값 | 규칙 |
|---|---|---|---|
| 브랜드/크롬 | 상단바, 활성 탭 밑줄 | **`#1F4436`** 포레스트그린 | 어둡게, 오직 하나. 밝은 초록 띠 금지 |
| 콘텐츠(카테고리) | 사장님이 고르는 카드 색 | 밝은 팔레트 (주황 `#D98A4E`, 초록 `#8CB56A` 등) | 밝은 구간 유지. **딥그린 제외** |
| 핵심 액션(CTA) | 저장·입고 확정 등 | 파랑 `#3182F6` | **초록 금지** — 브랜드/콘텐츠 초록에 파묻힘 |
| 시맨틱 | 상태 신호 | 재고부족=빨강 / 임박=앰버 / 성공=밝은 초록 | 브랜드 딥그린과 명확히 구분 |
| 중립 | 배경·텍스트·보더 | 그레이 스케일 | 기본 바탕 |

**입고=초록 / 출고=빨강** 색상 시스템은 위와 별개로 세션 프레임에 적용(§7).
상세 규칙은 `sherpa-pos-design` 스킬 문서(디자인 헌법) 참조.

### 안티패턴 (생성 금지)
- 실제로 안 쓰는 버튼으로 화면 채우기
- **카페/음식점 POS 요소 복제** (매장/포장 토글, S/M/L·컵 옵션, 분할결제 등)
- 브랜드 딥그린을 카드 색·성공 색으로 재사용 / CTA를 브랜드 초록과 같은 색으로
- 파괴적 동작을 확인 없이 즉시 실행
- 명세에 없는 걸 "POS니까 보통 있으니까"라며 임의 추가
- **Toss Place 결제 파이프라인 UI를 입고/출고 모델에 직접 매핑** — 목적이 다른 인터페이스이므로 레이아웃 패턴만 선택 차용

### 벤치마크 스크린샷 취급
참고 스크린샷은 **패턴 학습용이지 복제 대상이 아니다.**
추출할 것: 통합 상품 데이터 모델, 탭 구조, 목록 레이아웃 개념 / 무시할 것: 카페 전용 기능 전부.

---

## 9. 명령어

**모든 명령은 워크스페이스 루트 `hub/`(= git 루트)에서 실행한다.**

```bash
# --- hub/ 에서 ---
pnpm install                          # 의존성 설치
pnpm dev                              # 프론트(web) 개발 서버      = --filter @sherpa/web dev
pnpm build                            # 프론트 빌드 (tsc --noEmit && vite build)
pnpm typecheck                        # 타입 검사 (tsc --noEmit)   ← 현재 주 검증 수단
pnpm test                             # 워크스페이스 재귀 패스스루 (pnpm -r --if-present test) — 구현된 테스트 없음
pnpm dev:landing                      # 소개 페이지(landing) 개발 서버

# 개별 패키지 직접 지정도 가능
pnpm --filter @sherpa/web dev
pnpm --filter @sherpa/web build
pnpm --filter @sherpa/web typecheck
```

- **현재 실효 검증 수단은 `typecheck`(tsc --noEmit) + `build` + dev 렌더가 전부다.**
- **테스트는 실재하지 않는다.** 루트 `test`는 위 재귀 패스스루 placeholder일 뿐 — 테스트 파일 0개,
  테스트 러너(Vitest 등)·설정 파일 미도입. `lint` 스크립트·ESLint 설정도 없다.
- **테스트 인프라: Vitest + React Testing Library 도입 예정(계획, 아직 미설정).** 테스트가 실제로 필요해지는
  Slice에서 세팅하고, 세팅되면 각 패키지에 `test` 스크립트를 채운다.
- `server` 관련 명령은 백엔드 착수 후 추가(현재 server 미생성).

---

## 10. 컨벤션

- **커밋:** Conventional Commits — **type 접두사는 영문, 본문은 한국어**.
  예) `feat: 바코드 스캔 입력 및 상품 조회 분기 구현`
- **네이밍:** 변수·함수·타입·식별자는 **영어**(`productName`, `ScanLine`, `inbound`).
  도메인 용어의 한글 매핑은 코드가 아니라 **문서/용어집(§3)** 에 둔다.
- **문서:** 저장소 안 **`/docs` 마크다운**으로 관리(현재 `docs/slice-0-decisions.md`).
  기획·설계 결정을 커밋/PR 이력으로 가시화(자기 자신에게 PR을 날려 결정 변경을 리뷰 형태로 남길 수 있음).
  Wiki는 지양(커밋 이력·가시성에서 불리).
- **작업 방식:** **바로 코드부터 쏟지 말 것.** 먼저 (1)고려사항·결정 포인트를 나열하고,
  애매하면 질문한 뒤 진행한다. 완료 시 "검증된 것 / 다음으로 넘긴 것"을 한 문단 요약.

---

## 11. 포트폴리오 가드레일

- **프론트엔드가 주인공.** Express는 얇게 유지하고, 그 범위를 문서에 한 줄로 정당화할 수 있어야 한다.
- 백엔드에 시간을 많이 쓰면 정작 평가받는 프론트가 얇아진다 — 인증·복잡한 서버 로직으로 새지 말 것.
- 보여줄 강점: 제품 사고 / UX 설계 / 상태 관리·에러 처리·낙관적 업데이트.

---

## 12. 미결 결정사항 (여기서 확정하지 말고 논의 후 반영)

1. **서버 DB 접근:** `mysql2` 직접(가볍다, "얇은 백엔드" 포트폴리오 의도에 부합) **vs** Prisma(무겁지만 풀스택 타입 공유).
   → Slice 0에는 영향 없어 **의도적으로 미룸**.
2. **기본 화면 무게중심:** 스캔 우선 vs 그리드(상품 목록) 우선.
3. **재고 변경 커밋 방식:** 즉시 반영 vs 세션 누적 후 확인(현재 세션 누적 방향으로 기움).

> ↑ 이 3개는 결정되면 `/docs`의 ADR로 남기고, 확정된 부분만 이 파일 위쪽 해당 섹션으로 승격한다.

---

## 13. 개발 전략 (incremental slices)

기능을 한 번에 벌리지 않고 얇게 쌓는다. **진행 상태는 여기 적지 않는다**(git/PR로 확인).

- **Slice 0** — 바코드 스캔 → 상품 조회 → 등록/미등록 분기 (모든 흐름이 지나는 허브)
- **Slice 1** — 입고 경로 (상품 등록 + Lot 생성 + 유통기한 입력)
- **Slice 2** — 출고 경로 (FEFO 자동 차감) ← `packages/core`의 FEFO 로직이 실제로 등장하는 지점
- **Slice 3** — 재고·유통기한 현황 조회/리포트
