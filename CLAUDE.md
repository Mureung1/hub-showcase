# CLAUDE.md

## 새 세부과목 구현 전 최우선 원칙: 학습 효과 먼저, 재사용은 그다음

새로운 전공/세부과목 화면을 만들 때는 "기존 컴포넌트를 재사용할 수 있는가"를 먼저 묻지 말고, **"이 개념을 배우는 학생에게 실제로 어떤 시각화가 진짜 도움이 되는가"**를 항상 먼저 생각한다.

판단 순서 (6단계, **반드시 이 순서대로**, 항상 0단계부터 시작):

0. **핵심 기능(기능 A/B) 부합 여부 확인 — 항상 제일 먼저.** 이 프로젝트는 학습 개념을 두 갈래로만 다룬다: **기능 A(절차 자동 재생)** — 정해진 하나의 순서 자체가 곧 개념이라 그 순서를 그대로 지켜봐야 하는 절차형 개념(재생/한 단계/처음부터/속도 컨트롤로 구현). **기능 B(사용자 조작형 인터랙티브 시뮬레이터)** — 정해진 하나의 순서가 없고, 사용자가 스스로 무엇을 어떻게 살펴볼지 결정하는 행위 자체가 이해를 만드는 개념. **기능 B의 "조작"을 스택 Push/Pop 같은 "값 입력+연산"으로 좁게 해석하지 말 것** — 3D 구조를 원하는 각도로 직접 돌려보고 다른 예시와 비교하며 탐색하는 것도 똑같이 기능 B다(예: 분자 뷰어). 새 콘텐츠를 만들기 전에 **이게 A인지 B인지부터 명확히 정한다.** 이 넓은 정의로도 둘 중 어디에도 안 맞는다면:
   - 조용히 제3의 패턴을 만들어서 구현하지 않는다.
   - 왜 안 맞는지 명확히 정리하고, "A/B 중 하나에 맞게 콘텐츠 형태를 재설계할지" 아니면 "정말 새 패턴이 필요한지"를 **사용자와 먼저 논의**한 뒤에만 진행한다.
1. **핵심 학습 포인트 파악** — 이 과목의 모든 개념을 다 시각화하지 않는다. 텍스트 설명만으로는 이해하기 어렵고, 그림/모형으로 봐야 감이 오는 부분이 어디인지부터 정확히 짚는다.
2. **계획 생성 — 학과별 플래너 에이전트가 설계한다(화학과는 `chemistry-planner`, 컴퓨터공학과는 `cs-planner`).** 코드를 한 줄도 쓰기 전에, 내가(메인 Claude) 형식을 정해 계획을 짜지 않는다. 1단계에서 짚은 **학습 장애물만** 플래너에게 넘기고 — 어떤 렌더링 형식(구조식·모형·회전 뷰·그래프·2D/3D 등)도 미리 정하지 않은 채로 — 계획(무엇을 봐야 풀리는가 → 형태 도출 → 함정 체크리스트 → 데이터 소스 → 확신도)은 플래너가 생성한다. 사용자가 그 과목을 이미 잘 아는 경우(예: 컴퓨터공학)에도 이 단계는 생략하지 않는다 — 익숙한 분야일수록 본인은 못 보는 사소한 맹점(경계값, 안정성 여부, 시간복잡도 라벨링 등)을 독립된 플래너가 잡아낼 수 있다. 3렌즈(정확성·학습 적합성·시각적 전달력)와 "형태는 어려움에서 거꾸로 도출" 원칙은 각 플래너 정의(`.claude/agents/chemistry-planner.md`, `.claude/agents/cs-planner.md`)에 내장되어 있다.
   - **플래너가 "만들 가치 없음" 판정** → 1단계로 돌아가거나 사용자에게 판단을 넘긴다. 같은 계획이 2번 연속 걸러지면 사용자 판단.
   - **에스컬레이션**: 플래너가 **확신도 낮음**을 표시한 계획(화학: 입체화학 R/S, 열역학 부호 관례, 전기화학 극성 / CS: 동시성·경쟁 상태, 상각 복잡도 주장, 스케줄링 타이브레이크 규칙 등 함정 밀집 영역)만 예외적으로 같은 학과의 리뷰어 에이전트 **사전 검증을 추가로** 거친다. 평상시엔 사전 검증 라운드 없음 — 그만큼 워크플로우가 한 단계 짧다.
   - **5단계 사후 검증(학과별 리뷰어)은 반드시 유지한다** — 계획자(플래너)·구현자(나)와 **다른 눈**이 결과물을 보는 유일한 독립 검증 지점이다. 플래너가 아무리 좋아도 자기 계획의 맹점은 자기가 못 보므로, 이 그물은 플래너로 대체되지 않는다. (실제로 사전 게이트를 통과한 계획도 사후 검증에서 결함이 발견된 사례가 있음: 오비탈 마디 안 보임, 킬레이트 SMILES 조각남)
   - **적용 범위**: 신규 화면·신규 시각화 컴포넌트를 새로 만들 때만. 오타/문구/버그 수정처럼 개념을 새로 시각화하지 않는 작업은 제외.
   - **사용자가 매번 요청하지 않아도 항상 자동으로 실행한다** — 신규 화면(화학·컴공 모두) 착수 시 해당 학과 플래너 호출이 기본 동작이다.
   - **이 구조가 된 이유(연혁)**: ① VSEPR에서 기존 2D/3D 뷰어를 관성 재사용했다가 핵심 개념(전자쌍 반발)을 전혀 못 보여준 걸 뒤늦게 발견 → 구현 전 검증 게이트 신설. ② cis/trans에서 내가 "2D+3D 재사용"으로 형식을 정해 넘기자 검증자가 그 틀 안에서만 검증 → "형식 어휘 금지" 원칙 추가. ③ 실패의 뿌리가 "계획을 짜는 쪽의 프레이밍"임이 확인되어, 검증 페르소나+원칙을 내장한 **플래너 에이전트가 계획 생성 자체를 맡는 구조**로 개편(사전 검증 라운드를 플래너로 흡수, 사후 검증은 독립 유지). 처음엔 화학과에서만 시작했지만, 학과별로 **플래너+리뷰어 한 쌍**이 설계와 검증을 분담하는 것이 이 플랫폼의 표준 에이전트 구조라는 결론에 따라 4주차에 컴퓨터공학과에도 동일하게 확장(`cs-planner`/`cs-reviewer`). 앞으로 학과가 추가될 때도 같은 쌍 구조를 그대로 적용한다.
3. **시각화 형태 확정 — 플래너의 "권장 형태"를 구현 관점에서 확정한다.** 플래너 계획이 이미 형태를 도출해오므로, 여기서는 그 형태가 실제 코드베이스(기존 컴포넌트·패턴)와 어떻게 맞물릴지 확정한다. 플래너의 도출을 뒤집고 기존 화면 형식을 관성으로 대입하지 말 것 — 형태를 바꿔야 할 구현상 이유가 있으면 플래너에 되물어 계획을 갱신한다.
4. **데이터 소스 확인** — 필요한 데이터를 공개 데이터(오픈 API)에서 구할 수 있는지 확인한다.
   - **있으면** API로 연동한다.
   - **없거나 부정확하면**(예: 특정 분야 데이터를 아예 취급 안 하는 API) 왜 없는지 원인을 확인한 뒤, **새 기술 스택 도입처럼 지나치게 무리한 작업이 아닌 이상** 직접 계산/작성해서 구현한다. 재사용을 포기하는 쪽이 맞는 선택일 수 있다.
   - **이 확인은 매번 실제로 해야 한다.** "지난번 대상엔 없었으니 이번에도 없겠지"처럼 이전 사례의 결론을 확인 없이 재사용하지 않는다 — 대상이 다르면 결과가 정반대일 수 있다. 예: 배위 화합물(금속 착이온)은 PubChem에 3D 데이터가 없었지만, 카이랄 유기분자(예: D/L-알라닌)는 평범한 유기분자라 PubChem에 3D 데이터가 이미 있음 — 확인 없이 "또 직접 계산해야겠지"라고 넘겨짚었다가 뒤늦게 WebFetch로 실제 조회해서 정정한 사례가 있음.
5. **결과물 검증** — 구현이 끝났다고 완료가 아니다. "에러 없이 작동함"과 "실제로 정확하고 학생이 오해 없이 이해함"은 별개이므로, 검증 단계(해당 학과 리뷰어 서브에이전트 — `chemistry-reviewer` 또는 `cs-reviewer` — 로 전문가 관점 리뷰)를 거쳐야 완료로 본다.

"기존 도구로 어떻게든 구현되긴 함"과 "그 구현이 실제로 그 개념의 핵심을 정확히 가르치는가"는 별개다. 전자를 만족해도 후자를 만족 못 하면 미완성으로 취급한다.

## 사이드바 leaf 구성 원칙: 같은 챕터면 한 화면, 다른 챕터면 별도 화면

`navigation.ts`는 **학과 → 세부과목(그룹) → 개별화면(leaf)** 3단계 구조다(`NavDept.groups[].leaves[]`). 한 세부과목 안에 여러 개념이 있을 때, 그걸 한 화면(leaf)에 묶을지 별도 화면으로 나눌지는 **"기술적으로 같은 엔진을 쓰는가"가 아니라 "실제 대학 교재에서 같은 챕터로 다루는가"로 판단한다.**

- 같은 챕터 → 한 leaf(페이지)로 묶는다. 예: 정렬(버블/선택/퀵/힙 4종은 원래 같은 "정렬 알고리즘" 챕터에서 서로 비교하려고 만든 것 — 탭으로 묶어도 맞음), 배위 기하구조(헥사암민코발트/테트라암민구리 둘 다 "배위 기하구조" 챕터).
- 다른 챕터 → 별도 leaf로 나눈다. 예: 유기화학의 SN2+E2(치환·제거 챕터) / 마르코니코프(알켄 챕터) / 아실화(카르복시산 유도체 챕터)는 원래 한 페이지에 탭으로 있었지만 실제로는 3개 챕터라 3개 leaf로 분리함. 무기화학의 "배위 기하구조"와 "결정장 이론"도 겉보기엔 같은 화합물을 다루지만 실제 교재에선 별도 챕터(구조론 vs 결합이론)라 별도 leaf로 분리함.
- **엔진(코드 구현 방식)이 같다는 이유만으로 합치지 않는다** — 엔진 공유는 구현 편의일 뿐, chapter 판단과 무관하다. 반대로 엔진이 완전히 달라도(예: 물리화학의 곡선형 vs 단계재생형 vs 클릭형) 챕터가 다르면 어차피 별도 leaf가 맞다.
- 판단이 애매하면 실제 교재 목차를 확인한다(해당 학과 리뷰어 서브에이전트 — `chemistry-reviewer`/`cs-reviewer` — 가 이 판단에도 쓰일 수 있음).
- 그룹 안에 leaf가 1개뿐이어도 괜찮다(예: 일반화학, 물리화학의 각 챕터) — 나중에 그 그룹에 챕터가 추가되면 leaf가 늘어나는 구조로 설계되어 있다.

**실제 사례**: 무기화학 배위 구조를 이 기준으로 검토한 결과 — (1) 핵심은 "배위 기하구조(정팔면체/평면사각형)"라는 공간적 개념이라 판단, (2) 결합 관계(2D)와 실제 공간 배치(3D) 둘 다 필요하다고 판단, (3) PubChem에 배위 화합물의 3D 데이터가 없어(자동 3D 생성 파이프라인이 금속 화합물을 다루지 못함) 교과서 결합각으로 직접 좌표를 계산, (4) `chemistry-reviewer` 에이전트 검증으로 실제 버그 2개(관련 없는 화합물 예시의 수소 개수 오류, 카메라 각도로 기하구조가 구분 안 되던 문제)를 발견해 수정. 아래 "화학 구조 시각화" 섹션 참고.

**0단계가 새로 추가된 이유**: 무기화학 배위 화합물 페이지(그리고 원래 있던 일반화학 분자 뷰어)를 만들 때 "이게 기능 A인가 B인가"를 애초에 물은 적이 없었음. 뒤늦게 확인해보니 재생 컨트롤이 없어 기능 A는 아니었고, 처음엔 "조작해서 규칙을 발견하는 구조도 아니니 기능 B도 아닌 제3의 패턴 아닌가" 의심했으나, 기능 B의 "조작"을 스택처럼 값 입력에 국한하지 않고 "3D 구조를 직접 돌려보며 탐색하는 것"까지 넓혀서 다시 보니 **결국 기능 B였음**(코드 변경 불필요, 개념 분류만 늦게 확정됨). 1단계 이후 단계들(개념 판단 → 사전 검증 → 시각화 형태 → 데이터 → 사후 검증)을 아무리 꼼꼼히 밟아도, 애초에 "이게 두 핵심 기능 중 뭐에 해당하는가"를 먼저 안 물으면 이런 확인을 통째로 건너뛸 수 있음 — 그래서 항상 0단계부터 시작해야 함.

## 화학 구조 시각화 — "에러 없이 렌더링됨" ≠ "정상"

화학 구조를 화면에 그리는 기능(분자 뷰어, 무기화학 등)을 만들거나 건드릴 때는, **콘솔 에러가 없다는 것만으로 "정상 작동"이라고 판단하지 말 것**. 이 프로젝트는 학습 도구이므로, 기술적으로는 렌더링에 성공해도 화학적으로 의미 없거나 잘못된 그림이 나오면 그 자체로 결함이다. 시연/데모를 통과하는 것이 목적이 아니라, 실제로 화학적으로 올바른 구조가 나오는 것이 목적임을 항상 우선한다.

**구체적으로 확인된 함정**: PubChem PUG-REST API(`src/features/chemistry/lib/pubchem.ts`)가 주는 `CanonicalSMILES`는 배위결합(금속-리간드)을 일반 결합선으로 표현하지 않고 `.`(점, "서로 연결 안 된 별개 조각")으로 나열하는 경우가 흔하다. 예:
- 페로센(ferrocene): `C1=C[CH]C=C1.C1=C[CH]C=C1.[Fe]` — Cp 고리 2개와 Fe 원자가 전부 따로 노는 그림이 됨
- 헥사암민코발트(III): `N.N.N.N.N.N.[Co+3]` — 암모니아 6개와 Co 이온이 전부 따로 노는 그림이 됨

`smiles-drawer`(`Structure2DViewer.tsx`)는 이걸 그대로 받아 "서로 아무 연결선 없이 흩어진 조각들"로 그려버린다 — 에러는 안 나지만 학습 자료로는 무의미하거나 오히려 오해를 유발하는 그림이 된다.

**새 화합물 예시를 추가하기 전에 반드시**: 그 화합물의 PubChem SMILES를 미리 확인해서 `.`으로 조각나 있는지 확인할 것. 조각나 있다면:
- 리간드가 금속 하나에 1:1로 붙는 일반적인 배위 화합물(예: 암민 착물류)은 SMILES를 손으로 다시 써서 결합선을 명시하는 방식으로 고칠 수 있음
- 페로센처럼 금속이 고리 전체(면)와 결합하는 유기금속 화합물(hapticity 구조)은 원자-원자 결합선 나열 방식(SMILES/smiles-drawer) 자체로 표현이 불가능 — 이런 경우는 예시로 채택하지 말 것
- 3D 뷰(`Structure3DViewer.tsx`, SDF 기반)는 실제 좌표 데이터라 이 문제의 영향을 덜 받지만, 애초에 PubChem에 3D 컨포머 레코드가 없는 화합물도 많으므로 3D 표시 가능 여부도 별도로 확인 필요

**기존 `ChemistryPage.tsx`(일반화학 분자 뷰어)의 자유 텍스트 검색에도 이 위험이 잠재해 있음** — 기본값(`'CCO'`, 에탄올)은 안전하지만, 사용자가 금속 화합물을 직접 검색하면 같은 문제가 재현될 수 있음. 아직 실제로 고친 적은 없는 알려진 한계.

## Git 안전 수칙 — push 전 자동 비밀키 스캔

`git push`가 포함된 명령을 실행하기 직전, `.claude/hooks/pre-push-secret-scan.sh`가 `PreToolUse` 훅(`.claude/settings.json`)으로 자동 실행되어 origin과 갈라진 지점부터의 diff + 스테이징된 변경을 스캔한다. Google API 키(`AIza...`), 일반 API 키 포맷(`sk-...`), JWT(`eyJ...`), `SUPABASE`/`GEMINI` 관련 키 이름, `api_key: "..."` 형태의 하드코딩된 값 패턴에 걸리면 push를 차단하고 매치된 파일/라인을 보여준다.

- 이 저장소 전용(project-level) 훅이며, jq가 이 Git Bash 환경에 없어 JSON 파싱은 Node.js(`node -e`)로 처리함 — 새 환경에서 훅이 조용히 안 걸린다면 `jq`/`node` 존재 여부부터 확인할 것.
- 이 자동 스캔은 push 시점의 마지막 방어선이고, PR을 올리기 전에는 여전히 `/security-review`를 별도로 실행한다(강의 보안 지침) — 두 검증은 서로 대체하지 않고 "push마다 자동" + "PR마다 수동 심층 검토"로 이중 구조를 이룬다.
- 오탐(실제 비밀키가 아닌데 패턴에 걸림)이 나오면 해당 부분을 코드에서 제거/마스킹한 뒤 다시 push한다 — 훅 자체를 우회(`--no-verify` 등으로 임의 스킵)하지 않는다.

## 재생 컨트롤 기본 속도

Step[] 미리 계산 + player 훅 패턴(`useSortPlayer.ts`, `useTreePlayer.ts`, `useMechanismPlayer.ts` 등, 기능 A 계열)의 기본 `speed`(스텝 간 딜레이 ms)는 각 컨트롤 속도 슬라이더의 게이지 기준 **약 30% 지점**에 오도록 맞춘다.

슬라이더는 `value={OFFSET - speed}` 형태로 반전되어 있음(오른쪽으로 갈수록 빠르게 느껴지도록) — 새 player 훅을 추가할 때도 절대 ms 값이 아니라 "게이지 30%"를 기준으로 기본값을 정할 것.

계산 방법: `sliderTarget = min + 0.3 * (max - min)`을 슬라이더 `step`에 맞게 반올림한 뒤, `speed = OFFSET - sliderTarget`.

현재 적용된 값 (재생 버튼이 있는 화면만 — 아래 "재생 컨트롤 구성" 원칙에 따라 재생 버튼 자체가 없는 화면은 해당 없음):
- `src/features/sorting/hooks/useSortPlayer.ts`: `speed = 700` (슬라이더 min 50 / max 1000 / step 50 / offset 1050, `SortControls.tsx`)
- `src/features/tree/hooks/useTreePlayer.ts`: `speed = 900` (슬라이더 min 100 / max 1200 / step 100 / offset 1300, `TreeControls.tsx`)

## 재생 컨트롤 구성 — 화면마다 어울리는 방식인지 다시 판단할 것

기존 화면의 컨트롤 UI(버튼 종류·이름·개수)를 새 화면에 그대로 복붙하지 않는다. "Step[] 미리 계산 + player 훅" 패턴은 원래 정렬·트리처럼 **스텝 수가 많고 "그 순서로 진행되는 과정 자체를 지켜보는 것"이 학습 포인트인 화면**을 위해 설계됐다. 스텝 수가 적고(3~6개 안팎) 사용자가 각 단계를 자기 속도로 검토하거나 앞뒤로 왔다갔다 하며 비교하는 화면(예: 루이스 구조, 유기화학 반응 메커니즘)에 이 패턴을 그대로 쓰면 안 맞는 UI가 나온다.

**판단 기준**:
- **재생(자동 진행) 버튼이 필요한가**: "그 순서로 자동 재생되는 걸 지켜보는 것" 자체가 학습 포인트면 필요(정렬, 트리 삽입). 스텝 수가 적고 각 단계에서 멈춰 이해할 시간이 필요한 화면은 자동 재생이 방해가 되므로 넣지 않는다.
- **버튼 이름이 실제 동작과 정확히 일치하는가**: 방향을 명시 안 한 이름("한 단계")은 앞으로만 갈 수 있다는 오해를 주거나, 실제 세는 단위(반응 "단계"가 아니라 화살표 "프레임" 등)와 다른 이름이면 안 된다. 앞뒤 이동이 가능하면 "다음 단계"/"전 단계"처럼 방향을 명시한다.
- **컴포넌트를 공유하더라도 판단은 화면별로 새로 한다**: 여러 화면이 같은 컨트롤 컴포넌트를 재사용해도 괜찮지만(예: `MechanismControls`가 루이스 구조·유기화학 3종 공유), "이미 있는 컴포넌트니 그대로 쓴다"가 아니라 "이 화면에 정말 맞는 컨트롤 구성인가"를 매번 먼저 확인한 뒤에 재사용하거나 새로 만든다.

**이 원칙이 생긴 이유**: 루이스 구조·유기화학 반응 메커니즘 화면들이 `MechanismControls`를 통해 트리/정렬 계열의 "재생/한 단계/처음부터" 패턴을 그대로 물려받았는데, 실사용해보니 스텝 수가 3~6개뿐이라 재생 버튼이 불필요했고(사용자 표현으로 "컴퓨터공학과 페이지의 잔해"), "한 단계"라는 이름도 실제로는 화살표 단위 프레임이라 혼란을 줬다. 재생/속도 상태를 통째로 제거하고 "전 단계"/"다음 단계" 양방향 버튼으로 교체한 뒤에야 화면 성격에 맞는 컨트롤이 됐다(`useMechanismPlayer.ts`, `useLewisPlayer.ts`, `MechanismControls.tsx`).
