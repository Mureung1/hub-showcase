## 답냥이
매번 맥락을 설명하지 않아도, 대학생이 자주 겪는 꺼내기 어려운 메시지 상황에서 몇 번의 빠른 답변만으로 상황과 관계에 맞는 보낼 말을 받을 수 있는, 관계별 냥이 조력자가 함께 써주는 가이드형 대화 UI의 메시지 작성 도우미 서비스. 자유 대화형 챗봇이 아니라 S0 답장·먼저 보내기 → 관계 → S2 상황 카드를 냥이의 질문과 빠른 답변으로 안내한다. 상황 카드(시나리오당 6장)는 검수 기준에 따라 준비한 템플릿을 즉시 응답하고(현재 초안, T25 통과 후 검수 완료), 맞는 카드가 없을 때만 "다른 상황이냥?"으로 목적+텍스트 입력 후 AI 생성한다 (라우팅 규칙은 docs/SPEC.md 1·2장).


# 기본원칙
- 작업 규칙의 정본은 `AGENTS.md`이며 `CLAUDE.md`는 도구 호환용 동기화 미러다. 한쪽을 바꾸면 다른 쪽도 함께 바꾼다

# 절대 규칙 
- any 사용 금지
- MVP 범위 밖 기능 구현 금지
- 구조 변경 전에는 먼저 제안하고 승인받는다

# 금지 사항 
- 요청하지 않은 기능 추가 금지
- 전체 구조 임의 변경 금지
- 불필요한 리팩토링 금지
- 테스트 없이 완료 선언 금지
- 모르는 내용을 아는 척하지 말 것
- 결제 기능 구현 금지

# 작업 절차 스킬
- 기능 항목(T번호) 착수: `/task-start` — 의존성 확인 후 5요소(맥락·구체성·역할예시·단계화·검증) 계획을 먼저 세운다
- 시드 예시 작성·검수(T15/T16): `/seed`
- 과제 PR 생성: `/pr` — 회고 섹션은 사용자 본인이 작성
- 커밋: `/commit`
- 하네스 운영: `harness/README.md` — 기존 정본을 연결하고, 작업 계획·검증 보고서 템플릿을 사용한다. 위 스킬을 대체하지 않는다

# 커밋 규칙
- **커밋은 최소 단위로 한다**: 하나의 커밋 = 하나의 논리적 변경 (기능 1개 / 버그 수정 1개 / 문서 변경 1건). 파일 개수가 아니라 의도 단위로 나눈다
- 서로 다른 관심사를 한 커밋에 섞지 않는다 (기능+포맷팅 금지, 기능A+기능B 금지)
- 각 커밋은 그 시점에 독립적으로 빌드·린트를 통과해야 한다 (테스트 도입 후에는 테스트 포함)
- 일괄 스테이징 금지: `git add .` / `-A` / `--all`, `git commit -a` 사용 금지 — 커밋할 파일을 경로로 명시해 스테이징한다 (훅으로 차단됨)
- 메시지 형식: `type: 한국어 요약` (50자 이내). type은 feat / fix / docs / style / refactor / test / chore
- 커밋·푸시는 사용자가 요청할 때만 한다
- 커밋 절차는 `/commit` 스킬을 따른다

# 참고 문서
- 서비스 배경/요구사항: docs/PRD.md
- 디자인 토큰(색/타이포/간격): docs/DESIGN.md
- 화면/상태/컴포넌트 설계: docs/SCREENS.md
- MVP 범위 (In/Out 판단 기준): docs/MVP.md
- 엣지케이스 대응: docs/EDGE_CASES.md
- 3주 구현 계획: docs/PLAN.md
- 구현 스펙 (데이터/API 계약/프롬프트/시드/검수 기준): docs/SPEC.md
- 기능 개발 체크리스트 (의존성 순서 — 완료 체크의 단일 기준): docs/CHECKLIST.md
- UX 지침 (대기/선택/복사 패턴 조사·결정): docs/UX.md
- CI/CD 파이프라인 (CI 단계/브랜치·PR/Vercel 배포·롤백): docs/CICD.md
- 시드 예시 원본 24개 + 검수 현황: docs/SEEDS.md (블라인드 정렬 검수지: docs/SEEDS_REVIEW.md)
- 작업 진행 기록 (일자별 TODO/결과물/검토/남은 작업): docs/LOG.md — 각 문서 안의 개정 이력이 아니라 여기에 "언제 무엇을 왜 했는지"를 남긴다
- 제품 6축 점검(가치·지속 사용·AI·UX·기술·포트폴리오): docs/PRODUCT_REVIEW.md — 내부 진단이며 완료 상태는 CHECKLIST를 따른다
- 문헌 기반 제품·검수 근거(효과·관계·어려운 대화·구조화 UI·한국어 공손성): docs/RESEARCH_REVIEW.md — 문헌을 사용자 검증으로 표현하지 않는다
- 범용 AI 대비 가치 검증(문헌→무참여자 모델 벤치마크→T22 짧은 과업): docs/COMPETITIVE_VALIDATION.md — 실제 결과 전에는 우월성을 주장하지 않는다
