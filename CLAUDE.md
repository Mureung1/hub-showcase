# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Claude가 이 저장소에서 작업할 때 따르는 지침. **세부 내용은 `docs/`의 MD 파일을 정본으로 참고한다.**

## 참고 문서 (정본 = source of truth)

작업 전 관련 문서를 먼저 읽는다. 내용이 겹치면 아래 문서가 이 파일보다 우선한다.

| 문서 | 역할 | 언제 참고 |
|------|------|-----------|
| **`docs/plan.md`** | 서비스 기획 · 핵심 기능 2개 · 성공 지표 | 기능/스코프 판단 시 **1차 기준** |
| **`docs/checklist.md`** | 주차별 개발 태스크 · 완료 기준 | 구현 순서·범위 확인 시 |
| **`docs/DESIGN.md`** | 디자인 시스템(색·타이포·컴포넌트 스펙) | UI/화면 작업 시 |

> ⚠️ 위 3개 문서를 열어보지 않고 기획·구현·디자인 관련 결정을 내리지 말 것.

## 프로젝트 한 줄 정의

낮은 자존감 때문에 대외활동·공모전을 시작조차 못 하는 대학생이, 도전을 작은 **퀘스트**로 쪼개 완료 → 코인·경험치 보상 → **캐릭터 육성**이라는 간접 재미로 시작의 심리적 부담을 넘게 하는 **캐릭터 육성형 도전 앱 (One-Step)**.
자세한 배경·시나리오·기능은 → `docs/plan.md`.

## 구현 우선순위

1. **AI 핵심 기능 2개를 최우선 구현** (`docs/plan.md` 기준)
   - ① **AI 도전 분해 엔진** — 큰 목표 → 마이크로 퀘스트 + 난이도 자동 분류, 수정/삭제/재분해, JSON 스키마 강제 + 실패 시 템플릿 폴백.
   - ② **퀘스트 실행 루프** — 완료 → 난이도별 코인·XP 지급 → 캐릭터 성장.
2. **그 다음** 넓은 그림(캐릭터 진화·환생·상점·보관함 등)으로 확장. 지금은 인지만 하되, 2대 기능 설계가 이쪽으로 자연스럽게 확장되도록 방향을 맞춘다.
   - 충돌 시 우선순위: **plan.md의 2대 기능 > 넓은 그림**.

## 디자인 핵심 규칙 (상세는 `docs/DESIGN.md`)

색 역할을 반드시 분리한다:
- 🟢 **그린**(`#006e2f`) = 성장 · 완료 · 메인 액션
- 🔵 **블루**(`#0058be`) = **AI · 정보**
- 🟡 **노랑**(`#ef9900`) = **코인 · 보상 전용** (다른 곳에 쓰지 말 것)
- 폰트 **Sora**, 아이콘 **Material Symbols**, 12px 라운드, 흰 탭바 + 그린 활성 탭.

## 기술 스택

- **Front**: Flutter (iOS·Android). **Backend**: Firebase(Auth·Firestore·FCM·Storage). **디자인**: Figma + 도트아트. **협업**: GitHub, Notion.
- 예상 컬렉션: `users`(xp·level·coin·rebirth·equipped), `quests`(제목·난이도·마감·done), `achievements`, `inventory`. (상세는 필요 시 `docs/plan.md` 참고)

## 현재 산출물

- `prototype/` — HTML/CSS/JS 클릭 가능 프로토타입 (`docs/DESIGN.md` 디자인 반영). 실제 Flutter 구현 전 화면 흐름·보상 루프 검증용.

## 작업 규칙

- **git 관련 명령은 사용자가 직접 실행한다.** Claude는 명령어와 설명만 제공하고 실행하지 않는다.
- 원본 기획서 `.docx`는 로컬에만 있고 git에 추적하지 않는다. 필요한 맥락은 `docs/*.md`에 정리되어 있다.
