## 답답
매번 맥락을 설명하지 않아도, 대학생이 자주 겪는 꺼내기 어려운 메시지 상황(답장·먼저 보내기)에서 몇 번의 선택만으로 상황과 관계에 맞는 보낼 말을 받을 수 있는 메시지 작성 도우미 서비스.


# 기본원칙

# 절대 규칙 
- any 사용 금지
- MVP 범위 밖 기능 구현 금지
- 구조 변경 전에는 먼저 제안하고 승인받는다

# 금지 사항 
- 요청하지 않은 기능 추가 금지
- 전체 구조 임의 변경 금지
- 불필요한 리팩토링 금지
- 테스트 없이 완료 선언 금지
- 모르는 내용을 아는 척하지 말 것
- 결제 기능 구현 금지

# 작업 절차 스킬
- 기능 항목(T번호) 착수: `/task-start` — 의존성 확인 후 5요소(맥락·구체성·역할예시·단계화·검증) 계획을 먼저 세운다
- 시드 예시 작성·검수(T15/T16): `/seed`
- 과제 PR 생성: `/pr` — 회고 섹션은 사용자 본인이 작성
- 커밋: `/commit`

# 커밋 규칙
- **커밋은 최소 단위로 한다**: 하나의 커밋 = 하나의 논리적 변경 (기능 1개 / 버그 수정 1개 / 문서 변경 1건). 파일 개수가 아니라 의도 단위로 나눈다
- 서로 다른 관심사를 한 커밋에 섞지 않는다 (기능+포맷팅 금지, 기능A+기능B 금지)
- 각 커밋은 그 시점에 독립적으로 빌드·린트를 통과해야 한다 (테스트 도입 후에는 테스트 포함)
- 일괄 스테이징 금지: `git add .` / `-A` / `--all`, `git commit -a` 사용 금지 — 커밋할 파일을 경로로 명시해 스테이징한다 (훅으로 차단됨)
- 메시지 형식: `type: 한국어 요약` (50자 이내). type은 feat / fix / docs / style / refactor / test / chore
- 커밋·푸시는 사용자가 요청할 때만 한다
- 커밋 절차는 `/commit` 스킬을 따른다

# 참고 문서
- 서비스 배경/요구사항: docs/PRD.md
- 디자인 토큰(색/타이포/간격): docs/DESIGN.md
- 화면/상태/컴포넌트 설계: docs/SCREENS.md
- MVP 범위 (In/Out 판단 기준): docs/MVP.md
- 엣지케이스 대응: docs/EDGE_CASES.md
- 3주 구현 계획: docs/PLAN.md
- 구현 스펙 (데이터/API 계약/프롬프트/시드/검수 기준): docs/SPEC.md
- 기능 개발 체크리스트 (의존성 순서 — 완료 체크의 단일 기준): docs/CHECKLIST.md
- UX 지침 (대기/선택/복사 패턴 조사·결정): docs/UX.md
- CI/CD 파이프라인 (CI 단계/브랜치·PR/Vercel 배포·롤백): docs/CICD.md