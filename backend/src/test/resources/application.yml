spring:

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

  test:
    database:
      replace: none

seoul:
  api:
    key: test-key

kakao:
  api:
    key: test-key

tmap:
  api:
    key: test-key