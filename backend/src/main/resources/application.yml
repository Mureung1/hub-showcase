spring:
  application:
    name: devpulse
  datasource:
    url: ${DEVPULSE_DB_URL:jdbc:postgresql://localhost:5432/devpulse}
    username: ${DEVPULSE_DB_USERNAME:devpulse}
    password: ${DEVPULSE_DB_PASSWORD:devpulse}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
