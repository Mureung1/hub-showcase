# 디자인 시스템 — TimeTable 추천 서비스

`style.css` 기준으로 정리한 디자인 가이드입니다. 새 화면/컴포넌트를 만들 때 이 문서의 토큰과 패턴을 그대로 재사용하세요.

---

## 1. 컬러 토큰

`:root`에 정의된 CSS 변수를 기준으로 사용합니다. **하드코딩된 색상값을 새로 만들지 말고, 항상 아래 변수를 참조하세요.**

| 변수 | 값 | 용도 |
|---|---|---|
| `--bg` | `#eef0fb` | 전체 배경 (연보라 톤) |
| `--card` | `#ffffff` | 카드/화면 바탕 |
| `--ink` | `#241f3d` | 기본 텍스트 (진한 남보라) |
| `--sub` | `#6c6a85` | 보조 텍스트 (설명, 라벨, 시간) |
| `--line` | `#e7e6f3` | 구분선, 테두리 |
| `--primary1` | `#6d5bf0` | 주요 강조색 (버튼/활성 상태 그라데이션 시작) |
| `--primary2` | `#8f6df5` | 주요 강조색 (그라데이션 끝) |
| `--primary-ink` | `#4b3fc0` | 강조색 텍스트 (칩 안 글자, 뒤로가기 아이콘 등) |
| `--chip-bg` | `#f1effc` | 옅은 강조 배경 (뱃지, 안내 박스, 뒤로가기 버튼 배경) |

**규칙**
- 본문 텍스트는 `--ink`, 부가 설명/시간/교수명은 `--sub`
- 버튼·활성 탭·강조 텍스트는 `--primary1` 계열만 사용 (다른 색으로 강조하지 않기)
- 배경 위 배경은 항상 `--bg` → `--card` 두 단만 사용 (3단 이상 쌓지 않기)

### 과목 블록 컬러 팔레트 (파스텔, 구분용)
시간표 블록처럼 "같은 의미의 여러 항목을 구분"할 때만 아래 파스텔 팔레트를 순환 사용합니다. 상태/강조 의미로는 쓰지 않습니다.

```
#e7e2fb  #e2f3fb  #fbe9e2  #e2ecfb  #e2fbe8
#fff3d6  #fbe2ec  #f5e2fb  #e2fbf5  #fff0e2
```

### 의미가 있는 색 (상태 표시)
- 성공/일치 상태: 배경 `#e2fbe8` + 텍스트 `#1f8a4c` (예: `.credit-badge.exact`)
- 그 외 상태색(경고/에러 등)은 아직 정의되지 않음 — 새로 추가할 때 이 표와 톤을 맞출 것 (파스텔 배경 + 진한 텍스트 조합 유지)

---

## 2. 타이포그래피

- **폰트**: `"Pretendard", "Apple SD Gothic Neo", "Malgun Gothic", sans-serif`
- 폰트는 body에서 한 번만 지정, 컴포넌트별로 재지정하지 않음

| 용도 | 크기 | 굵기 | 색상 |
|---|---|---|---|
| 화면 제목 (topbar h1) | 16px | 700 | `--ink` |
| 섹션 타이틀 | 15px | 700 | `--ink` |
| 섹션 설명 | 12.5px | 400 | `--sub` |
| 필드 라벨 | 14px | 600 | `--ink` |
| 입력값(select 등) | 14px | 600 | `--sub` |
| 본문/카드 텍스트 | 13~14px | 400~600 | `--ink` |
| 캡션/보조 텍스트 | 11~11.5px | 400~500 | `--sub` |
| 하단 네비 라벨 | 11px | 600 | `--sub` (활성 시 `--primary1`) |

**규칙**: 라벨=`600`, 설명/보조=`400~500`, 강조 타이틀=`700~800`. 중간 굵기(500)는 보조 텍스트에만 쓰고 버튼/타이틀에는 쓰지 않습니다.

---

## 3. 여백 · 반경 · 그림자 스케일

임의의 숫자를 새로 만들지 말고 아래 스케일 중에서 고르세요.

**모서리 반경 (radius)**
| 값 | 용도 |
|---|---|
| `7~9px` | 작은 요소 (뒤로가기 버튼, 과목 블록) |
| `12px` | 안내 박스(note-box) |
| `14px` | 버튼, 결과 카드 |
| `16px` | field-list, reco-card 같은 큰 컨테이너 |
| `28px` | 최상위 화면(phone) 컨테이너 |
| `999px` | 뱃지/칩 (완전한 pill 형태) |

**여백 (padding/margin)**
- 화면 좌우 여백: `18px`
- 카드/리스트 내부 padding: `12~16px`
- 요소 간 세로 간격: `6px`(밀접) → `10~16px`(일반) → `20px+`(섹션 구분)

**그림자**
- 최상위 컨테이너: `0 20px 60px rgba(76,63,192,0.18), 0 2px 8px rgba(76,63,192,0.08)` (연보라 톤 그림자만 사용, 회색 그림자 쓰지 않기)
- 주요 버튼: `0 10px 24px rgba(109,91,240,0.35)`
- 그 외 요소는 그림자 없이 `1px solid var(--line)` 테두리로만 구분

---

## 4. 컴포넌트 패턴

### 버튼
- **Primary** (`primary-btn`): `--primary1→--primary2` 그라데이션 배경, 흰 글자, 굵게, 그림자 있음 → 화면당 핵심 액션 1개에만 사용
- **Ghost** (`ghost-btn`): 흰 배경 + `--line` 테두리 + `--primary-ink` 글자 → 보조 액션(뒤로가기 성격의 액션, 재시도 등)
- 버튼은 항상 `width:100%`, 세로 padding `13~15px`, radius `14px`

### 입력 필드 (`field`, `field-list`)
- 여러 필드는 하나의 `field-list` 안에 넣고, 필드 사이는 `--line` 구분선으로만 나눔 (박스를 각각 나누지 않기)
- 라벨은 왼쪽 정렬 + 아이콘, 값은 오른쪽 정렬

### 뱃지 / 칩 (`credit-badge`, `note-box`)
- 기본 상태: `--chip-bg` 배경 + `--primary-ink` 텍스트
- 완료/일치 같은 긍정 상태만 초록 계열(`#e2fbe8` / `#1f8a4c`)로 예외 처리

### 카드 (`reco-card`, `field-list`)
- `1px solid var(--line)` 테두리 + radius `16px`, 그림자 없음
- 카드 안에 카드를 중첩하지 않기 (예: reco-card 안의 grid-wrap은 배경/테두리 없는 순수 레이아웃 영역)

### 하단 네비게이션 (`bottombar`)
- 아이콘(18px) + 라벨(11px, 600) 세로 배치
- 비활성: `--sub`, 활성: `--primary1` (배경색 변화 없이 텍스트/아이콘 색만 전환)

### 시간표 그리드 (`grid-wrap`, `course-block`)
- 요일 헤더/시간 라벨은 `--sub`, 셀 구분선은 `--line`
- 과목 블록은 파스텔 팔레트 배경 + `--ink` 텍스트(굵게) + `--sub` 텍스트(교수/학점, 보통 굵기)
- 블록 radius는 `7px`로 다른 카드보다 작게 (그리드 안의 작은 단위이므로)

---

## 5. 레이아웃 원칙

- 전체 화면은 `max-width:430px`짜리 "폰 프레임" 안에 그린다 (데스크톱에서도 모바일 폭 유지)
- 화면 전환은 라우팅 없이 같은 페이지 안에서 섹션을 `hidden` 클래스로 보이기/숨기기
- 상단바(topbar)는 항상 고정 높이, 제목은 중앙 정렬, 뒤로가기는 좌측 (없을 땐 `visibility:hidden`으로 자리만 유지 — 다른 요소가 밀리지 않도록)

---

## 6. 새 화면/기능 만들 때 체크리스트

- [ ] 색은 `:root` 변수만 사용했는가? (새 hex 코드를 직접 쓰지 않았는가)
- [ ] radius가 위 스케일(7~28px, 999px) 중 하나인가?
- [ ] Primary 버튼이 화면당 1개인가?
- [ ] 카드/리스트 테두리가 `--line`이고, 불필요한 그림자를 추가하지 않았는가?
- [ ] 폰트 크기가 위 타이포그래피 표에 있는 값 중 하나인가?
- [ ] 모바일 폭(430px) 기준으로 잘리거나 넘치는 부분이 없는가?