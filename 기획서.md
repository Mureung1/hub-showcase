# 이력 기반 AI 채용 매칭 서비스 — MVP 기획서

> 문서 버전: v0.1 (MVP)
> 작성 목적: 개인 개발 기준 정의 + 외부 평가용
> 대상 플랫폼: 웹 (반응형)
> 🔗 **프로토타입: [핵심 흐름 클릭형 데모 열기 (HTML)](./프로토타입_핵심흐름.html)** — 순수 HTML/CSS, 6단계 시나리오를 눌러볼 수 있음 (자세한 내용 11장)

---

## 1. 서비스 개요

### 1.1 한 줄 정의
내 이력(자격증·포트폴리오·경력·회사)을 입력하면, **회사가 요구하는 조건과의 적합도를 자동 계산해 순위대로 채용 포지션을 추천**하고, 선택한 포지션에 맞춰 **AI가 이력서·자기소개서를 생성**해 바로 지원까지 연결하는 서비스.

### 1.2 해결하려는 문제
- 구직자는 수많은 채용공고 중 **내 이력에 실제로 맞는 곳이 어디인지** 판단하기 어렵다.
- 포지션마다 이력서·자기소개서를 **매번 새로 맞춰 쓰는 것**이 번거롭고 시간이 많이 든다.
- "내 이력이 이 포지션에 왜 부족한지 / 어떻게 어필해야 하는지"에 대한 **구체적 가이드가 없다.**

### 1.3 핵심 가치
| 구분 | 기존 채용 서비스 | 본 서비스 |
|------|----------------|-----------|
| 포지션 탐색 | 사용자가 직접 검색·필터 | 이력 기반 자동 적합도 순위 |
| 지원 준비 | 사용자가 직접 작성 | AI가 포지션 맞춤 생성 |
| 방향성 | 없음 | 내 이력으로 어떻게 맞출지 제시 |

### 1.4 타깃 사용자 (MVP)
- 이직·구직 중인 실무자 (경력·자격증·포트폴리오가 있는 사용자에 최적화)
- 초기에는 **특정 직군 1~2개**(예: 개발/디자인)로 좁혀 검증하는 것을 권장 (매칭 정확도 확보 목적)

---

## 2. 핵심 사용자 시나리오

1. 사용자가 **회원가입/로그인**을 한다.
2. 가입 시 **자격증, 포트폴리오, 경력, 재직 회사** 등 이력 정보를 입력한다.
3. 입력한 이력을 기반으로, **회사 요구조건 대비 적합도(가중치)가 높은 순서**로 채용 포지션 목록이 자동 정렬되어 나열된다.
4. 특정 포지션을 선택하면, **현재 가진 이력으로 이 포지션에 최대한 맞추려면 어떻게 써야 하는지** 방향을 제시한다.
5. AI 기반으로 **포지션 맞춤 이력서·자기소개서를 생성**하고, 사용자가 확인 후 **[지원하기]**를 누른다.
6. **해당 채용 사이트로 이동**하여 지원을 완료한다.

---

## 3. MVP 기능 정의

### 3.1 기능 범위 (In Scope)

| # | 기능 | 설명 | 우선순위 |
|---|------|------|:---:|
| F1 | 회원가입 / 로그인 | 이메일 기반 인증 (소셜 로그인은 후순위) | ★★★ |
| F2 | 이력 입력·관리 | 자격증·포트폴리오·경력·회사 입력/수정 | ★★★ |
| F3 | 포지션 수집 | 외부 채용정보 크롤링/API/제휴로 수집·저장 | ★★★ |
| F4 | 적합도 매칭·랭킹 | 요구조건 대비 이력 가중치 계산 → 자동 정렬 | ★★★ |
| F5 | 포지션 상세 + 방향 제시 | 적합도 근거 + 내 이력으로 맞추는 방향 안내 | ★★★ |
| F6 | AI 문서 생성 | 포지션 맞춤 이력서·자기소개서 생성 | ★★★ |
| F7 | 외부 지원 연결 | [지원하기] → 원본 채용 페이지로 이동 | ★★☆ |

### 3.2 범위 제외 (Out of Scope — 향후 확장)
- 기업 회원 / 채용공고 직접 등록 (양면 플랫폼화)
- 플랫폼 내 직접 지원·제출 (현재는 외부 사이트 연결까지만)
- 지원 현황 관리(ATS), 합격률 통계
- 소셜 로그인, 결제/구독, 알림(신규 포지션 알람)
- 모바일 앱

---

## 4. 매칭 로직 (핵심 차별점)

### 4.1 개념
포지션마다 **요구조건(requirement)** 이 있고, 각 요구조건에는 **중요도(가중치)** 가 있다. 사용자의 이력이 각 요구조건을 얼마나 충족하는지를 계산해 합산한다.

```
포지션 적합도 점수 = Σ ( 요구조건 가중치 × 내 이력의 해당 조건 충족도 )
```

- **요구조건 가중치**: 공고에서 "필수 vs 우대", 언급 빈도·강조 정도로 추정
- **충족도(0~1)**: 내 이력(자격증/경력/포트폴리오/회사)이 그 조건에 얼마나 부합하는지

### 4.2 예시
| 요구조건 | 가중치 | 내 이력 충족도 | 기여 점수 |
|----------|:---:|:---:|:---:|
| Python 3년+ | 0.4 | 1.0 (경력 4년) | 0.40 |
| AWS 경험 | 0.3 | 0.5 (부분 경험) | 0.15 |
| 정보처리기사 | 0.2 | 1.0 (보유) | 0.20 |
| 팀 리딩 경험 | 0.1 | 0.0 (없음) | 0.00 |
| **합계** | 1.0 | — | **0.75** |

→ 이 점수를 포지션별로 계산해 **높은 순으로 정렬**한다.

### 4.3 MVP 구현 방향 (권장)
공고 텍스트에서 요구조건을 정형화하는 것이 가장 어려운 부분이라, 초기에는 규칙 기반보다 **LLM/임베딩 기반**을 권장한다.
- **요구조건 추출**: LLM으로 공고 텍스트 → 조건·필수여부 구조화
- **충족도 계산**: 사용자 이력과 요구조건 간 의미 유사도(임베딩) + LLM 판단
- 정확한 수치보다 **상대 순위**가 목적이므로, MVP는 "대략 맞는 순서"를 목표로 한다.

> ⚠️ 매칭 품질은 이 서비스의 성패를 좌우하는 핵심이라, 초기에 직군을 좁혀 정확도를 검증한 뒤 확장하는 것을 강하게 권장.

---

## 5. 화면 구성 (IA)

| 화면 | 주요 요소 |
|------|-----------|
| 로그인/회원가입 | 이메일 인증, 이력 초기 입력 유도 |
| 이력 관리 | 자격증·포트폴리오·경력·회사 입력 폼, 수정 |
| 포지션 목록(홈) | 적합도 순 정렬 리스트, 점수/매칭 배지 |
| 포지션 상세 | 공고 요약, 적합도 근거, 이력으로 맞추는 방향 제시 |
| AI 문서 생성 | 이력서·자기소개서 생성 결과, 편집, [지원하기] |

### 화면 흐름
```
[회원가입/로그인] → [이력 입력] → [포지션 목록(자동정렬)]
        → [포지션 상세 + 방향 제시] → [AI 문서 생성] → [외부 채용사이트 이동]
```

---

## 6. 데이터 모델 (개략)

- **User**: id, email, password_hash, 생성일
- **Credential(이력)**: user_id, 유형(자격증/포트폴리오/경력/회사), 내용, 기간 등
- **JobPosting(포지션)**: id, 회사명, 직무, 원본 URL, 공고 원문, 수집일
- **JobRequirement(요구조건)**: posting_id, 조건 텍스트, 필수여부, 추정 가중치
- **MatchScore**: user_id, posting_id, 점수, 계산일
- **GeneratedDoc**: user_id, posting_id, 유형(이력서/자소서), 내용, 생성일

---

## 7. 기술 스택 제안 (웹 MVP)

| 영역 | 후보 | 비고 |
|------|------|------|
| 프론트엔드 | React / Next.js | 반응형 웹 |
| 백엔드 | Node.js 또는 Python(FastAPI) | AI 연동 고려 시 Python도 유리 |
| DB | PostgreSQL | 관계형 + 임베딩 확장(pgvector) 가능 |
| AI | LLM API (문서 생성·요구조건 추출) | 비용·토큰 관리 필요 |
| 수집 | 크롤러 또는 공식/제휴 API | 6항 리스크 참고 |

> 스택은 본인 익숙도에 맞춰 조정. MVP는 **가장 빠르게 검증 가능한 조합**이 우선.

---

## 8. 주요 리스크 & 고려사항

1. **크롤링 법적/약관 리스크 (가장 중요)**
   - 주요 채용 사이트는 이용약관에서 크롤링을 금지하는 경우가 많다. 무단 크롤링은 약관 위반·법적 분쟁 소지가 있음.
   - **대안**: 공식 채용 API(제공 시), 제휴, 또는 공공 데이터(예: 고용노동부 워크넷 API) 우선 검토. MVP 검증 단계에서는 데이터 출처를 제한적으로 확보하는 방식 권장.

2. **개인정보 보호**
   - 자격증·경력·회사 등은 민감한 개인정보. 저장·암호화·동의 절차 필요. 외부 공개 시 특히 유의.

3. **매칭 정확도**
   - 공고 요구조건 정형화가 어려워 초기 정확도가 낮을 수 있음 → 직군 축소 + 사용자 피드백으로 개선.

4. **AI 생성 품질·비용**
   - 생성물 품질 편차, 사실과 다른 내용(할루시네이션) 가능 → 사용자 검수·편집 단계 필수. 토큰 비용 관리 필요.

5. **외부 지원 연결의 한계**
   - 지원은 외부 사이트에서 이루어지므로, 실제 지원 완료 여부를 서비스가 추적하기 어려움 (MVP에서는 감수).

---

## 9. 향후 로드맵 (참고)

| 단계 | 내용 |
|------|------|
| MVP | 위 F1~F7, 특정 직군 검증 |
| 확장 1 | 신규 포지션 알림, 지원 이력 관리 |
| 확장 2 | 기업 회원·직접 지원(양면화) |
| 수익화 후보 | 프리미엄 구독(무제한 생성), 지원당 과금, 향후 기업 과금 등 — *현재 미정* |

---

## 10. 열린 이슈 (결정 필요)

- 채용 데이터 확보 방식 확정 (크롤링 vs 공식 API vs 제휴) — **법적 검토 선행 권장**
- MVP 검증 대상 직군 선정
- AI 생성물의 사용자 편집 범위 (전체 편집 vs 부분 수정)
- 회원가입 시 이력 입력을 필수로 할지, 이후 단계로 미룰지

---

## 11. 프로토타입

핵심 기능이 실제로 어떻게 동작하는지 **클릭형 프로토타입**으로 확인할 수 있다. 순수 HTML/CSS로만 제작(자바스크립트 없음)했으며, 카드·버튼을 눌러 6단계 시나리오를 그대로 따라갈 수 있다.

**▶ [프로토타입 열기 — 프로토타입_핵심흐름.html](./프로토타입_핵심흐름.html)**

### 담긴 화면 (기획서 대응)
| 프로토타입 화면 | 대응 기능/장 |
|-----------------|--------------|
| 로그인 | F1 (3.1) |
| 이력 입력 (자격증·포트폴리오·경력·회사) | F2 (3.1) |
| **포지션 목록 — 적합도 순 정렬** | F4 매칭·랭킹 (4장) |
| 포지션 상세 — 요구조건별 가중치×충족도 + 방향 제시 | F5 (3.1, 4.2) |
| AI 이력서·자기소개서 생성 | F6 (3.1) |
| 외부 채용사이트 연결 | F7 (3.1) |

### 확인 포인트
- **적합도 미터**: 포지션마다 점수(예: 92%)와 색상 미터로 순위를 시각화 → 4장 매칭 로직이 화면에서 어떻게 보이는지 검증용.
- **방향 제시**: 상세 화면에서 요구조건별 충족도 분해 + "내 이력으로 맞추는 방향" 3가지를 제시.
- 데이터는 백엔드 개발자 페르소나(김서준)로 채워진 **예시 목업**이며, 실제 매칭/생성 로직은 미연결.

> 프로토타입은 흐름·화면 검증용이라 실제 크롤링·매칭·AI 생성은 동작하지 않는다. 화면 전환은 브라우저에서 링크(`:target`)로 이동한다.

---

## 12. 피드백 & 변경 이력

동료 리뷰를 받아 기획서를 개선하기 위한 기록 공간.

### 받은 피드백
| 날짜 | 리뷰어 | 피드백 요약 | 반영 여부 |
|------|--------|-------------|:---:|
| | | | ☐ |
| | | | ☐ |

### 버전 이력
| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| v0.1 | — | 최초 작성 (MVP 기능·매칭 로직·프로토타입 연결) |
