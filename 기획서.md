# 기획서 — AI 미스터리 쇼퍼

소상공인을 위한 경쟁업체 리뷰 분석 AI 에이전트

| 항목 | 내용 |
|---|---|
| 프로젝트 기간 | 1개월 (네이버 AI 부트캠프) |
| 산출물 | 웹 서비스 (React + FastAPI), 배포 환경 포함 |
| 대상 사용자 | 요식업·카페 등 오프라인 매장을 운영하는 소상공인 |

---

## 1. 문제 정의

### 이 서비스가 푸는 문제

소상공인은 경쟁 매장의 강점과 약점을 파악해 자기 매장의 전략을 세우고 싶어 하지만, 다음 제약이 있다.

1. **시간 부족**: 매장 운영만으로 하루가 소진되어, 경쟁 매장의 리뷰를 수십~수백 건씩 읽을 시간이 없다.
2. **분석 역량 부족**: 리뷰를 읽더라도 "그래서 우리 가게는 무엇을 해야 하는가"라는 실행 전략으로 바꾸는 일은 별도의 분석 역량이 필요하다.
3. **비정형 데이터**: 리뷰는 정제되지 않은 텍스트여서 수작업으로는 통계화(불만 유형별 비중 등)가 사실상 불가능하다.

### 해결 방식

AI 에이전트가 경쟁 매장의 리뷰 텍스트를 분석하여 (1) 긍정/부정 비율, (2) 언급 키워드 순위라는 정형 데이터로 변환하고, 이를 근거로 (3) 우리 매장이 취할 전략을 담은 컨설팅 리포트를 자동 생성한다. 사장님은 가게 이름 하나만 입력하면 된다.

### 데이터 수집 방식 (최종 확정)

네이버 플레이스 리뷰는 공식 API가 없고 자동화 수집은 이용약관 위반이다. 따라서:

- **1단계(MVP, 완료)**: 가상 리뷰 데이터로 분석 파이프라인 검증 ✅
- **2단계(1주차, 완료)**: 네이버 블로그 검색 API(공식 제공) 기반 후기 텍스트 수집으로 교체 ✅

수집 계층을 `collector.py`로 분리하여 데이터 소스 교체 비용 최소화.

---

## 2. 사용자 시나리오

### 주 시나리오: 경쟁 매장 분석

1. 디저트 카페를 운영하는 사장 A는 근처에 새로 생긴 카페 '보노베리'로 손님이 몰리는 것을 체감한다.
2. A는 서비스에 접속해 검색창에 "보노베리"를 입력하고 분석 시작 버튼을 누른다.
3. 시스템이 리뷰를 수집·분석하는 동안 진행 상태가 표시된다.
4. 화면에 결과가 나타난다.
   - 감성 분석: 긍정 60% / 부정 40% 도넛 게이지
   - 키워드 순위: 1위 타르트, 2위 웨이팅, 3위 주차 ...
   - AI 전략 리포트: "경쟁사는 주차 불만 비중이 높으므로, 주차 편의성을 강조한 안내와 이벤트가 유효하다" 형태의 실행 제안
   - 근거가 된 리뷰 원문 목록 (감성 라벨·키워드 태그 포함)
5. A는 리포트를 참고해 매장 안내문과 이벤트를 조정한다.

### 부 시나리오: 재조회

1. A가 일주일 뒤 같은 매장을 다시 조회한다.
2. 이미 분석된 데이터가 DB에 있으므로, 시스템은 LLM을 재호출하지 않고 저장된 결과를 즉시 반환한다. (비용 절감·응답 속도 개선)

### 예외 시나리오

- 백엔드 서버 미기동/네트워크 오류: 화면에 오류 원인과 확인 방법을 안내한다. (구현 완료)
- 존재하지 않는 매장 ID로 리포트 조회: 404와 안내 메시지를 반환한다. (구현 완료)
- Gemini API 할당량 초과: 429 에러 반환, 내일 리셋 대기 또는 결제 연결 안내 (구현 완료)

---

## 3. 핵심 기능

우선순위 순. P0은 MVP 필수, P1은 고도화.

### P0-1. 리뷰 감성 분석 (구현 완료) ✅

- 리뷰 한 건을 입력받아 긍정/부정 중 하나로 분류
- LangChain `with_structured_output` + Pydantic 스키마로 출력 형식을 강제하여, 값이 두 가지 외로 나오지 않도록 보장

### P0-2. 키워드 추출 (구현 완료) ✅

- 리뷰에서 핵심 키워드(맛, 가격, 불친절, 웨이팅 등)를 리스트로 추출
- 매장 단위로 집계하여 언급 횟수 기준 순위 산출

### P0-3. AI 전략 리포트 생성 (구현 완료) ✅

- 집계된 통계(긍부정 건수, 키워드 순위, 리뷰 요약)를 입력으로 LLM이 컨설팅 리포트 생성
- 관점 고정: "벤치마킹할 강점"과 "공략 가능한 약점" 중심, 5문장 이내

### P0-4. 분석 결과 저장 (구현 완료) ✅

- 리뷰 원문(비정형)과 분석 결과(정형)를 reviews 테이블에 함께 저장
- competitors / reviews 2개 테이블, 외래키 연결

### P0-5. 대시보드 (구현 완료) ✅

- 가게 이름 입력 → 분석 요청 → 결과 시각화 단일 화면
- 도넛 게이지(긍정 비율), 키워드 가로 바 차트, 리뷰 카드 목록
- 응답에 `source`(naver_blog/fallback_dummy/database)와 `cached` 플래그 포함

### P1-1. 실데이터 수집 계층 (구현 완료) ✅

- 네이버 블로그 검색 API로 "매장명 + 후기" 검색 결과 텍스트 수집
- 수집 함수를 분리해 데이터 소스 교체가 코드 한 곳 수정으로 가능하도록 설계
- 환경변수 기반 인증 (NAVER_CLIENT_ID, NAVER_CLIENT_SECRET)

### P1-2. 분석 결과 캐싱 (구현 완료, 부분) ✅

- 분석 이력이 있는 매장 재조회 시 리뷰 분석 LLM 호출 생략, DB 조회로 응답
- 목적: API 비용 절감, 응답 시간 단축
- 현황: 리뷰 분석 캐싱은 완료, 컨설팅 리포트는 아직 재생성 중 (2주차에서 완성)

### P1-3. 배포 및 운영 (예정)

- Docker 컨테이너화(백엔드/프론트엔드/DB), docker-compose 구성 (2주차)
- GitHub Actions로 빌드·배포 자동화 (3주차)
- 네이버 클라우드 플랫폼 배포, 헬스체크 엔드포인트 제공 (3주차)

---

## 4. 기술 스택 및 아키텍처

| 계층 | 기술 | 채택 이유 |
|---|---|---|
| Frontend | React (Vite), Tailwind CSS v4 | 프론트-백 분리 구조 학습, 포트폴리오 활용도 |
| Backend | FastAPI | 비동기 지원, 자동 API 문서, Pydantic 통합 |
| AI | LangChain + Google Gemini (gemini-2.5-flash) | 구조화된 출력 지원, 무료 사용 가능 범위 내 개발 |
| Data Collection | Naver Blog Search API (공식) | 약관 준수, 안정성 보장 |
| DB | SQLite(MVP) → MySQL(배포) | 개발 속도 우선, ORM으로 교체 비용 최소화 |
| 환경 변수 관리 | python-dotenv (.env) | 매번 터미널 입력 제거, 보안 (GitHub 제외) |

```
사용자 ─ React 대시보드 ─ POST /api/analyze ─ FastAPI
                                               ├─ collector.py (네이버 블로그 검색)
                                               ├─ LangChain + Gemini 2.5 Flash (분석·리포트)
                                               └─ SQLAlchemy ─ SQLite/MySQL (캐싱·이력)
```

---

## 5. 성공 기준

### MVP(1주차 완료 여부)

- [x] 가게 이름 입력부터 리포트 표시까지 오류 없이 동작
- [x] LLM 출력이 항상 지정된 JSON 스키마를 따름
- [x] 분석 결과가 DB에 저장되고 통계 API로 재조회 가능
- [x] 네이버 블로그 검색 API로 실제 텍스트 수집 가능 (환경변수 설정 후)
- [x] 모델 안정화 (gemini-2.5-flash 확정)
- [x] 환경변수 자동 로드 (.env)

### 고도화(2~4주차 목표)

- [ ] 컨설팅 리포트도 DB에 저장 (완전 캐싱)
- [ ] Docker 컨테이너화 및 docker-compose
- [ ] CI/CD 파이프라인 (GitHub Actions)
- [ ] 클라우드 배포 (네이버 클라우드 플랫폼)

---

## 6. 1주차 진행 현황

### 완료 항목

| 항목 | 상태 | 비고 |
|---|---|---|
| 수집 계층 분리 (collector.py) | ✅ | `fetch_reviews()` 함수 분리 |
| 네이버 블로그 검색 API 연동 | ✅ | 환경변수: NAVER_CLIENT_ID/SECRET |
| Gemini 모델 확정 | ✅ | gemini-1.5 → gemini-2.5-flash |
| 캐싱 구현 | ✅ | 동일 매장 재조회 시 LLM 호출 생략 |
| 응답 메타데이터 | ✅ | source, cached 플래그 추가 |
| 환경변수 자동 로드 | ✅ | python-dotenv (.env) |
| .gitignore 설정 | ✅ | .env는 GitHub에 올리지 않음 |
| 헬스체크 엔드포인트 | ✅ | GET /health |

### 현재 제약사항

- Gemini API 무료 등급 일일 한도 (20회/day) — 내일 리셋 예정
- 네이버 API 통합 테스트 대기 중 (할당량 리셋 후)

---

## 7. 다음 단계

### 즉시 (1주차 마무리)

1. 할당량 리셋 후 새 매장 이름으로 전체 파이프라인 검증
2. `source: naver_blog` 확인
3. GitHub에 푸시 (최종 확정)

### 2주차 (컨테이너화)

1. Dockerfile 작성 (FastAPI, React, MySQL)
2. docker-compose.yml 구성
3. 로컬 docker-compose up 테스트

### 3주차 (배포)

1. GitHub Actions 워크플로우 작성
2. 네이버 클라우드 플랫폼 가입 및 배포
3. 자동 배포 파이프라인 구축

### 4주차 (운영 및 마무리)

1. 컨설팅 리포트 완전 캐싱 (DB 저장)
2. 간단한 모니터링 대시보드
3. 최종 문서화 및 아키텍처 다이어그램