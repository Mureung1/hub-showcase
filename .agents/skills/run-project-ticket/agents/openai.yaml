interface:
  display_name: "Run Project Ticket"
  short_description: "Route ticket analysis and implementation by role"
  default_prompt: "Use $run-project-ticket to route the requested ticket through analyst analysis, user choice, and implementer work when analyst is named."

policy:
  allow_implicit_invocation: true
