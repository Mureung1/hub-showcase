interface:
  display_name: "Run Project Ticket"
  short_description: "Analyze and implement repository tickets with context"
  default_prompt: "Use $run-project-ticket to analyze or implement the requested project ticket."

policy:
  allow_implicit_invocation: true
