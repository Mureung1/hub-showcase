interface:
  display_name: "AY-PLE E2E Smoke"
  short_description: "Run the live AY-PLE browser smoke suite"
  default_prompt: "Use $ay-ple-e2e-smoke to run the default live AY-PLE browser smoke against the disposable fixture."
policy:
  allow_implicit_invocation: false
