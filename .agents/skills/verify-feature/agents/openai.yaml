interface:
  display_name: "기능 검증"
  short_description: "요구사항부터 화면·API·DB까지 기능 동작을 검증합니다"
  default_prompt: "Use $verify-feature to verify this feature against approved requirements, report evidence and defects, and recommend fixes without modifying code."

policy:
  allow_implicit_invocation: true
