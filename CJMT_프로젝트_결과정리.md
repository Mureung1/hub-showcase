# CJMT 프로젝트 결과 정리

> **CJMT** = Chungnam Just-eat Meal Tracker
> 음식 사진 한 장 → 내 몸 기준 부족 영양소와 보충 메뉴를 알려주는 모바일 웹 서비스
> (Naver Connect / Boostcamp AI Agent Challenge)

---

## 1. 프로젝트 개요

- **한 줄 정의**: "음식 사진 한 장 → 내 몸 기준 부족 영양소와 보충 메뉴를 알려주는 웹"
- **문제**: 대학 식당·주변 식당은 영양 정보가 없고, 매번 칼로리 계산은 귀찮으며, 내 몸에 지금 뭐가 부족한지 판단하기 어렵다.
- **차별점**: 단순 "사진 영양분석"을 넘어, **내 신체 기준으로 지금 무엇을 더 먹어야 하는지** 진단·추천한다.
- **형태**: 모바일 우선 웹앱(향후 안드로이드 웹앱 패키징 계획), 토스(Toss) 스타일 UI.

---

## 2. 최종 기술 스택

| 영역 | 사용 기술 |
|------|-----------|
| 프론트엔드 | React 18 + Vite (SPA), react-router-dom v6 |
| 스타일 | 인라인/CSS + theme.js 디자인 토큰 (토스 스타일), lucide-react 아이콘 |
| 상태관리 | React 내장 훅(useState/useContext), localStorage |
| AI(비전) | OpenRouter 경유 `google/gemini-3-flash-preview` (이미지+텍스트 멀티모달) |
| 지도/장소 | 카카오맵 (REST 로컬 API + JavaScript SDK) |
| 영양 DB | 식약처 공공데이터포털 — 음식 표준데이터 + 가공식품 표준데이터 |
| 백엔드 | Node.js + Express 프록시 (API 키 보호용) |
| 저장 | 브라우저 localStorage (사용자·식단·기록·상태) |

### 백엔드 프록시 엔드포인트 (키 보호)
- `POST /api/gemini` — OpenRouter(Gemini) 호출
- `POST /api/places` — 카카오 키워드 장소 검색
- `POST /api/fooddb` — 식약처 음식/가공식품 영양 DB (source 파라미터로 분기)

> 모든 외부 API 키는 프록시 서버 환경변수로만 사용하며 프론트에 노출되지 않음.
> (예외: 카카오 JavaScript SDK 키는 정상적으로 프론트 노출 + 도메인 화이트리스트로 보호)

---

## 3. 화면·기능 구조

### 하단 탭 내비게이션 (5개)
홈(촬영) · 식단 · 달력 · 지도 · MY

### 화면별 기능

**F1·F2 — 로그인 / 신체정보 입력 (`/login`, `/profile`)**
- ID/PW 회원가입·로그인 (MVP 한정 평문 저장, 실서비스 시 해시 필요)
- 나이·성별·키·몸무게·활동량·기저질환 입력
- Mifflin-St Jeor BMR → TDEE → 하루 권장 영양소 자동 계산·저장

**홈 / F3 — 사진 촬영·영양분석 (`/analyze`)**
- 인사 + 카메라/업로드 카드 + 메뉴명·브랜드 입력(선택)
- 이미지 리사이즈(긴 변 1024px) 후 분석
- 분석 직후 1회만 홈에 상세 카드 표시, 결과는 식단 탭에 저장

**식단 탭 (`/meals`)**
- 상단: "오늘의 영양 섭취량" 그래프(권장량 대비 6개 영양소 막대, 부족=주황/충족=그린/나트륨 초과=경고)
- 오늘 먹은 음식 카드 목록(요약 + 나트륨 포함)
- 각 카드 "자세한 영양" 버튼 → 상세 카드 펼침/접힘(아코디언)
- 삭제 버튼 → 그래프 실시간 갱신

**F4 — 영양 진단·보충 추천 (`/result`)**
- 하루 목표 달성률(원형 게이지)
- 부족 영양소 카드(상위 3개 강조)
- 보충 추천 메뉴 2~3개 + 각 메뉴의 예상 섭취량("단백질 18g 섭취 가능")

**지도 탭 (`/map`)**
- 진입 시 카카오맵 + 내 위치 핀 자동 표시
- "내 주변에서 찾기" → 부족 영양소 기반 식당 유형 추천(3km) + 추천 근거 표시
- 식당 목록 카드 + 카카오맵 링크 + 지도 마커

**달력 탭 (`/calendar`)**
- 영양 상태 3단계 색상 표시(좋음=그린/보통=주황/위험=빨강)
  - 판정 기준: 6개 영양소 중 5개↑ 충족=좋음, 3개↓=보통, 1개↓=위험
- 기록 없는 과거 날짜는 상태 수동 선택(좋음/보통/나쁨)
- 미래 날짜 선택 시 "선택할 수 없습니다" 안내, 미래 달 이동 차단

---

## 4. 핵심: 영양 분석 하이브리드 알고리즘

CJMT의 정확도 핵심. **AI는 "무슨 음식·얼마나"만 판단하고, 실제 영양 수치는 공공 DB를 우선 사용**한다.

### 5단 폴백 구조
1. AI(비전)가 음식 식별: `dbSearchName`(표준명), `fallbackSearchName`(넓은 범주명), `displayName`, `estimatedGrams`, `estimatedNutrients`(폴백용 참고값) 반환
2. **음식 DB** 검색(dbSearchName) → 매칭 시 사용
3. 실패 시 **가공식품 DB** 검색(dbSearchName)
4. 실패 시 **음식 DB** 재검색(fallbackSearchName)
5. 실패 시 **가공식품 DB** 재검색(fallbackSearchName)
6. 전부 실패 시 **AI 추정치**(estimatedNutrients) 사용

### 양 환산
- DB 값은 기준량(보통 100g)당 → `scaleNutrients(dbNutrients, baseValue, grams)`로 실제 섭취량 환산
- 가공식품은 DB의 `servSize`(1회 섭취참고량)를 우선 사용(더 정확), 없으면 AI `estimatedGrams`
- DB에 없는 개별 항목(예: 식이섬유)만 `fillMissingNutrients`로 AI 값 보완

### 출처 배지 (신뢰도 투명화)
- **식약처DB**(블루) / **식약처DB(가공)**(블루) / **공식 영양표**(그린, 프랜차이즈) / **추정**(회색)

### 검증 예시
- 떡볶이: 음식DB 100g(144kcal) → 250g 환산 → 360kcal
- 초코파이: 가공식품DB servSize 70g 기준 347.9kcal, 식이섬유만 AI 보완

---

## 5. 데이터 모델 (요약)

**NutrientSet**: `{ calories, protein, carbs, fat, fiber, sodium }`

**MealAnalysis**: `{ items: [{ name, brand?, nutrients, source }], total }`

**저장 키(localStorage)**
- `cjmt:users` / `cjmt:currentUserId` — 계정·신체정보·권장량
- `cjmt:meals:<userId>:<YYYY-MM-DD>` — 날짜별 식단 목록
- `cjmt:daystatus:<userId>:<date>` — 달력 수동 영양상태

---

## 6. 개발 과정에서 해결한 주요 이슈

- **API 비용/한도**: 초기 Gemini 무료 티어(하루 20회) → 503/429 빈발 → **OpenRouter 유료 크레딧 + gemini-3-flash-preview**로 전환해 안정화. 프록시에 429/503 지수 백오프(1s→2s→4s) 적용.
- **Gemini 인증 방식 변경**: 신형 키(AQ.Ab...)는 `?key=` 대신 `x-goog-api-key` 헤더 필요.
- **영양 과대추정**: 프롬프트 정교화(한국 표준 1인분·기준값 앵커) → 이후 **식약처 공공 DB 연동**으로 근본 해결.
- **카카오 도메인 등록**: "앱 대표 도메인"(실존 도메인만)과 "플랫폼 키 → JS SDK 도메인"(localhost 가능) 구분 필요.
- **카카오 좌표**: x=경도, y=위도 순서 — 전 구간 일치 검증 완료.
- **환경 이슈**: OneDrive/한글·공백 경로, npm 실행 위치(cjmt 하위 폴더) 등.

---

## 7. 코드 리뷰에서 확인된 상태

- **보안**: API 키 하드코딩 없음, `.env` git 미추적, 히스토리 깨끗 ✅
- **좌표 매핑**: 카카오 x/y 전 구간 정확 ✅
- **영양 공식**: Mifflin-St Jeor 정확 ✅
- **무한 호출 방지**: useRef 가드로 자동 호출 1회 제한 ✅

### 배포 전 처리 필요(🔴)
- 정적 서빙/프록시 구조(현재 dev 전용 프록시) — 배포 방식 결정 필요
- 네트워크 타임아웃(AbortController) 부재
- 프록시 무인증(오픈 프록시 → 쿼터 남용 위험, 프로덕션 한정)

### 알려진 한계(의도/MVP 범위)
- 비밀번호 평문 저장(데모 한정)
- 사진 기반 "양(그램)" 추정의 본질적 오차(영양소 자체는 DB 기반으로 신뢰 가능)

---

## 8. 남은 로드맵

- **배포** (Render / Vercel / 단일서버 중 택1) + 🔴 3종 처리
- (선택) 국가표준식품성분표 등 DB 추가 확장
- (선택) 활동계수·todayMeal 복원 등 정리 항목
- (장기) 안드로이드 웹앱 패키징, 서버 인증·비밀번호 해시
