# 잇다(Eat-da) 데모 영상 스크립트 (5분)

---

## 🎬 인트로 (0:00 ~ 0:30)

**[화면: 잇다 로고 + 서비스 메인 화면]**

> "안녕하세요, 잇다(Eat-da) 팀입니다."
>
> "혹시 대학교에서 밥 먹을 사람을 구하느라 단톡방에 '밥 먹을 사람?'이라고 올려본 적 있으신가요?"
>
> "답이 없거나, 시간이 안 맞거나, 매번 같은 메뉴만 돌게 되죠."
>
> "잇다는 이 문제를 해결합니다. 대학생들의 시간표를 기반으로 함께 밥 먹을 사람을 매칭해주고, AI가 메뉴까지 추천해주는 서비스입니다."

---

## 📋 서비스 소개 (0:30 ~ 1:30)

**[화면: 서비스 흐름도 또는 아키텍처 다이어그램]**

> "잇다의 핵심 기능은 세 가지입니다."
>
> "첫째, **시간표 기반 매칭**입니다. Google Calendar 또는 에브리타임에서 시간표를 가져와서, 서로 빈 시간이 겹치는 학생들끼리 자동으로 매칭합니다."
>
> "둘째, **밥약 방 만들기**입니다. 원하는 시간대와 인원수를 설정해서 방을 만들면, 조건이 맞는 학생들이 참여할 수 있습니다."
>
> "셋째, **AI 메뉴 추천**입니다. Google Gemini AI가 학교 주변 맛집을 카테고리별로 추천하고, 네이버 검색 API로 실제 식당 정보를 보여줍니다."
>
> **[화면: 기술 스택 한 줄 요약]**
>
> "기술 스택은 React + TypeScript 프론트엔드, Express.js 백엔드, MongoDB Atlas 데이터베이스, 그리고 Firebase Authentication으로 구성되어 있습니다. 프론트엔드는 Vercel, 백엔드는 Render에 배포했습니다."

---

## 🖥️ 라이브 데모 (1:30 ~ 3:30)

### 회원가입 & 로그인 (1:30 ~ 2:00)

**[화면: 실제 앱 조작]**

> "먼저 회원가입 과정을 보여드리겠습니다."
>
> "이메일을 입력하면 인증 코드가 발송됩니다. nodemailer로 Gmail SMTP를 통해 실제 이메일이 전송되고, 4자리 코드를 입력하면 인증이 완료됩니다."
>
> "또는 Google 계정으로 바로 로그인할 수도 있습니다. Firebase Authentication의 Google OAuth를 사용하고, 이때 캘린더 읽기 권한도 함께 받아옵니다."

### 시간표 연동 (2:00 ~ 2:30)

**[화면: 시간표 동기화 과정]**

> "온보딩에서 대학교, 학과, 학년, MBTI를 입력하면 프로필이 만들어집니다."
>
> "시간표 연동은 세 가지 방법을 지원합니다."
>
> "Google Calendar 연동은 OAuth 토큰으로 이번 주 일정을 자동으로 가져옵니다."
>
> "에브리타임 연동은 공유 URL에서 identifier를 추출하고, 에브리타임 API에 요청해서 XML로 받은 시간표를 파싱합니다. 예를 들어 starttime 값에 5를 곱하면 실제 시작 시간(분)이 됩니다."
>
> "iCal 파일 업로드도 가능합니다."

### 밥약 방 & AI 추천 (2:30 ~ 3:30)

**[화면: 방 만들기 → AI 추천 → 매칭]**

> "빈 시간대를 선택하고 인원수를 설정해서 밥약 방을 만들어 보겠습니다."
>
> "방이 만들어지면 참여 코드가 생성되고, 다른 학생은 이 코드로 방에 참여할 수 있습니다."
>
> "AI 메뉴 추천을 눌러보겠습니다. Google Gemini API에 음식 카테고리와 시간대를 전달하면, AI가 맞춤 추천 메시지와 함께 식당 목록을 보여줍니다."
>
> "식당 검색은 네이버 Search API를 사용해서, 실제 학교 주변 맛집을 평점, 거리 정보와 함께 제공합니다."

---

## 🔧 기술적 문제 해결 (3:30 ~ 4:15)

**[화면: 코드 하이라이트 또는 before/after 비교]**

> "개발 과정에서 만난 주요 기술 문제와 해결 과정을 공유하겠습니다."
>
> **문제 1: 서버 무응답으로 UI 멈춤**
>
> "Render 무료 서버는 15분 비활동 시 sleep 모드에 들어가는데, cold start에 30~60초가 걸립니다. fetch에 타임아웃이 없어서 UI가 '발송 중...' 상태로 무한 대기하는 문제가 있었습니다."
>
> "AbortController 기반 fetchWithTimeout 헬퍼를 만들어서 모든 API 호출 11곳에 20초 타임아웃을 적용했습니다. 타임아웃 시 사용자에게 '서버 응답 지연' 메시지를 보여줍니다."
>
> **문제 2: 이메일 인증 SMTP 무한 대기**
>
> "nodemailer로 Gmail SMTP 연결 시 타임아웃 설정이 없어서, 서버가 영원히 응답을 기다리는 버그가 있었습니다. connectionTimeout, socketTimeout을 추가해서 해결했습니다."
>
> **문제 3: AI 추천에 'undefined' 노출**
>
> "음식 카테고리나 시간대를 선택하지 않고 추천을 요청하면, 템플릿 리터럴에서 undefined가 문자열로 출력되는 문제가 있었습니다. 파라미터 기본값 처리로 해결했습니다."

---

## 🤖 AI Agent 활용 (4:15 ~ 4:50)

**[화면: Claude Code 작업 화면 캡처 또는 PR 화면]**

> "이번 프로젝트에서 AI Agent를 적극적으로 활용했습니다."
>
> "Claude Code를 사용해서 배포된 앱의 전체 기능을 점검하고, 18개 기능을 동작/미동작/Mock/UX이슈로 분류했습니다."
>
> "버그 발견부터 수정, 테스트, PR 작성까지의 전 과정을 Agent와 함께 진행했습니다. 예를 들어 fetchWithTimeout 패턴은 Agent가 제안한 AbortController 기반 구현을 함께 검토하고 적용한 결과입니다."
>
> "발표 자료 제작, 아키텍처 시각화, 일일 회고 작성 등 개발 외 작업에서도 Agent를 활용해서 생산성을 높였습니다."
>
> "중요한 점은, Agent의 제안을 그대로 쓰는 것이 아니라 직접 실행하고 테스트하면서 수정하는 과정을 거쳤다는 것입니다."

---

## 🎯 마무리 (4:50 ~ 5:00)

**[화면: 잇다 메인 화면 + 팀 정보]**

> "잇다는 대학생의 일상적인 문제인 '밥 같이 먹을 사람 찾기'를 시간표 기반 매칭과 AI 추천으로 해결하는 서비스입니다."
>
> "감사합니다."

---

## 📝 영상 제작 참고사항

- **총 길이**: 약 5분
- **필수 포함**: 서비스 설명, 라이브 데모, 기술적 특징, 문제 해결 과정, Agent 활용
- **자막/음성**: 자막 또는 음성 녹음 중 택 1 (자막 추천 — 편집이 더 쉬움)
- **업로드**: YouTube (비공개 가능) 또는 Google Drive 공유 링크
- **마감**: 수요일 밤 10시까지
