# 작업 계획 (4주)

솔로 개발 기준. 템플릿 재사용을 전제로 주차를 구성함.

## 템플릿 정리

| 템플릿 | 특징 | 대상 |
|---|---|---|
| 기능 A. 코드-시각화 동기화 재생 | 정해진 시나리오가 자동으로 진행 (재생 · 한 단계 · 처음부터 · 속도) | 정렬, 트리 |
| 기능 B. 사용자 조작형 인터랙티브 시뮬레이터 | 사용자가 값을 입력하고 직접 조작 | 스택, 큐, 덱 |

컴퓨터공학과·화학과는 이번 프로젝트에서 전부 구현 확정. 그 외 학과(수학과·물리학과·생명과학과 등)는 사이드바에 노출만 하고 클릭 비활성화 상태로 유지 (별도 안내 화면 없음).

## 1주차 — 완료: 레이아웃 + 컴퓨터공학과 + 화학과

- [x] 전체 레이아웃, 사이드바 네비게이션
- [x] 정렬 엔진(기능 A) — 버블·선택·퀵·힙 4종
- [x] 스택·큐·덱(기능 B) — Push/Pop, Enqueue/Dequeue, 앞/뒤 삽입삭제, 오버플로우/언더플로우 처리
- [x] 트리(기능 A) — 이진·완전·편향 3종(실제 BST 삽입 기반), 삽입 비교 과정 시각화(대기 칩+대기열), 삽입/순회 독립 재생 분리
- [x] 분자 뷰어(일반화학) — SMILES/이름 → 2D/3D 구조
- [x] 유기화학 반응 메커니즘 애니메이션 — SN2/마르코니코프/Fischer 에스테르화/E2

## 2주차 — 핵심 목표: 디자인 재정립 + Supabase 연동 + 로그인 + DB + AI Q&A

- [ ] 전체 앱 디자인 재정립 (트리 페이지·홈페이지·사이드바 완료, 정렬/화학과/스택-큐-덱은 진행 중)
- [x] 로그인/회원가입 화면 UI 껍데기 (`LoginPage`/`AuthForm`/`TextField`) — 아이디+비밀번호만 사용(이메일 불필요), 실제 인증은 아래 Supabase 연동 후 연결
- **(갱신) 아키텍처를 Supabase 중심으로 변경**: 원래 Express가 로그인/DB/Gemini 프록시를 전부 담당하는 구조였는데, Supabase(Postgres DB + Auth + 자동 API를 묶은 BaaS)를 쓰기로 하면서 스코프가 줄어듦 — 로그인/회원가입/Q&A 기록 저장·조회는 **프론트엔드에서 Supabase 클라이언트 SDK로 직접** 처리(커스텀 백엔드 코드 불필요). Express(또는 서버리스 함수)는 **Gemini API 키를 숨겨서 대신 호출해주는 프록시 하나만** 남음 — API 키는 어차피 서버에서만 다뤄야 해서 이 부분만은 여전히 백엔드가 필요함.
  - [ ] Supabase 프로젝트 생성 + React 클라이언트 연동(`@supabase/supabase-js`)
  - [x] **데이터 모델 확정** (아래 표) — 사용자 인증은 Supabase Auth 기본 테이블(`auth.users`)을 그대로 쓰되, 이메일 없이 "아이디"만으로 가입/로그인하기 위해 `아이디@내부도메인` 형태의 내부용 가짜 이메일을 만들어 Auth에 등록하고, 실제 아이디는 별도 `profiles` 테이블에 저장(비밀번호 해싱·세션 관리는 Supabase Auth가 그대로 담당 — 직접 구현 안 함)

    **`profiles`** (아이디 ↔ Supabase Auth 사용자 매핑)
    | 컬럼 | 타입 | 필수 | 설명 |
    |---|---|---|---|
    | `id` | uuid | 필수 (PK, `auth.users.id` 참조) | Supabase Auth가 발급한 사용자 ID와 동일 |
    | `username` | text | 필수 (unique) | 사용자가 로그인 시 입력하는 아이디 |
    | `created_at` | timestamp | 필수 (기본값: 현재 시각) | 가입 시각 |

    **`qna_logs`** (Q&A 대화 기록)
    | 컬럼 | 타입 | 필수 | 설명 |
    |---|---|---|---|
    | `qna_id` | uuid | 필수 (PK, 자동 생성) | 채팅 기록 하나를 구분하는 고유 식별자 (테이블 자기 자신의 `id`인 `profiles.id`와 달리, 매번 새로 생성되는 값이라 이름으로 구분되게 `qna_id`로 명명) |
    | `user_id` | uuid | 필수 (`auth.users.id` 참조) | 누가 질문했는지 |
    | `question` | text | 필수 | 사용자가 입력한 질문 |
    | `answer` | text | 필수 | AI가 준 답변 |
    | `screen_context` | text | 필수 | 질문 당시 화면 상태(예: 현재 step description) |
    | `created_at` | timestamp | 필수 (기본값: 현재 시각) | 질문 시각 |
    - [ ] (중요도 낮음) `feedback` 컬럼으로 답변 좋아요/싫어요 남기기 — 활용 계획(수동 검토·발표용 지표) 대비 스코프 부담이 있어 시간 남을 때만
  - [ ] 회원가입 — `아이디` 입력 → `아이디@내부도메인` 가짜 이메일로 `supabase.auth.signUp()` 호출 → 성공 시 반환된 user id로 `profiles`에 `{ id, username }` insert. `AuthForm`의 제출 콜백(현재 콘솔 로그만 남기는 부분)을 이 호출로 교체
  - [ ] 로그인 — 입력한 `아이디`로 같은 규칙의 가짜 이메일을 재구성해 `supabase.auth.signInWithPassword()` 호출
  - [ ] Q&A 기록 저장/조회 — Supabase 클라이언트로 `qna_logs` 테이블에 직접 insert/select
  - [ ] Gemini 프록시만 담당하는 최소 백엔드: `POST /api/ask` — 화면 상태(현재 step description)를 컨텍스트로 받아 Gemini 호출 후 응답 반환
- [ ] 화면: AI Q&A 챗 위젯(`ChapterAssistant`) — 화면 상태 인지 컨텍스트 전달, API 장애 시에도 나머지 기능 100% 독립 동작
  - [ ] (중요도 낮음) 답변마다 👍/👎 버튼 — 위 feedback 컬럼과 세트
- [ ] PR 제출 전 `/security-review` 스킬로 자체 보안 점검 (Supabase anon key/Gemini secret 키 노출 여부 등)

## 3주차 — 남은 화학과 + 확장 콘텐츠

- [ ] 무기화학 결정/배위 구조 (기존 분자 뷰어 재사용)
- [ ] (시간 남을 시) 물리화학 후보 1개 — 반응속도 그래프 또는 르샤틀리에 평형이동
- [ ] 사이드바에 타 학과 항목 추가 (수학과·물리학과·생명과학과 등, 클릭 비활성)
- [ ] 반응형 대응
- [ ] 접근성 보완 (색상 외 이중 표시, 키보드 조작)

## 4주차 — 마무리

- [ ] 전체 버그 수정 및 성능 점검
- [ ] README / 기획서 최종 업데이트
- [ ] 배포 및 최종 점검 (도전 목표)
- [ ] 헤더 검색 기능
