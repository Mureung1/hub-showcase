# 작업 계획 (4주)

솔로 개발 기준. 템플릿 재사용을 전제로 주차를 구성함.

## 템플릿 정리

| 템플릿 | 특징 | 대상 |
|---|---|---|
| 기능 A. 코드-시각화 동기화 재생 | 정해진 시나리오가 자동으로 진행 (재생 · 한 단계 · 처음부터 · 속도) | 정렬, 트리 |
| 기능 B. 사용자 조작형 인터랙티브 시뮬레이터 | 사용자가 값을 입력하고 직접 조작 | 스택, 큐, 덱 |

컴퓨터공학과·화학과는 이번 프로젝트에서 전부 구현 확정. 그 외 학과(수학과·물리학과·생명과학과 등)는 사이드바에 노출만 하고 클릭 비활성화 상태로 유지 (별도 안내 화면 없음).

## 1주차 — 완료: 레이아웃 + 컴퓨터공학과 + 화학과

- [x] 전체 레이아웃, 사이드바 네비게이션
- [x] 정렬 엔진(기능 A) — 버블·선택·퀵·힙 4종
- [x] 스택·큐·덱(기능 B) — Push/Pop, Enqueue/Dequeue, 앞/뒤 삽입삭제, 오버플로우/언더플로우 처리
- [x] 트리(기능 A) — 이진·완전·편향 3종(실제 BST 삽입 기반), 삽입 비교 과정 시각화(대기 칩+대기열), 삽입/순회 독립 재생 분리
- [x] 분자 뷰어(일반화학) — SMILES/이름 → 2D/3D 구조
- [x] 유기화학 반응 메커니즘 애니메이션 — SN2/마르코니코프/Fischer 에스테르화/E2

## 2주차 — 핵심 목표: 디자인 재정립 + Supabase 연동 + 로그인 + DB + AI Q&A

- [ ] 전체 앱 디자인 재정립 (트리 페이지·홈페이지·사이드바 완료, 정렬/화학과/스택-큐-덱은 진행 중)
- [x] 로그인/회원가입 화면 UI 껍데기 (`LoginPage`/`AuthForm`/`TextField`) — 아이디+비밀번호만 사용(이메일 불필요), 실제 인증은 아래 Supabase 연동 후 연결
- **(갱신) 아키텍처를 Supabase 중심으로 변경**: 원래 Express가 로그인/DB/Gemini 프록시를 전부 담당하는 구조였는데, Supabase(Postgres DB + Auth + 자동 API를 묶은 BaaS)를 쓰기로 하면서 스코프가 줄어듦 — 로그인/회원가입/Q&A 기록 저장·조회는 **프론트엔드에서 Supabase 클라이언트 SDK로 직접** 처리(커스텀 백엔드 코드 불필요). Express(또는 서버리스 함수)는 **Gemini API 키를 숨겨서 대신 호출해주는 프록시 하나만** 남음 — API 키는 어차피 서버에서만 다뤄야 해서 이 부분만은 여전히 백엔드가 필요함.
  - [x] Supabase 프로젝트 생성 + React 클라이언트 연동(`@supabase/supabase-js`)
  - [x] **데이터 모델 확정** (아래 표) — 사용자 인증은 Supabase Auth 기본 테이블(`auth.users`)을 그대로 쓰되, 이메일 없이 "아이디"만으로 가입/로그인하기 위해 `아이디@내부도메인` 형태의 내부용 가짜 이메일을 만들어 Auth에 등록하고, 실제 아이디는 별도 `profiles` 테이블에 저장(비밀번호 해싱·세션 관리는 Supabase Auth가 그대로 담당 — 직접 구현 안 함)

    **`profiles`** (아이디 ↔ Supabase Auth 사용자 매핑)
    | 컬럼 | 타입 | 필수 | 설명 |
    |---|---|---|---|
    | `id` | uuid | 필수 (PK, `auth.users.id` 참조) | Supabase Auth가 발급한 사용자 ID와 동일 |
    | `username` | text | 필수 (unique) | 사용자가 로그인 시 입력하는 아이디 |
    | `created_at` | timestamp | 필수 (기본값: 현재 시각) | 가입 시각 |

    **`qna_logs`** (Q&A 대화 기록)
    | 컬럼 | 타입 | 필수 | 설명 |
    |---|---|---|---|
    | `qna_id` | uuid | 필수 (PK, 자동 생성) | 채팅 기록 하나를 구분하는 고유 식별자 (테이블 자기 자신의 `id`인 `profiles.id`와 달리, 매번 새로 생성되는 값이라 이름으로 구분되게 `qna_id`로 명명) |
    | `user_id` | uuid | 필수 (`auth.users.id` 참조) | 누가 질문했는지 |
    | `question` | text | 필수 | 사용자가 입력한 질문 |
    | `answer` | text | 필수 | AI가 준 답변 |
    | `screen_context` | text | 필수 | 질문 당시 화면 상태(예: 현재 step description) |
    | `created_at` | timestamp | 필수 (기본값: 현재 시각) | 질문 시각 |
    - [ ] (중요도 낮음) `feedback` 컬럼으로 답변 좋아요/싫어요 남기기 — 활용 계획(수동 검토·발표용 지표) 대비 스코프 부담이 있어 시간 남을 때만
  - [x] 회원가입 — `아이디` 입력 → `아이디@내부도메인` 가짜 이메일로 `supabase.auth.signUp()` 호출 → 성공 시 반환된 user id로 `profiles`에 `{ id, username }` insert. `AuthForm`의 제출 콜백(현재 콘솔 로그만 남기는 부분)을 이 호출로 교체
  - [x] 로그인 — 입력한 `아이디`로 같은 규칙의 가짜 이메일을 재구성해 `supabase.auth.signInWithPassword()` 호출
  - [x] Q&A 기록 저장/조회 — Supabase 클라이언트로 `qna_logs` 테이블에 직접 insert/select
  - [x] Gemini 프록시만 담당하는 최소 백엔드: `POST /api/ask` — 화면 상태(현재 step description)를 컨텍스트로 받아 Gemini 호출 후 응답 반환
- [x] 화면: AI Q&A 챗 위젯(`ChapterAssistant`) — 화면 상태 인지 컨텍스트 전달, API 장애 시에도 나머지 기능 100% 독립 동작
  - [ ] (중요도 낮음) 답변마다 👍/👎 버튼 — 위 feedback 컬럼과 세트
- [ ] PR 제출 전 `/security-review` 스킬로 자체 보안 점검 (Supabase anon key/Gemini secret 키 노출 여부 등)

## 3주차 — 남은 화학과 + 확장 콘텐츠

- [x] 무기화학 배위 화합물 — 결정 격자(단위세포)는 SMILES/SDF로 표현 불가능해 범위 제외(자세한 이유는 CLAUDE.md 참고), 대신 배위 화합물(헥사암민코발트(III)=정팔면체, 테트라암민구리(II)=평면사각형) 2종 구현. PubChem이 이 화합물들의 3D 데이터를 제공하지 않아, 이상적 결합각으로 직접 계산한 SDF/SMILES를 작성해 기존 `Structure2DViewer`/`Structure3DViewer`에 그대로 재사용. 자유 검색(PubChem 경로)은 유지하되 배위 화합물에선 부정확할 수 있다는 안내 문구 추가
- [ ] (시간 남을 시) 물리화학 후보 1개 — 반응속도 그래프 또는 르샤틀리에 평형이동
- [ ] 사이드바에 타 학과 항목 추가 (수학과·물리학과·생명과학과 등, 클릭 비활성)
- [ ] 접근성 보완 (색상 외 이중 표시, 키보드 조작)

## 3주차 부록: 화학 교수 검증(chemistry-reviewer 에이전트)에서 발견된 후속 작업

`.claude/agents/chemistry-reviewer.md` 에이전트로 일반화학/유기화학/무기화학 3개 화면을 전부 검증한 결과 발견된 화학 정확성 이슈. 오늘은 유기화학의 시인성(가독성) 개선(전자쌍 점, 반응 중심 강조, 분자 크기, 화살표-라벨 겹침)만 처리하고, 아래 콘텐츠 정확성 이슈는 다음 작업으로 미룸.

- [ ] (중간) `fischerEsterification.ts` — 이름이 "Fischer 에스테르화"인데 실제 반응(살리실산+무수아세트산)은 무수물 아실화 반응. Fischer 에스테르화는 정의상 카르복실산+알코올, 산 촉매, 부산물이 물이어야 함 — 이름 정정 필요(예: "아실화 반응"으로 변경) 또는 진짜 Fischer 에스테르화 예시로 교체
- [x] (치명적, 수정 완료) `fischerEsterification.ts` 1단계 화살표가 엉뚱한 원자를 가리키던 인덱스 버그 — SMILES 중간의 명시적 `[H]`(페놀성 수소)에 smiles-drawer가 별도 인덱스를 안 줘서 그 뒤 원자 인덱스가 전부 1씩 밀려있었음(원래 15/16으로 적혀있던 카르보닐 탄소/산소가 실제로는 14/15). 오늘 추가한 강조 링 기능 덕분에 눈에 띄게 발견되어 바로 수정, 렌더링 좌표로 재검증 완료
- [ ] (사소) `e2Elimination.ts` 1단계 — lone-pair 점 배치 휴리스틱("타겟 반대 방향")이 이 경우 우연히 에톡시화이온의 실제 C-O 결합선과 거의 겹쳐 그려짐. 결합 방향을 모르는 상태의 근사치라 생기는 한계 — 필요시 원자 주변 결합 방향까지 고려하는 배치 로직으로 개선 검토
- [ ] (중간) `e2Elimination.ts` — 화살표가 2개뿐(염기 전자쌍→베타탄소, C-Br결합→Br)인데 설명 문구는 "세 가지 변화가 동시에 일어난다"고 되어있음. 새로 생기는 C=C 파이결합 화살표가 데이터에 없음 — 베타 탄소의 수소를 명시적 원자로 추가하거나(Fischer의 `O[H]`처럼) 화살표 3개 구조로 보완 필요
- [ ] (중간) `ChemistryPage.tsx`(일반화학 분자 뷰어) — 첫 로드 시 기본값(에탄올 CCO)에 대해 3D를 자동으로 안 가져와서 "PubChem에 등록되지 않은 분자"라는 거짓 에러가 표시됨. 실제로는 에탄올 3D 데이터가 있음 — 마운트 시 기본 분자도 자동 조회하도록 수정 필요
- [ ] (중간) `coordinationCompounds.ts` — 테트라암민구리(II)를 "평면사각형"으로 캐비어트 없이 단정. 실제 문헌상 이 이온은 대부분 5배위(사각뿔)나 6배위(정팔면체, Jahn-Teller 왜곡)로 관찰됨 — "단순화된 예시"라는 안내 문구 추가 검토
- [ ] (사소) `coordinationCompounds.ts`의 3D SDF에 리간드 수소 원자가 전혀 없음(금속+N만 존재) — 같은 화면의 2D 패널은 NH3(수소 3개)로 정확히 그려져서 2D/3D가 서로 안 맞음. 3D에도 H 좌표 추가 검토
- [ ] (사소) `electrophilicAddition.ts` 1단계 — 화살표 1개가 두 전자쌍 이동(π→H, H-Br 결합 끊어짐)을 압축 표현. 설명 문구로 이미 보완되어 있어 우선순위 낮음

## 4주차 — 마무리

- [ ] 전체 버그 수정 및 성능 점검
- [ ] README / 기획서 최종 업데이트
- [ ] 배포 및 최종 점검 (도전 목표)
- [ ] 헤더 검색 기능

## 부록: AI Q&A 화면 인식 방식 검토

지금 `ChapterAssistant`가 Gemini한테 넘기는 컨텍스트는 `step.description` 텍스트 한 줄뿐 — 실제 화면(시각화 이미지) 자체는 안 보냄. 두 가지 개선 방향을 검토함(둘 다 미착수).

- **방향 A. 텍스트 컨텍스트 강화** — 현재 스텝 설명을 더 풍부하게(예: 트리 전체 구조까지 문자로 표현) 만드는 방식.
  - 장점: 비용 거의 안 늚, 숫자·판단 근거를 정확하게 그대로 전달 가능(오차 없음), 구현 부담 작음
  - 단점: 진짜 "화면을 보고" 답하는 건 아님
- **방향 B. 실제 화면 이미지를 Gemini Vision으로 전송**
  - 장점: 말 그대로 화면을 보고 답변
  - 단점: 이미지 토큰 비용이 텍스트보다 훨씬 큼(무료 티어 한도 더 빨리 소진) / 화면을 이미지로 캡처해 base64로 보내는 새 프론트 코드 필요(SVG라 `html2canvas`보다 SVG→canvas 직접 변환이 더 안정적일 듯) / Express 요청 크기 제한 상향 필요 / 응답 속도 느려짐 / **작은 SVG 텍스트(숫자)를 이미지에서 잘못 읽어 오히려 부정확해질 위험** — 지금 텍스트 컨텍스트가 이미 정확한 수치를 콕 집어 주고 있어서, 정확도 이득이 불확실함

결론: 방향 A가 비용·정확도 면에서 더 안전한 선택. 방향 B는 우선순위 낮음, 필요성이 명확해지면 재검토.
