# 작업 계획 (4주)

솔로 개발 기준. 템플릿 재사용을 전제로 주차를 구성함.

## 템플릿 정리

| 템플릿 | 특징 | 대상 |
|---|---|---|
| 기능 A. 코드-시각화 동기화 재생 | 정해진 시나리오가 자동으로 진행 (재생 · 한 단계 · 처음부터 · 속도) | 정렬, 트리 |
| 기능 B. 사용자 조작형 인터랙티브 시뮬레이터 | 사용자가 값을 입력하고 직접 조작 | 스택, 큐, 덱 |

컴퓨터공학과·화학과는 이번 프로젝트에서 전부 구현 확정. 그 외 학과(수학과·물리학과·생명과학과 등)는 사이드바에 노출만 하고 클릭 비활성화 상태로 유지 (별도 안내 화면 없음).

## 1주차 — 완료: 레이아웃 + 컴퓨터공학과 + 화학과

- [x] 전체 레이아웃, 사이드바 네비게이션
- [x] 정렬 엔진(기능 A) — 버블·선택·퀵·힙 4종
- [x] 스택·큐·덱(기능 B) — Push/Pop, Enqueue/Dequeue, 앞/뒤 삽입삭제, 오버플로우/언더플로우 처리
- [x] 트리(기능 A) — 이진·완전·편향 3종(실제 BST 삽입 기반), 삽입 비교 과정 시각화(대기 칩+대기열), 삽입/순회 독립 재생 분리
- [x] 분자 뷰어(일반화학) — SMILES/이름 → 2D/3D 구조
- [x] 유기화학 반응 메커니즘 애니메이션 — SN2/마르코니코프/Fischer 에스테르화/E2

## 2주차 — 핵심 목표: 디자인 재정립 + Supabase 연동 + 로그인 + DB + AI Q&A

- [ ] 전체 앱 디자인 재정립 (트리 페이지·홈페이지·사이드바 완료, 정렬/화학과/스택-큐-덱은 진행 중)
- [x] 로그인/회원가입 화면 UI 껍데기 (`LoginPage`/`AuthForm`/`TextField`) — 아이디+비밀번호만 사용(이메일 불필요), 실제 인증은 아래 Supabase 연동 후 연결
- **(갱신) 아키텍처를 Supabase 중심으로 변경**: 원래 Express가 로그인/DB/Gemini 프록시를 전부 담당하는 구조였는데, Supabase(Postgres DB + Auth + 자동 API를 묶은 BaaS)를 쓰기로 하면서 스코프가 줄어듦 — 로그인/회원가입/Q&A 기록 저장·조회는 **프론트엔드에서 Supabase 클라이언트 SDK로 직접** 처리(커스텀 백엔드 코드 불필요). Express(또는 서버리스 함수)는 **Gemini API 키를 숨겨서 대신 호출해주는 프록시 하나만** 남음 — API 키는 어차피 서버에서만 다뤄야 해서 이 부분만은 여전히 백엔드가 필요함.
  - [x] Supabase 프로젝트 생성 + React 클라이언트 연동(`@supabase/supabase-js`)
  - [x] **데이터 모델 확정** (아래 표) — 사용자 인증은 Supabase Auth 기본 테이블(`auth.users`)을 그대로 쓰되, 이메일 없이 "아이디"만으로 가입/로그인하기 위해 `아이디@내부도메인` 형태의 내부용 가짜 이메일을 만들어 Auth에 등록하고, 실제 아이디는 별도 `profiles` 테이블에 저장(비밀번호 해싱·세션 관리는 Supabase Auth가 그대로 담당 — 직접 구현 안 함)

    **`profiles`** (아이디 ↔ Supabase Auth 사용자 매핑)
    | 컬럼 | 타입 | 필수 | 설명 |
    |---|---|---|---|
    | `id` | uuid | 필수 (PK, `auth.users.id` 참조) | Supabase Auth가 발급한 사용자 ID와 동일 |
    | `username` | text | 필수 (unique) | 사용자가 로그인 시 입력하는 아이디 |
    | `created_at` | timestamp | 필수 (기본값: 현재 시각) | 가입 시각 |

    **`qna_logs`** (Q&A 대화 기록)
    | 컬럼 | 타입 | 필수 | 설명 |
    |---|---|---|---|
    | `qna_id` | uuid | 필수 (PK, 자동 생성) | 채팅 기록 하나를 구분하는 고유 식별자 (테이블 자기 자신의 `id`인 `profiles.id`와 달리, 매번 새로 생성되는 값이라 이름으로 구분되게 `qna_id`로 명명) |
    | `user_id` | uuid | 필수 (`auth.users.id` 참조) | 누가 질문했는지 |
    | `question` | text | 필수 | 사용자가 입력한 질문 |
    | `answer` | text | 필수 | AI가 준 답변 |
    | `screen_context` | text | 필수 | 질문 당시 화면 상태(예: 현재 step description) |
    | `created_at` | timestamp | 필수 (기본값: 현재 시각) | 질문 시각 |
    - [ ] (중요도 낮음) `feedback` 컬럼으로 답변 좋아요/싫어요 남기기 — 활용 계획(수동 검토·발표용 지표) 대비 스코프 부담이 있어 시간 남을 때만
  - [x] 회원가입 — `아이디` 입력 → `아이디@내부도메인` 가짜 이메일로 `supabase.auth.signUp()` 호출 → 성공 시 반환된 user id로 `profiles`에 `{ id, username }` insert. `AuthForm`의 제출 콜백(현재 콘솔 로그만 남기는 부분)을 이 호출로 교체
  - [x] 로그인 — 입력한 `아이디`로 같은 규칙의 가짜 이메일을 재구성해 `supabase.auth.signInWithPassword()` 호출
  - [x] Q&A 기록 저장/조회 — Supabase 클라이언트로 `qna_logs` 테이블에 직접 insert/select
  - [x] Gemini 프록시만 담당하는 최소 백엔드: `POST /api/ask` — 화면 상태(현재 step description)를 컨텍스트로 받아 Gemini 호출 후 응답 반환
- [x] 화면: AI Q&A 챗 위젯(`ChapterAssistant`) — 화면 상태 인지 컨텍스트 전달, API 장애 시에도 나머지 기능 100% 독립 동작
  - [ ] (중요도 낮음) 답변마다 👍/👎 버튼 — 위 feedback 컬럼과 세트
- [ ] PR 제출 전 `/security-review` 스킬로 자체 보안 점검 (Supabase anon key/Gemini secret 키 노출 여부 등)

## 3주차 — 남은 화학과 + 확장 콘텐츠

- [x] 무기화학 배위 화합물 — 결정 격자(단위세포)는 SMILES/SDF로 표현 불가능해 범위 제외(자세한 이유는 CLAUDE.md 참고), 대신 배위 화합물(헥사암민코발트(III)=정팔면체, 테트라암민구리(II)=평면사각형) 2종 구현. PubChem이 이 화합물들의 3D 데이터를 제공하지 않아, 이상적 결합각으로 직접 계산한 SDF/SMILES를 작성해 기존 `Structure2DViewer`/`Structure3DViewer`에 그대로 재사용. 자유 검색(PubChem 경로)은 유지하되 배위 화합물에선 부정확할 수 있다는 안내 문구 추가
- [ ] 사이드바에 타 학과 항목 추가 (수학과·물리학과·생명과학과 등, 클릭 비활성)
- [ ] 접근성 보완 (색상 외 이중 표시, 키보드 조작)

화학과 세부분야별 남은 작업(물리화학 후보 포함)은 아래 "화학과 세부분야별 확장 작업 계획"으로 통합 정리함.

## 화학과 세부분야별 확장 작업 계획 (최종 정리)

여러 차례에 걸쳐 나온 화학과 관련 항목(구현 완료분, `chemistry-reviewer` 에이전트의 정확성 검증, 학과 단위 1단계 재검토, 학생 관점 "필요한 시각자료" 리뷰)을 세부분야별로 통합함. 이전에 흩어져 있던 "3주차 부록"/"3주차 부록 2" 내용은 전부 여기로 병합됨.

### 일반화학

- [x] 분자 뷰어(SMILES/이름 검색 → 2D/3D) — 완료(1주차)
- [x] 첫 로드 시 거짓 3D 에러 수정 — 완료. `ChemistryPage.tsx`에 마운트용 `useEffect` 추가해 기본 분자(에탄올) 3D를 자동 조회, "PubChem에 등록되지 않은 분자" 거짓 안내 제거
- [ ] (높음, 신규) **오비탈 모양과 결합 형성 과정** — 원자구조·양자수 단원은 텍스트로 가장 이해 어려운 대표 개념인데 전무함. 기능 B(오비탈을 돌려보며 lobe 모양·부호 탐색)
- [x] **VSEPR 전자쌍 반발 이론(CH4/NH3/H2O)** — 완료. `VseprPage.tsx`(`/chemistry/vsepr`). 전자 도메인 4개로 동일하지만 비공유쌍 개수(0/1/2)에 따라 결합각이 109.5°→107°→104.5°로 압축되는 걸 비교. 배위 화합물과 달리 평범한 공유결합 분자라 PubChem에 실제 3D 데이터가 있음을 확인(WebFetch) 후, 직접 계산 없이 기존 `pubchem.ts` API 파이프라인을 그대로 재사용 — 기능 B
- [x] **루이스 구조 도출 절차(H2O)** — 완료. `LewisStructurePage.tsx`(`/chemistry/lewis-structure`), 신규 `src/features/lewisStructure/*`(전용 SVG 렌더러 — 결합 방향을 계산해 비공유 전자쌍을 빈 방향에 자동 배치). 원자가전자 세기 → 골격 → 결합쌍 → 옥텟(듀엣) 확인 → 비공유쌍 배치 → 형식전하 확인까지 6단계, 기존 `MechanismControls`/재생 패턴 재사용(기능 A). VSEPR과 같은 "화학결합" 큰 단원이지만 실제 교재에서도 별도 챕터(루이스구조 vs 분자기하)로 다뤄 별도 leaf 유지가 맞음
- [ ] (중간, 신규) **산-염기 적정 곡선**(강산/강염기, 약산/강염기, 완충 구간·당량점) — 분석화학(제외됨)과는 구분되는 일반화학 산염기평형 기본 개념. 기능 B(적정량 슬라이더 조작)

### 유기화학

- [x] 반응 메커니즘 4종(SN2/마르코니코프 첨가/아실화/E2) — 완료(1주차), 시인성 개선(전자쌍 점·반응 중심 강조·분자 크기·화살표 겹침 수정) 완료(3주차)
- [x] (치명적, 수정 완료) `fischerEsterification.ts` 1단계 화살표 원자 인덱스 버그 — SMILES 중간 명시적 `[H]`에 smiles-drawer가 별도 인덱스를 안 줘서 그 뒤 원자 인덱스가 1씩 밀려있었음(15/16으로 적힌 카르보닐 탄소/산소가 실제로는 14/15). 강조 링 기능 덕분에 발견, 렌더링 좌표로 재검증 완료
- [x] `fischerEsterification.ts` 이름 정정 — 완료. "아실화 반응(아스피린 합성)"으로 변경, summary에 Fischer 에스테르화(카르복시산+알코올·산촉매·부산물 물)와 구분되는 이유 명시
- [x] `e2Elimination.ts` 화살표 누락 보완 — 완료. 화살표 3개(①염기의 H제거 ②C=C 파이결합 형성 ③C-Br 이탈)로 협동 반응 정확히 표현, 설명도 ①②③으로 명확화
- [ ] (사소) `e2Elimination.ts` 1단계 — lone-pair 점 배치 휴리스틱이 실제 C-O 결합선과 거의 겹쳐 그려짐. 결합 방향까지 고려하는 로직으로 개선 검토
- [ ] (사소) `electrophilicAddition.ts` 1단계 — 화살표 1개가 두 전자쌍 이동을 압축 표현. 설명 문구로 이미 보완돼 있어 우선순위 낮음
- [x] **SN1 메커니즘 추가(SN2와 대조)** — 완료. `sn1.ts`(tert-뷰틸 브로마이드, 2단계: 이온화(속도 결정)→친핵체 공격), `SubstitutionEliminationPage.tsx`에 SN2/E2와 함께 탭으로 추가. 설명에 SN2와의 대조(1단계 협동 vs 2단계·탄소양이온·라세미화)까지 포함. 기존 기능 A 엔진 그대로 재사용. **chemistry-reviewer 검증 반영**: 처음엔 친핵체로 OH⁻을 썼으나 3차 기질+강염기는 실제로 E2가 우세해 SN1 예시로 부적절 → 물(H₂O) 친핵체·가용매분해 조건으로 교체하고, "강염기(OH⁻)에서는 E2가 우세" 캐비어트 추가
- [ ] (중간~높음, 신규) **SN2 배치 반전(inversion)을 실제로 보여줄 카이랄 예시로 교체** — 지금 예시(브로민화메틸)는 카이랄 중심이 없어 "반전"을 텍스트로만 언급하고 실제로 안 보여줌. 카이랄 2급 기질(예: (R)-2-브로모부탄)로 교체해 3D 회전 비교 필요. 기능 B
- [ ] (중간, 신규) **반응 좌표 에너지 다이어그램** — 카르보양이온 안정성 차이 등 지금 텍스트로만 설명하는 상대적 안정성을 그래프로 보여줌. 스텝 재생과 자연스럽게 동기화 가능(기능 A)
- [ ] (중간, 신규) **Newman 투영도/형태 이성질체**(에탄·부탄의 겹침/엇갈림, 형태 에너지) — 입체구조 단원의 대표 시각 자료가 전무함. 기능 B
- [ ] (중간, 신규) **E2의 안티페리플라나 요구조건** — 베타 수소와 이탈기가 반대 방향 정렬돼야 하는 입체전자적 조건은 평면 SMILES로는 표현 불가, Newman 투영도 필요. 기능 B

### 무기화학

- [x] 배위 화합물 2종(헥사암민코발트(III)=정팔면체, 테트라암민구리(II)=평면사각형) — 완료(3주차). 결정 격자(단위세포)는 SMILES/SDF 한계로 범위 제외
- [x] **결정장 이론 — 배위 화합물이 왜 색을 띠는가** — 완료. `InorganicChemistryPage.tsx`에 "결정장 이론" 패널 추가, `CrystalFieldDiagram.tsx`로 d오비탈 에너지 준위 갈라짐(Δ) + 실제 색 표시(WebFetch로 문헌 확인: 헥사암민코발트(III)=노랑-주황 결정, 테트라암민구리(II)=진한 파랑-보라). 평면사각형은 정확한 오비탈 순서에 불확실성이 있어 간략화했다는 캐비어트 문구 포함. 검증된 예시(활성 프리셋)에서만 표시, 자유 검색 시엔 숨김
- [ ] (중간, 신규) **cis/trans 기하 이성질체·킬레이트 리간드** — 지금 예시는 전부 단일자리(monodentate) 리간드뿐이라 "같은 조성, 다른 배치"라는 이성질체 개념이 빠짐. 기존 좌표 계산 기법 그대로 재사용 가능해 비용 낮음. 기능 B(두 이성질체를 나란히 놓고 비교)
- [ ] (중간) 산화-환원 반응(전자 이동)/전기화학 — 절차형 개념이라 유기화학 반응 엔진(기능 A) 재사용 적합. 반응 데이터는 새로 작성 필요
- [x] 테트라암민구리(II) "평면사각형" 캐비어트 문구 추가 — 완료. description에 5배위(사각뿔)·6배위(정팔면체, Jahn-Teller 왜곡)로 관찰되는 경우가 많고 여기서는 이상적 평면사각형만 단순화한다는 안내 추가
- [x] 3D 뷰 회전 유도 + 배위 골격 안내 문구 추가 — 완료. `CoordinationGeometryPage.tsx` 3D 패널에 "마우스로 드래그해 돌려보세요" + 아래 3D SDF 항목 안내를 한 문구로 통합
- [x] 3D SDF에 리간드 수소 좌표 추가 — 해결(다른 방식). 수소를 그리면 정팔면체/평면사각형 기하구조가 오히려 가려져 학습에 해로움 → 교재의 배위 다면체 표현처럼 3D는 배위 골격(금속+공여원자)만 표시하고, "리간드 전체 모양은 2D에서 확인"이라는 안내로 2D/3D 불일치를 disclosure로 해소
- [ ] (낮음, 보류) 주기율표 경향성 — 수치 비교라 그래프면 충분, 물리화학의 그래프형 시각화와 겹쳐 보류
- (참고) 결정 격자/단위세포 — SMILES/SDF로 표현 불가해 범위 제외했으나, 추후 별도 렌더링 방식(예: Three.js 직접 구현)이 가능해지면 재검토 가치 있음

### 물리화학

- [x] **Maxwell-Boltzmann 분자 속도 분포** — 완료. `src/pages/chemistry/PhysicalChemistryPage.tsx`(`/chemistry/physical`). 기체 5종(H₂/He/N₂/O₂/CO₂) 선택 + 온도 슬라이더(100~1000K) 조작으로 분포 곡선이 실시간으로 바뀜, "현재 조건을 비교선으로 저장" 버튼으로 두 조건을 나란히 비교 가능(기능 B). 외부 데이터/API 불필요 — 물리 공식(4단계 원칙 3단계: 데이터 소스가 아예 필요 없는 순수 계산 케이스)을 `src/features/physicalChemistry/lib/maxwellBoltzmann.ts`에 직접 구현, 기존 차트 라이브러리 없이 SVG로 직접 렌더링(다른 화면들과 동일한 패턴). 질소 298K→최빈속도 421m/s, CO₂ 700K→514m/s로 실제 물리값과 일치 확인
- [ ] (높음, 신규) **PV 다이어그램 열역학 순환과정(카르노 사이클 등)** — 정해진 단계를 순서대로 거치며 상태가 바뀌는 절차형 개념. 기능 A(단계별 재생, P-V 그래프 위 현재 위치 강조)
- [ ] (중간~높음) 반응속도 그래프(적분 속도식·반감기) — 기존 후보 유지. 기능 B(반응 차수 조작 → 농도-시간 곡선 비교)
- [ ] (중간, 신규) **상평형 그림(P-T 다이어그램, 삼중점·임계점)** — 임의의 (P,T) 지점을 클릭해 상 상태를 확인하는 기능 B에 적합
- [ ] (낮음, 재조정) 르샤틀리에 평형이동 — 기존 후보였으나 정성적 방향성 판단이라 물리화학다운 정량성이 약함. 위 반응속도 그래프가 더 적합한 후보로 우선순위 하향
- (참고, 낮음) 양자화학 파동함수/오비탈 확률밀도 — 일반화학의 오비탈 시각화와 개념이 겹쳐, 일반화학 쪽이 먼저 만들어지면 그 위에 수학적 디테일(마디 개수 등)만 얹는 방식으로 재사용 검토

### 제외/보류 (학과·과목 단위)

- (제외) **분석화학** — 화학과의 다섯 번째 축(적정 곡선·크로마토그래피·분광법 등, 전부 시각화 가치 있음)이지만 시간 제약으로 스코프에서 명시적으로 제외

## 컴퓨터공학과 확장 후보 (CLAUDE.md 4단계 원칙, 학과 단위로 재적용)

화학과와 마찬가지로 "컴퓨터공학과=자료구조·정렬 4~5개"라고 이미 전제하고 그 안에서만 판단해왔던 걸 뒤늦게 발견 — 학과 단위(1단계)로 다시 확인함.

- [ ] (중간) **운영체제 — CPU 스케줄링 시각화**(Round Robin, FCFS 등) — 정해진 규칙대로 프로세스가 순서대로 처리되는 절차형 개념이라 기존 기능 A(코드-시각화 동기화 재생) 엔진 그대로 재사용 가능. 지금까지 후보에 한 번도 언급된 적 없던 완전히 새로운 발견. `TASK.md`에 확장 후보로 등록
- [ ] (중간) **데이터베이스 — B-트리 인덱스 시각화** — 삽입 과정이 기존 트리 기능과 개념적으로 거의 같아 재사용도가 가장 높은 후보. `TASK.md`에 확장 후보로 등록
- 그래프(BFS/DFS)·해시테이블·**네트워크**(오늘 새로 추가)는 아직 시급성보다 "장기 비전" 성격이 강해 `기획서.md`의 "장기 비전 vs 구현 범위" 다이어그램에 반영(CS 확장: 그래프·해시테이블·네트워크)

## 4주차 — 마무리

- [ ] 전체 버그 수정 및 성능 점검
- [ ] README / 기획서 최종 업데이트
- [ ] 배포 및 최종 점검 (도전 목표)
- [ ] 헤더 검색 기능

## 부록: AI Q&A 화면 인식 방식 검토

지금 `ChapterAssistant`가 Gemini한테 넘기는 컨텍스트는 `step.description` 텍스트 한 줄뿐 — 실제 화면(시각화 이미지) 자체는 안 보냄. 두 가지 개선 방향을 검토함(둘 다 미착수).

- **방향 A. 텍스트 컨텍스트 강화** — 현재 스텝 설명을 더 풍부하게(예: 트리 전체 구조까지 문자로 표현) 만드는 방식.
  - 장점: 비용 거의 안 늚, 숫자·판단 근거를 정확하게 그대로 전달 가능(오차 없음), 구현 부담 작음
  - 단점: 진짜 "화면을 보고" 답하는 건 아님
- **방향 B. 실제 화면 이미지를 Gemini Vision으로 전송**
  - 장점: 말 그대로 화면을 보고 답변
  - 단점: 이미지 토큰 비용이 텍스트보다 훨씬 큼(무료 티어 한도 더 빨리 소진) / 화면을 이미지로 캡처해 base64로 보내는 새 프론트 코드 필요(SVG라 `html2canvas`보다 SVG→canvas 직접 변환이 더 안정적일 듯) / Express 요청 크기 제한 상향 필요 / 응답 속도 느려짐 / **작은 SVG 텍스트(숫자)를 이미지에서 잘못 읽어 오히려 부정확해질 위험** — 지금 텍스트 컨텍스트가 이미 정확한 수치를 콕 집어 주고 있어서, 정확도 이득이 불확실함

결론: 방향 A가 비용·정확도 면에서 더 안전한 선택. 방향 B는 우선순위 낮음, 필요성이 명확해지면 재검토.
