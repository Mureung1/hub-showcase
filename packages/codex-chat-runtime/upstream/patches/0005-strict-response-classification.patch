diff --git a/sdk/python/src/openai_codex/_message_router.py b/sdk/python/src/openai_codex/_message_router.py
index 3262952c..d37441c7 100644
--- a/sdk/python/src/openai_codex/_message_router.py
+++ b/sdk/python/src/openai_codex/_message_router.py
@@ -60,0 +61,6 @@ class _RouterUsageSnapshot:
+class _MalformedResponseError(CodexError):
+    """Terminal failure raised for one invalid correlated response envelope."""
+
+    code = "malformed_response"
+
+
@@ -321 +327,8 @@ class MessageRouter:
-            if "error" in msg:
+            has_result = "result" in msg
+            has_error = "error" in msg
+            if has_result == has_error:
+                raise _MalformedResponseError(
+                    "JSON-RPC response must contain exactly one of result or error"
+                )
+
+            if has_error:
@@ -323,5 +336,7 @@ class MessageRouter:
-                if isinstance(err, dict):
-                    item: ResponseQueueItem = map_jsonrpc_error(
-                        int(err.get("code", -32000)),
-                        str(err.get("message", "unknown")),
-                        err.get("data"),
+                if not isinstance(err, dict):
+                    raise _MalformedResponseError("JSON-RPC error must be an object")
+                code = err.get("code")
+                message = err.get("message")
+                if type(code) is not int or not isinstance(message, str):
+                    raise _MalformedResponseError(
+                        "JSON-RPC error requires an integer code and string message"
@@ -329,2 +344,5 @@ class MessageRouter:
-                else:
-                    item = CodexError("Malformed JSON-RPC error response")
+                item: ResponseQueueItem = map_jsonrpc_error(
+                    code,
+                    message,
+                    err.get("data"),
+                )
@@ -332 +350 @@ class MessageRouter:
-                item = msg.get("result")
+                item = msg["result"]
diff --git a/sdk/python/tests/test_client_rpc_methods.py b/sdk/python/tests/test_client_rpc_methods.py
index d36c1bb8..0fa11efb 100644
--- a/sdk/python/tests/test_client_rpc_methods.py
+++ b/sdk/python/tests/test_client_rpc_methods.py
@@ -16 +16 @@ from openai_codex.client import CodexClient, CodexConfig, _params_dict
-from openai_codex.errors import CodexError
+from openai_codex.errors import CodexError, JsonRpcError
@@ -114 +114,18 @@ def _assert_router_usage_zero(router: MessageRouter) -> None:
-def test_reader_response_decode_failure_settles_the_registered_waiter() -> None:
+@pytest.mark.parametrize(
+    "malformed_response",
+    [
+        {"id": "request-1"},
+        {"id": "request-1", "result": {}, "error": {}},
+        {"id": "request-1", "error": None},
+        {"id": "request-1", "error": []},
+        {"id": "request-1", "error": {"message": "missing code"}},
+        {"id": "request-1", "error": {"code": True, "message": "bad"}},
+        {"id": "request-1", "error": {"code": -32000.0, "message": "bad"}},
+        {"id": "request-1", "error": {"code": "-32000", "message": "bad"}},
+        {"id": "request-1", "error": {"code": -32000}},
+        {"id": "request-1", "error": {"code": -32000, "message": 42}},
+    ],
+)
+def test_reader_rejects_malformed_response_before_removing_waiter(
+    malformed_response: dict[str, object],
+) -> None:
@@ -117,4 +133,0 @@ def test_reader_response_decode_failure_settles_the_registered_waiter() -> None:
-    malformed_response = {
-        "id": "request-1",
-        "error": {"code": "not-an-int", "message": "bad"},
-    }
@@ -126,2 +139,3 @@ def test_reader_response_decode_failure_settles_the_registered_waiter() -> None:
-    assert isinstance(failure, ValueError)
-    with pytest.raises(ValueError) as future_failure:
+    assert isinstance(failure, CodexError)
+    assert getattr(failure, "code", None) == "malformed_response"
+    with pytest.raises(CodexError) as future_failure:
@@ -132,0 +147,24 @@ def test_reader_response_decode_failure_settles_the_registered_waiter() -> None:
+def test_router_accepts_exactly_one_valid_result_or_error() -> None:
+    result_router = MessageRouter()
+    result_waiter = result_router.create_response_waiter("result")
+    result = {"value": "ok"}
+    result_router.route_response({"id": "result", "result": result})
+    assert result_waiter.get(timeout=1) is result
+
+    error_router = MessageRouter()
+    error_waiter = error_router.create_response_waiter("error")
+    error_router.route_response(
+        {
+            "id": "error",
+            "error": {"code": -32602, "message": "invalid params", "data": {}},
+        }
+    )
+    failure = error_waiter.get(timeout=1)
+    assert isinstance(failure, JsonRpcError)
+    assert (failure.code, failure.message, failure.data) == (
+        -32602,
+        "invalid params",
+        {},
+    )
+
+
