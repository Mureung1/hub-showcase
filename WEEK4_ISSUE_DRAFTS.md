# Week 4 주간 목표 및 GitHub 이슈 초안

작성 기준일: 2026-07-27 월요일

이 문서는 10:00~11:00 주간 계획 수립 시간에 바로 사용할 수 있도록 현재 저장소, 기존 PR, 최근 커밋과 멘토링 피드백을 대조해 작성했다.

---

## 1. 이번 주 목표

> 민감한 감정 기록을 계정과 클라우드 저장에 강제로 묶지 않는 게스트 기반 핵심 흐름을 완성하고, Render에 배포해 외부 브라우저에서 입력 → 정리 → 저장 → 캘린더 조회 → 삭제를 시연할 수 있게 한다.

### 완료 기준

- 회원가입 없이 `게스트로 시작`할 수 있다.
- 게스트 기록은 브라우저에 저장되고 새로고침 후에도 유지된다.
- 게스트 기록의 생성·조회·삭제 과정에서 Supabase에 저장 요청을 보내지 않는다.
- AI 또는 서버 전송이 발생하는 기능은 사용자에게 구분해서 안내한다.
- Render 영구 URL에서 핵심 흐름을 실행할 수 있다.
- 모바일 화면과 오류·빈 상태를 확인한다.
- 클라이언트 테스트, 서버 테스트와 프로덕션 빌드가 통과한다.
- 수요일 영상 제출과 금요일 데모데이에 사용할 시연 흐름이 준비된다.

### 이번 주 범위에서 제외

- 실제 회원가입과 Supabase Auth
- `user_id` 기반 RLS와 클라우드 동기화
- 공개 서버에서 실제 LiteLLM·Vertex Gemini 호출
- PWA 설치 지원
- 주간 모아보기 UI
- 반복 감정 패턴과 상담 지원 정보

위 항목은 핵심 배포가 완료된 뒤 진행할 후속 백로그다. 로그인 버튼을 화면에 보여주더라도 실제 인증이 아니라면 `준비 중`으로 표시한다.

---

## 2. 현재 저장소 상태

### 브랜치

- 저장소: `Yooseunghyeok/hub`
- 브랜치: `N114_유승혁`
- 로컬 HEAD와 `origin/N114_유승혁`이 `50d6d35`에서 일치

### 현재 구현

- React 입력·결과 수정·기록 목록·상세·캘린더
- Express 미리보기·생성·조회·사진 업로드·삭제 API
- Supabase Database·Storage
- LiteLLM → Vertex Gemini 로컬 실제 연동과 mock fallback
- 무드 피커와 사진 첨부
- 메모리 기반 데모 모드
- React 빌드 결과와 Express API를 함께 제공하는 Render 설정

### 현재 검증 기준선

- 클라이언트 Vitest: 9개 통과
- 서버 Node test runner: 12개 통과
- Vite 프로덕션 빌드: 통과

### 실제 남은 핵심 차이

- Render 배포 구성은 코드에 존재하고 이미 푸시되어 있다.
- 실제 Render 서비스와 영구 URL은 아직 없다.
- 게스트 로컬 저장은 문서 계획만 있고 코드에는 없다.
- 현재 React는 모든 기록 생성·조회·삭제를 `/api/checkins`로 요청한다.
- 현재 데모 모드는 서버 메모리에 기록하므로 서버가 재시작되면 사라지고 방문자가 같은 데이터를 공유한다.

### 현재 미커밋 파일

- `docs/learning/retrospectives.md`
- `.claude/skills/retro/`
- `PPT_CONTEXT.md`

이 파일들은 이번 주 핵심 기능 구현과 섞어서 한 번에 커밋하지 않는다.

---

## 3. 지금까지의 PR·커밋 흐름

### 개인 포크 PR

1. PR #16 — 2주차 하루 체크아웃 수직 슬라이스 구현
   - React 입력·결과·최근 기록 화면
   - Express preview/save/list API와 공통 오류 처리
   - Supabase 저장·조회
   - LiteLLM·Vertex 호출과 mock fallback
   - 개인 포크의 `N114_유승혁` → `main` Draft PR
   - 2026-07-14에 병합 없이 닫힘
   - `https://github.com/Yooseunghyeok/hub/pull/16`

### 공용 Challenge 저장소의 닫힌 PR 전체

GitHub에서 `유승혁 is:closed`로 검색되는 PR은 총 15개다. 이 가운데 14개는 `N114_유승혁` 브랜치에 병합되었고, #1254만 실수로 `main`을 대상으로 열었다가 닫은 뒤 #1257로 다시 제출했다.

#### Week 1 — 주제 구체화와 인터랙티브 프로토타입

1. [PR #20 — 프로젝트 주제(가안) 소개 컴포넌트 및 React 환경설정](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/20)
   - Vite·React 환경, `ProjectIntro`, README 초안
2. [PR #149 — 하루 체크아웃 기획 구체화 및 학습 내용 정리](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/149)
   - 문제 정의, 사용자 시나리오, 핵심 기능, 프롬프트 학습
3. [PR #286 — 프로토타입 제작 및 기획서 보강](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/286)
   - 입력 → 로딩 → 결과 → 저장 흐름과 플립 카드·상담 프렙시트 프로토타입
4. [PR #419 — 디자인 시스템 연동 및 개발 보강](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/419)
   - client/server 구조, 화면 설계, 디자인 토큰, LiteLLM·Vertex 방향, 디자인 검증 Skill
5. [PR #572 — 1주차 발표 및 개발 백로그 정리](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/572)
   - AI 응답 형식 초안, 1주차 발표 자료, 2~4주차 백로그

#### Week 2 — React·Express·Supabase·AI 수직 슬라이스

6. [PR #869 — 수직 슬라이스 및 AI 연동](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/869)
   - React 화면, Express API, Supabase 저장·조회, LiteLLM·Vertex, mock fallback
7. [PR #1016 — 입력 폼 컴포넌트 분리 및 React 학습 정리](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1016)
   - `CheckinForm` 분리, state·props·fetch 학습
8. [PR #1141 — 결과·기록 컴포넌트 분리와 상세 화면](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1141)
   - `SummaryCard`·`RecordCard`, 상세 전환, 화면·데이터 흐름도, 데이터 모델 초안
9. [PR #1254 — 계획·검증 서브에이전트 추가 및 Express 학습 정리](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1254)
   - `main`을 대상으로 잘못 열어 병합 없이 닫은 PR
10. [PR #1257 — 서브에이전트 추가 및 Express 학습 정리](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1257)
    - #1254를 올바른 브랜치 대상으로 다시 제출해 병합

#### Week 3 — 기능 확장, 테스트, 배포 준비

11. [PR #1448 — 무드/사진/캘린더/플립카드 기능 추가](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1448)
    - DB 컬럼, 무드 피커, 사진 업로드, 캘린더, 정리 근거 플립 카드
12. [PR #1575 — 체크인 삭제 기능 설계 및 개발 절차 Skill](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1575)
    - 하드·소프트·프론트 삭제 비교, API 계약·완료 기준, `feature-development` Skill
13. [PR #1701 — 체크인 삭제 기능 및 아키텍처 다이어그램](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1701)
    - `DELETE /api/checkins/:id`, 삭제·404 테스트, 화면 상태 갱신, 통합 아키텍처
14. [PR #1851 — TDD 및 프로젝트 쇼케이스 작성](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1851)
    - Vitest 환경, 캘린더 테스트 6개, 주간 필터 red → green, 테스트 Skill, showcase
15. [PR #1953 — 배포 설정 시도](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1953)
    - Cloudflare 임시 데모, 안전한 메모리·mock 데모 모드, Render 단일 서비스 설정

### PR #1953에 포함된 최근 커밋

- `acd552c` — 게스트 모드 중심 Week 4 계획
- `c8c621b` — 멘토링 피드백 기반 데이터 저장 원칙
- `50d6d35` — 메모리 데모 모드와 Render 단일 서비스 배포 지원

따라서 이번 주에는 테스트 환경이나 Render 설정을 다시 만드는 이슈를 등록하지 않는다. 준비된 기반을 이용해 게스트 저장과 실제 배포를 완료한다.

---

# GitHub 이슈 등록 결과

2026-07-27 개인 포크 `Yooseunghyeok/hub`에 등록 완료했다.

- [#25 게스트 기록을 브라우저에 저장하고 새로고침 후 복원](https://github.com/Yooseunghyeok/hub/issues/25)
- [#26 게스트 진입과 데이터·AI 전송 안내 UI 완성](https://github.com/Yooseunghyeok/hub/issues/26)
- [#27 Render 첫 배포와 공개 URL 스모크 테스트](https://github.com/Yooseunghyeok/hub/issues/27)
- [#28 핵심 흐름 회귀 테스트와 수요일 데모 영상 제출](https://github.com/Yooseunghyeok/hub/issues/28)
- [#29 Agent 워크플로우 정리와 데모데이 발표 준비](https://github.com/Yooseunghyeok/hub/issues/29)
- [#30 배포 후 UI·오류 상태 보완과 재배포](https://github.com/Yooseunghyeok/hub/issues/30)

기존 열린 이슈 `#15, #18~#24`는 모두 닫았다. 완료된 항목은 `completed`, 이번 주 범위에서 제외한 주간 모아보기 화면(#19)과 PWA(#20)는 `not planned`로 정리했다.

---

# 등록한 GitHub 이슈 상세

## Issue 1

### 제목

`[Week4][P0] 게스트 기록을 브라우저에 저장하고 새로고침 후 복원`

### 본문

## 배경

현재 React는 기록 생성·조회·삭제를 모두 Express `/api/checkins`에 요청한다. 서버의 데모 모드는 메모리 기반이라 재시작 시 기록이 사라지고 여러 방문자가 데이터를 공유한다.

감정 기록의 민감성을 고려해 게스트 모드에서는 계정이나 Supabase 없이 해당 브라우저에만 기록을 보관한다.

## 작업

- [ ] 게스트 기록 저장 인터페이스 정의
  - [ ] `getCheckins()`
  - [ ] `createCheckin()`
  - [ ] `deleteCheckin()`
  - [ ] `clearCheckins()`
- [ ] IndexedDB 기반 저장소 구현
- [ ] 앱 시작 시 브라우저 기록 불러오기
- [ ] 게스트 저장 시 `/api/checkins` POST를 호출하지 않도록 분리
- [ ] 게스트 삭제 시 `/api/checkins/:id`를 호출하지 않도록 분리
- [ ] 새로고침 후 목록과 캘린더 기록 유지
- [ ] 전체 기록 삭제 기능 추가
- [ ] 저장·조회·삭제·초기화 테스트 작성

## 완료 기준

- [ ] 회원가입 없이 기록을 저장할 수 있다.
- [ ] 새로고침 후 기록이 유지된다.
- [ ] 같은 브라우저에서 목록과 캘린더가 동일한 기록을 보여준다.
- [ ] 개별 삭제와 전체 삭제가 동작한다.
- [ ] 게스트 기록 CRUD 중 Supabase 저장 API를 호출하지 않는다.
- [ ] 관련 테스트가 통과한다.

## 범위 제외

- 로그인 사용자 클라우드 동기화
- 게스트 사진 Blob 저장은 핵심 텍스트 흐름 완료 후 별도 보완

---

## Issue 2

### 제목

`[Week4][P0] 게스트 진입과 데이터·AI 전송 안내 UI 완성`

### 본문

## 배경

멘토링에서 감정 기록은 회원가입과 클라우드 저장 자체가 사용 장벽이 될 수 있다는 피드백을 받았다. 게스트가 어떤 데이터를 어디에 저장하는지 이해할 수 있어야 한다.

로컬 저장을 사용하더라도 AI 정리를 호출하면 입력 원문이 서버로 전송된다는 점은 별도로 안내해야 한다.

## 작업

- [ ] 첫 진입 화면 또는 안내 영역에 `게스트로 시작` 제공
- [ ] `이 기기의 브라우저에만 저장됩니다` 안내 표시
- [ ] 브라우저 데이터 삭제 시 기록이 사라질 수 있음을 표시
- [ ] AI 또는 서버 전송 전에 짧은 안내 표시
- [ ] mock 결과와 실제 AI 결과를 화면에서 오해하지 않게 구분
- [ ] `모든 기록 삭제` 실행 전 확인 UI 추가
- [ ] 로그인·동기화는 실제 구현 전까지 `준비 중`으로 표시하거나 숨김

## 완료 기준

- [ ] 사용자가 가입 없이 10초 안에 기록을 시작할 수 있다.
- [ ] 기록의 저장 위치를 화면에서 확인할 수 있다.
- [ ] AI 사용 시 외부 전송 가능성을 알 수 있다.
- [ ] 실제 인증이 없는 기능을 로그인 완료처럼 보이게 하지 않는다.
- [ ] 전체 삭제는 확인 후 실행된다.

---

## Issue 3

### 제목

`[Week4][P0] Render 첫 배포와 공개 URL 스모크 테스트`

### 본문

## 배경

`render.yaml`과 React·Express 단일 서비스 제공 코드는 이미 준비되어 있다. 이번 이슈에서는 설정 작성이 아니라 실제 Render 서비스를 만들고 외부 URL에서 핵심 흐름을 검증한다.

공개 배포는 비밀키 없는 안전한 데모 모드로 진행한다.

## 작업

- [ ] GitHub `N114_유승혁` 브랜치를 Render에 연결
- [ ] `render.yaml` 기준 Web Service 생성
- [ ] 빌드 명령 `npm run render:build` 성공 확인
- [ ] 실행 명령 `npm start` 성공 확인
- [ ] `DEMO_MODE=true`, `SERVE_CLIENT=true` 확인
- [ ] `/api/health`가 `storage: demo`를 반환하는지 확인
- [ ] 루트 URL에서 React 화면 확인
- [ ] 새로고침과 SPA 직접 접근 확인
- [ ] 게스트 입력 → 정리 → 저장 → 캘린더 → 삭제 확인
- [ ] 모바일 브라우저 또는 모바일 크기에서 접속 확인
- [ ] 실패 내용과 해결 과정을 기록
- [ ] 검증이 끝난 뒤 공개 URL을 제출 문서에 반영

## 완료 기준

- [ ] 영구적인 `onrender.com` URL이 존재한다.
- [ ] 다른 기기나 시크릿 창에서 접속할 수 있다.
- [ ] 공개 환경에서 핵심 사용자 흐름이 동작한다.
- [ ] 실제 Supabase 서비스 역할 키나 Vertex 인증정보를 Render에 넣지 않는다.
- [ ] 민감한 실제 감정 대신 테스트 문장으로 검증한다.

## 배포 스모크 테스트

- [ ] `GET /api/health`
- [ ] `/` React 화면
- [ ] 기록 생성
- [ ] 기록 조회
- [ ] 캘린더 표시
- [ ] 기록 삭제
- [ ] 새로고침
- [ ] 404 또는 API 오류 화면

---

## Issue 4

### 제목

`[Week4][P0] 핵심 흐름 회귀 테스트와 수요일 데모 영상 제출`

### 본문

## 배경

수요일 영상 제출 전에 로컬과 Render 환경에서 같은 시나리오를 반복해 기능 회귀를 확인한다. 영상은 기능 수보다 핵심 흐름과 데이터 저장 방식을 명확하게 보여주는 데 집중한다.

## 작업

- [ ] 클라이언트 테스트 실행
- [ ] 서버 테스트 실행
- [ ] 프로덕션 빌드 실행
- [ ] 로컬 게스트 흐름 수동 확인
- [ ] Render 게스트 흐름 수동 확인
- [ ] 빈 입력, 긴 입력, AI 실패, 저장 실패, 삭제 실패 확인
- [ ] 새로고침 후 기록 복원 확인
- [ ] 모바일 크기에서 레이아웃 확인
- [ ] 데모용 테스트 문장 준비
- [ ] 프로그램 제출 기준에 맞춰 영상 녹화
- [ ] 제출 링크 또는 파일 최종 확인

## 완료 기준

- [ ] 클라이언트 테스트가 모두 통과한다.
- [ ] 서버 테스트가 모두 통과한다.
- [ ] 프로덕션 빌드가 통과한다.
- [ ] 로컬과 공개 URL의 핵심 흐름이 동일하다.
- [ ] 개인정보가 포함되지 않은 영상이 제출된다.

## 영상 흐름

1. 회원가입 없이 게스트로 시작
2. 저장 위치 안내 확인
3. 테스트 감정 문장 입력
4. 정리 결과 확인과 수정
5. 저장
6. 캘린더에서 확인
7. 새로고침 후 유지 확인
8. 삭제

---

## Issue 5

### 제목

`[Week4][P1] 배포 후 UI·오류 상태 보완과 재배포`

### 본문

## 배경

첫 배포 이후 실제 브라우저에서 확인된 문제를 우선 수정한다. 새로운 대형 기능을 추가하기보다 데모 중 사용자가 막히는 지점을 제거한다.

## 작업

- [ ] 첫 배포에서 발견한 버그 목록 작성
- [ ] 모바일 화면의 버튼·카드·캘린더 레이아웃 보완
- [ ] 로딩 상태 중 중복 클릭 방지
- [ ] 빈 기록 상태 문구 보완
- [ ] 네트워크·AI·저장 실패 메시지 구분
- [ ] mock 결과를 실제 AI처럼 오해하지 않도록 표시
- [ ] 게스트 사진 기능의 지원 범위를 결정하고 화면에 반영
- [ ] 수정 후 테스트·빌드 재실행
- [ ] Render 재배포
- [ ] 재배포 URL 스모크 테스트

## 완료 기준

- [ ] 모바일에서 핵심 버튼과 텍스트가 잘리지 않는다.
- [ ] 로딩·빈 상태·오류 상태가 구분된다.
- [ ] 사용자가 실패 후 다시 시도할 수 있다.
- [ ] 재배포 후 기존 핵심 기능이 유지된다.

---

## Issue 6

### 제목

`[Week4][P0] Agent 워크플로우 정리와 데모데이 발표 준비`

### 본문

## 배경

이번 프로젝트의 핵심 학습은 웹 기능 개수뿐 아니라 낯선 기술에서 Agent가 만든 결과를 요구사항, 테스트와 실행 검증으로 관리한 과정이다. 금요일 데모데이에서 구현 범위와 한계를 정확히 설명한다.

## 작업

- [ ] Week 1~4 작업 흐름을 한 장으로 정리
- [ ] 요구사항 → Agent 계획 → 구현 → 테스트 → 실행 검증 사례 정리
- [ ] 삭제 기능 TDD 사례 정리
- [ ] 배포 시도에서 배운 인증·비밀키·비용 경계 정리
- [ ] 멘토링 이후 게스트 로컬 저장으로 방향을 바꾼 이유 정리
- [ ] 현재 구현·미구현 범위 구분
- [ ] PPT 최종 수정
- [ ] 발표 대본 또는 발표자 노트 작성
- [ ] 실제 공개 URL을 이용한 리허설
- [ ] 네트워크 실패를 대비한 화면 녹화 또는 캡처 준비
- [ ] 최종 데모 체크리스트 수행

## 완료 기준

- [ ] 발표 자료의 기능 설명이 실제 코드와 일치한다.
- [ ] 영구 배포 URL을 슬라이드 또는 제출 자료에서 확인할 수 있다.
- [ ] Agent가 한 일과 사용자가 판단·검증한 일을 구분해 설명한다.
- [ ] 로그인·RLS·실제 공개 AI 등 미구현 항목을 완료했다고 말하지 않는다.
- [ ] 제한 시간 안에 발표와 데모를 끝낼 수 있다.

## 발표 핵심 메시지

> Agent는 낯선 기술에서 결과를 빠르게 만들게 해주었지만, 결과에 대한 이해와 책임까지 대신 가져가지는 않는다. 이번 프로젝트에서는 요구사항과 테스트, 실제 실행 검증으로 그 경계를 관리했고, 멘토링을 통해 감정 데이터의 민감성을 제품 설계에 반영해 게스트 로컬 저장 중심으로 방향을 바꿨다.

---

## 4. 일정에 배치

| 날짜 | 핵심 작업 | 관련 이슈 |
|---|---|---|
| 월요일 | 게스트 로컬 저장, 진입·개인정보 안내 | Issue 1, Issue 2 |
| 화요일 | Render 첫 배포, 외부 URL 검증 | Issue 3 |
| 수요일 | 회귀 테스트, 모바일 확인, 영상 제출 | Issue 4 |
| 목요일 | 부족한 UI·오류 상태 수정, 재배포, 발표 정리 | Issue 5, Issue 6 |
| 금요일 | 최종 리허설과 데모데이 | Issue 6 |

---

## 5. 등록 순서

시간이 부족하면 다음 순서로 등록한다.

1. Issue 1 — 게스트 브라우저 저장
2. Issue 3 — Render 첫 배포
3. Issue 4 — 테스트와 영상 제출
4. Issue 6 — 데모데이 준비
5. Issue 2 — 개인정보 안내 UI
6. Issue 5 — 배포 후 보완

Issue 1, 3, 4, 6은 이번 주 필수다. Issue 2는 Issue 1과 함께 진행하고, Issue 5는 첫 배포 결과에 따라 세부 체크리스트를 갱신한다.

---

## 6. Zoom에서 공유할 짧은 계획

> 이번 주에는 기능을 많이 늘리기보다 게스트 기반 핵심 흐름을 완성하고 실제 URL로 배포하는 데 집중하겠습니다. 오늘은 감정 기록을 브라우저에 저장하고 저장 위치와 AI 전송 안내를 추가합니다. 화요일에는 Render에 첫 배포하고, 수요일에는 전체 흐름을 테스트해서 영상을 제출하겠습니다. 목요일에는 배포에서 발견한 오류와 모바일 UI를 보완하고, 금요일에는 Agent 활용 과정과 개인정보를 고려해 게스트 모드로 방향을 바꾼 이유를 중심으로 데모하겠습니다.
