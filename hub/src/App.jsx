:root {
  /* ==========================================================================
     1. Color Palette (색상)
     ========================================================================== */
  /* Backgrounds */
  --bg-main: #B4B1DC;          /* 가장 바깥 배경 (연보라) */
  --bg-sidebar: #A4A0D4;       /* 사이드바 배경 (약간 어두운 연보라) */
  --bg-container: #E7E6F4;     /* 내부 메인 컨테이너 배경 (밝은 라벤더 그레이) */
  --bg-card: #FFFFFF;          /* 캘린더, 리스트 등 개별 카드 배경 */

  /* Point Colors (캘린더 이벤트 등) */
  --color-primary: #7C73C5;    /* 주색상 (캘린더 선택 항목 / 버튼 / 활성화 아이콘 등) */
  --color-accent-lime: #D7FF63;/* 포인트 색상 1 (라임 - 활성화/운동/강조 등) */
  --color-accent-purple: #867EC7;/* 포인트 색상 2 (중간 보라 - 일정 블록 등) */
  --color-dark-purple: #2F295B;/* 포인트 색상 3 (다크 퍼플 - 칩 버튼 / 토글 헤더 등) */

  /* Typography Colors */
  --text-main: #2F295B;        /* 메인 타이틀, 중요한 텍스트 (다크 퍼플) */
  --text-muted: #7E79A4;       /* 보조 텍스트, 설명글, 비활성화 날짜 */
  --text-white: #FFFFFF;       /* 어두운 배경 위 텍스트 */

  /* ==========================================================================
     2. Typography (폰트)
     ========================================================================== */
  /* UI 라이브러리를 쓰지 않으므로 기본 샌스세리프 계열 지정 (Poppis, Pretendard 권장) */
  --font-family: 'Poppins', 'Pretendard', -apple-system, sans-serif;
  
  --font-size-xs: 0.75rem;     /* 12px - 캘린더 요일, 보조 스케줄 텍스트 */
  --font-size-sm: 0.875rem;    /* 14px - 일반 본문, 체크리스트 텍스트 */
  --font-size-base: 1rem;      /* 16px - 카드 제목, 네비게이션 텍스트 */
  --font-size-lg: 1.25rem;     /* 20px - 서브 섹션 타이틀 */
  --font-size-xl: 1.75rem;     /* 28px - 메인 대타이틀 (My Calendar 등) */

  --font-weight-normal: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;

  /* ==========================================================================
     3. Border Radius (모서리 곡률)
     ========================================================================== */
  --radius-sm: 8px;            /* 작은 칩 버튼, 미니 캘린더 날짜 활성화 블록 */
  --radius-md: 16px;           /* 내부 작은 카드, 일정 상세 블록, 인풋 창 */
  --radius-lg: 24px;           /* 메인 캘린더 카드, 투두 컨테이너 등 큰 카드 */
  --radius-xl: 32px;           /* 전체 메인 프레임, 사이드바를 감싸는 바깥 라운드 */
  --radius-full: 9999px;       /* 아바타, 원형 아이콘, 토글 스위치 버튼 */

  /* ==========================================================================
     4. Spacing & Padding (여백)
     ========================================================================== */
  --spacing-xs: 4px;           /* 아주 좁은 여백 (텍스트-아이콘 사이 등) */
  --spacing-sm: 8px;           /* 좁은 여백 (내부 요소 간 격리) */
  --spacing-md: 16px;          /* 일반 여백 (카드 내부 패딩, 리스트 아이템 간격) */
  --spacing-lg: 24px;          /* 넓은 여백 (큰 카드 내부 패딩) */
  --spacing-xl: 32px;          /* 섹션과 섹션 사이의 메인 마진/패딩 */
}