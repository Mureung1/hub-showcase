{
  "name": "uncoach-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "언코 라이브 프록시(api/gemini) 의존성. 풀스택 재구현은 web/ 참고.",
  "dependencies": {
    "@upstash/ratelimit": "^2.0.0",
    "@upstash/redis": "^1.34.0"
  }
}
