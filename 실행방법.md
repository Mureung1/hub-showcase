# 웹 실행하는 법 (자꾸 까먹으니까 여기 적어둠)

터미널(터미널 탭 or VSCode 하단 터미널)을 **2개** 열어야 함 — 화면(프론트)이랑 서버(백엔드)를 각각 따로 띄우는 거라서.

## 터미널 1 — 화면 띄우기
```
npm run dev
```
- 실행되면 `http://localhost:5173` 같은 주소가 나옴 → 그 주소를 브라우저에서 열면 됨

## 터미널 2 — 서버 띄우기
```
npm run dev:api
```
- 이게 안 켜져 있으면 화면은 뜨는데 레시피 추천 같은 게 안 나옴 (서버한테 데이터 물어보는 부분이라서)

## 둘 다 켰으면
브라우저에서 **http://localhost:5173** 열기 → 냉장고 화면부터 시작됨

## 끌 때
각 터미널에서 `Ctrl + C`

## 그 외 자주 쓰는 것
- `npm install` — 의존성 설치 (처음 받았을 때, 또는 새 패키지 추가된 뒤에 1번)
- `npm run lint` — 코드에 이상한 거 없는지 검사
