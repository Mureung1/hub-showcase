# 홈키퍼(HomeKeeper) 프로젝트 인수인계

> 이 문서는 새 대화 공간에서 작업을 이어가기 위한 인수인계서다.
> 이 문서를 읽은 AI는 아래 내용을 전제로 대화를 시작한다.
> 최종 업데이트: 2026-07-21 (지출 항목 CRUD + 입력 검증 완료 시점)

---

## 0. 이 문서를 읽은 AI가 가장 먼저 할 일

1. `agents/` 폴더의 모든 md 파일 내용을 참고해 작업한다 (아래 5장 참고)
2. 작업 시작 전 **환경 상태부터 확인**한다 (컴퓨터가 바뀌었을 수 있음)
3. 새 개념은 반드시 설명하고 넘어간다 (사용자는 React·백엔드 초보)
4. **요청받은 것만 만든다.** 마음대로 기능을 확장하지 않는다

---

## 1. 프로젝트 개요

**서비스명**: 홈키퍼 (HomeKeeper)
**슬로건**: "집에 나가는 돈, 이제 신경 쓰지 마세요"
**설명**: 월세·관리비·공과금부터 보증금 반환까지, 전월세 세입자가 집에 나가는 돈을 관리하는 서비스
**타겟**: 첫 자취를 시작하는 사회초년생·대학생
**정체성**: 법률 자문이 아니라 **'일정·지출 관리자 + 공식 링크 연결자'**

**가계부와의 차별점**
- 세입자 맥락(월세·관리비·보증금)을 알기 때문에, 단순 기록이 아니라 판단을 목표로 함
- 고정비는 자동, 변동비만 확인
- 계약 만료 시 돌려받을 돈까지 정산 (향후)

**부트캠프 프로젝트**이며, 매일 PR과 회고 제출이 필요하다.

---

## 2. 사용자(김동환) 정보와 작업 스타일

- **N021_김동환**, GitHub: `donghwan2928-ops`
- **React·백엔드 초보**. 새 개념은 먼저 쉬운 말로 설명하고 진행할 것
- **단계를 작게 나누고, 단계마다 본인이 확인·승인**하길 원함
- 방향 선택이 필요하면 **선택지 2~3개와 장단점을 제시**하고 사용자가 고르게 할 것
- 한 번에 여러 단계를 몰아치지 말 것
- 솔직한 피드백을 원함. 문제가 있으면 돌려 말하지 말 것
- **작업 환경이 자주 바뀐다** (노트북 ↔ 본가 데스크탑). 매번 환경 확인 필요

---

## 3. 현재 완성된 것

### 3-1. 기능 (지출 항목 관리 — CRUD 완성)

| 기능 | 방식 | 상태 |
|---|---|---|
| 등록 | POST /api/expenses | ✅ |
| 조회 | GET /api/expenses | ✅ |
| 수정 | PUT /api/expenses/:id | ✅ |
| 삭제 | DELETE /api/expenses/:id | ✅ |
| 입력 검증 | 프론트(실시간) + 서버(400 응답) | ✅ |
| 실시간 총액 | 프론트 계산 (reduce) | ✅ |

**수직슬라이스 완성**: 화면 → 서버 → DB → 화면 한 바퀴가 실제로 동작한다.
새로고침해도 데이터가 남는다.

### 3-2. 화면

- **첫 화면(계약 입력)**: 홈키퍼 브랜딩 완료(로고 "홈", 태그라인). 하단에
  "💰 집에 나가는 돈 관리하기 →" 버튼으로 지출 화면 진입
- **지출 화면(ExpenseSetup)**: 홈키퍼 로고 + "← 홈으로" + 입력폼 +
  초록 총액 카드 + 목록(🏠 아이콘, ✏️ 수정, × 삭제)

### 3-3. 테스트

- **Vitest** 설치 완료, `npm test`로 실행
- `client/src/validate.test.js` — 검증 규칙 테스트 4개 (전부 통과)
- TDD 방식으로 개발함 (실패하는 테스트 먼저 → 구현)

---

## 4. 기술 구조

```
[화면] React (Vite)     [서버] Express        [DB] Supabase
localhost:5173      →   localhost:3001    →   클라우드
```

브라우저가 DB에 직접 접근하지 않고 **서버를 경유**한다.
이유: DB 비밀 키가 브라우저에 노출되면 안 되기 때문.

### 폴더 구조

```
hub/
├─ agents/              ← Agent·Skill 문서 (아래 5장)
│  ├─ planner.md
│  ├─ verifier.md
│  ├─ kb_supabase.md
│  ├─ skill_study.md
│  └─ skill_wrapup.md
├─ client/              ← 프론트엔드 (React + Vite)
│  ├─ src/
│  │  ├─ App.jsx        ← 모든 화면이 이 한 파일에 있음
│  │  ├─ validate.js    ← 입력 검증 함수
│  │  └─ validate.test.js
│  └─ package.json
├─ server/              ← 백엔드 (Express)
│  ├─ index.js          ← 모든 API가 이 한 파일에 있음
│  ├─ .env              ← ⚠️ git에 안 올라감. 새 환경에서 다시 만들어야 함
│  └─ .gitignore
├─ App.jsx              ← ⚠️ 루트의 사본. 사용 안 함. 무시할 것
└─ prototype/           ← 초기 HTML 프로토타입
```

### Supabase 정보

- 프로젝트: `homekeeper` (Free, Seoul)
- URL: `https://xvpuvhfszvmjbuvfrvrx.supabase.co`
- 키: **Legacy service_role 키** 사용
  - 경로: Settings(⚙️) → API Keys → "Legacy anon, service_role API keys" 탭
  - ⚠️ 값은 `.env`에만. 절대 코드나 문서에 적지 않는다
- **RLS는 꺼둠** (개발 편의). 실제 배포 전에 반드시 켜고 정책 작성할 것

### expenses 테이블

| 컬럼 | 타입 | 비고 |
|---|---|---|
| id | int8 | 자동 증가 (DB가 채움) |
| name | text | 항목 이름 |
| amount | int8 | 금액 (원 단위 정수) |
| due_day | int2 | 납부일 1~31 |
| type | text | 미사용 (고정/변동 구분 예정) |
| category | text | 미사용 |
| created_at | timestamptz | 자동 기록 |

저장 시 보내는 값은 `name, amount, due_day` 3개뿐.

---

## 5. agents/ 폴더 — 반드시 참고할 것

작업할 때 이 문서들의 규칙을 적용한다.

| 파일 | 역할 |
|---|---|
| **planner.md** | 계획 담당. 작업을 반나절 Task → 30분~1시간 하위 단계로 쪼개고, 의존 순서·우선순위를 정해 백로그 형식 표로 제시 |
| **verifier.md** | 검증 담당. 정상/비정상 흐름(빈 값·0개·서버 꺼짐·중복)으로 나눠 통과/실패 판정표 작성. 고치지는 않음 |
| **kb_supabase.md** | Supabase 정보 지식베이스 |
| **skill_study.md** | 학습 방식. 새 개념은 "왜 필요한가 → 비유 → 우리 코드에서 → 넘어갈 것 → 확인 질문" 순서로 설명. **마음대로 기능 확장 금지** |
| **skill_wrapup.md** | 하루 마무리 절차. ①push·PR ②회고(잘한일/아쉬운일/다시한다면) ③작업정리(오늘 한 것/어려웠던 것/내일 할 것). **각 단계마다 사용자에게 확인 받고 진행** |

---

## 6. 개발 환경 — 실행 방법

### 새 환경에서 시작할 때

```bash
# 1. 코드 받기
git clone https://github.com/donghwan2928-ops/hub.git
cd hub
git switch N021_김동환        # ⚠️ 작업물은 이 브랜치에 있음. main에는 없음

# 2. 부품 설치 (두 폴더 각각)
cd client && npm install
cd ../server && npm install

# 3. .env 다시 만들기 (git에 없으므로)
# server/.env 파일 생성 후 아래 내용 입력
# SUPABASE_URL=https://xvpuvhfszvmjbuvfrvrx.supabase.co
# SUPABASE_KEY=(Supabase 대시보드에서 service_role 키 복사)
```

### 매번 작업 시작할 때 (터미널 2개 필요)

```bash
# 터미널 1 — 서버
cd hub/server
node index.js
# → "서버 실행 중: http://localhost:3001"

# 터미널 2 — React
cd hub/client
npm run dev
# → "Local: http://localhost:5173"
```

**둘 다 켜져 있어야** 저장·조회가 작동한다.

### 테스트 실행

```bash
cd hub/client
npm test
```

### 커밋·푸시

```bash
git add .
git commit -m "메시지"
git push origin N021_김동환
```

PR: compare 브랜치는 **반드시 `N021_김동환`** (main 아님)

---

## 7. ⚠️ 반드시 알아야 할 함정들 (실제로 겪은 것)

### 7-1. "코드를 썼다"의 네 가지 상태

이것들이 **모두 다르며 각각 확인해야 한다**:

```
코드를 썼다 ≠ 파일에 저장됐다 ≠ 의도한 위치에 들어갔다 ≠ 서버에 반영됐다
```

실제로 겪은 사례:
- DELETE API가 `Cannot DELETE` 에러 → **코드가 파일에 아예 없었음**
- 수정 버튼(✏️)을 눌렀는데 삭제 확인창이 뜸 → **삭제 버튼 태그 안쪽에 중첩**되어 있었음
- 서버 검증이 안 걸림 → 코드는 정확했으나 **서버를 재시작하지 않음**
- App.jsx 코드가 통째로 사라짐 → **Ctrl+Z가 여러 번 눌림**

**대응**: 코드를 붙여넣은 뒤 `Ctrl+F`로 검색해 실제로 있는지 확인하고,
서버 코드라면 재시작까지 마친 뒤 다음으로 넘어간다.

### 7-2. 서버는 재시작해야 반영된다

React(`npm run dev`)는 저장하면 자동 반영되지만,
**Express 서버는 `Ctrl+C` → `node index.js`로 재시작**해야 코드가 반영된다.

### 7-3. 환경이 바뀌기 전에는 반드시 push

노트북에서 작업하고 push하지 않아, 다음 날 본가에서 처음부터 다시 만든 적이 있다.
**작업이 끝나면 무조건 push한다.**

### 7-4. .eq() 없는 delete/update는 위험

```javascript
.delete().eq("id", id)   // ✅ 해당 항목만
.delete()                // ❌ 전체 삭제됨
```

### 7-5. 에러 메시지가 가리키는 줄이 진짜 원인이 아닐 수 있다

에러는 68번 줄을 가리켰으나, 실제 원인은 51번 줄의 `return` 뒤 여는 괄호 누락이었다.
(자바스크립트 자동 세미콜론 삽입) → 에러 위치 **위아래를 함께 볼 것**

### 7-6. clone하면 기본 브랜치만 온다

작업물은 `N021_김동환` 브랜치에 있다. clone 후 `git switch N021_김동환` 필수.

---

## 8. 다음에 할 일

### 계획된 핵심 기능 3가지

| # | 기능 | 사용자에게 하는 약속 | 상태 |
|---|---|---|---|
| **A** | 지출 항목 관리 | 고정비를 등록하고 관리한다 | ✅ 완료 |
| **B** | 납부 챙김 | 언제 뭘 내야 하는지 알려준다 | ⬜ **다음 작업** |
| **C** | 납부 기록 | 냈는지 안 냈는지 기록한다 | ⬜ |

### B. 납부 챙김 (다음 작업)

**BE 작업 없음** — 날짜 계산은 순수 함수라 프론트에서 처리 가능.
순수 함수이므로 **TDD로 개발하기 적합**.

- D-day 계산 함수 (`getDaysUntil(due_day)`)
- "3일 뒤" 임박 뱃지 표시
- 임박순 정렬
- 이번 달 남은 지출 합계

### C. 납부 기록 (그 다음)

- DB: `payments` 테이블 신규 (expense_id, paid_at, amount)
- BE: POST/GET /api/payments
- FE: "냈어요" 버튼, 납부 완료 표시

### 부트캠프 요구사항 중 남은 것

- **아키텍처 시각화**: mermaid로 화면·서버·DB 구조와 데이터 흐름을 그려 README에 추가
- **이슈 등록 흐름**: 작업을 작게 나눠 GitHub 이슈로 등록한 뒤 개발
- **테스트 확대**: 현재 검증 함수만 테스트됨. 다른 기능으로 확대

---

## 9. 미해결·보류 사항

- **DB에 쓰레기 데이터 1건**: 서버 검증 테스트 중 curl로 들어간 항목
  (id 18, 이름 비어있음, -5000원, 납부일 99). 화면에서 × 버튼으로 삭제하면 됨
- **계약/타임라인 화면들**: 예전 "보증금 타임키퍼" 시절 흐름. 지금 지출 관리와
  연결되어 있지 않음. 첫 화면에 버튼 하나로만 붙어 있는 상태.
  **당분간 건드리지 않고 나중에 정리**하기로 함
- **루트의 App.jsx**: `client/src/App.jsx`의 사본. 사용하지 않음
- **검증 함수 중복**: `client/src/validate.js`와 `server/index.js`에 같은 내용이
  각각 있음. 공유 방법을 배우면 정리 예정
- **RLS 꺼짐**: 배포 전 반드시 켜고 정책 작성 필요
- **에러 메시지 잔상**: 실시간 검증으로 대부분 해결되었으나, 엣지 케이스 미확인

---

## 10. 하루 마무리 절차

사용자가 "마무리하자"고 하면 `agents/skill_wrapup.md`대로 진행한다.

1. **push·PR** (`.env`가 목록에 없는지 확인 필수, PR은 팀 양식으로)
2. **회고**: [오늘 잘한 일] [아쉬운 일] [다음에 다시 한다면?]
3. **작업 정리**: 오늘 한 것 / 어려웠던 것·중요한 것 / 내일 할 것

**각 단계가 끝나면 사용자에게 다음으로 넘어갈지 물어본다.**

### PR 본문 양식 (팀 규칙)

```
## 완료한 작업 목록
## 설계한 내용
## 가장 막혔던 순간
## 새로 알게 된 것
## 다르게 한다면
```
