# 데이터 모델 설계 — 상권 스캐너

서비스에 필요한 데이터를 정리하고, 테이블과 컬럼을 설계한 문서입니다.
현재 스택인 MySQL 기준으로 작성했습니다.

## 어떤 데이터가 필요한가

서비스 흐름을 따라가며 필요한 데이터를 정리했습니다.

```
내 가게 이름 입력
  → 내 가게 정보 (이름, 위치, 업종)          ── my_stores
  → 주변 경쟁업체 목록 (이름, 거리, 위치)      ── competitors
  → 각 경쟁업체의 리뷰 (원문, 감성, 키워드)    ── reviews
  → 각 경쟁업체의 분석 리포트 (전략 요약)       ── reports
```

크게 네 종류의 데이터가 있습니다: 내 가게, 경쟁업체, 리뷰, 리포트입니다.

## 테이블 설계

### 1. my_stores — 내 가게

사용자가 입력한 "내 가게" 정보를 저장합니다. 위치(좌표)를 저장해야 주변 경쟁업체를 거리 계산으로 찾을 수 있습니다.

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | BIGINT (PK) | 고유 번호 |
| name | VARCHAR(255) | 내 가게 이름 |
| latitude | DOUBLE | 위도 |
| longitude | DOUBLE | 경도 |
| category | VARCHAR(100) | 업종 (예: 카페, 음식점) |
| address | TEXT | 주소 |
| created_at | TIMESTAMP | 등록 시각 |

### 2. competitors — 경쟁업체

주변에서 검색된 경쟁업체를 저장합니다. 어느 내 가게를 기준으로 찾은 경쟁업체인지 연결해야 하므로 my_store_id로 묶습니다.

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | BIGINT (PK) | 고유 번호 |
| my_store_id | BIGINT (FK) | 어느 내 가게 기준인지 (my_stores.id) |
| name | VARCHAR(255) | 경쟁업체 이름 |
| latitude | DOUBLE | 위도 |
| longitude | DOUBLE | 경도 |
| distance | DOUBLE | 내 가게로부터 거리 (km) |
| address | TEXT | 주소 |
| category | VARCHAR(100) | 업종 |
| created_at | TIMESTAMP | 검색 시각 |

### 3. reviews — 리뷰

각 경쟁업체의 리뷰 원문과 분석 결과를 저장합니다. 어느 경쟁업체의 리뷰인지 competitor_id로 연결합니다.

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | BIGINT (PK) | 고유 번호 |
| competitor_id | BIGINT (FK) | 어느 경쟁업체 리뷰인지 (competitors.id) |
| content | TEXT | 리뷰 원문 |
| sentiment | VARCHAR(10) | 감성 (긍정 / 부정) |
| keywords | JSON | 키워드 목록 (예: ["맛", "친절"]) |
| summary | TEXT | 한 줄 요약 |
| analyzed_at | TIMESTAMP | 분석 시각 |
| created_at | TIMESTAMP | 저장 시각 |

### 4. reports — 분석 리포트

각 경쟁업체에 대한 AI 전략 리포트를 저장합니다. 리뷰를 종합한 결과라 경쟁업체당 하나씩 생깁니다. (지금은 매번 새로 생성하지만, 저장해두면 재조회 시 LLM 호출을 줄일 수 있습니다.)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | BIGINT (PK) | 고유 번호 |
| competitor_id | BIGINT (FK) | 어느 경쟁업체 리포트인지 (competitors.id) |
| positive_count | INT | 긍정 리뷰 수 |
| negative_count | INT | 부정 리뷰 수 |
| positive_ratio | DOUBLE | 긍정 비율 (%) |
| report_text | TEXT | AI 전략 리포트 본문 |
| created_at | TIMESTAMP | 생성 시각 |

## 테이블 관계

```
my_stores (내 가게)
    │ 1
    │
    │ N
competitors (경쟁업체) ────┐
    │ 1                    │ 1
    │                      │
    │ N                    │ 1
reviews (리뷰)         reports (리포트)
```

- 내 가게 하나에 경쟁업체 여러 개 (1:N)
- 경쟁업체 하나에 리뷰 여러 개 (1:N)
- 경쟁업체 하나에 리포트 하나 (1:1)

## 설계 시 고려한 점

**위치 정보를 좌표(위도/경도)로 저장하는 이유** — 주소 텍스트만으로는 거리 계산이 안 됩니다. Haversine 공식으로 거리를 구하려면 숫자 좌표가 필요하므로 latitude, longitude를 따로 둡니다.

**keywords를 JSON 타입으로 저장하는 이유** — 키워드는 개수가 정해져 있지 않고 여러 개입니다. 이걸 별도 테이블로 빼면 복잡해지므로, MySQL의 JSON 타입에 배열째로 저장합니다.

**리포트를 리뷰와 분리한 이유** — 리뷰는 개별 데이터이고 리포트는 그것들을 종합한 결과입니다. 성격이 다르고, 리포트만 따로 캐싱해서 재사용할 수 있도록 분리했습니다.

## 다음 단계

- 이 설계를 SQLAlchemy 모델(database.py)에 반영
- 기존 Competitor, Review 모델에 my_stores, reports 테이블 추가
- 실제 데이터를 저장·조회하며 관계가 잘 작동하는지 확인
