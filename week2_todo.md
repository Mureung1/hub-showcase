# Week 2 Todo

## 이번 주 목표

3주 개발계획 기준 2주차 목표는 10단계 중 4~8단계를 최대한 끝내는 것이다.

- 4단계: 손님이 자기 회원번호, QR, 스탬프 보기
- 5단계: 사장님이 매장 스캐너 또는 수동 입력으로 손님 찾기
- 6단계: 사장님이 조회 결과를 확인하고 스탬프 1개 적립하기
- 7단계: 손님 화면에서 바뀐 결과 확인하기
- 8단계: 쿠폰과 알림 화면 붙이기

이번 주가 끝나면 손님이 QR을 보여주고, 사장님이 QR 값 또는 회원번호로 손님을 조회하고, 스탬프를 적립하며, 목표 달성 시 쿠폰과 알림이 생성되고, 손님 화면에서 최신 결과를 확인할 수 있어야 한다.

Week 3에는 새 기능을 크게 추가하기보다 기능 검증, 오류 처리, 화면 보정, 배포, 시연 준비에 집중할 수 있도록 이번 주에 핵심 기능을 가능한 한 많이 끝낸다.

## 이번 주 시작 전제

Week 1에서 아래 작업은 이미 끝난 것으로 본다.

- React Router 기본 라우팅
- `/login`, `/customer`, `/owner` 화면 연결
- Supabase 클라이언트 연결
- 기본 테이블과 seed/demo 데이터 준비
- 이메일/비밀번호 로그인, 로그아웃, 세션 복원
- 역할별 화면 이동과 접근 보호
- 손님 기본 회원번호와 카페별 스탬프 조회
- 사장님 본인 카페 조회

## 금요일 완료 상태

- 손님 화면에 실제 QR이 표시된다.
- QR 값은 `cafe-stamp:<회원번호>` 형식이다.
- 사장님 화면에서 QR 값 또는 회원번호를 입력해 고객을 조회할 수 있다.
- 입력창은 화면 진입 후 자동 포커스된다.
- 스캐너처럼 값이 입력되고 Enter가 들어오면 고객 조회가 실행된다.
- 조회만으로는 적립되지 않는다.
- 사장님이 조회 결과를 확인한 뒤 버튼을 눌러 스탬프를 1개 적립할 수 있다.
- 적립 중 중복 클릭이 막힌다.
- 일반 적립이면 스탬프가 1개 증가하고 알림이 1개 생성된다.
- 목표 달성 적립이면 쿠폰 1장이 발급되고 스탬프가 0으로 초기화되며 알림 2개가 생성된다.
- 손님 화면 새로고침 후 사장님 처리 결과와 같은 데이터가 보인다.
- 손님 쿠폰 화면에서 발급된 쿠폰을 확인할 수 있다.
- 손님 알림 화면에서 적립/쿠폰 발급 알림을 확인할 수 있다.
- `npm run verify`가 통과한다.

## GitHub Issue로 등록할 작업

### Issue 1. 손님 회원번호 QR 표시

**관련 단계:** 4단계. 손님이 자기 회원번호, QR, 스탬프 보기

**무엇을 하나?**

손님 화면에 실제 QR을 표시한다. QR에는 회원번호 자체가 아니라 `cafe-stamp:<회원번호>` 형식의 문자열을 넣는다.

**어떻게 하나?**

- QR 표시 라이브러리를 추가한다.
- 회원번호에서 QR 값을 만드는 작은 유틸 함수를 만든다.
- 손님 화면의 QR placeholder를 실제 QR로 바꾼다.
- QR 값은 DB에 따로 저장하지 않고 `customers.member_number`에서 생성한다.
- QR 아래에 사람이 읽을 수 있는 회원번호도 유지한다.

**기능으로 어떻게 연결되나?**

- 손님은 매장에서 QR을 보여줄 수 있다.
- 사장님 화면은 이 QR 값을 입력받아 고객 조회에 사용할 수 있다.
- QR은 적립 실행이 아니라 고객 조회를 위한 입력값 역할만 한다.

**완료 기준**

- 손님 화면에 실제 QR이 보인다.
- QR 값이 `cafe-stamp:<회원번호>` 형식이다.
- 회원번호가 바뀌면 QR 값도 함께 바뀐다.
- QR 값을 별도 DB 필드에 저장하지 않는다.
- `npm run verify`가 통과한다.

### Issue 2. 사장님 회원번호/QR 입력과 파싱

**관련 단계:** 5단계. 사장님이 매장 스캐너 또는 수동 입력으로 손님 찾기

**무엇을 하나?**

사장님 화면에 회원번호 또는 QR 값을 입력하는 입력창을 만들고, `cafe-stamp:` 접두어가 있는 값을 회원번호로 파싱한다.

**어떻게 하나?**

- 사장님 화면에 입력창과 조회 버튼을 추가한다.
- 화면 진입 시 입력창에 자동 포커스를 준다.
- 입력값 앞뒤 공백을 제거한다.
- `cafe-stamp:<회원번호>` 형식이면 접두어를 제거한다.
- 접두어 없이 회원번호만 입력해도 조회할 수 있게 한다.
- 잘못된 QR 형식이면 명확한 오류를 표시한다.
- Enter 입력으로 조회 버튼과 같은 흐름을 실행한다.

**기능으로 어떻게 연결되나?**

- 실제 매장 스캐너가 키보드처럼 값을 입력하고 Enter를 보내는 흐름을 지원한다.
- 스캐너가 없을 때도 사장님이 회원번호를 직접 입력해 같은 기능을 쓸 수 있다.
- 이후 고객 조회와 스탬프 적립의 시작점이 된다.

**완료 기준**

- 입력창에 `cafe-stamp:C-1001`을 넣으면 `C-1001`로 파싱된다.
- 입력창에 `C-1001`만 넣어도 조회 준비가 된다.
- 빈 값은 조회되지 않고 오류가 보인다.
- 잘못된 접두어 값은 QR 오류로 안내된다.
- Enter로 조회가 실행된다.
- 조회 후 입력창에 다시 포커스가 돌아온다.

### Issue 3. 사장님 고객 조회 기능

**관련 단계:** 5단계. 사장님이 매장 스캐너 또는 수동 입력으로 손님 찾기

**무엇을 하나?**

파싱된 회원번호로 고객을 조회하고, 사장님이 담당한 카페에서의 현재 스탬프 상태를 보여준다.

**어떻게 하나?**

- 파싱된 회원번호로 `customers`를 조회한다.
- 로그인한 사장님의 `profiles.cafe_id`를 기준으로 해당 카페의 `stamp_cards`를 조회한다.
- 고객 이름, 회원번호, 현재 스탬프, 목표 스탬프, 보상명을 표시한다.
- 조회 중 로딩 상태를 보여준다.
- 고객 없음 오류를 보여준다.
- 조회 결과가 바뀌면 이전 적립 결과 메시지를 초기화한다.

**기능으로 어떻게 연결되나?**

- 사장님이 적립 전에 고객이 맞는지 확인할 수 있다.
- QR 스캔이 바로 적립으로 이어지지 않고 안전한 확인 단계가 생긴다.
- 다음 Issue의 스탬프 적립 버튼 활성화 조건이 된다.

**완료 기준**

- 존재하는 회원번호로 고객이 조회된다.
- 없는 회원번호는 오류를 보여준다.
- 고객 이름과 현재 스탬프가 보인다.
- 사장님 카페 기준의 스탬프만 보인다.
- 조회만으로 스탬프는 증가하지 않는다.

### Issue 4. 스탬프 적립 RPC 만들기

**관련 단계:** 6단계. 사장님이 조회 결과를 확인하고 스탬프 1개 적립하기

**무엇을 하나?**

스탬프 1개 적립, 쿠폰 자동 발급, 알림 생성을 하나의 안전한 DB 작업으로 처리하는 Supabase RPC를 만든다.

**어떻게 하나?**

- 로그인 사용자가 사장님인지 확인한다.
- `profiles`에서 사장님의 `cafe_id`를 결정한다.
- 클라이언트가 임의로 보낸 `cafe_id`는 사용하지 않는다.
- 회원번호로 고객을 찾는다.
- 해당 고객과 사장님 카페의 `stamp_cards`를 조회하거나 없으면 0개로 생성한다.
- 일반 적립이면 `stamp_count`를 1 증가시킨다.
- `stamp_goal - 1` 상태에서 적립하면 쿠폰을 발급하고 스탬프를 0으로 초기화한다.
- 일반 적립 알림을 생성한다.
- 쿠폰 발급 시 쿠폰 알림도 생성한다.
- 실패하면 전체 작업이 롤백되게 한다.
- 처리 결과를 사장님 화면에서 사용할 수 있게 반환한다.

**기능으로 어떻게 연결되나?**

- 핵심 MVP인 "사장님이 스탬프를 적립한다"가 실제 데이터 변경으로 연결된다.
- 쿠폰과 알림이 적립 결과로 자동 생성된다.
- 손님 화면 새로고침 시 같은 결과를 확인할 수 있다.

**완료 기준**

- 일반 적립 시 스탬프가 정확히 1 증가한다.
- 목표 달성 적립 시 쿠폰 1장이 생성되고 스탬프가 0이 된다.
- 일반 적립 알림이 생성된다.
- 쿠폰 발급 시 알림이 2개 생성된다.
- 다른 카페 스탬프는 변경되지 않는다.
- 권한 없는 사용자의 요청은 실패한다.

### Issue 5. 사장님 적립 버튼과 결과 표시

**관련 단계:** 6단계. 사장님이 조회 결과를 확인하고 스탬프 1개 적립하기

**무엇을 하나?**

사장님 화면에서 조회된 고객에게 스탬프를 적립하는 버튼과 처리 결과 메시지를 연결한다.

**어떻게 하나?**

- 고객 조회가 성공했을 때만 적립 버튼을 활성화한다.
- 버튼 클릭 시 적립 RPC를 호출한다.
- 처리 중 버튼을 비활성화한다.
- 일반 적립 결과와 쿠폰 발급 결과를 다르게 표시한다.
- 적립 성공 후 고객 조회 결과의 스탬프 값을 최신 값으로 갱신한다.
- 성공 또는 실패 후 입력창 포커스를 복원한다.

**기능으로 어떻게 연결되나?**

- 사장님이 실제 매장에서 고객 확인 후 적립을 완료할 수 있다.
- 중복 클릭으로 스탬프가 여러 번 찍히는 위험을 줄인다.
- 처리 결과가 바로 보여서 사장님이 손님에게 안내할 수 있다.

**완료 기준**

- 조회 전에는 적립 버튼을 누를 수 없다.
- 처리 중 중복 클릭이 막힌다.
- 일반 적립 성공 메시지가 보인다.
- 쿠폰 발급 성공 메시지가 보인다.
- 실패 시 오류가 보이고 중간 상태가 남지 않는다.

### Issue 6. 손님 화면 새로고침 반영

**관련 단계:** 7단계. 손님 화면에서 바뀐 결과 확인하기

**무엇을 하나?**

사장님이 처리한 적립 결과가 손님 화면 새로고침 후 같은 데이터로 보이도록 확인하고 필요한 갱신 흐름을 정리한다.

**어떻게 하나?**

- 손님 홈 데이터 조회 함수를 재사용 가능하게 정리한다.
- 새로고침 버튼 또는 화면 재진입 시 최신 데이터를 다시 가져온다.
- 일반 적립 후 스탬프 증가가 보이는지 확인한다.
- 쿠폰 발급 후 스탬프 0이 보이는지 확인한다.
- 다른 카페 스탬프가 바뀌지 않는지 확인한다.

**기능으로 어떻게 연결되나?**

- 사장님 화면과 손님 화면이 같은 Supabase 데이터를 보고 있음을 증명한다.
- MVP 시연의 핵심 장면인 "적립 후 손님 화면 확인"이 가능해진다.

**완료 기준**

- 일반 적립 후 손님 화면 새로고침 시 스탬프가 증가한다.
- 쿠폰 발급 후 손님 화면 새로고침 시 스탬프가 0이 된다.
- 적립하지 않은 다른 카페 스탬프는 그대로다.
- 로딩/오류 상태가 어색하지 않게 표시된다.

### Issue 7. 손님 쿠폰 목록 연결

**관련 단계:** 8단계. 쿠폰과 알림 화면 붙이기

**무엇을 하나?**

손님 쿠폰 화면에서 Supabase에 발급된 쿠폰 목록을 보여준다.

**어떻게 하나?**

- 로그인한 손님의 `customer_id`를 기준으로 쿠폰을 조회한다.
- 쿠폰과 카페 정보를 함께 표시한다.
- 쿠폰명, 카페명, 발급일, 바코드 번호를 표시한다.
- `issued` 상태 쿠폰을 기본 표시한다.
- 쿠폰 사용 버튼이나 만료 처리는 넣지 않는다.
- 빈 상태, 로딩 상태, 오류 상태를 만든다.

**기능으로 어떻게 연결되나?**

- 목표 달성 적립으로 발급된 쿠폰을 손님이 확인할 수 있다.
- Week 3 시연에서 쿠폰 발급 결과를 별도 화면으로 보여줄 수 있다.

**완료 기준**

- 발급된 쿠폰 목록이 보인다.
- 쿠폰이 없으면 빈 상태가 보인다.
- 쿠폰 사용/만료 기능은 없다.
- 다른 손님의 쿠폰은 보이지 않는다.

### Issue 8. 손님 알림 목록 연결

**관련 단계:** 8단계. 쿠폰과 알림 화면 붙이기

**무엇을 하나?**

손님 알림 화면에서 스탬프 적립 알림과 쿠폰 발급 알림을 최신순으로 보여준다.

**어떻게 하나?**

- 로그인한 손님의 `customer_id`를 기준으로 알림을 조회한다.
- 최신 알림이 위에 오도록 정렬한다.
- 알림 메시지와 생성 시간을 표시한다.
- 읽음 처리나 푸시 알림은 만들지 않는다.
- 빈 상태, 로딩 상태, 오류 상태를 만든다.

**기능으로 어떻게 연결되나?**

- 사장님 적립 결과가 손님에게 기록으로 남는다.
- 쿠폰 발급 시 알림 2개가 생기는 요구사항을 화면에서 확인할 수 있다.

**완료 기준**

- 일반 적립 후 알림 1개가 보인다.
- 쿠폰 발급 후 적립 알림과 쿠폰 알림이 보인다.
- 최신순으로 정렬된다.
- 다른 손님의 알림은 보이지 않는다.

### Issue 9. 2주차 핵심 흐름 검증

**관련 단계:** 4~8단계 완료 검증

**무엇을 하나?**

이번 주에 만든 핵심 흐름이 실제로 이어지는지 확인한다.

**어떻게 하나?**

- 손님 계정으로 로그인해 QR과 현재 스탬프를 확인한다.
- 사장님 계정으로 로그인해 QR 값 또는 회원번호로 고객을 조회한다.
- 일반 적립을 실행한다.
- 손님 화면 새로고침 후 스탬프 증가를 확인한다.
- 목표 직전 상태에서 적립해 쿠폰 발급을 확인한다.
- 손님 쿠폰 화면에서 쿠폰을 확인한다.
- 손님 알림 화면에서 알림을 확인한다.
- 잘못된 회원번호, 잘못된 QR 형식, 빈 입력 오류를 확인한다.
- `npm run verify`를 실행한다.

**기능으로 어떻게 연결되나?**

- Week 3를 기능 추가가 아니라 검증, 보정, 배포 준비 중심으로 쓸 수 있게 만든다.
- MVP 핵심 루프가 실제 시연 가능한 상태인지 판단한다.

**완료 기준**

- QR 조회 흐름이 동작한다.
- 수동 회원번호 조회 fallback이 동작한다.
- 조회만으로는 적립되지 않는다.
- 일반 적립이 동작한다.
- 쿠폰 자동 발급이 동작한다.
- 손님 화면에서 최신 결과가 확인된다.
- `npm run verify`가 통과한다.

## GitHub Project 보드 추천 컬럼

- Todo
- In Progress
- Review / Check
- Done

처음에는 모든 이슈를 Todo에 두고, 작업하는 이슈만 In Progress로 옮긴다. 구현이 끝나면 Review / Check로 옮겨 직접 시나리오를 확인하고, 완료 기준을 만족하면 Done으로 옮긴다.
