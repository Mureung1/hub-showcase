# 개발 TASK — 2주차

> 대상: 이력 기반 AI 채용 매칭 서비스 (MVP)
> 이번 주 테마: **데이터 확보 + 적합도 매칭 코어 + 포지션 목록 화면**
> 관련 문서: `기획서.md`(F3·F4·4장), `API설계서.md`(§5·§6·§9), `디자인.md`(포지션 목록)

---

## 이번 주 목표

**"김서준 페르소나로 로그인하면, 내 이력 기준 적합도 순으로 정렬된 포지션 목록이 뜬다."**

이 서비스의 핵심 차별점인 매칭 로직(기획서 4장)을 실제로 동작시키는 주간. 정확한 수치보다 **"대략 맞는 순서"**가 목표다(기획서 4.3).

---

## 4주 전체 맵 (맥락용)

| 주차 | 테마 | 핵심 |
|:---:|------|------|
| 1주차 | 기반 구축 | 셋업 · DB 스키마 · F1 인증 · F2 이력 관리 |
| **2주차** | **데이터 + 매칭 코어** | **F3 수집 · 요구조건 추출 · F4 매칭·랭킹 · 목록 화면** |
| 3주차 | AI 기능 | F5 상세+방향 · F6 문서 생성 · F7 지원 연결 |
| 4주차 | 마감 | 디자인 적용 · 반응형/접근성 · 배포 · 데모 |

---

## 사전 조건 (1주차에서 끝나 있어야 함)

- [ ] DB에 `JobPosting`, `JobRequirement`, `MatchScore`, `Job` 테이블 생성됨
- [ ] pgvector 확장 설치 (`CREATE EXTENSION vector`)
- [ ] LLM API 키 발급 + 호출 래퍼 함수 준비
- [ ] 로그인 상태에서 내 `Credential`(이력) 조회 가능 (F2)

> 하나라도 안 됐으면 여기부터 메꾸고 시작. 특히 pgvector와 LLM 호출은 이번 주 전제.

---

## 태스크

효과 표기: `[S]` 1~2시간 · `[M]` 반나절 · `[L]` 하루+

### A. 포지션 데이터 확보 (F3)

- [ ] **A-1** 데이터 출처 결정 `[S]`
  - 크롤링 대신 **시드 데이터**로 시작 권장 (기획서 8장 법적 리스크). 공고 원문을 수동으로 모은 JSON 10~30건.
  - 검증 대상 **직군 1개로 고정** (예: 백엔드) — 기획서 4.3 강조사항
- [ ] **A-2** 적재 엔드포인트 구현 `[M]`
  - `POST /internal/positions/ingest` (API설계서 §9.1)
  - 저장 필드: company, title, job_category, location, source_url, raw_description
- [ ] **A-3** 시드 스크립트로 10~30건 적재 `[S]`
  - 완료 기준: `SELECT count(*) FROM job_posting` ≥ 10

### B. 요구조건 추출 (LLM)

- [ ] **B-1** 추출 프롬프트 설계 `[M]`
  - 입력: 공고 원문 → 출력: `[{text, is_required, weight}]` (JSON만 반환하도록 지시)
  - 가중치 추정 근거: 필수 vs 우대, 언급 강조 정도 (기획서 4.1)
- [ ] **B-2** 추출 엔드포인트 구현 `[M]`
  - `POST /internal/positions/{id}/extract-requirements` (API설계서 §9.2)
  - MVP는 **동기 처리**로 시작해도 됨 (비동기는 C-5에서)
  - 결과를 `JobRequirement`로 저장
- [ ] **B-3** 추출 결과 수동 검수 `[S]`
  - 공고 3~5건 눈으로 확인 → 가중치·필수여부가 말이 되는지

### C. 적합도 매칭·랭킹 (F4) — 이번 주 핵심

- [ ] **C-1** 임베딩 파이프라인 `[M]`
  - 이력(Credential) + 요구조건(JobRequirement) 텍스트 → 임베딩 → pgvector 저장
- [ ] **C-2** 충족도(0~1) 계산 `[L]`
  - 요구조건 ↔ 이력 간 의미 유사도(임베딩) + 필요 시 LLM 판단 보정
  - 기획서 4.2 예시처럼 요구조건별 `fulfillment` 산출
- [ ] **C-3** 점수 합산 & 저장 `[M]`
  - `점수 = Σ(가중치 × 충족도)` → `MatchScore`에 저장 (score, breakdown JSONB, computed_at)
  - 이력 변경 시 stale 처리 로직 골격만 (실제 재계산은 C-5)
- [ ] **C-4** 목록 API `[M]`
  - `GET /positions?sort=match_score:desc` (API설계서 §5.1)
  - 응답에 `match.score`, `match.status`, `matched_count` 포함
- [ ] **C-5** 비동기 잡 기초 `[M]`
  - `POST /matches/recompute` → 202 job_id, `GET /jobs/{job_id}` 폴링 (API설계서 §6.2, §1.4)
  - MVP 풀이 작으니 잡 안에서 동기 계산해도 무방

### D. 포지션 목록 화면 (프론트)

- [ ] **D-1** 목록 화면 API 연동 `[M]`
  - `GET /positions` 결과를 적합도 순으로 렌더
- [ ] **D-2** 적합도 미터 + 요구조건 배지 `[M]`
  - `포지션목록_디자인예시.html` 컴포넌트 이식 (미터 색·요구조건 눈금)
- [ ] **D-3** 상태 처리 `[S]`
  - `match.status`가 `computing`이면 로딩 표시, `ready`면 점수 표시
  - 이력 0건이면 매칭 없이 안내 문구

### E. 검증 (Sanity check)

- [ ] **E-1** 김서준 페르소나로 end-to-end 확인 `[S]`
  - 로그인 → 목록이 적합도 순으로 뜨는지
  - 순위가 **"대략 맞는지"** 눈으로 판단 (완벽한 수치 X)
- [ ] **E-2** 명백히 안 맞는 케이스 1~2개 점검 `[S]`
  - 이력과 전혀 다른 포지션이 상위에 뜨면 프롬프트/유사도 임계값 조정

---

## 이번 주 완료 기준 (Definition of Done)

- [ ] 시드 포지션 10건+ 적재됨
- [ ] 각 포지션에 요구조건이 구조화되어 저장됨
- [ ] `GET /positions`가 로그인 사용자 기준 적합도 순으로 응답
- [ ] 프론트 목록 화면에서 점수 + 미터 + 요구조건 배지가 보임
- [ ] 김서준 기준 순위가 상식적으로 납득됨

---

## 산출물

- 동작하는 `GET /positions` (적합도 정렬)
- 요구조건 추출 프롬프트 (별도 파일로 버전 관리 권장)
- 적합도 계산 모듈
- 연동된 포지션 목록 화면

---

## 주의 · 리스크

- **매칭 품질이 이 서비스의 성패**(기획서 4장 경고). 이번 주에 "완벽"을 노리지 말 것. 순위가 대략 맞으면 통과, 세밀 조정은 사용자 피드백 이후로.
- **LLM 비용**: 요구조건 추출·충족도 판단에서 토큰이 쌓인다. 시드 적재 시 한 번만 추출하고 결과를 저장해 재호출을 막을 것.
- **직군 확장 금지**: 이번 주는 한 직군만. 여러 직군을 섞으면 정확도 검증이 어려워진다.
- **크롤링 유혹 주의**: 시간 아끼려 실사이트 크롤링 붙이지 말 것 — 약관/법적 리스크(기획서 8장). MVP는 시드로 충분.

---

## 다음 주(3주차) 예고

이번 주에 만든 `MatchScore.breakdown`을 그대로 활용해:
- F5 포지션 상세 + 요구조건별 분해 + **방향 제시**(LLM guidance)
- F6 AI 이력서·자소서 생성
- F7 외부 지원 연결
