---
name: tdd-workflow
description: 기능 요구사항을 테스트 시나리오로 변환하고 Red, Green, Refactor 순서로 구현과 검증을 반복할 때 사용한다. 새로운 계산 함수, 입력 검증, 정렬, 상태 변경 로직을 테스트 우선 방식으로 개발하거나 기존 기능에 회귀 테스트를 추가할 때 적용한다.
---

# TDD Workflow

## 1. 준비

1. 사용자 요구사항과 기존 코드를 확인한다.
2. 테스트 도구, 테스트 명령, 파일 작성 방식을 확인한다.
3. 불명확한 입력, 경계값, 예외 동작은 구현 전에 사용자에게 질문한다.
4. 요구사항을 입력과 기대 결과 형태의 테스트 시나리오로 정리한다.

## 2. Red

1. 실제 기능 구현보다 테스트를 먼저 작성한다.
2. 정상 입력, 경계값, 무관한 입력, 반올림 또는 정렬 기준을 포함한다.
3. 기능 코드는 구현하지 않거나 `Not implemented`를 발생시키는 최소 스텁만 둔다.
4. 대상 테스트만 실행한다.
5. 테스트가 요구사항 불일치 또는 구현 누락 때문에 실패하는지 확인한다.
6. 문법 오류, 잘못된 import, 테스트 환경 오류로 실패하면 Red로 인정하지 않고 먼저 고친다.
7. 실패한 테스트 파일 수, 테스트 수, 실패 원인을 기록한다.

## 3. Green

1. 테스트를 통과하는 데 필요한 최소 기능만 구현한다.
2. 테스트를 우회하는 하드코딩이나 기존 완성 함수를 대신 호출하는 방식을 사용하지 않는다.
3. 아직 불필요한 추상화, 최적화, 구조 개선을 추가하지 않는다.
4. 대상 테스트를 다시 실행한다.
5. 모든 테스트가 통과하는지 확인한다.
6. 통과한 테스트 파일 수와 테스트 수를 기록한다.

## 4. Refactor

1. 테스트가 통과하는 상태에서만 리팩터링한다.
2. 중복 계산, 반복 조건문, 불명확한 상수와 복잡한 표현을 정리한다.
3. 필요하면 설정값을 객체로 분리하고 반복 계산을 공통 로직으로 통합한다.
4. 함수의 입력, 출력, 동작과 테스트 기대값은 변경하지 않는다.
5. 리팩터링 직후 동일한 테스트를 다시 실행한다.
6. 전체 테스트, 타입 검사, 린트 또는 빌드를 실행해 회귀 오류를 확인한다.

## 5. 검증 보고

다음 내용을 결과로 보고한다.

- 변경한 파일
- 추가한 테스트 시나리오
- Red 단계의 실패 테스트 수와 실패 원인
- Green 단계의 통과 테스트 수
- Refactor에서 개선한 구조
- Refactor 이후 테스트 결과
- 타입 검사, 린트, 빌드 결과
- 남아 있는 위험이나 미검증 항목

## 검증 체크리스트

- [ ] 각 요구사항에 대응하는 테스트가 있다.
- [ ] 정상 입력이 검증된다.
- [ ] 최솟값과 최댓값이 검증된다.
- [ ] 반올림 또는 동점 처리 기준이 검증된다.
- [ ] 계산에 사용하지 않는 입력이 결과에 영향을 주지 않는다.
- [ ] Red 단계가 기능 구현 누락 때문에 실패했다.
- [ ] Green 단계에서 최소 코드로 모든 테스트가 통과했다.
- [ ] Refactor 전후 테스트 결과가 동일하다.
- [ ] 전체 테스트와 정적 검증이 통과했다.

## 적용 예시

상황 적합도 계산 기능을 개발할 때 다음 순서로 사용한다.

1. 상황별 점수와 기대 결과를 작성한다.
2. 모든 점수가 0과 100인 경우를 테스트한다.
3. 각 항목의 가중치를 개별적으로 테스트한다.
4. 여러 점수를 조합한 결과를 테스트한다.
5. 반올림과 무관한 필드 처리를 테스트한다.
6. 구현 전 테스트를 실행해 Red를 확인한다.
7. 최소 가중치 계산을 구현해 Green을 확인한다.
8. 가중치를 객체로 분리하고 공통 계산으로 리팩터링한다.
9. 동일한 테스트를 다시 실행한다.
