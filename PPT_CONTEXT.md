# 하루 체크아웃 발표 제작용 전체 컨텍스트

> 이 문서는 2026-07-27 기준으로 Claude Code가 저장소와 함께 읽고 발표용 PPT를 만들 수 있도록 작성한 인수인계 자료다.  
> 사용자와 Codex 사이의 긴 대화에서 프로젝트 사실, 사용자의 실제 고민, 멘토링에서 얻은 관점, 발표에서 강조할 메시지를 한곳에 모았다.  
> 이 문서는 발표 준비용 로컬 자료이며 PR이나 최종 산출물에 반드시 포함할 필요는 없다.

---

## 0. Claude Code가 먼저 지켜야 할 것

1. PPT를 만들기 전에 저장소의 실제 코드와 문서, 현재 Git 상태를 확인한다.
2. 이 문서에 적힌 내용이라도 코드에서 확인되지 않으면 단정하지 않는다.
3. 기존 코드, README, 문서를 발표 자료 제작이라는 이유로 임의 수정하지 않는다.
4. 사용자에게 보여주기 전에는 PPT에 다음 내용을 사실처럼 쓰지 않는다.
   - 영구 배포 URL이 존재한다.
   - 로그인과 회원가입이 구현되어 있다.
   - 사용자별 RLS가 적용되어 있다.
   - LiteLLM과 Vertex가 공개 서버에서 동작한다.
   - 이 웹앱 자체가 자율적으로 도구를 사용하는 AI Agent다.
5. 저장소의 `docs/app-preview.png`, `docs/architecture-diagram.png` 등 기존 시각 자료를 우선 재사용한다.
6. 발표의 핵심을 “웹 기술을 많이 구현했다”가 아니라 아래 흐름으로 잡는다.

> 낯선 웹 스택에서 코딩 Agent를 이용해 기능을 빠르게 연결했지만, 결과가 만들어지는 것과 개발자가 이해하고 책임지는 것은 다르다는 문제를 경험했다. 요구사항, 테스트, 실제 실행 검증을 통해 Agent 결과를 관리하려 했고, 멘토링 이후에는 민감한 감정 데이터를 무조건 클라우드에 저장하지 않는 로컬 우선 게스트 경험으로 서비스 방향을 구체화했다.

---

## 1. 사용자 배경

- 현재 대학교 4학년이다.
- 목표 직무는 Android·모바일 SW 개발이다.
- 웹 프론트엔드나 백엔드 개발자가 주목표는 아니다.
- 삼성전자 개발실에서 갤럭시 잠금화면 관련 인턴 경험이 있다.
- 인턴 당시 개발자들이 사용자 동작 로그와 오류 로그를 확인하고, 문제를 재현하고, 원인을 좁혀 개선하는 과정을 중요하게 다루는 모습을 보았다.
- 이 경험 때문에 기능을 빠르게 만드는 것만으로 개발 역량을 설명할 수 있는지 고민하고 있다.
- Android Studio와 Kotlin을 사용할 때도 AI가 작성한 코드를 활용한 경험이 있다.
- 이번 활동에는 “AI Agent를 공부해보고 싶다”는 목적으로 참여했다.
- React, Express, Supabase는 거의 처음 접하는 기술이었다.

발표에서 인턴 경험을 언급할 때는 회사 내부 도구명, 데이터, 비공개 프로세스를 말하지 않는다. 다음 정도의 일반적인 관찰만 사용한다.

> 인턴 당시 현업 개발자들이 사용자 동작과 오류 로그를 바탕으로 문제를 재현하고 개선하는 과정을 보았다.

---

## 2. 프로젝트 기본 정보

- 프로젝트명: 하루 체크아웃
- 저장소 로컬 경로: `C:\Users\GOM\Desktop\naveragentai\hub`
- 개인 GitHub 포크: `https://github.com/Yooseunghyeok/hub`
- 작업 브랜치: `N114_유승혁`
- 공용 Challenge 저장소 PR을 통해 매일 미션 결과를 제출했다.

### 프로젝트 문제의식

사용자가 하루의 감정을 자유롭게 입력하면 내용을 감정, 원인, 다음 행동으로 나누어 다시 볼 수 있게 하는 기록 도구다.

이 서비스는 의료 상담, 진단, 치료를 제공하는 서비스로 설명하면 안 된다. AI가 사용자를 과도하게 위로하거나 판단하는 것이 아니라, 사용자가 입력한 범위 안에서 내용을 구조화하는 데 초점을 둔다.

### 현재 사용자 흐름

1. 오늘의 기분을 선택한다.
2. 자유 텍스트로 하루의 감정을 적는다.
3. AI 또는 mock fallback이 감정·원인·다음 행동으로 구조화한다.
4. 사용자가 결과를 직접 수정한다.
5. 기록을 저장한다.
6. 기록 목록과 캘린더에서 다시 확인한다.
7. 선택적으로 사진을 첨부한다.
8. 기록을 삭제한다.

### 현재 구현된 주요 기능

- 감정 텍스트 입력
- 기분 선택
- 최대 입력 길이와 기본 검증
- 감정·원인·다음 행동 구조화
- AI 응답에 대한 이유 필드 처리
- 사용자의 구조화 결과 수정
- Supabase 체크인 저장과 조회
- 캘린더 기반 기록 조회
- 사진 업로드와 표시
- 체크인 삭제
- AI 실패 시 mock fallback
- 일부 클라이언트 및 서버 자동 테스트

### 아직 구현되지 않았거나 완성되지 않은 기능

- 실제 회원가입과 로그인
- 인증 토큰 기반 API 보호
- `user_id`를 이용한 사용자별 데이터 분리
- 완전한 Supabase RLS 정책
- 선택적 클라우드 동기화
- 영구적인 공개 배포 URL
- 공개 환경의 LiteLLM 배포
- 사용자별 AI 호출 제한
- 완성된 개인정보 안내와 동의 흐름

---

## 3. 현재 기술 구조

### 화면

- React
- Vite

### 서버

- Express
- 화면의 `/api` 요청을 처리한다.
- 입력 검증, AI 요약 요청, 저장, 조회, 사진, 삭제를 담당한다.

### 데이터

- Supabase Database
- Supabase Storage

### AI

- Express가 OpenAI 호환 형식으로 LiteLLM에 요청한다.
- LiteLLM이 Vertex Gemini를 호출한다.
- AI 요청이나 JSON 파싱이 실패하면 mock 결과로 전환한다.

### 테스트

- 서버: Node test runner
- 클라이언트: Vitest 관련 변경이 로컬에 있으나 아직 커밋되지 않았다.

### 중요한 구분

현재 제품 내부의 `LiteLLM → Vertex Gemini`는 기본적으로 LLM 호출 기능이다. 그 자체만으로 이 웹앱을 “Agent 서비스”라고 단정하면 안 된다.

이번 활동에서 실제로 주로 사용한 Agent는 저장소를 읽고 계획, 코드 수정, 실행, 테스트를 수행한 코딩 Agent다.

AI와 Agent의 차이를 발표에서 설명한다면 다음처럼 말한다.

- 일반적인 AI 코드 생성: 질문에 설명이나 코드 조각을 답한다.
- 코딩 Agent: 저장소와 실행 환경을 읽고, 계획하고, 여러 파일을 고치고, 테스트와 실행 결과에 따라 반복한다.
- 둘 다 AI가 코드를 생성하지만 Agent는 도구와 작업 절차를 이용해 여러 단계를 수행한다.

---

## 4. 실제 실행과 검증에서 확인된 사실

2026-07-23에 다음을 실제로 확인했다.

- React 개발 서버가 `http://localhost:5173`에서 동작했다.
- Express가 `http://localhost:3001`에서 동작했다.
- Express `/api/health`가 Supabase 저장 모드임을 반환했다.
- LiteLLM이 `http://localhost:4000`에서 동작했다.
- Express → LiteLLM → Vertex Gemini 실제 응답 경로가 한 번 이상 성공했고 응답의 `source`가 `ai`임을 확인했다.
- 프로덕션용 React 빌드가 성공했다.
- 서버 테스트 12개가 모두 통과했다.
- 테스트에는 AI 파싱, AI 실패 fallback, 체크인 삭제, 데모 모드 생성·조회·삭제가 포함되었다.

2026-07-24 현재 사용자가 요청해 모든 개발 포트를 종료했다.

- `5173`: 종료
- `3001`: 종료
- `4000`: 종료

내일 다시 실행해야 한다.

---

## 5. 전체 PR 흐름

공용 Challenge 저장소에서 `유승혁 is:closed`로 검색되는 PR은 3개가 아니라 총 15개다. 14개는 작업 브랜치에 병합됐고, #1254는 실수로 `main`을 대상으로 열어 닫은 뒤 #1257로 다시 제출했다.

### Week 1 — 주제와 프로토타입

- [#20](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/20): Vite·React 환경과 프로젝트 주제 소개
- [#149](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/149): 문제 정의, 사용자 시나리오, 핵심 기능, 프롬프트 학습
- [#286](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/286): 입력 → 결과 → 저장, 플립 카드, 상담 프렙시트 프로토타입
- [#419](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/419): client/server 구조, 화면 설계, 디자인 토큰, LiteLLM·Vertex 방향
- [#572](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/572): AI 출력 형식, 발표 자료, 2~4주차 백로그

### Week 2 — 실제 수직 슬라이스와 구조 이해

- [#869](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/869): React → Express → Supabase → LiteLLM·Vertex 수직 슬라이스
- [#1016](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1016): `CheckinForm` 분리와 state·props·fetch 학습
- [#1141](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1141): 결과·기록 컴포넌트, 상세 화면, 화면·데이터 흐름도
- [#1254](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1254): 대상을 `main`으로 잘못 지정해 병합 없이 닫음
- [#1257](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1257): 계획·검증 서브에이전트와 Express 학습을 올바른 브랜치로 다시 제출

### Week 3 — 기능 확장, 테스트, 배포 준비

- [#1448](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1448): 무드, 사진, 캘린더, 정리 근거 플립 카드
- [#1575](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1575): 삭제 방식 비교, API 계약·완료 기준, 개발 절차 Skill
- [#1701](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1701): 삭제·404 테스트, 삭제 API와 UI, 아키텍처 다이어그램
- [#1851](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1851): Vitest, 캘린더 테스트, 주간 필터 red → green, 테스트 Skill, showcase
- [#1953](https://github.com/connect-AIAgentChallenge-26-1/hub/pull/1953): Cloudflare 임시 공개, 안전한 데모 모드, Render 배포 설정

### 발표에서 이 흐름을 한 문장으로 요약

> 1주차에는 문제와 화면을 구체화했고, 2주차에는 React·Express·Supabase·AI를 끝까지 연결했으며, 3주차에는 기능을 확장한 뒤 테스트 우선 개발과 배포 준비까지 진행했다.

### TDD 사례를 발표에서 설명하는 방식

삭제 기능과 주간 필터 기능을 다음 흐름으로 설명한다.

1. 삭제의 정상 동작과 예외를 먼저 적었다.
2. 실제 Supabase에 테스트 기록을 넣고 삭제·404 동작을 검증했다.
3. 주간 필터는 테스트를 먼저 실패시킨 뒤 최소 구현으로 통과시켰다.
4. 서버 테스트, 클라이언트 테스트, 빌드로 기존 기능 회귀 여부를 확인했다.

과장해서 “프로젝트 전체를 TDD로 개발했다”고 말하면 안 된다. 정확한 표현은 다음과 같다.

> 기존 프로젝트의 삭제 기능을 추가하면서 일부 기능에 테스트 우선 접근을 적용해보았다.

---

## 6. Agent를 사용한 방식과 실제 고민

### 사용한 방식

- 요구사항과 백로그 초안 작성
- Agent에게 저장소 구조 파악 요청
- 기능별 계획 작성
- 코드 구현
- 테스트 코드 생성과 실행
- 빌드 및 API 확인
- 문서와 아키텍처 그림 작성
- Skill 작성 연습
- 회고 정리

### 사용자가 느낀 솔직한 문제

React, Express, Supabase를 잘 모르는 상태에서 Agent가 기술 설명뿐 아니라 설계와 구현까지 대부분 처리했다. 그 결과 기능은 빠르게 연결되었지만 다음 경계가 흐려졌다.

- 내가 판단한 부분과 Agent가 판단한 부분
- Agent와 협업한 것과 코딩을 외주 준 것
- 기능이 동작하는 것과 내가 구조를 이해하는 것
- 테스트가 통과하는 것과 실제 운영 안전성이 확보된 것

사용자가 멘토에게 실제로 물어보고 싶었던 핵심 질문은 다음이었다.

> 낯선 기술을 Agent에게 맡겨 결과를 내는 것과, Agent를 제대로 활용하면서 개발 역량을 쌓는 것의 차이는 무엇인가요?

조금 더 구체적으로는 다음과 같다.

> DB·Express·React가 모두 낯설다 보니 Agent가 기술 설명뿐 아니라 설계와 구현까지 대부분 맡게 됐습니다. 결과물은 계속 만들어지지만, 이것이 Agent와 협업하는 법을 배운 것인지 단순히 코딩을 맡긴 것인지 구분이 잘 안 됩니다. 좋은 Agent 활용과 단순한 코드 생성의 차이는 무엇이고, 사용자인 제가 반드시 직접 결정하고 검증해야 하는 부분은 무엇일까요?

### 발표에서 가져갈 수 있는 Agent 협업 기준

코드 타이핑의 주체보다 다음 책임을 누가 가졌는지 보여준다.

1. 왜 필요한 기능인지 사용자가 결정
2. 완료 조건과 실패 조건을 사용자가 작성
3. Agent에게 구현 전에 계획을 요청
4. 이해하지 못한 설계를 질문하고 수정
5. Agent가 코드 구현
6. 테스트와 실제 실행으로 사용자가 검증
7. Agent의 제안과 사용자의 판단을 회고에 기록

발표 핵심 문장 후보:

> Agent가 코드를 작성했지만 목표, 완료 기준, 실패 조건과 최종 검증은 개발자가 가져야 한다는 점을 배웠다.

---

## 7. 인턴 경험과 이번 프로젝트의 연결

삼성전자 개발실 인턴 당시 사용자가 본 중요한 개발 활동은 새 기능을 빠르게 추가하는 것만이 아니었다.

- 실제 사용자 환경에서 발생한 문제 확인
- 사용자 동작과 오류 로그 분석
- 동일 조건 재현
- 원인 범위 축소
- 수정 후 재발 여부 확인

이번 프로젝트에서는 Agent로 기능을 빠르게 추가하는 데 집중하면서 실제 사용 이후의 관찰, 오류 재현, 운영 로그라는 과정이 부족했다.

이를 발표의 반성으로만 표현하지 말고 다음 학습 방향으로 연결한다.

> 현업에서 본 개발은 구현 이후의 관찰과 재현까지 포함했지만, 이번 프로젝트는 구현 속도에 치우쳤다. 남은 기간에는 기능 수보다 실패 상황을 관찰하고 다시 검증할 수 있는 흐름을 보강하려 한다.

---

## 8. 멘토링 전 배포 혼란에서 얻은 내용

사용자는 멘토가 프로젝트를 직접 눌러볼 수 있다고 생각해 멘토링 직전에 공개 URL을 급하게 만들려고 했다.

처음에는 다음 개념이 섞여 있었다.

- `localhost`
- 정적 HTML
- Render 배포
- 임시 URL
- 실제 Supabase와 AI 연결
- GitHub에 코드가 있는 상태

### 정리된 차이

- `localhost`: 내 PC에서만 접속 가능
- 임시 터널: 내 PC에서 실행 중인 서버를 짧은 시간 외부에 공개
- Render 배포: 같은 코드를 클라우드 서버에서 별도로 실행
- GitHub 푸시: 코드를 원격 저장소에 올리는 것이며 배포 자체는 아님
- 정적 배포: React 화면은 제공할 수 있지만 Express와 AI 호출이 필요하면 서버 배포가 별도로 필요

### 임시 데모 사건

멘토링 직전에 Cloudflare Quick Tunnel을 사용해 임시 URL을 생성했다.

임시 URL:

`https://abstracts-cooling-inc-oklahoma.trycloudflare.com/`

이 URL은 현재 종료되었고 다시 접속할 수 없다. 발표 자료에 사용 가능한 URL로 넣으면 안 된다.

실제 Supabase와 Vertex를 로그인 없이 공개하는 것은 위험하므로, 외부 임시 링크는 다음과 같은 안전한 데모 모드로 제공했다.

- 실제 Supabase 대신 서버 메모리
- 실제 Vertex 대신 mock fallback
- 샘플 기록 2개
- 입력, 저장, 조회, 삭제 체험 가능

외부에서 다음을 실제 확인했다.

- HTTP 200
- React 화면 렌더링
- 제목 `하루 체크아웃`
- 입력창과 기분 선택 버튼
- API health `storage: demo`
- 샘플 기록 2개

멘토링 종료 후 터널과 임시 데모 서버를 종료했고 외부 URL이 HTTP 530으로 접근 불가한 것을 확인했다.

### 배포에서 배운 핵심

공개 URL을 만드는 것은 단순히 코드를 서버에 올리는 문제가 아니다.

- 환경변수와 비밀키
- 사용자 인증
- 사용자별 데이터 격리
- DB 권한
- AI 호출 비용
- 호출 제한
- 로그와 오류 처리
- 개인정보 안내

가 함께 필요하다.

---

## 9. Render와 배포 준비의 정확한 현재 상태

로컬 작업 트리에는 Render 데모 배포를 위한 미커밋 파일과 수정이 존재한다.

예상 관련 파일:

- `render.yaml`
- 루트 `package.json`
- `server/.env.example`
- `server/index.js`
- `server/config/runtimeMode.js`
- `server/services/checkinService.js`
- `server/services/storageService.js`
- `server/services/demoCheckinStore.js`
- `server/tests/demoMode.test.js`

이 구성은 다음 목적이었다.

- React 빌드 결과를 Express가 함께 제공
- Render가 하나의 Node Web Service로 실행
- `/api/health` 제공
- `DEMO_MODE=true`로 비밀키 없는 메모리 데모 실행

중요:

- 이 배포 관련 변경은 아직 커밋되지 않았다.
- 실제 Render 서비스도 생성되지 않았다.
- 영구적인 `onrender.com` URL은 없다.
- “Render 배포를 완료했다”고 발표하면 안 된다.

### 실제 DB와 AI까지 공개하려면 필요한 구조

```text
사용자 브라우저
  → 배포된 React + Express
      → Supabase Auth + Database + Storage + RLS
      → 배포된 LiteLLM
          → Vertex Gemini
```

Supabase와 Vertex는 이미 클라우드 서비스다. 현재 로컬에 남아 있는 React, Express, LiteLLM을 서버에 올리고 안전한 인증 경계를 만들어야 한다.

실제 공개 서비스의 최소 조건:

1. Supabase Auth
2. `user_id` 필수화
3. SELECT·INSERT·UPDATE·DELETE RLS
4. Storage 사용자별 정책
5. 비로그인 API 401
6. Express가 요청 본문이 아니라 검증된 토큰에서 사용자 ID 결정
7. LiteLLM 별도 배포
8. Vertex 서비스 계정과 비밀키의 서버 전용 보관
9. 사용자별 AI 요청 제한
10. 두 계정으로 데이터 격리 테스트

---

## 10. README와 저장소 정리 과정

사용자는 저장소, 특히 `docs` 아래에 현재 필요하지 않은 자료가 너무 많다고 느꼈다.

이에 핵심 문서만 남기고 저장소를 정리하는 커밋을 만들었다.

- 커밋: `d25aae7`
- 메시지: `chore: 핵심 문서만 남기고 저장소 정리`

이 커밋은 개인 포크의 `N114_유승혁` 브랜치에 푸시되었다.

하지만 정리 과정에서 README가 기존 33줄 정도의 문서에서 119줄 정도로 과도하게 확장되었다. 아직 커밋되지 않은 Render와 데모 모드를 이미 준비된 실행 방식처럼 설명한 문제가 있었다.

사용자가 README를 원래대로 돌려달라고 요청해 정리 커밋 직전 버전과 파일 해시가 동일하도록 복원했다.

- 복원 커밋: `9bce19d`
- 메시지: `revert: README를 정리 전 버전으로 복원`
- 개인 포크에 푸시 완료

현재 README는 이전 버전으로 돌아갔지만, 정리 커밋에서 일부 예전 문서가 삭제되었기 때문에 README의 일부 링크가 실제로 존재하지 않을 가능성이 있다. Claude Code는 PPT 제작 시 README 링크만 믿지 말고 실제 파일 존재 여부를 확인해야 한다.

README를 발표 준비라는 이유로 다시 확장하거나 수정하지 않는다.

---

## 11. 현재 Git 상태에서 주의할 점

2026-07-23 마지막 확인 기준으로 원격 브랜치와 현재 커밋은 일치했다.

- HEAD: `9bce19d`
- origin/N114_유승혁: `9bce19d`

그러나 작업 트리에는 여러 미커밋 변경이 있다.

대표적인 변경:

- 클라이언트 Vitest 설정
- 캘린더 관련 테스트
- 주간 유틸리티와 테스트
- 회고 문서
- Render 설정
- 데모 모드
- 서버 런타임 설정
- Agent Skill 관련 파일

PPT 제작 중 다음을 하지 않는다.

- `git add -A`
- 모든 변경을 한 번에 커밋
- 미커밋 코드를 발표 완료 기능으로 단정
- 기존 사용자 변경을 삭제하거나 reset

PPT 파일만 새로 만들고, 코드 변경은 별도 요청이 있을 때 진행한다.

---

## 12. 다른 멘티와 비교하며 확인한 범위

멘토링 조의 다른 멘티로 다음 사용자가 있었다.

- N038 김승은
- N158 정지웅

확인했던 저장소:

- `https://github.com/seungeun3/title/tree/N038_김승은`
- `https://github.com/studentnoname/hub/tree/N158_정지웅`

확인 당시 두 저장소의 GitHub 코드와 README에서는 공개 배포 URL을 찾지 못했다. 그러나 저장소 밖에서 별도 배포했는지는 알 수 없다.

발표에서 다음처럼 단정하면 안 된다.

- “다른 사람들도 모두 배포하지 않았다.”
- “내 프로젝트가 다른 사람보다 낫다.”
- “다른 사람들은 테스트가 없다.”

비교는 발표 핵심이 아니며, 사용자의 문제 해결 과정에 집중한다.

---

## 13. 멘토링에서 얻은 가장 중요한 방향 전환

멘토는 감정 기록 서비스에서 사용자가 실제 회원가입과 개인정보 입력을 부담스러워할 수 있고, 자신의 감정이 데이터베이스에 올라가는 것을 꺼릴 수 있다는 관점을 주었다.

이 피드백으로 남은 일주일의 목표를 다음처럼 바꾸려 한다.

> 실제 서비스처럼 보이는 기능을 무조건 추가하는 것이 아니라, 민감한 감정 기록을 계정과 클라우드 저장에 강제로 묶지 않는 완성도 높은 웹앱 데모를 만든다.

### 기본 게스트 모드

- 회원가입 없이 시작
- 해당 브라우저의 IndexedDB에만 기록
- 새로고침 후 유지
- 다른 기기와 자동 동기화하지 않음
- 전체 삭제와 내보내기 제공

이미지와 여러 구조화 기록을 저장하므로 단순 `localStorage`보다 IndexedDB가 더 적합하다.

### 선택적 AI 사용

매우 중요한 개인정보 경계:

> 기록을 로컬에 저장하더라도 Vertex AI로 정리하면 감정 원문은 AI 처리를 위해 외부 서버로 전송된다.

따라서 게스트 모드를 만들었다고 “모든 데이터가 기기 밖으로 나가지 않는다”고 말하면 안 된다.

권장 모드:

- 기본 정리: mock 또는 로컬 규칙, 외부 전송 없음
- AI로 정리: 외부 전송 안내와 사용자의 명시적 선택

### 선택적 로그인과 클라우드 동기화

- 로그인은 핵심 기능의 필수 조건이 아니다.
- 로그인 사용자는 선택한 기록만 클라우드에 동기화하는 방향을 고려한다.
- 프런트 화면만 만든 가짜 로그인은 실제 인증이 아니다.
- 실제 동기화라면 Supabase Auth와 RLS가 함께 필요하다.

---

## 14. 남은 일주일 권장 우선순위

### P0 — 게스트 데모 완성

1. 시작 화면에 `게스트로 시작`
2. IndexedDB 저장소
3. 입력 → 구조화 → 수정 → 저장
4. 새로고침 후 기록 유지
5. 목록과 캘린더 연동
6. 개별 삭제
7. 전체 삭제
8. 로컬 저장 안내
9. 관련 자동 테스트

### P1 — 개인정보 선택과 데모 품질

1. 기본 정리와 AI 정리 구분
2. AI 전송 안내
3. JSON 내보내기
4. 빈 상태와 실패 상태
5. 모바일 크기 검증
6. 네트워크가 없을 때 기본 기록 흐름 확인

### P2 — 시간이 남을 때

1. 실제 Supabase Auth
2. `user_id`와 RLS
3. 선택적 클라우드 동기화
4. 영구 배포
5. LiteLLM 공개 서버
6. 사용자별 AI 제한

P2 때문에 P0의 완성도를 희생하지 않는다.

---

## 15. 발표의 중심 서사

발표를 단순 기능 소개로 끝내지 않는다.

### 시작

> Android·모바일 SW를 목표로 하지만 이번 활동에서는 낯선 React·Express·Supabase 스택으로 감정 기록 웹앱을 만들었다.

### Agent 활용

> Agent 덕분에 낯선 기술에서도 기능을 빠르게 연결할 수 있었다.

### 문제 발견

> 구현 속도는 빨라졌지만 내가 이해하고 검증한 부분과 Agent가 판단한 부분의 경계가 흐려졌다.

### 대응

> 요구사항, 완료 기준, 테스트, 실제 실행 결과를 남겨 Agent 결과를 검증하려 했다.

### 구체 사례

> 체크인 삭제 기능에서는 실패 조건을 먼저 정하고 테스트 후 구현하는 TDD 흐름을 일부 적용했다.

### 배포에서 배운 점

> localhost에서 동작하는 것과 실제 사용자가 안전하게 쓰는 서비스는 다르며, 인증·권한·비용·비밀키가 필요하다는 것을 직접 경험했다.

### 멘토링 이후의 제품 판단

> 감정 기록을 무조건 클라우드에 저장하는 것이 사용자에게 부담이 될 수 있어, 게스트 로컬 저장을 기본으로 하고 AI와 클라우드는 선택하게 하는 방향으로 전환했다.

### 결론

> 이번 경험의 핵심은 웹 기술을 많이 아는 것이 아니라, Agent가 만든 결과를 기준과 검증으로 관리하고 사용자 관점에서 기술 선택을 다시 판단한 것이다.

---

## 16. PPT 권장 구성

### 1장 — 제목

- 하루 체크아웃
- 낯선 기술에서 AI Agent와 함께 만든 감정 기록 웹앱
- N114 유승혁

### 2장 — 문제와 초기 목표

- 정리되지 않은 감정을 감정·원인·다음 행동으로 구조화
- 조언이나 진단보다 사용자의 자기 정리 지원
- 기존 화면 캡처 활용

### 3장 — 현재 구현 흐름

- 입력
- AI 구조화
- 사용자 수정
- 저장
- 캘린더 조회
- 삭제

`docs/app-preview.png` 활용 후보.

### 4장 — 기술 구조

- React
- Express
- Supabase DB·Storage
- LiteLLM
- Vertex Gemini

`docs/architecture-diagram.png` 활용 후보.

### 5장 — Agent와 작업한 방법

- 요구사항
- 완료 기준
- Agent 계획
- 구현
- 테스트
- 실행 검증

전체 PR 15개의 발전 흐름과 삭제·주간 필터 TDD 사례 연결.

### 6장 — 가장 큰 학습: 구현과 책임의 차이

- Agent가 기능을 빠르게 만듦
- 이해하지 못한 코드가 늘어날 위험
- 테스트 통과와 운영 안전성의 차이
- 개발자가 가져야 할 목표·기준·검증 책임

### 7장 — 배포 시도에서 발견한 실제 서비스의 조건

- localhost와 공개 배포의 차이
- 인증
- 사용자 격리
- 비밀키
- AI 비용
- 임시 데모와 실제 서비스 분리

임시 URL은 종료되었으므로 QR 코드나 링크로 넣지 않는다.

### 8장 — 멘토링 이후 방향 전환

- 회원가입이 감정 기록의 장벽이 될 수 있음
- 기본: 게스트 + IndexedDB
- 선택: AI 전송
- 선택: 로그인 + 클라우드 동기화

### 9장 — 남은 일주일

- 게스트 모드 완성
- 로컬 저장
- 전체 삭제와 내보내기
- 개인정보 안내
- 테스트와 모바일 검증

### 10장 — 회고

- 낯선 기술에서도 Agent로 빠르게 시도할 수 있었다.
- 기능을 만드는 것보다 검증과 사용자 데이터 책임이 중요했다.
- 다음에는 Agent에게 맡길 일과 직접 판단할 일을 더 명확하게 나누겠다.

발표 시간이 짧으면 2장과 3장, 9장과 10장을 합쳐 8장으로 줄인다.

---

## 17. 발표에서 사용하기 좋은 문장

### 프로젝트 설명

> 사용자가 하루의 감정을 적으면 감정, 원인, 다음 행동으로 구조화하고 직접 수정한 뒤 기록과 캘린더에서 다시 볼 수 있는 웹앱입니다.

### Agent 사용

> Agent가 저장소 탐색과 구현 속도를 높여줬지만, 결과를 그대로 완료로 보지 않고 요구사항과 테스트, 실제 실행 결과로 검증하려 했습니다.

### 솔직한 한계

> 로그인과 사용자별 데이터 분리, 영구 배포는 아직 완료하지 않았습니다.

### TDD

> 프로젝트 전체를 TDD로 개발한 것은 아니고, 삭제 기능을 추가하면서 실패 테스트를 먼저 작성하는 흐름을 적용했습니다.

### 배포

> 멘토링 전 임시 데모를 외부에 공개해보면서 로컬 실행과 실제 서비스 운영 사이에 인증, 권한, 비용과 비밀키 문제가 있다는 것을 구체적으로 알게 됐습니다.

### 개인정보 방향 전환

> 감정 기록은 민감하기 때문에 회원가입과 클라우드 저장을 강제하지 않고, 게스트 로컬 저장을 기본으로 바꾸려 합니다.

### 진로 연결

> 웹 기술 자체보다 API 경계, 상태와 데이터 흐름, 오류 재현, 테스트와 사용자 데이터 보호 경험을 모바일 개발에도 가져가고 싶습니다.

---

## 18. 발표에서 피해야 할 표현

- “AI가 거의 다 해서 저는 모릅니다.”
- “제가 웹 서비스를 완성했습니다.”
- “실제 서비스가 배포되어 있습니다.”
- “Supabase를 쓰므로 사용자 데이터가 안전합니다.”
- “RLS를 켰으므로 사용자별 데이터가 분리됩니다.”
- “로그인 화면이 있으므로 인증이 구현되었습니다.”
- “로컬 저장이므로 AI를 사용해도 데이터가 외부로 나가지 않습니다.”
- “LiteLLM을 사용했으므로 이 서비스는 Agent입니다.”
- “테스트가 통과했으므로 버그가 없습니다.”
- “프로젝트 전체를 TDD로 개발했습니다.”

대신 다음처럼 말한다.

> 구현한 범위, 검증한 범위와 아직 해결하지 못한 범위를 분리해 설명한다.

---

## 19. 내일 Claude Code에게 권장하는 작업 순서

1. `git status -sb`로 미커밋 변경을 확인한다.
2. 실제 존재하는 문서와 이미지 목록을 확인한다.
3. README 링크가 아니라 실제 파일을 기준으로 PPT 자료를 선택한다.
4. 현재 코드에서 확인되는 기능만 슬라이드에 넣는다.
5. PPT 초안을 먼저 텍스트 개요로 사용자에게 보여준다.
6. 사용자 승인 후 PPT 파일을 생성한다.
7. 각 슬라이드에 발표자 노트를 짧게 넣는다.
8. 최종 PPT를 이미지 또는 PDF로 렌더링해 글자 잘림과 레이아웃을 검증한다.
9. 기존 코드와 문서는 수정하지 않는다.
10. PPT 파일만 별도 경로에 저장하고 커밋 여부는 사용자에게 묻는다.

### 디자인 방향

- 모바일 앱 또는 차분한 감정 기록 도구처럼 보이게 한다.
- 과도한 AI 미래 이미지, 로봇 이미지, 네온 효과를 피한다.
- 실제 앱의 베이지·갈색 계열 화면 톤을 우선 참고한다.
- 한 슬라이드에 긴 문단을 넣지 않는다.
- 화면 흐름, 아키텍처, 전후 방향 전환을 시각적으로 보여준다.
- “문제 → 시도 → 검증 → 배운 점 → 다음 선택” 구조가 눈에 보이게 한다.

---

## 20. 최종 핵심 메시지

이 발표에서 가장 중요한 내용은 기능 목록이 아니다.

> Agent는 낯선 기술에서 결과를 빠르게 만들게 해주었지만, 결과에 대한 이해와 책임까지 대신 가져가지는 않는다. 이번 프로젝트에서는 요구사항과 테스트, 실제 실행 검증을 통해 그 경계를 관리하려 했고, 멘토링을 통해 감정 데이터의 민감성을 제품 설계에 반영하여 게스트 로컬 저장 중심으로 다음 방향을 정했다.
