# 최종 핵심 흐름 점검 및 기능 동결

이 문서는 마지막 주 Issue 1의 완료 기준이다. 새 기능은 추가하지 않고, 아래 흐름의 차단 문제만 수정한다.

## 핵심 흐름

1. 로그인 화면에서 아이디와 비밀번호로 로그인한다.
2. 프로필과 개인 설정을 저장한다.
3. 정보 사이트 추천에서 출처를 추가하고, 저장된 출처를 스캔한다.
4. 공지 링크 또는 본문을 분석하고 결과를 확인한다.
5. 공고를 저장하거나 태스크화한다.
6. 저장한 공고와 마감 태스크에서 분석 결과와 태스크를 확인한다.
7. 로그아웃 후 같은 계정으로 다시 로그인해 서버 저장 데이터를 확인한다.

## 계정 분리 완료 조건

계정 A와 계정 B를 준비해 아래를 확인한다.

- 계정 B는 계정 A의 프로필, 개인 설정, 저장 출처, 저장 공고, 공고 안의 태스크를 볼 수 없다.
- 계정 B는 계정 A의 스캔 기록과 지원 공지 탐색 및 사이트 추천의 이전 화면 결과를 볼 수 없다.
- 계정 A의 데이터를 삭제하거나 초기화해도 계정 B의 데이터는 유지된다.
- 로그인 환경에서 익명 로컬 저장 API(`/api/opportunities`)는 `403`으로 차단된다.
- 요청 body에 `userId`를 넣어도 저장 소유자는 Bearer 토큰의 사용자 ID로만 결정된다.

## 자동 검증

```bash
npm run verify:requirements
```

이 명령은 프로필, 개인 설정, 저장 출처, 저장 공고, 스캔 기록의 사용자 분리 회귀 테스트와 프로덕션 빌드를 포함한다. 실제 Supabase 계정 A/B 로그인은 아래 수동 점검을 함께 수행한다.

## 수동 점검

1. 계정 A에서 프로필, 개인 설정, 출처 하나, 분석 공고 하나를 저장하고 태스크화를 실행한다.
2. 로그아웃한 뒤 계정 B로 로그인한다. 모든 개인 데이터와 탐색 결과가 비어 있거나 계정 B의 값만 표시되는지 확인한다.
3. 계정 B에서 별도 프로필, 설정, 출처, 공고를 저장한다.
4. 계정 A로 다시 로그인해 A 데이터만 복원되는지 확인한다.
5. DevTools Network에서 사용자별 API 요청이 Bearer token을 포함하고, `/api/opportunities`가 `403`인지 확인한다. 토큰 원문은 기록하지 않는다.

## 기능 동결 범위

- 기능 추가 대신 핵심 흐름의 차단 오류와 데이터 격리 오류만 수정한다.
- 실제 다계정·다기기 동기화와 태스크 개별 수정/완료 상태 저장은 다음 범위에서 별도로 다룬다.