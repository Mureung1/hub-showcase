# 팀플, 이지! — 팀 프로젝트 킥오프 도구

> **한 줄 정의**: 대학생 팀 프로젝트의 '킥오프 1시간'을 AI가 중립적 중재자로 대신해, 15분 만에 팀 전원이 납득하는 계획과 역할 분담을 만들어주는 웹 서비스

---

## 1. 문제 정의

대학생 팀 프로젝트는 **시작 단계에서 실패한다.**

서로 잘 모르는 팀원들이 위계도 경험도 없이 첫 회의에서 계획과 역할을 정해야 하는데, 이 과정이 어색함과 눈치보기 속에 흐지부지되면서 **계획 없는 프로젝트**와 **불공정한 분담**이 굳어진다.

### 세부 문제

| # | 문제 | 원인 |
|---|------|------|
| 1 | 역할 분담 실패 | 조장·발표 등 기피 역할을 아무도 먼저 말하지 못함. 위계가 없어 "너 이거 해"라고 말할 사람이 없음 |
| 2 | 일정 수립 실패 | 마감일만 알고 마일스톤·세부 태스크로 쪼개는 경험이 없음. 시험 기간 등 학사 일정 고려 실패 |
| 3 | 진행 파악 불가 | 각자 역할에 매몰되어 전체 진행률과 병목을 아무도 모름 |

### 핵심 인사이트

- 기업용 PM 툴과의 차별점: 대학생 팀에는 **PM(위계)이 없다.** AI가 "관리자"가 아니라 **아무도 하기 싫은 말을 대신 해주는 중립적 중재자** 역할을 맡는다.
- 대학생 팀플은 기간이 짧고(3~8주), 사용 빈도가 낮으며, 툴 설치 자체가 진입장벽이다. → **일회성 고밀도 사용(킥오프)** 에 집중하고, 지속 사용 기능(모니터링)은 v2로 분리한다.

---

## 2. 타깃 사용자

- **주 사용자**: 팀 프로젝트(조별과제)를 처음 또는 몇 번 경험한 대학생 (3~8인 팀)
- **사용 시점**: 팀 구성 직후 첫 회의 자리 (팀원 전원이 모인 상태에서 함께 세팅)
- **접속 기기**: 모바일 우선 (첫 회의에서 각자 폰으로 접속하는 시나리오)

---

## 3. MVP 범위

### 포함 (v1)

| 기능 | 설명 | AI 활용 |
|------|------|---------|
| ① AI 플래닝 | 과제 유형·목표·마감일 입력 → 마일스톤·태스크 제안 → 팀 수정·승인 | LLM이 학사 일정(시험 주간 회피)을 반영한 계획 초안 생성 |
| ② AI 역할 배정 | 비공개 설문 → 점수 매트릭스 배정 → AI가 이유 설명 | 배정은 결정적 로직, **설명·타협안 제안은 LLM** |
| ③ 진행 대시보드 (그릇) | 3열 고정 칸반 + 마일스톤별 진행률 바 | 없음 (수동 CRUD). v2 모니터링의 토대 |

### 제외 (v2 로드맵)

- **모니터링 에이전트**: 지연 감지·리마인드·주간 팀 리포트
  - 제외 근거: (a) 데모에서 시간 경과를 보여줄 수 없음 (b) 데이터 소스가 팀원의 지속적 수동 입력에 의존 (c) 알림 인프라 구현 비용 대비 AI 가시성 낮음
  - v1의 태스크 상태 데이터 구조가 그대로 v2의 데이터 소스가 됨 → 확장성 확보
- 최종 기여도 요약 (자기 신고 데이터의 신뢰성 문제, 성적 분쟁 악용 리스크)
- 외부 채널 알림 (이메일·카카오)

---

## 4. 사용자 시나리오 (MVP, 5단계)

1. **프로젝트 생성** — 조장(또는 아무나)이 과제 유형·목표·마감일을 입력하고 팀원에게 초대 링크를 공유한다.
2. **AI 계획 제안 → 승인** — 플래너 에이전트가 마일스톤과 태스크를 제안하고, 팀이 그 자리에서 수정·승인한다.
3. **비공개 설문** — 각 팀원이 2분짜리 설문(선호 역할 1~3순위 / 기피 1개 / 경험 유무 / 리더 의향)을 제출한다.
4. **AI 역할 배정** — 배정 에이전트가 역할을 배정하고 **팀 단위 논리**로 이유를 설명한다. 이견이 있으면 48시간 내 1회 스왑 요청(양측 동의 시 교환).
5. **대시보드 확정** — 계획과 역할이 대시보드에 반영되고, 이후 팀원은 자기 태스크 상태를 직접 갱신한다.

---

## 5. 역할 배정 설계 (핵심 차별 기능)

### 설문 문항 (2분 완료)

| 문항 | 형식 | 비고 |
|------|------|------|
| 선호 역할 | 역할 카드 드래그로 1~3순위 | 선호 ≠ 능력 분리 |
| 기피 역할 | 최대 1개 선택 | 절대 배제 조건 |
| 역할별 경험 | 해봤다/안 해봤다 체크 | 자신감 5점 척도 대신 사용 — 조작 유인이 적음 |
| 리더 의향 | 예/아니오/상관없음 | |

### 배정 로직: "결정적 로직 + LLM 설명" 2단 구조

**1단 — 점수 매트릭스 (코드로 구현, LLM 미사용)**

```
점수 = 선호 1순위 +3 / 2순위 +2 / 3순위 +1
     + 경험 있음 +1
     + 기피 역할 -10
→ 팀원×역할 매트릭스에서 그리디 배정 (4~8인 규모에서 충분)
```

- 같은 입력 → 같은 결과 (재현성·공정성 보장, "왜?"에 답할 수 있음)
- 인원 ≠ 역할 수: 역할별 최소/최대 인원 규칙으로 1인 다역·다인 1역 처리

**2단 — LLM의 역할**

- 배정 결과의 **이유를 자연어로 설명** (개인 응답 노출 금지 → "팀 전체 선호를 종합했을 때 이 조합의 만족도가 가장 높음" 식의 팀 단위 서술)
- **전원 기피 역할(예: 발표) 타협안 제안**: 로테이션, 해당 역할 담당자의 다른 태스크 경감 등

### 공정성 절차

- 설문은 비공개, 설명은 팀 단위 서술 (비공개 약속과 투명성의 충돌 해소)
- 배정 발표 후 **48시간 내 1회 스왑 요청** 가능 (양측 동의 시 교환) — 무한 재배정 루프 방지
- 사람은 결과보다 **절차**가 공정할 때 납득한다

---

## 6. 화면 설계 (5개 화면)

| # | 화면 | 사용자가 보는 것 | 입력하는 것 | 결정하는 것 |
|---|------|-----------------|------------|------------|
| 1 | 프로젝트 생성 (위저드 3스텝) | 과제 유형 카드 3종(발표/개발/보고서) | 목표 한 문장, 마감일, 팀원 수 | 유형 선택 1회 |
| 2 | AI 계획 검토 | 좌: 마일스톤 타임라인 / 우: 태스크 목록 | 인라인 수정·삭제·추가 | "이대로 확정" or "다시 제안받기" |
| 3 | 팀원 설문 | 역할 카드, 제출 현황(3/4명) | 드래그 순위, 기피, 경험, 리더 토글 | 제출 |
| 4 | 배정 결과 | 팀원별 역할 카드 + AI 설명 | — | 수락 or 스왑 요청 (48h) |
| 5 | 대시보드 | 진행률 바 + D-day, 3열 칸반(내 태스크 강조), 마일스톤별 진행률 | 태스크 상태 드래그 | — |

### UX 원칙

1. **15분 온보딩**: 팀 생성 → 배정 완료까지 첫 회의 자리에서 끝난다
2. **AI는 제안자, 사람이 결정자**: 모든 AI 출력에 편집·재생성 버튼
3. **모바일 우선**: 처음부터 모바일 뷰 기준으로 개발
4. **설정 최소화**: 칸반 열 고정(할 일/진행 중/완료), 커스터마이징은 v2

---

## 7. 기술 구성

- **프론트엔드**: React (Vite), 모바일 우선 반응형
- **백엔드**: Express + 경량 DB (SQLite)
- **AI**: LLM API 서버사이드 호출 2종
  - 플래너: 과제 정보 → JSON 스키마(마일스톤/태스크) 구조화 출력
  - 배정 설명: 배정 결과 + 팀 통계 → 자연어 설명 생성
- **인증**: 정식 회원가입 없이 **초대 링크 토큰 + 닉네임** (진입장벽 최소화)

---

## 8. 엣지케이스 대응

| 케이스 | 대응 |
|--------|------|
| 배정 불복 | 48시간 내 1회 스왑 요청 (양측 동의), 이후 확정 |
| 전원 기피 역할 | LLM이 로테이션/보상 타협안 제안, 최종은 팀 투표 |
| 팀원 일부 미가입 | 초대 링크만으로 접근 (가입 불필요), 미제출자는 마감 후 "상관없음"으로 처리하고 배정 진행 |
| 과제 요건 변경 | 계획 검토 화면 재진입 → 기존 진행률·역할 유지한 채 태스크만 재생성 |
| 인원 ≠ 역할 수 | 역할별 최소/최대 인원 규칙으로 1인 다역·다인 1역 |
| 설문 응답 역추론 | 배정 설명을 팀 단위 서술로 제한, 개인 응답 인용 금지 |

---

## 9. 데모 시나리오 (5분)

1. "OO 전공 발표 과제, 마감 4주 뒤, 5명" 입력 → **30초 만에 계획 생성** (시험 주간 회피 포인트 강조)
2. 태스크 하나를 즉석에서 수정 → 승인 (AI는 초안, 사람이 결정)
3. 팀원 5명 설문 제출 (사전 준비된 계정)
4. **배정 결과 + AI 설명 공개** — "아무도 하기 싫은 말을 AI가 대신 해준다" 메시지 전달 (하이라이트)
5. 대시보드에서 태스크 상태 드래그 → v2 모니터링 확장 로드맵 언급

---

## 10. v2 로드맵

- 모니터링 에이전트: 마감일 규칙 기반 지연 감지 + 주간 팀 요약 리포트 ("사람이 아니라 태스크를 주어로" 원칙)
- 주간 체크인 봇 (30초, 상태 업데이트 + 막힌 점)
- 학교 학사일정 API 연동, 과제 유형 템플릿 확장

---

## 11. 프로토타입 구현 체크리스트

> 프론트 온리(localStorage) + 목 LLM + 단일 브라우저 시뮬레이션으로 화면 5개 전부 구현.
> **한 단계 구현 → 사용자 검증 → 다음 단계 지시** 순서로 진행한다.

- [x] **1단계 — 기반 구축 + 프로젝트 생성 위저드**
  - 소개 페이지(ProjectIntro) 삭제, `App.jsx` 단계 상태 머신, `store.jsx`(useReducer + localStorage), `data/templates.js`·`services/mockAi.js`·`logic/assignRoles.js` 스켈레톤, `CreateWizard.jsx`(3스텝) 구현
  - **완료 검증**: `npm run dev` → 위저드에서 유형/목표/마감일/팀원 입력 후 생성하면 다음 단계 자리표시자 화면으로 이동. 새로고침 시 상태 유지, "처음부터 다시" 버튼으로 초기화. `npm run lint`·`npm run build` 통과
- [x] **2단계 — AI 계획 검토 화면**
  - `mockAi.generatePlan()`(유형별 템플릿 + 1~2초 딜레이) + `PlanReview.jsx`(마일스톤 타임라인, 태스크 인라인 수정/삭제/추가, "이대로 확정"/"다시 제안받기")
  - **완료 검증**: 생성 직후 로딩 연출 → 유형별로 다른 계획 표시(시험 주간 회피 문구 포함). 태스크 수정·추가·삭제가 즉시 반영되고, "다시 제안받기"로 변형안이 오며, "이대로 확정"으로 설문 단계 진입
- [x] **3단계 — 팀원 설문 화면**
  - `Survey.jsx`: 팀원 전환 탭 + 제출 현황(n/5), 탭 방식 1~3순위, 기피 최대 1개, 경험 체크, 리더 의향, "남은 팀원 랜덤 채우기", 미제출자 "상관없음" 처리 후 배정 진행
  - **완료 검증**: 5명을 전환하며 제출 시 현황이 갱신되고, 기피는 1개만 선택됨. 랜덤 채우기로 잔여 팀원이 채워지고, 일부 미제출 상태에서도 "마감하고 배정 진행"이 동작
- [x] **4단계 — 배정 로직 + 결과 화면**
  - `assignRoles.js`(점수 매트릭스 + 그리디 + 역할별 min/max) 완성, `mockAi.explainAssignment()`(팀 단위 서술), `Assignment.jsx`(역할 카드 + AI 설명 + 스왑 요청 1회 + 전원 기피 타협안 카드)
  - **완료 검증**: 같은 설문 입력으로 재실행해도 동일한 배정 결과(결정성). 기피 역할은 배정되지 않고, 전원이 '발표'를 기피하면 타협안 카드가 표시됨. 스왑은 양측 동의 다이얼로그를 거쳐 1회만 가능(이후 버튼 비활성)
- [ ] **5단계 — 대시보드**
  - `Dashboard.jsx`: 전체 진행률 바 + D-day, 3열 고정 칸반(내 태스크 강조), 마일스톤별 진행률, 탭 방식 태스크 상태 이동
  - **완료 검증**: 확정된 계획·역할이 대시보드에 반영되고, 태스크 상태를 옮기면 전체·마일스톤 진행률이 갱신됨. 새로고침 후에도 유지. ~400px 모바일 뷰에서 가로 스크롤 없이 표시. 최종 `npm run lint`·`npm run build` 통과
