# Briefy — 자연어 라이프 매니저

> **말하면 정리되고, 아침이면 브리핑되는 나의 라이프 매니저**

- **프로젝트 기간**: 4주
- **주제 분류**: 대학생(본인)이 겪는 문제 해결
- **작성일**: 2026. 7. 6.

---

## 1. 프로젝트 개요

**Briefy**는 할 일, 일정, 운동·공부 루틴, 식단을 각각 다른 앱 대신 **하나의 입력창**에서 관리하는 서비스다. 텍스트로 적거나 말로 하면 AI가 해석해서 알맞은 곳에 분류·저장하고, 매일 아침 하루를 브리핑해준다.

이름 **Briefy**는 시그니처 기능인 "아침 브리핑(briefing)"에서 왔다. 입력은 짧게(brief), 확인은 브리핑으로.

## 2. 문제 정의

**누가**: 관리할 게 많은 대학생 — 수업 과제, 자격증 공부, 운동, 식단, 어학

**무엇이 불편한가**:

1. **관리 항목마다 앱이 다르다** — 할 일은 리마인더, 일정은 캘린더, 운동은 운동 앱, 식단은 또 다른 앱. 앱을 오가다 결국 아무것도 안 쓰게 된다.
2. **입력이 귀찮다** — 일정 하나 넣는 데 날짜 선택, 시간 설정, 반복 여부까지 폼을 채워야 한다. 기록은 "귀찮은 순간"에 무너진다.
3. **데이터가 쌓여도 해석이 없다** — 캘린더는 나열만 할 뿐, "이번 주 내가 어떻게 살았는지" 말해주지 않는다.

**근거(본인 경험)**: 자격증 실기 준비 + 팀플 + 운동 루틴을 동시에 관리하면서 앱 3~4개를 쓰다가 전부 방치한 경험.

## 3. 솔루션

**핵심 아이디어**: 입력과 해석을 AI에 맡긴 라이프 매니저

| 구분 | 기존 앱 | Briefy |
|---|---|---|
| 입력 | 폼 채우기 (앱별로 따로) | 한 입력창에 자연어 한 줄 — **텍스트 또는 음성** |
| 분류 | 사람이 앱을 골라서 들어감 | AI가 의도를 파악해 자동 분류 |
| 애매할 때 | 처리 불가 | AI가 선택지를 제시하며 되물음 |
| 확인 | 앱마다 따로 조회 | 오늘 화면 하나 + 아침 브리핑 |
| 회고 | 없음 | 주간 리포트 자동 생성 |

### AI의 역할 (기술 포인트)

1. **자연어 파싱**: "다음주 화요일 오후 치과, 전날 알려줘" → 일정 + 리마인더 + 카테고리를 구조화된 JSON으로 변환
2. **의도 명확화**: "운동해야 함"처럼 모호한 입력 → 선택지 버튼으로 되묻기
3. **하루 브리핑**: 할 일 + 루틴 + 어제 기록을 종합해 아침에 한 문단으로 요약
4. **주간 리포트**: 달성률 + 패턴 발견 ("금요일마다 운동을 빼먹네요")

### 입력 채널

```
[텍스트 입력] ─┐
              ├→ [AI 파서] → 분류/저장 → [오늘 뷰 / 브리핑 / 리포트]
[음성 입력] ──┘  (음성은 STT로 텍스트 변환 후 같은 파이프라인)
```

- 음성 입력은 브라우저 내장 Web Speech API 사용 (한국어 지원, 무료)
- 음성 → 텍스트 변환 후 텍스트 입력과 동일한 파서를 타므로 파이프라인은 하나

## 4. 관리 대상 (모듈)

| 모듈 | 내용 | 우선순위 |
|---|---|---|
| 할 일 | 마감일 있는 태스크, 오늘 뷰 정렬 | **P0 (필수)** |
| 일정 | 날짜·시간 있는 이벤트, 리마인더 | **P0 (필수)** |
| 운동 루틴 | 주간 반복("월수금 하체"), 완료 체크, 스트릭 | P1 |
| 영어 공부 루틴 | 일간 체크, 스트릭 | P1 |
| 식단 기록 | 끼니별 기록, 빠른 선택 버튼 | P2 (여유 시) |

P0만 완성돼도 프로젝트가 성립하고, P1까지가 목표, P2는 시간이 남으면 진행.

## 5. 핵심 사용자 시나리오

1. **아침**: 앱을 열면 브리핑 — "오늘 마감 2개, 수요일이라 하체 운동 날, 영어 스트릭 4일째"
2. **등굣길**: 걸으면서 🎤 버튼 누르고 "금요일까지 팀플 자료 만들기"라고 말하면 끝 — 자동으로 할 일에 저장
3. **낮에 생각날 때**: "다음주 화요일 오후 치과, 전날 알려줘" 한 줄 입력 → 일정 + 리마인더 동시 생성
4. **애매한 입력**: "운동" 입력 → "오늘 할 일 / 운동 완료 기록 / 루틴 추가" 버튼 제시 → 탭 한 번으로 확정
5. **저녁**: 오늘 한 루틴 체크 (자연어 또는 체크박스)
6. **일요일 밤**: 주간 리포트 확인 — 달성률과 패턴 피드백

## 6. 4주 개발 계획

| 주차 | 목표 | 산출물 |
|---|---|---|
| 1주차 | 자연어 → JSON 파서 + 할 일/일정 CRUD | 한 줄 입력으로 할 일이 저장되는 데모 |
| 2주차 | 루틴 모듈(운동·영어) + 되묻기 UX + **음성 입력** | 반복 루틴, 스트릭, 모호 입력 처리, 말로 입력 |
| 3주차 | 오늘 뷰 대시보드 + 하루 브리핑 | 아침 브리핑이 생성되는 화면 |
| 4주차 | 주간 리포트 + 다듬기 + 발표 준비 | 완성 데모 + 발표 자료 |

### 리스크와 대비

- **파서 정확도가 낮으면** → 되묻기 UX로 커버 (오히려 데모 포인트가 됨)
- **일정이 밀리면** → 식단(P2) → 영어 루틴 순으로 잘라냄. 코어 파서 + 할 일/일정 + 루틴 1개만으로도 성립
- **상대 날짜("내일", "담주 금") 오류** → 프롬프트에 오늘 날짜 주입으로 해결
- **음성 인식 브라우저 편차** → Web Speech API는 크롬에서 안정적. 시연은 크롬 기준으로 준비

## 7. 범위 제외 (Out of Scope)

이번 4주에는 하지 않고, 발표 때 "확장 계획"으로 언급:

- 푸시 알림 (브라우저/앱 알림 인프라)
- 구글 캘린더 연동
- 식단 영양 분석
- 다중 사용자 / 소셜 기능

## 8. 예상 질문 대비

**Q. 그냥 구글 캘린더 쓰면 되지 않나?**
캘린더는 구조화된 입력을 요구하지만, Briefy는 비정형 문장을 받는다. 그리고 할 일·루틴·기록까지 한 곳에서 관리한다.

**Q. 시리 리마인더랑 뭐가 다른가?**
시리는 단순 문장만 처리하고 복합 문장("치과 예약, 전날 알려줘")이나 모호한 입력에 약하다. Briefy는 복합 파싱 + 되묻기 UX로 해결한다.

**Q. AI가 왜 필요한가?**
저장·조회는 일반 앱과 같지만, 들어가는 문(자연어·음성 파싱)과 나오는 문(브리핑·리포트)이 AI다. 규칙 기반으로는 모호한 자연어 처리가 불가능하다.

---

## 한 문단 소개 (동료 공유용)

> **Briefy — 말하면 정리되는 나의 라이프 매니저**
> 할 일, 일정, 운동, 식단, 공부 루틴을 각각 다른 앱으로 관리하는 대신, 하나의 입력창에 "내일까지 과제 제출", "다음주 화요일 치과, 전날 알려줘"처럼 적거나 말하면 AI가 알아서 분류·저장해주는 개인 관리 서비스입니다. 매일 아침 오늘의 할 일과 루틴을 브리핑으로 확인하고, 주말엔 내 일주일을 리포트로 돌아볼 수 있습니다.