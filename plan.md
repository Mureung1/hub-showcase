# 오늘의 가성비 밥상 (가칭) — 프로젝트 기획

## 문제 정의
자취생·학생 등 1인 가구가 저녁 메뉴를 정할 때, 재료별 최저가를 여러 쇼핑몰에서 하나하나 비교하느라 시간을 쓴다.

그 결과 비슷한 예산으로 더 맛있고 다양한 요리를 시도해볼 기회를 놓친다.

## 기존 서비스 분석 (참고할 점)
| 카테고리 | 예시 서비스 | 제공하는 것 | 부족한 것 |
|---|---|---|---|
| 가격비교 | 다나와, 에누리, 네이버 가격비교 | 개별 상품 단위 최저가 비교는 매우 강력 | "요리 한 그릇" 단위 총 비용은 계산 안 해줌 |
| 레시피 | 만개의레시피, 해먹남녀 | 요리별 재료 목록·조리법 제공 | 실시간 가격·구매 링크 연동 없음 |
| 신선식품 커머스 | 마켓컬리, 쿠팡프레시, 네이버 장보기 | 레시피 기반 재료 세트 판매(자사 상품) | 타 쇼핑몰 가격 비교 안 됨, 자사 상품만 유도 |
| 가계부 | 뱅크샐러드, 브로콜리 | 지출 기록·카테고리 분석 UX가 훌륭함 | 식재료 가격/요리 추천과 연결되지 않음 |
| 공공 시세 정보 | KAMIS 홈페이지 | 품목별 시세 원자료 제공(무료) | 일반 소비자용 UX 아님, 요리 추천 없음 |
**따라 하면 좋을 점:**
- 다나와식 "쇼핑몰 목록 형태로 구매 링크 모아 보여주기" UI (실제 최저가 순위는 API 연동 전까지 표시 안 함)
- 마켓컬리식 "큰 상품 사진 + 굵고 큰 가격 강조" 카드 디자인
- 레시피 대표 사진은 유튜브 영상 썸네일을 사용하고, 상세 화면에서 그 영상으로 바로 연결 (만드는 법도 함께 보여줌)

**차별점(핵심 기회):** "요리 단위 가성비 랭킹 + 공공 시세 기반 자동 계산 + 실제 구매 링크"를 한 번에 묶어 제공하는 서비스가 없음 — 이 조합이 이 프로젝트의 핵심 가치.

## 사용자 시나리오
1. 홈 화면에서 카테고리(메인음식/반찬/간식)별로 가장 저렴한 요리 가격을 확인한다.
2. 카테고리를 클릭해 그 안의 요리들을 가격순으로 본다.
3. 요리를 클릭해 재료 목록과 1인분 총 재료비를 확인한다.
4. 재료를 클릭해 네이버/쿠팡 구매 링크를 확인한다.
5. (예정) 구매 후 가계부에 지출을 기록해 이번 달 식비를 확인한다.

## 핵심 기능 (2개)
- **기능 A: 가성비 Top10 요리 추천 + 구매 링크**
  - 요리별 1인분 총 재료비는 네이버/쿠팡에서 실제로 살 수 있는 가격(현재는 수동 참고가, 추후 API 연동 시 실시간화) 기준으로 계산해 저렴한 순으로 Top10을 보여줌
  - 공공데이터포털(KAMIS) 평균 시세는 총 재료비 계산에 쓰지 않고, "지금 참고 구매가가 시세보다 싼지/비싼지" 비교하는 기준으로만 사용해 사용자가 사기 좋은 시점인지 판단하도록 도움
  - 요리 상세 화면에서 재료별 네이버/쿠팡 검색 링크 제공
- **기능 B: 간단 가계부 (식비 지출 기록)**
  - 브라우저에 지출 내역(날짜, 요리/항목, 금액)을 저장
  - 이번 달 총 식비, 항목별 리스트 확인

## 기술 스택
- 프론트엔드: React + Vite (기존 프로젝트 유지) + Tailwind CSS
- 백엔드: Vercel Serverless Functions (Node.js) — KAMIS API 키를 서버 환경변수로 보관, 프론트에는 노출 안 함
- 데이터 저장: 가계부는 브라우저 localStorage (추후 로그인+DB로 교체 가능하도록 저장 로직을 별도 모듈로 분리)
- 가격 데이터: 요리 총 재료비 계산은 재료별 참고 구매가(현재는 수동 관리, 추후 네이버/쿠팡 API 연동 예정)를 사용. KAMIS 농산물유통정보 오픈API는 이 참고 구매가가 평균 시세보다 싼지 비싼지 비교하는 용도로만 사용 (농산물에만 적용, 고기·수산물·조미료 등은 시세 비교 대상 아님)
- 레시피 데이터: 직접 큐레이션한 요리를 JSON으로 관리, 대표 사진은 유튜브 영상 썸네일(`img.youtube.com`)을 사용하고 영상 링크로 연결
- 구매 연결: 네이버쇼핑/쿠팡 검색 결과 딥링크 (파트너스 API 승인 전까지 임시 방식)
- 배포: Vercel

## 화면 흐름
1. **홈 화면**: 오늘의 가성비 Top10 요리 카드 리스트 (요리명, 1인분 총 재료비, 순위)
2. **요리 상세 화면**: 재료 목록(이름·필요량·참고 구매가·시세 대비 비교 배지) + 총 재료비 + 재료별 네이버/쿠팡 구매 링크
3. **가계부 화면**: 이번 달 지출 내역 리스트 + 합계 + 지출 추가/삭제

## 데이터 흐름
1. (하루 1회 배치) KAMIS API에서 농산물 평균 시세를 가져옴
2. 재료별 참고 구매가(현재는 수동 관리)와 KAMIS 시세를 비교해 "시세보다 싼지/비싼지" 판단
3. 재료별 참고 구매가를 합산해 요리별 1인분 총 재료비를 계산하고, 저렴한 순으로 Top10 선정
4. 프론트엔드는 이 결과를 홈 화면에 표시
5. 사용자가 요리를 클릭하면 재료별 참고 구매가, 시세 비교 배지, 구매 링크를 보여줌
6. 사용자가 가계부에 지출을 기록하면 localStorage에 저장

## 알려진 제약/리스크
- 쿠팡/네이버 파트너스 API 미승인 상태라 재료별 참고 구매가는 실시간 최저가가 아니라 수동으로 관리하는 값이며, 구매는 검색 링크로 대체함 (승인 후 실시간 가격·링크로 교체 예정)
- KAMIS는 농산물 위주라 시세 비교 배지도 농산물 재료에만 적용되고, 고기·수산물·조미료·가공식품은 시세 비교를 제공하지 못함
- 레시피 데이터가 수동 큐레이션이라 초기 요리 가짓수가 적음
- 레시피 영상은 검색으로 고른 것이라 공식 제휴 채널이 아니며, 영상이 비공개/삭제되면 썸네일이 깨질 수 있음
