# 오늘의 가성비 밥상 (가칭) — 프로젝트 기획

## 문제 정의
자취생·학생 등 1인 가구는 저녁 메뉴를 정할 때 재료별 최저가를 여러 쇼핑몰에서 직접 비교하느라 시간을 쓰고, 결과적으로 비슷한 예산으로 더 맛있고 다양한 요리를 시도해볼 기회를 놓친다.

## 기존 서비스 분석 (참고할 점)
| 카테고리 | 예시 서비스 | 제공하는 것 | 부족한 것 |
|---|---|---|---|
| 가격비교 | 다나와, 에누리, 네이버 가격비교 | 개별 상품 단위 최저가 비교는 매우 강력 | "요리 한 그릇" 단위 총 비용은 계산 안 해줌 |
| 레시피 | 만개의레시피, 해먹남녀 | 요리별 재료 목록·조리법 제공 | 실시간 가격·구매 링크 연동 없음 |
| 신선식품 커머스 | 마켓컬리, 쿠팡프레시, 네이버 장보기 | 레시피 기반 재료 세트 판매(자사 상품) | 타 쇼핑몰 가격 비교 안 됨, 자사 상품만 유도 |
| 가계부 | 뱅크샐러드, 브로콜리 | 지출 기록·카테고리 분석 UX가 훌륭함 | 식재료 가격/요리 추천과 연결되지 않음 |
| 공공 시세 정보 | KAMIS 홈페이지 | 품목별 시세 원자료 제공(무료) | 일반 소비자용 UX 아님, 요리 추천 없음 |

**따라 하면 좋을 점:**
- 다나와식 "여러 쇼핑몰 링크를 한 화면에 모아 보여주기" UI
- 마켓컬리식 "요리 단위로 재료를 묶어서 보여주기" 구성 방식
- 뱅크샐러드식 "카드형 요약 + 카테고리 정리" UI 패턴

**차별점(핵심 기회):** "요리 단위 가성비 랭킹 + 공공 시세 기반 자동 계산 + 실제 구매 링크"를 한 번에 묶어 제공하는 서비스가 없음 — 이 조합이 이 프로젝트의 핵심 가치.

## 사용자 시나리오
1. 사용자가 웹에 접속하면 오늘 기준 "가성비 Top10 요리" 카드 목록이 바로 보인다.
2. 마음에 드는 요리를 클릭하면 필요한 재료 목록, 재료별 예상 가격, 1인분 총 재료비를 확인한다.
3. 재료 옆의 "네이버에서 구매" / "쿠팡에서 구매" 링크를 눌러 실제 쇼핑몰로 이동해 구매한다.
4. 구매 후 이번 지출을 가계부에 기록해서 이번 달 식비를 확인한다.

## 핵심 기능 (2개)
- **기능 A: 가성비 Top10 요리 추천 + 구매 링크**
  - 공공데이터포털(KAMIS) 시세를 반영해 요리별 1인분 총 재료비를 계산하고, 저렴한 순으로 Top10을 보여줌
  - 요리 상세 화면에서 재료별 네이버/쿠팡 검색 링크 제공
- **기능 B: 간단 가계부 (식비 지출 기록)**
  - 브라우저에 지출 내역(날짜, 요리/항목, 금액)을 저장
  - 이번 달 총 식비, 항목별 리스트 확인

## 기술 스택
- 프론트엔드: React + Vite (기존 프로젝트 유지) + Tailwind CSS
- 백엔드: Vercel Serverless Functions (Node.js) — KAMIS API 키를 서버 환경변수로 보관, 프론트에는 노출 안 함
- 데이터 저장: 가계부는 브라우저 localStorage (추후 로그인+DB로 교체 가능하도록 저장 로직을 별도 모듈로 분리)
- 시세 데이터: KAMIS 농산물유통정보 오픈API (농산물), 비농산물(고기·수산물·조미료 등)은 수동 정리한 참고 고정가 사용
- 레시피 데이터: 직접 큐레이션한 요리 10~20개를 JSON으로 관리
- 구매 연결: 네이버쇼핑/쿠팡 검색 결과 딥링크 (파트너스 API 승인 전까지 임시 방식)
- 배포: Vercel

## 화면 흐름
1. **홈 화면**: 오늘의 가성비 Top10 요리 카드 리스트 (요리명, 1인분 총 재료비, 순위)
2. **요리 상세 화면**: 재료 목록(이름·필요량·참고 가격) + 총 재료비 + 재료별 네이버/쿠팡 구매 링크
3. **가계부 화면**: 이번 달 지출 내역 리스트 + 합계 + 지출 추가/삭제

## 데이터 흐름
1. (하루 1회 배치) KAMIS API에서 농산물 시세를 가져와 요리별 총 재료비를 재계산
2. 계산 결과를 저렴한 순으로 정렬해 Top10 선정
3. 프론트엔드는 이 결과를 홈 화면에 표시
4. 사용자가 요리를 클릭하면 상세 재료·구매 링크를 보여줌
5. 사용자가 가계부에 지출을 기록하면 localStorage에 저장

## 알려진 제약/리스크
- 쿠팡/네이버 파트너스 API 미승인 상태라 실제 "최저가"는 제공하지 못하고 검색 링크로 대체함 (승인 후 교체 예정)
- KAMIS는 농산물 위주라 고기·수산물·조미료·가공식품 가격은 수동으로 관리해야 하며, 시세 변동을 실시간 반영하지 못함
- 레시피 데이터가 수동 큐레이션이라 초기 요리 가짓수가 적음 (10~20개)
