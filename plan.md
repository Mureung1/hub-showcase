# 끼니픽 — 프로젝트 기획

> 똑똑하게 고르고, 맛있게 아끼는 한 끼

## 이번 주 목표 (4주차, 데모 준비 주간)

**목표**
- 데모에 필요한 핵심 사용자 흐름을 완성한다.
- React와 Express를 외부에서 접속할 수 있게 배포한다.
- 배포 환경에서 화면, 서버, DB 연결을 확인한다.
- 개발과 배포에 사용한 Agent 협업 과정을 설명한다.
- 다시 사용할 수 있는 워크플로우 문서를 만든다.

**사전지식**
- React 화면을 만들고 Express API와 연결해봤음
- Supabase에 데이터를 저장하고 조회해봤음
- 환경변수로 주소와 비밀 키를 관리해봤음
- 테스트와 직접 확인으로 기능을 검증해봤음

### 기능요구사항

**핵심 기능 완료**
- 데모에서 보여줄 핵심 사용자 흐름 하나를 정한다.
- 핵심 흐름이 화면, 서버, DB까지 동작하게 만든다.
- 배포 전에 끝낼 기능과 미룰 기능을 구분한다.

**서비스 배포**
- React 서비스를 Vercel에 배포한다.
- Express 서버를 Render에 배포한다.
- Supabase 연결에 필요한 환경변수를 설정한다.
- 배포된 서비스에서 핵심 기능을 확인한다.
- 배포 환경에서 발견한 오류를 고친다.

**영상 제출 — 수요일까지**
- 수요일 밤 10시까지 5분 미만 영상을 만들어서 PR에 포함한다.
- 영상에 포함할 것(아래 내용을 기반으로 자유롭게 구성): 서비스 설명·동작 시연, 기술적 특징, 주요 문제해결 과정, Agent 활용 방식 등.
- 영상 녹화 후 음성 또는 자막으로 내용 설명이 있어야 함.
- youtube(비공개 업로드 가능), 그 외 영상 확인 URL(drive 등) 가능.
- 완성 후 `showcase.json`에 `demoVideoUrl` 항목을 추가 ([showcase 가이드](https://connect-aiagentchallenge-26-1.github.io/hub/guide/) 참고).
- 수요일 PR에 포함한다.

**데모 준비**
- 서비스 소개와 핵심 기능 시연 순서를 정한다.
- Agent·Skill·규칙 문서의 사용 관계를 그림으로 정리한다.
- 배포 주소와 핵심 기능을 데모 전에 다시 확인한다.

### 프로그래밍 요구사항

**배포 설정**
- 비밀 키를 GitHub에 올리지 않는다.
- FE와 BE가 다른 폴더에 있으면 배포 서비스의 기준 폴더를 각각 설정한다.
- Express는 배포 환경이 제공하는 포트를 사용한다.
- FE는 배포된 BE 주소를 환경변수로 읽는다.

**검증**
- FE 주소가 외부에서 열리는지 확인한다.
- BE 상태 확인 API가 응답하는지 확인한다.
- 화면에서 보낸 요청이 DB 저장·조회까지 이어지는지 확인한다.
- 실패한 배포는 로그를 읽고 원인을 기록한다.

**Agent 관련 산출물**
- 배포 과정과 확인 기준을 워크플로우에 추가한다.
- 4주간 사용한 Agent·Skill·규칙 문서를 정리한다.
- Agent 협업 과정을 설명하는 시각 자료를 만든다.
- 캠프 이후에도 사용할 워크플로우 문서를 완성한다.

> 참고: 기존 기술 스택 절(아래 "기술 스택" 섹션)은 백엔드를 Vercel Serverless Functions로 명시하고 있는데, 이번 주 목표는 Express를 Render에 별도 배포하는 것으로 바뀌었다 — 실제로 어느 쪽으로 갈지 결정되면 "기술 스택" 섹션도 함께 갱신할 것.

## 브랜드 · 캐릭터
- **서비스명**: 끼니픽 
- **마스코트**: 기니피그 캐릭터 '기니' — 끼니픽의 가이드 역할. 돋보기로 재료를 살펴보고, 계산기를 두드리고, 절약에 성공하면 함께 기뻐하는 등 사용자의 감정 흐름(탐색 → 계산 → 발견 → 절약 성공)에 맞춰 다양한 표정·포즈로 등장
- **홈 화면 오프닝 연출**: 원룸(자취방) 공간 일러스트 안에 문이 열린 냉장고가 있고, 그 옆에 기니가 서서 열린 냉장고 안을 들여다보며 "어떤 재료가 있더라?" 하는 장면으로 시작. 이 장면이 곧 "지금 냉장고 재료를 골라줘"라는 유도 역할을 함
- **톤앤매너**: 귀엽고 친근한 일러스트 중심. 기존에 검토했던 Figma/Miro 스타일(`DESIGN.md`, `miro/DESIGN.md`)은 마스코트 방향과 안 맞아 재검토 필요 — 캐릭터 일러스트가 확정되는 대로 색상·타이포 톤을 마스코트에 맞춰 다시 정의할 예정
- **주의**: 캐릭터 디자인은 아직 확정이 아니라 계속 수정될 예정. 화면 목업에 넣을 때도 "최종본"이 아니라 방향성 참고로만 사용

## 문제 정의
- **누가**: 예산이 빠듯한 1인 가구(대학생·사회초년생 등)를 주 타겟으로 하되, 냉장고 재료를 파악하고 소진해야 하는 고민 자체는 자취 여부와 무관하게 누구에게나 해당됨
- **언제**: 냉장고에 재료가 애매하게 남아 있을 때 — 다 먹기엔 애매하고 버리기엔 아까운 시점. 예산이 빠듯한 시기일수록 이 고민의 무게가 더 커짐
- **어디서**: 집 냉장고 앞에서 뭘 해먹을지 고민하다가, 결국 답을 못 찾고 네이버·쿠팡 등 쇼핑몰 앱으로 넘어가 핫딜에 혹하거나 새 재료를 사들임
- **무엇을**: 냉장고에 뭐가 있는지 정확히 파악하지 못한 채로 뭘 해먹을지 고민하다가, 결국 배달을 시키거나 할인 상품을 사거나 장을 보고 난 뒤 그대로 방치해서 낭비함
- **어떻게**: 가진 재료로 만들 수 있는 요리를 모르는 채로, 정작 재료를 살 땐 할인 상품과 광고에 이끌려 구매하고 여러 쇼핑몰 앱을 오가며 가격을 비교함
- **왜**: 기존 레시피 서비스는 재료를 검색하면 요리는 알려주지만 "내가 가진 여러 재료를 동시에 소진할 수 있는 요리"는 걸러주지 않고, 기존 가격비교 서비스는 상품 단위 최저가 비교에는 강해도 "요리 단위로 뭘 사야 하는지"는 알려주지 않음 — 이 빈틈 때문에 냉장고 재료 파악도, 낭비 없는 장보기도 둘 다 해결되지 않고 있음
- **빠지기 쉬운 함정**: "가진 재료로 완성되는 요리"와 "몇 가지만 더 사면 완성되는 요리"를 한 리스트에 섞어서 보여주면, 사용자는 "지금 바로 먹을 수 있는 요리"인 줄 알고 골랐다가 실제로는 배송을 기다려야 한다는 걸 뒤늦게 알게 됨 — 두 상황은 사용자에게 전혀 다른 의미(즉시 조리 vs 장보기 계획)이므로 추천 단계에서부터 구분해서 보여줘야 함

## 기존 서비스 분석 (참고할 점)
| 카테고리 | 예시 서비스 | 제공하는 것 | 부족한 것 |
|---|---|---|---|
| 가격비교 | 다나와, 네이버 가격비교 | 개별 상품 단위 최저가 비교는 매우 강력 | "요리 한 그릇" 단위 총 비용은 계산 안 해줌 |
| 레시피 | 만개의레시피 | 요리별 재료 목록·조리법 제공, 종류별/상황별/재료별/방법별 필터로 좁혀보는 UX | 재료 하나씩만 검색될 뿐, "내가 가진 여러 재료를 동시에 소진하는 요리"는 추천 안 해줌. 실시간 가격·구매 링크 연동도 없음 |
| 신선식품 커머스 | 마켓컬리, 쿠팡프레시 | 레시피 기반 재료 세트 판매(자사 상품) | 타 쇼핑몰 가격 비교 안 됨, 자사 상품만 유도 |

**따라 하면 좋을 점:**
- 다나와식 "쇼핑몰 목록 형태로 구매 링크 모아 보여주기" UI (실제 최저가 순위는 API 연동 전까지 표시 안 함)
- 마켓컬리식 "큰 상품 사진 + 굵고 큰 가격 강조" 카드 디자인
- 레시피 대표 사진은 유튜브 영상 썸네일을 사용하고, 상세 화면에서 그 영상을 끼니픽 페이지 안에 바로 재생 (새 탭으로 이동하지 않고 임베드 플레이어로 보여줌 — 사용자 피드백 반영, 예전에는 새 탭에서 유튜브로 이동하는 방식이었음)
- 만개의레시피식 "필터로 좁혀보기" UX — 다만 종류별/상황별/재료별/방법별을 그대로 따라 하지 않고, 우리 서비스 목적(가성비)에 맞게 **음식종류·시간·재료** 3가지로 재구성

**차별점(핵심 기회):** "냉장고에 있는 재료를 동시에 소진하는 요리 추천 + 부족한 재료만 구매 링크로 연결 + 요리 단위 총 재료비 계산"을 한 번에 묶어 제공하는 서비스가 없음 — 이 조합이 이 프로젝트의 핵심 가치.

## 사용자 시나리오
1. 냉장고 앞에서 뭘 해먹을지 몰라 고민되면, 배달을 시키거나 핫딜에 혹해서 새 재료를 사기 전에 끼니픽을 먼저 연다. 첫 화면은 원룸 배경 속 문이 열린 냉장고 옆에 기니가 서서 안을 들여다보며 "어떤 재료가 있더라?" 하는 장면이고, 그 아래 대표 재료 체크리스트가 있다.
2. 사용자가 실제로 냉장고에 있는 식재료들을 체크하고 "완료"를 누른다.
3. 홈 화면으로 넘어가면, 고른 재료가 들어가는 요리들이 "지금 바로 만들 수 있어요"와 "재료 조금만 사면 돼요" 두 섹션으로 나뉘어 나타난다 (각 섹션 안에서는 1인분 총 재료비가 저렴한 순, 스크롤하면 계속 이어짐). 뒤 섹션의 카드에는 부족 재료 개수 배지가 붙어 있어 클릭하기 전부터 장보기가 필요하다는 걸 알 수 있다. 음식종류·시간 필터로 더 좁힐 수 있다.
4. 마음에 드는 요리를 클릭하면 상단에 어느 트랙인지(지금 바로 완성 / 재료 N개 필요) 다시 한번 안내되고, 재료 목록에서 "구매 필요" 재료가 먼저 눈에 띄게 보이고 그 아래 "보유" 재료도 함께 보인다. 1인분 총 재료비도 같이 표시된다.
5. "구매 필요" 재료는 우선적으로 체크하고, "보유" 재료도 다시 살지 말지는 사용자가 직접 골라 체크할 수 있다. 체크한 재료들을 네이버쇼핑 구매 링크로 한 번에 넘긴다.
6. (다음 단계) 지금까지 확인한 요리들을 바탕으로 이번 달 식비 예상치를 확인하며, 낭비 없이 재료를 썼는지 돌아본다.

## 핵심 기능 (3개)
- **기능 A: 냉장고 재료 선택 → 보유 재료 기반 레시피 추천 (즉시 조리 / 장보기 2개 트랙으로 분리)**
  - 첫 화면(냉장고 화면)은 "원룸 + 문 열린 냉장고 + 냉장고를 들여다보는 기니" 장면으로 시작. 이 장면 아래에 자취생이 자주 쓰는 대표 재료 체크리스트(현재 29개)를 보여주고, 사용자가 지금 냉장고에 있는 재료를 고르고 "완료"를 누르면 저장됨
  - "완료"를 누르면 홈 화면으로 넘어가는데, 추천 리스트를 하나로 섞지 않고 두 트랙으로 나눠서 보여줌 (각 트랙 안에서는 1인분 총 재료비가 저렴한 순으로 정렬, 스크롤하면 계속 이어짐):
    - **지금 바로 만들 수 있어요**: 보유 재료만으로 완성되는 레시피(부족 재료 0개). 구매 링크를 노출하지 않아 "당장 조리 가능"이라는 의미가 명확함
    - **재료 조금만 사면 돼요**: 부족 재료가 1~2개인 레시피. 카드에 "○개만 더 있으면" 배지로 부족분을 미리 표시해, 클릭하기 전부터 장보기가 필요한 요리라는 걸 알 수 있게 함
    - 부족 재료가 3개 이상인 레시피는 위 두 트랙에서는 제외 — "거의 다 있는" 요리만 장보기 트랙에 노출해서, 이 트랙이 사실상 새로운 장보기 목록 만들기가 되지 않도록 함 (1~2개 / 3개 기준은 잠정값, 실사용 보며 조정)
    - **전체 둘러보기(가격순)**: 위 두 트랙과 별개로, 부족 재료 개수와 상관없이 전체 레시피를 1인분 총 재료비 낮은 순으로 보여주는 3번째 섹션. 보유 재료 매칭과 무관하게 "그냥 뭐 있나 둘러보고 싶다"는 탐색 용도 — 두 트랙에서 제외된(부족 3개 이상) 레시피도 여기서는 보임
  - 음식종류(메인음식/반찬/간식)·시간(10분 미만/10~20분/20~30분/30분 이상) 필터로 추천 리스트를 추가로 좁힐 수 있음 (필터는 세 섹션 각각에 적용)
  - 매칭되는 레시피가 하나도 없을 때(주로 조미료만 체크하고 실제 재료를 안 고른 경우) 보여주는 빈 결과 카드에 "재료 고르기"(냉장고 화면으로 이동)와 "재료 구매"(재료픽 페이지로 이동) 버튼을 함께 제공 — 추천을 못 받아도 바로 장보기로 이어지게 함. 재료픽은 아래 "기능 C" 참고
- **기능 B: 레시피 상세 — 보유/구매 필요 재료 구분 + 구매 여부 선택 + 구매 링크**
  - 상세 화면 상단에 어느 트랙에서 들어왔는지 한 줄로 안내 (예: "지금 있는 재료로 완성돼요" / "재료 N개만 더 있으면 완성돼요") — 리스트에서 본 즉시성 여부를 상세에서도 다시 한번 분명히 함
  - 재료 목록에서 "구매 필요" 재료를 먼저 눈에 띄게 보여주고, "보유" 재료는 그 아래 함께 보여줌 — 부족한 재료부터 먼저 챙기도록 우선순위를 시각적으로 구분
  - "구매 필요" 재료는 기본으로 체크돼 있고, "보유" 재료도 다시 살지 말지는 체크박스로 사용자가 직접 선택할 수 있음 (예: 양이 부족해 보이면 보유 재료도 추가 구매 가능)
  - 요리별 1인분 총 재료비는 네이버/쿠팡에서 실제로 살 수 있는 가격(현재는 수동 참고가, 네이버는 검색 오픈API 연동으로 실시간화 진행 중, 쿠팡은 파트너스 승인 전까지 수동 유지) 기준으로 계산
  - 공공데이터포털(KAMIS) 평균 시세는 총 재료비 계산에 쓰지 않고, "지금 참고 구매가가 시세보다 싼지/비싼지" 비교하는 기준으로만 사용해 사용자가 사기 좋은 시점인지 판단하도록 도움
  - 체크한 재료들을 네이버 검색 링크로 연결 (쿠팡은 파트너스 승인 후 추가 예정)
- **기능 C: 재료픽 — 냉장고에 재료가 아예 없는 사용자를 위한 개별 식재료 구매 페이지**
  - 마켓컬리의 "오늘의 카테고리 랭킹"(카테고리 탭 + 순위 배지 그리드) UI를 참고하되, 완성품이 아니라 냉장고 체크리스트의 개별 식재료(현재 65개)를 파는 페이지로 각색
  - 상단 네비게이션 "재료픽" 탭 또는 홈 화면 빈 결과 카드의 "재료 구매" 버튼으로 진입(`/shop`)
  - 카테고리 탭(채소/고기/해산물/가공식품/면·곡물/기타/조미료)을 고르면 그 카테고리의 재료가 카드 그리드로 나타남 — 카드 사진·가격은 네이버쇼핑 검색 최저가 결과에서 가져오고, 아직 안 불러왔거나 실패하면 이모지로 대체 (순위 배지는 실제 판매 데이터가 없어 오해 소지가 있다고 판단해 뺌)
  - 카드의 "가격 비교" 버튼을 누르면 페이지 이동 없이 하단 패널에 그 재료의 네이버 판매처별 후보 목록이 뜸(레시피 상세 화면의 구매 패널과 동일한 컴포넌트 재사용) — 후보를 체크하면 링크로 바로 이동해 구매 가능

**우선순위에서 미룬 기능:** 이번 달 식비 예상치 — 사용자가 확인한 요리들의 총 재료비를 브라우저에 누적해 이번 달 예상 식비를 보여주는 기능(수동 지출 입력 없음). 필터+랭킹 기능이 자리잡은 뒤 다음 단계로 진행 예정 (`checklist.md` 참고).

## 기술 스택
- 프론트엔드: React + Vite (기존 프로젝트 유지) + Tailwind CSS
- 백엔드: Vercel Serverless Functions (Node.js) — KAMIS API 키·네이버 Client ID/Secret을 서버 환경변수로 보관, 프론트에는 노출 안 함
- 데이터 저장: 레시피·재료 데이터는 Supabase(Postgres) `recipes` 테이블에 저장하고, Express API(`/api/recipes`, `/api/recipes/:id`)를 통해서만 조회 — 프론트엔드가 Supabase에 직접 접근하지 않음. 이번 달 식비 예상치 계산에 쓸 요리 확인 기록은 브라우저 localStorage 예정(추후 로그인+DB로 교체 가능하도록 저장 로직을 별도 모듈로 분리)
- 가격 데이터: 요리 총 재료비 계산은 재료별 참고 구매가(현재는 수동 관리 값을 `recipes` 테이블에 함께 저장, 네이버쇼핑 검색 오픈API 연동을 우선 진행 중 — 승인 없이 바로 발급되는 개발자 API라 쿠팡보다 먼저 진행)를 사용. KAMIS 농산물유통정보 오픈API는 이 참고 구매가가 평균 시세보다 싼지 비싼지 비교하는 용도로만 사용 (농산물에만 적용, 고기·수산물·조미료 등은 시세 비교 대상 아님)
- 레시피 데이터: 직접 큐레이션한 요리를 Supabase `recipes` 테이블로 관리하고(원본은 `src/data/mockRecipes.js`, `scripts/seedRecipes.js`로 시딩), 필터에 쓸 음식종류·예상 조리시간·주재료 태그를 함께 저장. 대표 사진은 유튜브 영상 썸네일(`img.youtube.com`)을 사용하고, 상세 화면에서는 유튜브 iframe 임베드 플레이어로 페이지 안에서 바로 재생 (새 탭으로 안 열림 — 사용자 피드백 반영). 공공데이터포털(data.go.kr) 레시피 데이터로 교체하는 방안도 검토했으나, 재료가 비정형 문자열이라 파싱 부담이 크고 가격·영상 정보가 없어 이번 주는 보류 (`task.md` P2)
- 레시피 데이터 제작 방법: 영상 자막을 그대로 옮기기보다, 조회수·구독자 수 기준으로 **유명한(신뢰도 높은) 영상 위주로 먼저 선정**한 뒤 그 영상의 **댓글(재료 대체 팁, "저는 이렇게 했어요" 같은 실제 시청자 피드백)**을 참고해서 재료·분량을 정리하는 방식을 우선 시도 (사용자 피드백 반영) — 자막은 "한 큰술"·"적당량"처럼 표현이 애매하고 영상마다 분량이 제각각이라 그대로 쓰기엔 정확도가 떨어짐
- 구매 연결: 네이버는 네이버쇼핑 검색 오픈API(승인 불필요, `/api/naver/search` Express 라우트로 프록시)로 실제 상품·가격 데이터 연동 진행. 쿠팡은 쿠팡파트너스 신청·승인 전까지 검색 결과 딥링크 방식 유지 (신청 자체는 뒤로 미룸)
- 배포: Vercel

## 화면 흐름
1. **냉장고 화면 (첫 화면)**: 원룸 배경 + 문 열린 냉장고 + 냉장고를 들여다보는 기니 일러스트, 그 아래 대표 재료 체크리스트 + 완료 버튼
2. **홈 화면 (추천 리스트)**: 상단에 음식종류·시간 필터, 그 아래 "지금 바로 만들 수 있어요"(부족 재료 0개)·"재료 조금만 사면 돼요"(부족 재료 1~2개, 부족분 배지 표시)·"전체 둘러보기(가격순)"(부족 개수 상관없이 전체) 세 섹션으로 나뉜 추천 리스트 — 각 섹션 안에서 가격순 정렬 (스크롤하면 계속 이어짐)
3. **레시피 상세 화면**: 상단에 트랙 안내 문구(지금 바로 완성 / 재료 N개 필요) + 재료 목록(이름·필요량·참고 구매가, 보유/구매 필요 구분) + 총 재료비 + 유튜브 임베드 플레이어(끼니픽 페이지 안에서 바로 재생, 새 탭 이동 없음) + "구매 필요" 재료의 네이버 구매 링크
4. **재료픽 화면**: 카테고리 탭 + 카드 그리드(사진·가격은 네이버 최저가) + "가격 비교" 클릭 시 왼쪽에 뜨는 판매처별 후보 패널
5. **(다음 단계) 이번 달 식비 예상치 화면**: 이번 달 확인한 요리 목록 + 예상 총 식비

![화면별 UI 동작 시각화](assets/ui-flow.png)
> 이 이미지는 리브랜딩 전(식비구조대 시절) 버전이라 냉장고 씬·기니 캐릭터·보유/구매 필요 구분이 반영돼 있지 않음 — 캐릭터 디자인이 정리되는 대로 다시 그려야 함

> 정적 HTML 프로토타입(`prototype/`, `prototype-v2/`)은 실제 React 앱 화면·플로우가 안정화되면서 역할을 다해 삭제함(2026-07-27) — 화면 흐름 확인은 이제 실제 앱으로 한다.

## 데이터 흐름
1. 사용자가 냉장고 화면에서 냉장고 속 재료를 고르면 그 목록을 저장
2. 고른 재료가 하나 이상 들어가는 요리를 후보로 추리고, 레시피별로 부족 재료 개수를 계산해 0개(즉시 조리)/1~2개(장보기)/3개 이상(두 트랙에서는 제외)으로 그룹핑 — 재료 매칭 기준·부족 개수 임계값은 추후 실제 사용성을 보고 조정 가능
3. (하루 1회 배치, 예정) KAMIS API에서 농산물 평균 시세를 가져와 재료별 참고 구매가와 비교해 "시세보다 싼지/비싼지" 판단
4. 0개/1~2개 그룹과 전체 레시피(부족 개수 무관) 각각에서 후보 요리들의 재료별 참고 구매가를 합산해 1인분 총 재료비를 계산하고, 그룹 내에서 저렴한 순으로 정렬 — 음식종류·시간 필터가 있으면 각 그룹에 추가로 적용해 좁힘
5. 프론트엔드는 "지금 바로 만들 수 있어요" / "재료 조금만 사면 돼요" / "전체 둘러보기(가격순)" 세 섹션으로 홈 화면에 표시하고, 재료 선택이나 필터를 바꾸면 다시 계산해 보여줌
6. 사용자가 요리를 클릭하면 재료 목록을 보여주되, 1번에서 고른 보유 재료는 "보유"로, 나머지는 "구매 필요"로 표시하고 "구매 필요" 재료에만 네이버 구매 링크를 붙임
7. (다음 단계) 사용자가 요리를 확인할 때마다 이번 달 식비 예상치에 반영해 localStorage에 저장

## 알려진 제약/리스크
- 캐릭터(기니) 일러스트와 브랜드 톤앤매너가 아직 확정 전이라 계속 바뀔 수 있음 — 화면 디자인을 캐릭터에 맞춰 다시 잡는 작업이 별도로 필요하고, 기존 `DESIGN.md`/`miro/DESIGN.md`(Figma·Miro 참고)는 리브랜딩 전 스타일이라 나중에 다시 정리해야 함
- 서비스명이 "식비구조대"에서 "끼니픽"으로 바뀌면서 `README.md`·로고 이미지(`src/assets/logo-*.png`) 등 다른 문서·리소스는 아직 예전 이름 그대로라 별도로 맞춰야 함
- 네이버쇼핑 검색 오픈API는 검색 결과 상품·가격 데이터는 주지만 제휴 수수료가 붙는 구매 링크는 아님 (그건 네이버 커머스API 영역이라 승인 필요, 지금 범위 아님)
- 쿠팡은 쿠팡파트너스 미신청 상태라 재료별 참고 구매가·구매 링크가 검색 결과로 대체됨 (신청·승인 후 실시간 가격·제휴 링크로 교체 예정, 지금은 신청 자체를 미룸)
- KAMIS는 농산물 위주라 시세 비교 배지도 농산물 재료에만 적용되고, 고기·수산물·조미료·가공식품은 시세 비교를 제공하지 못함
- 레시피 데이터가 수동 큐레이션이라 초기 요리 가짓수가 적음
- 레시피 영상은 검색으로 고른 것이라 공식 제휴 채널이 아니며, 영상이 비공개/삭제되면 썸네일이 깨질 수 있음. 페이지 안에서 바로 재생하는 임베드 플레이어로 바꾸면서, 업로더가 퍼가기(embed)를 막아둔 영상은 재생이 안 될 수 있어 영상 선정 시 임베드 허용 여부도 함께 확인해야 함
- 요리별 "예상 조리시간"은 실측이 아니라 대략적인 추정치라 실제 조리 시간과 다를 수 있음
- 재료 필터가 대표 재료 체크리스트(현재 29개)로 한정돼 있어, 목록에 없는 재료로는 필터링할 수 없음
- 기능 A에 명시된 음식종류·시간 필터는 아직 미구현 — 홈 화면엔 필터 UI 없이 추천 리스트/전체 둘러보기만 있음 (`checklist.md`「프론트엔드」참고)
- Supabase 연동 구조 점검 결과(2026-07-21): env 변수·시크릿 관리(`.env.local`이 `*.local` 규칙으로 gitignore됨, service_role 키는 서버에서만 사용, 프론트엔드엔 노출 안 됨)는 문제없지만, 아래 두 가지는 안 되어 있음
  - `server/routes/recipes.js`가 라우트 핸들러 안에서 Supabase 쿼리를 직접 호출하고 있어 route → controller → service → client로 계층을 나누지 않음 (지금은 route 파일 하나에 쿼리·응답 가공이 같이 있음) — 라우트가 늘어나면 `server/services/`로 분리하는 리팩터링 필요
  - RLS(Row Level Security) 활성화 여부·정책이 코드·문서 어디에도 명시돼 있지 않음 (`server/lib/supabaseClient.js` 주석에 "service_role 키는 RLS를 우회한다"는 설명만 있음) — 지금은 서버만 DB에 접근하는 구조라 당장 위험하진 않지만, 나중에 프론트에서 직접 Supabase에 접근하는 기능이 생기기 전에 RLS 정책을 정하고 문서화해야 함
