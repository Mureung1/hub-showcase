# 나만의 워크플로우 정리 — 3주간 AI와 일한 방식

`TASKS.md`에 기록된 3주간(7/13~7/24)의 실제 작업 로그를 근거로 정리한다. "이렇게 일하면 좋다더라"가 아니라, 실제로 이 프로젝트에서 반복된 패턴만 담았다.

---

## 1. 일하는 순서 (기본 루프)

```
① 결정(사람)
   ↓
② 계획 분해 — planning-agent
   ↓
③ 구현 — Claude Code(대화형)
   ↓
④ 검증 — feature-verifier
   ↓
⑤ 리뷰 — code-reviewer(버그) / simplify(품질)
   ↓
⑥ 문서 동기화 — spec-drift-check
   ↓
⑦ 커밋 · PR
```

이 루프가 정착되기까지 3주가 걸렸다. 1주차엔 ②④⑤⑥ 단계가 아예 없었고(에이전트를 "이해"만 하던 시기), 2주차 중반부터 ④(feature-verifier)가, 3주차부터 ⑤⑥(code-reviewer, spec-drift-check)이 생겼다. 즉 **매주 "이 단계가 왜 필요한지"를 실제로 겪은 뒤에 그 단계를 자동화하는 도구를 만드는 순서**로 워크플로우 자체가 진화했다.

## 2. 역할 분담 원칙

| 항상 내가 하는 것 | 항상 AI에게 맡기는 것 |
|---|---|
| 아키텍처/컨셉 결정 ("리뷰 답변 도우미" → "리뷰 매니저 AI" 확장, DB를 sqlite→Supabase로 옮기는 결정, "로그인 없는 익명 세션" 원칙을 뒤집는 결정) | 결정된 방향을 실제 코드로 구현 |
| 트레이드오프가 걸린 확인 필요 항목에 답하기 (레이트리밋 수치, API 장애 시 폴백 여부, 타임아웃 정책) | 구현 전 "확인 필요" 항목을 스스로 찾아 목록화 (planning-agent) |
| 검증 결과를 보고 다음 우선순위 정하기 | 실제로 실행해서 PASS/FAIL 근거 남기기 (feature-verifier) |
| 리팩토링 방향(어디까지 손댈지) 승인 | 코드 리뷰 관점을 나눠 병렬로 점검 (code-reviewer/simplify) |
| 최종 리뷰 및 팀 공유 여부 결정 | 문서-코드 드리프트 초안 잡기 (spec-drift-check) |

기준은 하나다 — **"되돌리기 어렵거나 여러 사람에게 영향을 주는 결정은 내가, 그 결정을 코드로 옮기는 실행은 AI가."**

## 3. 만든 Agent

| 이름 | 언제, 왜 만들었나 | 트리거 |
|---|---|---|
| `planning-agent` | 2주차 후반, 3주차 착수 전 "뭐부터 할지" 매번 새로 판단하기 지쳐서 도입 | "작업 나눠줘", "우선순위 정해줘" |
| `feature-verifier` | 2주차 마지막 날, "코드가 맞아 보인다"와 "실제로 그렇게 동작한다"가 다르다는 걸 겪고 신설 — 코드는 안 고치고 실행해서 PASS/FAIL만 남김 | "이 기능 검증해줘", "제대로 도는지 봐줘" |
| `code-reviewer` | 3주차, "코드 리뷰(에이전트 활용)" 이슈를 매번 즉석 프롬프트로 처리하다가 고정 — 정확성(버그)만 보고, 품질은 안 봄(simplify와 역할 분리) | "코드 리뷰해줘", "버그 있는지 봐줘" |

## 4. 쓰는 Skill

| 이름 | 종류 | 언제 쓰나 |
|---|---|---|
| `design-build` | 기본 제공, 1주차에 저장소에 정착 | 디자인 핸드오프를 받아 구현/검증할 때 |
| `simplify` | 기본 제공 | 리팩토링 — 재사용/단순화/효율성/구현 깊이 4관점, 버그는 안 봄 |
| `spec-drift-check` | 3주차 신설(project-local) | 기획서/CLAUDE.md가 실제 코드보다 오래됐는지 점검할 때 |

## 5. 실제로 효과를 본 사례

- **feature-verifier**가 2주차 마지막 날 예시 문구 불일치 버그를 발견 (기획서 예시가 실제 로직으론 `neutral`인데 문서엔 `negative`로 적혀있던 것)
- **code-reviewer**가 3주차 금요일, 방금 짠 레이트리밋 코드를 리뷰하다가 실제 우회 버그 발견 — `X-Session-Id` 헤더 없는 요청은 매번 새 세션으로 취급되어 제한이 무력화되고 있었음
- **feature-verifier**가 3주차 마무리 재검증 중, Claude API가 스키마 밖 키워드를 반환해도 서버가 걸러내지 못하는 버그 발견 (4주차로 인계)

공통점: 셋 다 "코드를 읽기만 해서는" 못 찾고, **직접 실행하거나 다른 관점으로 다시 봐서** 찾은 버그다. 이게 이 세 에이전트를 만든 이유이기도 하다.

## 6. 3주 회고 — 잘한 것 / 아쉬운 것

**잘한 것**
- 매주 "이번 주에 반복해서 힘들었던 일"을 다음 주에 Agent/Skill로 고정하는 패턴을 유지함
- 코드 리뷰(버그)와 리팩토링(품질)을 처음부터 다른 도구로 분리해서, 하나가 다른 하나를 가리는 일이 없었음

**아쉬운 것**
- 테스트 러너(`node:test` → vitest) 같은 컨벤션 결정을 미리 안 정하고 진행하다가, 이미 짠 테스트를 두 번 고쳐 쓴 적이 있음
- `.claude/`가 통째로 gitignore되어 있던 걸 3주차 중반까지 몰랐음 — 만든 Agent/Skill이 한동안 팀에 안 보이는 상태였음(3주차에 뒤늦게 발견·수정)

**다음에 다시 한다면**
- 새 프로젝트를 시작할 때 "이 저장소에서 반복될 도구적 결정"(테스트 러너, 커밋 컨벤션, gitignore 범위)을 첫날에 한 번에 점검하고 시작

---

## 7. 4주차 — 배포와 디자인 반영에서 달라진 것

3주차까지는 루프(①~⑦)가 전부 로컬 코드 안에서 돌았다. 4주차는 "AI가 접근할 수 없는 외부 대시보드(Vercel/Render/Figma)"와 "사람 눈에만 보이는 시각적 결과물(스크린샷)"이 루프에 끼어들면서 역할 분담이 한 번 더 세분화됐다.

### 배포: AI는 안내만, 클릭은 사람이

Vercel/Render 대시보드는 AI가 로그인해서 조작할 수 없다. 그래서 배포는 기존 루프(②구현→③검증)가 아니라 **"AI가 다음 화면에서 뭘 눌러야 하는지 설명 → 사람이 클릭 → 결과(에러 메시지, URL)를 다시 AI에게 전달 → AI가 원인 진단"**을 반복하는 방식으로 진행했다. 이 과정에서 실제로 막힌 지점(포크의 기본 브랜치가 `main`이라 Vercel Root Directory 목록에 실제 코드 폴더가 안 보였던 것, `CORS_ORIGIN`이 로컬 임시값으로 남아 있었던 것)은 전부 사람이 화면을 보고 증상을 전달해야만 AI가 원인을 좁힐 수 있었다 — 로그에 안 남는 종류의 문제라 코드만 봐서는 못 찾는다.

### 사람이 스크린샷으로 버그를 신고하는 패턴

`design-build`로 대시보드를 새 디자인으로 갈아끼운 뒤, 사용자가 실제 화면 스크린샷을 캡처해서 "대기시간 건수도 안 맞고 리뷰도 10건인데 5건이라고 한다"고 신고했다 — 이게 `isToday()` 기준 캐싱 때문에 같은 날 추가된 리뷰가 인사이트에 반영되지 않던 버그(`insight.service.js`)를 잡아낸 계기였다. **되돌리기 어려운 정책 결정(무엇을 기준으로 캐시를 무효화할지)은 AskUserQuestion으로 사용자에게 넘기고, 구현만 AI가 했다** — 3주차 원칙("결정은 사람, 실행은 AI")이 시각적 버그에도 그대로 적용된 사례.

### 디자인 세부 조정은 AI 스크린샷 판단 → 사람 직접 수정으로 역전되기도 함

`design-build`의 "반드시 스크린샷 검증" 규칙은 레이아웃/데이터 정확성 확인에는 잘 맞았지만, "텍스트 입력창을 얼마나 더 키워야 자연스러운가" 같은 순수 취향 문제에서는 AI가 스크린샷을 보고 판단 → 사용자가 말로 다시 고쳐달라고 요청 → 다시 스크린샷을 반복하는 게 비효율적이었다(가로/세로를 번갈아 요청하는 왕복이 실제로 발생). 이 지점에서 사용자가 "내가 직접 수정할게, 어디 고치면 돼?"로 역할을 가져갔고, AI는 정확한 파일·줄 번호(`App.css`의 `.tool-columns`/`textarea min-height`)만 짚어주는 것으로 역할이 바뀌었다 — **미세한 시각적 취향 조정은 AI가 대신 판단하기보다 좌표만 알려주고 사람이 직접 하는 게 더 빠를 수 있다**는 걸 이번 주에 확인했다.

### 새로 굳어진 원칙

- 로그가 안 남는 버그(배포 설정, 시각적 디자인)는 사람의 스크린샷/화면 관찰이 유일한 입력 — AI는 그 관찰을 근거로 원인을 좁히는 역할.
- 정책 결정(캐시 무효화 기준 등)은 버그가 시각적으로 발견됐어도 여전히 AskUserQuestion으로 사람에게 넘긴다.
- 순수 취향 조정(색/크기/비율)은 왕복이 3회를 넘어가면 AI가 계속 대신하기보다 정확한 파일 위치를 알려주고 사람에게 넘기는 게 낫다.
