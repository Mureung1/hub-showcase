# 워크플로우 · Agent 협업 과정

4주간 끼니픽을 만들며 반복한 작업 패턴과 Claude Code와의 협업 방식, 그리고 실제로 겪은 어려움과 해결 과정을 정리한 문서입니다. (원본은 옵시디언 Canvas로도 정리했고, 이 문서는 그 내용을 GitHub에서도 볼 수 있게 마크다운으로 옮긴 것입니다.)

## 단계별 워크플로우 (기획 → 설계 → 구현 → 검증 → 배포)

| 단계 | 도구 | 입력 | 작업순서 | 역할 (사람 / AI) | 확인기준 | 결과물 |
|---|---|---|---|---|---|---|
| ① 기획 | 대화 · 리서치 | 문제의식, 유사 서비스 자료 | 시나리오 정리 → 비교표 → 차별점 압축 | 사람: 문제정의·핵심기능 확정 / AI: 시나리오·비교표 정리 | 문제정의·핵심기능 3개 확정 | `plan.md` |
| ② 설계 | `task-planner` 에이전트, Plan mode | 기획에서 나온 핵심 기능 하나 | 요구사항 쪼개기 → 재사용 확인 → 우선순위 부여 | 사람: P0~P2·범위 승인 / AI: 작업 분해·완료기준 부여 | 하루 크기로 쪼개지고 완료기준 부여됨 | `checklist.md` / `task.md` |
| ③ 구현 | `design-system`·`pptx-builder` 스킬 | 설계에서 나온 작업 목록 하나 | 디자인 토큰 확인 → 컴포넌트화 → 작은 단위 커밋 | 사람: 디자인·컴포넌트 방향 승인 / AI: 토큰 스타일링·코드 작성 | lint·Vitest 통과, 브라우저 재현 확인 | 커밋 단위 코드 변경 |
| ④ 검증 | lint(oxlint) · Vitest, 브라우저 재현 | 구현이 끝난 기능 하나 | 직접 클릭 재현 → 화면/서버/DB 원인 분리 | 사람: 오늘 범위 vs 백로그 이월 판단 / AI: 원인 위치 분리 | 사용 흐름 설명 가능 + 화면~DB 연결 확인 | `checklist.md` 체크 + 백로그 기록 |
| ⑤ 배포 | Vercel · Render 대시보드 | 검증까지 끝난 핵심 흐름 전체 | 가입·연결 → 환경변수 → `/health` → 재검증 | 사람: 대시보드 설정 실행·최종 클릭 / AI: 로그·스크린샷 진단 | FE 열림 + BE 응답 + DB 저장·조회 이어짐 | 라이브 배포 + `devlog.md` 기록 |

## 전 단계 공통 — 옵시디언 Task Board로 이슈·계획 관리

GitHub 이슈 대신 옵시디언 Task Board 플러그인에서 `#AI-Agent-Challenge` 태그로 할 일 상태(할 일/진행 중/완료)를 관리하고, `plan.md`·`checklist.md`의 계획 항목을 이 보드와 함께 추적했습니다.

## 4주간 어려웠던 점과 해결

| 주차 | 어려웠던 점 | 해결/대응 |
|---|---|---|
| 1주차 | 서비스 컨셉을 세 번 갈아엎음 — ScholarMap AI(논문 마인드맵·연구실 채용매칭 도구)는 아이디어보다 기간 안에 구현할 자신이 없어서 포기, 식비구조대(카테고리 랭킹형)로 재출발했지만 이번엔 디자인 자체가 마음에 안 들어서 다시 넘어감 | 마스코트가 있으면 더 친근할 거라 생각했고 "끼니"와 "기니피그"를 연결할 수 있다는 점이 좋아서 끼니픽으로 정착 |
| 1주차 | 디자인 레퍼런스 탐색 — 핀터레스트·다른 회사 디자인을 Claude에게 주고 직접 적용해본 뒤, 버튼 하나로 디자인이 바뀌게 만들어 여러 안을 빠르게 비교 | 정량적 기준보다는 "내가 볼 때 예쁜 디자인"을 기준으로 웜톤 팔레트를 확정하고 `DESIGN_SYSTEM.md`로 토큰화 |
| 2주차 | 진로(식량안보)와 맞닿아서 AI허브 식재료 평균가격으로 쿠팡·네이버 가격이 싼지 비싼지 알려주는 게 원래 기획이었는데, 핵심 기능에 집중하느라 시간이 부족했고 공공데이터포털 레시피도 사진이 있다더니 막상 안 들어가 있고 사용자가 쓰기 힘든 데이터였음 | 오히려 내 취향의 레시피를 직접 넣기로 하면서 마음이 편해짐 — 직접 큐레이션(`mockRecipes.js`)으로 전환 후 Supabase로 이관 |
| 2주차 | 1주차부터 문서를 지속적으로 관리하지 않아 이슈·task.md·checklist.md·plan.md 4곳의 기획 내용이 다 다르고 진행 상황도 안 적혀 있었음 — 작업 중 AI가 맥락을 놓치는 걸 보고 심각성을 깨달음 | 하루 잡아 네 문서를 한 번에 재검토·정리 |
| 3주차 | 실제로 테스트해보며 API 응답을 확인하다 발견 — "짜파게티"를 검색했더니 사천짜파게티나 다른 라면 묶음상품이 섞여서 응답됨 | 실측 샘플을 정규식 휴리스틱으로 정제, 개당가 자동 계산 |
| 3주차 | 밑그림을 주고 Claude로 웹 구조를 만들고 고치는 걸 반복 — 이미지도 밑그림을 그리면 나노바나나(이미지 생성 AI)가 그리고 수정하는 걸 반복했는데, 맥락을 못 잡고 잘못 그리는 경우가 많아 힘들었음 | 한 번에 다 고치려 하지 않고 조금씩(부분적으로) 고쳐가는 방식으로 반복 횟수를 줄임 |
| 3주차 | 네이버 연동을 끝내고 쿠팡으로 넘어가려는데 신청 과정에서야 오픈 API는 가입만으론 못 쓴다는 걸 알게 됨 — 판매 실적이 15만 원 넘어야 최종승인·활성화되는 조건(가입비 아님) | 검색 결과 딥링크로 유지, 실적 쌓이면 재신청 검토 |
| 4주차 | 배포 중 Vercel 결제 요구·빌드 실패·커밋 이메일 문제·SPA 404·로그인 요구·재료매칭 버그까지 오류 6건 연쇄 — AI가 알려주는 대로 했는데 안 되는 경우가 항상 당황스러웠음(설정·버튼이 화면에 안 보임), AI가 스크린샷을 못 알아먹거나 디테일하게 설명 안 하면 알아서 이상하게 만드는 게 답답했음 | 에러 로그·스크린샷 기반으로 하나씩 원인 진단해 해결(`devlog.md` 기록), 4주차에 크롬에서 Claude Code가 알아서 작업하게 만드는 방식(브라우저 자동화)을 알게 되면서 실제로 도움이 됨 |
| 4주차 | 유튜브 자동 자막은 오타가 많은 게 진짜 문제였음 | 댓글에 레시피를 정리해두는 사람이 꼭 있어서 참고, 직접 요리하는 걸 좋아해서 보면서 정리함 |
| 4주차 | 전공자도 아니고 진로도 웹개발자가 아닌데 이걸 왜 하고 있는지 혼란이 왔고, 잘하고 있는 건지 항상 의심됨 | 스킬·에이전트·훅 개념, 웹개발과 Obsidian 활용법(AI Agent 다루는 법, PPT 만드는 법, LLM Wiki로 자료 정리하는 법)을 배울 수 있었고, 매일 팀원들과 줌 회의하며 말하기·생각 전달 연습도 할 수 있어서 의미 있었음 |

## 문제가 생겼을 때 다시 확인할 단계

| 증상 | 먼저 확인할 것 |
|---|---|
| Vercel이 Pro 요금제를 요구함 | 원본 조직 저장소가 아니라 본인 포크를 선택했는지 |
| 빌드가 command not found로 실패 | Install/Build/Output Command가 비어있지 않은지 |
| 커밋 이메일 비공개 오류 | GitHub noreply 주소로 교체했는지 |
| SPA 라우팅 새로고침 시 404 | `vercel.json`에 rewrite가 있는지 |
| 배포 링크 접속 시 로그인 요구 | Deployment Protection이 꺼져 있는지 |
| 체크한 재료가 매칭 안 됨 | `matchNames`와 정확히 문자열 일치하는지 |
| Agent 자동클릭이 실패하는 것처럼 보임 | 좌표 클릭으로 재현해 앱/도구 문제 구분 |
| Render 응답이 느림 | 무료 플랜 슬립 — 미리 접속해 깨워뒀는지 |

## 다음 프로젝트에 가져갈 것

- **가장 효과적이었던 패턴**: 실제 API를 먼저 호출해 샘플 데이터를 모으고, 거기서 발견한 문제를 정규식 휴리스틱으로 고친 뒤 다시 실제 데이터로 재검증하는 반복 — 목업으로 미리 설계하지 않고 실측 우선
- **막혔을 때 도움이 된 질문**: "화면/서버/DB 중 어디서 끊겼는가?"로 항상 원인 위치부터 분리
- **도구별 최적 활용 시점**: `task-planner`는 요구사항이 모호할 때, `design-system` 스킬은 UI 파일을 만들거나 스타일을 고칠 때만, `pptx-builder`는 진짜 .pptx가 필요할 때만
- **아이디어가 옆길로 새는 걸 막아준 습관**: 핵심 기능을 생각하다가도 다른 아이디어가 자꾸 떠올라서 기획이 제일 힘들었는데, Obsidian과 `plan.md`에 기획 내용을 계속 쌓아가며 작업하니 맥락을 안 놓치고 원래 하려던 기획으로 돌아올 수 있었음
- **문서를 실시간으로 갱신해야 하는 이유**: 1주차에 계획 문서를 방치했더니 이슈·task.md·checklist.md·plan.md가 서로 다른 얘기를 하게 됐고, 작업 중 AI가 맥락을 놓치는 걸 직접 보고서야 심각성을 깨달음 → 작업할 때마다 바로바로 문서에 반영하는 습관으로 교정
