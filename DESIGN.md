# 우리결 — 디자인 시스템 (DESIGN.md)

> 이 문서는 Claude Design으로 제작한 14개 화면 시안을 기반으로 정리한 디자인 시스템이다.
> 개발 Agent(Claude Code)가 화면을 구현할 때 이 문서의 색상·타이포·컴포넌트 규칙을 그대로 따라야 한다.
> 목적: 어떤 화면을 새로 만들거나 수정하더라도 "우리결"만의 톤앤매너가 깨지지 않도록 하는 것.

---

## 1. 브랜드 톤

- 전체 무드: 따뜻하고 포근한 살구빛 파스텔. 대학생 대상 서비스답게 부담스럽지 않고 친근한 느낌.
- 로고: 무한대(∞) 모양 두 고리가 겹쳐진 형태 + 사람 아이콘. 오렌지 계열 그라데이션.
- 카피 톤: 존댓말이지만 딱딱하지 않게. "~해요", "~할게요" 같은 부드러운 어미 사용. 이모지 적절히 혼용(🎓 🧭 💬 등).

---

## 2. 색상 (Color Palette)

### 2.1 기본 팔레트

| 이름 | 역할 | 근사 HEX | 사용처 |
|---|---|---|---|
| `--bg-cream` | 배경 기본 | #FBE8DA | 모든 화면 배경 |
| `--coral-primary` | 메인 브랜드 컬러 / Primary 버튼 | #EE8F6C | 로그인 버튼, 가입하기 버튼, 팀확정 버튼, 로고 |
| `--coral-light` | 연한 강조 배경 | #F6C7B4 | 과팅 관련 카드 배경, 보조 강조 |
| `--text-brown-dark` | 제목/본문 텍스트 | #4A2E22 | 페이지 제목, 카드 제목 |
| `--text-warm-gray` | 보조/placeholder 텍스트 | #B49A8C | input placeholder, 부제, 캡션 |
| `--card-white` | 카드/입력창 배경 | #FFFFFF | 인풋, 리스트 카드, 흰 배경 카드 |

### 2.2 카테고리 컬러 (과팅 vs 룸메이트)

| 이름 | 역할 | 근사 HEX | 사용처 |
|---|---|---|---|
| `--roommate-mint` | 룸메이트 트랙 메인 컬러 | #A8DFC3 | 룸메 흐름 카드, "친구형 룸메" 카드, 생활성향 테스트 배경(좌측) |
| `--roommate-mint-bg` | 룸메이트 연한 배경 | #D6F0E4 | 룸메 관련 보조 배경 |
| `--dating-pink` | 과팅 트랙 메인 컬러 | #F0AFC4 | 과팅 흐름 카드, 이상형 테스트 관련 |
| `--dating-coral-bg` | 과팅 연한 배경 | #F6D4C8 | 과팅 카드 배경(우측), 시끄러운 장소 등 대비 옵션 |
| `--neutral-beige` | 중립/비즈니스 룸메 | #E8E0D6 | "비즈니스 룸메" 카드처럼 중립적인 옵션 |

### 2.3 아바타 컬러 세트 (매칭 리스트용)

사용자를 구분하기 위한 단색 원형 아바타 팔레트. 순서대로 순환 사용.
```
#A8DFC3 (민트) / #F0B49A (살구) / #F3AEC8 (핑크) / #A9C8EF (블루) / #F5C86A (옐로우)
```

### 2.4 시맨틱 컬러

| 이름 | 역할 | HEX |
|---|---|---|
| `--success-mint` | 성공/완료/인증 완료 배지 | #6FCF9B (인증하기 버튼 등 초록 계열) |
| `--like-red` | 관심/좋아요 하트 아이콘 | #E85D5D |

---

## 3. 타이포그래피

| 용도 | 굵기 | 크기(권장) | 색상 |
|---|---|---|---|
| 페이지 대제목 (예: "처음 오셨군요!") | Bold | 22~24px | `--text-brown-dark` |
| 카드/섹션 제목 (예: "회원가입", "1:1") | SemiBold~Bold | 18~20px | `--text-brown-dark` |
| 본문/설명 문구 | Regular | 14~15px | `--text-warm-gray` |
| 버튼 텍스트 | Bold | 16px | 배경에 따라 흰색 또는 진한 브라운 |
| 인풋 placeholder | Regular | 14px | `--text-warm-gray` |
| 진행률 표시 ("1/8") | Regular | 12px | `--text-warm-gray`, 좌상단 고정 |

- 한글 폰트 우선: 부드러운 라운드 계열(예: Gowun Dodum, Pretendard) — 기존 서비스 흐름 페이지 작업에서 Gowun Dodum 적용 이력 있음, 통일 권장.
- 숫자/영문 혼용 시에도 동일 폰트 패밀리 유지.

---

## 4. 컴포넌트 규칙

### 4.1 Primary 버튼
- 풀와이드(부모 컨테이너 100%), border-radius 완전 라운드(pill 또는 16px 이상)
- 배경 `--coral-primary`, 텍스트 흰색 또는 진한 브라운(대비 확인 후 결정), Bold
- 예: 로그인, 가입하기, 팀 확정, 인증하기(초록 버전은 `--success-mint`)

### 4.2 인풋 필드
- 흰 배경(`--card-white`), 테두리 없음, 은은한 그림자(box-shadow)로 카드와 구분
- border-radius 12~16px
- placeholder는 `--text-warm-gray`

### 4.3 양자택일 카드 (질문형 테스트 화면)
- 좌우 2분할, 세로로 긴 큰 카드
- 좌측/우측 각각 다른 배경색(대비되는 파스텔 톤: 민트 vs 코랄핑크)
- 카드 중앙에 큰 이모지/아이콘 심볼
- 하단에 흰색 pill 라벨로 선택지 텍스트 표시
- 좌상단에 진행률("1/8") 텍스트

### 4.4 선택 카드 (목적 선택형, 예: 과팅 vs 룸메이트)
- 세로로 큰 라운드 카드, 테두리 있는 버전(outline) 사용
- 카드 상단 큰 픽토그램(집 모양, 하트 등)
- 카드 하단 제목 + 부제
- 카테고리별 컬러(과팅=핑크 계열 테두리, 룸메=민트 계열 테두리)

### 4.5 리스트 카드 (매칭 결과)
- 흰 배경, border-radius 16~20px, 은은한 그림자
- 좌측 단색 원형 아바타 + 이름("~님") + 태그(pill) 여러 개 가로 나열 + 우측 말풍선 아이콘
- 태그는 작은 pill 형태, 파스텔 배경 + 진한 텍스트 (예: "7시 기상", "청소 주2회")

### 4.6 팀 구성 화면 (과팅 전용)
- 원형 아바타 여러 개 가로 나열 (참여자 색상 원 + "초대하기"는 점선 테두리 원 + "+" 아이콘)
- 아바타 하단에 이름 라벨

### 4.7 궁합/매칭 상세 화면
- 원형 아바타 여러 개 + 점선 구분선으로 연결
- 중앙 하단에 궁합 퍼센트 강조 텍스트(코랄 색, Bold, 큰 사이즈)
- 하단 CTA 버튼(하트 이모지 포함)

### 4.8 아이콘/이모지 사용
- 카드 심볼은 실제 아이콘보다 이모지 또는 심플한 플랫 일러스트 선호 (🌙 ⭐ 🌅 😴 🤝 💼 ❤️ 등)
- 톤: 플랫, 그림자 최소, 파스텔 컬러와 어울리는 채도

---

## 5. 레이아웃 원칙

> ⚠️ 우리결은 **앱이 아니라 웹 서비스**다. 모바일 앱처럼 세로로 길쭉하고 폭이 좁은(320~390px) 카드 레이아웃을 기본값으로 쓰지 않는다. 데스크톱 브라우저 화면을 시원시원하게 채우는 걸 기본으로 생각한다.

- **데스크톱 우선.** 기준 뷰포트는 1200~1440px 폭. 콘텐츠 영역은 화면 중앙에 두되, 좁은 모바일 카드처럼 작게 가두지 말고 충분히 넓게 쓴다.
  - 단일 폼(로그인 등): 최대 폭 480~560px 정도로 중앙 배치 — 이 정도까지는 좁아도 됨(입력 폼 특성상)
  - 목적 선택, 양자택일 테스트, 매칭 결과 같은 콘텐츠형 화면: 좌우로 넓게 펼쳐서 **가로 그리드**로 배치 (예: 선택 카드 2개를 세로로 쌓지 않고 좌우로 나란히, 매칭 결과 리스트는 2~3열 그리드 등 고려)
  - 화면 높이도 뷰포트에 맞춰 콘텐츠가 위아래로 여유 있게 배치되도록 하고, 불필요하게 길게 스크롤되지 않게 함
- 반응형은 고려하되, **디자인 기준 자체는 데스크톱 와이드 화면**으로 잡는다. 모바일 대응은 나중에 별도로 breakpoint를 정해서 좁힌다.
- 화면 상단에 로고 또는 진행률 표시로 현재 위치 안내
- 카드 간 여백(gap)은 넉넉하게 — 카드가 서로 붙지 않도록
- CTA 버튼은 폼형 화면에서는 폼 폭에 맞춰 풀와이드, 콘텐츠형 화면에서는 굳이 풀와이드로 늘리지 말고 적정 크기로
- 온보딩류 화면(테스트, 회원가입)은 여백을 넓게, 리스트류 화면(매칭 결과)은 가로 공간을 활용해 카드를 여러 열로 배치해 정보 밀도를 높게

---

## 6. 카테고리 구분 원칙 (중요)

우리결의 핵심 UX 규칙: **룸메이트 = 민트 계열, 과팅 = 핑크/코랄 계열**로 시각적으로 항상 구분한다.
이 색상 규칙은 서비스 흐름 다이어그램, 목적 선택 화면, 테스트 화면, 매칭 결과 화면까지 전 구간에서 일관되게 유지되어야 한다.
새 화면(채팅방, 신고하기, 마이페이지 등)을 만들 때도 어떤 카테고리 소속인지에 따라 이 컬러 규칙을 따를 것.

---

## 7. 아직 디자인되지 않은 화면 (P1, 참고용)

- S-01 랜딩 페이지
- S-12 채팅방 (폴링 방식)
- S-13 신고하기
- S-14 마이페이지

이 화면들을 새로 디자인할 때도 위 색상/컴포넌트 규칙을 그대로 적용해 톤을 유지할 것.

---

*본 문서는 2026.07.09 기준 Claude Design으로 제작한 14개 화면 시안을 분석하여 작성되었으며, 향후 화면이 추가/수정될 경우 이 문서도 함께 업데이트되어야 한다.*
