# 기능 명세 · 체크리스트

> 화면·기능 단위로 "무엇이 됐고 무엇이 안 됐는지"를 추적하는 문서. `client/src/` 실제 코드를 기준으로 지금 상태를 감사(audit)해서 작성했다.
> 전체 그림은 [백로그.md](./백로그.md), 요일별 실행 순서는 [개발_일정.md](./개발_일정.md) 참고.
>
> **이 문서는 초안이야 — 특히 "열려있는 질문" 섹션은 같이 정하고 나서 체크리스트에 반영하자.**

기준: ✅ 완료 · 🚧 UI는 있지만 mock 데이터/미동작 · ⬜ 미착수

---

## 1. 홈 (`pages/Home/HomePage.tsx`)

- [x] ✅ 기본 레이아웃 (eyebrow + D-day/과목명 + 부연설명)
- [x] ✅ "최근 계산 기록" 카드
- [x] ✅ "이렇게 계산해요" 안내 카드 3개
- [x] ✅ "새 스케줄 만들기" 버튼 → `/input`
- [ ] ⬜ `localStorage`에서 실제 최근 계산 기록 읽어오기
- [ ] ⬜ 저장된 기록이 없는 최초 접속일 때 문구/레이아웃 — **[디자인.md](./디자인.md) 9장에도 "별도 결정 필요"라고 적혀있는 미해결 항목, 같이 정하자**

## 2. 정보 입력 (`pages/Input/InputPage.tsx`)

- [x] ✅ 시험 일정 추가·수정·삭제
- [x] ✅ 평소 수면 패턴 입력
- [x] ✅ 카페인 섭취 추가·삭제
- [x] ✅ 카페인 민감도 / 나이·성별·건강상태
- [ ] ⬜ 몸무게 입력 필드 — 카페인 분포용적(Vd) 계산을 개인화하기 위해 필요, 2주차 계산 모델 확정 중 결정(2026-07-14). [계산_모델_리서치.md](./계산_모델_리서치.md) 2.1 참고
- [x] ✅ 최소 수면시간 슬라이더 + 이동시간 안내 배너
- [ ] 🚧 "계산하기" 버튼 — 지금은 그냥 `/processing`으로 이동만 함, 실제 API 호출 필요
- [ ] 🚧 음료 선택 시트의 "직접 입력" — 지금은 그냥 시트를 닫기만 함. [기획서.md](./기획서.md) 3장엔 "mg 직접 입력" 옵션이 명시되어 있는데 폼이 없음 → **직접 입력 폼(음료명 + mg + 시각) 만들지, 아니면 프리셋만으로 충분한지 정하자**
- [ ] ⬜ 자연어 입력 → 폼 자동 채우기 ("아래 항목에 채우기" 버튼 미동작, 기획서상 스트레치 기능)

## 3. 처리 중 (`pages/Processing/ProcessingPage.tsx`)

- [x] ✅ 5단계 체크리스트 연출 UI
- [ ] 🚧 지금은 `setTimeout` 타이머로 자동 이동 — 실제 API 응답을 기다렸다가 이동하도록 교체 필요

## 4. 결과 (`pages/Result/ResultPage.tsx`)

- [x] ✅ 레이아웃 / 각성도 그래프(SVG) / 경고 배너 / 날짜별 스케줄 카드
- [ ] 🚧 그래프·요약 문구·경고·스케줄 전부 하드코딩 — API 응답으로 교체 필요
- [ ] ⬜ "저장하고 마치기" 버튼이 실제로 `localStorage`에 저장하도록

## 5. 스케줄 조정 (`pages/Adjust/AdjustPage.tsx`)

- [x] ✅ 취침/기상/카페인 용량/카페인 시각 슬라이더 4종
- [x] ✅ 카페인 한도 근접 시 경고 배너
- [ ] ⬜ 슬라이더 초기값을 추천 스케줄로 채우기 (지금은 고정값)
- [ ] ⬜ 여러 날짜 중 조정 대상 선택 UI — 지금은 "월요일"로 하드코딩. [기획서.md](./기획서.md)엔 "다른 날짜는 목록에서 선택"이라고 되어있음 → **날짜 탭/드롭다운 형태 UI를 어떻게 할지 같이 정하자**
- [ ] ⬜ "재계산하기"가 실제로 조정값 반영해서 API 재호출

## 6. 계산 엔진 (백엔드, 아직 없음)

- [ ] ⬜ Express 서버(`server/`) 스캐폴딩
- [ ] ⬜ Process S(수면압) 구현
- [ ] ⬜ Process C(일주기리듬) 구현
- [ ] ⬜ 카페인 근사 모델 구현 — **정확한 논문 계수를 못 구해서 표준 포화형 용량-반응 곡선으로 근사하기로 함(PPT 작업 때 합의). 이대로 진행해도 되는지 재확인**
- [ ] ⬜ 안전 섭취 한도 로직 (연령/건강상태별 + 오늘 섭취량 차감)
- [ ] ⬜ 목표 각성 시각 역산 (이동시간·여유시간)
- [ ] ⬜ 다중 시험 통합 최적화 (그리드 탐색)
- [ ] ⬜ `POST /api/schedule/calculate` 엔드포인트

## 7. DB (Supabase, 아직 없음)

- [ ] ⬜ Supabase 프로젝트 생성
- [ ] ⬜ `caffeine_reference` 테이블 (음료별 카페인 함량)
- [ ] ⬜ `sensitivity_halflife` 테이블 (민감도별 반감기)
- [ ] ⬜ `safety_limits` 테이블 (연령/건강상태별 안전 한도)
- [ ] ⬜ `.env`에 키 채우기 + Express에서 연결

## 8. 공통 컴포넌트 (`client/src/components/`)

이미 있음: `Card`, `Row`, `Segmented`, `Switch`, `Slider`, `Button`, `Field`, `WarningBanner`, `BottomSheet`

- [ ] ⬜ `Chart` — 지금은 Result 페이지 안에 SVG가 인라인으로 박혀있음. 재사용 컴포넌트로 뺄지 결정
- [ ] ⬜ `ProgressTrack` — 지금 `AppShell` 안에 인라인. 그대로 둬도 되는지, 분리할지
- [ ] ⬜ `CheckItem` — 지금 Processing 페이지 안에 인라인
- [ ] ⬜ `Legend`, `Badge` — [디자인.md](./디자인.md) 5장엔 있는데 아직 안 만듦, 필요성 재검토

---

## 열려있는 질문 (같이 정할 것)

1. **홈 화면 최초 접속(저장 기록 없음) 문구** — 어떤 문구/레이아웃을 보여줄지
2. **카페인 mg 직접 입력** — 프리셋 음료 외에 직접 mg를 입력하는 폼을 만들지 말지
3. **스케줄 조정 화면의 날짜 선택 UI** — 탭? 드롭다운? 아니면 화면 자체를 날짜별로 분리?
4. **카페인 근사 모델 파라미터** — PPT 때 제안한 방향(표준 포화형 곡선)으로 확정할지, 파라미터를 어떻게 튜닝할지
5. **공통 컴포넌트 분리 범위** — Chart/ProgressTrack/CheckItem을 지금 인라인 그대로 둘지, 4주차에 분리할지
