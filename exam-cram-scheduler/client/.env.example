# 배포된 백엔드(Express/Render)의 주소. 개발 환경에선 비워 두면 vite.config.ts의
# 프록시가 /api 요청을 localhost:4000으로 중계하므로 채우지 않아도 된다(#30).
# 배포(Vercel)에서만 Render 서버 주소를 채운다. 예: https://exam-cram-api.onrender.com
VITE_API_BASE_URL=
