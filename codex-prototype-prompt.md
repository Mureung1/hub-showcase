# Codex 요청문 - 동네병원 원격 웨이팅 프로토타입

`AGENTS.md`, `docs/plan.md`, `docs/feature-spec.md`, `docs/checklist.md`, `docs/ux-structure.md`, `docs/design-system.md`를 먼저 읽고, `$build-clinic-ui`를 적용해 핵심 사용자 흐름을 검증하는 정적 프로토타입을 만드세요.

## 목적

다음 흐름을 설명 없이 이해할 수 있는 화면으로 표현합니다.

```text
병원 확인 → 가족 단위 원격 웨이팅 등록 → 실시간 순번 확인
→ 병원의 방문 요청 → 도착 확인 → 정식 접수 → 진료실 호출
```

직원 화면에서는 원격·현장 환자를 하나의 실제 진료 대기열로 관리해야 합니다.

## 제약

- `prototype/`에는 HTML과 CSS만 사용합니다.
- JavaScript, 프레임워크, 외부 CDN, 외부 폰트를 사용하지 않습니다.
- 버튼 이동은 HTML 링크로 표현합니다.
- 데이터 저장, 실시간 갱신, 알림톡, SMS는 실제로 동작하는 것처럼 표현하지 않습니다.
- 모든 문구는 한국어로 작성합니다.
- 모바일 390px과 데스크톱에서 읽을 수 있어야 합니다.
- 상태는 색상뿐 아니라 텍스트와 표시를 함께 사용합니다.
- 기존 PlaceSync 화면과 예시 데이터는 재사용하지 않습니다.

## 화면

1. 병원 상세 및 현재 대기 현황
2. 가족 단위 원격 웨이팅 등록
3. 환자 대기 현황과 방문 요청
4. 병원 직원용 통합 대기열
5. 직원용 현장 환자 등록

## Mock 시나리오

- 대표 병원 한 곳
- 원격 환자와 현장 환자가 섞인 대기열
- 소아 1명과 성인 1명으로 구성된 가족 접수
- 방문 요청 전과 후의 환자 상태
- 접수 완료 후 진료실 호출로 다음 순번이 진행되는 상태
- 미도착, 한 번 미루기 결과, 취소 상태

## 완료 기준

- 모든 화면 링크가 끊기지 않습니다.
- 가족 수가 아닌 실제 환자 수로 남은 인원을 표현합니다.
- 환자와 직원 화면에서 동일한 접수번호와 상태를 사용합니다.
- 예상 시간은 참고 정보라는 안내가 있습니다.
- 실제 연동이 아닌 항목은 Mock임이 명확합니다.
- 모바일 화면에서 가로 스크롤이나 텍스트 겹침이 없습니다.
