# Day 2: 로그인 슬라이스 (D1~D4) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 구글/카카오 소셜 로그인(D1/D2), 로그아웃(D3), 내 정보 조회(D4) API를 구현해 "브라우저에서 로그인 → `users` 테이블에 row 생성/조회 → 세션 쿠키로 로그인 유지" 흐름을 완성한다. FE 연동과 hub 저장소 이관은 이 플랜 범위 밖이며, 이 플랜은 BE(`번개모임-backend`)에서만 진행한다.

**Architecture:** `POST /api/auth/:provider` 라우트가 (1) `src/services/oauthClients.js`로 인가 코드를 실제 토큰/프로필로 교환하고, (2) `src/services/userService.js`로 `users` 테이블에서 찾거나 새로 만든 뒤, (3) `req.session.userId`에 저장한다. 이후 `GET /api/users/me`는 Day1에서 만든 `requireAuth` 미들웨어로 세션을 확인하고 프로필을 반환한다. 외부 OAuth 서버 호출은 `oauthClients.js` 한 파일에 격리해서, 라우트 테스트에서는 이 모듈을 모킹(mock)해 실제 구글/카카오 서버를 호출하지 않고도 로직을 검증한다.

**Tech Stack:** Node.js 24(전역 `fetch` 사용, 별도 HTTP 라이브러리 설치 불필요), Express, PostgreSQL(`pg`), Jest + Supertest.

## Global Constraints

(`.superpowers/sdd/global-constraints.md`에서 이 태스크들과 관련된 것만 발췌)

- 응답 포맷: 성공 `{ "data": ... }` / 실패 `{ "error": { "code": "...", "message": "..." } }` (전 엔드포인트 공통).
- 공통 에러 코드: `UNAUTHENTICATED`, `FORBIDDEN`, `NOT_FOUND`, `VALIDATION_ERROR`, `SUSPENDED`만 재사용한다. 새 비즈니스 코드를 임의로 만들지 않는다. (`INTERNAL_ERROR`/`DB_CONNECTION_ERROR`는 기존에 명문화된 예외이며 이번 플랜과 무관.)
- 인증 방식: 세션 쿠키(`express-session`, Day1에서 이미 구성됨). JWT 아님.
- `users` 테이블 유니크 제약: `UNIQUE(provider, provider_id)` — 이미 Day1에서 생성됨, 이 플랜은 그대로 사용만 한다.
- 자동화 테스트 범위: 핵심 비즈니스 로직에만 최소한으로 작성한다. 실제 구글/카카오 서버를 호출하는 E2E 테스트는 만들지 않는다(외부 서버 호출부는 모킹).
- YAGNI: 이 플랜은 D1~D4만 다룬다. D5(생년월일 수정 API), E1~E2(모임 등록/조회)는 다른 플랜에서 다룬다.
- FE 연동, hub 저장소 이관/푸시는 이 플랜에 포함하지 않는다 — 사용자 확인에 따라 이 플랜 완료 후 별도로 진행한다.

---

### Task 7: 사용자 findOrCreate 서비스 (`userService.js`)

**Files:**
- Create: `src/services/userService.js`
- Test: `tests/userService.test.js`

**Interfaces:**
- Consumes: `src/config/db.js`의 `pool` (Day1에서 이미 존재), `users` 테이블(Day1 마이그레이션으로 이미 존재 — `id, provider, provider_id, email, nickname, birth_date, trust_score`)
- Produces: `findOrCreateUserByProvider({ provider, providerId, email, nickname })` — Promise를 반환하며 `{ user, isNewUser }` 형태. `user`는 `{ id: Number, email, nickname, birthDate, trustScore: Number }`. `normalizeUser(row)` — DB row(snake_case, `id`/`trust_score`가 문자열)를 위 `user` 형태(camelCase, 숫자)로 변환. 이후 Task 9/10(라우트)과 Task 11(내 정보 조회)가 이 두 함수를 그대로 가져다 쓴다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/userService.test.js`:

```js
const pool = require('../src/config/db');
const {
  findOrCreateUserByProvider,
} = require('../src/services/userService');

afterAll(async () => {
  await pool.end();
});

describe('findOrCreateUserByProvider', () => {
  it('처음 보는 provider/providerId면 새 사용자를 만들고 isNewUser: true를 반환한다', async () => {
    const result = await findOrCreateUserByProvider({
      provider: 'google',
      providerId: 'google-user-1',
      email: 'a@test.com',
      nickname: '홍길동',
    });

    expect(result.isNewUser).toBe(true);
    expect(typeof result.user.id).toBe('number');
    expect(result.user.email).toBe('a@test.com');
    expect(result.user.nickname).toBe('홍길동');
    expect(result.user.birthDate).toBeNull();
    expect(result.user.trustScore).toBe(50);
  });

  it('이미 있는 provider/providerId면 기존 사용자를 반환하고 isNewUser: false를 반환한다', async () => {
    const first = await findOrCreateUserByProvider({
      provider: 'google',
      providerId: 'google-user-2',
      email: 'b@test.com',
      nickname: 'B',
    });

    const second = await findOrCreateUserByProvider({
      provider: 'google',
      providerId: 'google-user-2',
      email: 'b@test.com',
      nickname: 'B',
    });

    expect(second.isNewUser).toBe(false);
    expect(second.user.id).toBe(first.user.id);
  });

  it('provider가 다르면 provider_id가 같아도 별개 사용자로 취급한다', async () => {
    const google = await findOrCreateUserByProvider({
      provider: 'google',
      providerId: 'same-id',
      email: 'g@test.com',
      nickname: 'G',
    });
    const kakao = await findOrCreateUserByProvider({
      provider: 'kakao',
      providerId: 'same-id',
      email: 'k@test.com',
      nickname: 'K',
    });

    expect(kakao.user.id).not.toBe(google.user.id);
  });
});
```

- [ ] **Step 2: 테스트 실행해서 실패 확인**

Run: `npx jest tests/userService.test.js`
Expected: FAIL with `Cannot find module '../src/services/userService'`

- [ ] **Step 3: `userService.js` 구현**

`src/services/userService.js`:

```js
const pool = require('../config/db');

function normalizeUser(row) {
  return {
    id: Number(row.id),
    email: row.email,
    nickname: row.nickname,
    birthDate: row.birth_date,
    trustScore: Number(row.trust_score),
  };
}

async function findOrCreateUserByProvider({ provider, providerId, email, nickname }) {
  const existing = await pool.query(
    'SELECT id, email, nickname, birth_date, trust_score FROM users WHERE provider = $1 AND provider_id = $2',
    [provider, providerId]
  );

  if (existing.rows.length > 0) {
    return { user: normalizeUser(existing.rows[0]), isNewUser: false };
  }

  const inserted = await pool.query(
    `INSERT INTO users (provider, provider_id, email, nickname)
     VALUES ($1, $2, $3, $4)
     RETURNING id, email, nickname, birth_date, trust_score`,
    [provider, providerId, email, nickname]
  );

  return { user: normalizeUser(inserted.rows[0]), isNewUser: true };
}

module.exports = { findOrCreateUserByProvider, normalizeUser };
```

- [ ] **Step 4: 테스트 실행해서 통과 확인**

Run: `npx jest tests/userService.test.js`
Expected: PASS (3 tests)

- [ ] **Step 5: 전체 테스트 회귀 확인**

Run: `npm test`
Expected: 모든 테스트 PASS.

- [ ] **Step 6: Commit**

```bash
git add src/services/userService.js tests/userService.test.js
git commit -m "Add findOrCreateUserByProvider service (backs D1/D2/D4)"
```

---

### Task 8: OAuth 코드 교환 클라이언트 (`oauthClients.js`)

**Files:**
- Create: `src/services/oauthClients.js`
- Test: `tests/oauthClients.test.js`
- Modify: `.env.example` (리다이렉트 URI 항목 추가)

**Interfaces:**
- Consumes: 전역 `fetch`(Node 24 내장), `process.env.GOOGLE_CLIENT_ID/SECRET`, `process.env.KAKAO_CLIENT_ID/SECRET`, `process.env.OAUTH_REDIRECT_URI`, `src/utils/apiError.js`의 `ApiError`(Day1에서 이미 존재)
- Produces: `exchangeGoogleCode(code)`, `exchangeKakaoCode(code)` — 둘 다 Promise를 반환하며 성공 시 `{ providerId: string, email: string, nickname: string }`, 실패 시 `ApiError('VALIDATION_ERROR', ...)`를 던진다. 이후 Task 9(D1)/Task 10(D2)가 이 두 함수를 호출한다.

- [ ] **Step 1: `.env.example`에 리다이렉트 URI 항목 추가**

`.env.example`의 카카오 항목 아래에 추가:

```
# 구글/카카오 OAuth 앱에 등록한 리다이렉트 URI (Day1 Task 0에서 http://localhost:3000으로 등록함)
OAUTH_REDIRECT_URI=http://localhost:3000
```

본인의 `.env` 파일(커밋 안 됨)에도 같은 줄을 직접 추가해야 Day2 라우트가 동작한다. (`.env`는 gitignore 대상이라 이 플랜에서 자동으로 수정하지 않는다.)

- [ ] **Step 2: 실패하는 테스트 작성**

`tests/oauthClients.test.js`:

```js
const {
  exchangeGoogleCode,
  exchangeKakaoCode,
} = require('../src/services/oauthClients');

describe('exchangeGoogleCode', () => {
  afterEach(() => {
    jest.restoreAllMocks();
  });

  it('토큰 교환 + 프로필 조회 후 정규화된 프로필을 반환한다', async () => {
    jest
      .spyOn(global, 'fetch')
      .mockResolvedValueOnce({
        ok: true,
        json: async () => ({ access_token: 'token-abc' }),
      })
      .mockResolvedValueOnce({
        ok: true,
        json: async () => ({
          sub: 'google-user-1',
          email: 'a@test.com',
          name: '홍길동',
        }),
      });

    const profile = await exchangeGoogleCode('auth-code-1');

    expect(profile).toEqual({
      providerId: 'google-user-1',
      email: 'a@test.com',
      nickname: '홍길동',
    });
    expect(global.fetch).toHaveBeenCalledTimes(2);
  });

  it('토큰 교환이 실패하면 VALIDATION_ERROR를 던진다', async () => {
    jest.spyOn(global, 'fetch').mockResolvedValueOnce({ ok: false });

    await expect(exchangeGoogleCode('bad-code')).rejects.toMatchObject({
      code: 'VALIDATION_ERROR',
    });
  });
});

describe('exchangeKakaoCode', () => {
  afterEach(() => {
    jest.restoreAllMocks();
  });

  it('토큰 교환 + 프로필 조회 후 정규화된 프로필을 반환한다', async () => {
    jest
      .spyOn(global, 'fetch')
      .mockResolvedValueOnce({
        ok: true,
        json: async () => ({ access_token: 'token-xyz' }),
      })
      .mockResolvedValueOnce({
        ok: true,
        json: async () => ({
          id: 987654321,
          kakao_account: {
            email: 'b@test.com',
            profile: { nickname: '카카오유저' },
          },
        }),
      });

    const profile = await exchangeKakaoCode('auth-code-2');

    expect(profile).toEqual({
      providerId: '987654321',
      email: 'b@test.com',
      nickname: '카카오유저',
    });
  });

  it('토큰 교환이 실패하면 VALIDATION_ERROR를 던진다', async () => {
    jest.spyOn(global, 'fetch').mockResolvedValueOnce({ ok: false });

    await expect(exchangeKakaoCode('bad-code')).rejects.toMatchObject({
      code: 'VALIDATION_ERROR',
    });
  });
});
```

- [ ] **Step 3: 테스트 실행해서 실패 확인**

Run: `npx jest tests/oauthClients.test.js`
Expected: FAIL with `Cannot find module '../src/services/oauthClients'`

- [ ] **Step 4: `oauthClients.js` 구현**

`src/services/oauthClients.js`:

```js
const ApiError = require('../utils/apiError');

async function exchangeGoogleCode(code) {
  const tokenRes = await fetch('https://oauth2.googleapis.com/token', {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body: new URLSearchParams({
      code,
      client_id: process.env.GOOGLE_CLIENT_ID,
      client_secret: process.env.GOOGLE_CLIENT_SECRET,
      redirect_uri: process.env.OAUTH_REDIRECT_URI,
      grant_type: 'authorization_code',
    }),
  });

  if (!tokenRes.ok) {
    throw new ApiError('VALIDATION_ERROR', '구글 인증 코드 교환에 실패했습니다');
  }

  const { access_token: accessToken } = await tokenRes.json();

  const profileRes = await fetch(
    'https://www.googleapis.com/oauth2/v3/userinfo',
    { headers: { Authorization: `Bearer ${accessToken}` } }
  );

  if (!profileRes.ok) {
    throw new ApiError('VALIDATION_ERROR', '구글 사용자 정보 조회에 실패했습니다');
  }

  const profile = await profileRes.json();

  return {
    providerId: profile.sub,
    email: profile.email,
    nickname: profile.name || profile.email,
  };
}

async function exchangeKakaoCode(code) {
  const params = {
    grant_type: 'authorization_code',
    client_id: process.env.KAKAO_CLIENT_ID,
    redirect_uri: process.env.OAUTH_REDIRECT_URI,
    code,
  };
  if (process.env.KAKAO_CLIENT_SECRET) {
    params.client_secret = process.env.KAKAO_CLIENT_SECRET;
  }

  const tokenRes = await fetch('https://kauth.kakao.com/oauth/token', {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body: new URLSearchParams(params),
  });

  if (!tokenRes.ok) {
    throw new ApiError('VALIDATION_ERROR', '카카오 인증 코드 교환에 실패했습니다');
  }

  const { access_token: accessToken } = await tokenRes.json();

  const profileRes = await fetch('https://kapi.kakao.com/v2/user/me', {
    headers: { Authorization: `Bearer ${accessToken}` },
  });

  if (!profileRes.ok) {
    throw new ApiError('VALIDATION_ERROR', '카카오 사용자 정보 조회에 실패했습니다');
  }

  const profile = await profileRes.json();
  const kakaoAccount = profile.kakao_account || {};

  return {
    providerId: String(profile.id),
    email: kakaoAccount.email,
    nickname: (kakaoAccount.profile && kakaoAccount.profile.nickname) || kakaoAccount.email,
  };
}

module.exports = { exchangeGoogleCode, exchangeKakaoCode };
```

- [ ] **Step 5: 테스트 실행해서 통과 확인**

Run: `npx jest tests/oauthClients.test.js`
Expected: PASS (4 tests)

- [ ] **Step 6: 전체 테스트 회귀 확인**

Run: `npm test`
Expected: 모든 테스트 PASS.

- [ ] **Step 7: Commit**

```bash
git add src/services/oauthClients.js tests/oauthClients.test.js .env.example
git commit -m "Add Google/Kakao OAuth code exchange client"
```

---

### Task 9: `POST /api/auth/google` (D1)

**Files:**
- Create: `src/routes/auth.js`
- Modify: `src/app.js` (`express.json()` 미들웨어 + `/api/auth` 라우터 연결)
- Test: `tests/auth.routes.test.js`

**Interfaces:**
- Consumes: Task 7의 `findOrCreateUserByProvider`, Task 8의 `exchangeGoogleCode`, Day1의 `ApiError`
- Produces: `router`(Express Router, `module.exports`) — `POST /google` 핸들러. 응답 바디는 API 명세서 형식 그대로: `{ data: { user: { id, nickname, email, birthDateRequired }, isNewUser } }`. 로그인 성공 시 `req.session.userId`를 설정한다. 이후 Task 10(D2)이 같은 파일에 `POST /kakao`를 추가하고, Task 12(D3)가 `POST /logout`을 추가한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/auth.routes.test.js`:

```js
jest.mock('../src/services/oauthClients');

const request = require('supertest');
const app = require('../src/app');
const pool = require('../src/config/db');
const { exchangeGoogleCode } = require('../src/services/oauthClients');

afterAll(async () => {
  await pool.end();
});

describe('POST /api/auth/google', () => {
  it('code가 없으면 400 VALIDATION_ERROR', async () => {
    const res = await request(app).post('/api/auth/google').send({});
    expect(res.status).toBe(400);
    expect(res.body.error.code).toBe('VALIDATION_ERROR');
  });

  it('신규 사용자면 회원가입 후 세션을 발급하고 isNewUser: true를 반환한다', async () => {
    exchangeGoogleCode.mockResolvedValueOnce({
      providerId: 'google-1',
      email: 'a@test.com',
      nickname: '홍길동',
    });

    const agent = request.agent(app);
    const res = await agent.post('/api/auth/google').send({ code: 'abc' });

    expect(res.status).toBe(200);
    expect(res.body.data.isNewUser).toBe(true);
    expect(res.body.data.user.nickname).toBe('홍길동');
    expect(res.body.data.user.birthDateRequired).toBe(true);
  });

  it('이미 로그인 상태에서 다시 요청하면 같은 사용자로 로그인되고 isNewUser: false', async () => {
    exchangeGoogleCode.mockResolvedValue({
      providerId: 'google-2',
      email: 'b@test.com',
      nickname: 'B',
    });

    const agent = request.agent(app);
    const first = await agent.post('/api/auth/google').send({ code: 'first' });
    const second = await agent.post('/api/auth/google').send({ code: 'second' });

    expect(second.body.data.isNewUser).toBe(false);
    expect(second.body.data.user.id).toBe(first.body.data.user.id);
  });
});
```

- [ ] **Step 2: 테스트 실행해서 실패 확인**

Run: `npx jest tests/auth.routes.test.js`
Expected: FAIL — `/api/auth/google`가 아직 없어 404가 반환되어 `expect(res.status).toBe(400)` 등이 실패함.

- [ ] **Step 3: `src/routes/auth.js` 구현**

```js
const express = require('express');
const ApiError = require('../utils/apiError');
const { exchangeGoogleCode } = require('../services/oauthClients');
const { findOrCreateUserByProvider } = require('../services/userService');

const router = express.Router();

function toAuthResponse(user, isNewUser) {
  return {
    user: {
      id: user.id,
      nickname: user.nickname,
      email: user.email,
      birthDateRequired: user.birthDate === null,
    },
    isNewUser,
  };
}

router.post('/google', async (req, res, next) => {
  try {
    const { code } = req.body;
    if (!code) {
      throw new ApiError('VALIDATION_ERROR', 'code가 필요합니다');
    }

    const profile = await exchangeGoogleCode(code);
    const { user, isNewUser } = await findOrCreateUserByProvider({
      provider: 'google',
      providerId: profile.providerId,
      email: profile.email,
      nickname: profile.nickname,
    });

    req.session.userId = user.id;
    res.json({ data: toAuthResponse(user, isNewUser) });
  } catch (err) {
    next(err);
  }
});

module.exports = router;
```

- [ ] **Step 4: `src/app.js`에 JSON 파서 + 라우터 연결**

`src/app.js`를 아래처럼 수정한다 (기존 `require`문 아래, `app.use(sessionMiddleware)` 다음 줄에 `express.json()`과 라우터 추가):

```js
require('dotenv').config();

const express = require('express');
const path = require('path');
const pool = require('./config/db');
const errorHandler = require('./middleware/errorHandler');
const sessionMiddleware = require('./config/session');
const authRoutes = require('./routes/auth');

const app = express();
app.use(sessionMiddleware);
app.use(express.json());
app.use(express.static(path.join(__dirname, '..', 'public')));

app.use('/api/auth', authRoutes);

app.get('/health', async (req, res) => {
  try {
    await pool.query('SELECT 1');
    res.json({ data: { status: 'ok', db: 'connected' } });
  } catch (err) {
    res
      .status(500)
      .json({ error: { code: 'DB_CONNECTION_ERROR', message: err.message } });
  }
});

app.use(errorHandler);

if (require.main === module) {
  const PORT = process.env.PORT || 3000;
  app.listen(PORT, () => {
    console.log(`Server listening on port ${PORT}`);
  });
}

module.exports = app;
```

- [ ] **Step 5: 테스트 실행해서 통과 확인**

Run: `npx jest tests/auth.routes.test.js`
Expected: PASS (3 tests)

- [ ] **Step 6: 전체 테스트 회귀 확인**

Run: `npm test`
Expected: 모든 테스트 PASS. (Day1의 `tests/auth.test.js`, `tests/errorHandler.test.js`도 함께 통과해야 함 — `express.json()` 추가가 기존 라우트에 영향 없는지 확인하는 것이 이 스텝의 목적.)

- [ ] **Step 7: Commit**

```bash
git add src/routes/auth.js src/app.js tests/auth.routes.test.js
git commit -m "Add POST /api/auth/google route (Task D1)"
```

---

### Task 10: `POST /api/auth/kakao` (D2)

**Files:**
- Modify: `src/routes/auth.js` (Task 9에서 만든 파일에 `POST /kakao` 추가)
- Modify: `tests/auth.routes.test.js` (Task 9에서 만든 파일에 카카오 테스트 추가)

**Interfaces:**
- Consumes: Task 8의 `exchangeKakaoCode`, Task 9의 `toAuthResponse`(같은 파일 내 헬퍼), Task 7의 `findOrCreateUserByProvider`
- Produces: `POST /kakao` 핸들러 — `POST /google`과 동일한 응답 형식, `provider: 'kakao'`로 저장.

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/auth.routes.test.js` 상단 `jest.mock(...)` 아래 import에 `exchangeKakaoCode`를 추가하고, 파일 끝에 아래 `describe` 블록을 추가한다:

```js
const { exchangeGoogleCode, exchangeKakaoCode } = require('../src/services/oauthClients');
```

(기존의 `const { exchangeGoogleCode } = require(...)` 줄을 위 줄로 교체)

```js
describe('POST /api/auth/kakao', () => {
  it('code가 없으면 400 VALIDATION_ERROR', async () => {
    const res = await request(app).post('/api/auth/kakao').send({});
    expect(res.status).toBe(400);
    expect(res.body.error.code).toBe('VALIDATION_ERROR');
  });

  it('신규 사용자면 회원가입 후 세션을 발급하고 isNewUser: true를 반환한다', async () => {
    exchangeKakaoCode.mockResolvedValueOnce({
      providerId: 'kakao-1',
      email: 'k@test.com',
      nickname: '카카오유저',
    });

    const agent = request.agent(app);
    const res = await agent.post('/api/auth/kakao').send({ code: 'abc' });

    expect(res.status).toBe(200);
    expect(res.body.data.isNewUser).toBe(true);
    expect(res.body.data.user.nickname).toBe('카카오유저');
  });

  it('구글과 카카오가 같은 email이어도 별개 계정으로 취급한다', async () => {
    exchangeGoogleCode.mockResolvedValueOnce({
      providerId: 'dup-google',
      email: 'same@test.com',
      nickname: 'GoogleUser',
    });
    exchangeKakaoCode.mockResolvedValueOnce({
      providerId: 'dup-kakao',
      email: 'same@test.com',
      nickname: 'KakaoUser',
    });

    const googleRes = await request
      .agent(app)
      .post('/api/auth/google')
      .send({ code: 'g' });
    const kakaoRes = await request
      .agent(app)
      .post('/api/auth/kakao')
      .send({ code: 'k' });

    expect(kakaoRes.body.data.user.id).not.toBe(googleRes.body.data.user.id);
  });
});
```

- [ ] **Step 2: 테스트 실행해서 실패 확인**

Run: `npx jest tests/auth.routes.test.js`
Expected: FAIL — `/api/auth/kakao`가 아직 없어 404.

- [ ] **Step 3: `src/routes/auth.js`에 `/kakao` 라우트 추가**

`module.exports = router;` 줄 바로 위에 추가 (그리고 파일 상단 import에 `exchangeKakaoCode` 추가):

```js
const { exchangeGoogleCode, exchangeKakaoCode } = require('../services/oauthClients');
```

```js
router.post('/kakao', async (req, res, next) => {
  try {
    const { code } = req.body;
    if (!code) {
      throw new ApiError('VALIDATION_ERROR', 'code가 필요합니다');
    }

    const profile = await exchangeKakaoCode(code);
    const { user, isNewUser } = await findOrCreateUserByProvider({
      provider: 'kakao',
      providerId: profile.providerId,
      email: profile.email,
      nickname: profile.nickname,
    });

    req.session.userId = user.id;
    res.json({ data: toAuthResponse(user, isNewUser) });
  } catch (err) {
    next(err);
  }
});
```

- [ ] **Step 4: 테스트 실행해서 통과 확인**

Run: `npx jest tests/auth.routes.test.js`
Expected: PASS (6 tests)

- [ ] **Step 5: 전체 테스트 회귀 확인**

Run: `npm test`
Expected: 모든 테스트 PASS.

- [ ] **Step 6: Commit**

```bash
git add src/routes/auth.js tests/auth.routes.test.js
git commit -m "Add POST /api/auth/kakao route (Task D2)"
```

---

### Task 11: `GET /api/users/me` (D4)

**Files:**
- Create: `src/routes/users.js`
- Modify: `src/app.js` (`/api/users` 라우터 연결)
- Test: `tests/users.routes.test.js`

**Interfaces:**
- Consumes: Day1의 `requireAuth` 미들웨어, Task 7의 `normalizeUser`, `src/config/db.js`의 `pool`
- Produces: `router`(Express Router) — `GET /me` 핸들러. 로그인 상태면 `{ data: { id, email, nickname, birthDate, trustScore } }`, 비로그인이면 `requireAuth`가 401 `UNAUTHENTICATED`를 반환. 이후 Task 12(D3 로그아웃)가 이 엔드포인트로 "로그아웃 후 세션이 실제로 파기됐는지"를 검증한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/users.routes.test.js`:

```js
jest.mock('../src/services/oauthClients');

const request = require('supertest');
const app = require('../src/app');
const pool = require('../src/config/db');
const { exchangeGoogleCode } = require('../src/services/oauthClients');

afterAll(async () => {
  await pool.end();
});

describe('GET /api/users/me', () => {
  it('비로그인 상태면 401 UNAUTHENTICATED', async () => {
    const res = await request(app).get('/api/users/me');
    expect(res.status).toBe(401);
    expect(res.body.error.code).toBe('UNAUTHENTICATED');
  });

  it('로그인 상태면 내 정보를 반환한다', async () => {
    exchangeGoogleCode.mockResolvedValueOnce({
      providerId: 'me-user-1',
      email: 'me@test.com',
      nickname: '내정보',
    });

    const agent = request.agent(app);
    await agent.post('/api/auth/google').send({ code: 'abc' });

    const res = await agent.get('/api/users/me');
    expect(res.status).toBe(200);
    expect(res.body.data.email).toBe('me@test.com');
    expect(res.body.data.nickname).toBe('내정보');
    expect(res.body.data.birthDate).toBeNull();
    expect(res.body.data.trustScore).toBe(50);
  });
});
```

- [ ] **Step 2: 테스트 실행해서 실패 확인**

Run: `npx jest tests/users.routes.test.js`
Expected: FAIL — `/api/users/me`가 아직 없어 404.

- [ ] **Step 3: `src/routes/users.js` 구현**

```js
const express = require('express');
const pool = require('../config/db');
const requireAuth = require('../middleware/auth');
const ApiError = require('../utils/apiError');
const { normalizeUser } = require('../services/userService');

const router = express.Router();

router.get('/me', requireAuth, async (req, res, next) => {
  try {
    const { rows } = await pool.query(
      'SELECT id, email, nickname, birth_date, trust_score FROM users WHERE id = $1',
      [req.session.userId]
    );

    if (rows.length === 0) {
      throw new ApiError('NOT_FOUND', '사용자를 찾을 수 없습니다');
    }

    res.json({ data: normalizeUser(rows[0]) });
  } catch (err) {
    next(err);
  }
});

module.exports = router;
```

- [ ] **Step 4: `src/app.js`에 라우터 연결**

`src/app.js`에서 `const authRoutes = require('./routes/auth');` 아래에 추가:

```js
const usersRoutes = require('./routes/users');
```

그리고 `app.use('/api/auth', authRoutes);` 아래에 추가:

```js
app.use('/api/users', usersRoutes);
```

- [ ] **Step 5: 테스트 실행해서 통과 확인**

Run: `npx jest tests/users.routes.test.js`
Expected: PASS (2 tests)

- [ ] **Step 6: 전체 테스트 회귀 확인**

Run: `npm test`
Expected: 모든 테스트 PASS.

- [ ] **Step 7: Commit**

```bash
git add src/routes/users.js src/app.js tests/users.routes.test.js
git commit -m "Add GET /api/users/me route (Task D4)"
```

---

### Task 12: `POST /api/auth/logout` (D3)

**Files:**
- Modify: `src/routes/auth.js` (`POST /logout` 추가)
- Modify: `tests/auth.routes.test.js` (로그아웃 테스트 추가)

**Interfaces:**
- Consumes: Task 11의 `GET /api/users/me` (로그아웃 후 세션이 파기됐는지 검증하는 용도)
- Produces: `POST /logout` — 세션을 파기하고 `{ data: { status: 'logged_out' } }` 반환. 로그인 여부와 무관하게 항상 200을 반환한다(이미 로그아웃 상태에서 호출해도 에러 아님).

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/auth.routes.test.js` 끝에 추가:

```js
describe('POST /api/auth/logout', () => {
  it('로그인 상태에서 로그아웃하면 세션이 파기된다', async () => {
    exchangeGoogleCode.mockResolvedValueOnce({
      providerId: 'logout-user',
      email: 'l@test.com',
      nickname: 'L',
    });

    const agent = request.agent(app);
    await agent.post('/api/auth/google').send({ code: 'abc' });

    const logoutRes = await agent.post('/api/auth/logout');
    expect(logoutRes.status).toBe(200);
    expect(logoutRes.body.data.status).toBe('logged_out');

    const meRes = await agent.get('/api/users/me');
    expect(meRes.status).toBe(401);
  });

  it('로그인하지 않은 상태에서도 200을 반환한다', async () => {
    const res = await request(app).post('/api/auth/logout');
    expect(res.status).toBe(200);
  });
});
```

- [ ] **Step 2: 테스트 실행해서 실패 확인**

Run: `npx jest tests/auth.routes.test.js`
Expected: FAIL — `/api/auth/logout`이 아직 없어 404.

- [ ] **Step 3: `src/routes/auth.js`에 `/logout` 라우트 추가**

`module.exports = router;` 줄 바로 위에 추가:

```js
router.post('/logout', (req, res, next) => {
  req.session.destroy((err) => {
    if (err) {
      next(err);
      return;
    }
    res.clearCookie('connect.sid');
    res.json({ data: { status: 'logged_out' } });
  });
});
```

- [ ] **Step 4: 테스트 실행해서 통과 확인**

Run: `npx jest tests/auth.routes.test.js`
Expected: PASS (8 tests — Task 9의 3개 + Task 10의 3개 + 이번 2개)

- [ ] **Step 5: 전체 테스트 회귀 확인 (선택, 실제 OAuth 앱으로 검증하고 싶다면 아래 수동 확인도 진행)**

Run: `npm test`
Expected: 모든 테스트 PASS.

수동 확인(선택): `npm start` 후 브라우저에서 구글 OAuth 동의 화면 URL(`https://accounts.google.com/o/oauth2/v2/auth?client_id=<GOOGLE_CLIENT_ID>&redirect_uri=http://localhost:3000&response_type=code&scope=openid%20email%20profile`)로 이동해 로그인하고, 리다이렉트된 URL의 `code` 쿼리 파라미터 값을 복사한다.
Run: `curl -X POST http://localhost:3000/api/auth/google -H "Content-Type: application/json" -d "{\"code\":\"<복사한 code>\"}" -c cookies.txt`
Expected: `{ "data": { "user": {...}, "isNewUser": true } }` 형태 응답.
Run: `curl http://localhost:3000/api/users/me -b cookies.txt`
Expected: 방금 로그인한 사용자 정보가 반환됨 (세션 쿠키 유지 확인).

- [ ] **Step 6: Commit**

```bash
git add src/routes/auth.js tests/auth.routes.test.js
git commit -m "Add POST /api/auth/logout route (Task D3)"
```

---

## 이 플랜 완료 후

- `.superpowers/sdd/progress.md`에 Task 7~12(D1~D4) 완료 상태를 기록한다.
- 사용자가 정한 대로: BE(Task 7~12) 완료 → `hub\bungae-moim-backend`로 파일 이관 및 GitHub 푸시 → 이어서 `hub\bungae-moim`의 `LoginPage.jsx`/`AppStateContext.jsx`에 실제 `fetch` 연동(FE)을 진행한다. 이관/FE 연동은 이 플랜의 범위가 아니므로 별도 플랜으로 다룬다.
