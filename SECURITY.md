# 보안 정책 (Security Policy)

## 1. 개요

이 저장소(`NAVER_AIchallenge_personal_project`)는 NAVER AI Challenge 부트캠프 과정에서 진행 중인 **개인 프로젝트**입니다. 공식 대회 규정이 있는 제품이 아니라, 여러 초기 단계 트랙(홈택스 자동화 스파이크, 장부 자동화 MVP, 연차보고서 자동화 등)이 함께 들어 있는 리서치/스파이크 허브입니다. 상용 서비스 운영을 전제로 한 보안 대응 체계(전담 보안팀, 정식 SLA, 버그 바운티 등)는 갖추고 있지 않습니다.

## 2. 지원 범위 (Supported Versions)

이 저장소는 정식 릴리스/버전 태깅 체계를 사용하지 않습니다. 별도의 "지원 버전" 표는 적용되지 않으며, 대신 다음 기준으로 이해해 주시기 바랍니다.

- `main` 브랜치가 사실상 최신·기준 상태입니다.
- `feature/*` 등 작업 브랜치는 활발히 변경되는 진행 중인 코드이며, 보안 검토가 끝난 상태를 보장하지 않습니다.
- 과거 커밋/브랜치에 대한 별도의 패치 백포트는 제공되지 않습니다. 문제가 발견되면 현재 작업 중인 브랜치 기준으로 수정합니다.

## 3. 취약점 신고 방법

민감할 수 있는 보안 문제(인증 우회, 자격 증명 노출, 실제 홈택스 등 외부 서비스에 대한 오작동 위험 등)를 발견하신 경우:

- **공개 GitHub 이슈로 등록하지 말아 주세요.** 이슈는 누구나 볼 수 있어 악용 위험이 있습니다.
- 대신 이메일로 직접 연락해 주시기 바랍니다: **w72575535@gmail.com**
- 신고 시 다음 정보를 포함해 주시면 도움이 됩니다: 문제가 있는 파일/경로, 재현 방법, 예상되는 영향 범위.

이 저장소는 1인이 운영하는 개인 프로젝트이므로, 특정 응답 시간(SLA)이나 정식 공개(disclosure) 일정은 약속드리기 어렵습니다. 다만 확인 즉시 검토하고, 필요한 경우 커밋 이력에서 민감정보를 제거하거나 관련 코드를 수정하는 등 가능한 범위에서 신속히 대응하겠습니다.

## 4. 민감정보 취급 원칙

이 저장소의 각 트랙에서 실제로 자격 증명/키를 다루는 부분과 그 취급 원칙은 다음과 같습니다 (`CLAUDE.md`에 명시된 규칙을 따릅니다).

- **`projects/hometax/fincert-spike/` (홈택스 Playwright 자동화 스파이크)**
  - 금융인증서 PIN/비밀번호 등 자격 증명은 오직 이 디렉터리 자체의 `.env` 파일(자체 `.gitignore`로 보호됨)에만 두거나, 사용자가 직접 입력하도록 합니다. 코드에 하드코딩하거나 로그/콘솔에 출력하지 않습니다.
  - 실제 홈택스 사이트에 대한 쓰기 동작(예: 세금계산서 실제 발급/제출)은 반드시 사람이 문자열을 직접 입력해 확인하는 명시적 확인 절차(gate)를 거쳐야 하며, 이 확인 절차를 임의로 제거하거나 자동 응답 처리해서는 안 됩니다. 단순 조회(읽기 전용) 동작은 이 확인 절차가 필요 없지만, "발급"/"신청"/"제출" 같은 버튼은 확인 절차 없이 클릭하지 않습니다.
- **`projects/hometax/guide-extension/` (Chrome 확장 + Node 프록시 서버)**
  - 프록시 서버 실행에 `ANTHROPIC_API_KEY` 환경변수가 필요합니다. 이 값은 `.env`에만 두고 커밋하지 않습니다.
- **루트 `.env`**
  - `FINANCE_AUTH_PASSWORD` 등을 담고 있으며, 루트 `.gitignore`의 규칙에 의해 커밋되지 않도록 무시됩니다. `git add -f`로 강제 추가하지 않습니다.
- **`NAVER_AIchallenge_hub.wiki/`**
  - 이 저장소와는 별도의 git 저장소(GitHub Wiki 클론)입니다. 이 저장소의 `.gitignore`에서도 제외되어 있으며, 커밋/푸시는 해당 디렉터리 안에서 독립적으로 이루어집니다.
- **공통 원칙**
  - 모든 `.env` 파일(하위 경로 포함)은 루트 `.gitignore`에 의해 커밋 대상에서 제외되며, `.env.example`만 추적 대상입니다. 실제 키/비밀번호 값은 예시 파일에 넣지 않습니다.
  - 자격 증명, API 키, 비밀번호는 코드나 커밋 메시지, 로그에 절대 남기지 않습니다.

## 5. 알려진 주의사항 / 범위 제한

- 이 저장소에는 CI 기반 시크릿 스캐닝, 정적 보안 분석(SAST) 등 자동화된 보안 점검 파이프라인이 별도로 구성되어 있지 않습니다. 현재는 수동 검토(커밋 전 확인, `.gitignore` 규칙)에 의존하고 있습니다.
- `projects/hometax/` 하위 코드는 실제 국세청 홈택스 사이트와 상호작용하는 스파이크 코드입니다. 자동화/스크래핑에 대한 홈택스 측의 정책 변화, 세션 충돌(동일 `storageState.json`에 대한 동시 접속 등) 위험이 `CLAUDE.md`에 문서화되어 있으니 참고해 주세요.
- 각 하위 프로젝트(`projects/*`)는 독립적인 빌드/의존성 구성을 가지고 있어, 의존성 취약점 점검 범위도 프로젝트별로 다를 수 있습니다. 루트 차원에서 통합된 의존성 스캔은 수행되지 않습니다.
