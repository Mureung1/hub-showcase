# [N168_채민지] - 소상공인 맞춤형 숏폼 자동 제작 대시보드 기획 및 아키텍처 설계

## 주요 작업 리스트

- 소상공인의 리소스 부족 문제를 해결하기 위한 **숏폼 자동 제작 대시보드 웹 서비스**의 전체 사용자 시나리오(User Journey) 작성
- 프론트엔드(대시보드)와 백엔드(AI 파이프라인) 간의 연동 아키텍처를 Mermaid 다이어그램으로 시각화 및 워크플로우 설계
- YOLOv8(비전 객체 스마트 크롭), KoBERT(문맥 기반 트렌드 매칭), 외부 TTS API, FFmpeg(오디오/비디오 렌더링)를 결합한 **AI 자동화 파이프라인 프로세스 정의**
- 틱톡/인스타그램 마케팅 지표 분석(목적지 클릭/CTR) 및 숏폼 성과를 극대화하기 위한 후킹성 자막/CTA 전략 마련
- **스크린샷:**
  > (이곳에 이전에 생성해드린 '대시보드 UI 목업 예상 화면'이나 아키텍처 다이어그램 스크린샷을 첨부해 주세요.)

## 내가 설명할 수 있는 부분

- **KoBERT 모델을 활용한 문맥 벡터화 및 매칭 알고리즘 로직**
  - 단순한 텍스트 1:1 매칭("디저트" == "디저트") 방식의 한계를 극복하기 위해 기획된 부분입니다. 한국어 자연어 처리에 강한 KoBERT 모델을 도입해 사장님의 고유 정보(시그니처 메뉴 등)와 크롤링된 실시간 트렌드 키워드를 고차원 벡터로 변환합니다. 이후 코사인 유사도(Cosine Similarity)를 계산해 가장 핏이 잘 맞는 후킹 자막과 음성을 매칭하는 전체적인 구조와 도입 이유를 명확하게 설명할 수 있습니다.

## 아직 이해 못 한 부분

- **YOLOv8 모델 최적화 및 파이프라인 렌더링 지연(Latency) 문제**
  - 음식이나 공간의 경계가 모호한 사진이 업로드되었을 때, YOLOv8이 완벽하게 핵심 객체만 크롭하도록 모델을 미세조정(Fine-Tuning)하는 구체적인 백엔드 레벨의 기술적 방법론은 아직 완벽히 파악하지 못했습니다.
  - 또한, 외부 TTS API 호출과 FFmpeg 영상 렌더링이 순차적으로 맞물려 돌아갈 때 발생하는 서버 대기 시간을 프론트엔드 단에서 사용자 이탈 없이 자연스럽게(Progress Bar, Skeleton UI 등) 처리하는 실무적인 예외 처리 방식에 대해 더 깊은 이해와 논의가 필요합니다.

## 새로 알게 된 것

- 틱톡과 같은 숏폼 플랫폼의 인사이트 지표 중, 비즈니스 관점에서는 단순 조회수보다 프로필 링크 클릭이나 웹사이트 이동 등을 의미하는 **'목적지 클릭(Destination CTR)'**이 가장 중요하다는 점을 배웠습니다.
- 이를 높이기 위해 영상 초반 2~3초 내에 TTS를 활용한 청각적 훅을 넣고, 아웃트로에 CTA(행동 유도) 자막을 자동 삽입하는 식의 **마케팅 관점이 결합된 기술 설계의 중요성**을 새롭게 알게 되었습니다.
