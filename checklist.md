# 작업 체크리스트

이번 주(4주차, 데모 준비 주간) 목표 — 자세한 내용은 [plan.md](plan.md)「이번 주 목표」참고:
1. **데모에 필요한 핵심 사용자 흐름 완성**
2. **React·Express 외부 배포** (Vercel / Render)
3. **배포 환경에서 화면·서버·DB 연결 확인**
4. **개발·배포에 사용한 Agent 협업 과정 설명**
5. **재사용 가능한 워크플로우 문서화**
6. **영상 제출** — 수요일(7/29) 밤 10시까지 5분 미만 영상 + `showcase.json`의 `demoVideoUrl` 추가해서 PR 포함

## 오늘 (7/27, 월요일)

### 1. 데모에 필요한 핵심 기능 정하기
- [x] 지금까지 만든 기능을 실행해봄 — FE(5173)·BE(3001) 로컬 구동, 냉장고→홈→상세→구매링크 전체 플로우를 브라우저로 직접 확인
- [x] 데모에서 보여줄 핵심 사용자 흐름 하나를 정함 — **냉장고 재료 선택 → 홈 추천(지금 바로/조금만 사면/전체 3섹션) → 레시피 상세(있는·없는 재료 구분 + 총 재료비 + 유튜브 임베드 + 네이버/쿠팡 구매 링크)**. 이미 화면·서버·DB까지 한 사이클로 이어져 있어 그대로 데모 흐름으로 확정
- [x] 오늘 끝낼 기능과 이후로 미룰 기능을 구분함 — 핵심 흐름 자체는 이미 동작하므로 오늘은 배포 준비(3번)에 집중, 이번 달 식비 예상치·KAMIS 시세 비교는 계속 다음 단계로 미룸
- [x] 화면 다듬기보다 핵심 흐름의 동작을 먼저 확인함

완료 기준:
- [x] 사용자가 어떤 순서로 기능을 사용하는지 설명할 수 있음 — 위 핵심 흐름 참고
- [x] 핵심 흐름이 화면, 서버, DB까지 이어짐 — `/api/recipes?matchNames=...`가 Supabase에서 실제 레시피를 가져오고, `/api/naver/search`도 실시간 응답 확인
- [x] 아직 동작하지 않는 부분을 목록으로 남김 — 네이버 최저가 매칭이 가끔 관련 없는 상품을 1위로 잡음(예: "소금" 검색 시 최저가가 조리용 소금이 아니라 "히말라야 핑크솔트 결혼식 답례품") → 오늘은 보류, 아래 백로그로 이월

### 2. 핵심 기능 완료하기
- [x] 핵심 흐름에 필요한 작업부터 진행함 — 유튜브 영상 비율 자동 감지, 누락된 마스코트 이미지 커밋, 홈 로딩 화면(전체화면 + 영상 끝까지 재생 후 전환), `Home.jsx` 로딩·카드 그리드 중복을 `LoadingIndicator`/`RecipeGrid` 컴포넌트로 분리
- [x] 작업 하나마다 완료 기준을 확인함 — 각각 브라우저로 직접 테스트, lint·vitest(34개) 통과 확인
- [x] 문제가 생기면 화면·서버·DB 중 어디에서 실패했는지 나눠서 확인함 — 네이버 검색 관련성 이슈(서버 쪽)를 구분해서 백로그로 기록
- [x] 완료한 작업은 커밋함 — 총 8개 커밋으로 나눠서 `origin/N179_표정한`에 푸시 완료
- [ ] PR에 확인 결과를 남김 — 아직 안 함, 지금 이 브랜치에 열려있는 PR 없음(직전 PR들은 다 머지·클로즈됨)

### 3. 첫 배포 준비하기
- [x] FE와 BE가 있는 폴더 위치를 확인함 — FE는 저장소 루트(`package.json`, `src/`), BE는 `server/`(Express 본체) + `api/index.js`(Vercel 진입점, 이번 주는 안 씀) + `scripts/dev-server.js`(local/Render용 `app.listen()` 실행기)
- [x] 로컬에서 FE 빌드와 BE 실행이 되는지 확인함 — `npm run build` 성공(`dist/` 생성), BE는 핵심 흐름 테스트 때 이미 확인. `scripts/dev-server.js`가 이미 `process.env.PORT || 3001`을 써서 "배포 환경이 제공하는 포트 사용" 요건은 충족돼 있음
- [x] 배포 환경에 옮겨야 할 환경변수 이름을 정리함
  - BE(Render): `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` (`.env.local`에 이미 있는 4개)
  - FE(Vercel): `VITE_API_BASE_URL` — 신규. FE가 `/api/...` 상대경로로 fetch하고 있어서 BE가 다른 도메인(Render)에 있으면 요청이 실패하는 문제를 발견, `src/utils/apiBaseUrl.js`를 만들고 fetch 4곳(`purchaseLinks.js`, `RecipeDetailPage.jsx` 2곳, `Home.jsx`)을 이 값 기준으로 바꿈. 값이 없으면(로컬) 기존처럼 상대경로 그대로 동작 — 로컬 `.env.local`은 안 건드려도 됨. 내일 배포 시 Render URL이 나오면 Vercel 환경변수에 등록 필요
- [x] 비밀 키가 GitHub에 올라가지 않았는지 확인함 — `.env.local`이 `*.local` gitignore 규칙에 걸려있고 git 히스토리 전체에서 커밋된 적 없음 확인, `src/` 안에도 하드코딩 없음 확인
- 배포 자체(Vercel/Render에 실제 올리기)는 내일부터 본격적으로 시도 예정 — 오늘은 준비만

배포 사전 준비:
- [x] Vercel에 GitHub 계정으로 가입
- [x] Render에 GitHub 계정으로 가입
- [ ] 두 서비스에서 본인 포크 저장소가 보이는지 확인 — 내일 실제 배포할 때 같이 확인하기로 하고 오늘은 보류
- 완료 기준: Vercel과 Render에서 본인 포크 저장소를 선택할 수 있으면 준비 완료

## 백로그 (이번 주 범위 아님)
- [ ] 네이버 최저가 매칭이 검색어와 관련 없는 상품을 1위로 잡는 문제 — `naverClient.js`(2026-07-27 확인: "소금"으로 검색하면 최저가가 실제 조리용 소금이 아니라 "히말라야 핑크솔트 결혼식 답례품"). 지금 있는 `isBundleCandidate` 필터는 묶음상품만 걸러내고 상품명 관련성은 안 봄 — 데모에서 이 재료가 노출되지 않게 시연 재료를 고르는 걸로 우선 우회, 관련성 필터링 로직 추가는 이번 주 범위 밖
- [ ] 네이버 카탈로그(가격비교) 상품의 표시 가격이 실제 판매 페이지 가격과 다른 문제 — `naverClient.js`(2026-07-27 확인: "농심 짜파게티 더 블랙 116g 4개"가 검색 API `lprice` 기준 3,980원으로 잡히는데, 같은 옵션인데도 실제 네이버 카탈로그 페이지(`isCatalogMatch: true`)에서는 6,690원으로 표시됨). 검색 API 가격이 실시간이 아니라 크롤링 주기로 갱신되는 스냅숏이라 시차가 있거나, 카탈로그 페이지가 최저가 판매처를 기본으로 안 보여주는 것으로 추정 — 원인 특정과 대응은 이번 주 범위 밖
- [ ] 기니 마스코트를 `Home.jsx`의 "재료 조금만 사면 돼요" 섹션에도 추가 — 현재 "지금 바로 만들 수 있어요" 섹션에만 `mascotWave` 이미지가 있음 (3주차에서 이월, 이번 주는 화면 다듬기보다 핵심 흐름 우선이라 보류)
- [ ] `끼니캐릭터.png`(`src/assets/`, git 미추적)는 임시 목업 일러스트 — 홈 화면 빈 추천 카드(`Home.jsx`)에 자리만 잡아둔 것이고 나중에 최종본으로 교체 필요. 최종본은 캐릭터에 요리사 모자 씌운 버전으로 제작
- [ ] 앱 전체 카피/마이크로카피를 끼니 1인칭 말투로 통일하는 아이디어 — 홈 빈 상태 카드와 FridgePage confirm 문구에는 부분 적용했지만, 에러 메시지·빈 목록 문구 등 다른 화면도 전부 통일하면 좋을 것 같음. 범위가 커서 별도 작업으로 검토
- [ ] if문 안에 if문(중첩 조건문) 없애기 — 조건 분기를 평평하게(early return 등으로) 정리해서 가독성 높이기
- [ ] "재료 조금만 사면 돼요" 카드 — `MenuCard.jsx`의 부족 개수 뱃지에서 `missingCount === 1`일 때 포인트 컬러로 강조, 문구도 더 눈에 띄게 (예: "1개만 더 있으면 완성!")
  - 계기: 레시피 추천이 0건일 때 "빈 화면"으로 보이는 문제에 대해 제미나이한테 물어봤고, 그중 "부족 1개 카드 가시성 강화" 아이디어가 적은 공수로 바로 적용할 만해서 기록해둠
