# test-writing — 테스트 작성 Skill

새로운 함수를 만들 때, 테스트를 먼저 작성하는 절차를 정리한 규칙입니다.
"feature-slice로 계획을 나눈 작업 하나"를 구현하기 전에 이 절차를 따릅니다.

## 순서 (Red → Green → Refactor)

1. **함수의 입력과 출력을 한 문장으로 정의한다**
   예: "리뷰 목록을 받아 긍정 비율(%)을 반환한다"

2. **정상 케이스 테스트를 먼저 쓴다**
   가장 흔하고 뻔한 경우부터. (예: 절반이 긍정이면 50%)

3. **경계 케이스(edge case)를 반드시 포함한다**
   - 입력이 비어있을 때 (빈 리스트, 빈 문자열, None)
   - 입력이 하나뿐일 때
   - 0으로 나누는 상황이 생기는지
   - 모두 같은 값일 때 (전부 긍정, 전부 부정)

4. **아직 함수가 없는 상태에서 테스트를 실행해 실패(Red)를 직접 확인한다**
   `ModuleNotFoundError` 또는 `NameError`가 뜨는 것이 정상.
   여기서 통과해버리면 테스트 자체가 잘못된 것이니 다시 확인한다.

5. **테스트를 통과시키는 최소한의 코드를 작성한다 (Green)**
   처음부터 완벽하게 짜려 하지 않는다. 테스트를 통과시키는 데만 집중한다.

6. **모든 테스트가 통과하면, 코드를 다듬는다 (Refactor)**
   중복 로직이 다른 곳(예: main.py)에도 있다면, 그 함수를 재사용하도록 정리한다.

## 파일 명명 규칙

- 테스트 대상: `기능이름.py` (예: sentiment.py, distance.py)
- 테스트 파일: `test_기능이름.py` (예: test_sentiment.py)
- 같은 폴더에 나란히 둔다.

## 테스트 이름 규칙

`test_상황_기대결과` 형식으로 짓는다.
예: `test_empty_list_is_zero`, `test_all_positive_is_100_percent`

## 실행 방법

```
python -m pytest test_기능이름.py -v
```

## 체크리스트 (테스트 작성 전 확인)

- [ ] 정상 케이스 테스트가 있는가
- [ ] 빈 입력(0개, 빈 문자열 등)에 대한 테스트가 있는가
- [ ] 극단값(전부 같은 값, 최댓값/최솟값)에 대한 테스트가 있는가
- [ ] 함수 구현 전에 실행해서 실패(Red)를 직접 확인했는가
