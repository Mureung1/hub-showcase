# 사이트 추천 기능

## 목적

UniRadar는 사용자 프로필, 저장된 출처, 원하는 정보 종류를 비교해 부족한 정보 영역을 보완할 등록 사이트를 추천한다. 이 기능은 인터넷 전체 검색기가 아니라 프로젝트에 사전 등록하고 확인한 사이트를 안전하게 조합하는 추천 기능이다.

## 데이터 흐름

```text
React SiteRecommendations
→ POST /api/recommend-sites
→ siteRecommendationService
→ siteRegistry + 규칙 기반 recommendSites
→ 선택적으로 Gemini가 설명 문장만 보조
→ 추천 카드 표시
→ 추천 카드에서 선택한 사이트를 실제 스캔 출처에 저장
```

## 사이트 레지스트리

`server/data/siteRegistry.js`의 각 사이트는 다음 정보를 가진다.

- `id`, `name`, `url`, `description`
- 제공 정보 종류(`informationTypes`)
- 대상 사용자, 지역, 제공 기관 유형
- 장점과 확인할 제한 사항
- 신뢰·활성 상태, 관련·보완 출처 ID

활성화된 레지스트리 항목만 `/api/sites`와 추천 후보에 노출된다. 추천 응답의 URL, 정보 종류, 신뢰도, 장점, 제한 사항은 항상 레지스트리 값으로 결정한다.

## 점수 기준

`server/services/recommendSites.js`는 난수를 사용하지 않는다. 점수는 0부터 100 사이에서 다음 요소를 합산한다.

- 사용자가 직접 선택한 정보 종류와의 일치
- 저장된 출처에서 빠진 정보 종류의 보완
- 관심 분야와 전공의 관련성
- 활동 가능 지역과 전국·온라인 범위의 적합성
- 공식·공공 제공 기관과 신뢰 여부
- 현재 출처 범위와 과도하게 겹치는 정보의 감점

점수와 함께 추천 이유, 프로필 관련성, 중복 정보, 보완 정보, 장점, 확인할 점을 반환한다.

## Gemini 역할과 fallback

Gemini가 `AI_PROVIDER=gemini`, `ALLOW_LIVE_GEMINI=true`, API 키 존재의 조건에서 활성화되어 있으면 등록된 상위 후보의 설명 문장만 다듬는다. Gemini는 사이트 ID, URL, 점수, 정보 종류, 신뢰 여부를 만들거나 변경할 수 없다.

Gemini 요청·JSON 검증이 실패하면 서버는 `mode: "rules"`, `fallbackUsed: true`를 응답하고 규칙 기반 결과를 그대로 표시한다.

## 저장된 출처 기준

추천에 반영하는 사이트는 별도 목록이 아니라 공지 스캔에 쓰는 저장된 출처를 기준으로 계산한다. 인증 정보, API 키, 비밀번호는 출처 저장소에 기록하지 않는다.

## 레지스트리 추가 방법

새 사이트는 `server/data/siteRegistry.js`에 URL을 먼저 확인한 뒤 추가한다. `active`, `trusted`, `informationTypes`, 장점·제한 사항을 근거에 맞게 작성한다. 외부 모델이 생성한 이름이나 URL을 레지스트리에 자동 추가하지 않는다.

## 현재 한계와 다음 작업

- 등록 사이트 수는 검증 가능한 소수의 공식·공공 채널로 제한된다.
- 추천 사이트를 추가해도 자동 크롤링이나 자동 분석은 시작하지 않는다.
- 로그인 전에는 저장된 출처가 현재 브라우저에만 남는다.
- 다음 단계에서는 사이트별 공식 API 또는 허용된 목록 수집 어댑터를 추가하고, 사용자별 서버 저장과 인증을 검토한다.
