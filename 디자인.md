# 이력 기반 AI 채용 매칭 서비스 — 디자인 명세서

> 문서 버전: v0.1
> 연계 문서: [기획서.md](./기획서.md) (F1~F7 기능·화면 정의)
> 디자인 방향: 밝은 라벤더-그레이 배경 + 페리윙클 블루 포인트의 **깔끔한 SaaS 대시보드** 스타일
> 대상 플랫폼: 웹 (반응형)

---

## 1. 디자인 컨셉

### 1.1 한 줄 방향
**"신뢰감 있고 정돈된 채용 대시보드"** — 정보 밀도가 높은 화면(이력·포지션·적합도)을 흰 카드와 넉넉한 여백으로 나눠, 복잡한 데이터를 부담 없이 훑을 수 있게 한다.

### 1.2 무드 키워드
- **Clean / Trustworthy** — 채용·이력이라는 민감한 데이터를 다루므로 과하지 않고 신뢰감 있는 톤
- **Card-based** — 포지션·이력·문서를 카드 단위로 구획
- **Data-friendly** — 적합도 점수·요구조건 충족도를 배지·미터·색상으로 즉시 인지
- **Soft & Rounded** — 둥근 모서리, 부드러운 그림자로 위압감 제거

### 1.3 레퍼런스에서 가져온 요소
| 레퍼런스 요소 | 본 서비스 적용 |
|---------------|----------------|
| 좌측 사이드바 + 하단 파란 패널 | 전역 네비게이션 + "이력 완성도" 안내 패널 |
| 흰 카드 + 소프트 섀도우 | 포지션 카드, 이력 항목, 생성 문서 |
| 파스텔 상태 배지 | 적합도 등급 / 요구조건 충족·미충족 |
| 프로필 화면 좌우 분할 | 이력 관리, 포지션 상세 |
| 온보딩 좌측 파란 패널 | 회원가입 / 이력 초기 입력 |
| 모달(Add Event/Project) | 이력 항목 추가, 문서 편집 |

---

## 2. 컬러 시스템

### 2.1 브랜드 / 프라이머리
| 토큰 | HEX | 용도 |
|------|-----|------|
| `--primary` | `#4C6FFF` | 주요 버튼, 활성 네비, 강조 |
| `--primary-hover` | `#3A5CF0` | hover 상태 |
| `--primary-soft` | `#EAEEFF` | 선택된 항목 배경, 배지 배경 |
| `--primary-panel` | `#4C6FFF` → `#6B8AFF` (그라데이션 가능) | 온보딩/사이드바 파란 패널 |

### 2.2 뉴트럴 (배경·텍스트·보더)
| 토큰 | HEX | 용도 |
|------|-----|------|
| `--bg` | `#F4F5FA` | 페이지 배경 (라벤더-그레이) |
| `--surface` | `#FFFFFF` | 카드·패널 표면 |
| `--border` | `#EDEFF5` | 카드 테두리, 구분선 |
| `--text-strong` | `#1A1D2B` | 제목, 핵심 값 |
| `--text` | `#4A4F63` | 본문 |
| `--text-muted` | `#8A8FA3` | 보조 라벨, 캡션 |

### 2.3 상태 컬러 (적합도·요구조건에 직결)
| 의미 | 텍스트/아이콘 | 배경(soft) | 사용처 |
|------|---------------|-----------|--------|
| 성공 · 충족 · 보유 | `#22A06B` | `#E4F7EF` | 요구조건 충족, 자격증 보유 |
| 주의 · 부분충족 | `#E8A33D` | `#FCF3E3` | 부분 경험(0.5), 우대사항 |
| 위험 · 미충족 · 필수 | `#E5484D` | `#FDECEC` | 필수 요구조건 미충족 |
| 정보 · 방향제시 | `#7A5AF8` | `#F0EBFF` | "이력으로 맞추는 방향" 안내 |

> **적합도 미터 색상 규칙** (기획서 4장 매칭 로직 시각화)
> - 85%+ : `--success` · 60~84% : `--primary` · 40~59% : `--warning` · 40% 미만 : `--danger`

---

## 3. 타이포그래피

- **글꼴**: `Pretendard` (국문 최적) / 대체 `Inter`, `Poppins` — sans-serif, 깔끔한 지오메트릭 계열
- **기준 크기**: 본문 14px, 라인하이트 1.5

| 스타일 | 크기 / 두께 | 용도 |
|--------|-------------|------|
| Display | 28px / 700 | 온보딩 헤드라인 ("이력을 입력하세요") |
| H1 | 22px / 700 | 화면 제목 (포지션 목록, 이력 관리) |
| H2 | 18px / 600 | 카드 제목, 섹션 헤더 |
| Body | 14px / 400 | 본문 |
| Label | 13px / 500 | 폼 라벨, 배지 |
| Caption | 12px / 400 | 보조 설명, 수집일 등 |
| Metric | 24px / 700 | 적합도 점수(예: 92%) |

---

## 4. 레이아웃 & 스페이싱

### 4.1 그리드
- **좌측 고정 사이드바** (240px) + **메인 콘텐츠**(가변) 2단 구조
- 메인은 최대 폭 1200px, 좌우 24~32px 여백
- 카드 그리드: 데스크톱 2~3열 → 태블릿 2열 → 모바일 1열

### 4.2 스페이싱 스케일 (4px 기준)
`4 · 8 · 12 · 16 · 24 · 32 · 48`

### 4.3 라운딩 & 그림자
| 토큰 | 값 |
|------|-----|
| `--radius-card` | 16px |
| `--radius-btn` | 10px |
| `--radius-badge` | 999px (pill) |
| `--shadow-card` | `0 4px 16px rgba(26,29,43,0.06)` |
| `--shadow-modal` | `0 12px 40px rgba(26,29,43,0.12)` |

---

## 5. 핵심 컴포넌트

### 5.1 사이드바 (전역 네비)
- 흰 배경, 아이콘+라벨 메뉴, **활성 항목은 `--primary-soft` 배경 + `--primary` 텍스트**
- 하단에 **파란 패널** = "이력 완성도 78%" 진행바 + [이력 완성하기] CTA (레퍼런스의 Get started 패널 자리)
- 메뉴: 포지션 · 내 이력 · 생성 문서 · 설정

### 5.2 버튼
| 종류 | 스타일 |
|------|--------|
| Primary | `--primary` 배경, 흰 텍스트, radius 10px |
| Secondary | 흰 배경 + `--border` 테두리, `--text` |
| Ghost | 배경 없음, `--primary` 텍스트 (링크형) |
| Danger | `--danger` 배경 (삭제 등) |

### 5.3 카드
- 흰 표면 + `--shadow-card` + radius 16px + 내부 패딩 20~24px
- **포지션 카드**: 회사 로고(원형) · 직무명 · 태그 · **적합도 미터 & 점수 배지**를 우측 상단에 고정

### 5.4 배지 / 칩
- pill 형태, 상태 컬러의 soft 배경 + 진한 텍스트
- 예: `필수` (danger soft), `우대` (warning soft), `보유` (success soft)

### 5.5 적합도 미터 (⭐ 시그니처 컴포넌트)
- 가로 프로그레스 바 + 우측 퍼센트 텍스트
- 색상은 2.3 규칙을 따름
- 포지션 목록에서는 컴팩트 미터, 상세 화면에서는 **요구조건별로 분해된 스택 미터**

### 5.6 입력 폼
- 라벨(위) + 인풋(radius 10px, `--border` 테두리, focus 시 `--primary` 링)
- 이력 입력은 **유형 선택 칩**(자격증/포트폴리오/경력/회사) + 동적 필드

### 5.7 모달
- 중앙 정렬, `--shadow-modal`, radius 16px, 상단 제목 + 우측 닫기(×)
- 용도: 이력 항목 추가/수정, 생성 문서 편집

### 5.8 아바타 / 로고
- 원형, 32~48px. 회사 로고 없을 시 이니셜 + 브랜드 틴트 배경

---

## 6. 화면별 디자인 (기획서 IA 매핑)

### 6.1 로그인 / 회원가입 · 이력 초기 입력 (F1·F2)
- 레퍼런스 온보딩처럼 **좌: 파란 패널(브랜드 메시지·일러스트)** / **우: 폼**
- 회원가입 직후 이력 입력으로 자연스럽게 유도 (스텝 인디케이터)

### 6.2 이력 관리 (F2)
- 레퍼런스 My Profile 구조: **좌측 요약(완성도·기본정보)** + **우측 상세(자격증·경력·포트폴리오·회사 리스트)**
- 각 항목은 카드/행 단위, [추가] → 모달

### 6.3 포지션 목록 = 홈 (F4) ⭐ 핵심
- **적합도 순 정렬 카드 리스트**
- 각 카드: 회사·직무·요약 태그 + **적합도 미터/점수 배지**
- 상단 정렬·필터(직군) 바

### 6.4 포지션 상세 + 방향 제시 (F5)
- 상단: 공고 요약 + 큰 적합도 점수(Metric 24px)
- 중단: **요구조건 분해 리스트** — 각 조건에 `가중치 × 충족도` 시각화 + 상태 배지
- 하단: **"내 이력으로 맞추는 방향" 3가지** 카드 (정보 컬러 `--primary`/purple 톤)

### 6.5 AI 문서 생성 (F6·F7)
- 탭: `이력서` / `자기소개서`
- 생성 결과를 편집 가능한 카드로, 하단에 [지원하기] Primary 버튼 → 외부 사이트 이동
- 생성 중 로딩(스켈레톤/스피너), 편집 가능 표시

---

## 7. 인터랙션 & 상태

- **Hover**: 카드는 그림자 강화 + 살짝 올라오는 transform(2px)
- **Focus**: 인풋 `--primary` 2px 링
- **Loading**: 스켈레톤(카드 형태 유지), AI 생성은 진행 문구
- **Empty**: 일러스트 + 안내 문구 + CTA (예: "아직 입력한 이력이 없어요 → 이력 추가")
- **Disabled**: 40% 투명도, 커서 not-allowed

---

## 8. 접근성 · 반응형 원칙

- 상태를 **색상만으로 구분하지 않기** — 배지 텍스트/아이콘 병행 (색약 대응)
- 본문 대비비 4.5:1 이상, 텍스트 위 파란 패널은 흰색 텍스트로 대비 확보
- 터치 타깃 최소 44px, 모바일에서 사이드바는 하단 탭/햄버거로 전환

---

## 9. 디자인 토큰 요약 (개발 인계용)

```css
:root {
  /* Primary */
  --primary: #4C6FFF;
  --primary-hover: #3A5CF0;
  --primary-soft: #EAEEFF;

  /* Neutral */
  --bg: #F4F5FA;
  --surface: #FFFFFF;
  --border: #EDEFF5;
  --text-strong: #1A1D2B;
  --text: #4A4F63;
  --text-muted: #8A8FA3;

  /* Status */
  --success: #22A06B;  --success-soft: #E4F7EF;
  --warning: #E8A33D;  --warning-soft: #FCF3E3;
  --danger:  #E5484D;  --danger-soft:  #FDECEC;
  --info:    #7A5AF8;  --info-soft:    #F0EBFF;

  /* Radius */
  --radius-card: 16px;
  --radius-btn: 10px;
  --radius-badge: 999px;

  /* Shadow */
  --shadow-card: 0 4px 16px rgba(26,29,43,0.06);
  --shadow-modal: 0 12px 40px rgba(26,29,43,0.12);
}
```

---

## 10. 변경 이력
| 버전 | 날짜 | 내용 |
|------|------|------|
| v0.1 | — | 레퍼런스 기반 최초 작성 (컬러·타이포·컴포넌트·화면 매핑) |
