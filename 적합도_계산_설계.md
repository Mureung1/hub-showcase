# 적합도 계산 설계 (Matching Score Design)

> 문서 버전: v0.1
> 대응 기능: F4 (적합도 매칭·랭킹), F5 (방향 제시)
> 상위 문서: `기획서.md` 4장 매칭 로직

---

## 0. 설계 원칙

**정밀도의 병목은 가중치가 아니라 충족도(0~1) 추정이다.**
"LLM이 알아서 0.5를 준다"는 방식은 재현성이 없고 근거를 제시할 수 없어 개선 자체가 불가능하다.

따라서 다음 원칙을 따른다.

1. **LLM은 판정자가 아니라 어댑터다.** 비정형 텍스트 → 정형 데이터 변환만 담당.
2. **점수 계산은 결정론적 코드가 한다.** 같은 입력이면 항상 같은 출력.
3. **계산 가능한 것은 계산한다.** LLM 판정은 계산 불가능한 서술형에만 사용.
4. **모든 점수는 근거를 동반한다.** 근거 없는 점수는 상한을 둔다.

### 역할 분담 요약

| 항목 | 처리 주체 | 방식 |
|---|---|---|
| 요구조건 추출 | LLM | 공고 원문 → 구조체 파싱 (점수 없음) |
| 연차 충족도 | 코드 | 산술 계산 |
| 자격증 보유 | 코드 | DB 조회 + 상위자격 대체 규칙 |
| 스킬 매칭 | 코드 | 정규화 사전 → 실패 시 임베딩 |
| 최신성 감쇠 | 코드 | 지수 감쇠 함수 |
| 가중치 추정 | 코드 | 규칙 기반 (필수/우대·위치·빈도) |
| **서술형 판정** | **LLM** | **이산 등급 + 근거 인용 강제** |
| 최종 합산·게이트 | 코드 | 산술 |

> LLM 판정이 필요한 요구조건은 전체의 약 15~25% 수준으로 유지하는 것이 목표.

---

## 1. 요구조건 정형화

LLM 추출 단계에서 자유 텍스트가 아니라 **타입이 있는 구조체**를 뽑는다.

```json
{
  "raw": "Python 개발 경력 3년 이상",
  "type": "EXPERIENCE_YEARS",
  "subject": "python",
  "threshold": 3,
  "necessity": "REQUIRED",
  "source_position": "requirements_top"
}
```

### 요구조건 타입

| type | 의미 | 충족도 산출 |
|---|---|---|
| `EXPERIENCE_YEARS` | 연차 요구 | 코드 (산술) |
| `SKILL_USE` | 기술 사용 경험 | 코드 (사전 + 임베딩) |
| `CERTIFICATION` | 자격증 보유 | 코드 (조회) |
| `EDUCATION` | 학력 | 코드 (조회) |
| `DOMAIN` | 도메인 경험 (핀테크, 커머스 등) | 코드 + 임베딩 |
| `SOFT` | 서술형 (팀 리딩, 주도성 등) | **LLM 판정** |

### subject 정규화

`subject`는 반드시 **정규화 사전**을 거친다.
"파이썬 / Python / Py3"가 같은 ID로 모이지 않으면 이후 계산이 전부 흔들린다.

```
원문 표현 → 사전 조회 → 정규화 ID
  실패 시 → 임베딩 최근접 매칭 (threshold 0.85 이상)
  그것도 실패 → 미분류 큐에 적재 (수동 확인 대상)
```

미분류 큐는 사전을 성장시키는 입력이 된다. 초기에는 직군 1~2개로 좁혀 사전 커버리지를 빠르게 확보한다.

---

## 2. 타입별 충족도 함수

하나의 LLM 프롬프트로 전부 처리하지 않고, 타입마다 다른 함수를 적용한다.

### 2.1 연차형 (EXPERIENCE_YEARS)

선형이 아니라 **포화 곡선**이 맞다. 3년 요구에 6년이라고 2배 좋은 것이 아니다.

```
f = clamp(years / threshold, 0, 1)
```

- 초과분은 1.0으로 캡
- 미달이어도 0이 아니라 부분점수 유지 (2년 / 3년 요구 → 0.67)

### 2.2 보유형 (CERTIFICATION, EDUCATION)

기본은 0/1 이지만 **상위 자격 대체 규칙**을 둔다.

```
정보처리기사 요구 + 정보관리기술사 보유 → 1.0
학사 요구 + 석사 보유 → 1.0
```

대체 관계는 별도 테이블로 관리한다.

### 2.3 스킬 사용형 (SKILL_USE)

pgvector가 실제로 쓰이는 자리. 단, **유사도 하나로 쓰지 않고 세 축을 곱한다.**

```
f = sim(요구조건, 근거_이력) × depth × recency
```

| 축 | 값 | 설명 |
|---|---|---|
| `sim` | 0~1 | 임베딩 코사인 유사도 (사전 매칭 성공 시 1.0) |
| `depth` | 0.3 / 0.7 / 1.0 | 언급만 / 프로젝트 사용 / 주도적 구현·운영 |
| `recency` | `exp(-λ × 경과년수)` | λ ≈ 0.15 → 5년 전 경험은 약 0.47 |

`depth`는 이력 입력 폼(F2)에서 사용자가 직접 선택하게 하는 것이 가장 정확하다.

### 2.4 서술형 (SOFT)

LLM 판정을 쓰되, 자유롭게 두지 않는다.

**제약 조건**

- 출력은 0~1 연속값이 아니라 **이산 등급**
  ```
  없음(0.0) / 간접(0.3) / 부분(0.6) / 충족(1.0)
  ```
- 등급마다 **근거 인용(evidence span)** 을 사용자 이력 원문에서 발췌하도록 강제
- **근거를 제시하지 못하면 0.3을 초과할 수 없다**
- `temperature = 0`
- 조건 하나씩 **독립 호출** (한 번에 여러 개를 물으면 서로 영향을 준다)

이 설계는 할루시네이션 억제와 F5(적합도 근거 표시)를 동시에 해결한다.

---

## 3. 필수 조건 게이트

가중합만 쓰면 **"필수 조건 하나가 완전 미충족인데 나머지가 좋아서 88%"** 가 나온다.
실제 지원 결과와 어긋나는 최대 원인이다.

```
적합도 = ( Σ wᵢ · fᵢ ) × Π (필수 조건 게이트)

게이트 = 0.4 + 0.6 · f
```

- 필수 조건이 `f = 0`이면 전체 점수에 0.4배가 곱해진다
- 우대 조건은 게이트에 넣지 않고 가중합에만 반영한다

### 계산 예시 (기획서 4.2 확장)

| 요구조건 | 필수 | 가중치 | 충족도 | 기여 |
|---|:---:|:---:|:---:|:---:|
| Python 3년+ | ● | 0.4 | 1.0 | 0.40 |
| AWS 경험 | ○ | 0.3 | 0.5 | 0.15 |
| 정보처리기사 | ○ | 0.2 | 1.0 | 0.20 |
| 팀 리딩 경험 | ● | 0.1 | 0.0 | 0.00 |
| **가중합** | | 1.0 | | **0.75** |

게이트 적용:
```
Python 게이트   = 0.4 + 0.6 × 1.0 = 1.0
팀 리딩 게이트  = 0.4 + 0.6 × 0.0 = 0.4

최종 = 0.75 × 1.0 × 0.4 = 0.30
```

→ 가중합만 보면 75%지만, 필수 조건 미충족으로 실제 적합도는 30%. 이쪽이 현실에 가깝다.

---

## 4. 가중치 추정

초기에는 학습 데이터가 없으므로 **사전값(prior) + 신호 보정**으로 간다.

```
w_raw = base(necessity) × position_bonus × frequency_bonus
```

| 계수 | 값 |
|---|---|
| `base` | REQUIRED 1.0 / PREFERRED 0.4 |
| `position_bonus` | 자격요건 상단 1.2 / 하단 1.0 |
| `frequency_bonus` | 공고 내 2회 이상 언급 1.15 |

마지막에 `Σw = 1`로 정규화한다.

**직군별 prior 테이블**을 하나 두면 초기 품질이 눈에 띄게 올라간다.
(예: 백엔드 직군은 언어·프레임워크 가중치 상향, 자격증 가중치 하향)

향후 클릭·지원 로그가 쌓이면 이 계수를 로지스틱 회귀 학습값으로 대체한다.

---

## 5. 점수 보정과 표시

원점수는 대체로 **0.6~0.85 구간에 몰려서 변별이 되지 않는다.**
화면에 보여줄 %는 원점수가 아니라 **보정값**으로 한다.

- 방법 A: 해당 사용자의 전체 포지션 대비 **퍼센타일**
- 방법 B: 골든셋으로 **isotonic regression** 피팅 후 매핑

### confidence (신뢰도)

근거가 부족한 요구조건 비율이 높으면 `confidence`를 낮게 매긴다.

```
confidence = 1 - (근거_미확보_요구조건_가중치합 / 전체_가중치합)
```

confidence가 낮은 항목은 UI에서 `87%` 같은 단정적 수치 대신
**구간 표시(`80~90%`)나 회색 처리**로 보여주는 편이 신뢰를 지킨다.

---

## 6. 정밀도 측정

**이것이 없으면 개선 여부를 알 수 없다. 현 단계에서 투자 가치가 가장 높은 작업이다.**

### 골든셋

- 규모: 사용자 이력 × 공고 **50~100쌍**
- 라벨: 적합도 **상 / 중 / 하** 3단계로 충분 (연속값 라벨링은 사람도 일관성이 없다)

### 지표

| 대상 | 지표 |
|---|---|
| 최종 랭킹 | **Spearman ρ**, **NDCG@10** (절대오차 아님) |
| 요구조건 추출 | precision / recall **별도 측정** |
| 충족도 계산 | 골든셋 항목별 오차 |

요구조건 추출 단계의 오류는 하류 전체를 오염시키므로 반드시 분리해서 측정한다.
이 분리가 있어야 "추출이 틀린 건지, 계산식이 틀린 건지"를 구분할 수 있다.

---

## 7. 구현 시 주의사항

### 캐싱

| 대상 | 키 | 정책 |
|---|---|---|
| 요구조건 추출 결과 | `posting_id` | posting당 1회, 영구 |
| 충족도 | `(requirement_id, credential_set_version)` | 이력 미변경 시 재계산 금지 |
| 임베딩 | `normalized_text_hash` | 영구 |

`credential_set_version`은 사용자 이력이 수정될 때마다 증가시킨다.

### LLM 호출

- `temperature = 0` 고정
- 서술형 판정은 조건 단위 독립 호출
- 추출·판정 모두 **비동기 잡**으로 처리 (동기 응답 금지)

### 저장 스키마

`MatchScore` 테이블에 최종 점수만 저장하지 말고 **항목별 기여도를 JSONB로 함께 저장**한다.

```json
{
  "total": 0.30,
  "weighted_sum": 0.75,
  "confidence": 0.82,
  "breakdown": [
    {
      "requirement_id": 101,
      "raw": "Python 개발 경력 3년 이상",
      "type": "EXPERIENCE_YEARS",
      "necessity": "REQUIRED",
      "weight": 0.4,
      "fulfillment": 1.0,
      "contribution": 0.40,
      "gate": 1.0,
      "evidence": "OO사 백엔드 개발 2021.03~현재 (4년)"
    }
  ]
}
```

F5의 "방향 제시" 화면은 이 데이터를 그대로 재활용한다.
`fulfillment`가 낮고 `weight`가 높은 항목이 곧 "보완해야 할 지점"이다.

---

## 8. 우선순위

단계적으로 도입할 경우 순서는 다음과 같다.

| 순위 | 항목 | 이유 |
|:---:|---|---|
| 1 | **타입별 충족도 함수 분리 (2장)** | LLM 단일 판정에서 계산 가능한 부분을 빼내는 것만으로 재현성·정확도가 크게 상승. LLM 비용도 서술형에만 집중 가능 |
| 2 | 필수 조건 게이트 (3장) | 구현 비용 대비 체감 정확도 개선폭이 가장 큼 |
| 3 | 골든셋 + 측정 (6장) | 이후 모든 개선의 기준선 |
| 4 | subject 정규화 사전 (1장) | 직군을 좁히면 초기 커버리지 확보가 빠름 |
| 5 | 점수 보정 (5장) | 기능이 아니라 표시 품질 문제이므로 후순위 가능 |

---

## 9. 열린 이슈

- `depth` 값을 사용자 입력으로 받을지, 이력 텍스트에서 LLM이 추정할지
- 최신성 감쇠 계수 λ를 직군별로 다르게 둘지 (기술 변화 속도 차이)
- 게이트 하한 0.4가 적절한지 — 골든셋으로 튜닝 필요
- 자격증 상위 대체 테이블의 초기 커버리지 범위
