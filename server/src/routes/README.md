# server/src/routes

이 디렉토리는 REST API 엔드포인트 URL 경로와 그에 대응하는 비즈니스 컨트롤러를 매핑해 주는 라우터 파일들이 들어가는 곳입니다.

## 역할 및 설계 방향
- `auth.ts`: 회원가입, 메일 인증, 본인인증(SMS), 로그인 등 인증 API 경로를 전담 매핑할 라우터입니다.
- **인증 연동 계획**: 초기 가상 유저 단계에서는 이곳이 활성화되지 않거나 비어 있으며, 나중에 인증 로직 개발 시 컨트롤러의 실질적인 회원 로직과 연결하여 API 엔드포인트를 열어줍니다.
