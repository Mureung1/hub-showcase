# Turso(libSQL) 연결 정보 — 실제 값은 팀 노션/1Password에서 확인, 여기엔 넣지 않는다.
TURSO_DATABASE_URL=libsql://<db-name>-<org>.turso.io
TURSO_AUTH_TOKEN=

# Prisma가 참조하는 접속 문자열. 로컬 개발 중 잠깐 붙여볼 땐 file:./dev.db 로 대체 가능.
DATABASE_URL=

# Express dev 서버 포트 (vite.config.js의 proxy 대상과 일치해야 함)
PORT=3001
