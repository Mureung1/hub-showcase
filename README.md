# 팀플, 이지! (Team Project, Easy!)

> 대학생 팀 프로젝트의 '킥오프 1시간'을 AI가 중립적 중재자로 대신해, 15분 만에 팀 전원이 납득하는 계획과 역할 분담을 만들어주는 웹 서비스

---

## 📌 프로젝트 개요

대학생 팀 프로젝트는 시작 단계에서 실패합니다. 서로 잘 모르는 팀원들이 위계도 경험도 없이 첫 회의에서 계획과 역할을 정해야 하고, 이 과정이 어색함과 눈치보기 속에 흐지부지되면서 계획 없는 프로젝트와 불공정한 분담이 굳어집니다.

**팀플, 이지!**는 AI를 "관리자"가 아니라 **아무도 하기 싫은 말을 대신 해주는 중립적 중재자**로 세워, 첫 회의 자리에서 계획 수립부터 역할 배정까지 끝내는 킥오프 도구입니다.

상세 기획은 [plan.md](./plan.md)를 참고하세요.

### 현재 상태: 데모용 프로토타입 (v1 화면 5개 완성)

- **프론트 온리**: 백엔드 없이 React + localStorage로 동작
- **목(mock) LLM**: 과제 유형별 사전 제작 응답 + 로딩 연출 (`src/services/mockAi.js`의 함수만 교체하면 실제 LLM API 연동 가능)
- **단일 브라우저 시뮬레이션**: 팀원 설문을 한 화면에서 팀원을 전환하며 제출 (실제 초대 링크는 v2)

---

## 🔄 사용자 흐름 (화면 5개)

```
① 프로젝트 생성  →  ② AI 계획 검토  →  ③ 팀원 설문  →  ④ 역할 배정  →  ⑤ 대시보드
   (위저드 3스텝)     (수정·승인)        (비공개 2분)     (AI 설명·스왑)    (칸반·진행률)
```

1. **프로젝트 생성**: 과제 유형(발표/개발/보고서) 카드 선택 → 목표 한 문장·마감일 → 팀원 수(3~8명)·닉네임 입력
2. **AI 계획 검토**: 플래너 에이전트가 마일스톤·태스크를 제안 (목 학사일정 기반 **시험 주간 회피**). 태스크 인라인 수정·삭제·추가 후 "이대로 확정" 또는 "다시 제안받기"
3. **팀원 설문**: 팀원별로 선호 역할 1~3순위(탭 순서로 지정), 기피 역할(최대 1개), 역할 경험, 리더 의향을 제출. 데모용 "랜덤 채우기" 지원, 미제출자는 '상관없음' 처리
4. **역할 배정**: 결정적 로직이 배정하고 AI가 **팀 단위 서술**로 이유를 설명 (개인 응답 비공개 유지). 전원 기피 역할은 타협안(로테이션 등) 제안, 48시간 내 1회 스왑 가능
5. **대시보드**: 전체 진행률 + D-day, 3열 고정 칸반(내 태스크 강조), 마일스톤별 진행률. 태스크 상태는 카드의 ‹ › 버튼으로 이동

---

## 🎯 핵심 설계: 역할 배정 2단 구조

**1단 — 결정적 점수 매트릭스** (`src/logic/assignRoles.js`, LLM 미사용)

```
점수 = 선호 1순위 +3 / 2순위 +2 / 3순위 +1
     + 경험 있음 +1
     + 기피 역할 -10
     + (조장 한정) 리더 의향 예 +2 / 아니오 -10
→ 역할별 최소/최대 인원 규칙을 지키는 그리디 배정
```

같은 입력이면 항상 같은 결과가 나와 재현성과 공정성이 보장됩니다. 인원 ≠ 역할 수인 경우 1인 다역·다인 1역으로 처리합니다.

**2단 — LLM 설명** (`src/services/mockAi.js`, 현재 목 응답)

배정 결과의 이유를 개인 응답 인용 없이 팀 단위 통계로만 서술하고, 전원 기피 역할이 있으면 타협안을 제안합니다.

---

## 🛠️ 기술 스택

| 구분 | 기술 | 버전 |
|------|------|------|
| **프레임워크** | React | ^19.2.7 |
| **빌드 도구** | Vite | ^8.1.1 |
| **상태 관리** | React Context + useReducer, localStorage 영속화 | — |
| **언어** | JavaScript (ES Module) | — |
| **린터** | Oxlint | ^1.71.0 |

백엔드·인증·라우팅 라이브러리는 사용하지 않습니다 (프로토타입 범위).

---

## 📁 프로젝트 구조

```
hub/
├── src/
│   ├── main.jsx               # 앱 진입점
│   ├── App.jsx                # 단계 상태 머신 + 진행 표시 + 리셋
│   ├── store.js               # 상태 정의·리듀서·useProject 훅
│   ├── StoreProvider.jsx      # Context Provider (localStorage 영속화)
│   ├── data/
│   │   └── templates.js       # 과제 유형·역할 정의, 목 계획 템플릿, 목 학사일정
│   ├── services/
│   │   └── mockAi.js          # 목 LLM: generatePlan / explainAssignment (교체 지점)
│   ├── logic/
│   │   └── assignRoles.js     # 결정적 역할 배정 (점수 매트릭스 + 그리디)
│   ├── utils/
│   │   └── dates.js           # 날짜 유틸
│   └── screens/
│       ├── CreateWizard.jsx   # ① 프로젝트 생성 위저드
│       ├── PlanReview.jsx     # ② AI 계획 검토
│       ├── Survey.jsx         # ③ 팀원 설문
│       ├── Assignment.jsx     # ④ 배정 결과
│       └── Dashboard.jsx      # ⑤ 대시보드 (칸반·진행률)
├── plan.md                    # 서비스 기획서 + 구현 체크리스트
├── index.html
├── vite.config.js
└── package.json
```

---

## 🚀 실행 방법

### 사전 요구사항
- **Node.js** (v18 이상 권장)

### 설치 및 실행

```bash
# 1. 의존성 설치
npm install

# 2. 개발 서버 실행
npm run dev
```

실행 후 브라우저에서 **http://localhost:5173** 으로 접속합니다.

### 기타 명령어

```bash
npm run lint      # 코드 린트 검사
npm run build     # 프로덕션 빌드
npm run preview   # 빌드 결과물 미리보기
```

### 데모 시나리오 (5분)

1. "전공 발표 과제, 마감 4주 뒤, 5명"으로 프로젝트 생성 → AI 계획 생성 확인 (마감일을 10월 말 이후로 잡으면 시험 주간 회피 동작을 볼 수 있음)
2. 태스크 하나를 즉석에서 수정 → "이대로 확정"
3. 팀원 탭을 전환하며 설문 제출 (또는 "랜덤 채우기")
4. 배정 결과 + AI 설명 확인 — 전원이 '발표'를 기피하면 타협안 카드 표시
5. 대시보드에서 태스크 상태 이동 → 진행률 갱신 확인

데이터는 localStorage에 저장되며, 헤더의 "처음부터 다시" 버튼으로 초기화할 수 있습니다.

---

## 📝 작업 진행 상황

### ✅ 완료 (v1 프로토타입)
- [x] 1단계 — 기반 구축 + 프로젝트 생성 위저드
- [x] 2단계 — AI 계획 검토 화면 (목 플래너 + 시험 주간 회피)
- [x] 3단계 — 팀원 설문 화면 (단일 브라우저 시뮬레이션)
- [x] 4단계 — 배정 로직 + 결과 화면 (결정성 검증 완료)
- [x] 5단계 — 대시보드 (3열 칸반 + 진행률)

### 🔲 향후 작업 (v2 로드맵)
- [ ] 실제 LLM API 연동 (`mockAi.js` 교체) + 서버사이드 프록시
- [ ] 백엔드(Express + SQLite) 도입 — 실제 초대 링크 + 멀티 디바이스
- [ ] 모니터링 에이전트: 지연 감지·리마인드·주간 팀 리포트
- [ ] 학교 학사일정 API 연동, 과제 유형 템플릿 확장

---

## 👥 팀 정보

AI 팀 프로젝트 관리 서비스 개발팀

---

## 📄 라이선스

이 프로젝트는 비공개 프로젝트입니다.
