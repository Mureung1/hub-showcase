---
title: 커리어 코파일럿 (Career Copilot) — 기획 전체 기록
project: AI Agent Challenge
owner: 승현 (제주대 컴퓨터공학과, 학사, Java/Spring 백엔드 베이스)
status: 기획 완료 (설계 착수 전) — 코드 미착수
created: 2026-07-07
updated: 2026-07-07
---

# 커리어 코파일럿 — 기획 전체 기록

> 이 문서는 2026-07-07 기획 세션 전체를 한 줄도 빠짐없이 담은 스펙이다.
> 담주 설계 착수의 기준점. "뭐였더라"가 없게 결정·근거·정정·검증 엔드포인트까지 모두 기록한다.

## 목차
1. 프로젝트 정체성 & 포지셔닝
2. 아키텍처 개요 (3단 + 런타임)
3. 데이터 수집 설계 (전체)
4. 매칭 엔진 (개념 × 구현 2축)
5. 내 현재 프로필 (실측)
6. 출력 설계 (2원형 + 스킬 바)
7. 가능성 랭킹 (A-1)
8. 갭 → 우선순위 액션 (A-2)
9. eval & 정직성 레이어
10. 남은 것 (미설계)
11. 부록: 검증된 엔드포인트·법적 근거·정정 기록

---

## 1. 프로젝트 정체성 & 포지셔닝

**한 줄**: 내 위키·GitHub·관심 공고를 기반으로, 매일 자동으로 "나와 맞닿은 진로 · 스킬 갭 · 가능성 · 마감일"을 조사해 정적 웹 대시보드로 보여주는 **개인 커리어 에이전트**.

**정체성**
- 개인용(나=승현)으로 시작 → 멘토 붙으면 배포 가능한 서비스로 확장. (지금은 개인용 v1)
- ❌ 시장 전체 조회기 / 정보 덤프  →  ✅ **나를 위한, 검증된, "뭘 할지"로 바꿔주는 코파일럿**
- **판단은 프롬프트, 검증은 에이전트** (도구로 실제 데이터에 접근해야 검증이 됨. "프롬프트에서 검사해봐"는 자기 답 자기 채점 = 검증 아님)

**닫은 3대 킬러** (원래 이 아이디어의 사망 원인이었던 것들)
- 리텐션 → **매일 갱신**(돌아올 이유)
- 그라운딩 → **내 위키·GitHub**(자기보고 아닌 실제 나)
- "왜 챗 말고 에이전트?" → **자율 일일 실행**(챗이 구조적으로 못 하는 것)

**왜 에이전트인가 (프롬프트/범용Claude 대비)**
- 도구(url_fetch 등)는 커모디티(10%). **하네스가 값(90%)**: 언제 fetch·뭘 검증·좋은지 어떻게 아나·너 없이도 돌게.
- 목적형 에이전트 = 내 오케스트레이션을 코드로 박아 (a)결정성 (b)너 없이 자율실행 (c)남한테 줄 수 있음 (d)eval로 신뢰성 보장.
- "범용 Claude를 내가 잘 몰아서" 얻은 이 대화의 품질 = 하네스가 내 머릿속에 있던 것. 만든다 = 그걸 코드로 꺼내는 것.

**비전 정합**: 대시보드 = [[커리어 비전 (AI 안전 적용·검증)]]의 **human-in-the-loop 면**. 에이전트가 검증·나열, 최종 판단은 내가. (검증 + 사람 판단 = 내 비전 그대로)

**하드한 90% (이게 진짜 프로젝트)**: 구조화 추출 + 적대적 검증 + 출처 링크 + eval.

---

## 2. 아키텍처 개요

**3단 (느린 배치 / 빠른 렌더 분리)**
1. **소스**: 내 위키·GitHub(나=실제 스킬, ground truth) + 채용 데이터(회사/ATS·공공 API)
2. **매일 1회 에이전트 배치** (느림·비쌈·비결정): 검색 → fetch·검증(만료·403 거르기) → 스킬갭 매칭 → 가능성 랭킹 → 스키마 검증 → **검증된 JSON 산출**
3. **정적 웹 대시보드** (코드 고정, 데이터만 매일 갱신, 빠름·쌈, LLM 안 돎): 맞닿은 진로 / 스킬 갭 / 가능성순 / 마감일 / "어제 대비 +N new"

**핵심 분리 원칙**: LLM을 페이지 로드마다 돌리면 느리고·비싸고·매번 답 달라짐(비결정). → 느린 배치가 검증된 데이터 파일 하나 뱉고, 멍청하고 빠른 정적 렌더가 그것만 읽음.

**런타임 (개인용 v1 → 배포판)**
- **개인용 v1 = 로컬-퍼스트**: 수집기를 내 PC에서 → 내 IP·내 브라우저라 WAF·쿠키재사용·확장배포 문제 **전부 증발**. 로컬 스크립트가 `jobs.json` 뱉고 정적 HTML이 읽음. 호스팅 불필요.
- **배포판**: 어댑터·스키마 그대로, **런타임만 로컬→서버** + 티어B를 내브라우저→유저확장으로 교체. **지금 설계 하나도 안 버려짐.**

---

## 3. 데이터 수집 설계 (전체)

### 3.1 대원칙
- **무-스크래핑.** 원천(회사/ATS·공공 API)에서만.
- 🚫 **사람인·잡코리아·링크드인 DB 직접 크롤 금지** — 국내 판례(DB권 침해, 확정) + ToS(hiQ). 상세는 §11.
- **링크드인·사람인은 "거울"(하류 집계), 원본은 회사/ATS(상류).** 거울을 깨지 말고 원본을 봐라.
  - 링크드인 Basic Jobs(Limited Listings) = ① 파트너 ATS ② 제휴 잡보드 ③ **회사 career 사이트 직접 크롤(Job Wrapping)** 로 무료 ingest. Promoted(유료 Job Slots)는 별도.
  - 사람인·잡코리아 = **기업 직접 등록(유료 마켓플레이스)** 이 1차 소스. (걔들끼리 크롤 = 위법 판례)

### 3.2 4층 소스 (우선순위)
1. **ATS 공개 API** — Greenhouse/Lever/Ashby. 무인증·문서화. 최우선.
   - Greenhouse: `GET https://boards-api.greenhouse.io/v1/boards/{slug}/jobs?content=true`
   - Lever: `GET https://api.lever.co/v0/postings/{slug}?mode=json`
   - Ashby: 공개 피드(+`includeCompensation=true`)
2. **회사 내부 JSON API** — SPA 백엔드. DevTools Network 탭으로 발견. ToS상 대량수집만 주의(robots 위반 아님).
3. **공식 sitemap + SSR HTML / JSON-LD 파싱** — sitemap이 전체 공고 URL 열거, 상세는 서버렌더면 파싱.
4. **합법 집계 API** — 시장 신호용.
   - 워크넷 공공데이터: `https://www.data.go.kr/data/3038225/openapi.do` (무료·정부, ~1,000/day) — 가장 합법·공짜
   - 사람인 공식 API: `https://oapi.saramin.co.kr/` (승인제, 일 500회)
   - 원티드 OpenAPI: `https://openapi.wanted.jobs/` (key 발급)

### 3.3 어댑터 패턴 (수집 아키텍처)
- **JSON-LD 범용 파서 1개** = 준-범용(schema.org JobPosting 심은 회사 다 커버). 가성비 1위, 먼저 만들 것.
- **ATS 어댑터** = 플랫폼당 1개(Greenhouse/Lever/Ashby), 회사 N개 커버.
- **자체 어댑터** = 고가치 회사당 1개(네이버·우아한 등). 유지비 큼.
- **유저 제공** = 롱테일·WAF 막힌 곳.

### 3.4 공개 API 없는 회사 수집 사다리 (깨끗한 순 → 회색 순)
1. **내부 JSON API 훔쳐보기** — F12 → Network → Fetch/XHR 필터 → 새로고침 → 공고 목록 JSON 응답 찾아 URL·파라미터·헤더 복사.
2. **JSON-LD 뽑기** — 상세페이지 소스보기(Ctrl+U) → `<script type="application/ld+json">` JobPosting → JSON.parse. **범용**(파서 1개가 심은 회사 다 먹힘). 단 클라 렌더 SPA면 raw HTML에 안 보임(렌더 필요).
3. **Sitemap + SSR HTML 파싱** — sitemap.xml이 전체 URL, 상세 서버렌더 HTML 파싱. HTML이라 리디자인하면 깨짐(약함).
4. **RSS** — 있으면 개꿀(드묾). `/rss`·`/feed`.
5. **헤드리스 브라우저(Playwright)** — 클라 렌더 SPA만. WAF·ToS 회색. 개인용 한정.
6. **유저 직접(브라우저 확장/URL 붙이기)** — WAF 막힌 곳·롱테일. 유저가 자기 브라우저로 = 제일 깨끗.

### 3.5 A/B 티어 (갱신 성격)
| 티어 | 대상 | 개인판 | 배포판 | 갱신 |
|---|---|---|---|---|
| **A. 자동** | API·내부API·sitemap 회사 | 내 로컬 크론 | 서버 크론 | 매일 확실 |
| **B. 유저-보조** | WAF 막힘·유료 | 내 브라우저/로컬 | 브라우저 확장 | 베스트에포트(브라우저 열릴 때) |

- **티어 B의 확장은 "워크어라운드가 아니라 기능"**: 유저가 자기 관심 회사(WAF 포함)를 자기 브라우저에서 큐레이션 = human-in-the-loop 입력단 + 개인 데이터 해자.

### 3.6 국내 빅테크 10곳 확정 (실측)
| 회사 | 플랫폼 | 데이터 통로 (직접 검증) | 판정 |
|---|---|---|---|
| **쿠팡** | Greenhouse | `boards-api.greenhouse.io/v1/boards/coupang/jobs` → 공고 JSON | ✅ 공개 API |
| **당근** | Greenhouse | `boards-api.greenhouse.io/v1/boards/daangn/jobs` → 39건 JSON (+sitemap `/jobs/role/{id}`) | ✅ 공개 API |
| **네이버** | 자체 Java/Spring | 내부 JSON API `recruit.navercorp.com/rcrt/loadJobList.do`(annoId·제목·회사·기간·상세링크). robots 404(없음) | ✅ 열림(비공개 SPA 백엔드) |
| **네이버클라우드** | 자체(네이버 동일) | `recruit.navercloudcorp.com/rcrt/loadJobList.do`(동일 스키마·코드 재사용) | ✅ 열림 |
| **우아한/배민** | 자체 SPA(클라 렌더) | 내부 JSON API `career.woowahan.com/w1/recruits` → `{code,message,data:{totalSize:67,list:[{recruitSeq,recruitNumber,recruitName,careerType,jobGroup,employmentType,recruitOpenDate,recruitEndDate,keywords}]}}`. 상세 `/w1/recruits/{recruitSeq}/metadata`. 페이지네이션·필터(`?keyword=&category=jobGroupCodes:...`) 됨. 코드값(BA00...)은 라벨 매핑 필요 | ✅ 열림(무인증) |
| **토스** | 자체 FE(+Greenhouse 흔적) | 공식 `toss.im/career/sitemap.xml`(495+ URL) → 상세 SSR HTML 파싱. robots `/career/jobs` 명시 허용. (Greenhouse/Lever slug는 404) | 🟡 sitemap(가장 방어적 안전) |
| **넥슨** | 자체 | WAF **403**(서버 fetch 차단), 접근 API·피드 미발견. (넥슨 아메리카=Jobscore, 별개) | 🟠 유저 직접 |
| **라인** | 자체 SPA | 상세 SSR·Google색인이나 상세/api/sitemap **WAF 403**. robots는 2021_1st만 차단 | 🟠 유저 직접 |
| **카카오** | 자체 SPA(JS필수) | robots·전 페이지 **WAF 401** → Selenium만. 상세 `careers.kakao.com/jobs/S-{id}` | 🚫 유저 직접 |
| **무신사** | 그리팅 이전(구 Workday `musinsa.wd3` 폐기·302 outage) | 무인증 공개 API 없음. 그리팅 Open API=유료 키 필수 → 공개페이지+sitemap만 | 🟠 유저 직접 |

- **정리**: ✅ 자동 6곳(쿠팡·당근·네이버·네이버클라우드·우아한·토스) / 🟠 유저-보조 4곳(넥슨·라인·카카오·무신사). "국내 대기업은 유저 URL밖에 없다"던 첫 판단보다 크게 나아짐.

### 3.7 robots.txt 판정 (직접 fetch)
- **LinkedIn** 🔴: 최상단 "자동수단 접근 엄격 금지" 법적 경고 + `/jobs` 전 경로 비-링크드인봇 차단.
- **사람인** 🔴: GPTBot·Bytespider 전면차단, 대부분 `/recruit`·`/jobs/view` Disallow. (접속 자체도 거부당함)
- **잡코리아** 🟡: 일반봇은 공고 리스트 허용(`/recruit/joblist`), 근데 **ClaudeBot 이름 찍혀 제한**, CCBot·Diffbot 스크래퍼 전면차단 (2026-04-01 갱신).
- **그리팅(greetinghr)** 🟢: `User-agent: * / Allow: /`. 회사 career 페이지(`{회사}.career.greetinghr.com`)는 회사 본인 공개공고라 경쟁DB 크롤과 결이 다름(단 상세엔 anti-bot 403 있음, ToS 확인 권장).
- **Greenhouse** 🟢: `/embed/`만 Disallow, 공고 크롤 가능 + 공개 API.

### 3.8 국내 ATS 실태
- **그리팅(두들린)**: 국내 대표 채용솔루션. Open API 있으나 **고용주용·비즈플랜**(제3자 전체공고 API 아님). **MCP 연동 출시**(채용데이터→AI, 고용주측) = 생태계가 MCP/에이전트 방향으로 감(내 베팅 정합). 잡플래닛·프로그래머스·캐치·원티드(2025.7)로 원클릭 배포.
- **점핏**: 사람인 소유. **잡다(JOBDA)·리멤버**: 매칭·B2B, 제3자 공개 API 미발견.
- 결론: 국내 ATS는 다 고용주용 SaaS → 제3자가 국내 전체를 합법 API로 = 불가.

### 3.9 WAF (넥슨·카카오·라인 막힘) 상세
- **왜 막히나(403/401)**: User-Agent(비브라우저), IP 평판(데이터센터), TLS 지문(JA3), 브라우저 헤더/쿠키 없음, JS 챌린지. → 진짜 사람 브라우저는 통과, 자동화만 차단.
- **"한 번 들어가면 서버가 계속 긁기" = 안 됨**: WAF 통과권(`cf_clearance` 등)은 수명 짧고(수십분~몇시간) **IP+브라우저 지문에 바인딩** → 서버로 복사하면 거부. 넥슨은 로그인도 아니라(비로그인 열람) 재사용할 세션도 없음.
- **되는 것**: 확장이 **유저 브라우저 안에서** 대신 fetch(1회 설치+회사선택 → 브라우저 켜지면 백그라운드 갱신 → 서버 전송). WAF 통과 주체가 유저 브라우저(진짜 사람)라 합법.
- **Selenium**: 도구가 면죄부 아님(WAF 우회 = "차단 알고도 뚫음" = 최악 포지션). 개인·저볼륨·무재게시면 실질 리스크 낮으나 **clean은 아님**(ToS 위반은 개인용도 위반). 아키텍처가 이미 유저-직접으로 우회하므로 서버가 뚫을 필요 없음.

### 3.10 정직 레이어
- 공고마다 `{출처 URL, 수집시각, 확신도}` + "미확인" 필수 필드.
- 소스별 "마지막 갱신" 시각 표시(티어 B는 늦을 수 있음 명시).

---

## 4. 매칭 엔진 (개념 × 구현 2축)

### 4.1 두 소스의 비대칭 (설계 핵심)
- **GitHub = 넓이·최신성·"만졌다"** — 언어·프레임워크·뭘 코딩했나. 바이브코딩 함정: 코드 있다고 이해한 건 아님.
- **위키 = 깊이·이해·"판단했다"** — 트레이드오프·실측·회고.
- **위키로 GitHub를 채점**: GitHub 코드 있는데 위키 뒷받침 없으면 → surface(면접 리스크). 위키 뒷받침 있으면 → 개념 deep.

### 4.2 2축 등급 (⚠️ 중요 — 초기 설계 정정)
초기엔 단일 등급(deep/used/surface)이었는데, **위키 깊이를 "구현 실력"으로 착각하는 버그**가 있었다(승현 정정: 알고리즘도 개념 deep·코드 얕음). → **2축으로 분리**:
- **개념/판단 축** ← 위키에서. "왜 골랐나, 트레이드오프 설명되나."
- **구현/독립 축** ← 코드 + "AI 없이 다시 짤 수 있나". **기본값 보수적**(AI로 짰으면 못 올림).
- **위키가 아무리 깊어도 구현 축은 못 올린다.** (이게 정정의 핵심)
- 각 축의 등급: deep / used / surface.

### 4.3 파이프라인 5단계
1. **스킬 프로필 추출** ← 위키(개념) + GitHub(구현) → `{스킬, 증거링크[], 개념등급, 구현등급}`
2. **JD 요건 추출** ← 타겟 공고 → `{요건, 필수/우대, 하드게이트 여부}`
3. **요건별 매칭** → 각 JD 요건 ↔ 프로필 → `충족(증거)/부분/갭`. 단어 안 맞아도 의미로 매칭("낙관적 락·k6" ↔ JD "대용량 트래픽"), **단 증거로 재검증**.
4. **가능성 판정** → §7
5. **갭 → 우선순위 액션** → §8

### 4.4 설계로 막을 함정
| 함정 | 장치 |
|---|---|
| 환각(없는 스킬 있다고) | 모든 "충족"에 **증거 링크 필수**, 없으면 "미확인" |
| 바이브코딩(코드만·이해X) | **2축 depth-grading**(위키 뒷받침 없으면 구현 surface) |
| 스킬 단어 안 맞음 | 세만틱 매칭 + **증거로 재검증**(그럴듯한 매칭 방지) |
| 과대평가 | 적대적 체크 "이 증거로 이 요건 정말 충족?" |

---

## 5. 내 현재 프로필 (실측 — GitHub 16 repos × 위키)

### 5.1 GitHub 실체 (handle: seunghyeon-L)
- **실전 3개**: `ai-virtual-pet`(=OnTrack, Python 7.7MB) · `seat-reservation`(캡스톤, TS) · `algorithm-hospital`(병원 스케줄링, 497KB)
- **멋사 학습**: `2026_BE_Homework`·`2026_BE_TIL`·`besession0402`(과제·TIL·포크)
- **토이/포크/스터디**: `URL-shorter`·`exex`(QR)·`about-me`·`github-readme-stats`(포크)·`Computer-Networking...`(책 노트)·`school-meetingRoom`(회의실 예약, TS)
- **빈 껍데기**: `keyword-alarm-app` 1KB(교내상 수상작인데 코드 비공개), `ai-agent-challenge` 0KB

### 5.2 2축 판정 (승현 자가신고 반영 = eval 첫 ground truth)
| 영역 | 개념/판단 | 구현/독립 |
|---|---|---|
| 알고리즘·최적화(CP-SAT/OR) | 🟢 deep | 🟡 used (코드 잘 모름 — 본인) |
| AI 에이전트·LLM 통합 | 🟢 deep | 🟡 used (AI 구현) |
| 동시성·성능 | 🟢 deep | 🟡 used (캡스톤 AI코드) |
| 검증·방법론(시그니처) | 🟢 deep | (메타스킬) · **GitHub 증거 0** |
| 풀스택 Next.js/TS·Supabase | 🟡 중 | 🟡 used |
| Java/Spring | 🟡 중(학습단계) | 🟡 used(과제) |
| 네트워킹 | ⚪ used(지식) | — |
| Kotlin/모바일 | 🔴 surface(빈 repo) | 🔴 surface |
| 퀀트·데이터분석 | 🟢 위키 deep | ⚠️ GitHub 갭(로컬만) |
| K8s·서빙·배포·RAG실구현·분산실무 | ⚫ 없음(갭) | ⚫ 없음(갭) |

- **패턴**: 거의 전 축에서 **개념 ≫ 구현**.
- 스택: Java/Spring · Next.js/React · Python · Supabase · Kotlin · PyQt5 · k6

### 5.3 엔진이 잡은 메타 2가지
1. **개념 deep / 구현 used 분리** — 3대 프로젝트는 위키가 설계·이유를 깊게 문서화(deep)했으나, 본인이 "코드는 AI가 짬"이라 인정 → 면접 "왜 이 설계?"(강)/"이 줄 뭐야?"(리스크). = 바이브코딩 ②단계(AI 코드 읽고 설명)를 메꿔야 함.
2. **시그니처가 GitHub에 안 보임** — 최강 자산(검증·엄밀함·하네스)이 위키에만, GitHub엔 흔적 0 → 채용담당자가 GitHub만 보면 "멋사 과제+토이"로 보임(실제 깊이 안 드러남). = "검색되는 사람" 갭. 액션: OnTrack·병원·검증방법론을 README·블로그로 밖에서 검증가능하게.

---

## 6. 출력 설계 (2원형 + 스킬 바)

- **원형 그래프 2개**:
  - **① 개념·스펙 적합도** (구현 제외 — 내 이해·방향이 이 직군과 몇 % 맞나)
  - **② 구현·실전 적합도** (AI 없이 코드로 해낼 수 있나)
  - (이름 "흥미"는 예시였음 → "개념·스펙"으로 확정)
- 각 원형 = %(적합도) 표시. 아래에 **스킬별 개념/구현 바**: 연한 바=개념, 진한 바=구현, **틈 = "알지만 못 짜는 폭"**, 나머지=요구 갭.
- **승현 예시(목업)**: 개념 66% ≫ 구현 34%.
- **기준 직군 = 유저 선택·갈아끼우기.** 직군 바꾸면 오른쪽 "요구(100%)" 모양이 바뀜.
  - **요구 프로파일 출처 = JD 집계.** "이 직군 공고 N개가 요구하는 스킬·빈도·가중치" → 그 직군 요구셋. **수집 파트 ↔ 매칭 파트가 여기서 연결**(원래 강점 "JD 빈도 집계"가 여기 삶).
  - 흐름: [수집 → JD 요구 집계] → [직군 선택 시 요구셋 로드] → [내 개념/구현과 대조 → 2원형 재계산]
- ⚠️ 수치는 엔진이 계산: **위키(개념 증거) × GitHub·자가신고(구현) × JD(요구·가중치)**.

---

## 7. 가능성 랭킹 (A-1)

**원칙**: "합격률 73%" 가짜 확률 ❌ → **게이트 필터 + 분해 적합도.**

**3단계**
1. **하드 게이트 필터**(pass/fail, 점수 아님) — 경력연차·학위·신입채용 여부. 막히면 점수 안 매기고 "북극성" 버킷.
2. **게이트 통과분만 적합도 산출**(2축) — 개념 적합도% · 구현 적합도% · 갭(미충족 필수요건+난이도).
3. **버킷 + 정렬**:
   - 🟢 **바로 도전** (게이트 통과 + 적합도 높음)
   - 🟡 **갭 메우면 도전** (통과 + 갭 K개, "이거 메우면 열림")
   - 🔴 **북극성** (게이트 막힘, "왜 안 되나 + 뭘 채우면/언제 열림")

**표기 결정**: **티어(🟢🟡🔴) + 적합도 숫자.** 예: `🟡 갭 메우면 · 적합도 68 (개념82/구현41)`
- ⚠️ 그 숫자는 **"적합도(내가 얼마나 맞나)"지 "합격률"이 아님.** 합격확률은 못 알고(지어내면 함정), 적합도는 증거로 계산. **항상 분해(개념/구현/갭) 동반, 단독 % 금지.**

**정렬 결정**: 기본 = **가까운 순**(갭 작은 = 빨리 도달). 유저가 드롭다운으로 변경 가능: `적합도 높은 순 / 비전정합 순 / 마감 임박 / 신규(어제 대비)`.
- 설계 함의: 각 행이 정렬키 다 보유(적합도·갭크기·마감·신규·비전정합).

**승현 예시**: 🟢 백엔드/서버 신입(당근·우아한류) · 🟡 AI백엔드(갭 K8s·서빙·RAG) · 🔴 AI Quality(티맵)·FDE(CJ)=경력 게이트 → 1~2년 뒤.

---

## 8. 갭 → 우선순위 액션 (A-2)

정보 덤프를 행동으로 바꾸는 부분.

**1. 갭 모으기**: 타겟 직군들 요구 − 내것 = 갭 유니언.

**2. 우선순위 점수** = `(JD빈도 × 게이트성 × 비전정합) ÷ 학습거리`
- **JD빈도**: 타겟 공고 몇 %가 요구 (강점 "빈도 집계")
- **게이트성**: 이거 하나로 막힌 공고 여러 개가 열리나 (고영향)
- **비전정합**: [[커리어 비전 (AI 안전 적용·검증)|내 방향]]과 맞나
- **학습거리**: 없음 > surface > used (가까우면 quick win)

**결정 (승현이 "너 생각대로" 위임 → 확정)**: **취업 현실(JD빈도 × 게이트성) 우선, 비전정합 = 부스터/타이브레이커.**
- 이유: 게이트 못 넘으면 비전도 의미 없음 → 현실이 주동력. 근데 우선순위 비슷한 갭끼리는 비전 쪽으로 기울여 = 같은 노력이 generic 커모디티 대신 **내 해자(검증·AI)로 복리** + 스토리 일관성.
- 순수 비전-우선 = "멋지지만 안 뽑히는" 위험 / 순수 빈도-우선 = "뽑히지만 남과 똑같은" 위험. → 현실을 축으로, 비전으로 기울임.
- **구현 축은 이 계산 위에 고정 #1** (개념≫구현이 구조적 게이트라 직군 불문 최상단).

**3. 각 갭 → 액션 → 증명법**: "K8s — 타겟 4/5 요구·넌 0·~3주 → 작은 배포 1개 + 블로그"(학습을 "검색되는 증거"로).

**승현 갭 우선순위 (규칙 적용 예시)**
1. 🔒 **구현 축**(고정 #1) — 코테 꾸준히 + AI 최소로 프로젝트 하나 재구현
2. **K8s·Docker·배포** — 고빈도 + 게이트성 큼(여러 공고 동시에 열림)
3. **RAG 실구현 / 모델 서빙(vLLM)** — 고빈도 + **비전정합 부스트**로 K8s 바로 뒤
4. **분산 실무(멱등성·Kafka)** — 비전정합(신뢰성) + 중빈도

**합치면 = 행동 전환**: 랭킹이 *어디로*(지금 도전 vs 북극성), 갭→액션이 *다음 뭐*를 짚음. "정보 50개" ❌ → "당근 백엔드 지금 도전, 구현축·K8s부터, 이렇게 증명" ✅. 이게 GPT한테 묻는 것보다 나은 이유 그 자체.

---

## 9. eval & 정직성 레이어

- **골든 eval 셋 시드 = 이 대화 전체** (LLMOps 판정·실측 공고·정직성 플래그·만료공고 거르기·2축 등급 자가신고).
- **매일 산출물(JSON)이 곧 eval 대상**: "오늘 run이 만료 걸렀나 / 안 지어냈나 / 갭 정확한가."
- **자가신고 교정 루프**: 승현이 준 "다 used" 정정이 엔진의 "위키=deep" 순진함을 2축·구현보수적으로 교정 → 이 루프가 엔진을 정직하게 만듦(딴 도구엔 없음).
- **랭킹 eval(장기)**: "가능성 높다 했는데 떨어짐" = 교정 데이터. 유저 지원 결과 피드백.
- 정직 라벨: 모든 주장에 출처·수집시각·확신도, "미확인" 필수.

---

## 10. 남은 것 (미설계)

- **대시보드 전체 화면 구성** — 직군 선택 + 2원형 + 랭킹 리스트 + 갭 액션 + 마감·델타를 한 화면에.
- **내 프로필 파싱 상세** — 위키(md 파일들) → 개념 추출 로직 / GitHub API → 구현 추출 로직. (스킬 온톨로지/정규화 포함)
- **JD 요건 추출·요구 프로파일 집계** — 직군별 요구셋 만드는 파이프라인 상세.
- **eval 골든셋 정식 구축** — 이 대화를 케이스로 구조화.
- **배포판 전환** — 서버 크론 + 브라우저 확장.
- **코드값 매핑** — 우아한 `careerType/jobGroup/employmentType`(BA00...) 등 라벨 테이블.

---

## 11. 부록

### 11.1 검증된 엔드포인트 (직접 fetch/호출 확인)
```
# ATS 공개 API (무인증)
GET https://boards-api.greenhouse.io/v1/boards/{slug}/jobs?content=true   # 쿠팡=coupang, 당근=daangn
GET https://api.lever.co/v0/postings/{slug}?mode=json

# 회사 내부 JSON API (무인증, SPA 백엔드)
GET https://recruit.navercorp.com/rcrt/loadJobList.do            # 네이버 (list[]: annoId,제목,회사,기간,상세링크)
GET https://recruit.navercloudcorp.com/rcrt/loadJobList.do       # 네이버클라우드 (동일 스키마)
GET https://career.woowahan.com/w1/recruits                      # 우아한 (67건, list[]: recruitSeq,recruitName,careerType...)
GET https://career.woowahan.com/w1/recruits/{recruitSeq}/metadata # 우아한 상세

# 공식 sitemap
https://toss.im/career/sitemap.xml                               # 토스 (495+ 공고 URL) → 상세 SSR HTML 파싱

# 합법 집계 API
https://www.data.go.kr/data/3038225/openapi.do                   # 워크넷 공공(무료, ~1000/day)
https://oapi.saramin.co.kr/                                       # 사람인(승인, 500/day)
https://openapi.wanted.jobs/                                      # 원티드(key)

# 막힘 (유저-직접 필요)
넥슨 careers.nexon.com (WAF 403) · 라인 careers.linecorp.com (WAF 403) · 카카오 careers.kakao.com (WAF 401) · 무신사 www.musinsacareers.com (그리팅 유료키)
```

### 11.2 법적 근거
- **잡코리아 v 사람인** (서울고법 2016나2019365, 2017.4.6): 사람인이 잡코리아 HTML 무단 복제·게재 = **DB제작자 권리 침해**. **총 4.5억(간접강제금 2억 + 배상 2.5억)** + 공고 396건 폐기. 대법 2017.9.27 심리불속행 기각 확정. → 경쟁 채용DB 크롤 = 위법 확정.
- **hiQ v LinkedIn** (미): 공개 데이터 스크래핑이 CFAA 위반은 아님(9th Cir 2022)이나 **ToS 계약 위반은 성립**, hiQ 영구금지·데이터삭제·$50만 합의(2022.12). → 공개 데이터라도 ToS 스크래핑 금지 유효.
- **인디드**: Publisher/Job Search API 폐기(2023, 신규 불가).
- **Google for Jobs**: 결과 pull API 없음(2021 종료). JSON-LD는 회사가 자기 페이지에 게시하는 공개 표준 → 직접 읽는 건 정당.

### 11.3 정정 기록 (verify-the-verifier — 내가 세션 중 틀렸다 고친 것)
- **판례 금액**: "4.5억"→"2.5억"으로 정정했다가 재확인 → **총 4.5억(간접강제 2억 + 배상 2.5억)** 이 맞음. 4.5억=총액, 2.5억=배상분만.
- **"직접 제공분 합법 예외"**: 여러 번 언급했으나 **판결문 근거 없음**. 그건 일반 크롤링 법리(대상 사이트 동의·robots·ToS)에서 나오는 원칙이지 이 판례의 판시가 아님. 결론(회사 본인 robots-허용 공개페이지 읽기가 안전)은 유효하나 근거는 "robots 허용 + 경쟁DB 복제 아님".
- **"ATS" 용어 혼동**: ①공개 ATS 데이터 API(국내 드묾) vs ②회사 공고 원천 자체(항상 존재)를 뭉갬. 원천은 늘 있으나 접근 난이도가 clean-API/공개페이지/비공개내부API로 갈림.
- **쿠팡 Workday**: 쿠팡은 Greenhouse지 Workday 아님. Workday는 무신사였고 그것도 폐기됨.
- **스킬 등급 과대평가**: 단일 등급이 "위키 깊이=deep"으로 구현을 부풀림 → 승현 정정으로 2축(개념/구현)·구현 보수적으로 교정.

### 11.4 관련 위키 페이지
- [[커리어 비전 (AI 안전 적용·검증)]] · [[흥미 신호와 직군 발산 (만드는 사람)]] · [[신입 취업 실행 전략 (증거·면접·검색되는 사람)]] · [[바이브코딩 시대의 실력 기준 (도메인 vs 코딩)]] · [[분산 시스템 (실패가 정상인 시스템)]] · [[개발자 취업 시장과 커리어 전략]] · [[검증의 함정 (도구 기본값과 자기검증)]] · [[근거 검증과 1차 출처 원칙]] · [[하네스 엔지니어링]]
