# 🎰 DropCast 2주차 주간 계획 및 GitHub Issues 등록 대장 (주말 포함 풀 스택 확장 버전)

토요일과 일요일 개발 리소스까지 전면 가용함을 반영하여, 단순 투표 기능을 넘어 **실시간 웹소켓 연동, 주간 크론 자동 정산 엔진, 포디움 랭킹 보드 및 어드민 제어 시스템**까지 갖춘 완전한 풀 사이클 예측 대시보드로 개발 볼륨을 대폭 확장한 계획표입니다.

---

## 📅 요일별 개발 계획 (Weekly Schedule)

* **월요일 (오늘)**: 계획 수립 및 전용 Agent 구축, 개발 환경 셋업(MySQL 연결 완료)
* **화요일**: **MySQL 테이블 생성 및 크림/스니커덩크 크롤러 구현을 통한 실데이터 적재 완료**
* **수요일**: 백엔드 REST API 개발 완료 (`GET /api/drops`, `POST /api/votes` - 중복 방지 & 락인 검증)
* **목요일**: **[Socket.io] 실시간 웹소켓 서버 구축 및 신규 아이템 스폰/투표 상태 브로드캐스트 구현**
* **금요일**: **[React FE] API 및 웹소켓 연동 및 0.3초 탄성 물리 팽창 스폰 애니메이션 구현**
* **토요일**: **[정산 엔진] `node-cron` 주간 자동 정산 배칭 및 크롤링 실패 대비 수동 가격 덤프 어드민 페이지 개발**
* **일요일**: **[통합 테스트] Verification Agent를 동원한 E2E 시스템 종합 검증 및 `canvas-confetti` 랭킹 리워드 정산 웰컴 모달 최종 검수**

---

## 🎫 GitHub Issues 목록

### [Issue #1] [P0] [월요일] 개발 전용 Agent (계획 수립 및 검증 Skill) 환경 구축
* **설명**: 프로젝트 협업 효율 및 요구사항 무결성 검증을 위해 `skills/` 하위에 Planning Agent 및 Verification Agent의 구체적인 마크다운 워크플로우를 구성합니다.
* **할 일 (TODO)**:
  - [x] `skills/planning-agent/SKILL.md` 생성 및 YAML frontmatter 지정
  - [x] `skills/verification-agent/SKILL.md` 생성 및 YAML frontmatter 지정
* **완료 기준**:
  - `skills/planning-agent/SKILL.md`와 `skills/verification-agent/SKILL.md` 파일이 존재하고 정상 로드됨.

### [Issue #2] [P0] [월요일/화요일] MySQL 데이터베이스 연결 환경 셋업 및 Prisma 마이그레이션
* **설명**: 로컬 MySQL 서버와 백엔드 Express를 연결하고, 데이터베이스 `dropcast`에 Prisma 스키마 테이블 구조를 마이그레이션합니다.
* **할 일 (TODO)**:
  - [ ] 로컬 MySQL root 비밀번호를 확인하여 `backend/.env`의 `DATABASE_URL` 설정 완료
  - [ ] `backend/prisma/schema.prisma` 파일의 provider를 `"mysql"`로 수정
  - [ ] Prisma 마이그레이션 커맨드(`npx prisma db push`)를 정상 수행하여 테이블 생성 완료
* **완료 기준**:
  - MySQL 데이터베이스에 `User`, `Drop`, `Vote` 테이블이 정상 생성됨.

### [Issue #3] [P0] [화요일] [BE] 크림/스니커덩크 실제 가격 정보 수집 크롤러 및 데이터 시딩
* **설명**: `Puppeteer`를 활용하여 크림(KREAM) 또는 스니커덩크에서 대표적인 인기 스니커즈/카드 상품들의 실시간 가격 정보를 가져와서 MySQL `Drop` 테이블에 실제 데이터를 적재합니다.
* **할 일 (TODO)**:
  - [ ] `backend/src/crawler/crawler.js`에 Puppeteer 기반 마켓 가격 크롤러 모듈 개발
  - [ ] 크롤링된 실시간 시세 데이터를 바탕으로 `Drop` 레코드들을 데이터베이스에 자동 Seeding하는 배치 스크립트 작성
  - [ ] 크롤링 차단 대책: 크롤러 실패 시를 대비하여 수동으로 데이터를 직접 덤프하고 세팅할 수 있는 Fallback 데이터 인서트 스크립트/API 구현
* **완료 기준**:
  - 크롤링 스크립트 작동 후 MySQL `Drop` 테이블에 크림/스니커덩크 기반의 실시간 시세와 정가가 입력된 실제 아이템 데이터(최소 4개 이상)가 확인됨.

### [Issue #4] [P0] [수요일] [BE] 투표 제출 및 드롭 조회 API 라우트 구현
* **설명**: 수직 슬라이스의 서버 사이드 처리를 위해 투표 데이터를 MySQL에 기록하는 API(`POST /api/votes`)와 드롭 목록 조회 API(`GET /api/drops`)를 구현합니다.
* **할 일 (TODO)**:
  - [ ] `GET /api/drops`: 전체 드롭 목록을 조회하며, 상승/하락 투표 수를 세어 `consensusPrice` 및 `bullish` 비율을 계산해 반환하는 로직 구현.
  - [ ] `POST /api/votes`: 유저의 투표를 받아 `Vote` 테이블에 적재하는 로직 구현.
  - [ ] 중복 투표 방지: 동일 유저가 동일 드롭에 중복 투표 시 `400 Bad Request` 에러 처리.
  - [ ] 데드라인 락인: 매주 금요일 오전 9시 이후의 투표 제출 요청 시 `403 Forbidden` 에러 처리.
* **완료 기준**:
  - `/api/drops` 조회 시 계산된 대중 합의 가격과 상승률이 올바르게 JSON으로 리턴됨.
  - `/api/votes`를 통해 중복 투표 및 금요일 9시 이후 투표 제한 검증 코드 작동 확인.

### [Issue #5] [P0] [목요일] [BE] Socket.io 실시간 소켓 서버 개발
* **설명**: 사용자들이 실시간으로 투표 현황과 신규 아이템 스폰 연출을 모든 클라이언트 화면에서 동시에 관찰할 수 있도록 백엔드 웹소켓 인프라를 연동합니다.
* **할 일 (TODO)**:
  - [ ] `backend/src/socket.js`에 Socket.io 라이브러리 연동 및 Express 서버 포트 결합
  - [ ] 신규 아이템 검색/등록 시 데이터베이스 저장과 동시에 전체 클라이언트에 `item:spawn` 브로드캐스트 이벤트 구현
  - [ ] 유저가 투표 성공 시 실시간 상승률과 예상가를 전체 소켓 세션에 브로드캐스트 전송
* **완료 기준**:
  - 소켓 통신을 통한 브로드캐스트 페이로드가 정상적으로 주고받아지는지 콘솔 로그로 확인.

### [Issue #6] [P0] [금요일] [FE] REST API & WebSocket 클라이언트 통합 연동 및 탄성 스폰 연출
* **설명**: 리액트 프론트엔드와 백엔드의 REST API 및 Socket.io 인터페이스를 최종 결합하고, August* 디자인 규격에 맞게 버블이 튕기며 커지는 애니메이션을 입힙니다.
* **할 일 (TODO)**:
  - [ ] `App.tsx` 내에 `socket.io-client` 훅을 셋업해 실시간 이벤트 수신 대기
  - [ ] 신규 아이템 추가 이벤트 수신 시 화면 중앙에 scale 0에서 타겟 크기까지 0.3초간 elastic bounce 팽창하는 CSS 효과 바인딩
  - [ ] 투표 수치가 소켓을 통해 날아왔을 때 state를 즉각 업데이트하여 새로고침 없이 연동
* **완료 기준**:
  - 2개의 브라우저를 나란히 열고 한쪽에서 신규 아이템을 검색/추가하거나 투표했을 때, 다른 쪽 화면에서도 즉시 튕기며 생성되고 그래프가 동기화되는 E2E 동작 완성.

### [Issue #7] [P0] [토요일] [BE] 일요일 밤 23:59 자동 정산 스케줄러 및 수동 가격 덤프 어드민 페이지 개발
* **설명**: `node-cron` 배치 정산 엔진을 탑재하고, 크롤링 실패를 대비해 시세를 수동으로 적어넣을 수 있는 관리자 화면을 구성합니다.
* **할 일 (TODO)**:
  - [ ] `node-cron`으로 매주 일요일 23:59:00에 Puppeteer 크롤링을 동작시켜 실제 종가를 가져와 예측 유저의 포인트와 정확도를 가감하는 배치 비즈니스 로직 작성
  - [ ] 크롤링 실패 시 관리자가 웹 화면에서 직접 종가를 입력하고 강제로 정산 배치를 돌릴 수 있는 수동 가격 덤프 어드민 뷰 & API(`POST /api/admin/batch-trigger`) 개발
* **완료 기준**:
  - 어드민 화면에서 강제 정산 API 호출 시 데이터베이스의 유저 포인트(`points`)와 정확도(`accuracyRate`) 데이터가 맞춘 결과에 비례해 일괄 업데이트됨.

### [Issue #8] [P1] [일요일] 정산 결과 브리핑 팝업 모달 & canvas-confetti 연출 및 종합 UI/UX QA
* **설명**: 첫 진입 시 정산이 완료된 랭킹 정보와 개인 결과를 브리핑하는 Scene 1 모달을 활성화하고, 상위 랭커 축하 화려한 폭죽 효과를 장식하여 E2E 1사이클 전체 연동을 최종 완료합니다.
* **할 일 (TODO)**:
  - [ ] `App.tsx` 로딩 시 정산 성공 여부를 감지해 웰컴 브리핑 모달(`localStorage`로 24시간 미노출 제어) 활성화
  - [ ] 상위 3위(🥇🥈🥉)에 위치한 랭커들의 정보를 보여주는 웰컴 브리핑 렌더링 시 `canvas-confetti` 모듈을 실행해 폭죽 스프레드 이펙트 연출
  - [ ] **Verification Agent**를 동원해 August* 스타일 규칙(철저한 소문자 lowercase, 알약 형태 외에 둥근 모서리 배제 규정 등) 최종 교정 및 E2E 기능 동작 서약
* **완료 기준**:
  - 데이터 정산 후 서비스 재접속 시 정산 결과 브리핑 모달과 화려한 폭죽 효과가 정상적으로 작동하며, 전체 웹서비스의 데이터/디자인 무결성이 확보됨.
