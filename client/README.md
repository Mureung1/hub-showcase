# Client Starter

React + TypeScript + Vite client for early project exploration.

```bash
npm run dev -w client
npm run build -w client
npm run typecheck -w client
```

The Vite dev server proxies `/api` requests to the Express server at `http://localhost:3000`. Keep this layer minimal until the project direction is decided.
