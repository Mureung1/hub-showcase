# client/.env 로 복사해서 사용 (실제 .env는 커밋 금지)
#
# 서버 오리진. 비워두면 상대경로 '/api'를 쓰고, 로컬에서는 vite proxy가 4000으로 전달한다.
# 로컬 개발에서는 설정할 필요가 없다.
#
# 배포(Vercel)에서는 반드시 채운다 — 정적 호스팅이라 프록시가 없어 /api가 404가 된다.
# 끝에 /api를 붙이지 않는다(코드가 붙인다). 예: https://hub-api.onrender.com
VITE_API_BASE_URL=
