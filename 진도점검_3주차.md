# 진도 점검 — 3주차 시작 시점

## 수직 슬라이스 현황

화면(FE) → 서버(BE) → DB가 한 바퀴 도는지 점검했습니다.

| 계층 | 상태 | 설명 |
|---|---|---|
| 화면 (FE) | 완료 | React로 목록 → 상세 화면 흐름 완성 (목 데이터 기반) |
| 서버 (BE) | 부분 | 기존 리뷰 분석 API는 있으나, 주변 검색 API는 아직 없음 |
| DB | 미연결 | 화면과 DB가 실제로 연결되지 않음. 목 데이터로만 동작 |

**결론: 수직 슬라이스가 아직 한 바퀴 돌지 않았습니다.**

화면은 완성됐지만, "화면에서 입력 → 서버가 처리 → DB에 저장 → 다시 화면으로"라는 한 사이클이 실제 데이터로 이어지지 않은 상태입니다.

## 이번 주 1순위

미션 지침에 따라, 이번 주 1순위는 **기능 강화가 아니라 수직 슬라이스 완성**입니다.

즉, 목 데이터를 걷어내고 "내 가게명 입력 → 네이버 API로 주변 검색 → DB 저장 → 조회"가 실제로 한 바퀴 도는 것을 최우선으로 합니다. 기능 강화(반경 조절 등)는 이 사이클이 완성된 후에 진행합니다.

## 오늘 한 일

1. 주간 계획 수립 및 GitHub 이슈 8개 등록
2. 진도 점검 (이 문서)
3. 개발 착수: 거리 계산 함수(Haversine)를 TDD로 구현
   - DB·API 없이 순수 계산이라 오늘 바로 가능
   - 이번 주 TDD 요구사항도 미리 하나 충족

## 내일 계획

- 내 가게 좌표 조회 API (이슈 2)
- 주변 경쟁업체 검색에 오늘 만든 거리 함수 연결 (이슈 3)
- DB 테이블 생성 및 저장 (이슈 1, 4)
